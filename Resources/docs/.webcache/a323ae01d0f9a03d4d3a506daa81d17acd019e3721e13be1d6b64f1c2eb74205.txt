[std](https://doc.rust-lang.org/std/index.html)::[random](https://doc.rust-lang.org/std/random/index.html)

# Trait Rng Copy item path

[Source](https://doc.rust-lang.org/src/core/random.rs.html#7)

``` rust
pub trait Rng {
    // Required method
    fn fill_bytes(&mut self, bytes: &mut [u8]);
}
```

🔬This is a nightly-only experimental API. (`random` [\#130703](https://github.com/rust-lang/rust/issues/130703))

Expand description

A source of randomness.

## Required Methods

[Source](https://doc.rust-lang.org/src/core/random.rs.html#14)

🔬This is a nightly-only experimental API. (`random` [\#130703](https://github.com/rust-lang/rust/issues/130703))

Fills `bytes` with random bytes.

Note that calling `fill_bytes` multiple times is not equivalent to calling `fill_bytes` once with a larger buffer. An `Rng` is allowed to return different bytes for those two cases. For instance, this allows an `Rng` to generate a word at a time and throw part of it away if not needed.

## Dyn Compatibility

This trait **is** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

[Source](https://doc.rust-lang.org/src/std/random.rs.html#68-72)