[std](https://doc.rust-lang.org/std/index.html)::[field](https://doc.rust-lang.org/std/field/index.html)

# Macro field_of Copy item path

[Source](https://doc.rust-lang.org/src/core/field.rs.html#135)

``` rust
pub macro field_of($Container:ty, $($fields:expr)+ $(,)?) {
    ...
}
```

🔬This is a nightly-only experimental API. (`field_projections` [\#145383](https://github.com/rust-lang/rust/issues/145383))

Expand description

Expands to the field representing type of the given field.

The container type may be a tuple, `struct`, `union` or `enum`. In the case of an enum, the variant must also be specified. Only a single field is supported.