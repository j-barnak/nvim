# miasm.arch.msp430.sem

## Classes

### class `Lifter_MSP430(Lifter)`

- **`__init__(self, loc_db)`**
- **`mod_pc(self, instr, instr_ir, extra_ir)`**
- **`get_ir(self, instr)`**
- **`mod_sr(self, instr, instr_ir, extra_ir)`**

## Functions

### `hex2bcd(val)`

Return val as BCD

### `bcd2hex(val)`

Return the hex value of a BCD

### `reset_sr_res()`

### `update_flag_cf_inv_zf(a)`

### `update_flag_zf_eq(a, b)`

### `update_flag_zf(a)`

### `update_flag_nf(arg)`

### `update_flag_add_cf(op1, op2, res)`

Compute cf in @res = @op1 + @op2

### `update_flag_add_of(op1, op2, res)`

Compute of in @res = @op1 + @op2

### `update_flag_sub_cf(op1, op2, res)`

Compote CF in @op1 - @op2

### `update_flag_sub_of(op1, op2, res)`

Compote OF in @res = @op1 - @op2

### `update_flag_arith_sub_zn(arg1, arg2)`

Compute znp flags for (arg1 - arg2)

### `update_flag_arith_add_zn(arg1, arg2)`

Compute zf and nf flags for (arg1 + arg2)

### `mng_autoinc(a, b, size)`

### `mov_b(ir, instr, a, b)`

### `mov_w(ir, instr, a, b)`

### `and_b(ir, instr, a, b)`

### `and_w(ir, instr, a, b)`

### `bic_b(ir, instr, a, b)`

### `bic_w(ir, instr, a, b)`

### `bis_w(ir, instr, a, b)`

### `bit_w(ir, instr, a, b)`

### `sub_w(ir, instr, a, b)`

### `add_b(ir, instr, a, b)`

### `add_w(ir, instr, a, b)`

### `dadd_w(ir, instr, a, b)`

### `xor_w(ir, instr, a, b)`

### `push_w(ir, instr, a)`

### `call(ir, instr, a)`

### `swpb(ir, instr, a)`

### `cmp_w(ir, instr, a, b)`

### `cmp_b(ir, instr, a, b)`

### `jz(ir, instr, a)`

### `jnz(ir, instr, a)`

### `jl(ir, instr, a)`

### `jc(ir, instr, a)`

### `jnc(ir, instr, a)`

### `jge(ir, instr, a)`

### `jmp(ir, instr, a)`

### `rrc_w(ir, instr, a)`

### `rra_w(ir, instr, a)`

### `sxt(ir, instr, a)`

### `ComposeExprAssign(dst, src)`
