The `drwrap` DynamoRIO Extension provides function wrapping and replacing support.

- [Setup](https://dynamorio.org/page_drwrap.html#sec_drwrap_setup)
- [Event Replacement](https://dynamorio.org/page_drwrap.html#sec_drwrap_events)
- [API](https://dynamorio.org/page_drwrap.html#sec_drwrap_api)
- [Performance](https://dynamorio.org/page_drwrap.html#sec_drwrap_perf)
- [LGPL 2.1 License](https://dynamorio.org/page_drwrap.html#sec_drwrap_license)

#  Setup

To use `drwrap` with your client simply include this line in your client's `CMakeLists.txt` file:

use_DynamoRIO_extension(clientname drwrap)

That will automatically set up the include path and library dependence.

Initialize and clean up `drwrap` by calling [drwrap_init()](https://dynamorio.org/group__drwrap.html#ga23837833101ebc340ac7de6e8340bb4a) and [drwrap_exit()](https://dynamorio.org/group__drwrap.html#ga0e58babc53301c4ade4027dc1e09a088).

#  Event Replacement

`drwrap` uses the `drmgr` Extension to ensure its events occur at the proper order. A user of `drwrap` must use the `drmgr` versions of the basic block and thread events.

#  API

The first step in replacing or wrapping is to determine the address of the target application function. For functions exported by an application library, use [dr_get_proc_address()](https://dynamorio.org/dr__modules_8h.html#acb571b80307fd90538cb006725ab6183) to locate the entry point. For internal functions, use the drsyms Extension (see [Symbol Access Library](https://dynamorio.org/page_drsyms.html)).

Function replacing is provided by [drwrap_replace()](https://dynamorio.org/group__drwrap.html#ga488a6566cd760a3919bdd2f49a6d672f). The replacement function executes as application code and will be passed to the client via the basic block and trace events just like any other application code. The replaced function may still show up in the basic block event as a jump instruction; none of its actual code will execute. To avoid changing application behavior, ensure that the replacement function mirrors the calling convention and other semantics of the original function.

Function wrapping is provided by [drwrap_wrap()](https://dynamorio.org/group__drwrap.html#ga11d9a64732099e298a35dd414c400667). A pre-function and/or post-function callback must be provided. The pre-function callback is invoked prior to every execution of the target function. The callback can examine and change function arguments and can skip the call to the target function (in which case there will be no post-function callback). The post-function callback can examine and change the target function's return value. Information (including copies of the arguments for examination in the post-function) can be passed between the pre- and post-functions via a user-controlled parameter.

Multiple wrap requests are allowed for one target function. Their callbacks are called sequentially in the reverse order of registration. If any pre-function callback asks to skip the function, the remaining pre-function callbacks will not be called, nor will any post-function callback.

The pre-function callback occurs at the beginning of the wrapped function, i.e., it executes after the `call` instruction but before any instructions within the body of the wrapped function. The post-function callback occurs after the wrapped function returns, as if inserted just after the call instruction.

#  Performance

For the lowest overhead, if your use case is compatible with the (relatively minor) limitations imposed by the two flags [DRWRAP_NO_FRILLS](https://dynamorio.org/group__drwrap.html#ggab82c71baab4fe0c5f9962383818b9c92aeb299993e17993cb86912f93391f6b86) and [DRWRAP_FAST_CLEANCALLS](https://dynamorio.org/group__drwrap.html#ggab82c71baab4fe0c5f9962383818b9c92add150e3e7ba4ee203a045b936f872b4b), we recommend setting them both.

A major cause of overhead is flushing when a post-call point is dynamically discovered and it is already present in the code cache. The [drwrap_get_stats()](https://dynamorio.org/group__drwrap.html#gad10811a375941191e1f6aee228fd3583) interface can be used to measure the number of flushes triggered by drwrap.

#  LGPL 2.1 License

The `drwrap` Extension is licensed under the LGPL 2.1 License and NOT the BSD license used for the rest of DynamoRIO.