[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_expose_provenance Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#681)

``` rust
pub unsafe fn simd_expose_provenance<T, U>(ptr: T) -> U
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Exposes a vector of pointers as a vector of addresses.

`T` must be a vector of pointers.

`U` must be a vector of `usize` with the same length as `T`.