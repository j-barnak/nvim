[std](https://doc.rust-lang.org/std/index.html)::[thread](https://doc.rust-lang.org/std/thread/index.html)

# Function current Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/thread/current.rs.html#276-286)

``` rust
pub fn current() -> Thread
```

Expand description

Gets a handle to the thread that invokes it.

## Examples

Getting a handle to the current thread with `thread::current()`:

``` rust
use std::thread;

let handler = thread::Builder::new()
    .name("named thread".into())
    .spawn(|| {
        let handle = thread::current();
        assert_eq!(handle.name(), Some("named thread"));
    })
    .unwrap();

handler.join().unwrap();
```