[std](https://doc.rust-lang.org/std/index.html)::[io](https://doc.rust-lang.org/std/io/index.html)

# Struct Error Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/core/io/error.rs.html#86)

``` rust
pub struct Error { /* private fields */ }
```

Expand description

The error type for I/O operations of the [`Read`](https://doc.rust-lang.org/std/io/trait.Read.html), [`Write`](https://doc.rust-lang.org/std/io/trait.Write.html), [`Seek`](https://doc.rust-lang.org/std/io/trait.Seek.html), and associated traits.

Errors mostly originate from the underlying OS, but custom instances of `Error` can be created with crafted error messages and a particular value of [`ErrorKind`](https://doc.rust-lang.org/std/io/enum.ErrorKind.html "enum std::io::ErrorKind").

## Implementations

[Source](https://doc.rust-lang.org/src/std/io/error.rs.html#17-86)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/error.rs.html#43-45)

Returns an error representing the last OS error which occurred.

This function reads the value of `errno` for the target platform (e.g. `GetLastError` on Windows) and will return a corresponding instance of [`Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error") for the error code.

This should be called immediately after a call to a platform function, otherwise the state of the error value is indeterminate. In particular, other standard library functions may call platform functions that may (or may not) reset the error value even if they succeed.

##### Examples

``` rust
use std::io::Error;

let os_error = Error::last_os_error();
println!("last OS error: {os_error:?}");
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/error.rs.html#76-85)

Creates a new instance of an [`Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error") from a particular OS error code.

##### Examples

On Linux:

``` rust
use std::io;

let error = io::Error::from_raw_os_error(22);
assert_eq!(error.kind(), io::ErrorKind::InvalidInput);
```

On Windows:

``` rust
use std::io;

let error = io::Error::from_raw_os_error(10022);
assert_eq!(error.kind(), io::ErrorKind::InvalidInput);
```

[Source](https://doc.rust-lang.org/src/core/io/error.rs.html#222)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/io/error.rs.html#319)

Returns the OS error that this error represents (if any).

If this [`Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error") was constructed via [`last_os_error`](https://doc.rust-lang.org/std/io/struct.Error.html#method.last_os_error) or [`from_raw_os_error`](https://doc.rust-lang.org/std/io/struct.Error.html#method.from_raw_os_error), then this function will return [`Some`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.Some "variant std::option::Option::Some"), otherwise it will return [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None").

##### Examples

``` rust
use std::io::{Error, ErrorKind};

fn print_os_error(err: &Error) {
    if let Some(raw_os_err) = err.raw_os_error() {
        println!("raw OS error: {raw_os_err:?}");
    } else {
        println!("Not an OS error");
    }
}

fn main() {
    // Will print "raw OS error: ...".
    print_os_error(&Error::last_os_error());
    // Will print "Not an OS error".
    print_os_error(&Error::new(ErrorKind::Other, "oh no!"));
}
```

1.3.0 · [Source](https://doc.rust-lang.org/src/core/io/error.rs.html#358)

Returns a reference to the inner error wrapped by this error (if any).

If this [`Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error") was constructed via [`new`](https://doc.rust-lang.org/alloc/io/struct.Error.html#method.new) then this function will return [`Some`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.Some "variant std::option::Option::Some"), otherwise it will return [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None").

##### Examples

``` rust
use std::io::{Error, ErrorKind};

fn print_error(err: &Error) {
    if let Some(inner_err) = err.get_ref() {
        println!("Inner error: {inner_err:?}");
    } else {
        println!("No inner error");
    }
}

fn main() {
    // Will print "No inner error".
    print_error(&Error::last_os_error());
    // Will print "Inner error: ...".
    print_error(&Error::new(ErrorKind::Other, "oh no!"));
}
```

1.3.0 · [Source](https://doc.rust-lang.org/src/core/io/error.rs.html#433)

Returns a mutable reference to the inner error wrapped by this error (if any).

If this [`Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error") was constructed via [`new`](https://doc.rust-lang.org/alloc/io/struct.Error.html#method.new) then this function will return [`Some`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.Some "variant std::option::Option::Some"), otherwise it will return [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None").

##### Examples

``` rust
use std::io::{Error, ErrorKind};
use std::{error, fmt};
use std::fmt::Display;

#[derive(Debug)]
struct MyError {
    v: String,
}

impl MyError {
    fn new() -> MyError {
        MyError {
            v: "oh no!".to_string()
        }
    }

    fn change_message(&mut self, new_message: &str) {
        self.v = new_message.to_string();
    }
}

impl error::Error for MyError {}

impl Display for MyError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "MyError: {}", self.v)
    }
}

fn change_error(mut err: Error) -> Error {
    if let Some(inner_err) = err.get_mut() {
        inner_err.downcast_mut::<MyError>().unwrap().change_message("I've been changed!");
    }
    err
}

fn print_error(err: &Error) {
    if let Some(inner_err) = err.get_ref() {
        println!("Inner error: {inner_err}");
    } else {
        println!("No inner error");
    }
}

fn main() {
    // Will print "No inner error".
    print_error(&change_error(Error::last_os_error()));
    // Will print "Inner error: ...".
    print_error(&change_error(Error::new(ErrorKind::Other, MyError::new())));
}
```

1.0.0 · [Source](https://doc.rust-lang.org/src/core/io/error.rs.html#472)

Returns the corresponding [`ErrorKind`](https://doc.rust-lang.org/std/io/enum.ErrorKind.html "enum std::io::ErrorKind") for this error.

This may be a value set by Rust code constructing custom `io::Error`s, or if this `io::Error` was sourced from the operating system, it will be a value inferred from the system’s error encoding. See [`last_os_error`](https://doc.rust-lang.org/std/io/struct.Error.html#method.last_os_error) for more details.

##### Examples

``` rust
use std::io::{Error, ErrorKind};

fn print_error(err: Error) {
    println!("{:?}", err.kind());
}

fn main() {
    // As no error has (visibly) occurred, this may print anything!
    // It likely prints a placeholder for unidentified (non-)errors.
    print_error(Error::last_os_error());
    // Will print "AddrInUse".
    print_error(Error::new(ErrorKind::AddrInUse, "oh no!"));
}
```

[Source](https://doc.rust-lang.org/src/alloc/io/error.rs.html#11)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/io/error.rs.html#42-44)

Creates a new I/O error from a known kind of error as well as an arbitrary error payload.

This function is used to generically create I/O errors which do not originate from the OS itself. The `error` argument is an arbitrary payload which will be contained in this [`Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error").

Note that this function allocates memory on the heap. If no extra payload is required, use the `From` conversion from `ErrorKind`.

##### Examples

``` rust
use std::io::{Error, ErrorKind};

// errors can be created from strings
let custom_error = Error::new(ErrorKind::Other, "oh no!");

// errors can also be created from other errors
let custom_error2 = Error::new(ErrorKind::Interrupted, custom_error);

// creating an error without payload (and without memory allocation)
let eof_error = Error::from(ErrorKind::UnexpectedEof);
```

1.74.0 · [Source](https://doc.rust-lang.org/src/alloc/io/error.rs.html#75-77)

Creates a new I/O error from an arbitrary error payload.

This function is used to generically create I/O errors which do not originate from the OS itself. It is a shortcut for [`Error::new`](https://doc.rust-lang.org/std/io/struct.Error.html#method.new) with [`ErrorKind::Other`](https://doc.rust-lang.org/std/io/enum.ErrorKind.html#variant.Other "variant std::io::ErrorKind::Other").

##### Examples

``` rust
use std::io::Error;

// errors can be created from strings
let custom_error = Error::other("oh no!");

// errors can also be created from other errors
let custom_error2 = Error::other(custom_error);
```

1.3.0 · [Source](https://doc.rust-lang.org/src/alloc/io/error.rs.html#116)

Consumes the `Error`, returning its inner error (if any).

If this [`Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error") was constructed via [`new`](https://doc.rust-lang.org/std/io/struct.Error.html#method.new) or [`other`](https://doc.rust-lang.org/std/io/struct.Error.html#method.other), then this function will return [`Some`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.Some "variant std::option::Option::Some"), otherwise it will return [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None").

##### Examples

``` rust
use std::io::{Error, ErrorKind};

fn print_error(err: Error) {
    if let Some(inner_err) = err.into_inner() {
        println!("Inner error: {inner_err}");
    } else {
        println!("No inner error");
    }
}

fn main() {
    // Will print "No inner error".
    print_error(Error::last_os_error());
    // Will print "Inner error: ...".
    print_error(Error::new(ErrorKind::Other, "oh no!"));
}
```

1.79.0 · [Source](https://doc.rust-lang.org/src/alloc/io/error.rs.html#206-208)

Attempts to downcast the custom boxed error to `E`.

If this [`Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error") contains a custom boxed error, then it would attempt downcasting on the boxed error, otherwise it will return [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err").

If the custom boxed error has the same type as `E`, it will return [`Ok`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Ok "variant std::result::Result::Ok"), otherwise it will also return [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err").

This method is meant to be a convenience routine for calling `Box<dyn Error + Sync + Send>::downcast` on the custom boxed error, returned by [`Error::into_inner`](https://doc.rust-lang.org/std/io/struct.Error.html#method.into_inner).

##### Examples

``` rust
use std::fmt;
use std::io;
use std::error::Error;

#[derive(Debug)]
enum E {
    Io(io::Error),
    SomeOtherVariant,
}

impl fmt::Display for E {
   // ...
}
impl Error for E {}

impl From<io::Error> for E {
    fn from(err: io::Error) -> E {
        err.downcast::<E>()
            .unwrap_or_else(E::Io)
    }
}

impl From<E> for io::Error {
    fn from(err: E) -> io::Error {
        match err {
            E::Io(io_error) => io_error,
            e => io::Error::new(io::ErrorKind::Other, e),
        }
    }
}

let e = E::SomeOtherVariant;
// Convert it to an io::Error
let io_error = io::Error::from(e);
// Cast it back to the original variant
let e = E::from(io_error);
assert!(matches!(e, E::SomeOtherVariant));

let io_error = io::Error::from(io::ErrorKind::AlreadyExists);
// Convert it to E
let e = E::from(io_error);
// Cast it back to the original variant
let io_error = io::Error::from(e);
assert_eq!(io_error.kind(), io::ErrorKind::AlreadyExists);
assert!(io_error.get_ref().is_none());
assert!(io_error.raw_os_error().is_none());
```

## Trait Implementations

1.0.0 · [Source](https://doc.rust-lang.org/src/core/io/error.rs.html#91)

[Source](https://doc.rust-lang.org/src/core/io/error.rs.html#92)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/io/error.rs.html#520)

[Source](https://doc.rust-lang.org/src/core/io/error.rs.html#521)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Display.html#tymethod.fmt)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/io/error.rs.html#535)

[Source](https://doc.rust-lang.org/src/core/io/error.rs.html#537)

👎Deprecated since 1.33.0:

replaced by Error::source, which can support downcasting

[Source](https://doc.rust-lang.org/src/core/io/error.rs.html#546)

Returns the lower-level source of this error, if any. [Read more](https://doc.rust-lang.org/std/error/trait.Error.html#method.source)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/error.rs.html#137)

👎Deprecated since 1.42.0:

use the Display impl or to_string()

[Read more](https://doc.rust-lang.org/std/error/trait.Error.html#method.description)

[Source](https://doc.rust-lang.org/src/core/error.rs.html#260)

🔬This is a nightly-only experimental API. (`error_generic_member_access` [\#99301](https://github.com/rust-lang/rust/issues/99301))

Provides type-based access to context intended for error reports. [Read more](https://doc.rust-lang.org/std/error/trait.Error.html#method.provide)

1.14.0 · [Source](https://doc.rust-lang.org/src/core/io/error.rs.html#202)

Intended for use for errors not exposed to the user, where allocating onto the heap (for normal construction via Error::new) is too costly.

[Source](https://doc.rust-lang.org/src/core/io/error.rs.html#217)

Converts an [`ErrorKind`](https://doc.rust-lang.org/std/io/enum.ErrorKind.html "enum std::io::ErrorKind") into an [`Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error").

This conversion creates a new error with a simple representation of error kind.

##### Examples

``` rust
use std::io::{Error, ErrorKind};

let not_found = ErrorKind::NotFound;
let error = Error::from(not_found);
assert_eq!("entity not found", format!("{error}"));
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/mod.rs.html#175-179)

[Source](https://doc.rust-lang.org/src/std/io/buffered/mod.rs.html#176-178)

Converts to this type from the input type.

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/io/error.rs.html#229)

Available on **non-`no_rc` and non-`no_sync`** only.

[Source](https://doc.rust-lang.org/src/alloc/io/error.rs.html#231)

Converts a [`crate::ffi::NulError`](https://doc.rust-lang.org/std/ffi/struct.NulError.html "struct std::ffi::NulError") into a [`Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error").

1.89.0 · [Source](https://doc.rust-lang.org/src/std/fs.rs.html#532-539)

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#533-538)

Converts to this type from the input type.

1.78.0 · [Source](https://doc.rust-lang.org/src/alloc/io/error.rs.html#237)

[Source](https://doc.rust-lang.org/src/alloc/io/error.rs.html#242)

Converts `TryReserveError` to an error with [`ErrorKind::OutOfMemory`](https://doc.rust-lang.org/std/io/enum.ErrorKind.html#variant.OutOfMemory "variant std::io::ErrorKind::OutOfMemory").

`TryReserveError` won’t be available as the error `source()`, but this may change in the future.

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/string.rs.html#2918)

[Source](https://doc.rust-lang.org/src/alloc/string.rs.html#2920)

Converts the given value to a `String`. [Read more](https://doc.rust-lang.org/std/string/trait.ToString.html#tymethod.to_string)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.