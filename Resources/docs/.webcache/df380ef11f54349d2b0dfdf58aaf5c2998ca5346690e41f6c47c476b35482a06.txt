[std](https://doc.rust-lang.org/std/index.html)::[arch](https://doc.rust-lang.org/std/arch/index.html)

# Macro is_mips64_feature_detected Copy item path

[Source](https://doc.rust-lang.org/src/std_detect/detect/arch/mips64.rs.html#3-15)

``` rust
macro_rules! is_mips64_feature_detected {
    ("msa") => { ... };
    ($t:tt,) => { ... };
    ($t:tt) => { ... };
}
```

🔬This is a nightly-only experimental API. (`stdarch_mips_feature_detection` [\#111188](https://github.com/rust-lang/rust/issues/111188))

Expand description

Check for the presence of a CPU feature at runtime.

When the feature is known to be enabled at compile time (e.g. via `-Ctarget-feature`) the macro expands to `true`.