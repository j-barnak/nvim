# Equality

Racket has more than one function for testing the equality of two items. Why? So that specialized equality functions can be optimized to run faster. But if you’re not comparing data types that are appropriate, you’ll get inaccurate results. Choose with care.

[equal?](http://docs.racket-lang.org/reference/Equality.html#(def._((quote._~23~25kernel)._equal~3f))) gives the right answer for all data types, though it’s the slowest of the equality functions. When in doubt, use this. It diligently inspects every value, including values enclosed in two lists:

    (equal? "foobar" (string-append "foo" "bar")) ; #t
    (equal? '(1 2 3 4) (append '(1 2) '(3 4))) ; #t
    (equal? 42 (* 6 7)) ; #t
    (equal? 'symbol (string->symbol "symbol")) ; #t

[eq?](http://docs.racket-lang.org/reference/Equality.html#(def._((quote._~23~25kernel)._eq~3f))) is mostly restricted to symbols, small integers (called [fixnums](https://beautifulracket.com/appendix/glossary.html#fixnum)), and objects. eq? can be much faster than equal? because it doesn’t actually inspect the values of the two items. Rather, it simply checks if the items refer to the same chunk of memory. (Those familiar with memory operations might know this as *[pointer comparison](https://beautifulracket.com/appendix/glossary.html#pointer-comparison)*.)

    (eq? "foobar" (string-append "foo" "bar")) ; #f
    (eq? '(1 2 3 4) (append '(1 2) '(3 4))) ; #f
    (eq? 42 (* 6 7)) ; #t
    (eq? 'symbol (string->symbol "symbol")) ; #t

Its reliance on pointer comparison, however, is why eq? is limited to [symbols](https://beautifulracket.com/appendix/glossary.html#symbol) and a handful of other data types that are [interned](https://beautifulracket.com/appendix/glossary.html#interned). Conversely, it’s also why you’ll see symbols chosen for situations where speedy comparisons are needed, because then you can use eq?.

Take extra care with eq?, which can lead newcomers astray with its ability to give seemingly accurate results in simple test cases (like numbers and strings), then seemingly fail when the input gets more complex. In those cases, eq? is right; your expectations are wrong.

    (eq? "foobar" "foobar") ; #t, but ...
    (eq? "foobar" (string-append "foo" "bar")) ; #f
    (eq? 42 (* 6 7)) ; #t, but ...
    (eq? (expt 9 999) (expt 9 999)) ; #f

[eqv?](http://docs.racket-lang.org/reference/Equality.html#(def._((quote._~23~25kernel)._eqv~3f))) is like eq?, but handles characters and all numbers consistently.

    (eqv? 'symbol (string->symbol "symbol")) ; #t
    (eqv? 42 (* 6 7)) ; #t
    (eqv? (expt 9 999) (expt 9 999)) ; #t, but was #f with `eq?`

[=](http://docs.racket-lang.org/reference/generic-numbers.html#(def._((quote._~23~25kernel)._~3d))) is like eqv?, but limited to numbers, and can be faster than equal?.

    (= 42 (* 6 7)) ; #t
    (= '(1 2 3 4) (append '(1 2) '(3 4))) ; error

### [Further](#further)

-

  [Stringlike types](https://beautifulracket.com/explainer/stringlike-types.html) explainer

-

  [Numbers](https://beautifulracket.com/explainer/numbers.html) explainer

-

  [Equality](http://docs.racket-lang.org/reference/Equality.html) in the Racket Reference