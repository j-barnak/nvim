[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)

# Function unaligned_volatile_load Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/mod.rs.html#1014)

``` rust
pub unsafe fn unaligned_volatile_load<T>(src: *const T) -> T
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Performs a volatile load from the `src` pointer The pointer is not required to be aligned.

This intrinsic does not have a stable counterpart.