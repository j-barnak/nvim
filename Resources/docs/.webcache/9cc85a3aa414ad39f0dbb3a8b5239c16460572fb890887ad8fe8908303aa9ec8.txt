# miasm.analysis.depgraph

Provide dependency graph

## Classes

### class `DependencyNode(object)`

Node elements of a DependencyGraph

A dependency node stands for the dependency on the @element at line number
@line_nb in the IRblock named @loc_key, *before* the evaluation of this
line.

- **`__init__(self, loc_key, element, line_nb)`** - Create a dependency node with:
- **`__hash__(self)`** - Returns a hash of @self to uniquely identify @self
- **`__eq__(self, depnode)`** - Returns True if @self and @depnode are equals.
- **`__ne__(self, depnode)`**
- **`__lt__(self, node)`** - Compares @self with @node.
- **`__str__(self)`** - Returns a string representation of DependencyNode
- **`__repr__(self)`** - Returns a string representation of DependencyNode
- **`loc_key(self)`** - Name of the current IRBlock
- **`element(self)`** - Current tracked Expr
- **`line_nb(self)`** - Line in the current IRBlock

### class `DependencyState(object)`

Store intermediate depnodes states during dependencygraph analysis

- **`__init__(self, loc_key, pending, line_nb=None)`**
- **`__repr__(self)`**
- **`extend(self, loc_key)`** - Return a copy of itself, with itself in history
- **`get_done_state(self)`** - Returns immutable object representing current state
- **`as_graph(self)`** - Generates a Digraph of dependencies
- **`graph(self)`** - Returns a DiGraph instance representing the DependencyGraph
- **`remove_pendings(self, nodes)`** - Remove resolved @nodes
- **`add_pendings(self, future_pending)`** - Add @future_pending to the state
- **`link_element(self, element, line_nb)`** - Link element to its dependencies
- **`link_dependencies(self, element, line_nb, dependencies, future_pending)`** - Link unfollowed dependencies and create remaining pending elements.

### class `DependencyResult(DependencyState)`

Container and methods for DependencyGraph results

- **`__init__(self, ircfg, initial_state, state, inputs)`**
- **`unresolved(self)`** - Set of nodes whose dependencies weren't found
- **`relevant_nodes(self)`** - Set of nodes directly and indirectly influencing inputs
- **`relevant_loc_keys(self)`** - List of loc_keys containing nodes influencing inputs.
- **`has_loop(self)`** - True iff there is at least one data dependencies cycle (regarding
- **`irblock_slice(self, irb, max_line=None)`** - Slice of the dependency nodes on the irblock @irb
- **`emul(self, lifter, ctx=None, step=False)`** - Symbolic execution of relevant nodes according to the history

### class `DependencyResultImplicit(DependencyResult)`

Stand for a result of a DependencyGraph with implicit option

Provide path constraints using the z3 solver

- **`_gen_path_constraints(self, translator, expr, expected)`** - Generate path constraint from @expr. Handle special case with
- **`emul(self, lifter, ctx=None, step=False)`**
- **`is_satisfiable(self)`** - Return True iff the solution path admits at least one solution
- **`constraints(self)`** - If satisfiable, return a valid solution as a Z3 Model instance

### class `FollowExpr(object)`

Stand for an element (expression, depnode, ...) to follow or not

- **`__init__(self, follow, element)`**
- **`__repr__(self)`**
- **`to_depnodes(follow_exprs, loc_key, line)`** - Build a set of FollowExpr(DependencyNode) from the @follow_exprs set
- **`extract_depnodes(follow_exprs, only_follow=False)`** - Extract depnodes from a set of FollowExpr(Depnodes)

### class `FilterExprSources(ExprWalk)`

Walk Expression to find sources to track
@follow_mem: (optional) Track memory syntactically
@follow_call: (optional) Track through "call"

- **`__init__(self, follow_mem, follow_call)`**
- **`visit(self, expr, *args, **kwargs)`**
- **`visit_inner(self, expr, *args, **kwargs)`**

### class `DependencyGraph(object)`

Implementation of a dependency graph

A dependency graph contains DependencyNode as nodes. The oriented edges
stand for a dependency.
The dependency graph is made of the lines of a group of IRblock
*explicitly* or *implicitly* involved in the equation of given element.

- **`__init__(self, ircfg, implicit=False, apply_simp=True, follow_mem=True, follow_call=True)`** - Create a DependencyGraph linked to @ircfg
- **`do_follow(exprs, follow_mem, follow_call)`**
- **`_follow_simp_expr(exprs)`** - Simplify expression so avoid tracking useless elements,
- **`_follow_apply_cb(self, expr)`** - Apply callback functions to @expr
- **`_track_exprs(self, state, assignblk, line_nb)`** - Track pending expression in an assignblock
- **`_compute_intrablock(self, state)`** - Follow dependencies tracked in @state in the current irbloc
- **`get(self, loc_key, elements, line_nb, heads)`** - Compute the dependencies of @elements at line number @line_nb in
- **`get_from_depnodes(self, depnodes, heads)`** - Alias for the get() method. Use the attributes of @depnodes as
- **`address_to_location(self, address)`** - Helper to retrieve the .get() arguments, ie.
