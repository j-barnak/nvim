# 02 Modern SQL (slides)

15-445/645 FALL 2025   PROF. ANDY PAVLO
                                                                              2

                                                  ADMINISTRIVIA
                               Project #0 is due Sunday Sept 7th @ 11:59pm

                               Homework #1 is due Sunday Sept 7th @ 11:59pm

                               No in-class lecture next Wednesday Sept 3rd




DATABASE SYSTEMS (FALL 2025)
                                                                                          3

                                                    LAST CLASS
                               We introduced the Relational Model as the superior
                               data model for databases.

                               We then showed how Relational Algebra is the
                               building blocks that will allow us to query and modify a
                               relational database.




DATABASE SYSTEMS (FALL 2025)
                                                                                          4

                                                     SQL HISTORY
                               In 1971, IBM created its first relational query language
                               called SQUARE.
                                     SQUARE




                                                                           IBM System R




                               IBM then created "SEQUEL" in 1972 for IBM System R
                               prototype DBMS.
                               → Structured English Query Language

                               IBM releases commercial SQL-based DBMSs:
                               → System/38 (1979), SQL/DS (1981), and DB2 (1983).



DATABASE SYSTEMS (FALL 2025)
                                                                                          4

                                                     SQL HISTORY
                                                        6




                               In 1971, IBM created its first relational query language
                               called SQUARE.
                                     SQUARE




                                                                           IBM System R




                               IBM then created "SEQUEL" in 1972 for IBM System R
                               prototype DBMS.
                               → Structured English Query Language

                               IBM releases commercial SQL-based DBMSs:
                               → System/38 (1979), SQL/DS (1981), and DB2 (1983).



DATABASE SYSTEMS (FALL 2025)
                                                                                       5

                                                    SQL HISTORY
                               ANSI Standard in 1986. ISO in 1987
                               → Structured Query Language

                               Current standard is SQL:2023
                               → SQL:2023 → Property Graph Queries, Muti-Dim. Arrays
                               → SQL:2016 → JSON, Polymorphic tables
                               → SQL:2011 → Temporal DBs, Pipelined DML
                               → SQL:2008 → Truncation, Fancy Sorting
                               → SQL:2003 → XML, Windows, Sequences, Auto-Gen IDs.
                               → SQL:1999 → Regex, Triggers, OO

                               The minimum language syntax a system needs to say
                               that it supports SQL is SQL-92.
                                                       SQL-92




DATABASE SYSTEMS (FALL 2025)
                                                                                       5

                                                    SQL HISTORY
                               ANSI Standard in 1986. ISO in 1987
                               → Structured Query Language

                               Current standard is SQL:2023
                               → SQL:2023 → Property Graph Queries, Muti-Dim. Arrays
                               → SQL:2016 → JSON, Polymorphic tables
                               → SQL:2011 → Temporal DBs, Pipelined DML
                               → SQL:2008 → Truncation, Fancy Sorting
                               → SQL:2003 → XML, Windows, Sequences, Auto-Gen IDs.
                               → SQL:1999 → Regex, Triggers, OO

                               The minimum language syntax a system needs to say
                               that it supports SQL is SQL-92.
                                                       SQL-92




DATABASE SYSTEMS (FALL 2025)
                                                                                       5

                                                    SQL HISTORY
                               ANSI Standard in 1986. ISO in 1987
                               → Structured Query Language

                               Current standard is SQL:2023
                               → SQL:2023 → Property Graph Queries, Muti-Dim. Arrays
                               → SQL:2016 → JSON, Polymorphic tables
                               → SQL:2011 → Temporal DBs, Pipelined DML
                               → SQL:2008 → Truncation, Fancy Sorting
                               → SQL:2003 → XML, Windows, Sequences, Auto-Gen IDs.
                               → SQL:1999 → Regex, Triggers, OO

                               The minimum language syntax a system needs to say
                               that it supports SQL is SQL-92.
                                                       SQL-92




DATABASE SYSTEMS (FALL 2025)
                                                                                       5

                                                    SQL HISTORY
                               ANSI Standard in 1986. ISO in 1987
                               → Structured Query Language

                               Current standard is SQL:2023
                               → SQL:2023 → Property Graph Queries, Muti-Dim. Arrays
                               → SQL:2016 → JSON, Polymorphic tables
                               → SQL:2011 → Temporal DBs, Pipelined DML
                               → SQL:2008 → Truncation, Fancy Sorting
                               → SQL:2003 → XML, Windows, Sequences, Auto-Gen IDs.
                               → SQL:1999 → Regex, Triggers, OO

                               The minimum language syntax a system needs to say
                               that it supports SQL is SQL-92.
                                                       SQL-92




DATABASE SYSTEMS (FALL 2025)
                                                                                       7

                                              RELATIONAL LANGUAGES
                               Data Manipulation Language (DML)
                               Data Definition Language (DDL)
                               Data Control Language (DCL)

                               Also includes:
                               → View definition
                               → Integrity & Referential Constraints
                               → Transactions

                               Important: SQL is based on bags (duplicates) not sets
                               (no duplicates).

DATABASE SYSTEMS (FALL 2025)
                                                                 8

                                                TODAYS AGENDA
                               Aggregations + Group By
                               String / Date / Time Operations
                               Output Control + Redirection
                               Nested Queries
                               Lateral Joins
                               Common Table Expressions
                               Window Functions
                                DB Flash Talk: dbt

DATABASE SYSTEMS (FALL 2025)
                                                                                                 9

                                                       EXAMPLE DATABASE
                                   student(sid,name,login,gpa)         enrolled(sid,cid,grade)
                               sid   name     login         age gpa    sid     cid      grade
                               53666 RZA      rza@cs         56 4.0    53666   15-445   C
                               53688 Taylor   swift@cs       35 3.9    53688   15-721   A
                               53655 Tupac    shakur@cs      25 3.5    53688   15-826   B
                                                                       53655   15-445   B
                                         course(cid,name)              53666   15-721   C
                               cid      name
                               15-445    Database Systems
                               15-721    Advanced Database Systems
                               15-826    Data Mining
                               15-799    Special Topics in Databases


DATABASE SYSTEMS (FALL 2025)
                                                                                    10

                                                     AGGREGATES
                               Functions that return a single value from a bag of
                               tuples:
                               → AVG(col)→ Return the average col value.
                               → MIN(col)→ Return minimum col value.
                               → MAX(col)→ Return maximum col value.
                               → SUM(col)→ Return sum of values in col.
                               → COUNT(col)→ Return # of values for col.




DATABASE SYSTEMS (FALL 2025)
                                                                                      11

                                                      AGGREGATES
                               Aggregate functions can (almost) only be used in the
                               SELECT output list.

                               Get # of students with a “@cs” login:

                                     SELECT COUNT(login) AS cnt
                                       FROM student WHERE login LIKE '%@cs'




DATABASE SYSTEMS (FALL 2025)
                                                                                      11

                                                      AGGREGATES
                               Aggregate functions can (almost) only be used in the
                               SELECT output list.

                               Get # of students with a “@cs” login:

                                     SELECT COUNT(login) AS cnt
                                       FROM student WHERE login LIKE '%@cs'
                                        SELECT COUNT(*) AS cnt
                                          FROM student WHERE login LIKE '%@cs'




DATABASE SYSTEMS (FALL 2025)
                                                                                      11

                                                      AGGREGATES
                               Aggregate functions can (almost) only be used in the
                               SELECT output list.

                               Get # of students with a “@cs” login:

                                     SELECT COUNT(login) AS cnt
                                       FROM student WHERE login LIKE '%@cs'
                                        SELECT COUNT(*) AS cnt
                                          FROM student WHERE login LIKE '%@cs'
                                           SELECT COUNT(1) AS cnt
                                             FROM student WHERE login LIKE '%@cs'


DATABASE SYSTEMS (FALL 2025)
                                                                                       11

                                                      AGGREGATES
                               Aggregate functions can (almost) only be used in the
                               SELECT output list.

                               Get # of students with a “@cs” login:

                                     SELECT COUNT(login) AS cnt
                                       FROM student WHERE login LIKE '%@cs'
                                        SELECT COUNT(*) AS cnt
                                          FROM student WHERE login LIKE '%@cs'
                                           SELECT COUNT(1) AS cnt
                                             FROM student WHERE login LIKE '%@cs'
                                              SELECT COUNT(1+1+1) AS cnt
                                                FROM student WHERE login LIKE '%@cs'
DATABASE SYSTEMS (FALL 2025)
                                                                                                        12

                                                     AGGREGATES
                               Output of other columns outside of an aggregate is
                               undefined.

                               Get the average GPA of students enrolled in each course.
                                                                                   AVG(s.gpa)   e.cid
                                    SELECT AVG(s.gpa), e.cid                        3.86        ???
                                      FROM enrolled AS e JOIN student AS s
                                        ON e.sid = s.sid




DATABASE SYSTEMS (FALL 2025)
                                                                                                          12

                                                     AGGREGATES
                               Output of other columns outside of an aggregate is
                               undefined.

                               Get the average GPA of students enrolled in each course.
                                                                                    AVG(s.gpa)    e.cid
                                    SELECT AVG(s.gpa), e.cid                        3.86           ???
                                      FROM enrolled AS e JOIN student AS s
                                        ON e.sid = s.sid
                                                                                   AVG(s.gpa)    e.cid
                                    SELECT AVG(s.gpa), ANY_VALUE(e.cid)             3.86         15-445
                                      FROM enrolled AS e JOIN student AS s
                                        ON e.sid = s.sid
DATABASE SYSTEMS (FALL 2025)
                                                                                                               13

                                                               GROUP BY
                               Project tuples into subsets and
                               calculate aggregates against           SELECT AVG(s.gpa), e.cid
                               each subset.                             FROM enrolled AS e JOIN student AS s
                                                                          ON e.sid = s.sid
                                                                       GROUP BY e.cid

                                      e.sid   s.sid    s.gpa       e.cid
                                      53435    53435   2.25    15-721
                                      53439    53439   2.70    15-721
                                      56023    56023   2.75    15-826
                                      59439    59439   3.90    15-826
                                      53961    53961   3.50    15-826
                                      58345    58345   1.89    15-445

DATABASE SYSTEMS (FALL 2025)
                                                                                                               13

                                                               GROUP BY
                               Project tuples into subsets and
                               calculate aggregates against           SELECT AVG(s.gpa), e.cid
                               each subset.                             FROM enrolled AS e JOIN student AS s
                                                                          ON e.sid = s.sid
                                                                       GROUP BY e.cid

                                      e.sid   s.sid    s.gpa       e.cid
                                      53435    53435   2.25    15-721              AVG(s.gpa)     e.cid
                                      53439    53439   2.70    15-721                2.46     15-721
                                      56023    56023   2.75    15-826                3.39     15-826
                                      59439    59439   3.90    15-826                1.89     15-445
                                      53961    53961   3.50    15-826
                                      58345    58345   1.89    15-445

DATABASE SYSTEMS (FALL 2025)
                                                                                         14

                                                  GROUPING SETS
                               Specify multiple groupings in a single query instead of
                               using UNION ALL to combine the results of several
                               individual GROUP BY queries.
                                    SELECT c.name AS c_name, e.grade,
                                           COUNT(*) AS num_students
                                      FROM enrolled AS e
                                      JOIN course AS c ON e.cid = c.cid
                                     GROUP BY GROUPING SETS (
                                      (c.name, e.grade), -- By course and grade
                                      (c.name),          -- By course only
                                      ()                 -- Overall total
                                    );
DATABASE SYSTEMS (FALL 2025)
                                                                                                      14

                                                  GROUPING SETS
                               Specify multiple groupings in a single query instead of
                               using UNION ALL to combine the results of several
                               individual GROUP BY queries.               cid    grade num_students
                                                                            null   null 5
                                    SELECT c.name AS c_name, e.grade, 15-721 C          1
                                           COUNT(*) AS num_students     15-826 B        1
                                      FROM enrolled AS e                15-445 B        1
                                      JOIN course AS c ON e.cid = c.cid 15-445 C        1
                                                                        15-721 A        1
                                     GROUP BY GROUPING SETS (           15-445 null     2
                                      (c.name, e.grade), -- By course and  grade
                                                                        15-826 null     1
                                      (c.name),          -- By course only
                                                                        15-721 null     2
                                      ()                 -- Overall total
                                    );
DATABASE SYSTEMS (FALL 2025)
                                                                                                      14

                                                  GROUPING SETS
                               Specify multiple groupings in a single query instead of
                               using UNION ALL to combine the results of several
                               individual GROUP BY queries.               cid    grade num_students
                                                                            null   null 5
                                    SELECT c.name AS c_name, e.grade, 15-721 C          1
                                           COUNT(*) AS num_students     15-826 B        1
                                      FROM enrolled AS e                15-445 B        1
                                      JOIN course AS c ON e.cid = c.cid 15-445 C        1
                                                                        15-721 A        1
                                     GROUP BY GROUPING SETS (           15-445 null     2
                                      (c.name, e.grade), -- By course and  grade
                                                                        15-826 null     1
                                      (c.name),          -- By course only
                                                                        15-721 null     2
                                      ()                 -- Overall total
                                    );
DATABASE SYSTEMS (FALL 2025)
                                                                                   15

                                                      HAVING
                               Filters results based on aggregation computation.
                               Like a WHERE clause for a GROUP BY

                                    SELECT AVG(s.gpa) AS avg_gpa, e.cid
                                      FROM enrolled AS e, student AS s
                                     WHERE e.sid = s.sid
                                       AND avg_gpa > 3.9
                                     GROUP BY e.cid




DATABASE SYSTEMS (FALL 2025)
                                                                                   15

                                                      HAVING
                               Filters results based on aggregation computation.
                               Like a WHERE clause for a GROUP BY

                                    SELECT AVG(s.gpa) AS avg_gpa, e.cid
                                      FROM enrolled AS e, student AS s
                                     WHERE e.sid = s.sid
                                       AND avg_gpa > 3.9
                                     GROUP BY e.cid




DATABASE SYSTEMS (FALL 2025)
                                                                                   15

                                                      HAVING
                               Filters results based on aggregation computation.
                               Like a WHERE clause for a GROUP BY

                                    SELECT AVG(s.gpa) AS avg_gpa, e.cid
                                      FROM enrolled AS e, student AS s
                                     WHERE e.sid = s.sid
                                     GROUP
                                       AND BY
                                            avg_gpa
                                               e.cid> 3.9
                                     GROUP BY
                                     HAVING  avg_gpa
                                               e.cid > 3.9;




DATABASE SYSTEMS (FALL 2025)
                                                                                      15

                                                         HAVING
                               Filters results based on aggregation computation.
                               Like a WHERE clause for a GROUP BY

                                    SELECT AVG(s.gpa) AS avg_gpa, e.cid
                                      FROM enrolled AS e, student AS s
                                     WHERE e.sid = s.sid
                                     GROUP
                                       AND BY
                                            avg_gpa
                                               e.cid> 3.9
                                     GROUP BY
                                     HAVING  avg_gpa
                                             AVG(s.gpa)
                                               e.cid > 3.9;
                                                        > 3.9;

                                     AVG(s.gpa) e.cid
                                       3.75     15-415            avg_gpa    e.cid
                                       3.950000 15-721              3.950000 15-721
                                       3.900000 15-826
DATABASE SYSTEMS (FALL 2025)
                                                                             16

                                          STRING OPERATIONS
                                           String Case    String Quotes
                               SQL-92      Sensitive      Single Only
                               Postgres    Sensitive      Single Only
                               MySQL       Insensitive    Single/Double
                               SQLite      Sensitive      Single/Double
                               MSSQL       Sensitive      Single Only
                               Oracle      Sensitive      Single Only

                               WHERE UPPER(name) = UPPER('TuPaC')   SQL-92

                               WHERE name = "TuPaC"                  MySQL
DATABASE SYSTEMS (FALL 2025)
                                                                                                         17

                                                       STRING OPERATIONS
                               LIKE provides string matching with          SELECT * FROM enrolled AS e
                               special match operators:                     WHERE e.cid LIKE '15-%';
                               → '%' Matches any substring (including      SELECT * FROM student AS s
                                 empty strings).
                                                                            WHERE s.login LIKE '%@c_';
                               → '_' Match any one character

                               SIMILAR TO allows for regular               SELECT * FROM student AS s
                               expression matching.                         WHERE login SIMILAR TO
                               → In the SQL standard but not all systems          '[\w]{3}@cs';
                                 support it.
                               → Other systems also support POSIX-style
                                 regular expressions.
DATABASE SYSTEMS (FALL 2025)
                                                                                   18

                                                STRING OPERATIONS
                               SQL-92 defines string functions.
                               → Many DBMSs also have their own unique functions
                               Can be used in either output and predicates:

                                    SELECT SUBSTRING(name,1,5) AS abbrv_name
                                      FROM student WHERE sid = 53688

                                    SELECT * FROM student AS s
                                     WHERE UPPER(s.name) LIKE 'KAN%'



DATABASE SYSTEMS (FALL 2025)
                                                                                        19

                                              STRING OPERATIONS
                               SQL standard defines the || operator for concatenating
                               two or more strings together.

                                    SELECT name FROM student
                                     WHERE login = LOWER(name) || '@cs'

                                    SELECT name FROM student
                                     WHERE login = LOWER(name) + '@cs'

                                    SELECT name FROM student
                                     WHERE login = CONCAT(LOWER(name), '@cs')

DATABASE SYSTEMS (FALL 2025)
                                                                               20

                                           DATE/TIME OPERATIONS
                               Operations to manipulate and modify DATE/TIME
                               attributes.
                               Can be used in both output and predicates.
                               Support/syntax varies wildly…

                               Demo: Compute the number of days since the
                               beginning of the year.




DATABASE SYSTEMS (FALL 2025)
                                                                                                             21

                                                          OUTPUT CONTROL
                               ORDER BY <column*> [ASC|DESC] SELECT sid, name FROM student
                               → Sort tuples by the values in one or more    WHERE login LIKE '%@cs'
                                 of their columns.                           FETCH FIRST 10 ROWS ONLY;

                               FETCH {FIRST|NEXT} <#> ROWS                  SELECT sid, name FROM student
                               OFFSET <#> ROWS                               WHERE login LIKE '%@cs'
                               → Limit # of tuples returned in output.       ORDER BY gpa
                               → Can set an offset to return a “range”      OFFSET 5 ROWS
                                                                             FETCH FIRST 5 ROWS WITH TIES;

                                                                            SELECT TOP 10 sid, name
                                                                              FROM student
                                                                             WHERE login LIKE '%@cs';
DATABASE SYSTEMS (FALL 2025)
                                                                                                22

                                                OUTPUT REDIRECTION
                               Store query results in another table:
                               → Table must not already be defined.
                               → Table will have the same # of columns with the same types as
                                 the input.

                                     SELECT DISTINCT cid INTO CourseIds
                                       FROM enrolled;



                                     CREATE TABLE CourseIds (
                                       SELECT DISTINCT cid FROM enrolled);
DATABASE SYSTEMS (FALL 2025)
                                                                                                22

                                                OUTPUT REDIRECTION
                               Store query results in another table:
                               → Table must not already be defined.
                               → Table will have the same # of columns with the same types as
                                 the input.

                                     SELECT DISTINCT cid INTO CourseIds
                                       FROM enrolled;
                                            SELECT DISTINCT cid
                                              INTO TEMPORARY CourseIds
                                              FROM enrolled;
                                     CREATE TABLE CourseIds (
                                       SELECT DISTINCT cid FROM enrolled);
DATABASE SYSTEMS (FALL 2025)
                                                                                        23

                                                   NESTED QUERIES
                               Invoke a query inside of another query to compose
                               more complex computations.
                               → Inner queries can appear (almost) anywhere in query.

                                         SELECT name FROM student WHERE
                                          sid IN (SELECT sid FROM enrolled)




DATABASE SYSTEMS (FALL 2025)
                                                                                        23

                                                   NESTED QUERIES
                               Invoke a query inside of another query to compose
                               more complex computations.
                               → Inner queries can appear (almost) anywhere in query.

                                         SELECT name FROM student WHERE
                                          sid IN (SELECT sid FROM enrolled)
                                       SELECT sid,
                                              (SELECT name FROM student AS s
                                                WHERE s.sid = e.sid) AS name
                                         FROM enrolled AS e;


DATABASE SYSTEMS (FALL 2025)
                                                                       24

                                                   NESTED QUERIES
                               Get the names of students in '15-445'

                                       SELECT name FROM student
                                        WHERE ...




DATABASE SYSTEMS (FALL 2025)
                                                                       24

                                                   NESTED QUERIES
                               Get the names of students in '15-445'

                                       SELECT name FROM student
                                        WHERE ...
                                          SELECT sid FROM enrolled
                                           WHERE cid = '15-445'




DATABASE SYSTEMS (FALL 2025)
                                                                       24

                                                   NESTED QUERIES
                               Get the names of students in '15-445'

                                       SELECT name FROM student
                                        WHERE ...
                                              sid IN (
                                          SELECT sid FROM enrolled
                                           WHERE cid = '15-445'
                                        )




DATABASE SYSTEMS (FALL 2025)
                                                                       24

                                                   NESTED QUERIES
                               Get the names of students in '15-445'

                                       SELECT name FROM student
                                        WHERE ...
                                              sid IN (
                                          SELECT sid FROM enrolled
                                           WHERE cid = '15-445'
                                        )




DATABASE SYSTEMS (FALL 2025)
                                                                                       25

                                                NESTED QUERIES
                               ALL→ The expression must be true for all rows in the
                               sub-query.

                               ANY→ The expression must be true for at least one row
                               in the sub-query.

                               IN→ Equivalent to '=ANY()' .

                               EXISTS→ At least one row is returned without
                               comparing it to an attribute in outer query.

DATABASE SYSTEMS (FALL 2025)
                                                                                                26

                                                   NESTED QUERIES
                               Find student record with the highest id that is enrolled in at
                               least one course.

                                     SELECT sid, name FROM student
                                      WHERE ...




DATABASE SYSTEMS (FALL 2025)
                                                                                                26

                                                   NESTED QUERIES
                               Find student record with the highest id that is enrolled in at
                               least one course.

                                     SELECT sid, name FROM student
                                      WHERE sid
                                            ... =
                                        SELECT MAX(sid) FROM enrolled




DATABASE SYSTEMS (FALL 2025)
                                                                                                26

                                                   NESTED QUERIES
                               Find student record with the highest id that is enrolled in at
                               least one course.

                                     SELECT sid, name FROM student             sid     name
                                      WHERE sid
                                            ... =IN (                          53688   Bieber
                                        SELECT MAX(sid) FROM enrolled
                                     );




DATABASE SYSTEMS (FALL 2025)
                                                                                                26

                                                   NESTED QUERIES
                               Find student record with the highest id that is enrolled in at
                               least one course.

                                     SELECT sid, name FROM student
                                          SELECT
                                      WHERE        sid,
                                             ... =IN
                                             sid     ( name FROM student
                                           WHEREMAX(sid)
                                        SELECT     sid IN (FROM enrolled
                                     );       SELECT sid FROM enrolled
                                               ORDER BY sid DESC FETCH FIRST 1 ROW ONLY
                                          );



DATABASE SYSTEMS (FALL 2025)
                                                                                                26

                                                   NESTED QUERIES
                               Find student record with the highest id that is enrolled in at
                               least one course.

                                     SELECT sid, name FROM student
                                          SELECT
                                      WHERE        sid,
                                             ... =IN
                                             sid     ( name FROM student
                                           WHEREMAX(sid)
                                        SELECT     sid IN (FROM enrolled
                                     );         SELECTsid
                                              SELECT     student.sid, name
                                                           FROM enrolled
                                                   FROMBYstudent
                                               ORDER      sid DESC FETCH FIRST 1 ROW ONLY
                                          );       JOIN (SELECT MAX(sid) AS sid
                                                            FROM enrolled) AS max_e
                                                     ON student.sid = max_e.sid;

DATABASE SYSTEMS (FALL 2025)
                                                                                                 27

                                                       NESTED QUERIES
                               Find all courses that have no students enrolled in it.

                                     SELECT * FROM course
                                      WHERE ...



                                                                        sid     cid      grade
                                 cid      name
                                                                        53666   15-445     C
                                 15-445   Database Systems
                                                                        53688   15-721     A
                                 15-721   Advanced Database Systems
                                                                        53688   15-826     B
                                 15-826   Data Mining
                                                                        53655   15-445     B
                                 15-799   Special Topics in Databases
                                                                        53666   15-721     C

DATABASE SYSTEMS (FALL 2025)
                                                                                        27

                                                   NESTED QUERIES
                               Find all courses that have no students enrolled in it.

                                     SELECT * FROM course
                                      WHERE NOT
                                            ... EXISTS(

                                     );




DATABASE SYSTEMS (FALL 2025)
                                                                                        27

                                                      NESTED QUERIES
                               Find all courses that have no students enrolled in it.

                                     SELECT * FROM course
                                      WHERE NOT
                                            ... EXISTS(
                                        SELECT * FROM enrolled
                                     ); WHERE course.cid = enrolled.cid
                                     );


                                             cid      name
                                             15-799   Special Topics in Databases


DATABASE SYSTEMS (FALL 2025)
                                                                                        27

                                                      NESTED QUERIES
                               Find all courses that have no students enrolled in it.

                                     SELECT * FROM course
                                      WHERE NOT
                                            ... EXISTS(
                                        SELECT * FROM enrolled
                                     ); WHERE course.cid = enrolled.cid
                                     );


                                             cid      name
                                             15-799   Special Topics in Databases


DATABASE SYSTEMS (FALL 2025)
                                                                                                 28

                                                     LATERAL JOINS
                               The LATERAL operator allows a nested query to
                               reference attributes in other nested queries that precede
                               it (according to position in the query).
                               → You can think of it like a for loop that allows you to invoke
                                 another query for each tuple in a table.
                                                                           t1.x   t2.y
                                       SELECT * FROM                       1      2
                                         (SELECT 1 AS x) AS t1,
                                         LATERAL (SELECT t1.x+1 AS y) AS t2;

                                                   for x in [1]:
                                                     for y in [x+1]:
                                                       print(x,y)
DATABASE SYSTEMS (FALL 2025)
                                                                                                 29

                                                      LATERAL JOIN
                                 Calculate the number of students enrolled in each course and
                                 the average GPA. Sort output by the courses' enrollment count
                                 in ascending order.


                               SELECT * FROM course AS c,

                                  ⮕

                                  ⮕


DATABASE SYSTEMS (FALL 2025)
                                                                                                 29

                                                      LATERAL JOIN
                                 Calculate the number of students enrolled in each course and
                                 the average GPA. Sort output by the courses' enrollment count
                                 in ascending order.


                               SELECT * FROM course AS c,
                                 LATERAL (SELECT COUNT(*) AS cnt FROM enrolled
                                   ⮕       WHERE enrolled.cid = c.cid) AS t1,
                                 LATERAL (SELECT AVG(gpa) AS avg FROM student AS s
                                            JOIN enrolled AS e ON s.sid = e.sid
                                   ⮕       WHERE e.cid = c.cid) AS t2
                                ORDER BY cnt ASC;
DATABASE SYSTEMS (FALL 2025)
                                                                                                 29

                                                      LATERAL JOIN
                                 Calculate the number of students enrolled in each course and
                                 the average GPA. Sort output by the courses' enrollment count
                                 in ascending order.


                               SELECT * FROM course AS c,
                                 LATERAL (SELECT COUNT(*) AS cnt FROM enrolled
                                   ⮕       WHERE enrolled.cid = c.cid) AS t1,
                                 LATERAL (SELECT AVG(gpa) AS avg FROM student AS s
                                            JOIN enrolled AS e ON s.sid = e.sid
                                   ⮕       WHERE e.cid = c.cid) AS t2
                                ORDER BY cnt ASC;
DATABASE SYSTEMS (FALL 2025)
                                                                                                 29

                                                      LATERAL JOIN
                                 Calculate the number of students enrolled in each course and
                                 the average GPA. Sort output by the courses' enrollment count
                                 in ascending order.


                               SELECT * FROM course AS c,
                                 LATERAL (SELECT COUNT(*) AS cnt FROM enrolled
                                   ⮕       WHERE enrolled.cid = c.cid) AS t1,
                                 LATERAL (SELECT AVG(gpa) AS avg FROM student AS s
                                            JOIN enrolled AS e ON s.sid = e.sid
                                   ⮕       WHERE e.cid = c.cid) AS t2
                                ORDER BY cnt ASC;
DATABASE SYSTEMS (FALL 2025)
                                                                                                 29

                                                      LATERAL JOIN
                                 Calculate the number of students enrolled in each course and
                                 the average GPA. Sort output by the courses' enrollment count
                                 in ascending order.


                               SELECT * FROM course AS c,
                                 LATERAL (SELECT COUNT(*) AS cnt FROM enrolled
                                   ⮕       WHERE enrolled.cid = c.cid) AS t1,
                                 LATERAL (SELECT AVG(gpa) AS avg FROM student AS s
                                            JOIN enrolled AS e ON s.sid = e.sid
                                   ⮕       WHERE e.cid = c.cid) AS t2
                                ORDER BY cnt ASC;
DATABASE SYSTEMS (FALL 2025)
                                                                                                                29

                                                      LATERAL JOIN
                                 Calculate the number of students enrolled in each course and
                                 the average GPA. Sort output by the courses' enrollment count
                                 in ascending order.
                                                              cid    name                          cnt   avg
                                                              15-799 Special Topics in Databases   0     null
                                                              15-826 Data Mining                   1     3.9
                               SELECT * FROM course AS c,     15-445 Database Systems              2     3.75
                                 LATERAL (SELECT COUNT(*)    AS   cntAdvanced
                                                              15-721  FROM enrolled
                                                                              Database Systems     2     3.95
                                   ⮕       WHERE enrolled.cid = c.cid) AS t1,
                                 LATERAL (SELECT AVG(gpa) AS avg FROM student AS s
                                            JOIN enrolled AS e ON s.sid = e.sid
                                   ⮕       WHERE e.cid = c.cid) AS t2
                                ORDER BY cnt ASC;
DATABASE SYSTEMS (FALL 2025)
                                                                                             30

                                          COMMON TABLE EXPRESSIONS
                               Specify a temporary result set that can then be
                               referenced by another part of that query.
                               → Bind/alias output columns to names before the AS keyword.
                               Alternative to nested queries, views, and explicit temp
                               tables.

                                     WITH cteName (col1, col2) AS (
                                        SELECT 1, 2
                                     )
                                     SELECT col1 + col2 FROM cteName

DATABASE SYSTEMS (FALL 2025)
                                                                                             30

                                          COMMON TABLE EXPRESSIONS
                               Specify a temporary result set that can then be
                               referenced by another part of that query.
                               → Bind/alias output columns to names before the AS keyword.
                               Alternative to nested queries, views, and explicit temp
                               tables.

                                     WITH cteName (col1, col2) AS (
                                        SELECT 1, 2
                                     )
                                     SELECT col1 + col2 FROM cteName

DATABASE SYSTEMS (FALL 2025)
                                                                                                31

                                          COMMON TABLE EXPRESSIONS
                               Find student record with the highest id that is enrolled in at
                               least one course.


                                     WITH maxCTE (maxId) AS (
                                        SELECT MAX(sid) FROM enrolled
                                     )
                                     SELECT name FROM student AS s
                                       JOIN maxCTE ON s.sid = maxCTE.maxId;




DATABASE SYSTEMS (FALL 2025)
                                                                                                31

                                          COMMON TABLE EXPRESSIONS
                               Find student record with the highest id that is enrolled in at
                               least one course.


                                     WITH maxCTE (maxId) AS (
                                        SELECT MAX(sid) FROM enrolled
                                     )
                                     SELECT name FROM student AS s
                                       JOIN maxCTE ON s.sid = maxCTE.maxId;




DATABASE SYSTEMS (FALL 2025)
                                                                                                34

                                                  WINDOW FUNCTIONS
                               Performs a calculation across a set of tuples that are
                               related to the current tuple, without collapsing them
                               into a single output tuple, to support running totals,
                               ranks, and moving averages.
                               → Like an aggregation but tuples are not grouped into a single
                                 output tuples.



                                         SELECT FUNC-NAME(...) OVER (...)
                                           FROM tableName;


DATABASE SYSTEMS (FALL 2025)
                                                                                                          35

                                                        WINDOW FUNCTIONS
                               Aggregation functions:
                               → Anything that we discussed earlier      sid     cid      grade row_num
                               Special window functions:                 53666   15-445   C     1
                               → ROW_NUMBER()→ # of the current row      53688   15-721   A     2
                                                                         53688   15-826   B     3
                               → RANK()→ Order position of the current
                                                                         53655   15-445   B     4
                                 row.                                    53666   15-721   C     5



                                           SELECT *, ROW_NUMBER() OVER () AS row_num
                                             FROM enrolled;


DATABASE SYSTEMS (FALL 2025)
                                                                                                          35

                                                        WINDOW FUNCTIONS
                               Aggregation functions:
                               → Anything that we discussed earlier      sid     cid      grade row_num
                               Special window functions:                 53666   15-445   C     1
                               → ROW_NUMBER()→ # of the current row      53688   15-721   A     2
                                                                         53688   15-826   B     3
                               → RANK()→ Order position of the current
                                                                         53655   15-445   B     4
                                 row.                                    53666   15-721   C     5



                                           SELECT *, ROW_NUMBER() OVER () AS row_num
                                             FROM enrolled;


DATABASE SYSTEMS (FALL 2025)
                                                                                    36

                                                   WINDOW FUNCTIONS
                               The OVER keyword specifies how to
                               group together tuples when
                               computing the window function.
                               Use PARTITION BY to specify group.



                                         SELECT cid, sid,
                                             ROW_NUMBER() OVER (PARTITION BY cid)
                                           FROM enrolled
                                          ORDER BY cid;

DATABASE SYSTEMS (FALL 2025)
                                                                                                   36

                                                   WINDOW FUNCTIONS
                               The OVER keyword specifies how to
                               group together tuples when            cid      sid     row_number
                               computing the window function.        15-445   53666   1
                                                                     15-445   53655   2
                               Use PARTITION BY to specify group.    15-721   53688   1
                                                                     15-721   53666   2
                                                                     15-826   53688   1


                                         SELECT cid, sid,
                                             ROW_NUMBER() OVER (PARTITION BY cid)
                                           FROM enrolled
                                          ORDER BY cid;

DATABASE SYSTEMS (FALL 2025)
                                                                                37

                                              WINDOW FUNCTIONS
                               You can also include an ORDER BY in the window
                               grouping to sort entries in each group.


                                    SELECT *,
                                        ROW_NUMBER() OVER (ORDER BY cid)
                                      FROM enrolled
                                     ORDER BY cid;




DATABASE SYSTEMS (FALL 2025)
                                                                                             38

                                                 WINDOW FUNCTIONS
                               Find the student with second highest grade for each course.



                                    SELECT * FROM (
                                      SELECT *, RANK() OVER (PARTITION BY cid
                                                ORDER BY grade ASC) AS rank
                                        FROM enrolled) AS ranking
                                     WHERE ranking.rank = 2



DATABASE SYSTEMS (FALL 2025)
                                                                                                                                     39

                                                            CONCLUSION
                               SQL is a hot language.
                                                                     A graph with numbers and text

                                                                     Description automatically generated




                               → Lots of NL2SQL tools, but writing
                                 SQL is not going away.

                               You should (almost) always strive
                               to compute your answer as a
                               single SQL statement.


                                                                          https://spectrum.ieee.org/top-programming-languages-2024




DATABASE SYSTEMS (FALL 2025)
                                                        HOMEWORK #1
                                                                                             40




                                  Write SQL queries to perform basic data analysis.
                                  → Write the queries locally using DuckDB.
                                  → Submit them to Gradescope
                                  → You can submit multiple times and use your best score.

                                  Due: Sunday Sept 7th @ 11:59pm


                               https://15445.courses.cs.cmu.edu/fall2025/homework1



DATABASE SYSTEMS (FALL 2025)
                                                                                      41

                                                    NEXT CLASS
                               We will begin our journey to understanding the
                               internals of database systems starting with Storage!


                               No In-Class Lecture!

DATABASE SYSTEMS (FALL 2025)
