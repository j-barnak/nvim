# manager

Base classes for PodmanResources and Manager’s.

*class* Manager(*client: APIClient \| None = None*, *podman_client: PodmanClient \| None = None*)[\[source\]](https://podman-py.readthedocs.io/en/latest/_modules/podman/domain/manager.html#Manager)  
Bases: `ABC`

Base class for representing a Manager of resources for a Podman service.

Initialize Manager() object.

Parameters:  
- **client** – APIClient() configured to connect to Podman service.

- **podman_client** – PodmanClient() configured to connect to Podman object.

*abstractmethod* exists(*key: str*) → bool[\[source\]](https://podman-py.readthedocs.io/en/latest/_modules/podman/domain/manager.html#Manager.exists)  
Returns True if resource exists.

Podman only.

Notes

This method does \_not\_ provide any mutex mechanism.

*abstractmethod* get(*key: str*) → PodmanResourceType[\[source\]](https://podman-py.readthedocs.io/en/latest/_modules/podman/domain/manager.html#Manager.get)  
Returns representation of resource.

*abstractmethod* list(*\*\*kwargs*) → [list](#podman.domain.manager.Manager.list "podman.domain.manager.Manager.list")\[PodmanResourceType\][\[source\]](https://podman-py.readthedocs.io/en/latest/_modules/podman/domain/manager.html#Manager.list)  
Returns list of resources.

prepare_model(*attrs: [PodmanResource](#podman.domain.manager.PodmanResource "podman.domain.manager.PodmanResource") \| Mapping\[str, Any\]*) → PodmanResourceType[\[source\]](https://podman-py.readthedocs.io/en/latest/_modules/podman/domain/manager.html#Manager.prepare_model)  
Create a model from a set of attributes.

*abstract property* resource  
Class which the factory method prepare_model() will use.

Type:  
Type\[[PodmanResource](#podman.domain.manager.PodmanResource "podman.domain.manager.PodmanResource")\]

&nbsp;

*class* PodmanResource(*attrs: Mapping\[str, Any\] \| None = None*, *client: APIClient \| None = None*, *collection: [Manager](#podman.domain.manager.Manager "podman.domain.manager.Manager") \| None = None*, *podman_client: PodmanClient \| None = None*)[\[source\]](https://podman-py.readthedocs.io/en/latest/_modules/podman/domain/manager.html#PodmanResource)  
Bases: `ABC`

Base class for representing resource of a Podman service.

attrs  
Mapping of attributes for resource from Podman service

Initialize base class for PodmanResource’s.

Parameters:  
- **attrs** – Mapping of attributes for resource from Podman service.

- **client** – Configured connection to a Podman service.

- **collection** – Manager of this category of resource, named collection for compatibility

- **podman_client** – PodmanClient() configured to connect to Podman object.

reload(*\*\*kwargs*) → None[\[source\]](https://podman-py.readthedocs.io/en/latest/_modules/podman/domain/manager.html#PodmanResource.reload)  
Refresh this object’s data from the service.

Keyword Arguments:  
**compatible** (*bool*) – Use Docker compatibility endpoint

*property* id  
Returns the identifier for the object.

Type:  
str

*property* short_id  
Returns truncated identifier. ‘sha256’ preserved when included in the id.

No attempt is made to ensure the returned value is semantically meaningful for all resources.

Type:  
str
