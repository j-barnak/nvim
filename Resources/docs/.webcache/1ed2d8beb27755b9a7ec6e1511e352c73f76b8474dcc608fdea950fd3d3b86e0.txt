[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_round_ties_even Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#755)

``` rust
pub const unsafe fn simd_round_ties_even<T>(x: T) -> T
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Rounds each element to the closest integer-valued float. Ties are resolved by rounding to the number with an even least significant digit

`T` must be a vector of floats.