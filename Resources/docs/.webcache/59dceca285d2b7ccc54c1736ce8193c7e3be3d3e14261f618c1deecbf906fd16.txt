# In-Depth: Coverage Instrumentation and LLVM Hell

This in-depth section dissects how AFL++ instruments a target through LLVM, split into the next two chapters:

- **`afl-cc`** — the compiler wrapper and how it drives clang/LLVM.
- **LLVM Pass Plugins** — the instrumentation passes themselves.
