[std](https://doc.rust-lang.org/std/index.html)::[io](https://doc.rust-lang.org/std/io/index.html)

# Struct BorrowedBuf Copy item path

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#28)

``` rust
pub struct BorrowedBuf<'data, T> { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`core_io_borrowed_buf` [\#117693](https://github.com/rust-lang/rust/issues/117693))

Expand description

A borrowed buffer of initially uninitialized elements, which is incrementally filled.

This type makes it safer to work with `MaybeUninit` buffers, such as to read into a buffer without having to initialize it first. It tracks the region of elements that have been filled and whether the unfilled region was initialized.

In summary, the contents of the buffer can be visualized as:

```
[                capacity                ]
[ filled | unfilled (may be initialized) ]
```

A `BorrowedBuf` is created around some existing elements (or capacity for elements) via a unique reference (`&mut`). The `BorrowedBuf` can be configured (e.g., using `clear` or `set_init`), but cannot be directly written. To write into the buffer, use `unfilled` to create a `BorrowedCursor`. The cursor has write-only access to the unfilled portion of the buffer (you can think of it as a write-only iterator).

The lifetime `'data` is a bound on the lifetime of the underlying elements.

The type is most commonly used to manage bytes, but can manage any type of elements.

## Implementations

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#83)

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#86)

🔬This is a nightly-only experimental API. (`core_io_borrowed_buf` [\#117693](https://github.com/rust-lang/rust/issues/117693))

Returns the total capacity of the buffer.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#92)

🔬This is a nightly-only experimental API. (`core_io_borrowed_buf` [\#117693](https://github.com/rust-lang/rust/issues/117693))

Returns the length of the filled part of the buffer.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#99)

🔬This is a nightly-only experimental API. (`borrowed_buf_init` [\#78485](https://github.com/rust-lang/rust/issues/78485))

Returns `true` if the buffer is initialized.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#104)

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#107)

🔬This is a nightly-only experimental API. (`core_io_borrowed_buf` [\#117693](https://github.com/rust-lang/rust/issues/117693))

Returns a shared reference to the filled portion of the buffer.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#117)

🔬This is a nightly-only experimental API. (`core_io_borrowed_buf` [\#117693](https://github.com/rust-lang/rust/issues/117693))

Returns a mutable reference to the filled portion of the buffer.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#127)

🔬This is a nightly-only experimental API. (`core_io_borrowed_buf` [\#117693](https://github.com/rust-lang/rust/issues/117693))

Returns a shared reference to the filled portion of the buffer with its original lifetime.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#137)

🔬This is a nightly-only experimental API. (`core_io_borrowed_buf` [\#117693](https://github.com/rust-lang/rust/issues/117693))

Returns a mutable reference to the filled portion of the buffer with its original lifetime.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#147)

🔬This is a nightly-only experimental API. (`core_io_borrowed_buf` [\#117693](https://github.com/rust-lang/rust/issues/117693))

Returns a cursor over the unfilled part of the buffer.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#163)

🔬This is a nightly-only experimental API. (`core_io_borrowed_buf` [\#117693](https://github.com/rust-lang/rust/issues/117693))

Clears the buffer, resetting the filled region to empty.

The contents of the buffer are not modified.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#175)

🔬This is a nightly-only experimental API. (`borrowed_buf_init` [\#78485](https://github.com/rust-lang/rust/issues/78485))

Asserts that the unfilled part of the buffer is initialized.

##### Safety

All the elements of the buffer must be initialized.

## Trait Implementations

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#37)

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#38)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#61)

Creates a new `BorrowedBuf` from an uninitialized buffer.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#63)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#48)

Creates a new `BorrowedBuf` from a fully initialized slice.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#50)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#71)

Creates a new `BorrowedBuf` from a cursor.

Use `BorrowedCursor::with_unfilled_buf` instead for a safer alternative.

[Source](https://doc.rust-lang.org/src/core/io/borrowed_buf.rs.html#73)

Converts to this type from the input type.

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.