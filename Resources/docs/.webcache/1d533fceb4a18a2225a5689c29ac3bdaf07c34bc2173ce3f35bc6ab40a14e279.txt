[std](https://doc.rust-lang.org/std/index.html)::[collections](https://doc.rust-lang.org/std/collections/index.html)::[btree_set](https://doc.rust-lang.org/std/collections/btree_set/index.html)

# Enum Entry Copy item path

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set/entry.rs.html#42-46)

``` rust
pub enum Entry<'a, T, A = Global>where
    A: Allocator + Clone,{
    Occupied(OccupiedEntry<'a, T, A>),
    Vacant(VacantEntry<'a, T, A>),
}
```

🔬This is a nightly-only experimental API. (`btree_set_entry` [\#133549](https://github.com/rust-lang/rust/issues/133549))

Expand description

A view into a single entry in a set, which may either be vacant or occupied.

This `enum` is constructed from the [`entry`](https://doc.rust-lang.org/std/collections/struct.BTreeSet.html#method.entry "method std::collections::BTreeSet::entry") method on [`BTreeSet`](https://doc.rust-lang.org/std/collections/struct.BTreeSet.html "struct std::collections::BTreeSet").

## Examples

``` rust
#![feature(btree_set_entry)]

use std::collections::btree_set::BTreeSet;

let mut set = BTreeSet::new();
set.extend(["a", "b", "c"]);
assert_eq!(set.len(), 3);

// Existing value (insert)
let entry = set.entry("a");
let _raw_o = entry.insert();
assert_eq!(set.len(), 3);
// Nonexistent value (insert)
set.entry("d").insert();

// Existing value (or_insert)
set.entry("b").or_insert();
// Nonexistent value (or_insert)
set.entry("e").or_insert();

println!("Our BTreeSet: {:?}", set);
assert!(set.iter().eq(&["a", "b", "c", "d", "e"]));
```

## Variants

🔬This is a nightly-only experimental API. (`btree_set_entry` [\#133549](https://github.com/rust-lang/rust/issues/133549))

An occupied entry.

#### Examples

``` rust
#![feature(btree_set_entry)]

use std::collections::btree_set::{Entry, BTreeSet};

let mut set = BTreeSet::from(["a", "b"]);

match set.entry("a") {
    Entry::Vacant(_) => unreachable!(),
    Entry::Occupied(_) => { }
}
```

🔬This is a nightly-only experimental API. (`btree_set_entry` [\#133549](https://github.com/rust-lang/rust/issues/133549))

A vacant entry.

#### Examples

``` rust
#![feature(btree_set_entry)]

use std::collections::btree_set::{Entry, BTreeSet};

let mut set = BTreeSet::new();

match set.entry("a") {
    Entry::Occupied(_) => unreachable!(),
    Entry::Vacant(_) => { }
}
```

## Implementations

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set/entry.rs.html#190)

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set/entry.rs.html#207)

🔬This is a nightly-only experimental API. (`btree_set_entry` [\#133549](https://github.com/rust-lang/rust/issues/133549))

Sets the value of the entry, and returns an `OccupiedEntry`.

##### Examples

``` rust
#![feature(btree_set_entry)]

use std::collections::BTreeSet;

let mut set = BTreeSet::new();
let entry = set.entry("horseyland").insert();

assert_eq!(entry.get(), &"horseyland");
```

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set/entry.rs.html#236)

🔬This is a nightly-only experimental API. (`btree_set_entry` [\#133549](https://github.com/rust-lang/rust/issues/133549))

Ensures a value is in the entry by inserting if it was vacant.

##### Examples

``` rust
#![feature(btree_set_entry)]

use std::collections::BTreeSet;

let mut set = BTreeSet::new();

// nonexistent key
set.entry("poneyland").or_insert();
assert!(set.contains("poneyland"));

// existing key
set.entry("poneyland").or_insert();
assert!(set.contains("poneyland"));
assert_eq!(set.len(), 1);
```

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set/entry.rs.html#261)

🔬This is a nightly-only experimental API. (`btree_set_entry` [\#133549](https://github.com/rust-lang/rust/issues/133549))

Returns a reference to this entry’s value.

##### Examples

``` rust
#![feature(btree_set_entry)]

use std::collections::BTreeSet;

let mut set = BTreeSet::new();
set.entry("poneyland").or_insert();

// existing key
assert_eq!(set.entry("poneyland").get(), &"poneyland");
// nonexistent key
assert_eq!(set.entry("horseland").get(), &"horseland");
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set/entry.rs.html#87)

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set/entry.rs.html#88)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.