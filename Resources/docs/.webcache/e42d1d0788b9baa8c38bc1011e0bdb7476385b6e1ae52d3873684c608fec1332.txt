[std](https://doc.rust-lang.org/std/index.html)::[pin](https://doc.rust-lang.org/std/pin/index.html)

# Struct UnsafePinned Copy item path

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#27)

``` rust
pub struct UnsafePinned<T>where
    T: ?Sized,{ /* private fields */ }
```

🔬This is a nightly-only experimental API. (`unsafe_pinned` [\#125735](https://github.com/rust-lang/rust/issues/125735))

Expand description

This type provides a way to entirely opt-out of typical aliasing rules; specifically, `&mut UnsafePinned<T>` is not guaranteed to be a unique pointer. This also subsumes the effects of `UnsafeCell`, i.e., `&UnsafePinned<T>` may point to data that is being mutated.

However, even if you define your type like `pub struct Wrapper(UnsafePinned<...>)`, it is still very risky to have an `&mut Wrapper` that aliases anything else. Many functions that work generically on `&mut T` assume that the memory that stores `T` is uniquely owned (such as `mem::swap`). In other words, while having aliasing with `&mut Wrapper` is not immediate Undefined Behavior, it is still unsound to expose such a mutable reference to code you do not control! Techniques such as pinning via [`Pin`](https://doc.rust-lang.org/std/pin/struct.Pin.html "struct std::pin::Pin") are needed to ensure soundness.

Similar to [`UnsafeCell`](https://doc.rust-lang.org/std/cell/struct.UnsafeCell.html "struct std::cell::UnsafeCell"), `UnsafePinned` will not usually show up in the public API of a library. It is an internal implementation detail of libraries that need to support aliasing mutable references.

This type blocks niches the same way `UnsafeCell` does.

## Implementations

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#46)

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#54)

🔬This is a nightly-only experimental API. (`unsafe_pinned` [\#125735](https://github.com/rust-lang/rust/issues/125735))

Constructs a new instance of `UnsafePinned` which will wrap the specified value.

All access to the inner value through `&UnsafePinned<T>` or `&mut UnsafePinned<T>` or `Pin<&mut UnsafePinned<T>>` requires `unsafe` code.

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#63)

🔬This is a nightly-only experimental API. (`unsafe_pinned` [\#125735](https://github.com/rust-lang/rust/issues/125735))

Unwraps the value, consuming this `UnsafePinned`.

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#68)

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#73)

🔬This is a nightly-only experimental API. (`unsafe_pinned` [\#125735](https://github.com/rust-lang/rust/issues/125735))

Get read-write access to the contents of a pinned `UnsafePinned`.

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#85)

🔬This is a nightly-only experimental API. (`unsafe_pinned` [\#125735](https://github.com/rust-lang/rust/issues/125735))

Get read-write access to the contents of an `UnsafePinned`.

You should usually be using `get_mut_pinned` instead to explicitly track the fact that this memory is “pinned” due to there being aliases.

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#110)

🔬This is a nightly-only experimental API. (`unsafe_pinned` [\#125735](https://github.com/rust-lang/rust/issues/125735))

Get mutable access to the contents of a shared `UnsafePinned`.

This can be cast to a pointer of any kind. When creating references, you must uphold the aliasing rules; see [`UnsafeCell`](https://doc.rust-lang.org/std/cell/struct.UnsafeCell.html#aliasing-rules "struct std::cell::UnsafeCell") for more discussion and caveats.

``` rust
#![feature(unsafe_pinned)]
use std::pin::UnsafePinned;

unsafe {
    let mut x = UnsafePinned::new(0);
    let ptr = x.get();
    x.get_mut_unchecked().write(1);
    assert_eq!(ptr.read(), 1);
}
```

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#123)

🔬This is a nightly-only experimental API. (`unsafe_pinned` [\#125735](https://github.com/rust-lang/rust/issues/125735))

Gets an immutable pointer to the wrapped value.

The difference from [`get`](https://doc.rust-lang.org/std/pin/struct.UnsafePinned.html#method.get "method std::pin::UnsafePinned::get") is that this function accepts a raw pointer, which is useful to avoid the creation of temporary references.

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#137)

🔬This is a nightly-only experimental API. (`unsafe_pinned` [\#125735](https://github.com/rust-lang/rust/issues/125735))

Gets a mutable pointer to the wrapped value.

The difference from [`get_mut_pinned`](https://doc.rust-lang.org/std/pin/struct.UnsafePinned.html#method.get_mut_pinned "method std::pin::UnsafePinned::get_mut_pinned") and [`get_mut_unchecked`](https://doc.rust-lang.org/std/pin/struct.UnsafePinned.html#method.get_mut_unchecked "method std::pin::UnsafePinned::get_mut_unchecked") is that this function accepts a raw pointer, which is useful to avoid the creation of temporary references.

## Trait Implementations

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#39)

When this type is used, that almost certainly means safe APIs need to use pinning to avoid the aliases from becoming invalidated. Therefore let’s mark this as `!Unpin`. You can always opt back in to `Unpin` with an `impl` block, provided your API is still sound while unpinned.

[Source](https://doc.rust-lang.org/src/core/marker.rs.html#931)

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#168)

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#160)

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#161)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#143)

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#145)

Creates an `UnsafePinned`, with the `Default` value for T.

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#180)

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#152)

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#154)

Creates a new `UnsafePinned<T>` containing the given value.

[Source](https://doc.rust-lang.org/src/core/pin/unsafe_pinned.rs.html#33)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#804)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#805)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.