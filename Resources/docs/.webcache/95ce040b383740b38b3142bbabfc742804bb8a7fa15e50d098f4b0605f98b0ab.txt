# LSM-based Storage Techniques: A Survey

LSM-based storage techniques: a survey




Chen Luo & Michael J. Carey



The VLDB Journal
The International Journal on Very Large
Data Bases

ISSN 1066-8888

The VLDB Journal
DOI 10.1007/s00778-019-00555-y




                                          1 23
Your article is protected by copyright and
all rights are held exclusively by Springer-
Verlag GmbH Germany, part of Springer
Nature. This e-offprint is for personal use only
and shall not be self-archived in electronic
repositories. If you wish to self-archive your
article, please use the accepted manuscript
version for posting on your own website. You
may further deposit the accepted manuscript
version in any repository, provided it is only
made publicly available 12 months after
official publication or later and provided
acknowledgement is given to the original
source of publication and a link is inserted
to the published article on Springer's
website. The link must be accompanied by
the following text: "The final publication is
available at link.springer.com”.




1 23
                                                     Author's personal copy
The VLDB Journal
https://doi.org/10.1007/s00778-019-00555-y

    SPECIAL ISSUE PAPER



LSM-based storage techniques: a survey
Chen Luo1        · Michael J. Carey1

Received: 20 December 2018 / Revised: 17 April 2019 / Accepted: 5 July 2019
© Springer-Verlag GmbH Germany, part of Springer Nature 2019


Abstract
Recently, the log-structured merge-tree (LSM-tree) has been widely adopted for use in the storage layer of modern NoSQL
systems. Because of this, there have been a large number of research efforts, from both the database community and the
operating systems community, that try to improve various aspects of LSM-trees. In this paper, we provide a survey of recent
research efforts on LSM-trees so that readers can learn the state of the art in LSM-based storage techniques. We provide a
general taxonomy to classify the literature of LSM-trees, survey the efforts in detail, and discuss their strengths and trade-offs.
We further survey several representative LSM-based open-source NoSQL systems and discuss some potential future research
directions resulting from the survey.

Keywords LSM-tree · NoSQL · Storage management · Indexing


1 Introduction                                                                on improving LSM-trees, ranging from key-value store set-
                                                                              tings with a single LSM-tree to more general database
The log-structured merge-tree (LSM-tree) has been widely                      settings with secondary indexes. This paper aims to serve
adopted in the storage layers of modern NoSQL systems,                        as a guide to the state of the art in LSM-based storage
including BigTable [16], Dynamo [23], HBase [32], Cassan-                     techniques for researchers, practitioners, and users. We first
dra [14], LevelDB [40], RocksDB [57], and AsterixDB [4].                      provide a general taxonomy to classify the existing LSM-
Different from traditional index structures that apply in-place               tree improvements based on the specific aspects that they
updates, the LSM-tree first buffers all writes in memory and                  attempt to optimize. We then present the various improve-
subsequently flushes them to disk and merges them using                       ments in detail and discuss their strengths and trade-offs. To
sequential I/Os. This design brings a number of advantages,                   reflect how LSM-trees are being used in real systems, we
including superior write performance, high space utiliza-                     further survey five representative LSM-based open-source
tion, tunability, and simplification of concurrency control                   NoSQL systems, including LevelDB [40], RocksDB [57],
and recovery. These advantages have enabled LSM-trees to                      HBase [32], Cassandra [14], and AsterixDB [3]. Finally, we
serve a large variety of workloads. As reported by Face-                      also identify several interesting future research directions as
book [24], RocksDB, an LSM-based key-value store engine,                      the result of categorizing the existing LSM-tree improve-
has been used for real-time data processing [18], graph                       ments.
processing [19], stream processing [18], and OLTP work-                          The reminder of this paper is organized as follows.
loads [46].                                                                   Section 2 briefly reviews the history of LSM-trees and
   Due to the popularity of LSM-trees among modern data                       presents the basics of today’s LSM-tree implementations.
stores, a large number of improvements on LSM-trees have                      Section 3 presents a taxonomy of the proposed LSM-tree
been proposed by the research community; these have come                      improvements and surveys the existing work using that
from both the database and operating systems communi-                         taxonomy. Section 4 surveys some representative LSM-
ties. In this paper, we survey these recent research efforts                  based NoSQL systems, focusing on their storage layers.
                                                                              Section 5 reflects on the result of this survey, identify-
B Chen Luo                                                                    ing several outages and opportunities for future work on
     cluo8@uci.edu
                                                                              LSM-based storage systems. Finally, Sect. 6 concludes the
     Michael J. Carey                                                         paper.
     mjcarey@ics.uci.edu
1    University of California, Irvine, Irvine, USA


                                                                                                                                  123
                                               Author's personal copy
                                                                                                                         C. Luo, M. J. Carey




 (a)                            (b)                                    Fig. 2 Original LSM-tree design

Fig. 1 Examples of in-place and out-of-place update structures: each
entry contains a key (denoted as “k”) and a value (denoted as “v”)
                                                                       database storage. Postgres appended all writes into a sequen-
                                                                       tial log, enabling fast recovery and “time-travel” queries.
2 LSM-tree basics                                                      It used a background process called the vacuum cleaner to
                                                                       continuously garbage-collect obsolete records from the log.
In this section, we present the background of LSM-trees. We            Similar ideas have been adopted by the file system com-
first briefly review of the history of work on LSM-trees. We           munity to fully utilize disk write bandwidth, such as in the
then discuss in more detail the basic structure of LSM-trees           log-structured file system (LFS) [60].
as used in today’s storage systems. We conclude this sec-                 Prior to the LSM-tree, the approaches to log-structured
tion by presenting a cost analysis of writes, reads, and space         storage suffered from several key problems. First and fore-
utilization of LSM-trees.                                              most, storing data into append-only logs leads to low query
                                                                       performance, as related records are scattered across the log.
2.1 History of LSM-trees                                               Another problem is low space utilization due to obsolete
                                                                       records that have not yet been removed. Even though var-
In general, an index structure can choose one of two strategies        ious data reorganization processes were designed, there was
to handle updates, that is, in-place updates and out-of-place          no principled cost model to analyze the trade-offs among the
updates. An in-place update structure, such as a B+ -tree,             write cost, read cost, and space utilization, which made early
directly overwrites old records to store new updates. For              log-structured storage hard to tune; data reorganization could
example in Fig. 1a, to update the value associated with                easily become a performance bottleneck [62].
key k1 from v1 to v4, the index entry (k1, v1) is directly                The LSM-tree [51], proposed in 1996, addressed these
modified to apply this update. These structures are often read-        problems by designing a merge process which is integrated
optimized since only the most recent version of each record            into the structure itself, providing high write performance
is stored. However, this design sacrifices write performance,          with bounded query performance and space utilization. The
as updates incur random I/Os. Moreover, index pages can be             original LSM-tree design contains a sequence of components
fragmented by updates and deletes, thus reducing the space             C0 , C1 , . . . , Ck , as shown in Fig. 2. Each component is struc-
utilization.                                                           tured as a B+ -tree. C0 resides in memory and serves incoming
    In contrast, an out-of-place update structure, such as an          writes, while all remaining components C1 , . . . , Ck reside on
LSM-tree, always stores updates into new locations instead             disk. When Ci is full, a rolling merge process is triggered to
of overwriting old entries. For example in Fig. 1b, the update         merge a range of leaf pages from Ci into Ci+1 . This design is
(k1, v4) is stored into a new place instead of updating the old        often referred to as the leveling merge policy [20,21] today.
entry (k1, v1) directly. This design improves write perfor-            However, as we shall see later, the originally proposed rolling
mance since it can exploit sequential I/Os to handle writes.           merge process is not used by today’s LSM-based storage
It can also simplify the recovery process by not overwrit-             systems due to its implementation complexity. The original
ing old data. However, the major problem of this design                paper on LSM-trees [51] further showed that under a sta-
is that read performance is sacrificed since a record may              ble workload, where the number of levels remains static,
be stored in any of multiple locations. Furthermore, these             write performance is optimized when the size ratios Ti =
structures generally require a separate data reorganization            |Ci+1 |/|Ci | between all adjacent components are the same.
process to improve storage and query efficiency continu-               This principle has impacted all subsequent implementations
ously.                                                                 and improvements of LSM-trees.
    The idea of sequential, out-of-place updates is not new;              In parallel to the LSM-tree, Jagadish et al. [34] proposed
it has been successfully applied to database systems since             a similar structure with the stepped-merge policy to achieve
the 1970s. Differential files [63], presented in 1976, were            better write performance. It organizes the components into
an early example of an out-of-place update structure. In this          levels, and when level L is full with T components, these
design, all updates are first applied to a differential file, which    T components are merged together into a new component at
is merged with the main file periodically. Later, in the 1980s,        level L +1. This policy becomes the tiering merge policy [20,
the Postgres project [65] pioneered the idea of log-structured         21] used in today’s LSM-tree implementations.


123
                                                  Author's personal copy
LSM-based storage techniques: a survey


2.2 Today’s LSM-trees

2.2.1 Basic structure

Today’s LSM-tree implementations still apply updates out-
of-place to reduce random I/Os. All incoming writes are              (a) Leveling Merge Policy: one component per level
appended into a memory component. An insert or update
operation simply adds a new entry, while a delete oper-
ation adds an antimatter entry indicating that a key has
been deleted. However, today’s LSM-tree implementations
commonly exploit the immutability of disk components1 to
simplify concurrency control and recovery. Multiple disk
components are merged2 together into a new one with-
out modifying existing components. This is different from
the rolling merge process proposed by the original LSM-
tree [51].                                                         (b) Tiering Merge Policy: up to T components per level
    Internally, an LSM-tree component can be implemented
                                                                 Fig. 3 LSM-tree merge policies
using any index structure. Today’s LSM-tree implementa-
tions typically organize their memory components using a
concurrent data structure such as a skip list or a B+ -tree,     tiering merge policy (Fig. 3b) maintains up to T components
while they organize their disk components using B+ -trees or     per level. When level L is full, its T components are merged
sorted-string tables (SSTables). An SSTable contains a list      together into a new component at level L + 1. In the fig-
of data blocks and an index block; a data block stores key-      ure, the two components at level 0 are merged together to
value pairs ordered by keys, and the index block stores the      form a new component at level 1. It should be noted that if
key ranges of all data blocks.                                   level L is already the configured maximum level, then the
    A query over an LSM-tree has to search multiple com-         resulting component remains at level L. In practice, for a
ponents to perform reconciliation, that is, to find the latest   stable workload where the volume of inserts equal the vol-
version of each key. A point lookup query, which fetches the     ume of deletes, the total number of levels remains static.3 In
value for a specific key, can simply search all components       general, the leveling merge policy optimizes for query per-
one by one, from newest to oldest, and stop immediately          formance since there are fewer components to search in the
after the first match is found. A range query can search all     LSM-tree. The tiering merge policy is more write-optimized
components at the same time, feeding the search results into     since it reduces the merge frequency. We will discuss the
a priority queue to perform reconciliation.                      performance of these two merge policies further in Sect. 2.3.
    As disk components accumulate over time, the query
performance of an LSM-tree tends to degrade since more
components must be examined. To address this, disk com-          2.2.2 Some well-known optimizations
ponents are gradually merged to reduce the total number of
components. Two types of merge policies are typically used       There are two well-known optimizations that are used by
in practice [20,21]. As shown in Fig. 3, both policies orga-     most LSM-tree implementations today.
nize disk components into logical levels (or tiers) and are         Bloom filter A Bloom filter [12] is a space-efficient
controlled by a size ratio T. Each component is labeled with     probabilistic data structure designed to aid in answering set
its potential key range in the figure. In the leveling merge     membership queries. It supports two operations, i.e., insert-
policy (Fig. 3a), each level only maintains one component,       ing a key and testing the membership of a given key. To insert
but the component at level L is T times larger than the com-     a key, it applies multiple hash functions to map the key into
ponent at level L − 1. As a result, the component at level       multiple locations in a bit vector and sets the bits at these
L will be merged multiple times with incoming components         locations to 1. To check the existence of a given key, the key
at level L − 1 until it fills up, and it will then be merged     is again hashed to multiple locations. If all of the bits are
into level L + 1. For example in the figure, the component       1, then the Bloom filter reports that the key probably exists.
at level 0 is merged with the component at level 1, which
will result in a bigger component at level 1. In contrast, the   3 Even for an append-mostly workload, the total number of levels will

1 Also referred to as runs in the literature.
                                                                 grow extremely slowly since the maximum number of entries that an
                                                                 LSM-tree can store increases exponentially with a factor of T as the
2 Also referred to as compaction in the literature.              number of levels increases.


                                                                                                                          123
                                             Author's personal copy
                                                                                                                          C. Luo, M. J. Carey


By design, the Bloom filter can report false positives but not
false negatives.
    Bloom filters can be built on top of disk components to
greatly improve point lookup performance. To search a disk
component, a point lookup query can first check its Bloom
filter and then proceed to search its B+ -tree only if its asso-
ciated Bloom filter reports a positive answer. Alternatively, a
Bloom filter can be built for each leaf page of a disk compo-
nent. In this design, a point lookup query can first search the
non-leaf pages of a B+ -tree to locate the leaf page, where the
non-leaf pages are assumed to be small enough to be cached,
and then check the associated Bloom filter before fetching           Fig. 4 Partitioned leveling merge policy
the leaf page to reduce disk I/Os. Note that the false positives
reported by a Bloom filter do not impact the correctness of a
                                                                     However, this design enforces strict key range boundaries
query, but a query may waste some I/O searching for nonex-
                                                                     among partitions, which reduces the flexibility of merges.
istent keys. The false positive rate of a Bloom filter can be
                                                                         We now discuss the partitioning optimization used in
computed as (1 − e−kn/m )k , where k is the number of hash
                                                                     today’s LSM-tree implementations. It should be noted that
functions, n is the number of keys, and m is the total number of
                                                                     partitioning is orthogonal to merge policies; both leveling
bits [12]. Furthermore, the optimal number of hash functions
                                                                     and tiering (as well as other emerging merge policies) can be
that minimizes the false positive rate is k = mn ln2. In practice,
                                                                     adapted to support partitioning. To the best of our knowl-
most systems typically use 10 bits/key as a default config-
                                                                     edge, only the partitioned leveling policy has been fully
uration, which gives a 1% false positive rate. Since Bloom
                                                                     implemented by industrial LSM-based storage systems, such
filters are very small and can often be cached in memory, the
                                                                     as LevelDB [40] and RocksDB [57]. Some recent papers
number of disk I/Os for point lookups is greatly reduced by
                                                                     [6,49,58,76,79] have proposed various forms of a partitioned
their use.
                                                                     tiering merge policy to achieve better write performance.4
    Partitioning Another commonly adopted optimization is
                                                                         In the partitioned leveling merge policy, pioneered by
to range-partition the disk components of LSM-trees into
                                                                     LevelDB [40], the disk component at each level is range-
multiple (usually fixed-size) small partitions. To minimize
                                                                     partitioned into multiple fixed-size SSTables, as shown in
the potential confusion caused by different terminologies, we
                                                                     Fig. 4. Each SSTable is labeled with its key range in the
use the term SSTable to denote such a partition, following the
                                                                     figure. Note that the disk components at level 0 are not par-
terminology from LevelDB [40]. This optimization has sev-
                                                                     titioned since they are directly flushed from memory. This
eral advantages. First, partitioning breaks a large component
                                                                     design can also help the system to absorb write bursts since
merge operation into multiple smaller ones, bounding the
                                                                     it can tolerate multiple unpartitioned components at level 0.
processing time of each merge operation as well as the tempo-
                                                                     To merge an SSTable from level L into level L + 1, all of its
rary disk space needed to create new components. Moreover,
                                                                     overlapping SSTables at level L + 1 are selected, and these
partitioning can optimize for workloads with sequentially
                                                                     SSTables are merged with it to produce new SSTables still at
created keys or skewed updates by only merging components
                                                                     level L + 1. For example, in the figure, the SSTable labeled
with overlapping key ranges. For sequentially created keys,
                                                                     0–30 at level 1 is merged with the SSTables labeled 0–15 and
essentially no merge is performed since there are no compo-
                                                                     16–32 at level 2. This merge operation produces new SSTa-
nents with overlapping key ranges. For skewed updates, the
                                                                     bles labeled 0–10, 11–19, and 20–32 at level 2, and the old
merge frequency of the components with cold update ranges
                                                                     SSTables will then be garbage-collected. Different policies
can be greatly reduced. It should be noted that the original
                                                                     can be used to select which SSTable to merge next at each
LSM-tree [51] automatically takes advantage of partitioning
                                                                     level. For example, LevelDB uses a round-robin policy (to
because of its rolling merges. However, due to the imple-
                                                                     minimize the total write cost).
mentation complexity of its rolling merges, today’s LSM-tree
                                                                         The partitioning optimization can also be applied to the
implementations typically opt for actual physical partitioning
                                                                     tiering merge policy. However, one major issue in doing so is
rather than rolling merges.
                                                                     that each level can contain multiple SSTables with overlap-
    An early proposal that applied partitioning to LSM-trees
                                                                     ping key ranges. These SSTables must be ordered properly
is the partitioned exponential file (PE file) [35]. A PE file
contains multiple partitions, where each partition can be log-       4 RocksDB supports a limited form of a partitioned tiering merge policy
ically viewed as a separate LSM-tree. A partition can be
                                                                     to bound the maximum size of each SSTable due to its internal restric-
further split into two partitions when it becomes too large.         tions. However, the disk space may still be doubled temporarily during
                                                                     large merges.


123
                                                    Author's personal copy
LSM-based storage techniques: a survey




Fig. 5 Partitioned tiering with vertical grouping
                                                                  Fig. 6 Partitioned tiering with horizontal grouping


based on their recency to ensure correctness. Two possi-          partitioned into a set of fixed-size SSTables, serves as a
ble schemes can be used to organize the SSTables at each          logical group directly. Each level L further maintains an
level, namely vertical grouping and horizontal grouping. In       active group, which is also the first group, to receive new
both schemes, the SSTables at each level are organized into       SSTables merged from the previous level. This active group
groups. The vertical grouping scheme groups SSTables with         can be viewed as a partial component being formed by merg-
overlapping key ranges together so that the groups have dis-      ing the components at level L −1 in the unpartitioned case. A
joint key ranges. Thus, it can be viewed as an extension of       merge operation selects the SSTables with overlapping key
partitioned leveling to support tiering. Alternatively, under     ranges from all of the groups at a level, and the resulting
the horizontal grouping scheme, each logical disk compo-          SSTables are added to the active group at the next level. For
nent, which is range-partitioned into a set of SSTables, serves   example in the figure, the SSTables labeled 35–70 and 35–
as a group directly. This allows a disk component to be           65 at level 1 are merged together, and the resulting SSTables
formed incrementally based on the unit of SSTables. We will       labeled 35–52 and 53–70 are added to the first group at level
discuss these two schemes in detail below.                        2. However, although SSTables are fixed-size under the hor-
   An example of the vertical grouping scheme is shown in         izontal grouping scheme, it is still possible that one SSTable
Fig. 5. In this scheme, SSTables with overlapping key ranges      from a group may overlap a large number of SSTables in the
are grouped together so that the groups have disjoint key         remaining groups.
ranges. During a merge operation, all of the SSTables in a
group are merged together to produce the resulting SSTables       2.2.3 Concurrency control and recovery
based on the key ranges of the overlapping groups at the next
level, which are then added to these overlapping groups. For      We now briefly discuss the concurrency control and recovery
example in the figure, the SSTables labeled 0–30 and 0–31 at      techniques used by today’s LSM-tree implementations. For
level 1 are merged together to produce the SSTables labeled       concurrency control, an LSM-tree needs to handle concur-
0–12 and 17–31, which are then added to the overlapping           rent reads and writes and to take care of concurrent flush and
groups at level 2. Note the difference between the SSTables       merge operations. Ensuring correctness for concurrent reads
before and after this merge operation. Before the merge oper-     and writes is a general requirement for access methods in
ation, the SSTables labeled 0–30 and 0–31 have overlapping        a database system. Depending on the transactional isolation
key ranges and both must be examined together by a point          requirement, today’s LSM-tree implementations either use
lookup query. However, after the merge operation, the SSTa-       a locking scheme [3] or a multi-version scheme [14,32,57].
bles labeled 0–12 and 17–31 have disjoint key ranges and          A multi-version scheme works well with an LSM-tree since
only one of them needs to be examined by a point lookup           obsolete versions of a key can be naturally garbage-collected
query. It should also be noted that under this scheme SSTa-       during merges. Concurrent flush and merge operations, how-
bles are no longer fixed-size since they are produced based       ever, are unique to LSM-trees. These operations modify the
on the key ranges of the overlapping groups at the next level.    metadata of an LSM-tree, e.g., the list of active components.
   Figure 6 shows an example of the horizontal grouping           Thus, accesses to the component metadata must be properly
scheme. In this scheme, each component, which is range-           synchronized. To prevent a component in use from being


                                                                                                                        123
                                             Author's personal copy
                                                                                                                           C. Luo, M. J. Carey


deleted, each component can maintain a reference counter.               The write cost, which is also referred to as write ampli-
Before accessing the components of an LSM-tree, a query             fication in the literature, measures the amortized I/O cost of
can first obtain a snapshot of active components and incre-         inserting an entry into an LSM-tree. It should be noted that
ment their in-use counters.                                         this cost measures the overall I/O cost for this entry to be
    Since all writes are first appended into memory, write-         merged into the largest level since inserting an entry into
ahead logging (WAL) can be performed to ensure their                memory does not incur any disk I/O. For leveling, a compo-
durability. To simplify the recovery process, existing systems      nent at each level will be merged T − 1 times until it fills
typically employ a no-steal buffer management policy [33].          up and is pushed to the next level. For tiering, multiple com-
That is, a memory component can only be flushed when all            ponents at each level are merged only once and are pushed
active write transactions have terminated. During recovery          to the next level directly. Since each disk page contains B
for an LSM-tree, the transaction log is replayed to redo all        entries, the write cost for each entry will be O(T · BL ) for
successful transactions, but no undo is needed due to the no-       leveling and O( BL ) for tiering.
steal policy. Meanwhile, the list of active disk components             The I/O cost of a query depends on the number of com-
must also be recovered in the event of a crash. For unpar-          ponents in an LSM-tree. Without Bloom filters, the I/O cost
titioned LSM-trees, this can be accomplished by adding a            of a point lookup will be O(L) for leveling and O(T · L) for
pair of timestamps to each disk component that indicate the         tiering. However, Bloom filters can greatly improve the point
range of timestamps of the stored entries. This timestamp           lookup cost. For a zero-result point lookup, i.e., for a search
can be simply generated using local wall-clock time or a            for a nonexistent key, all disk I/Os are caused by Bloom fil-
monotonic sequence number. To reconstruct the component             ter false positives. Suppose all Bloom filters have M bits in
list, the recovery process can simply find all components           total and have the same false positive rate across all levels.
with disjoint timestamps. In the event that multiple compo-         With N total keys, each Bloom filter has a false positive rate
                                                                              M
nents have overlapping timestamps, the component with the           of O(e− N ) [12]. Thus, the I/O cost of a zero-result point
largest timestamp range is chosen and the rest can simply be                                    M
                                                                    lookup will be O(L · e− N ) for leveling and O(T · L · e− N )
                                                                                                                                 M

deleted since they will have been merged to form the selected       for tiering. To search for an existing unique key, at least one
component. For partitioned LSM-trees, this timestamp-based          I/O must be performed to fetch the entry. Given that in prac-
approach does not work anymore since each component is              tice the Bloom filter false positive rate is much smaller than
further range-partitioned. To address this, a typical approach,     1, the successful point lookup I/O cost for both leveling and
used in LevelDB [40] and RocksDB [57], is to maintain a             tiering will be O(1).
separate metadata log to store all changes to the structural            The I/O cost of a range query depends on the query selec-
metadata, such as adding or deleting SSTables. The state of         tivity. Let s be the number of unique keys accessed by a
the LSM-tree structure can then be reconstructed by replay-         range query. A range query can be considered to be long if
ing the metadata log during recovery.
                                                                     B > 2 · L, otherwise it is short [20,21]. The distinction is
                                                                     s

                                                                    that the I/O cost of a long range query will be dominated
2.3 Cost analysis                                                   by the largest level since the largest level contains most of
                                                                    the data. In contrast, the I/O cost of a short range query will
To help understand the performance trade-offs of LSM-trees,         derive (almost) equally from all levels since the query must
we can turn to the cost analysis of writes, point lookups,          issue one I/O to each disk component. Thus, the I/O cost of
range queries, and space amplification presented in [20,21].        a long range query will be O( Bs ) for leveling and O(T · Bs )
The cost of writes and queries is measured by counting the          for tiering. For a short range query, the I/O cost will be O(L)
number of disk I/Os per operation. This analysis considers          for leveling and O(T · L) for tiering.
an unpartitioned LSM-tree and represents a worst-case cost.             Finally, let us examine the space amplification of an LSM-
   Let the size ratio of a given LSM-tree be T, and suppose the     tree, which is defined as the overall number of entries divided
LSM-tree contains L levels. In practice, for a stable LSM-tree      by the number of unique entries.5 For leveling, the worst case
where the volume of inserts equals the volume of deletes, L         occurs when all of the data at the first L − 1 levels, which
remains static. Let B denote the page size, that is, the number     contain approximately T1 of the total data, are updates to
of entries that each data page can store, and let P denote          the entries at the largest level. Thus, the worst-case space
the number of pages of a memory component. As a result, a           amplification for leveling is O( T T+1 ). For tiering, the worst
memory component will contain at most B · P entries, and            case happens when all of the components at the largest level
level i(i ≥ 0) will contain at most T i+1 ·B· P entries. Given N
total entries, the largest level contains approximately N · T T+1   5 The original analysis presented in [20,21] defines the space amplifica-
entries since it is T times larger than the previous level. Thus,
                                                                    tion to be the overall number of obsolete entries divided by the number
the number of levels for N entries can be approximated as           of unique entries. We slightly modified the definition to ensure that the
L = logT ( B·P N
                    · T T+1 ).                                     space amplification is no less than 1.


123
                                                    Author's personal copy
LSM-based storage techniques: a survey


Table 1 Summary of cost complexity of LSM-trees
Merge policy      Write          Point lookup (zero-result/nonzero-result)      Short range query     Long range query    Space amplification

                                                                                                                           O( T T+1 )
                                          M
Leveling          O(T · BL )     O(L · e− N )/O(1)                               O(L)                 O( Bs )
                                               −M
Tiering           O( BL )        O(T · L · e    N   )/O(1)                       O(T · L)             O(T · Bs )           O(T )



contain exactly the same set of keys. As a result, the worst-                    Write amplification Even though LSM-trees can provide
case space amplification for tiering will be O(T ). In practice,             much better write throughput than in-place update structures
the space amplification is an important factor to consider                   such as B+ -trees by reducing random I/Os, the leveling merge
when deploying storage systems [24], as it directly impacts                  policy, which has been adopted by modern key-value stores
the storage cost for a given workload.                                       such as LevelDB [40] and RocksDB [57], still incurs rela-
   The cost complexity of LSM-trees is summarized in                         tively high write amplification. High write amplification not
Table 1. Note how the size ratio T impacts the performance                   only limits the write performance of an LSM-tree but also
of leveling and tiering differently. In general, leveling is                 reduces the lifespan of SSDs due to frequent disk writes. A
optimized for query performance and space utilization by                     large body of research has been conducted to reduce the write
maintaining one component per level. However, components                     amplification of LSM-trees.
must be merged more frequently, which will incur a higher                        Merge operations Merge operations are critical to the
write cost by a factor of T . In contrast, tiering is optimized              performance of LSM-trees and must therefore be carefully
for write performance by maintaining up to T components                      implemented. Moreover, merge operations can have negative
at each level. This, however, will decrease query perfor-                    impacts on the system, including buffer cache misses after
mance and worsen space utilization by a factor of T . As                     merges and write stalls during large merges. Several improve-
one can see, the LSM-tree is highly tunable. For example, by                 ments have been proposed to optimize merge operations to
changing the merge policy from leveling to tiering, one can                  address these problems.
greatly improve write performance with only a small negative                     Hardware In order to maximize performance, LSM-trees
impact on point lookup queries due to the Bloom filters. How-                must be carefully implemented to fully utilize the under-
ever, range queries and space utilization will be significantly              ling hardware platforms. The original LSM-tree has been
impacted. As we proceed to examine the recent literature on                  designed for hard disks, with the goal being reducing random
improving LSM-trees, we will see that each makes certain                     I/Os. In recent years, new hardware platforms have presented
performance trade-offs. Actually, based on the RUM conjec-                   new opportunities for database systems to achieve better per-
ture [8], each access method has to make certain trade-offs                  formance. A significant body of recent research has been
among the read cost (R), update cost (U), and memory or stor-                devoted to improving LSM-trees to fully exploit the under-
age cost (M). It will be important for the reader to keep in                 ling hardware platforms, including large memory, multi-core,
mind the cost complexity described here to better understand                 SSD/NVM, and native storage.
the trade-offs made by the proposed improvements.                                Special workloads In addition to hardware opportunities,
                                                                             certain special workloads can also be considered to achieve
                                                                             better performance in those use cases. In this case, the basic
3 LSM-tree improvements                                                      LSM-tree implementation must be adapted and customized
                                                                             to exploit the unique characteristics exhibited by these special
In this section, we present a taxonomy for use in classifying                workloads.
the existing research efforts on improving LSM-trees. We                         Auto-tuning Based on the RUM conjecture [8], no access
then provide an in-depth survey of the LSM-tree literature                   method can be read-optimal, write-optimal, and space-
that follows the structure of the proposed taxonomy.                         optimal at the same time. The tunability of LSM-trees is a
                                                                             promising solution to achieve optimal trade-offs for a given
                                                                             workload. However, LSM-trees can be hard to tune because
3.1 A taxonomy of LSM-tree improvements                                      of too many tuning knobs, such as memory allocation, merge
                                                                             policy, size ratio, etc. To address this issue, several auto-
Despite the popularity of LSM-trees in modern NoSQL                          tuning techniques have been proposed in the literature.
systems, the basic LSM-tree design suffers from various                          Secondary indexing A given LSM-tree only provides a
drawbacks and insufficiencies. We now identify the major                     simple key-value interface. To support the efficient process-
issues of the basic LSM-tree design and further present a                    ing of queries on non-key attributes, secondary indexes must
taxonomy of LSM-tree improvements based on these draw-                       be maintained. One issue in this area is how to maintain a set
backs.


                                                                                                                                    123
                                           Author's personal copy
                                                                                                                 C. Luo, M. J. Carey




Fig. 7 Taxonomy of LSM-tree improvements


of related secondary indexes efficiently with a small over-       new SSTables that are added into its child nodes. When a leaf
head on write performance. Various LSM-based secondary            node becomes full with T SSTables, it is split into two leaf
indexing structures and techniques have been designed and         nodes by merging all of its SSTables into two leaf nodes with
evaluated as well.                                                smaller key ranges so that each new node receives about T /2
   Based on these major issues of the basic LSM-tree design,      SSTables.
we present a taxonomy of LSM-tree improvements, shown                The lightweight compaction tree (LWC-tree) [78,79]
in Fig. 7, to highlight the specific aspects that the existing    adopts a similar partitioned tiering design with vertical
research efforts try to optimize. Given this taxonomy, Table 2    grouping. It further presents a method to achieve workload
further classifies the LSM-tree improvements in terms of          balancing of SSTable groups. Recall that under the vertical
each improvement’s primary and secondary concerns. With           grouping scheme, SSTables are no longer strictly fixed-size
this taxonomy and classification in hand, we now proceed to       since they are produced based on the key ranges of the over-
examine each improvement in more depth.                           lapping groups at the next level instead of based on their
                                                                  sizes. In the LWC-tree, if a group contains too many entries,
                                                                  it will shrink the key range of this group after the group has
3.2 Reducing write amplification
                                                                  been merged (now temporarily empty) and will widen the
                                                                  key ranges of its sibling groups accordingly.
In this section, we review the improvements in the litera-
                                                                     PebblesDB [58] also adopts a partitioned tiering design
ture that aim to reduce the write amplification of LSM-trees.
                                                                  with vertical grouping. The major difference is that it deter-
Most of these improvements are based on tiering since it has
                                                                  mines the key ranges of SSTable groups using the idea of
much better write performance than leveling. Other proposed
                                                                  guards as inspired by the skip list [54]. Guards, which are
improvements have developed new techniques to perform
                                                                  the key ranges of SSTable groups, are selected probabilis-
merge skipping or to exploit data skews.
                                                                  tically based on inserted keys to achieve workload balance.
                                                                  Once a guard is selected, it is applied lazily during the next
3.2.1 Tiering                                                     merge. PebblesDB further performs parallel seeks of SSTa-
                                                                  bles to improve range query performance.
One way to optimize write amplification is to apply tiering          dCompaction [52] introduces the concept of virtual SSTa-
since it has much lower write amplification than leveling.        bles and virtual merges to reduce the merge frequency. A
However, recall from Sect. 2.3 that this will lead to worse       virtual merge operation produces a virtual SSTable that sim-
query performance and space utilization. The improvements         ply points to the input SSTables without performing actual
in this category can all be viewed as some variants of the        merges. However, since a virtual SSTable points to multiple
partitioned tiering design with vertical or horizontal grouping   SSTables with overlapping ranges, query performance will
discussed in Sect. 2.2.2. Here we will mainly discuss the         degrade. To address this, dCompaction introduces a thresh-
modifications made by these improvements.                         old based on the number of real SSTables to trigger actual
   The WriteBuffer (WB) Tree [6] can be viewed as a vari-         merges. It also lets queries trigger actual merges if a virtual
ant of the partitioned tiering design with vertical grouping.     SSTable pointing to too many SSTables is encountered during
It has made the following modifications. First, it relies on      query processing. In general, dCompaction delays a merge
hash-partitioning to achieve workload balance so that each        operation until multiple SSTables can be merged together,
SSTable group roughly stores the same amount of data. Fur-        and thus it can also be viewed as a variant of the tiering
thermore, it organizes SSTable groups into a B+ -tree-like        merge policy.
structure to enable self-balancing to minimize the total num-        As one can see, the four structures described above all
ber of levels. Specifically, each SSTable group is treated like   share a similar high-level design based on partitioned tiering
a node in a B+ -tree. When a non-leaf node becomes full with      with vertical grouping. They mainly differ in how workload
T SSTables, these T SSTables are merged together to form


123
                                                  Author's personal copy
LSM-based storage techniques: a survey


Table 2 Classification of existing LSM-tree improvements (♣ denotes primary category,  denotes secondary categories)
Publication                 Write amplification    Merge operations   Hardware     Special workloads     Auto-tuning    Secondary indexing

WB-tree [6]                 ♣
LWC-tree [78,79]            ♣
PebblesDB [58]              ♣
dCompaction [52]            ♣                                                                            
Zhang et al. [82]           ♣
SifrDB [49]                 ♣                                        
Skip-tree [81]              ♣
TRIAD [10]                  ♣
VT-tree [64]                                       ♣
Zhang et al. [84]                                  ♣
Ahmad et al. [2]                                   ♣
LSbM-tree [68,69]                                  ♣
bLSM [61]                                          ♣
FloDB [9]                                                            ♣
Accordion [13]                                                       ♣
cLSM [30]                                                             ♣
FD-tree [42]                                                          ♣
FD+tree [71]                                                         ♣
MaSM [7]                                                             ♣            
WiscKey [44]                                                         ♣
HashKV [15]                                                          ♣
Kreon [53]                                                           ♣
NoveLSM [36]                                                          ♣
LDS [48]                                                              ♣
LOCS [74]                                                             ♣
NoFTL-KV [73]                                                         ♣
LHAM [50]                                                                          ♣
LSM-trie [76]                                                                     ♣
SlimDB [59]                                                                       ♣
Mathieu et al. [47]                                                               ♣
Lim et al. [43]                                                                                          ♣
Monkey [21,22]                                                                                           ♣
Dostoevsky [20]                                                                                         ♣
Thonangi and Yang [70]                                                                                  ♣
ElasticBF [83]                                                                                           ♣
Mutant [80]                                                                                              ♣
LSII [75]                                                                                                               ♣
Kim et al. [39]                                                                                                         ♣
Filter [5]                                                                                                              ♣
Qader et al. [56]                                                                                                       ♣
Diff-Index [66]                                                                                                         ♣
DELI [67],                                                                                                              ♣
Luo and Carey [45]                                                                                                      ♣
Ildar et al. [1]                                                                                                        ♣
Joseph et al. [25]                                                                                                      ♣
Zhu et al. [85]                                                                                                         ♣
Duan et al. [26]                                                                                                        ♣




                                                                                                                               123
                                               Author's personal copy
                                                                                                                       C. Luo, M. J. Carey


                                                                        duces non-trivial implementation complexity to manage the
                                                                        mutable buffers. Moreover, since merge skipping essentially
                                                                        reduces some merges at the intermediate levels, it is not clear
                                                                        how the skip-tree would compare against a well-tuned LSM-
                                                                        tree by reducing the size ratio.

                                                                        3.2.3 Exploiting data skew
Fig. 8 Merge in skip-tree: entries from a lower level can be directly
pushed to the mutable buffer of a higher level                          TRIAD [10] reduces write amplification for skewed update
                                                                        workloads where some hot keys are updated frequently. The
                                                                        basic idea is to separate hot keys from cold keys in the mem-
balancing of SSTable groups is performed. For example, the              ory component so that only cold keys are flushed to disk.
WB-tree [6] relies on hashing, but doing so gives up the                As a result, when hot keys are updated, old versions can be
ability of supporting range queries. The LWC-tree [78,79]               discarded directly without writing them to disk. Even though
dynamically shrinks the key ranges of dense SSTable groups,             hot keys are not flushed to disk, they are periodically copied
while PebblesDB [58] relies on probabilistically selected               to a new transaction log so that the old transaction log can be
guards. In contrast, dCompaction [52] offers no built-in sup-           reclaimed. TRIAD also reduces write amplification by delay-
port for workload balancing. It is not clear how skewed                 ing merges at level 0 until level 0 contains multiple SSTables.
SSTable groups would impact the performance of these                    Finally, it presents an optimization that avoids creating new
structures, and future research is needed to understand this            disk components after flushes. Instead, the transaction log
problem and evaluate these workload balancing strategies.               itself is used as a disk component and an index structure is
   The partitioned tiering design with horizontal grouping              built on top of it to improve lookup performance. However,
has been adopted by Zhang et al. [82] and SifrDB [49].                  range query performance will still be negatively impacted
SifrDB also proposes an early-cleaning technique to reduce              since entries are not sorted in the log.
disk space utilization during merges. During a merge opera-
tion, SifrDB incrementally activates newly produced SSTa-               3.2.4 Summary
bles and deactivates the old SSTables. SifrDB further exploits
I/O parallelism to speedup query performance by examining               Tiering has been widely used to improve the write perfor-
multiple SSTables in parallel.                                          mance of LSM-trees, but this will decrease query perfor-
                                                                        mance and space utilization, as discussed in Sect. 2.3. The
3.2.2 Merge skipping                                                    existing tiering-based improvements mainly differ in how
                                                                        SSTables are managed, either by vertical grouping [6,52,58,
The skip-tree [81] proposes a merge skipping idea to improve            78,79] or horizontal grouping [49,82]. It is not clear how
write performance. The observation is that each entry must              these different grouping schemes impact system performance
be merged from level 0 down to the largest level. If some               and it would be useful as future work to study and evaluate
entries can be directly pushed to a higher level by skipping            their impact. The skip-tree [81] and TRIAD [10] propose
some level-by-level merges, then the total write cost will              several new ideas to improve write performance, ideas that
be reduced. As shown in Fig. 8, during a merge at level L,              are orthogonal to tiering. However, these optimizations bring
the skip-tree directly pushes some keys to a mutable buffer             non-trivial implementation complexity to real systems, such
at level L + K so that some level-by-level merges can be                as the mutable buffers introduced by the skip-tree and the use
skipped. Meanwhile, the skipped entries in the mutable buffer           of transaction logs as flushed components by TRIAD.
will be merged with the SSTables at level L + K during sub-                All of the improvements in this category, as well as
sequent merges. To ensure correctness, a key from level L               some improvements in the later sections, have claimed that
can be pushed to level L + K only if this key does not appear           they can greatly improve the write performance of LSM-
in any of the intermediate levels L + 1, . . . , L + K − 1. This        trees, but their performance evaluations have often failed to
condition can be tested efficiently by checking the Bloom               consider the tunability of LSM-trees. That is, these improve-
filters of the intermediate levels. The skip-tree further per-          ments have mainly been evaluated against a default (untuned)
forms write-ahead logging to ensure durability of the entries           configuration of LevelDB or RocksDB, which use the lev-
stored in the mutable buffer. To reduce the logging overhead,           eling merge policy with a size ratio of 10. It is not clear
the skip-tree only logs the key plus the ID of the original             how these improvements would compare against well-tuned
SSTable and prevents an SSTable from being deleted if it is             LSM-trees. To address this, one possible solution would be
referenced by any key in the buffer. Although merge skipping            to tune RocksDB to achieve a similar write throughput to
is an interesting idea to reduce write amplification, it intro-         the proposed improvements by changing the size ratio or


123
                                             Author's personal copy
LSM-based storage techniques: a survey


by adopting the tiering merge policy and then evaluating
how these improvements can improve query performance
and space amplification. Moreover, these improvements have
primarily focused on query performance; space amplification
has often been neglected. It would be a useful experimental
study to fully evaluate these improvements against well-            Fig. 9 Pipelined merge example: multiple input pages can be processed
tuned baseline LSM-trees to evaluate their actual usefulness.       in a pipelined fashion
We also hope that this situation can be avoided in future
research by considering the tunability of LSM-trees when
evaluating the proposed improvements.                               3.3.2 Reducing buffer cache misses

                                                                    Merge operations can interfere with the caching behavior of
3.3 Optimizing merge operations
                                                                    a system. After a new component is enabled, queries may
                                                                    experience a large number of buffer cache misses since the
Next we review some existing work that improves the imple-
                                                                    new component has not been cached yet. A simple write-
mentation of merge operations, including improving merge
                                                                    through cache maintenance policy cannot solve this problem.
performance, minimizing buffer cache misses, and eliminat-
                                                                    If all of the pages of the new component were cached during
ing write stalls.
                                                                    a merge operation, a lot of other working pages would be
                                                                    evicted, which will again cause buffer cache misses.
3.3.1 Improving merge performance                                       Ahmad et al. [2] conducted an experimental study of the
                                                                    impact of merge operations on system performance. They
The VT-tree [64] presents a stitching operation to improve          found that merge operations consume a large number of
merge performance. The basic idea is that when merging              CPU and I/O resources and impose a high overhead on query
multiple SSTables, if the key range of a page from an input         response times. To address this, this work proposed to offload
SSTable does not overlap the key ranges of any pages from           large merges to remote servers to minimize their impact.
other SSTables, then this page can be simply pointed to by          After a merge operation is completed, a smart cache warm-up
the resulting SSTable without reading and copying it again.         algorithm is used to fetch the new component incrementally
Even though stitching improves merge performance for cer-           to minimize buffer cache misses. The idea is to switch to the
tain workloads, it has a number of drawbacks. First, it can         new component incrementally, chunk by chunk, to smoothly
cause fragmentation since pages are no longer continuously          redirect incoming queries from the old components to the new
stored on disk. To alleviate this problem, the VT-tree intro-       component. As a result, the burst of buffer cache misses is
duces a stitching threshold K so that a stitching operation is      decomposed into a large number of smaller ones, minimiz-
triggered only when there are at least K continuous pages           ing the negative impact of component switching on query
from an input SSTable. Moreover, since the keys in stitched         performance.
pages are not scanned during a merge operation, a Bloom                 One limitation of the approach proposed by Ahmad et
filter cannot be produced. To address this issue, the VT-tree       al. [2] is that merge operations must be offloaded to separate
uses quotient filters [11] since multiple quotient filters can      servers. The incremental warm-up algorithm alone was sub-
be combined directly without accessing the original keys.           sequently found to be insufficient due to contention between
    Zhang et al. [84] proposed a pipelined merge implemen-          the newly produced pages and the existing hot pages [68,69].
tation to better utilize CPU and I/O parallelism to improve         To address this limitation, the log-structured buffered Merge-
merge performance. The key observation is that a merge oper-        tree (LSbM-tree) [68,69] proposes an alternative approach.
ation contains multiple phases, including the read phase,           As illustrated in Fig. 10, after an SSTable at level L is merged
merge-sort phase, and write phase. The read phase reads             into level L + 1, the old SSTables at level L is appended to
pages from input SSTables, which will then be merge-sorted          a buffer associated with level L + 1 instead of being deleted
to produce new pages during the merge-sort phase. Finally,          immediately. Note that there is no need to add old SSTables
the new pages will be written to disk during the write phase.       at level L + 1 into the buffer, as the SSTables at L + 1 all
Thus, the read phase and write phase are I/O heavy while the        come from level L and the entries of these old SSTables will
merge-sort phase is CPU heavy. To better utilize CPU and I/O        have already been added to the buffer before. The buffered
parallelism, the proposed approach pipelines the execution          SSTables are searched by queries as well to minimize buffer
of these three phases, as illustrated in Fig. 9. In this example,   cache misses, and they are deleted gradually based on their
after the first input page has been read, this approach contin-     access frequency. This approach does not incur any extra disk
ues reading the second input page (using disk) and the first        I/O during a merge operation since it only delays the deletion
page can be merge-sorted (using CPU).                               of the old SSTables. However, this approach is mainly effec-


                                                                                                                             123
                                              Author's personal copy
                                                                                                                       C. Luo, M. J. Carey


                                                                      by merges. However, both approaches appear to have cer-
                                                                      tain limitations. The approach proposed by Ahmed et al. [2]
                                                                      requires dedicated servers to perform merges, while the
                                                                      LSbM-tree [68,69] delays the deletion of old components
                                                                      that could negatively impact queries accessing cold data.
Fig. 10 LSbM-tree: the merged component is added to a buffer of the   Write stalls are a unique problem of LSM-trees due to its
next level instead of being deleted immediately                       out-of-place update nature. bLSM [61] is the only effort that
                                                                      attempts to address this problem. However, bLSM [61] only
                                                                      bounds the maximum latency of writing to memory com-
tive for skewed workloads where only a small range of keys
                                                                      ponents. The end-to-end write latency can still exhibit large
are frequently accessed. It can introduce extra overhead for
                                                                      variances due to queuing. More efforts need to be done to
queries accessing cold data that are not cached, especially for
                                                                      improve the performance stability of LSM-trees.
range queries since they cannot benefit from Bloom filters.

3.3.3 Minimizing write stalls                                         3.4 Hardware opportunities

Although the LSM-tree offers a much higher write through-             We now review the LSM-tree improvements proposed for
put compared to traditional B+ -trees, it often exhibits write        different hardware platforms, including large memory, multi-
stalls and unpredictable write latencies since heavy oper-            core, SSD/NVM, and native storage. A general paradigm of
ations such as flushes and merges run in the background.              these improvements is to modify the basic design of LSM-
bLSM [61] proposes a spring-and-gear merge scheduler to               trees to fully exploit the unique features provided by the target
minimize write stalls for the unpartitioned leveling merge            hardware platform to achieve better performance.
policy. Its basic idea is to tolerate an extra component at each
level so that merges at different levels can proceed in paral-        3.4.1 Large memory
lel. Furthermore, the merge scheduler controls the progress
of merge operations to ensure that level L produces a new             It is beneficial for LSM-trees to have large memory com-
component at level L + 1 only after the previous merge oper-          ponents to reduce the total number of levels, as this will
ation at level L + 1 has completed. This eventually cascades          improve both write performance and query performance.
to limit the maximum write speed at the memory component              However, managing large memory components brings sev-
and eliminates large write stalls. However, bLSM itself has           eral new challenges. If a memory component is implemented
several limitations. bLSM was only designed for the unpar-            directly using on-heap data structures, large memory can
titioned leveling merge policy. Moreover, it only bounds the          result in a large number of small objects that lead to sig-
maximum latency of writing to memory components while                 nificant GC overheads. In contrast, if a memory component
the queuing latency, which is often a major source of perfor-         is implemented using off-heap structures such as a concur-
mance variability, is ignored.                                        rent B+ -tree, large memory can still cause a higher search
                                                                      cost (due to tree height) and cause more CPU cache misses
3.3.4 Summary                                                         for writes, as a write must first search for its position in the
                                                                      structure.
The improvements in this category optimize the implemen-                  FloDB [9] presents a two-layer design to manage large
tation of merge operations in terms of performance, buffer            memory components. The top level is a small concurrent
cache misses, and write stalls. To speedup merge operations,          hash table to support fast writes, and the bottom level is a
the VT-tree [64] introduces the stitching operation that avoids       large skip list to support range queries efficiently. When the
copying input pages if applicable. However, this may cause            hash table is full, its entries are efficiently migrated into the
fragmentation, which is undesirable for hard disks. More-             skip list using a batched algorithm. By limiting random writes
over, this optimization is incompatible with Bloom filters,           to a small memory area, this design significantly improves
which are widely used in modern LSM-tree implementa-                  the in-memory write throughput. To support range queries,
tions. The pipelined merge implementation [84] improves               FloDB requires that a range query must wait for the hash
merge performance by exploiting CPU and I/O parallelism. It           table to be drained so that the skip list alone can be searched
should be noted that many LSM-based storage systems have              to answer the query. However, FloDB suffers from two major
already implemented some form of pipelining by exploiting             problems. First, it is not efficient for workloads containing
disk read-ahead and write-behind.                                     both writes and range queries due to their contention. Second,
    Ahmed et al. [2] and the LSbM-tree [68,69] present two            the skip list may have a large memory footprint and lead to
alternative methods to alleviating buffer cache misses caused         lower memory utilization.


123
                                             Author's personal copy
LSM-based storage techniques: a survey




                                                                     Fig. 12 Example FD-tree structure

Fig. 11 Accordion’s multi-layer structure
                                                                         The FD-tree [42] uses a similar design to LSM-trees to
                                                                     reduce random writes on SSDs. One major difference is that
   To address the drawbacks of FloDB, Accordion [13] uses            the FD-tree exploits fractional cascading [17] to improve
a multi-layer approach to manage its large memory com-               query performance instead of Bloom filters. For the com-
ponents. In this design (Fig. 11), there is a small mutable          ponent at each level, the FD-tree additionally stores fence
memory component in the top level to process writes. When            pointers that point to each page at the next level. For example
the mutable memory component is full, instead of being               in Fig. 12, the pages at level 2 are pointed at by fence pointers
flushed to disk, it is simply flushed into a (more compact)          with keys 1, 27, 51, 81 at level 1. After performing a binary
immutable memory component via an in-memory flush oper-              search at level 0, a query can follow these fence pointers
ation. Similarly, such immutable memory components can be            to traverse all of the levels. However, this design introduces
merged via in-memory merge operations to improve query               additional complexity to merges. When the component at
performance and reclaim space occupied by obsolete entries.          level L is merged into level L + 1, all of the previous levels
Note that in-memory flush and merge operations do not                0 to L − 1 must be merged as well to rebuild the fence point-
involve any disk I/O, which reduces the overall disk I/O cost        ers. Moreover, a point lookup still needs to perform disk I/Os
by leveraging large memory.                                          when searching for nonexistent keys, which can be mostly
                                                                     avoided by using Bloom filters. For these reasons, modern
                                                                     LSM-tree implementations prefer Bloom filters rather than
3.4.2 Multi-core
                                                                     fractional cascading.6
                                                                         The FD+tree [71] improves the merge process of the FD-
cLSM [30] optimizes for multi-core machines and presents
                                                                     tree [42]. In the FD-tree, when a merge happens from level 0
new concurrency control algorithms for various LSM-tree
                                                                     to level L, new components at levels 0 to L must be created,
operations. It organizes LSM components into a concurrent
                                                                     which will temporarily double the disk space. To address
linked list to minimize blocking caused by synchronization.
                                                                     this, during a merge operation, the FD+tree incrementally
Flush and merge operations are carefully designed so that
                                                                     activates the new components and reclaims pages from the
they only result in atomic modifications to the linked list
                                                                     old components that are not used by any active queries.
that will never block queries. When a memory component
                                                                         MaSM (materialized sort-merge) [7] is designed for sup-
becomes full, a new memory component is allocated while
                                                                     porting efficient updates for data warehousing workloads by
the old one will be flushed. To avoid writers inserting into
                                                                     exploiting SSDs. MaSM first buffers all updates into an SSD.
the old memory component, a writer acquires a shared lock
                                                                     It uses the tiering merge policy to merge intermediate com-
before modifications and the flush thread acquires an exclu-
                                                                     ponents with low write amplification. The updates are then
sive lock before flushes. cLSM also supports snapshot scans
                                                                     merged back to the base data, which resides in the hard disk.
via multi-versioning and atomic read–modify–write opera-
                                                                     MaSM can be viewed as a simplified form of the lazy-leveling
tions using an optimistic concurrency control approach that
                                                                     merge policy proposed by Dostoevsky [20], as we will see
exploits the fact that all writes, and thus all conflicts, involve
                                                                     later in this survey. Moreover, since MaSM mainly targets
the memory component.
                                                                     long range queries to support data warehousing workloads,
                                                                     the overhead introduced by intermediate components stored
3.4.3 SSD/NVM                                                        in SSDs is negligible compared to the cost of accessing the
                                                                     base data. This enables MaSM to only incur a small overhead
Different from traditional hard disks, which only support effi-      on queries with concurrent updates.
cient sequential I/Os, new storage devices such as solid-state
drives (SSDs) and non-volatile memories (NVMs) support               6 RocksDB [57] supports a limited form of fractional cascading by
efficient random I/Os as well. NVMs further provide efficient        maintaining the set of overlapping SSTables at the adjacent next level
byte-addressable random accesses with persistence guaran-            for each SSTable. These pointers are used to narrow down the search
tees.                                                                range when locating specific SSTables during point lookups.


                                                                                                                               123
                                              Author's personal copy
                                                                                                                      C. Luo, M. J. Carey


                                                                      It further optimizes the write performance of the NVM
                                                                      memory component by skipping logging since NVM itself
                                                                      provides persistence. Finally, it exploits I/O parallelism
                                                                      to search multiple levels concurrently to reduce lookup
                                                                      latency.


Fig. 13 WiscKey stores values into an append-only log to reduce the
                                                                      3.4.4 Native storage
write amplification of the LSM-tree

                                                                      Finally, the last line of work in this category attempts to per-
    Since SSDs support efficient random reads, separating val-        form native management of storage devices, such as HDDs
ues from keys becomes a viable solution to improve the                and SSDs, to optimize the performance of LSM-tree imple-
write performance of LSM-trees. This approach was first               mentations.
implemented by WiscKey [44] and subsequently adopted by                   The LSM-tree-based direct storage system (LDS) [48]
HashKV [15] and SifrDB [49]. As shown in Fig. 13, Wis-                bypasses the file system to better exploit the sequential and
cKey [44] stores key-value pairs into an append-only log and          aggregated I/O patterns exhibited by LSM-trees. The on-
the LSM-tree simply serves as a primary index that maps               disk layout of LDS contains three parts: chunks, a version
each key to its location in the log. While this can greatly           log, and a backup log. Chunks store the disk components of
reduce the write cost by only merging keys, range query per-          the LSM-tree. The version log stores the metadata changes
formance will be significantly impacted because values are            of the LSM-tree after each flush and merge. For exam-
not sorted anymore. Moreover, the value log must be garbage-          ple, a version log record can record the obsolete chunks
collected efficiently to reclaim the storage space. In WiscKey,       and the new chunks resulting from a merge. The ver-
garbage collection is performed in three steps. First, WiscKey        sion log is regularly checkpointed to aggregate all changes
scans the log tail and validates each entry by performing point       so that the log can be truncated. Finally, the backup log
lookups against the LSM-tree to find out whether the loca-            provides durability for in-memory writes by write-ahead log-
tion of each key has changed or not. Second, valid entries,           ging.
whose locations have not changed, are then appended to the                LOCS [74] is an implementation of the LSM-tree on
log and their locations are updated in the LSM-tree as well.          open-channel SSDs. Open-channel SSDs expose internal
Finally, the log tail is truncated to reclaim the storage space.      I/O parallelism via an interface called channels, where each
However, this garbage collection process has been shown to            channel functions independently as a logical disk device.
be a new performance bottleneck [15] due to its expensive             This allows applications to flexibly schedule disk writes
random point lookups.                                                 to leverage the available I/O parallelism, but disk reads
    HashKV [15] introduces a more efficient approach to               must be served by the same channel where the data is
garbage-collect obsolete values. The basic idea is to hash-           stored. To exploit this feature, LOCS dispatches disk writes
partition the value log into multiple partitions based on keys        due to flushes and merges to all channels using a least-
and to garbage-collect each partition independently. In order         weighted-queue-length policy to balance the total amount
to garbage-collect a partition, HashKV performs a group-              of work allocated to each channel. To further improve the
by operation on the keys to find the latest value for each            I/O parallelism for partitioned LSM-trees, LOCS places
key. Valid key-value pairs are added to a new log and their           SSTables from different levels with similar key ranges into
locations are then updated in the LSM-tree. HashKV further            different channels so that these SSTables can be read in par-
stores cold entries separately so that they can be garbage-           allel.
collected less frequently.                                                NoFTL-KV [73] proposes to extract the flash translation
    Kreon [53] exploits memory-mapped I/O to reduce CPU               layer (FTL) from the storage device into the key-value store to
overhead by avoiding unnecessary data copying. It imple-              gain direct control over the storage device. Traditionally, the
ments a customized memory-mapped I/O manager in the                   FTL translates the logical block address to the physical block
Linux kernel to control cache replacement and to enable blind         address to implement wear leveling, which improves the
writes. To improve range query performance, Kreon reorga-             lifespan of SSDs by distributing writes evenly to all blocks.
nizes data during query processing by storing the accessed            NoFTL-KV argues for a number of advantages of extract-
key-value pairs together in a new place.                              ing FTL, such as pushing tasks down to the storage device,
    NoveLSM [36] is an implementation of LSM-trees on                 performing more efficient data placement to exploit I/O par-
NVMs. NoveLSM adds an NVM-based memory compo-                         allelism, and integrating the garbage collection process of
nent to serve writes when the DRAM memory component                   the storage device with the merge process of LSM-trees to
is full so that writes can still proceed without being stalled.       reduce write amplification.


123
                                           Author's personal copy
LSM-based storage techniques: a survey


3.4.5 Summary

In this subsection, we have reviewed the LSM-tree improve-
ments exploiting hardware platforms, including large mem-
ory [9,13], multi-core [30], SSD/NVM [7,15,36,42,44,53,
71], and native storage [48,73,74]. To manage large mem-
ory components, both FloDB [9] and Accordion [13] take a         Fig. 14 The LSM-trie uses the prefix of hash values to manage SSTa-
multi-layer approach to limit random writes to a small mem-      bles. In this example, each level uses three bits to perform partitioning
ory area. The difference is that FloDB [9] only uses two
layers, while Accordion [13] uses multiple layers to provide
better concurrency and memory utilization. For multi-core        the metadata overhead. The LSM-trie adopts a partitioned
machines, cLSM [30] presents a set of new concurrency con-       tiering design to reduce write amplification. Instead of stor-
trol algorithms to improve concurrency.                          ing the key ranges of each SSTable directly, the LSM-trie
    A general theme of the improvements for SSD/NVM is           organizes its SSTables using the prefix of their hash values
to exploit the high random read throughput while reduc-          to reduce the metadata overhead, as shown in Fig. 14. The
ing the write amplification of LSM-trees to improve the          LSM-trie further eliminates the index page, instead assign-
lifespan of these storage devices. The FD-tree [42] and its      ing key-value pairs into fixed-size buckets based on their hash
successor FD+tree [71] propose to use fractional cascad-         values. Overflow key-value pairs are assigned to underflow
ing [17] to improve point lookup performance so that only        buckets and this information is recorded in a migration meta-
one random I/O is needed for searching each component.           data table. The LSM-trie also builds a Bloom filter for each
However, today’s implementations generally prefer Bloom          bucket. Since there are multiple SSTables in each group at
filters since unnecessary I/Os can be mostly avoided by          a level, the LSM-trie clusters all Bloom filters of the same
point lookups. Separating keys from values [15,44,53] can        logical bucket of these SSTables together so that they can be
significantly improve the write performance of LSM-trees         fetched using a single I/O by a point lookup query. In gen-
since only keys are merged. However, this leads to lower         eral, the LSM-trie is mainly effective when the number of
query performance and space utilization. Meanwhile, values       key-value pairs is so large that even the metadata, e.g., index
must be garbage-collected separately to reclaim disk space,      pages and Bloom filters, cannot be totally cached. However,
which is similar to the traditional log-structured file sys-     the LSM-trie only supports point lookups since its optimiza-
tem design [60]. Finally, some recent work has proposed          tions heavily depend on hashing.
to perform native management of storage devices, includ-             SlimDB [59] targets semi-sorted data in which each key
ing HDDs [48] and SSDs [73,74], which can often bring            contains a prefix x and a suffix y. It supports normal point
large performance gains by exploiting the sequential and non-    lookups, given both the prefix and the suffix, as well as
overwriting I/O patterns exhibited by LSM-trees.                 retrieving all the key-value pairs sharing the same prefix key
                                                                 x. To reduce write amplification, SlimDB adopts a hybrid
3.5 Handling special workloads                                   structure with tiering on the lower levels and leveling on
                                                                 the higher levels. SlimDB further uses multi-level cuckoo
We now review some existing LSM-tree improvements that           filters [28] to improve point lookup performance for levels
target special workloads to achieve better performance. The      that use the tiering merge policy. At each level, a multi-level
considered special workloads include temporal data, small        cuckoo filter maps each key to the ID of the SSTable where the
data, semi-sorted data, and append-mostly data.                  latest version of the key is stored so that only one filter check
    The log-structured history access method (LHAM) [50]         is needed by a point lookup. To reduce the metadata over-
improves the original LSM-tree to more efficiently support       head of SSTables, SlimDB uses a multi-level index structure
temporal workloads. The key improvement made by LHAM             as follows: It first maps each prefix key into a list of pages
is to attach a range of timestamps to each component to facil-   that contain this prefix key so that the key-value pairs can
itate the processing of temporal queries by pruning irrelevant   be retrieved efficiently given a prefix key. It then stores the
components. It further guarantees that the timestamp ranges      range of suffix keys for each page to efficiently support point
of components are disjoint from one another. This is accom-      lookup queries based on both prefix and suffix keys.
plished by modifying the rolling merge process to always             Mathieu et al. [47] proposed two new merge policies opti-
merge the records with the oldest timestamps from a compo-       mized for append-mostly workloads with a bounded number
nent Ci into Ci+1 .                                              of components. One problem of both leveling and tiering is
    The LSM-trie [76] is an LSM-based hash index for man-        that the number of levels depends on the total number of
aging a large number of key-value pairs where each key-value     entries. Thus, with an append-mostly workload, where the
pair is small. It proposes a number of optimizations to reduce   amount of data keeps increasing, the total number of levels


                                                                                                                             123
                                            Author's personal copy
                                                                                                                     C. Luo, M. J. Carey


will be unbounded in order to achieve the write cost described     T components at the last level, which contain most of the
in Sect. 2.3. To address this, this work studied the theoretical   data, consume most of the Bloom filter memory but their
lower bound of the write cost of an online merge policy for        Bloom filters can only save at most T disk I/Os for a point
an append-mostly workload given at most K components. It           lookup. To minimize the overall false positive rates across
further proposed two merge policies, MinLatency and Bino-          all of the Bloom filters, Monkey analytically shows that
mial, to achieve this lower bound.                                 more bits should be allocated to the components at the lower
   The four improvements presented here each target a spe-         levels so that the Bloom filter false positive rates will be
cialized workload. It should be noted that their optimizations     exponentially increasing. Under this scheme, the I/O cost of
may be useless or even inapplicable for general purpose            zero-result point lookup queries will be dominated by the
                                                                                                                   M
workloads. For example, the LSM-trie [76] only supports            last level, and the new I/O cost becomes O(e− N ) for level-
                                                                                     M
point lookups, while SlimDB [59] only supports a limited           ing and O(T ·e− N ) for tiering. Monkey then finds an optimal
form of range queries by fetching all values for a prefix key.     LSM-tree design by maximizing the overall throughput using
The adoption of these optimizations should be chosen care-         a cost model similar to the one in Sect. 2.3 considering the
fully based on the given workload.                                 workload’s mix of the various operations.

3.6 Auto-tuning

We now review some research efforts to develop auto-tuning         3.6.2 Tuning merge policies
techniques for the LSM-tree to reduce the tuning burden
for the end user. Some techniques perform co-tuning of all         Dostoevsky [20] shows that the existing merge policies, that
parameters to find an optimal design, while others focus on        is, tiering and leveling, are sub-optimal for certain work-
some specific aspect such as merge policies, Bloom filters,        loads. The intuition is that for leveling, the cost of zero-result
or data placement.                                                 point lookups, long range queries, and space amplification
                                                                   are dominated by the largest level, but the write cost derives
3.6.1 Parameter tuning                                             equally from all of the levels. To address this, Dostoevsky
                                                                   introduces a lazy-leveling merge policy that performs tiering
Lim et al. [43] presented an analytical model that incorpo-        at the lower levels but leveling at the largest level. Lazy lev-
rates the key distribution to improve the cost estimation of       eling has much better write cost than leveling, but has similar
LSM-tree operations and further used this model to tune the        point lookup cost, long range query cost, and space amplifi-
parameters of LSM-trees. The key insight is that the con-          cation to leveling. It only has a worse short range query cost
ventional worse-case analysis (Sect. 2.3) fails to take the        than leveling since the number of components is increased.
key distribution into consideration. If a key is found to be       Dostoevsky also proposes a hybrid policy that has at most Z
deleted or updated during an early merge, it will not par-         components in the largest level and at most K components at
ticipate in future merges and thus its overall write cost will     each of the smaller levels, where Z and K are tunable. It then
be reduced. The proposed model assumes a priori knowl-             finds an optimal LSM-tree design for a given workload using
edge of the key distribution using a probability mass function     a similar method as Monkey [21]. It is worth noting that the
 f X (k) that measures the probability that a specific key k is    performance evaluation of Dostoevsky [20] is very thorough;
written by a write request. Given p total write requests, the      it was performed against well-tuned LSM-trees to show that
number of unique keys is estimated using its expectation as        Dostoevsky strictly dominates the existing LSM-tree designs
                        
U nique( p) = N − k∈K (1 − f X (k)) p , where N is the             under certain workloads.
total number of unique keys and K is the total key space.              Thonangi and Yang [70] formally studied the impact of
Based on this formula, the total write cost for p writes can be    partitioning on the write cost of LSM-trees. This work first
computed by summing up the cost of all flushes and merges,         proposed a ChooseBest policy that always selects an SSTable
except that duplicates keys, if any, are excluded from future      with the fewest overlapping SSTables at the next level to
merges. Finally, the cost model is used to find the optimal        merge to bound the worst-case merge cost. Although the
system parameters by minimizing the total write cost.              ChooseBest policy outperforms the unpartitioned merge pol-
     Monkey [21,22] co-tunes the merge policy, size ratio, and     icy in terms of the overall write cost, there are certain periods
memory allocation between memory components and Bloom              when the unpartitioned merge policy has a lower write cost
filters to find an optimal LSM-tree design for a given work-       since the current level becomes empty after a full merge,
load. The first contribution of Monkey is to show that the         which reduces the future merge cost. To exploit this advan-
usual Bloom filter memory allocation scheme, which allo-           tage of full merges, this work further proposed a mixed merge
cates the same number of bits per key for all Bloom filters,       policy that selectively performs full merges or partitioned
results in sub-optimal performance. The intuition is that the      merges based on the relative size between adjacent levels and


123
                                            Author's personal copy
LSM-based storage techniques: a survey


that dynamically learns these size thresholds to minimize the      mation but only focuses on tuning the maximum level sizes
overall write cost for a given workload.                           of the leveling merge policy. In contrast, Monkey [21,22],
                                                                   as well as its follow-up work Dostoevsky [20], co-tunes all
3.6.3 Dynamic bloom filter memory allocation                       parameters of LSM-trees to find an optimal design but only
                                                                   optimize for the worst-case I/O cost. It would be useful to
All of the existing LSM-tree implementations, even Mon-            combine these two techniques together to enable more accu-
key [21], adopt a static scheme to manage Bloom filter             rate performance tuning and prediction.
memory allocation. That is, once the Bloom filter is created           Dostoevsky [20] extends the design space of LSM-trees
for a component, its false positive rate remains unchanged.        with a new merge policy by combining leveling and tiering.
Instead, ElasticBF [83] dynamically adjusts the Bloom fil-         This is very useful for certain workloads that require effi-
ter false positive rates based on the data hotness and access      cient writes, point lookups, and long range queries with less
frequency to optimize read performance. Given a budget             emphasis on short range queries. Thonangi and Yang [70]
of k Bloom filter bits per key, ElasticBF constructs mul-          proposed to combine full merges with partitioned merges to
tiple smaller Bloom filters with k1 , · · · , kn bits so that      achieve better write performance. Other tuning techniques
k1 + · · · + kn = k. When all of these Bloom filters are           focus on some aspects of the LSM-tree implementation, such
used together, they provide the same false positive rate as the    as tuning Bloom filters by ElasticBF [83] and optimizing data
original monolithic Bloom filter. ElasticBF then dynamically       placement by Mutant [80].
activates and deactivates these Bloom filters based on the
access frequency to minimize the total amount of extra I/O.        3.7 Secondary indexing
Their experiments reveal that ElasticBF is mainly effective
when the overall Bloom filter memory is very limited, such         So far, we have discussed LSM-tree improvements in a key-
as only 4 bits per key on average. In this case, the disk I/Os     value store setting that only contains a single LSM-tree.
caused by the Bloom filter false positives will be dominant.       Now we discuss LSM-based secondary indexing techniques
When memory is relatively large and can accommodate more           to support efficient query processing, including index struc-
bits per key, such as 10, the benefit of ElasticBF becomes lim-    tures, index maintenance, statistics collection, and distributed
ited since the number of disk I/Os caused by false positives       indexing.
is much smaller than the number of actual disk I/Os to locate          Before we present these research efforts in detail, we
the keys.                                                          first discuss some basic concepts for LSM-based secondary
                                                                   indexing techniques. In general, an LSM-based storage sys-
3.6.4 Optimizing data placement                                    tem will contain a primary index with multiple secondary
                                                                   indexes. The primary index stores the record values indexed
Mutant [80] optimizes the data placement of the LSM-tree on        by their primary keys. Each secondary index stores the cor-
cloud storage. Cloud vendors often provide a variety of stor-      responding primary keys for each secondary key using either
age options with different performance characteristics and         a composite key approach or a key list approach. In the com-
monetary costs. Given a monetary budget, it can be impor-          posite key approach, the index key of a secondary index is
tant to place SSTables on different storage devices properly       the composition of the secondary key and the primary key. In
to maximize system performance. Mutant solves this prob-           the key list approach, a secondary index associates a list of
lem by monitoring the access frequency of each SSTable and         primary keys with each secondary key. Either way, to process
finding a subset of SSTables to be placed in fast storage so       a query using a secondary index, the secondary index is first
that the total number of accesses to fast storage is maximized     searched to return a list of matching primary keys, and those
while the number of selected SSTables is bounded. This opti-       are then used to fetch the records from the primary index if
mization problem is equivalent to a 0/1 knapsack problem,          needed. An example of LSM-based secondary indexing is
which is N/P hard, and can be approximated using a greedy          shown in Fig. 15. The example user dataset has three fields,
algorithm.                                                         namely Id, Name, and Age, where Id is the primary key. The
                                                                   primary index stores full records indexed by Id, while the
3.6.5 Summary                                                      two secondary indexes store secondary keys, i.e., Name and
                                                                   Age, and their corresponding Ids.
The techniques presented in this category aim at automat-
ically tuning LSM-trees for given workloads. Both Lim et           3.7.1 Index structures
al. [43] and Monkey [21,22] attempt to find optimal designs
for LSM-trees to maximize system performance. However,             The log-structured inverted index (LSII) [75] is an index
these two techniques are complimentary to each other. Lim et       structure designed for exact real-time keyword search on
al. [43] uses a novel analytical model to improve the cost esti-   microblogs. A query q searches for the top K microblogs


                                                                                                                        123
                                              Author's personal copy
                                                                                                                          C. Luo, M. J. Carey




                                                                  Fig. 16 Filter update example: the dataset contains three fields, a pri-
                                                                  mary key (k), a value (v), and a creation time (T ) that is also the filter
                                                                  key

Fig. 15 Example LSM-based secondary indexes
                                                                  minimum and maximum values of the chosen filter key for
                                                                  the entries in a component. Thus, a component can be pruned
                                                                  by a query if the search condition is disjoint with the mini-
with the highest scores, which are computed as the weighted       mum and maximum values of its filter. Though a filter can
sum of significance, freshness, and relevance. To support         be built on arbitrary fields, it is really only effective for
efficient query processing, each keyword in a disk compo-         time-correlated fields since components are naturally par-
nent stores three inverted lists of primary keys in descending    titioned based on time and are likely to have disjoint filter
order of significance, freshness, and frequency, respectively.    ranges. Note that some special care is needed to maintain
Storing three inverted lists enables queries to be processed      filters when a key is updated or deleted. In this case, the fil-
efficiently via the threshold algorithm [27], which stops query   ter of the memory component must be maintained based on
evaluation once the upper bound of the scores of the unseen       both the old record and the new record so that future queries
microblogs is lower than the current top K answers. However,      will not miss new updates. Consider the example in Fig. 16,
only one inverted list is stored in the memory component          which depicts a filtered primary LSM-tree. After upserting
since documents in the memory component often have high           the new record (k1, v4, T4), the filter of the memory com-
freshness and most of them will be accessed by queries.           ponent becomes [T1, T4] so that future queries will properly
Moreover, storing multiple inverted lists would significantly     see that the old record (k1, v1, T1) in the disk component
increase the memory component’s write cost.                       has been deleted. Otherwise, if the filter of the memory com-
   Kim et al. [39] conducted an experimental study of LSM-        ponent were only maintained based on the new value T4,
based spatial index structures for geo-tagged data, including     which would be [T3, T4], a query with search condition T
LSM-tree versions of the R-tree [31], Dynamic Hilbert             ≤ T2 would erroneously prune the memory component and
B+ -tree (DHB-tree) [41], Dynamic Hilbert Value B+ -tree          thus actually see the deleted record (k1, v1, T1).
(DHVB-tree) [41], Static Hilbert B+ -tree (SHB-tree) [29],            Qadar et al. [56] conducted an experimental study of LSM-
and Spatial Inverted File (SIF) [37]. An R-tree is a bal-         based secondary indexing techniques including filters and
anced search tree that stores multi-dimensional spatial data      secondary indexes. For filters, they evaluated component-
using their minimum bounding rectangles. DHB-trees and            level range filters and Bloom filters on secondary keys. For
DHVB-trees store spatial points directly into B+ -trees using     secondary indexes, they evaluated two secondary indexing
space-filling curves. SHB-trees and SIFs exploit a grid-based     schemes based on composite keys and key lists. Depending
approach by statically decomposing a two-dimensional space        on how the secondary index is maintained, the key list scheme
into a multi-level grid hierarchy. For each spatial object, the   can be further classified as being either eager or lazy. The
IDs of its overlapping cells are stored. The difference between   eager key list scheme always reads the previous list to create
these two structures is that an SHB-tree stores the pairs of      a full new list with the new entry added and inserts the new list
cell IDs and primary keys in a B+ -tree, while a SIF stores       into the memory component. The lazy key list scheme simply
a list of primary keys for each cell ID in an inverted index.     maintains multiple partial lists at each component. The exper-
The key conclusion of this study is that there is no clear        imental results suggest that the eager inverted list scheme
winner among these index structures, but the LSM-based R-         incurs a large overhead on data ingestion because of the point
tree performs reasonably well for both ingestion and query        lookups and high write amplification. When the query selec-
workloads without requiring too much tuning. It also handles      tivity becomes larger, that is, when the result set contains
both point and non-point data well. Moreover, for non-index-      more entries, the performance difference between the lazy
only queries, the final primary key lookup step is generally      key list scheme and the composite key scheme diminishes,
dominant since it often requires a separate disk I/O for each     as the final point lookup step becomes dominant. Finally,
primary key. This further diminished the differences between      filters were found to be very effective with small storage
these spatial indexing methods.                                   overhead for time-correlated workloads. However, the study
   Filters [5] augment each component of the primary and          did not consider cleaning up secondary indexes in the case
secondary indexes with a filter to enable data pruning based      of updates, which means that secondary indexes could return
on a filter key during query processing. A filter stores the      obsolete primary keys.


123
                                           Author's personal copy
LSM-based storage techniques: a survey


3.7.2 Index maintenance                                          two are mainly effective for non-selective queries at further
                                                                 reducing the in-memory search cost. To maintain auxiliary
A key challenge of maintaining LSM-based secondary               structures efficiently, two strategies were further proposed.
indexes is handling updates. For a primary LSM-tree, an          The key insight is to maintain and exploit a primary key index,
update can blindly add the new entry (with the identical key)    which only stores primary keys plus timestamps, to reduce
into the memory component so that the old entry is automat-      disk I/Os. A validation strategy was proposed to maintain
ically deleted. However, this mechanism does not work for        secondary indexes lazily in the background, eliminating the
a secondary index since a secondary key value can change         synchronous point lookup overhead. Queries must validate
during an update. Extra work must be performed to clean up       the primary keys returned by secondary indexes either by
obsolete entries from secondary indexes during updates.          fetching records directly from the primary index or by search-
    Diff-Index [66] presents four index maintenance schemes      ing the primary key index to ensure that the returned primary
for LSM-based secondary indexes, namely sync-full, sync-         keys still have the latest timestamps. Secondary indexes are
insert, async-simple, and async-session. During an update,       cleaned up efficiently in the background using the primary
two steps must be performed to update a secondary index,         key index to avoid accessing full records; the basic idea for
namely inserting the new entry and cleaning up the old entry.    cleanup is to search the primary key index to validate whether
Inserting the new entry is very efficient for LSM-trees, but     each secondary index entry still has the latest timestamp, as
cleaning up the old entry is generally expensive since it        in query validation. Compared to DELI [67], the validation
requires a point lookup to fetch the old record. Sync-full       strategy [45] significantly reduces the I/O cost for cleaning
performs these two steps synchronously during the ingestion      up secondary indexes since only the primary key index is
time. It optimizes for query performance since secondary         accessed. A mutable-bitmap strategy was also introduced to
indexes are always up to date, but incurs a high overhead dur-   efficiently maintain a primary index with filters. It attaches a
ing data ingestion because of the point lookups. Sync-insert     mutable bitmap to each disk component so that old records
only inserts new data into secondary indexes, while cleaning     can be directly marked as deleted, thereby avoiding the need
up obsolete entries lazily by queries. Async-simple performs     to maintain filters based on old records.
index maintenance asynchronously but guarantees its even-
tual execution by appending updates into an asynchronous         3.7.3 Statistics collection
update queue. Finally, async-session enhances async-simple
with session consistency for applications by storing new         Absalyamov et al. [1] proposed a lightweight statistics col-
updates temporarily into a local cache on the client-side.       lection framework for LSM-based systems. The basic idea
    Deferred lightweight indexing (DELI) [67] enhances the       is to integrate the task of statistics collection into the flush
sync-insert update scheme of Diff-Index [66] with a new          and merge operations to minimize the statistics maintenance
method to cleanup secondary indexes by scanning the pri-         overhead. During flush and merge operations, statistical syn-
mary index components. Specifically, when multiple records       opses, such as histograms and wavelets, are created on the
with identical keys are encountered when scanning primary        fly and are sent back to the system catalog. Due to the multi-
index components, the obsolete records are used to produce       component nature of LSM-trees, the system catalog stores
antimatter entries to clean up the secondary indexes. Note       multiple statistics for a dataset. To reduce the overhead during
that this procedure can be naturally integrated with the merge   query optimization, mergeable statistics, such as equi-width
process of the primary index to reduce the extra overhead.       histograms, are merged beforehand. For statistics that are not
Meanwhile, since secondary indexes are not always up to          mergeable, multiple synopses are kept to improve the accu-
date, queries must always validate search results by fetching    racy of cardinality estimation.
records from the primary index. Because of this, DELI can-
not support index-only queries efficiently since point lookups   3.7.4 Distributed indexing
must be performed for validation.
    Luo and Carey [45] presented several techniques for the      Joseph et al. [25] described two basic implementations of
efficient exploitation and maintenance of LSM-based auxil-       distributed secondary indexes on top of HBase [32], namely
iary structures, including secondary indexes and filters. They   global secondary indexes and local secondary indexes, based
first conducted an experimental study to evaluate the effec-     on the two common approaches to indexing data in a parallel
tiveness of various point lookup optimizations, including        database. A global secondary index is implemented as a sepa-
a newly proposed batched lookup algorithm that accesses          rate table that stores secondary keys plus their corresponding
components sequentially for a batch of keys, stateful B+ -       primary keys, and it is maintained using co-processors pro-
tree search cursors, and blocked bloom filters [55]. They        vided by HBase (similar to database triggers). This approach
found that the batched lookup algorithm is the most effec-       is easy to implement, but incurs a higher communication cost
tive optimization for reducing random I/Os, while the other      during data ingestion since a secondary index partition may


                                                                                                                      123
                                              Author's personal copy
                                                                                                                    C. Luo, M. J. Carey


be stored at a separate node from the primary index parti-            3.8 Discussion of overall trade-offs
tion. A local secondary index avoids the communication cost
during data ingestion by colocating each secondary index              Based on the RUM conjecture [8], no access method can
partition together with the corresponding primary index par-          be read-optimal, write-optimal, and space-optimal at the
tition. However, the downside for HBase is that this approach         same time. As we have seen in this survey, many LSM-tree
has to be implemented from scratch. Moreover, all partitions          improvements that optimize for certain workload or sys-
of a local secondary index must be searched, eve n for highly         tem aspects will generally make trade-offs. To conclude this
selective queries, since a local secondary index is partitioned       section, we provide a qualitative analysis and summary of
by primary (not secondary) keys.                                      the trade-offs made by those research efforts that seek to
    Zhu et al. [85] introduced an efficient approach for loading      optimize various aspects of LSM-trees. We will consider
global secondary indexes using three steps: First, the primary        the leveling merge policy as the baseline for this discus-
index at each partition is scanned and sorted to create a local       sion.
secondary index. Meanwhile, the statistics of the secondary              The performance trade-offs of the various LSM-tree
key are collected to facilitate the next step. Second, based on       improvements are summarized in Table 3. As one can see,
the collected statistics from the first stage, the index entries of   most of these improvements try to improve the write perfor-
the secondary index will be range-partitioned and these parti-        mance of the leveling merge policy since it has relatively
tions will be assigned to physical nodes. Finally, based on the       high write amplification. A common approach taken by
assigned secondary key range, each node fetches secondary             existing improvements is to apply the tiering merge pol-
keys and their primary keys from all other nodes, which can           icy [6,49,52,58,76,78,79,82], but this will negatively impact
be done efficiently by scanning the local secondary index             query performance and space utilization. Moreover, tiering
built in the first stage.                                             has a larger negative impact on range queries than point
    Duan et al. [26] proposed a lazy maintenance approach             lookups since range queries cannot benefit from Bloom fil-
for materialized views on distributed LSM-trees. The basic            ters.
idea is to append new updates into a delta list of the material-         Other proposed improvements, such as the skip-tree [81],
ized view to reduce the overhead during data ingestion. The           TRIAD [10], and the VT-tree [64], propose several new ideas
changes in the delta list are then applied to the materialized        to improve the write performance of LSM-trees. However,
view lazily, during query processing.                                 in addition to extra overhead on query performance and
                                                                      space utilization, these optimizations may bring non-trivial
                                                                      implementation complexity to real systems. For example,
3.7.5 Summary                                                         the skip-tree introduces mutable buffers to store skipped
                                                                      keys, which contradicts the immutability of disk components.
The techniques in this category all focus on improving LSM-           TRIAD proposes to use transaction logs as disk components
trees in database settings with secondary indexes and other           to eliminate flushes, which is again highly non-trivial since
auxiliary structures. Several LSM-based secondary index-              transaction logs usually have very different storage formats
ing structures have been proposed, including LSM-based                and operation interfaces from disk components. Moreover,
inverted indexes [75], spatial indexes [39], and filters [5].         a common practice is to store transaction logs on a dedi-
These structures would be helpful to optimize certain query           cated disk to minimize the negative impacts caused by log
workloads. In terms of efficiently maintaining secondary              forces. The stitching operation proposed by the VT-tree [64]
indexes, a common approach is to defer the maintenance                can cause fragmentation and is incompatible with Bloom fil-
of secondary indexes so that expensive point lookups can              ters.
be avoided during the ingestion time. The proposed tech-                 The LSM-trie [76] and SlimDB [59] give up some
niques mainly differ in how secondary indexes are cleaned             query capabilities to improve performance. The LSM-trie
up in the background, either by queries [66], scanning the            exploits hashing to improve both read and write perfor-
primary index [67], or exploiting a primary key index [45].           mance, but range queries cannot be supported. SlimDB only
Since the optimality of these methods may be workload                 supports a limited form of range queries based on a com-
dependent, it would be useful as future work to design adap-          mon prefix key. These improvements would be desirable
tive maintenance mechanisms to maximize performance.                  for certain workloads where complete range queries are not
Absalyamov et al. [1] proposed a statistics collection frame-         needed.
work, which is a step toward cost-based query optimization               Separating keys from values [15,44,53] can drastically
on LSM-based systems. Finally, several distributed indexing           improve the write performance of LSM-trees since only
techniques [25,26,85] have also been presented. It should be          keys are merged. However, a major problem is that range
noted that these techniques are not specific to LSM-trees, but        queries will be significantly impacted because values are not
we have included them here for completeness.                          sorted anymore. Even though this problem can be mitigated


123
                                              Author's personal copy
LSM-based storage techniques: a survey


Table 3 Summary of trade-offs made by LSM-tree improvements (↑ denotes increasing, ↓ denotes decreasing, − denotes unaffected, and × denotes
unsupported)
Publication          Write     Point lookup   Short range    Long range     Space    Remark

WB-tree [6]          ↑↑        ↓              ↓↓             ↓↓             ↓↓       Tiering
LWC-tree [78,79]     ↑↑        ↓              ↓↓             ↓↓             ↓↓       Tiering
PebblesDB [58]       ↑↑        ↓              ↓↓             ↓↓             ↓↓       Tiering
dCompaction [52]     ↑↑        ↓              ↓↓             ↓↓             ↓↓       Tiering
Zhang et al. [82]    ↑↑        ↓              ↓↓             ↓↓             ↓↓       Tiering
SifrDB [49]          ↑↑        ↓              ↓↓             ↓↓             ↓↓       Tiering
Skip-tree [81]       ↑         ↓              ↓              ↓              −        Mutable skip buffers
TRIAD [10]           ↑         ↓              ↓              ↓              −        Separate cold entries from hot entries; delay merges at
                                                                                      level 0; use logs as flushed components
VT-tree [64]         ↑         −              ↓              ↓              ↓        Stitching merge
MaSM [7]             ↑↑        ↓              ↓↓             ↓              ↓        Lazy leveling
WiscKey [44]         ↑↑↑       ↓              ↓↓↓            ↓↓↓            ↓↓↓      KV separation
HashKV [15]          ↑↑↑       ↓              ↓↓↓            ↓↓↓            ↓↓↓      KV separation
Kreon [53]           ↑↑↑       ↓              ↓↓↓            ↓↓↓            ↓↓↓      KV separation
LSM-trie [76]        ↑↑        ↑              ×              ×              ↓↓       Tiering + hashing
SlimDB [59]          ↑↑        ↑              ↓↓/×           ↓/×            ↓        Only support range queries for each key prefix group
Lim et al. [43]      ↑         −              −              −              −        Exploit data redundancy
Monkey [21,22]       −         ↑              −              −              −        Better Bloom filter memory allocation
Dostoevsky [20]      ↑↑        ↓              ↓↓             ↓              ↓        Lazy leveling



by exploiting the I/O parallelism of SSDs [15,44], this still           4 Representative LSM-based systems
leads to lower disk efficiency especially when values are rel-
atively small. Moreover, storing values separately leads to             Having discussed LSM-trees and their improvements in
lower space utilization since values are not garbage-collected          detail, we now survey five representative LSM-based open-
during merges. A separate garbage collection process must               source NoSQL systems, namely LevelDB [40], RocksDB
be designed to reclaim disk space occupied by obsolete val-             [57], Cassandra [14], HBase [32], and AsterixDB [3]. We
ues.                                                                    will focus on their storage layers.
   Given that trade-offs are inevitable, it is valuable to
explore the design space of LSM-trees so that one can make              4.1 LevelDB
better or optimal trade-offs. For example, Lim et al. [43]
exploits data redundancy to tune the maximum sizes for                  LevelDB [40] is an LSM-based key-value store that was
each level to optimize write performance. This has lit-                 open-sourced by Google in 2011. It supports a simple key-
tle or no impact on other performance metrics since the                 value interface including puts, gets, and scans. LevelDB
number of levels remains the same. Another example is                   is not a full-fledged data management system, but rather
Monkey [21,22], which unifies the design space of LSM-                  an embedded storage engine intended to power higher-
trees in terms of merge policies, size ratios, and memory               level applications. The major contribution of LevelDB was
allocation between memory components and Bloom fil-                     that it pioneered the design and implementation of the
ters. It further identifies a better memory allocation scheme           partitioned leveling merge policy, which was described in
for Bloom filters that improves point lookup performance                Sect. 2.2.1. This design has impacted many subsequent LSM-
without any negative impact on other metrics. Finally, Dos-             tree improvements and implementations, as we have seen in
toevsky [20] extends the design space of LSM-trees with                 this survey. Since we have already described partitioned lev-
a new lazy-leveling merge policy. By performing tiering                 eling in Sect. 2.2.1, we omit further discussions here.
at lower levels while leveling at the largest level, lazy
leveling achieves similar write throughput to tiering but
only has slightly worse point lookup performance, long                  4.2 RocksDB
range query performance, and space utilization than level-
ing.                                                                    RocksDB [57] was initially a fork of LevelDB created by
                                                                        Facebook in 2012. Since then, RocksDB has added a large


                                                                                                                                  123
                                               Author's personal copy
                                                                                                                          C. Luo, M. J. Carey


number of new features. Due to its high performance and                 policy slightly differs from the one described in this paper
flexibility, RocksDB has successfully been used in various              (and elsewhere in the literature). RocksDB’s tiering merge
systems [24] both inside and outside of Facebook. Accord-               policy is controlled by two parameters, namely, the number
ing to Facebook, a major motivation of for their adoption of            of components to merge (K) and the size ratio (T). It works
LSM-based storage was its good space utilization [24]. With             by examining components from oldest to newest, and for
the default size ratio of 10, RocksDB’s leveling implemen-              each component Ci , it checks whether the total size of the K-
tation has about 90% percent of the total data at the largest           1 younger components Ci−1 , Ci−2 , . . . , Ci−K is larger than
level, ensuring that at most 10% of the total storage space can         T times the size of Ci . If so, the policy merges all of these
be wasted for storing obsolete entries. As mentioned earlier,           components together; otherwise, it proceeds to check the next
this outperforms traditional B-tree-based storage engines,              younger component. RocksDB performs limited partitioning
where pages are typically 2/3 full on average due to frag-              for its tiering merge policy, similar to the horizontal grouping
mentation [77]. Here we discuss various improvements made               design (Sect. 2.2.2), to bound the maximum size of SSTables.
by RocksDB, including its improvements to merge policies,               The motivation is that the maximum page size is limited to
merge operations, and new functionality.                                4 GB, but the index page of a huge component (stored as a
    RocksDB’s LSM-tree implementation remains based on                  single SSTable) could exceed this size limit. However, during
the partitioned leveling design, but with some improvements.            large merges the disk space may still be temporarily doubled
Since SSTables at level 0 are not partitioned, merging an               since RocksDB treats each SSTable group as a whole and
SSTable from level 0 to level 1 generally causes rewrites               only deletes old SSTables when the merge operation is fully
of all SSTables at level 1, which often makes level 0 the               completed. In the FIFO merge policy, components are not
performance bottleneck. To partially address this problem,              merged at all, but old components will be deleted based on a
RocksDB optionally merges SSTables at level 0 using the                 specified lifetime.
tiering merge policy. This elastic design allows RocksDB                    In LSM-based storage, merge operations typically con-
to better absorb write bursts without degrading query per-              sume a lot of CPU and disk resources that can negatively
formance too much. RocksDB further supports a dynamic                   impact query performance. Moreover, the timing of merges
level size scheme to bound the space amplification. The                 is generally unpredictable, as it directly depends on the write
issue is that the ideal leveling space amplification O( T T+1 )         rate. To alleviate this issue, RocksDB supports rate limiting
is achieved only when the last level reaches the maximum                to control the disk write speed of merge operations based on
size, which may not always happen in practice. To address               the leaky bucket mechanism [72]. The basic idea is to main-
this, RocksDB dynamically adjusts the maximum capaci-                   tain a “bucket” that stores a number of tokens controlled by
ties of all of the lower levels depending on the current size           a token refill speed. All flush and merge operations must
of the last level, thereby ensuring that the space amplifica-           request a certain number of tokens before performing each
tion is always O( T T+1 ). In addition to a round-robin policy          write. Thus, the disk write speed of flush and merge opera-
to select the SSTables to be merged, which is used in Lev-              tions will be bounded by the specified token refill speed.
elDB, RocksDB supports two additional policies—namely                       Finally, RocksDB supports a new operation called read–
cold-first and delete-first. The cold-first policy selects cold         modify–write. In practice, many applications typically update
SSTables to merge to optimize for skewed workloads. It                  existing values by reading them first. To support this opera-
ensures that hot SSTables that are updated frequently will              tion efficiently, RocksDB allows users to write delta records
remain in the lower levels to reduce their total write cost.            directly into memory, thereby avoiding reading the original
The delete-first policy selects SSTables with a large number            record. Delta records are then combined with base records
of antimatter entries to quickly reclaim the disk space occu-           during query processing and merges based on the user-
pied by the deleted entries. Finally, RocksDB supports an API           provided combination logic. If applicable, RocksDB further
called the merge filter7 that allows users to provide custom            combines multiple delta records together during merges to
logic to garbage-collect obsolete entries during merges effi-           improve subsequent query performance.
ciently. During a merge, RocksDB invokes the user-provided
merge filter with each key-value pair and only adds those
key-value pairs that are not filtered to the resulting SSTables.        4.3 HBase
    Besides the partitioned leveling merge policy, RocksDB
supports other merge policies such as tiering and FIFO. In              Apache HBase [32] is a distributed data storage system in the
RocksDB, as well as other systems, the actual tiering merge             Hadoop ecosystem; it was modeled after Google’s Bigtable
                                                                        design [16]. HBase is based on a master-slave architecture. It
7 It is called the compaction filter in RocksDB since RocksDB prefers   partitions (either hash or range) a dataset into a set of regions,
the term compaction to merge. We use the term merge here to minimize    where each region is managed by an LSM-tree. HBase sup-
the potential for terminology confusion.                                ports dynamic region splitting and merging to elastically


123
                                            Author's personal copy
LSM-based storage techniques: a survey


manage system resources based on the given workload. Here          of semi-structured (e.g., JSON) data efficiently. Here we
we focus on the storage engine of HBase.                           focus on the storage management aspect of AsterixDB [4].
    HBase’s LSM-tree implementation is generally based on             AsterixDB uses a shared-nothing parallel database style
the basic tiering merge policy. It supports some variations        architecture. The records of each dataset are hash-partitioned
of the tiering merge policy as well, such as the exploring         based on their primary keys across multiple nodes. Each
merge policy and the date-tiered merge policy. The explor-         partition of a dataset is managed by an LSM-based storage
ing merge policy checks all mergeable component sequences          engine, with a primary index, a primary key index, and mul-
and selects the one with the smallest write cost. This merge       tiple local secondary indexes. AsterixDB uses a record-level
policy is more robust than the basic tiering merge policy,         transaction model to ensure that all of the indexes are kept
especially when components have irregular sizes due to load-       consistent within each partition. The primary index stores
ing and deletions. Thus, it is used as the default merge policy    records indexed by primary keys, and the primary key index
in HBase. The date-tiered merge policy is designed for man-        stores primary keys only. The primary key index is built to
aging time-series data. It merges components based on their        support COUNT(*) style queries efficiently as well as various
time ranges, instead of their sizes, so that components will       index maintenance operations [45] since it is much smaller
be time-range-partitioned. This enables efficient processing       than the primary index.
of temporal queries.                                                  Secondary indexes use the composition of the secondary
    Recently, HBase has introduced a new feature, called           key and the primary key as their index keys. AsterixDB sup-
stripping, to partition a large region to improve merge effi-      ports LSM-based B+ -trees, R-trees, and inverted indexes
ciency. The idea is to partition the key space so that each        using a generic LSM-ification framework that can convert
partition, which contains a list of components, is merged          an in-place index into an LSM-based index. For LSM-based
independently. This is similar to the design proposed by PE        R-trees, a linear order, such as a Hilbert curve for point
files [35], but is different from the partitioned tiering merge    data and a Z-order curve for non-point data, is used to sort
policy described in Sect. 2.2.1.                                   the entries in disk components, while in the memory com-
    HBase does not support secondary indexes natively. How-        ponent, deleted keys are recorded in a separate B+ -tree to
ever, a secondary index can be implemented as a separate           avoid multi-path traversals during deletes. AsterixDB also
table that stores secondary keys plus their primary keys using     supports LSM-based inverted indexes to efficiently process
co-processors, as described in [25].                               full-text queries and similarity queries [38]. By default, each
                                                                   LSM index’s components are merged independently using
                                                                   a tiering-like merge policy. AsterixDB also supports a cor-
4.4 Cassandra
                                                                   related merge policy that synchronizes the merges of all of
                                                                   a dataset’s indexes together to improve query performance
Apache Cassandra [14] is an open-source distributed data
                                                                   with filters. The basic idea of this policy is to delegate merge
storage system modeled after both Amazon’s Dynamo [23]
                                                                   scheduling to the primary index. When a sequence of primary
and Google’s BigTable [16]. Cassandra relies on a decentral-
                                                                   index components are merged, all corresponding components
ized architecture to eliminate the possibility of a single point
                                                                   from other indexes will be merged as well.
of failure. Each data partition in Cassandra is powered by an
LSM-based storage engine.
   Cassandra supports a similar set of merge policies to
                                                                   5 Future research directions
RocksDB and HBase, including the (unpartitioned) tiering
merge policy, the partitioned leveling merge policy, and the
                                                                   Categorizing and summarizing the existing LSM-tree
date-tiered merge policy. Moreover, Cassandra supports local
                                                                   improvements in the literature reveal several interesting
secondary indexes to facilitate query processing. To avoid the
                                                                   outages and opportunities for future work on LSM-based
high-point lookup overhead, secondary indexes are main-
                                                                   storage. We now briefly discuss some future research direc-
tained lazily, similar to DELI [67]. During an update, if the
                                                                   tions suggested by the results of this survey.
old record is found in the memory component, then it is used
                                                                      Thorough performance evaluation As mentioned
to clean up secondary indexes directly. Otherwise, secondary
                                                                   before, the tunability of LSM-trees has not been adequately
indexes are cleaned up lazily when merging the primary index
                                                                   considered in many of the research efforts to date. Work on
components.
                                                                   improvements has typically been evaluated against a default
                                                                   (untuned) configuration of LevelDB or RocksDB. It is not
4.5 AsterixDB                                                      clear how the improvements would compare against a well-
                                                                   tuned baseline LSM-tree for a given workload. Moreover,
Apache AsterixDB [3] is an open-source Big Data Manage-            many of the improvement proposals have primarily evaluated
ment System (BDMS) that aims to manage massive amounts             their impact on query performance, with space utilization


                                                                                                                        123
                                             Author's personal copy
                                                                                                                           C. Luo, M. J. Carey


often being neglected. This situation can be addressed in            6 Conclusion
future research by more carefully considering the tunabil-
ity of LSM-trees.                                                    Recently, LSM-trees have become increasingly popular in
   Partitioned tiering structure Tiering has been used by            modern NoSQL systems due to advantages such as supe-
many LSM-tree improvements to reduce the write ampli-                rior write performance, high space utilization, immutability
fication of LSM-trees. In Sect. 2.2.1, we have identified            of on-disk data, and tunability. These factors have enabled
two possible partitioned tiering schemes, namely horizon-            LSM-trees to be widely adopted and deployed to serve a
tal grouping and vertical grouping, that cover virtually             variety of workloads.
all of the tiering-related LSM-tree improvements proposed               In this paper, we have surveyed the recent research efforts,
recently. However, the performance characteristics and trade-        including efforts from both the database community and the
offs of these two schemes are not yet clear. In general,             systems community, to improve LSM-trees. We presented
vertical grouping permits more freedom when selecting                a general taxonomy to classify existing LSM-tree improve-
SSTables to merge, while horizontal grouping ensures that            ments based on the specific aspects that they aim to optimize,
SSTables are fixed-size. It would be useful as future work           and we discussed the improvements in detail based on the
to systematically evaluate these two schemes and possi-              proposed taxonomy. We also reviewed several representative
bly design new schemes that combine the advantages of                LSM-based open-source NoSQL systems, and we identified
both.                                                                some interesting future research directions. We hope that this
   Hybrid merge policy Until recently, most LSM-tree                 survey will serve as a useful guide to the state of the art in
improvements have assumed a homogeneous merge policy                 LSM-based storage techniques for researchers, practitioners,
of either leveling or tiering at all of the levels of an LSM-tree.   and users.
However, this has been shown to be sub-optimal for certain
workloads [20]. A hybrid merge policy of leveling and tier-          Acknowledgements We would like to thank Mark Callaghan, Manos
                                                                     Athanassoulis, and the anonymous reviewers for their valuable com-
ing can provide much better write performance than leveling          ments and feedback. This work was supported by NSF awards CNS-
with minimal impact on point lookups, long range queries,            1305430, IIS-1447720, and IIS-1838248 along with industrial support
and space amplification. As a future direction, it would be          from Amazon, Google, and Microsoft and support from The Donald
interesting to design and implement LSM-trees with hybrid            Bren Foundation (via a Bren Chair) of UC Irvine.
merge policies and revisit some of the key questions raised
by this design choice.
   Minimizing performance variance In practice, perfor-
mance variance is as important a performance metric as               References
absolute throughput. Unfortunately, LSM-trees often exhibit
                                                                      1. Absalyamov, I., et al.: Lightweight cardinality estimation in LSM-
large performance variances because they decouple the in-                based systems. In: ACM SIGMOD, pp. 841–855 (2018)
memory writes from expensive background I/Os. As we have              2. Ahmad, M.Y., Kemme, B.: Compaction management in distributed
seen in this survey, bLSM [61] is the only attempt to min-               key-value datastores. PVLDB 8(8), 850–861 (2015)
                                                                      3. Alsubaiee, S., et al.: AsterixDB: a scalable, open source BDMS.
imize write stalls exhibited by LSM-trees. However, bLSM
                                                                         PVLDB 7(14), 1905–1916 (2014)
itself still has several limitations. It was designed just for the    4. Alsubaiee, S., et al.: Storage management in AsterixDB. PVLDB
unpartitioned leveling merge policy, and it only minimizes               7(10), 841–852 (2014)
long write latencies caused by write stalls instead of the vari-      5. Alsubaiee, S., et al.: LSM-based storage and indexing: an old idea
                                                                         with timely benefits. In: International ACM SIGMOD Workshop
ance of the overall ingestion throughput. As future work, it
                                                                         on Managing and Mining Enriched Geo-spatial Data (GeoRich),
would be very useful to design mechanisms to minimize the                pp 1–6 (2015)
performance variance of LSM-trees.                                    6. Amur, H., et al.: Design of a write-optimized data store. Tech. rep,
   Toward database storage engines Finally, most of                      Georgia Institute of Technology (2013)
                                                                      7. Athanassoulis, M., et al.: MaSM: efficient online updates in data
the existing LSM-tree improvements have focused rather
                                                                         warehouses. In: ACM SIGMOD, pp. 865–876. ACM (2011)
narrowly on a key-value store setting involving a single              8. Athanassoulis, M., et al.: Designing access methods: the RUM
LSM-tree. As LSM-trees are gradually becoming widely                     conjecture. In: EDBT, vol. 2016, pp. 461–466 (2016)
used inside DBMS storage engines, new query process-                  9. Balmau, O., et al.: FloDB: unlocking memory in persistent key-
                                                                         value stores. In: European Conference on Computer Systems
ing and data ingestion techniques should be developed for
                                                                         (EuroSys), pp. 80–94 (2017)
this more general (multi-index) setting. Possible examples           10. Balmau, O., et al.: TRIAD: creating synergies between memory,
include adaptive maintenance of auxiliary structures to facil-           disk and log in log structured key-value stores. In: USENIX Annual
itate query processing, LSM-aware query optimization, and                Technical Conference (ATC), pp. 363–375 (2017)
                                                                     11. Bender, M.A., et al.: Don’t thrash: how to cache your hash on flash.
co-planning of LSM-tree maintenance tasks with query exe-
                                                                         PVLDB 5(11), 1627–1637 (2012)
cution.                                                              12. Bloom, B.H.: Space/time trade-offs in hash coding with allowable
                                                                         errors. CACM 13(7), 422–426 (1970)


123
                                                  Author's personal copy
LSM-based storage techniques: a survey


13. Bortnikov, E., et al.: Accordion: better memory organization for        42. Li, Y., et al.: Tree indexing on solid state drives. PVLDB 3(1–2),
    LSM key-value stores. PVLDB 11(12), 1863–1875 (2018)                        1195–1206 (2010)
14. Cassandra. http://cassandra.apache.org/                                 43. Lim, H., et al.: Towards accurate and fast evaluation of multi-stage
15. Chan, H.H.W., et al.: HashKV: enabling efficient updates in KV              log-structured designs. In: USENIX Conference on File and Stor-
    storage via hashing. In: USENIX Annual Technical Conference                 age Technologies (FAST), pp. 149–166 (2016)
    (ATC), pp. 1007–1019 (2018)                                             44. Lu, L., et al.: WiscKey: separating keys from values in SSD-
16. Chang, F., et al.: Bigtable: a distributed storage system for struc-        conscious storage. In: USENIX Conference on File and Storage
    tured data. ACM TOCS 26(2), 4:1–4:26 (2008)                                 Technologies (FAST), pp. 133–148 (2016)
17. Chazelle, B., Guibas, L.J.: Fractional cascading: I. A data structur-   45. Luo, C., Carey, M.J.: Efficient data ingestion and query processing
    ing technique. Algorithmica 1(1), 133–162 (1986)                            for LSM-based storage systems. PVLDB 12(5), 531–543 (2019)
18. Chen, G.J., et al.: Realtime data processing at Facebook. In: ACM       46. MyRocks. http://myrocks.io/
    SIGMOD, pp. 1087–1098 (2016)                                            47. Mathieu, C., et al.: Bigtable merge compaction. CoRR
19. Dragon: A distributed graph query engine. https://code.fb.com/              arXiv:1407.3008 (2014)
    data-infrastructure/dragon-a-distributed-graph-query-engine/            48. Mei, F., et al.: LSM-tree managed storage for large-scale key-value
20. Dayan, N., Idreos, S.: Dostoevsky: Better space-time trade-offs for         store. In: ACM SoCC, pp. 142–156 (2017)
    LSM-tree based key-value stores via adaptive removal of superflu-       49. Mei, F., et al.: SifrDB: a unified solution for write-optimized key-
    ous merging. In: ACM SIGMOD, pp. 505–520 (2018)                             value stores in large datacenter. In: ACM SoCC, pp. 477–489
21. Dayan, N., et al.: Monkey: optimal navigable key-value store. In:           (2018)
    ACM SIGMOD, pp. 79–94 (2017)                                            50. Muth, P., et al.: The LHAM log-structured history data access
22. Dayan, N., et al.: Optimal Bloom filters and adaptive merging for           method. VLDBJ 8(3), 199–221 (2000)
    LSM-trees. ACM TODS 43(4), 16:1–16:48 (2018)                            51. O’Neil, P., et al.: The log-structured merge-tree (LSM-tree). Acta
23. DeCandia, G., et al.: Dynamo: Amazon’y highly available key-                Inf. 33(4), 351–385 (1996)
    value store. In: ACM SOSP, pp. 205–220 (2007)                           52. Pan, F.F., et al.: dCompaction: speeding up compaction of the LSM-
24. Dong, S., et al.: Optimizing space amplification in RocksDB. In:            tree via delayed compaction. J. Comput. Sci. Technol. 32(1), 41–54
    CIDR, vol. 3, p. 3 (2017)                                                   (2017)
25. D’silva, J.V., et al.: Secondary indexing techniques for key-value      53. Papagiannis, A., et al.: An efficient memory-mapped key-value
    stores: two rings to rule them all. In: International Workshop On           store for flash storage. In: ACM SoCC, pp. 490–502 (2018)
    Design, Optimization, Languages and Analytical Processing of Big        54. Pugh, W.: Skip lists: a probabilistic alternative to balanced trees.
    Data (DOLAP) (2017)                                                         CACM 33(6), 668–676 (1990)
26. Duan, H., et al.: Incremental materialized view maintenance on          55. Putze, F., et al.: Cache-, hash-, and space-efficient bloom filters. J.
    distributed log-structured merge-tree. In: DASFAA, pp. 682–700              Exp. Algorithmics 14, 4:4.4–4:4.18 (2010)
    (2018)                                                                  56. Qader, M.A., et al.: A comparative study of secondary indexing
27. Fagin, R., et al.: Optimal aggregation algorithms for middleware.           techniques in LSM-based NoSQL databases. In: ACM SIGMOD,
    In: ACM PODS, pp. 102–113 (2001)                                            pp. 551–566 (2018)
28. Fan, B., et al.: Cuckoo filter: practically better than bloom. In:      57. RocksDB. http://rocksdb.org/
    International Conference on emerging Networking EXperiments             58. Raju, P., et al.: PebblesDB: building key-value stores using frag-
    and Technologies (CoNEXT), pp. 75–88 (2014)                                 mented log-structured merge trees. In: ACM SOSP, pp. 497–514
29. Fang, Y., et al.: Spatial indexing in Microsoft SQL Server 2008. In:        (2017)
    ACM SIGMOD, pp. 1207–1216 (2008)                                        59. Ren, K., et al.: SlimDB: a space-efficient key-value storage engine
30. Golan-Gueta, G., et al.: Scaling concurrent log-structured data             for semi-sorted data. PVLDB 10(13), 2037–2048 (2017)
    stores. In: European Conference on Computer Systems (EuroSys),          60. Rosenblum, M., Ousterhout, J.K.: The design and implementation
    pp. 32:1–32:14 (2015)                                                       of a log-structured file system. ACM TOCS 10(1), 26–52 (1992)
31. Guttman, A.: R-trees: a dynamic index structure for spatial search-     61. Sears, R., Ramakrishnan, R.: bLSM: a general purpose log struc-
    ing. In: ACM SIGMOD, pp. 47–57 (1984)                                       tured merge tree. In: ACM SIGMOD, pp. 217–228 (2012)
32. HBase. https://hbase.apache.org/                                        62. Seltzer, M.I.: File system performance and transaction support.
33. Haerder, T., Reuter, A.: Principles of transaction-oriented database        Tech. rep., PhD Thesis, Department of Electrical Engineering and
    recovery. ACM CSUR 15(4), 287–317 (1983)                                    Computer Sciences, University of California Berkeley (1992)
34. Jagadish, H.V., et al.: Incremental organization for data recording     63. Severance, D.G., Lohman, G.M.: Differential files: their appli-
    and warehousing. In: VLDB, pp. 16–25 (1997)                                 cation to the maintenance of large databases. ACM TODS 1(3),
35. Jermaine, C., et al.: The partitioned exponential file for database         256–267 (1976)
    storage management. VLDBJ 16(4), 417–437 (2007)                         64. Shetty, P.J., et al.: Building workload-independent storage with VT-
36. Kannan, S., et al.: Redesigning LSMs for nonvolatile memory with            trees. In: USENIX Conference on File and Storage Technologies
    NoveLSM. In: USENIX Annual Technical Conference (ATC), pp.                  (FAST), pp. 17–30 (2013)
    993–1005 (2018)                                                         65. Stonebraker, M.: The design of the Postgres storage system. In:
37. Khodaei, A., et al.: Hybrid indexing and seamless ranking of spatial        VLDB, pp. 289–300 (1987)
    and textual features of web documents. In: Database and Expert          66. Tan, W., et al.: Diff-index: differentiated index in distributed log-
    Systems Applications (DEXA), pp. 450–466 (2010)                             structured data stores. In: EDBT, pp. 700–711 (2014)
38. Kim, T., et al.: Supporting similarity queries in Apache AsterixDB.     67. Tang, Y., et al.: Deferred lightweight indexing for log-structured
    In: EDBT, pp. 528–539 (2018)                                                key-value stores. In: International Symposium in Cluster, Cloud,
39. Kim, Y., et al.: A comparative study of log-structured merge-tree-          and Grid Computing (CCGrid), pp. 11–20 (2015)
    based spatial indexes for big data. In: ICDE, pp. 147–150 (2017)        68. Teng, D., et al.: LSbM-tree: Re-enabling buffer caching in data
40. LevelDB. http://leveldb.org/                                                management for mixed reads and writes. In: IEEE International
41. Lawder, J.: The application of space-filling curves to the storage          Conference on Distributed Computing Systems (ICDCS), pp. 68–
    and retrieval of multi-dimensional data. Ph.D. thesis, PhD Thesis,          79 (2017)
    University of London, UK (2000)



                                                                                                                                          123
                                                   Author's personal copy
                                                                                                                                   C. Luo, M. J. Carey


69. Teng, D., et al.: A low-cost disk solution enabling LSM-tree to          80. Yoon, H., et al.: Mutant: Balancing storage cost and latency in
    achieve high performance for mixed read/write workloads. ACM                 LSM-tree data stores. In: ACM SoCC, pp. 162–173 (2018)
    TOS 14(2), 15:1–15:26 (2018)                                             81. Yue, Y., et al.: Building an efficient put-intensive key-value store
70. Thonangi, R., Yang, J.: On log-structured merge for solid-state              with skip-tree. IEEE Trans. Parallel Distrib. Syst. 28(4), 961–973
    drives. In: ICDE, pp. 683–694 (2017)                                         (2017)
71. Thonangi, R., et al.: A practical concurrent index for solid-state       82. Zhang, W., et al.: Improving write performance of LSMT-based
    drives. In: ACM CIKM, pp. 1332–1341 (2012)                                   key-value store. In: International Conference on Parallel and Dis-
72. Turner, J.: New directions in communications (or which way to the            tributed Systems (ICPADS), pp. 553–560 (2016)
    information age?). IEEE Commun. Mag. 24(10), 8–15 (1986)                 83. Zhang, Y., et al.: ElasticBF: Fine-grained and elastic bloom filter
73. Vinçon, T., et al.: NoFTL-KV: Tackling write-amplification on KV-            towards efficient read for LSM-tree-based KV stores. In: USENIX
    stores with native storage management. In: EDBT, pp. 457–460                 Workshop on Hot Topics in Storage and File Systems (HotStorage)
    (2018)                                                                       (2018)
74. Wang, P., et al.: An efficient design and implementation of LSM-         84. Zhang, Z., et al.: Pipelined compaction for the LSM-tree. In:
    tree based key-value store on open-channel SSD. In: European                 IEEE International Parallel & Distributed Processing Symposium
    Conference on Computer Systems (EuroSys), pp. 16:1–16:14                     (IPDPS), pp. 777–786 (2014)
    (2014)                                                                   85. Zhu, Y., et al.: An efficient bulk loading approach of secondary
75. Wu, L., et al.: LSII: An indexing structure for exact real-time search       index in distributed log-structured data stores. In: DASFAA, pp.
    on microblogs. In: ICDE, pp. 482–493 (2013)                                  87–102 (2017)
76. Wu, X., et al.: LSM-trie: an LSM-tree-based ultra-large key-value
    store for small data. In: USENIX Annual Technical Conference
    (ATC), pp. 71–82 (2015)
                                                                             Publisher’s Note Springer Nature remains neutral with regard to juris-
77. Yao, A.C.C.: On random 2–3 trees. Acta Inf. 9(2), 159–170 (1978)
                                                                             dictional claims in published maps and institutional affiliations.
78. Yao, T., et al.: Building efficient key-value stores via a lightweight
    compaction tree. ACM TOS 13(4), 29:1–29:28 (2017)
79. Yao, T., et al.: A light-weight compaction tree to reduce I/O
    amplification toward efficient key-value stores. In: International
    Conference on Massive Storage Systems and Technology (MSST)
    (2017)




123
