[std](https://doc.rust-lang.org/std/index.html)::[ffi](https://doc.rust-lang.org/std/ffi/index.html)

# Struct VaList Copy item path

[Source](https://doc.rust-lang.org/src/core/ffi/va_list.rs.html#233)

``` rust
pub struct VaList<'a> { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`c_variadic` [\#44930](https://github.com/rust-lang/rust/issues/44930))

Expand description

A variable argument list, ABI-compatible with `va_list` in C.

This type is created in c-variadic functions when `...` is desugared. A `VaList` is automatically initialized (equivalent to calling `va_start` in C).

``` rust
#![feature(c_variadic)]

use std::ffi::VaList;

/// # Safety
/// Must be passed at least `count` arguments of type `i32`.
unsafe extern "C" fn my_func(count: u32, ap: ...) -> i32 {
    unsafe { vmy_func(count, ap) }
}

/// # Safety
/// Must be passed at least `count` arguments of type `i32`.
unsafe fn vmy_func(count: u32, mut ap: VaList<'_>) -> i32 {
    let mut sum = 0;
    for _ in 0..count {
        sum += unsafe { ap.next_arg::<i32>() };
    }
    sum
}

assert_eq!(unsafe { my_func(1, 42i32) }, 42);
assert_eq!(unsafe { my_func(3, 42i32, -7i32, 20i32) }, 55);
```

The [`VaList::next_arg`](https://doc.rust-lang.org/std/ffi/struct.VaList.html#method.next_arg "method std::ffi::VaList::next_arg") method reads the next argument from the variable argument list, and is equivalent to C `va_arg`.

Cloning a `VaList` performs the equivalent of C `va_copy`, producing an independent cursor that arguments can be read from without affecting the original. Dropping a `VaList` performs the equivalent of C `va_end`.

A `VaList` can be used across an FFI boundary, and fully matches the platform’s `va_list` in terms of layout and ABI.

## Implementations

[Source](https://doc.rust-lang.org/src/core/ffi/va_list.rs.html#381)

[Source](https://doc.rust-lang.org/src/core/ffi/va_list.rs.html#406)

🔬This is a nightly-only experimental API. (`c_variadic` [\#44930](https://github.com/rust-lang/rust/issues/44930))

Read the next argument from the variable argument list.

Only types that implement [`VaArgSafe`](https://doc.rust-lang.org/std/ffi/trait.VaArgSafe.html "trait std::ffi::VaArgSafe") can be read from a variable argument list.

##### Safety

This function is safe to call only if all of the following conditions are satisfied:

- There is another c-variadic argument to read.
- The actual type of the argument `U` is compatible with `T` (as defined below).
- If `U` and `T` are both integer types, then the value passed by the caller must be representable in both types.

Types `T` and `U` are compatible when:

- `T` and `U` are the same type.
- `T` and `U` are integer types of the same size.
- `T` and `U` are both pointers, and their target types are compatible.
- `T` is a pointer to [`c_void`](https://doc.rust-lang.org/std/ffi/enum.c_void.html "enum std::ffi::c_void") and `U` is a pointer to [`i8`](https://doc.rust-lang.org/std/primitive.i8.html "primitive i8") or [`u8`](https://doc.rust-lang.org/std/primitive.u8.html "primitive u8"), or vice versa.

## Trait Implementations

[Source](https://doc.rust-lang.org/src/core/ffi/va_list.rs.html#253)

[Source](https://doc.rust-lang.org/src/core/ffi/va_list.rs.html#258)

Clone the [`VaList`](https://doc.rust-lang.org/std/ffi/struct.VaList.html "struct std::ffi::VaList"), producing a second independent cursor into the variable argument list.

Corresponds to `va_copy` in C.

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/142757 "Tracking issue for const_clone")) · [Source](https://doc.rust-lang.org/src/core/clone.rs.html#245-247)

Performs copy-assignment from `source`. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#method.clone_from)

[Source](https://doc.rust-lang.org/src/core/ffi/va_list.rs.html#238)

[Source](https://doc.rust-lang.org/src/core/ffi/va_list.rs.html#239)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/core/ffi/va_list.rs.html#268)

[Source](https://doc.rust-lang.org/src/core/ffi/va_list.rs.html#273)

Drop the [`VaList`](https://doc.rust-lang.org/std/ffi/struct.VaList.html "struct std::ffi::VaList").

Corresponds to `va_end` in C.

[Source](https://doc.rust-lang.org/src/core/ops/drop.rs.html#267)

🔬This is a nightly-only experimental API. (`pin_ergonomics` [\#130494](https://github.com/rust-lang/rust/issues/130494))

Execute the destructor for this type, but different to [`Drop::drop`](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop "method std::ops::Drop::drop"), it requires `self` to be pinned. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.pin_drop)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.