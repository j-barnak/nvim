# miasm.expression.expression

## Module variables

- `TOK_INF = '<'`
- `TOK_INF_SIGNED = TOK_INF + 's'`
- `TOK_INF_UNSIGNED = TOK_INF + 'u'`
- `TOK_INF_EQUAL = '<='`
- `TOK_INF_EQUAL_SIGNED = TOK_INF_EQUAL + 's'`
- `TOK_INF_EQUAL_UNSIGNED = TOK_INF_EQUAL + 'u'`
- `TOK_EQUAL = '=='`
- `TOK_POS = 'pos'`
- `TOK_POS_STRICT = 'Spos'`
- `EXPRINT = 1`
- `EXPRID = 2`
- `EXPRLOC = 3`
- `EXPRASSIGN = 4`
- `EXPRCOND = 5`
- `EXPRMEM = 6`
- `EXPROP = 7`
- `EXPRSLICE = 8`
- `EXPRCOMPOSE = 9`
- `PRIORITY_MAX = len(priorities_list) - 1`
- `EXPR_ORDER_DICT = {ExprId: 1, ExprLoc: 2, ExprCond: 3, ExprMem: 4, ExprOp: 5, ExprSlice: 6, ExprCompose: 7, ExprInt: 8}`

## Classes

### class `DiGraphExpr(DiGraph)`

Enhanced graph for Expression display
Expression are displayed as a tree with node and edge labeled
with only relevant information

- **`node2str(self, node)`**
- **`edge2str(self, nfrom, nto)`**

### class `LocKey(object)`

- **`__init__(self, key)`**
- **`__hash__(self)`**
- **`__eq__(self, other)`**
- **`__ne__(self, other)`**
- **`__lt__(self, other)`**
- **`__repr__(self)`**
- **`__str__(self)`**

### class `ExprWalkBase(object)`

Walk through sub-expressions, call @callback on them.
If @callback returns a non None value, stop walk and return this value

- **`__init__(self, callback)`**
- **`visit(self, expr, *args, **kwargs)`**

### class `ExprWalk(ExprWalkBase)`

Walk through sub-expressions, call @callback on them.
If @callback returns a non None value, stop walk and return this value
Use cache mechanism.

- **`__init__(self, callback)`**
- **`visit(self, expr, *args, **kwargs)`**

### class `ExprGetR(ExprWalkBase)`

Return ExprId/ExprMem used by a given expression

- **`__init__(self, mem_read=False, cst_read=False)`**
- **`get_r_leaves(self, expr)`**
- **`visit(self, expr, *args, **kwargs)`**
- **`visit_inner(self, expr, *args, **kwargs)`**

### class `ExprVisitorBase(object)`

Rebuild expression by visiting sub-expressions

- **`visit(self, expr, *args, **kwargs)`**

### class `ExprVisitorCallbackTopToBottom(ExprVisitorBase)`

Rebuild expression by visiting sub-expressions
Call @callback on each sub-expression
if @callback return non None value, replace current node with this value
Else, continue visit of sub-expressions

- **`__init__(self, callback)`**
- **`visit(self, expr, *args, **kwargs)`**
- **`visit_inner(self, expr, *args, **kwargs)`**

### class `ExprVisitorCallbackBottomToTop(ExprVisitorBase)`

Rebuild expression by visiting sub-expressions
Call @callback from leaves to root expressions

- **`__init__(self, callback)`**
- **`visit(self, expr, *args, **kwargs)`**
- **`visit_inner(self, expr, *args, **kwargs)`**

### class `ExprVisitorCanonize(ExprVisitorCallbackBottomToTop)`

- **`__init__(self)`**
- **`canonize(self, expr)`**

### class `ExprVisitorContains(ExprWalkBase)`

Visitor to test if a needle is in an Expression
Cache results

- **`__init__(self)`**
- **`eq_expr(self, expr, needle, *args, **kwargs)`**
- **`visit(self, expr, needle, *args, **kwargs)`**
- **`contains(self, expr, needle)`**

### class `Expr(object)`

Parent class for Miasm Expressions

- **`set_size(self, _)`**
- **`__init__(self, size)`** - Instantiate an Expr with size @size
- **`get_object(expr_cls, args)`**
- **`get_is_canon(self)`**
- **`set_is_canon(self, value)`**
- **`__str__(self)`**
- **`__getitem__(self, i)`**
- **`get_size(self)`**
- **`is_function_call(self)`** - Returns true if the considered Expr is a function call
- **`__repr__(self)`**
- **`__hash__(self)`**
- **`__eq__(self, other)`**
- **`__ne__(self, other)`**
- **`__lt__(self, other)`**
- **`__add__(self, other)`**
- **`__sub__(self, other)`**
- **`__truediv__(self, other)`**
- **`__floordiv__(self, other)`**
- **`__mod__(self, other)`**
- **`__mul__(self, other)`**
- **`__lshift__(self, other)`**
- **`__rshift__(self, other)`**
- **`__xor__(self, other)`**
- **`__or__(self, other)`**
- **`__and__(self, other)`**
- **`__neg__(self)`**
- **`__pow__(self, other)`**
- **`__invert__(self)`**
- **`copy(self)`** - Deep copy of the expression
- **`__deepcopy__(self, _)`**
- **`replace_expr(self, dct)`** - Find and replace sub expression using dct
- **`canonize(self)`** - Canonize the Expression
- **`msb(self)`** - Return the Most Significant Bit
- **`zeroExtend(self, size)`** - Zero extend to size
- **`signExtend(self, size)`** - Sign extend to size
- **`graph_recursive(self, graph)`** - Recursive method used by graph
- **`graph(self)`** - Return a DiGraph instance standing for Expr tree
- **`set_mask(self, value)`**
- **`is_int(self, value=None)`**
- **`is_id(self, name=None)`**
- **`is_loc(self, label=None)`**
- **`is_aff(self)`**
- **`is_assign(self)`**
- **`is_cond(self)`**
- **`is_mem(self)`**
- **`is_op(self, op=None)`**
- **`is_slice(self, start=None, stop=None)`**
- **`is_compose(self)`**
- **`is_op_segm(self)`** - Returns True if is ExprOp and op == 'segm'
- **`is_mem_segm(self)`** - Returns True if is ExprMem and ptr is_op_segm
- **`__contains__(self, expr)`**
- **`visit(self, callback)`** - Apply callback to all sub expression of @self
- **`get_r(self, mem_read=False, cst_read=False)`**
- **`get_w(self, mem_read=False, cst_read=False)`**

### class `ExprInt(Expr)`

An ExprInt represent a constant in Miasm IR.

Some use cases:
 - Constant 0x42
 - Constant -0x30
 - Constant 0x12345678 on 32bits

- **`__init__(self, arg, size)`** - Create an ExprInt from num/size
- **`__reduce__(self)`**
- **`__new__(cls, arg, size)`** - Create an ExprInt from num/size
- **`__str__(self)`**
- **`get_w(self)`**
- **`_exprhash(self)`**
- **`_exprrepr(self)`**
- **`copy(self)`**
- **`depth(self)`**
- **`graph_recursive(self, graph)`**
- **`__int__(self)`**
- **`__long__(self)`**
- **`is_int(self, value=None)`**

### class `ExprId(Expr)`

An ExprId represent an identifier in Miasm IR.

Some use cases:
 - EAX register
 - 'start' offset
 - variable v1

- **`__init__(self, name, size=None)`** - Create an identifier
- **`__reduce__(self)`**
- **`__new__(cls, name, size=None)`**
- **`__str__(self)`**
- **`get_w(self)`**
- **`_exprhash(self)`**
- **`_exprrepr(self)`**
- **`copy(self)`**
- **`depth(self)`**
- **`graph_recursive(self, graph)`**
- **`is_id(self, name=None)`**

### class `ExprLoc(Expr)`

An ExprLoc represent a Label in Miasm IR.

- **`__init__(self, loc_key, size)`** - Create an identifier
- **`__reduce__(self)`**
- **`__new__(cls, loc_key, size)`**
- **`__str__(self)`**
- **`get_w(self)`**
- **`_exprhash(self)`**
- **`_exprrepr(self)`**
- **`copy(self)`**
- **`depth(self)`**
- **`graph_recursive(self, graph)`**
- **`is_loc(self, loc_key=None)`**

### class `ExprAssign(Expr)`

An ExprAssign represent an assignment from an Expression to another one.

Some use cases:
 - var1 <- 2

- **`__init__(self, dst, src)`** - Create an ExprAssign for dst <- src
- **`__reduce__(self)`**
- **`__new__(cls, dst, src)`**
- **`__str__(self)`**
- **`get_w(self)`**
- **`_exprhash(self)`**
- **`_exprrepr(self)`**
- **`copy(self)`**
- **`depth(self)`**
- **`graph_recursive(self, graph)`**
- **`is_aff(self)`**
- **`is_assign(self)`**

### class `ExprAff(ExprAssign)`

DEPRECATED class.
Use ExprAssign instead of ExprAff

- **`__init__(self, dst, src)`**

### class `ExprCond(Expr)`

An ExprCond stand for a condition on an Expr

Use cases:
 - var1 < var2
 - min(var1, var2)
 - if (cond) then ... else ...

- **`__init__(self, cond, src1, src2)`** - Create an ExprCond
- **`__reduce__(self)`**
- **`__new__(cls, cond, src1, src2)`**
- **`__str__(self)`**
- **`get_w(self)`**
- **`_exprhash(self)`**
- **`_exprrepr(self)`**
- **`copy(self)`**
- **`depth(self)`**
- **`graph_recursive(self, graph)`**
- **`is_cond(self)`**

### class `ExprMem(Expr)`

An ExprMem stand for a memory access

Use cases:
 - Memory read
 - Memory write

- **`__init__(self, ptr, size=None)`** - Create an ExprMem
- **`get_arg(self)`**
- **`set_arg(self, value)`**
- **`__reduce__(self)`**
- **`__new__(cls, ptr, size=None)`**
- **`__str__(self)`**
- **`get_w(self)`**
- **`_exprhash(self)`**
- **`_exprrepr(self)`**
- **`copy(self)`**
- **`is_mem_segm(self)`** - Returns True if is ExprMem and ptr is_op_segm
- **`depth(self)`**
- **`graph_recursive(self, graph)`**
- **`is_mem(self)`**

### class `ExprOp(Expr)`

An ExprOp stand for an operation between Expr

Use cases:
 - var1 XOR var2
 - var1 + var2 + var3
 - parity bit(var1)

- **`__init__(self, op, *args)`** - Create an ExprOp
- **`__reduce__(self)`**
- **`__new__(cls, op, *args)`**
- **`__str__(self)`**
- **`get_w(self)`**
- **`_exprhash(self)`**
- **`_exprrepr(self)`**
- **`is_function_call(self)`**
- **`is_infix(self)`**
- **`is_associative(self)`** - Return True iff current operation is associative
- **`is_commutative(self)`** - Return True iff current operation is commutative
- **`copy(self)`**
- **`depth(self)`**
- **`graph_recursive(self, graph)`**
- **`is_op(self, op=None)`**
- **`is_op_segm(self)`** - Returns True if is ExprOp and op == 'segm'

### class `ExprSlice(Expr)`

- **`__init__(self, arg, start, stop)`**
- **`__reduce__(self)`**
- **`__new__(cls, arg, start, stop)`**
- **`__str__(self)`**
- **`get_w(self)`**
- **`_exprhash(self)`**
- **`_exprrepr(self)`**
- **`copy(self)`**
- **`depth(self)`**
- **`slice_rest(self)`** - Return the completion of the current slice
- **`graph_recursive(self, graph)`**
- **`is_slice(self, start=None, stop=None)`**

### class `ExprCompose(Expr)`

Compose is like a hamburger. It concatenate Expressions

- **`__init__(self, *args)`** - Create an ExprCompose
- **`__reduce__(self)`**
- **`__new__(cls, *args)`**
- **`__str__(self)`**
- **`get_w(self)`**
- **`_exprhash(self)`**
- **`_exprrepr(self)`**
- **`copy(self)`**
- **`depth(self)`**
- **`graph_recursive(self, graph)`**
- **`iter_args(self)`**
- **`is_compose(self)`**

## Functions

### `should_parenthesize_child(child, parent)`

### `str_protected_child(child, parent)`

### `is_expr(expr)`

### `is_associative(expr)`

Return True iff current operation is associative

### `is_commutative(expr)`

Return True iff current operation is commutative

### `canonize_to_exprloc(locdb, expr)`

If expr is ExprInt, return ExprLoc with corresponding loc_key
Else, return expr

@expr: Expr instance

### `is_function_call(expr)`

Returns true if the considered Expr is a function call

### `compare_exprs_compose(expr1, expr2)`

### `compare_expr_list_compose(l1_e, l2_e)`

### `compare_expr_list(l1_e, l2_e)`

### `compare_exprs(expr1, expr2)`

Compare 2 expressions for canonization
@expr1: Expr
@expr2: Expr
0  => ==
1  => expr1 > expr2
-1 => expr1 < expr2

### `canonize_expr_list(expr_list)`

### `canonize_expr_list_compose(expr_list)`

### `ExprInt1(i)`

### `ExprInt8(i)`

### `ExprInt16(i)`

### `ExprInt32(i)`

### `ExprInt64(i)`

### `ExprInt_from(expr, i)`

Generate ExprInt with size equal to expression

### `get_expr_ids_visit(expr, ids)`

Visitor to retrieve ExprId in @expr
@expr: Expr

### `get_expr_locs_visit(expr, locs)`

Visitor to retrieve ExprLoc in @expr
@expr: Expr

### `get_expr_ids(expr)`

Retrieve ExprId in @expr
@expr: Expr

### `get_expr_locs(expr)`

Retrieve ExprLoc in @expr
@expr: Expr

### `test_set(expr, pattern, tks, result)`

Test if v can correspond to e. If so, update the context in result.
Otherwise, return False
@expr : Expr to match
@pattern : pattern Expr
@tks : list of ExprId, available jokers
@result : dictionary of ExprId -> Expr, current context

### `match_expr(expr, pattern, tks, result=None)`

Try to match the @pattern expression with the pattern @expr with @tks jokers.
Result is output dictionary with matching joker values.
@expr : Expr pattern
@pattern : Targeted Expr to match
@tks : list of ExprId, available jokers
@result : dictionary of ExprId -> Expr, output matching context

### `MatchExpr(expr, pattern, tks, result=None)`

### `get_rw(exprs)`

### `get_list_rw(exprs, mem_read=False, cst_read=True)`

Return list of read/write reg/cst/mem for each @exprs
@exprs: list of expressions
@mem_read: walk though memory accesses
@cst_read: retrieve constants

### `get_expr_ops(expr)`

Retrieve operators of an @expr
@expr: Expr

### `get_expr_mem(expr)`

Retrieve memory accesses of an @expr
@expr: Expr

### `_expr_compute_cf(op1, op2)`

Get carry flag of @op1 - @op2
Ref: x86 cf flag
@op1: Expression
@op2: Expression

### `_expr_compute_of(op1, op2)`

Get overflow flag of @op1 - @op2
Ref: x86 of flag
@op1: Expression
@op2: Expression

### `_expr_compute_zf(op1, op2)`

Get zero flag of @op1 - @op2
@op1: Expression
@op2: Expression

### `_expr_compute_nf(op1, op2)`

Get negative (or sign) flag of @op1 - @op2
@op1: Expression
@op2: Expression

### `expr_is_equal(op1, op2)`

if op1 == op2:
   Return ExprInt(1, 1)
else:
   Return ExprInt(0, 1)

### `expr_is_not_equal(op1, op2)`

if op1 != op2:
   Return ExprInt(1, 1)
else:
   Return ExprInt(0, 1)

### `expr_is_unsigned_greater(op1, op2)`

UNSIGNED cmp
if op1 > op2:
   Return ExprInt(1, 1)
else:
   Return ExprInt(0, 1)

### `expr_is_unsigned_greater_or_equal(op1, op2)`

Unsigned cmp
if op1 >= op2:
   Return ExprInt(1, 1)
else:
   Return ExprInt(0, 1)

### `expr_is_unsigned_lower(op1, op2)`

Unsigned cmp
if op1 < op2:
   Return ExprInt(1, 1)
else:
   Return ExprInt(0, 1)

### `expr_is_unsigned_lower_or_equal(op1, op2)`

Unsigned cmp
if op1 <= op2:
   Return ExprInt(1, 1)
else:
   Return ExprInt(0, 1)

### `expr_is_signed_greater(op1, op2)`

Signed cmp
if op1 > op2:
   Return ExprInt(1, 1)
else:
   Return ExprInt(0, 1)

### `expr_is_signed_greater_or_equal(op1, op2)`

Signed cmp
if op1 > op2:
   Return ExprInt(1, 1)
else:
   Return ExprInt(0, 1)

### `expr_is_signed_lower(op1, op2)`

Signed cmp
if op1 < op2:
   Return ExprInt(1, 1)
else:
   Return ExprInt(0, 1)

### `expr_is_signed_lower_or_equal(op1, op2)`

Signed cmp
if op1 <= op2:
   Return ExprInt(1, 1)
else:
   Return ExprInt(0, 1)

### `expr_is_NaN(expr)`

Return 1 or 0 on 1 bit if expr represent a NaN value according to IEEE754

### `expr_is_infinite(expr)`

Return 1 or 0 on 1 bit if expr represent an infinite value according to
IEEE754

### `expr_is_IEEE754_zero(expr)`

Return 1 or 0 on 1 bit if expr represent a zero value according to
IEEE754

### `expr_is_IEEE754_denormal(expr)`

Return 1 or 0 on 1 bit if expr represent a denormalized value according
to IEEE754

### `expr_is_qNaN(expr)`

Return 1 or 0 on 1 bit if expr represent a qNaN (quiet) value according to
IEEE754

### `expr_is_sNaN(expr)`

Return 1 or 0 on 1 bit if expr represent a sNaN (signalling) value according
to IEEE754

### `expr_is_float_lower(op1, op2)`

Return 1 on 1 bit if @op1 < @op2, 0 otherwise.
[!] Assume @op1 and @op2 are not NaN
Comparison is the floating point one, defined in IEEE754

### `expr_is_float_equal(op1, op2)`

Return 1 on 1 bit if @op1 == @op2, 0 otherwise.
[!] Assume @op1 and @op2 are not NaN
Comparison is the floating point one, defined in IEEE754
