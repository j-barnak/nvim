[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[windows](https://doc.rust-lang.org/std/os/windows/index.html)::[fs](https://doc.rust-lang.org/std/os/windows/fs/index.html)

# Trait PermissionsExt Copy item path

[Source](https://doc.rust-lang.org/src/std/os/windows/fs.rs.html#397-409)

``` rust
pub trait PermissionsExt {
    // Required methods
    fn file_attributes(&self) -> u32;
    fn set_file_attributes(&mut self, mask: u32);
    fn from_file_attributes(mask: u32) -> Self;
}
```

ⓘ *This trait cannot be implemented outside `std`.*

🔬This is a nightly-only experimental API. (`windows_permissions_ext` [\#152956](https://github.com/rust-lang/rust/issues/152956))

Available on **Windows** only.

Expand description

Windows-specific extensions to [`fs::Permissions`](https://doc.rust-lang.org/std/fs/struct.Permissions.html "struct std::fs::Permissions"). This extension trait provides extra utilities to shows what Windows file attributes are enabled in [`Permissions`](https://doc.rust-lang.org/std/fs/struct.Permissions.html "struct std::fs::Permissions") and to manually set file attributes on [`Permissions`](https://doc.rust-lang.org/std/fs/struct.Permissions.html "struct std::fs::Permissions").

See Microsoft’s [`File Attribute Constants`](https://learn.microsoft.com/en-us/windows/win32/fileio/file-attribute-constants) page to know what file attribute metadata are defined and stored on Windows files.

## Example

``` rust
#![feature(windows_permissions_ext)]
use std::fs::Permissions;
use std::os::windows::fs::PermissionsExt;

const FILE_ATTRIBUTE_SYSTEM: u32 = 0x4;
const FILE_ATTRIBUTE_ARCHIVE: u32 = 0x20;
let my_file_attr = FILE_ATTRIBUTE_SYSTEM | FILE_ATTRIBUTE_ARCHIVE;
let mut permissions = Permissions::from_file_attributes(my_file_attr);
assert_eq!(permissions.file_attributes(), my_file_attr);

const FILE_ATTRIBUTE_HIDDEN: u32 = 0x2;
let new_file_attr = permissions.file_attributes() | FILE_ATTRIBUTE_HIDDEN;
permissions.set_file_attributes(new_file_attr);
assert_eq!(permissions.file_attributes(), new_file_attr);
```

## Required Methods

[Source](https://doc.rust-lang.org/src/std/os/windows/fs.rs.html#400)

🔬This is a nightly-only experimental API. (`windows_permissions_ext` [\#152956](https://github.com/rust-lang/rust/issues/152956))

Returns the file attribute bits.

[Source](https://doc.rust-lang.org/src/std/os/windows/fs.rs.html#404)

🔬This is a nightly-only experimental API. (`windows_permissions_ext` [\#152956](https://github.com/rust-lang/rust/issues/152956))

Sets the file attribute bits.

[Source](https://doc.rust-lang.org/src/std/os/windows/fs.rs.html#408)

🔬This is a nightly-only experimental API. (`windows_permissions_ext` [\#152956](https://github.com/rust-lang/rust/issues/152956))

Creates a new instance from the given file attribute bits.

## Dyn Compatibility

This trait is **not** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

[Source](https://doc.rust-lang.org/src/std/os/windows/fs.rs.html#412-424)