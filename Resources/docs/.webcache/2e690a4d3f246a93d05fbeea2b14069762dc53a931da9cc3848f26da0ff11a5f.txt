[std](https://doc.rust-lang.org/std/index.html)

# Module path Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/path.rs.html#1-4151)

Expand description

Cross-platform path manipulation.

This module provides two types, [`PathBuf`](https://doc.rust-lang.org/std/path/struct.PathBuf.html "struct std::path::PathBuf") and [`Path`](https://doc.rust-lang.org/std/path/struct.Path.html "struct std::path::Path") (akin to [`String`](https://doc.rust-lang.org/std/string/struct.String.html "struct std::string::String") and [`str`](https://doc.rust-lang.org/std/primitive.str.html "primitive str")), for working with paths abstractly. These types are thin wrappers around [`OsString`](https://doc.rust-lang.org/std/ffi/struct.OsString.html "struct std::ffi::OsString") and [`OsStr`](https://doc.rust-lang.org/std/ffi/struct.OsStr.html "struct std::ffi::OsStr") respectively, meaning that they work directly on strings according to the local platform’s path syntax.

Paths can be parsed into [`Component`](https://doc.rust-lang.org/std/path/enum.Component.html "enum std::path::Component")s by iterating over the structure returned by the [`components`](https://doc.rust-lang.org/std/path/struct.Path.html#method.components "method std::path::Path::components") method on [`Path`](https://doc.rust-lang.org/std/path/struct.Path.html "struct std::path::Path"). [`Component`](https://doc.rust-lang.org/std/path/enum.Component.html "enum std::path::Component")s roughly correspond to the substrings between path separators (`/` or `\`). You can reconstruct an equivalent path from components with the [`push`](https://doc.rust-lang.org/std/path/struct.PathBuf.html#method.push "method std::path::PathBuf::push") method on [`PathBuf`](https://doc.rust-lang.org/std/path/struct.PathBuf.html "struct std::path::PathBuf"); note that the paths may differ syntactically by the normalization described in the documentation for the [`components`](https://doc.rust-lang.org/std/path/struct.Path.html#method.components "method std::path::Path::components") method.

### Case sensitivity

Unless otherwise indicated path methods that do not access the filesystem, such as [`Path::starts_with`](https://doc.rust-lang.org/std/path/struct.Path.html#method.starts_with "method std::path::Path::starts_with") and [`Path::ends_with`](https://doc.rust-lang.org/std/path/struct.Path.html#method.ends_with "method std::path::Path::ends_with"), are case sensitive no matter the platform or filesystem. An exception to this is made for Windows drive letters.

### Path normalization

Several methods in this module perform basic path normalization by disregarding repeated separators, non-leading `.` components, and trailing separators. These include:

- Methods for iteration, such as [`Path::components`](https://doc.rust-lang.org/std/path/struct.Path.html#method.components "method std::path::Path::components") and [`Path::iter`](https://doc.rust-lang.org/std/path/struct.Path.html#method.iter "method std::path::Path::iter")
- Methods for inspection, such as [`Path::has_root`](https://doc.rust-lang.org/std/path/struct.Path.html#method.has_root "method std::path::Path::has_root")
- Comparisons using [`PartialEq`](https://doc.rust-lang.org/std/cmp/trait.PartialEq.html "trait std::cmp::PartialEq"), [`PartialOrd`](https://doc.rust-lang.org/std/cmp/trait.PartialOrd.html "trait std::cmp::PartialOrd"), and [`Ord`](https://doc.rust-lang.org/std/cmp/trait.Ord.html "trait std::cmp::Ord")

[`Path::join`](https://doc.rust-lang.org/std/path/struct.Path.html#method.join "method std::path::Path::join") and [`PathBuf::push`](https://doc.rust-lang.org/std/path/struct.PathBuf.html#method.push "method std::path::PathBuf::push") also disregard trailing slashes.

These methods **do not** resolve `..` components or symlinks. For full normalization including `..` resolution, use [`Path::canonicalize`](https://doc.rust-lang.org/std/path/struct.Path.html#method.canonicalize "method std::path::Path::canonicalize") (which does access the filesystem).

### Simple usage

Path manipulation includes both parsing components from slices and building new owned paths.

To parse a path, you can create a [`Path`](https://doc.rust-lang.org/std/path/struct.Path.html "struct std::path::Path") slice from a [`str`](https://doc.rust-lang.org/std/primitive.str.html "primitive str") slice and start asking questions:

``` rust
use std::path::Path;
use std::ffi::OsStr;

let path = Path::new("/tmp/foo/bar.txt");

let parent = path.parent();
assert_eq!(parent, Some(Path::new("/tmp/foo")));

let file_stem = path.file_stem();
assert_eq!(file_stem, Some(OsStr::new("bar")));

let extension = path.extension();
assert_eq!(extension, Some(OsStr::new("txt")));
```

To build or modify paths, use [`PathBuf`](https://doc.rust-lang.org/std/path/struct.PathBuf.html "struct std::path::PathBuf"):

``` rust
use std::path::PathBuf;

// This way works...
let mut path = PathBuf::from("c:\\");

path.push("windows");
path.push("system32");

path.set_extension("dll");

// ... but push is best used if you don't know everything up
// front. If you do, this way is better:
let path: PathBuf = ["c:\\", "windows", "system32.dll"].iter().collect();
```

## Structs

[Ancestors](https://doc.rust-lang.org/std/path/struct.Ancestors.html "struct std::path::Ancestors")  
An iterator over [`Path`](https://doc.rust-lang.org/std/path/struct.Path.html "struct std::path::Path") and its ancestors.

[Components](https://doc.rust-lang.org/std/path/struct.Components.html "struct std::path::Components")  
An iterator over the [`Component`](https://doc.rust-lang.org/std/path/enum.Component.html "enum std::path::Component")s of a [`Path`](https://doc.rust-lang.org/std/path/struct.Path.html "struct std::path::Path").

[Display](https://doc.rust-lang.org/std/path/struct.Display.html "struct std::path::Display")  
Helper struct for safely printing paths with [`format!`](https://doc.rust-lang.org/std/macro.format.html "macro std::format") and `{}`.

[Iter](https://doc.rust-lang.org/std/path/struct.Iter.html "struct std::path::Iter")  
An iterator over the [`Component`](https://doc.rust-lang.org/std/path/enum.Component.html "enum std::path::Component")s of a [`Path`](https://doc.rust-lang.org/std/path/struct.Path.html "struct std::path::Path"), as [`OsStr`](https://doc.rust-lang.org/std/ffi/struct.OsStr.html "struct std::ffi::OsStr") slices.

[Path](https://doc.rust-lang.org/std/path/struct.Path.html "struct std::path::Path")  
A slice of a path (akin to [`str`](https://doc.rust-lang.org/std/primitive.str.html "primitive str")).

[PathBuf](https://doc.rust-lang.org/std/path/struct.PathBuf.html "struct std::path::PathBuf")  
An owned, mutable path (akin to [`String`](https://doc.rust-lang.org/std/string/struct.String.html "struct std::string::String")).

[PrefixComponent](https://doc.rust-lang.org/std/path/struct.PrefixComponent.html "struct std::path::PrefixComponent")  
A structure wrapping a Windows path prefix as well as its unparsed string representation.

[StripPrefixError](https://doc.rust-lang.org/std/path/struct.StripPrefixError.html "struct std::path::StripPrefixError")  
An error returned from [`Path::strip_prefix`](https://doc.rust-lang.org/std/path/struct.Path.html#method.strip_prefix "method std::path::Path::strip_prefix") if the prefix was not found.

[NormalizeError](https://doc.rust-lang.org/std/path/struct.NormalizeError.html "struct std::path::NormalizeError")Experimental  
An error returned from [`Path::normalize_lexically`](https://doc.rust-lang.org/std/path/struct.Path.html#method.normalize_lexically "method std::path::Path::normalize_lexically") if a `..` parent reference would escape the path.

## Enums

[Component](https://doc.rust-lang.org/std/path/enum.Component.html "enum std::path::Component")  
A single component of a path.

[Prefix](https://doc.rust-lang.org/std/path/enum.Prefix.html "enum std::path::Prefix")  
Windows path prefixes, e.g., `C:` or `\\server\share`.

## Constants

[MAIN_SEPARATOR](https://doc.rust-lang.org/std/path/constant.MAIN_SEPARATOR.html "constant std::path::MAIN_SEPARATOR")  
The primary separator of path components for the current platform, represented as a [`char`](https://doc.rust-lang.org/std/primitive.char.html "primitive char"); for example, this is `'/'` on Unix and `'\\'` on Windows.

[MAIN_SEPARATOR_STR](https://doc.rust-lang.org/std/path/constant.MAIN_SEPARATOR_STR.html "constant std::path::MAIN_SEPARATOR_STR")  
The primary separator of path components for the current platform, represented as a [`&str`](https://doc.rust-lang.org/std/primitive.str.html "primitive str"); for example, this is `"/"` on Unix and `"\\"` on Windows.

[SEPARATORS](https://doc.rust-lang.org/std/path/constant.SEPARATORS.html "constant std::path::SEPARATORS")Experimental  
All path separators recognized on the current platform, represented as [`char`](https://doc.rust-lang.org/std/primitive.char.html "primitive char")s; for example, this is `&['/'][..]` on Unix and `&['\\', '/'][..]` on Windows. The [primary separator](https://doc.rust-lang.org/std/path/constant.MAIN_SEPARATOR.html "constant std::path::MAIN_SEPARATOR") is always element 0 of the slice.

[SEPARATORS_STR](https://doc.rust-lang.org/std/path/constant.SEPARATORS_STR.html "constant std::path::SEPARATORS_STR")Experimental  
All path separators recognized on the current platform, represented as [`&str`](https://doc.rust-lang.org/std/primitive.str.html "primitive str")s; for example, this is `&["/"][..]` on Unix and `&["\\", "/"][..]` on Windows. The [primary separator](https://doc.rust-lang.org/std/path/constant.MAIN_SEPARATOR_STR.html "constant std::path::MAIN_SEPARATOR_STR") is always element 0 of the slice.

## Functions

[absolute](https://doc.rust-lang.org/std/path/fn.absolute.html "fn std::path::absolute")  
Makes the path absolute without accessing the filesystem.

[is_separator](https://doc.rust-lang.org/std/path/fn.is_separator.html "fn std::path::is_separator")  
Determines whether the character is one of the permitted path separators for the current platform.