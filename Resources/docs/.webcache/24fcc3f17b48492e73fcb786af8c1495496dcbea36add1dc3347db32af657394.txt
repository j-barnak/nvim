# miasm.core.objc

C helper for Miasm:
* raw C to Miasm expression
* Miasm expression to raw C
* Miasm expression to C type

## Module variables

- `PADDING_TYPE_NAME = '___padding___'`
- `OBJC_PRIO = {ObjC: 0, ObjCDecl: 1, ObjCInt: 2, ObjCPtr: 3, ObjCArray: 4, ObjCStruct: 5, ObjCUnion: 6, ObjCEllipsis: 7, ObjCFunc: 8}`

## Classes

### class `ObjC(object)`

Generic ObjC

- **`__init__(self, align, size)`**
- **`align(self)`** - Alignment (in bytes) of the C object
- **`size(self)`** - Size (in bytes) of the C object
- **`cmp_base(self, other)`**
- **`__hash__(self)`**
- **`__str__(self)`**
- **`__eq__(self, other)`**
- **`__ne__(self, other)`**
- **`__lt__(self, other)`**

### class `ObjCDecl(ObjC)`

C Declaration identified

- **`__init__(self, name, align, size)`**
- **`__hash__(self)`**
- **`__repr__(self)`**
- **`__str__(self)`**
- **`__eq__(self, other)`**
- **`__lt__(self, other)`**

### class `ObjCInt(ObjC)`

C integer

- **`__init__(self)`**
- **`__str__(self)`**

### class `ObjCPtr(ObjC)`

C Pointer

- **`__init__(self, objtype, void_p_align, void_p_size)`** - Init ObjCPtr
- **`get_objtype(self)`**
- **`set_objtype(self, objtype)`**
- **`__hash__(self)`**
- **`__repr__(self)`**
- **`__eq__(self, other)`**
- **`__lt__(self, other)`**

### class `ObjCArray(ObjC)`

C array (test[XX])

- **`__init__(self, objtype, elems)`** - Init ObjCArray
- **`__hash__(self)`**
- **`__repr__(self)`**
- **`__eq__(self, other)`**
- **`__lt__(self, other)`**

### class `ObjCStruct(ObjC)`

C object for structures

- **`__init__(self, name, align, size, fields)`**
- **`__hash__(self)`**
- **`__repr__(self)`**
- **`__str__(self)`**
- **`__eq__(self, other)`**
- **`__lt__(self, other)`**

### class `ObjCUnion(ObjC)`

C object for unions

- **`__init__(self, name, align, size, fields)`**
- **`__hash__(self)`**
- **`__repr__(self)`**
- **`__str__(self)`**
- **`__eq__(self, other)`**
- **`__lt__(self, other)`**

### class `ObjCEllipsis(ObjC)`

C integer

- **`__init__(self)`**

### class `ObjCFunc(ObjC)`

C object for Functions

- **`__init__(self, name, abi, type_ret, args, void_p_align, void_p_size)`**
- **`__hash__(self)`**
- **`__repr__(self)`**
- **`__str__(self)`**
- **`__eq__(self, other)`**
- **`__lt__(self, other)`**

### class `CGen(object)`

Generic object to represent a C expression

- **`__init__(self, ctype)`**
- **`ctype(self)`** - Type (ObjC instance) of the current object
- **`__hash__(self)`**
- **`__eq__(self, other)`**
- **`__ne__(self, other)`**
- **`to_c(self)`** - Generate corresponding C
- **`to_expr(self)`** - Generate Miasm expression representing the C access

### class `CGenInt(CGen)`

Int C object

- **`__init__(self, integer)`**
- **`integer(self)`** - Value of the object
- **`__hash__(self)`**
- **`__eq__(self, other)`**
- **`__ne__(self, other)`**
- **`to_c(self)`** - Generate corresponding C
- **`__repr__(self)`**
- **`to_expr(self)`** - Generate Miasm expression representing the C access

### class `CGenId(CGen)`

ID of a C object

- **`__init__(self, ctype, name)`**
- **`name(self)`** - Name of the Id
- **`__hash__(self)`**
- **`__eq__(self, other)`**
- **`__repr__(self)`**
- **`to_c(self)`** - Generate corresponding C
- **`to_expr(self)`** - Generate Miasm expression representing the C access

### class `CGenField(CGen)`

Field of a C struct/union

IN:
- struct (not ptr struct)
- field name
OUT:
- input type of the field => output type
- X[] => X[]
- X => X*

- **`__init__(self, struct, field, fieldtype, void_p_align, void_p_size)`**
- **`struct(self)`** - Structure containing the field
- **`field(self)`** - Field name
- **`__hash__(self)`**
- **`__eq__(self, other)`**
- **`to_c(self)`** - Generate corresponding C
- **`__repr__(self)`**
- **`to_expr(self)`** - Generate Miasm expression representing the C access

### class `CGenArray(CGen)`

C Array

This object does *not* deref the source, it only do object casting.

IN:
- obj
OUT:
- X* => X*
- ..[][] => ..[]
- X[] => X*

- **`__init__(self, base, elems, void_p_align, void_p_size)`**
- **`base(self)`** - Base object supporting the array
- **`elems(self)`** - Number of elements in the array
- **`__hash__(self)`**
- **`__eq__(self, other)`**
- **`__repr__(self)`**
- **`to_c(self)`** - Generate corresponding C
- **`to_expr(self)`** - Generate Miasm expression representing the C access

### class `CGenDeref(CGen)`

C dereference

IN:
- ptr
OUT:
- X* => X

- **`__init__(self, ptr)`**
- **`ptr(self)`** - Pointer object
- **`__hash__(self)`**
- **`__eq__(self, other)`**
- **`__repr__(self)`**
- **`to_c(self)`** - Generate corresponding C
- **`to_expr(self)`** - Generate Miasm expression representing the C access

### class `ExprToAccessC(ExprReducer)`

Generate the C access object(s) for a given native Miasm expression
Example:
IN:
@32[ptr_Test]
OUT:
[<CGenDeref <CGenArray <CGenField <CGenDeref <CGenId ptr_Test>> a>>>]

An expression may be represented by multiple accessor (due to unions).

- **`__init__(self, expr_types, types_mngr, enforce_strict_access=True)`** - Init GenCAccess
- **`updt_expr_types(self, expr_types)`** - Update expr_types
- **`cgen_access(self, cgenobj, base_type, offset, deref, lvl=0)`** - Return the access(es) which lead to the element at @offset of an
- **`reduce_known_expr(self, node, ctxt, **kwargs)`** - Generate access for known expr
- **`reduce_int(self, node, **kwargs)`** - Generate access for ExprInt
- **`get_solo_type(self, node)`** - Return the type of the @node if it has only one possible type,
- **`reduce_op(self, node, lvl=0, **kwargs)`** - Generate access for ExprOp
- **`reduce_mem(self, node, lvl=0, **kwargs)`** - Generate access for ExprMem:
- **`get_accesses(self, expr, expr_context=None)`** - Generate C access(es) for the native Miasm expression @expr

### class `ExprCToExpr(ExprReducer)`

Translate a Miasm expression (representing a C access) into a native
Miasm expression and its C type:

Example:

IN: ((ptr_struct -> f_mini) field x)
OUT: @32[ptr_struct + 0x80], int


Tricky cases:
Struct S0 {
    int x;
    int y[0x10];
}

Struct S1 {
    int a;
    S0 toto;
}

S1* ptr;

Case 1:
ptr->toto => ptr + 0x4
&(ptr->toto) => ptr + 0x4

Case 2:
(ptr->toto).x => @32[ptr + 0x4]
&((ptr->toto).x) => ptr + 0x4

Case 3:
(ptr->toto).y => ptr + 0x8
&((ptr->toto).y) => ptr + 0x8

Case 4:
(ptr->toto).y[1] => @32[ptr + 0x8 + 0x4]
&((ptr->toto).y[1]) => ptr + 0x8 + 0x4

- **`__init__(self, expr_types, types_mngr)`** - Init ExprCAccess
- **`updt_expr_types(self, expr_types)`** - Update expr_types
- **`reduce_known_expr(self, node, ctxt, **kwargs)`** - Reduce known expressions
- **`reduce_int(self, node, **kwargs)`** - Reduce ExprInt
- **`reduce_op_memberof(self, node, **kwargs)`** - Reduce -> operator
- **`reduce_op_field(self, node, **kwargs)`** - Reduce field operator (Struct or Union)
- **`reduce_op_array(self, node, **kwargs)`** - Reduce array operator
- **`reduce_op_addr(self, node, **kwargs)`** - Reduce addr operator
- **`reduce_op_deref(self, node, **kwargs)`** - Reduce deref operator
- **`get_expr(self, expr, c_context)`** - Translate a Miasm expression @expr (representing a C access) into a

### class `CTypesManager(object)`

Represent a C object, without any layout information

- **`__init__(self, types_ast, leaf_types)`**
- **`void_ptr(self)`** - Retrieve a void* objc
- **`padding(self)`** - Retrieve a padding ctype
- **`_get_objc(self, type_id, resolved=None, to_fix=None, lvl=0)`**
- **`get_objc(self, type_id)`** - Get the ObjC corresponding to the CType @type_id
- **`check_objc(self, objc, done=None)`** - Ensure each sub ObjC is resolved
- **`size_to_int(self, size)`** - Resolve an array size
- **`struct_compute_field_offset(self, obj, offset)`** - Compute the offset of the field @obj in the current structure
- **`struct_compute_align_size(self, align_max, size)`** - Compute the alignment and size of the current structure
- **`union_compute_align_size(self, align_max, size)`** - Compute the alignment and size of the current union

### class `CTypesManagerNotPacked(CTypesManager)`

Store defined C types (not packed)

- **`struct_compute_field_offset(self, obj, offset)`** - Compute the offset of the field @obj in the current structure
- **`struct_compute_align_size(self, align_max, size)`** - Compute the alignment and size of the current structure
- **`union_compute_align_size(self, align_max, size)`** - Compute the alignment and size of the current union

### class `CTypesManagerPacked(CTypesManager)`

Store defined C types (packed form)

- **`struct_compute_field_offset(self, _, offset)`** - Compute the offset of the field @obj in the current structure
- **`struct_compute_align_size(self, _, size)`** - Compute the alignment and size of the current structure
- **`union_compute_align_size(self, align_max, size)`** - Compute the alignment and size of the current union

### class `CHandler(object)`

C manipulator for Miasm
Miasm expr <-> C

- **`__init__(self, types_mngr, expr_types=None, C_types=None, simplify_c=access_simplifier, enforce_strict_access=True)`**
- **`updt_expr_types(self, expr_types)`** - Update expr_types
- **`expr_to_c_access(self, expr, expr_context=None)`** - Generate the C access object(s) for a given native Miasm expression.
- **`expr_to_c_and_types(self, expr, expr_context=None)`** - Generate the C access string and corresponding type for a given
- **`expr_to_c(self, expr, expr_context=None)`** - Convert a Miasm @expr into it's C equivalent string
- **`expr_to_types(self, expr, expr_context=None)`** - Get the possible types of the Miasm @expr
- **`c_to_expr_and_type(self, c_str, c_context=None)`** - Convert a C string expression to a Miasm expression and it's
- **`c_to_expr(self, c_str, c_context=None)`** - Convert a C string expression to a Miasm expression
- **`c_to_type(self, c_str, c_context=None)`** - Get the type of a C string expression

### class `CLeafTypes(object)`

Define C types sizes/alignment for a given architecture

## Functions

### `missing_definition(objtype)`

### `objc_to_str(objc, result=None)`

### `access_simplifier(expr)`

Expression visitor to simplify a C access represented in Miasm

@expr: Miasm expression representing the C access

Example:

IN: (In c: ['*(&((&((*(ptr_Test)).a))[0]))'])
[ExprOp('deref', ExprOp('addr', ExprOp('[]', ExprOp('addr',
ExprOp('field', ExprOp('deref', ExprId('ptr_Test', 64)),
ExprId('a', 64))), ExprInt(0x0, 64))))]

OUT: (In c: ['(ptr_Test)->a'])
[ExprOp('->', ExprId('ptr_Test', 64), ExprId('a', 64))]

### `access_str(expr)`

Return the C string of a C access represented in Miasm

@expr: Miasm expression representing the C access

In:
ExprOp('->', ExprId('ptr_Test', 64), ExprId('a', 64))
OUT:
'(ptr_Test)->a'

### `ast_get_c_access_expr(ast, expr_types, lvl=0)`

Transform C ast object into a C Miasm expression

@ast: parsed pycparser.c_ast object
@expr_types: a dictionary linking ID names to their types
@lvl: actual recursion level

Example:

IN:
StructRef: ->
  ID: ptr_Test
  ID: a

OUT:
ExprOp('->', ExprId('ptr_Test', 64), ExprId('a', 64))

### `parse_access(c_access)`

Parse C access

@c_access: C access string
