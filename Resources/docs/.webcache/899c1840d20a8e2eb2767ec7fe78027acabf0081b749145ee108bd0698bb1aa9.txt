[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_fma Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#777)

``` rust
pub const unsafe fn simd_fma<T>(x: T, y: T, z: T) -> T
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Computes `(x*y) + z` for each element, but without any intermediate rounding.

`T` must be a vector of floats.