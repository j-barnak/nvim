[std](https://doc.rust-lang.org/std/index.html)::[collections](https://doc.rust-lang.org/std/collections/index.html)::[btree_set](https://doc.rust-lang.org/std/collections/btree_set/index.html)

# Struct CursorMut Copy item path

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2159)

``` rust
pub struct CursorMut<'a, K, A = Global>where
    K: 'a,{ /* private fields */ }
```

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Expand description

A cursor over a `BTreeSet` with editing operations.

A `Cursor` is like an iterator, except that it can freely seek back-and-forth, and can safely mutate the set during iteration. This is because the lifetime of its yielded references is tied to its own lifetime, instead of just the underlying map. This means cursors cannot yield multiple elements at once.

Cursors always point to a gap between two elements in the set, and can operate on the two immediately adjacent elements.

A `CursorMut` is created with the [`BTreeSet::lower_bound_mut`](https://doc.rust-lang.org/std/collections/struct.BTreeSet.html#method.lower_bound_mut "method std::collections::BTreeSet::lower_bound_mut") and [`BTreeSet::upper_bound_mut`](https://doc.rust-lang.org/std/collections/struct.BTreeSet.html#method.upper_bound_mut "method std::collections::BTreeSet::upper_bound_mut") methods.

## Implementations

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2246)

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2253)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Advances the cursor to the next gap, returning the element that it moved over.

If the cursor is already at the end of the set then `None` is returned and the cursor is not moved.

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2263)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Advances the cursor to the previous gap, returning the element that it moved over.

If the cursor is already at the start of the set then `None` is returned and the cursor is not moved.

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2271)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Returns a reference to the next element without moving the cursor.

If the cursor is at the end of the set then `None` is returned.

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2279)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Returns a reference to the previous element without moving the cursor.

If the cursor is at the start of the set then `None` is returned.

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2290)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Returns a read-only cursor pointing to the same location as the `CursorMut`.

The lifetime of the returned `Cursor` is bound to that of the `CursorMut`, which means it cannot outlive the `CursorMut` and that the `CursorMut` is frozen for the lifetime of the `Cursor`.

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2305)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Converts the cursor into a [`CursorMutKey`](https://doc.rust-lang.org/std/collections/btree_set/struct.CursorMutKey.html "struct std::collections::btree_set::CursorMutKey"), which allows mutating elements in the tree.

##### Safety

Since this cursor allows mutating elements, you must ensure that the `BTreeSet` invariants are maintained. Specifically:

- The newly inserted element must be unique in the tree.
- All elements in the tree must remain in sorted order.

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2359)

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2374)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Inserts a new element into the set in the gap that the cursor is currently pointing to.

After the insertion the cursor will be pointing at the gap before the newly inserted element.

##### Safety

You must ensure that the `BTreeSet` invariants are maintained. Specifically:

- The newly inserted element must be unique in the tree.
- All elements in the tree must remain in sorted order.

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2392)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Inserts a new element into the set in the gap that the cursor is currently pointing to.

After the insertion the cursor will be pointing at the gap after the newly inserted element.

##### Safety

You must ensure that the `BTreeSet` invariants are maintained. Specifically:

- The newly inserted element must be unique in the tree.
- All elements in the tree must remain in sorted order.

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2407)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Inserts a new element into the set in the gap that the cursor is currently pointing to.

After the insertion the cursor will be pointing at the gap before the newly inserted element.

If the inserted element is not greater than the element before the cursor (if any), or if it not less than the element after the cursor (if any), then an [`UnorderedKeyError`](https://doc.rust-lang.org/std/collections/btree_map/struct.UnorderedKeyError.html "struct std::collections::btree_map::UnorderedKeyError") is returned since this would invalidate the [`Ord`](https://doc.rust-lang.org/std/cmp/trait.Ord.html "trait std::cmp::Ord") invariant between the elements of the set.

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2422)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Inserts a new element into the set in the gap that the cursor is currently pointing to.

After the insertion the cursor will be pointing at the gap after the newly inserted element.

If the inserted element is not greater than the element before the cursor (if any), or if it not less than the element after the cursor (if any), then an [`UnorderedKeyError`](https://doc.rust-lang.org/std/collections/btree_map/struct.UnorderedKeyError.html "struct std::collections::btree_map::UnorderedKeyError") is returned since this would invalidate the [`Ord`](https://doc.rust-lang.org/std/cmp/trait.Ord.html "trait std::cmp::Ord") invariant between the elements of the set.

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2431)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Removes the next element from the `BTreeSet`.

The element that was removed is returned. The cursor position is unchanged (before the removed element).

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2440)

🔬This is a nightly-only experimental API. (`btree_cursors` [\#107540](https://github.com/rust-lang/rust/issues/107540))

Removes the preceding element from the `BTreeSet`.

The element that was removed is returned. The cursor position is unchanged (after the removed element).

## Trait Implementations

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2165)

[Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#2166)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.