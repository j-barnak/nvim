[std](https://doc.rust-lang.org/std/index.html)::[task](https://doc.rust-lang.org/std/task/index.html)

# Struct Waker Copy item path

1.36.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#407)

``` rust
pub struct Waker { /* private fields */ }
```

Expand description

A `Waker` is a handle for waking up a task by notifying its executor that it is ready to be run.

This handle encapsulates a [`RawWaker`](https://doc.rust-lang.org/std/task/struct.RawWaker.html "struct std::task::RawWaker") instance, which defines the executor-specific wakeup behavior.

The typical life of a `Waker` is that it is constructed by an executor, wrapped in a [`Context`](https://doc.rust-lang.org/std/task/struct.Context.html "struct std::task::Context"), then passed to [`Future::poll()`](https://doc.rust-lang.org/std/future/trait.Future.html#tymethod.poll "method std::future::Future::poll"). Then, if the future chooses to return [`Poll::Pending`](https://doc.rust-lang.org/std/task/enum.Poll.html#variant.Pending "variant std::task::Poll::Pending"), it must also store the waker somehow and call [`Waker::wake()`](https://doc.rust-lang.org/std/task/struct.Waker.html#method.wake "method std::task::Waker::wake") when the future should be polled again.

Implements [`Clone`](https://doc.rust-lang.org/std/clone/trait.Clone.html "trait std::clone::Clone"), [`Send`](https://doc.rust-lang.org/std/marker/trait.Send.html "trait std::marker::Send"), and [`Sync`](https://doc.rust-lang.org/std/marker/trait.Sync.html "trait std::marker::Sync"); therefore, a waker may be invoked from any thread, including ones not in any way managed by the executor. For example, this might be done to wake a future when a blocking function call completes on another thread.

Note that it is preferable to use `waker.clone_from(&new_waker)` instead of `*waker = new_waker.clone()`, as the former will avoid cloning the waker unnecessarily if the two wakers [wake the same task](https://doc.rust-lang.org/std/task/struct.Waker.html#method.will_wake "method std::task::Waker::will_wake").

Constructing a `Waker` from a [`RawWaker`](https://doc.rust-lang.org/std/task/struct.RawWaker.html "struct std::task::RawWaker") is unsafe. Implementing the [`Wake`](https://doc.rust-lang.org/alloc/task/trait.Wake.html) trait is a safe alternative that requires memory allocation.

## Implementations

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#418)

1.36.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#439)

Wakes up the task associated with this `Waker`.

As long as the executor keeps running and the task is not finished, it is guaranteed that each invocation of [`wake()`](https://doc.rust-lang.org/std/task/struct.Waker.html#method.wake "method std::task::Waker::wake") (or [`wake_by_ref()`](https://doc.rust-lang.org/std/task/struct.Waker.html#method.wake_by_ref "method std::task::Waker::wake_by_ref")) will be followed by at least one [`poll()`](https://doc.rust-lang.org/std/future/trait.Future.html#tymethod.poll "method std::future::Future::poll") of the task to which this `Waker` belongs, such that the call to [`wake()`](https://doc.rust-lang.org/std/task/struct.Waker.html#method.wake "method std::task::Waker::wake") (or [`wake_by_ref()`](https://doc.rust-lang.org/std/task/struct.Waker.html#method.wake_by_ref "method std::task::Waker::wake_by_ref")) *happens-before* the beginning of the invocation of [`poll()`](https://doc.rust-lang.org/std/future/trait.Future.html#tymethod.poll "method std::future::Future::poll"). This makes it possible to temporarily yield to other tasks while running potentially unbounded processing loops.

Note that the above implies that multiple wake-ups may be coalesced into a single [`poll()`](https://doc.rust-lang.org/std/future/trait.Future.html#tymethod.poll "method std::future::Future::poll") invocation by the executor.

Also note that yielding to competing tasks is not guaranteed: it is the executor’s choice which task to run and the executor may choose to run the current task again.

1.36.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#459)

Wakes up the task associated with this `Waker` without consuming the `Waker`.

This is similar to [`wake()`](https://doc.rust-lang.org/std/task/struct.Waker.html#method.wake "method std::task::Waker::wake"), but may be slightly less efficient in the case where an owned `Waker` is available. This method should be preferred to calling `waker.clone().wake()`.

1.36.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#479)

Returns `true` if this `Waker` and another `Waker` would awake the same task.

This function works on a best-effort basis, and may return false even when the `Waker`s would awaken the same task. However, if this function returns `true`, it is guaranteed that the `Waker`s will awaken the same task.

This function is primarily used for optimization purposes — for example, this type’s [`clone_from`](https://doc.rust-lang.org/std/task/struct.Waker.html#method.clone_from "method std::task::Waker::clone_from") implementation uses it to avoid cloning the waker when they would wake the same task anyway.

1.83.0 (const: 1.83.0) · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#514)

Creates a new `Waker` from the provided `data` pointer and `vtable`.

The `data` pointer can be used to store arbitrary data as required by the executor. This could be e.g. a type-erased pointer to an `Arc` that is associated with the task. The value of this pointer will get passed to all functions that are part of the `vtable` as the first parameter.

It is important to consider that the `data` pointer must point to a thread safe type such as an `Arc`.

The `vtable` customizes the behavior of a `Waker`. For each operation on the `Waker`, the associated function in the `vtable` will be called.

##### Safety

The behavior of the returned `Waker` is undefined if the contract defined in [`RawWakerVTable`](https://doc.rust-lang.org/std/task/struct.RawWakerVTable.html "struct std::task::RawWakerVTable")’s documentation is not upheld.

(Authors wishing to avoid unsafe code may implement the [`Wake`](https://doc.rust-lang.org/alloc/task/trait.Wake.html) trait instead, at the cost of a required heap allocation.)

1.36.0 (const: 1.82.0) · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#533)

Creates a new `Waker` from [`RawWaker`](https://doc.rust-lang.org/std/task/struct.RawWaker.html "struct std::task::RawWaker").

##### Safety

The behavior of the returned `Waker` is undefined if the contract defined in [`RawWaker`](https://doc.rust-lang.org/std/task/struct.RawWaker.html "struct std::task::RawWaker")’s and [`RawWakerVTable`](https://doc.rust-lang.org/std/task/struct.RawWakerVTable.html "struct std::task::RawWakerVTable")’s documentation is not upheld.

(Authors wishing to avoid unsafe code may implement the [`Wake`](https://doc.rust-lang.org/alloc/task/trait.Wake.html) trait instead, at the cost of a required heap allocation.)

1.85.0 (const: 1.85.0) · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#568)

Returns a reference to a `Waker` that does nothing when used.

This is mostly useful for writing tests that need a [`Context`](https://doc.rust-lang.org/std/task/struct.Context.html "struct std::task::Context") to poll some futures, but are not expecting those futures to wake the waker or do not need to do anything specific if it happens.

More generally, using `Waker::noop()` to poll a future means discarding the notification of when the future should be polled again. So it should only be used when such a notification will not be needed to make progress.

If an owned `Waker` is needed, `clone()` this one.

##### Examples

``` rust
use std::future::Future;
use std::task;

let mut cx = task::Context::from_waker(task::Waker::noop());

let mut future = Box::pin(async { 10 });
assert_eq!(future.as_mut().poll(&mut cx), task::Poll::Ready(10));
```

1.83.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#577)

Gets the `data` pointer used to create this `Waker`.

1.83.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#585)

Gets the `vtable` pointer used to create this `Waker`.

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#593)

🔬This is a nightly-only experimental API. (`waker_from_fn_ptr` [\#148457](https://github.com/rust-lang/rust/issues/148457))

Constructs a `Waker` from a function pointer.

## Trait Implementations

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#950)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#951)

Converts this type into a shared reference of the (usually inferred) input type.

1.36.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#613)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#661)

Assigns a clone of `source` to `self`, unless [`self.will_wake(source)`](https://doc.rust-lang.org/std/task/struct.Waker.html#method.will_wake "method std::task::Waker::will_wake") anyway.

This method is preferred over simply assigning `source.clone()` to `self`, as it avoids cloning the waker if `self` is already the same waker.

##### Examples

``` rust
use std::future::Future;
use std::pin::Pin;
use std::sync::{Arc, Mutex};
use std::task::{Context, Poll, Waker};

struct Waiter {
    shared: Arc<Mutex<Shared>>,
}

struct Shared {
    waker: Waker,
    // ...
}

impl Future for Waiter {
    type Output = ();
    fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<()> {
        let mut shared = self.shared.lock().unwrap();

        // update the waker
        shared.waker.clone_from(cx.waker());

        // readiness logic ...
    }
}
```

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#615)

Returns a duplicate of the value. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#tymethod.clone)

1.36.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#680)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#681)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.36.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#669)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#671)

Executes the destructor for this type. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop)

[Source](https://doc.rust-lang.org/src/core/ops/drop.rs.html#267)

🔬This is a nightly-only experimental API. (`pin_ergonomics` [\#130494](https://github.com/rust-lang/rust/issues/130494))

Execute the destructor for this type, but different to [`Drop::drop`](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop "method std::ops::Drop::drop"), it requires `self` to be pinned. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.pin_drop)

1.51.0 · [Source](https://doc.rust-lang.org/src/alloc/task.rs.html#114)

Available on **`target_has_atomic=ptr`** only.

[Source](https://doc.rust-lang.org/src/alloc/task.rs.html#118)

Use a [`Wake`](https://doc.rust-lang.org/std/task/trait.Wake.html "trait std::task::Wake")-able type as a `Waker`.

No heap allocations or atomic operations are used for this conversion.

1.36.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#414)

1.36.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#416)

1.36.0 · [Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#412)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.