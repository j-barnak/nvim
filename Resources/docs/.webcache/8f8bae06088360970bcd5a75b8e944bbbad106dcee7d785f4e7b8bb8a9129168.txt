[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[windows](https://doc.rust-lang.org/std/os/windows/index.html)::[ffi](https://doc.rust-lang.org/std/os/windows/ffi/index.html)

# Trait OsStringExt Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/ffi.rs.html#66-86)

``` rust
pub trait OsStringExt {
    // Required method
    fn from_wide(wide: &[u16]) -> Self;
}
```

ⓘ *This trait cannot be implemented outside `std`.*

Available on **Windows** only.

Expand description

Windows-specific extensions to [`OsString`](https://doc.rust-lang.org/std/ffi/struct.OsString.html "struct std::ffi::OsString").

## Required Methods

1.0.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/ffi.rs.html#85)

Creates an `OsString` from a potentially ill-formed UTF-16 slice of 16-bit code units.

This is lossless: calling [`OsStrExt::encode_wide`](https://doc.rust-lang.org/std/os/windows/ffi/trait.OsStrExt.html#tymethod.encode_wide "method std::os::windows::ffi::OsStrExt::encode_wide") on the resulting string will always return the original code units.

##### Examples

``` rust
use std::ffi::OsString;
use std::os::windows::prelude::*;

// UTF-16 encoding for "Unicode".
let source = [0x0055, 0x006E, 0x0069, 0x0063, 0x006F, 0x0064, 0x0065];

let string = OsString::from_wide(&source[..]);
```

## Dyn Compatibility

This trait is **not** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

1.0.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/ffi.rs.html#89-93)