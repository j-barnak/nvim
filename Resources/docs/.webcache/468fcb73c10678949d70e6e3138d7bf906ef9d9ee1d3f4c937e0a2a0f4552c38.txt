[std](https://doc.rust-lang.org/std/index.html)::[env](https://doc.rust-lang.org/std/env/index.html)

# Function var_os Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/env.rs.html#264-266)

``` rust
pub fn var_os<K: AsRef<OsStr>>(key: K) -> Option<OsString>
```

Expand description

Fetches the environment variable `key` from the current process, returning [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") if the variable isn’t set or if there is another error.

It may return `None` if the environment variable’s name contains the equal sign character (`=`) or the NUL character.

Note that this function will not check if the environment variable is valid Unicode. If you want to have an error on invalid UTF-8, use the [`var`](https://doc.rust-lang.org/std/env/fn.var.html "fn std::env::var") function instead.

## Examples

``` rust
use std::env;

let key = "HOME";
match env::var_os(key) {
    Some(val) => println!("{key}: {val:?}"),
    None => println!("{key} is not defined in the environment.")
}
```

If expecting a delimited variable (such as `PATH`), [`split_paths`](https://doc.rust-lang.org/std/env/fn.split_paths.html "fn std::env::split_paths") can be used to separate items.