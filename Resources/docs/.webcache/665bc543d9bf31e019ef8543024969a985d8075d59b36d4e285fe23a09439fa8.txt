# `pwnlib.adb` — Android Debug Bridge

Provides utilities for interacting with Android devices via the Android Debug Bridge.

## Using Android Devices with Pwntools

Pwntools tries to be as easy as possible to use with Android devices.

If you have only one device attached, everything “just works”.

If you have multiple devices, you have a handful of options to select one, or iterate over the devices.

First and most important is the `context.device` property, which declares the “currently” selected device in any scope. It can be set manually to a serial number, or to a `Device` instance.

    # Take the first available device
    context.device = adb.wait_for_device()

    # Set a device by serial number
    context.device = 'ZX1G22LH8S'

    # Set a device by its product name
    for device in adb.devices():
        if device.product == 'shamu':
            break
    else:
        error("Could not find any shamus!")

Once a device is selected, you can operate on it with any of the functions in the `pwnlib.adb` module.

    # Get a process listing
    print(adb.process(['ps']).recvall())

    # Fetch properties
    print(adb.properties.ro.build.fingerprint)

    # Read and write files
    print(adb.read('/proc/version'))
    adb.write('/data/local/tmp/foo', 'my data')

*class *pwnlib.adb.adb.AdbDevice(*serial*, *type*, *port=None*, *product='unknown'*, *model='unknown'*, *device='unknown'*, *features=None*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L246-L401)  
Encapsulates information about a connected device.

Example:

     >>> device = adb.wait_for_device()
     >>> device.arch
     'amd64'
     >>> device.bits
     64
     >>> device.os
     'android'
     >>> device.product
     'sdk_...phone..._...'
     >>> device.serial
     'emulator-5554'

\_\_getattr\_\_(*name*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L376-L401)  
Provides scoped access to `adb` module propertise, in the context of this device.

     >>> property = 'ro.build.fingerprint'
     >>> device = adb.wait_for_device()
     >>> adb.getprop(property) == device.getprop(property)
     True

\_\_init\_\_(*serial*, *type*, *port=None*, *product='unknown'*, *model='unknown'*, *device='unknown'*, *features=None*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L266-L283)  

\_\_repr\_\_()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L335-L338)  
Return repr(self).

\_\_str\_\_()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L332-L333)  
Return str(self).

\_\_wrapped(*function*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py)  
Wrapps a callable in a scope which selects the current device.

&nbsp;

*class *pwnlib.adb.adb.Partitions[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1499-L1568)  
Enable access to partitions

Example:

     >>> hex(adb.partitions.vda.size)
     '0x...000'

\_\_dir\_\_()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1519-L1521)  
Default dir() implementation.

\_\_weakref\_\_[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py)  
list of weak references to the object

&nbsp;

pwnlib.adb.adb.\_build_date()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1276-L1300)  
Returns the build date in the form YYYY-MM-DD as a string

&nbsp;

pwnlib.adb.adb.adb(*argv*, *\*a*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L77-L97)  
Returns the output of an ADB subcommand.

     >>> adb.adb('get-serialno')
     b'emulator-5554\n'
     >>> adb.adb(['shell', 'uname']) # it is better to use adb.process
     b'Linux\n'

&nbsp;

pwnlib.adb.adb.boot_time() → [int](https://docs.python.org/3/library/functions.html#int "(in Python v3.14)")[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L224-L244)  
Returns:  
Boot time of the device, in Unix time, rounded to the nearest second.

Example:

     >>> import time
     >>> adb.boot_time() < time.time()
     True

&nbsp;

pwnlib.adb.adb.build()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1081-L1084)  
Returns the Build ID of the device.

&nbsp;

pwnlib.adb.adb.compile(*source*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1367-L1445)  
Compile a source file or project with the Android NDK.

Example:

     >>> temp = tempfile.mktemp('.c')
     >>> write(temp, '''
     ... #include <stdio.h>
     ... static char buf[4096];
     ... int main() {
     ...   FILE *fp = fopen("/proc/self/maps", "r");
     ...   int n = fread(buf, 1, sizeof(buf), fp);
     ...   fwrite(buf, 1, n, stdout);
     ...   return 0;
     ... }''')
     >>> filename = adb.compile(temp)
     >>> sent = adb.push(filename, "/data/local/tmp")
     >>> adb.process(sent).recvall()
     b'... /system/lib64/libc++.so\n...'

&nbsp;

pwnlib.adb.adb.current_device(*any=False*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L117-L133)  
Returns an `AdbDevice` instance for the currently-selected device (via `context.device`).

     >>> device = adb.current_device(any=True)
     >>> device
     AdbDevice(serial='emulator-5554', type='device', port='emulator', product='sdk_...phone..._...', model='...', device='...')
     >>> device.port
     'emulator'

&nbsp;

pwnlib.adb.adb.devices(*serial=None*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L99-L115)  
Returns a list of `Device` objects corresponding to the connected devices.

&nbsp;

pwnlib.adb.adb.disable_verity()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L455-L471)  
Disables dm-verity on the device.

&nbsp;

pwnlib.adb.adb.exists(*path*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L732-L750)  
Return `True` if `path` exists on the target device.

Examples:

     >>> adb.exists('/')
     True
     >>> adb.exists('/etc/hosts')
     True
     >>> adb.exists('/does/not/exist')
     False

&nbsp;

pwnlib.adb.adb.fastboot(*args*, *\*a*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1061-L1069)  
Executes a fastboot command.

Returns:  
The command output.

&nbsp;

pwnlib.adb.adb.find_ndk_project_root(*source*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1302-L1315)  
Given a directory path, find the topmost project root.

tl;dr “foo/bar/jni/baz.cpp” ==\> “foo/bar”

&nbsp;

pwnlib.adb.adb.fingerprint()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1071-L1074)  
Returns the device build fingerprint.

&nbsp;

pwnlib.adb.adb.forward(*port*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L938-L943)  
Sets up a port to forward to the device.

&nbsp;

pwnlib.adb.adb.getprop(*name=None*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L998-L1040)  
Reads a properties from the system property store.

Parameters:  
**name** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Optional, read a single property.

Returns:  
If `name` is not specified, a `dict` of all properties is returned. Otherwise, a string is returned with the contents of the named property.

Example:

     >>> adb.getprop()
     {...}

&nbsp;

pwnlib.adb.adb.install(*apk*, *\*arguments*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1572-L1598)  
Install an APK onto the device.

This is a wrapper around ‘pm install’, which backs ‘adb install’.

Parameters:  
- **apk** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path to the APK to intall (e.g. `'foo.apk'`)

- **arguments** – Supplementary arguments to ‘pm install’, e.g. `'-l',`` ``'-g'`.

&nbsp;

pwnlib.adb.adb.interactive(*\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L852-L855)  
Spawns an interactive shell.

&nbsp;

pwnlib.adb.adb.isdir(*path*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L752-L771)  
Return `True` if `path` is a on the target device.

Examples:

     >>> adb.isdir('/')
     True
     >>> adb.isdir('/init')
     False
     >>> adb.isdir('/does/not/exist')
     False

&nbsp;

pwnlib.adb.adb.listdir(*directory='/'*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1047-L1059)  
Returns a list containing the entries in the provided directory.

Note

This uses the SYNC LIST functionality, which runs in the adbd SELinux context. If adbd is running in the su domain (‘adb root’), this behaves as expected.

Otherwise, less files may be returned due to restrictive SELinux policies on adbd.

&nbsp;

pwnlib.adb.adb.logcat(*stream=False*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L945-L964)  
Reads the system log file.

By default, causes logcat to exit after reading the file.

Parameters:  
**stream** ([*bool*](https://docs.python.org/3/library/functions.html#bool "(in Python v3.14)")) – If `True`, the contents are streamed rather than read in a one-shot manner. Default is `False`.

Returns:  
If `stream` is `False`, returns a string containing the log data. Otherwise, it returns a [`pwnlib.tubes.tube.tube`](https://docs.pwntools.com/en/stable/tubes.html#pwnlib.tubes.tube.tube "pwnlib.tubes.tube.tube") connected to the log output.

&nbsp;

pwnlib.adb.adb.makedirs(*path*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L710-L730)  
Create a directory and all parent directories on the target device.

Note

Silently succeeds if the directory already exists.

Examples:

     >>> adb.makedirs('/data/local/tmp/this/is/a/directory/hierarchy')
     >>> adb.listdir('/data/local/tmp/this/is/a/directory')
     ['hierarchy']

&nbsp;

pwnlib.adb.adb.mkdir(*path*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L664-L708)  
Create a directory on the target device.

Note

Silently succeeds if the directory already exists.

Parameters:  
**path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Directory to create.

Examples:

     >>> adb.mkdir('/')

     >>> path = '/data/local/tmp/mkdir_test'
     >>> adb.exists(path)
     False
     >>> adb.mkdir(path)
     >>> adb.exists(path)
     True

     >>> adb.mkdir('/init')
     Traceback (most recent call last):
     ...
     PwnlibException: mkdir failed for /init, File exists

&nbsp;

pwnlib.adb.adb.packages()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1613-L1620)  
Returns a list of packages installed on the system

&nbsp;

pwnlib.adb.adb.pidof(*name*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L966-L979)  
Returns a list of PIDs for the named process.

&nbsp;

pwnlib.adb.adb.proc_exe(*pid*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L981-L996)  
Returns the full path of the executable for the provided PID.

Example:

     >>> adb.proc_exe(1)
     b'/system/bin/init'

&nbsp;

pwnlib.adb.adb.process(*argv*, *\*a*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L823-L850)  
Execute a process on the device.

See [`pwnlib.tubes.process.process`](https://docs.pwntools.com/en/stable/tubes/processes.html#pwnlib.tubes.process.process "pwnlib.tubes.process.process") documentation for more info.

Returns:  
A [`pwnlib.tubes.process.process`](https://docs.pwntools.com/en/stable/tubes/processes.html#pwnlib.tubes.process.process "pwnlib.tubes.process.process") tube.

Examples:

     >>> adb.root()
     >>> print(adb.process(['cat','/proc/version']).recvall().decode('utf-8'))
     Linux version ...

&nbsp;

pwnlib.adb.adb.product()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1076-L1079)  
Returns the device product identifier.

&nbsp;

pwnlib.adb.adb.pull(*remote_path*, *local_path=None*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L514-L547)  
Download a file from the device.

Parameters:  
- **remote_path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path or directory of the file on the device.

- **local_path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path to save the file to. Uses the file’s name by default.

Returns:  
The contents of the file.

Example:

     >>> _=adb.pull('/proc/version', './proc-version')
     >>> print(read('./proc-version').decode('utf-8'))
     Linux version ...

&nbsp;

pwnlib.adb.adb.push(*local_path*, *remote_path*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L549-L606)  
Upload a file to the device.

Parameters:  
- **local_path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path to the local file to push.

- **remote_path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path or directory to store the file on the device.

Returns:  
Remote path of the file.

Example:

     >>> write('./filename', 'contents')
     >>> adb.push('./filename', '/data/local/tmp')
     '/data/local/tmp/filename'
     >>> adb.read('/data/local/tmp/filename')
     b'contents'
     >>> adb.push('./filename', '/does/not/exist')
     Traceback (most recent call last):
     ...
     PwnlibException: Could not stat '/does/not/exist'

&nbsp;

pwnlib.adb.adb.read(*path*, *target=None*, *callback=None*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L608-L641)  
Download a file from the device, and extract its contents.

Parameters:  
- **path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path to the file on the device.

- **target** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Optional, location to store the file. Uses a temporary file by default.

- **callback** (*callable*) – See the documentation for `adb.protocol.AdbClient.read`.

Examples:

     >>> print(adb.read('/proc/version').decode('utf-8'))
     Linux version ...
     >>> adb.read('/does/not/exist')
     Traceback (most recent call last):
     ...
     PwnlibException: Could not stat '/does/not/exist'

&nbsp;

pwnlib.adb.adb.reboot(*wait=True*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L183-L194)  
Reboots the device.

&nbsp;

pwnlib.adb.adb.reboot_bootloader()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L196-L204)  
Reboots the device to the bootloader.

&nbsp;

pwnlib.adb.adb.remount()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L473-L484)  
Remounts the filesystem as writable.

&nbsp;

pwnlib.adb.adb.root()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L149-L172)  
Restarts adbd as root.

     >>> adb.root()

&nbsp;

pwnlib.adb.adb.setprop(*name*, *value*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1042-L1045)  
Writes a property to the system property store.

&nbsp;

pwnlib.adb.adb.shell(*\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L857-L860)  
Returns an interactive shell.

&nbsp;

pwnlib.adb.adb.uninstall(*package*, *\*arguments*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1600-L1611)  
Uninstall an APK from the device.

This is a wrapper around ‘pm uninstall’, which backs ‘adb uninstall’.

Parameters:  
- **package** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Name of the package to uninstall (e.g. `'com.foo.MyPackage'`)

- **arguments** – Supplementary arguments to `'pm`` ``install'`, e.g. `'-k'`.

&nbsp;

pwnlib.adb.adb.unlink(*path*, *recursive=False*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L773-L821)  
Unlinks a file or directory on the target device.

Examples:

     >>> adb.unlink("/does/not/exist")
     Traceback (most recent call last):
     ...
     PwnlibException: Could not unlink '/does/not/exist': Does not exist

     >>> filename = '/data/local/tmp/unlink-test'
     >>> adb.write(filename, 'hello')
     >>> adb.exists(filename)
     True
     >>> adb.unlink(filename)
     >>> adb.exists(filename)
     False

     >>> adb.mkdir(filename)
     >>> adb.write(filename + '/contents', 'hello')
     >>> adb.unlink(filename)
     Traceback (most recent call last):
     ...
     PwnlibException: Cannot delete non-empty directory '/data/local/tmp/unlink-test' without recursive=True

     >>> adb.unlink(filename, recursive=True)
     >>> adb.exists(filename)
     False

&nbsp;

pwnlib.adb.adb.unlock_bootloader()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1086-L1120)  
Unlocks the bootloader of the device.

Note

This requires physical interaction with the device.

&nbsp;

pwnlib.adb.adb.unroot()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L486-L498)  
Restarts adbd as AID_SHELL.

&nbsp;

pwnlib.adb.adb.uptime() → [float](https://docs.python.org/3/library/functions.html#float "(in Python v3.14)")[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L206-L222)  
Returns:  
Uptime of the device, in seconds

Example:

     >>> adb.uptime() > 3 # normally AVD takes ~7 seconds to boot
     True

&nbsp;

pwnlib.adb.adb.version()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L1622-L1626)  
Returns rthe platform version as a tuple.

&nbsp;

pwnlib.adb.adb.wait_for_device(*kick=False*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L403-L453)  
Waits for a device to be connected.

By default, waits for the currently-selected device (via `context.device`). To wait for a specific device, set `context.device`. To wait for *any* device, clear `context.device`.

Returns:  
An `AdbDevice` instance for the device.

Examples:

     >>> device = adb.wait_for_device()

&nbsp;

pwnlib.adb.adb.which(*name*, *all=False*, *\*a*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L862-L921)  
Retrieves the full path to a binary in `$PATH` on the device

Parameters:  
- **name** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Binary name

- **all** ([*bool*](https://docs.python.org/3/library/functions.html#bool "(in Python v3.14)")) – Whether to return all paths, or just the first

- **\*a** – Additional arguments for [`adb.process()`](#pwnlib.adb.adb.process "pwnlib.adb.adb.process")

- **\*\*kw** – Additional arguments for [`adb.process()`](#pwnlib.adb.adb.process "pwnlib.adb.adb.process")

Returns:  
Either a path, or list of paths

Example:

     >>> adb.which('sh')
     '/system/bin/sh'
     >>> adb.which('sh', all=True)
     ['/system/bin/sh', '/vendor/bin/sh']

     >>> adb.which('foobar') is None
     True
     >>> adb.which('foobar', all=True)
     []

&nbsp;

pwnlib.adb.adb.whoami()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L924-L936)  
Returns current shell user

Example:

    >>> adb.whoami()
    b'root'

&nbsp;

pwnlib.adb.adb.write(*path*, *data=b''*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/adb/adb.py#L643-L662)  
Create a file on the device with the provided contents.

Parameters:  
- **path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path to the file on the device

- **data** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Contents to store in the file

Examples:

     >>> adb.write('/dev/null', b'data')
     >>> adb.write('/data/local/tmp/')

This file exists only for backward compatibility