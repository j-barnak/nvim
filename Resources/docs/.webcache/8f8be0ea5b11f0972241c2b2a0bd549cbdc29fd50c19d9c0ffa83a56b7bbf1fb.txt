[std](https://doc.rust-lang.org/std/index.html)::[ptr](https://doc.rust-lang.org/std/ptr/index.html)

# Function metadata Copy item path

[Source](https://doc.rust-lang.org/src/core/ptr/metadata.rs.html#100)

``` rust
pub const fn metadata<T>(ptr: *const T) -> <T as Pointee>::Metadatawhere
    T: ?Sized,
```

🔬This is a nightly-only experimental API. (`ptr_metadata` [\#81513](https://github.com/rust-lang/rust/issues/81513))

Expand description

Extracts the metadata component of a pointer.

Values of type `*mut T`, `&T`, or `&mut T` can be passed directly to this function as they implicitly coerce to `*const T`.

## Example

``` rust
#![feature(ptr_metadata)]

assert_eq!(std::ptr::metadata("foo"), 3_usize);
```