[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[windows](https://doc.rust-lang.org/std/os/windows/index.html)::[ffi](https://doc.rust-lang.org/std/os/windows/ffi/index.html)

# Trait OsStrExt Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/ffi.rs.html#97-121)

``` rust
pub trait OsStrExt {
    // Required method
    fn encode_wide(&self) -> EncodeWide<'_> ⓘ;
}
```

ⓘ *This trait cannot be implemented outside `std`.*

Available on **Windows** only.

Expand description

Windows-specific extensions to [`OsStr`](https://doc.rust-lang.org/std/ffi/struct.OsStr.html "struct std::ffi::OsStr").

## Required Methods

1.0.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/ffi.rs.html#120)

Re-encodes an `OsStr` as a wide character sequence, i.e., potentially ill-formed UTF-16.

This is lossless: calling [`OsStringExt::from_wide`](https://doc.rust-lang.org/std/os/windows/ffi/trait.OsStringExt.html#tymethod.from_wide "associated function std::os::windows::ffi::OsStringExt::from_wide") and then `encode_wide` on the result will yield the original code units. Note that the encoding does not add a final null terminator.

##### Examples

``` rust
use std::ffi::OsString;
use std::os::windows::prelude::*;

// UTF-16 encoding for "Unicode".
let source = [0x0055, 0x006E, 0x0069, 0x0063, 0x006F, 0x0064, 0x0065];

let string = OsString::from_wide(&source[..]);

let result: Vec<u16> = string.encode_wide().collect();
assert_eq!(&source[..], &result[..]);
```

## Dyn Compatibility

This trait **is** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

1.0.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/ffi.rs.html#124-129)