[std](https://doc.rust-lang.org/std/index.html)::[fs](https://doc.rust-lang.org/std/fs/index.html)

# Struct Permissions Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/fs.rs.html#293)

``` rust
pub struct Permissions(/* private fields */);
```

Expand description

Representation of the various permissions on a file.

This module only currently provides one bit of information, [`Permissions::readonly`](https://doc.rust-lang.org/std/fs/struct.Permissions.html#method.readonly "method std::fs::Permissions::readonly"), which is exposed on all currently supported platforms. Unix-specific functionality, such as mode bits, is available through the [`PermissionsExt`](https://doc.rust-lang.org/std/os/unix/fs/trait.PermissionsExt.html "trait std::os::unix::fs::PermissionsExt") trait.

## Implementations

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#2217-2337)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/fs.rs.html#2267-2269)

Returns `true` if these permissions describe a readonly (unwritable) file.

##### Note

This function does not take Access Control Lists (ACLs), Unix group membership and other nuances into account. Therefore the return value of this function cannot be relied upon to predict whether attempts to read or write the file will actually succeed.

##### Windows

On Windows this returns [`FILE_ATTRIBUTE_READONLY`](https://docs.microsoft.com/en-us/windows/win32/fileio/file-attribute-constants). If `FILE_ATTRIBUTE_READONLY` is set then writes to the file will fail but the user may still have permission to change this flag. If `FILE_ATTRIBUTE_READONLY` is *not* set then writes may still fail due to lack of write permission. The behavior of this attribute for directories depends on the Windows version.

##### Unix (including macOS)

On Unix-based platforms this checks if *any* of the owner, group or others write permission bits are set. It does not consider anything else, including:

- Whether the current user is in the file’s assigned group.
- Permissions granted by ACL.
- That `root` user can write to files that do not have any write bits set.
- Writable files on a filesystem that is mounted read-only.

The [`PermissionsExt`](https://doc.rust-lang.org/std/os/unix/fs/trait.PermissionsExt.html "trait std::os::unix::fs::PermissionsExt") trait gives direct access to the permission bits but also does not read ACLs.

##### Examples

``` rust
use std::fs::File;

fn main() -> std::io::Result<()> {
    let mut f = File::create("foo.txt")?;
    let metadata = f.metadata()?;

    assert_eq!(false, metadata.permissions().readonly());
    Ok(())
}
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/fs.rs.html#2334-2336)

Modifies the readonly flag for this set of permissions. If the `readonly` argument is `true`, using the resulting `Permission` will update file permissions to forbid writing. Conversely, if it’s `false`, using the resulting `Permission` will update file permissions to allow writing.

This operation does **not** modify the files attributes. This only changes the in-memory value of these attributes for this `Permissions` instance. To modify the files attributes use the [`set_permissions`](https://doc.rust-lang.org/std/fs/fn.set_permissions.html "fn std::fs::set_permissions") function which commits these attribute changes to the file.

##### Note

`set_readonly(false)` makes the file *world-writable* on Unix. You can use the [`PermissionsExt`](https://doc.rust-lang.org/std/os/unix/fs/trait.PermissionsExt.html "trait std::os::unix::fs::PermissionsExt") trait on Unix to avoid this issue.

It also does not take Access Control Lists (ACLs) or Unix group membership into account.

##### Windows

On Windows this sets or clears [`FILE_ATTRIBUTE_READONLY`](https://docs.microsoft.com/en-us/windows/win32/fileio/file-attribute-constants). If `FILE_ATTRIBUTE_READONLY` is set then writes to the file will fail but the user may still have permission to change this flag. If `FILE_ATTRIBUTE_READONLY` is *not* set then the write may still fail if the user does not have permission to write to the file.

In Windows 7 and earlier this attribute prevents deleting empty directories. It does not prevent modifying the directory contents. On later versions of Windows this attribute is ignored for directories.

##### Unix (including macOS)

On Unix-based platforms this sets or clears the write access bit for the owner, group *and* others, equivalent to `chmod a+w <file>` or `chmod a-w <file>` respectively. The latter will grant write access to all users! You can use the [`PermissionsExt`](https://doc.rust-lang.org/std/os/unix/fs/trait.PermissionsExt.html "trait std::os::unix::fs::PermissionsExt") trait on Unix to avoid this issue.

##### Examples

``` rust
use std::fs::File;

fn main() -> std::io::Result<()> {
    let f = File::create("foo.txt")?;
    let metadata = f.metadata()?;
    let mut permissions = metadata.permissions();

    permissions.set_readonly(true);

    // filesystem doesn't change, only the in memory state of the
    // readonly permission
    assert_eq!(false, metadata.permissions().readonly());

    // just this particular `permissions`.
    assert_eq!(true, permissions.readonly());
    Ok(())
}
```

## Trait Implementations

1.0.0 · [Source](https://doc.rust-lang.org/src/std/fs.rs.html#290)

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#290)

Returns a duplicate of the value. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#tymethod.clone)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/142757 "Tracking issue for const_clone")) · [Source](https://doc.rust-lang.org/src/core/clone.rs.html#245-247)

Performs copy-assignment from `source`. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#method.clone_from)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/fs.rs.html#290)

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#290)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/fs.rs.html#290)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/fs.rs.html#290)

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#290)

Tests for `self` and `other` values to be equal, and is used by `==`.

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#263)

Tests for `!=`. The default implementation is almost always sufficient, and should not be overridden without very good reason.

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#460-472)

Available on **Unix** only.

[Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#461-463)

Returns the mode permission bits

[Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#465-467)

Sets the mode permission bits.

[Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#469-471)

Creates a new instance from the given mode permission bits.

[Source](https://doc.rust-lang.org/src/std/os/windows/fs.rs.html#412-424)

Available on **Windows** only.

[Source](https://doc.rust-lang.org/src/std/os/windows/fs.rs.html#413-415)

🔬This is a nightly-only experimental API. (`windows_permissions_ext` [\#152956](https://github.com/rust-lang/rust/issues/152956))

Returns the file attribute bits.

[Source](https://doc.rust-lang.org/src/std/os/windows/fs.rs.html#417-419)

🔬This is a nightly-only experimental API. (`windows_permissions_ext` [\#152956](https://github.com/rust-lang/rust/issues/152956))

Sets the file attribute bits.

[Source](https://doc.rust-lang.org/src/std/os/windows/fs.rs.html#421-423)

🔬This is a nightly-only experimental API. (`windows_permissions_ext` [\#152956](https://github.com/rust-lang/rust/issues/152956))

Creates a new instance from the given file attribute bits.

1.0.0 · [Source](https://doc.rust-lang.org/src/std/fs.rs.html#290)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.