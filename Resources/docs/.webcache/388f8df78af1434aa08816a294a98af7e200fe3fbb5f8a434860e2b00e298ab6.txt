# angr.analyses.variable_recovery.variable_recovery

class angr.analyses.variable_recovery.variable_recovery.VariableRecoveryState  
Bases: [`VariableRecoveryStateBase`](https://docs.angr.io/en/latest/api/angr.analyses.variable_recovery.variable_recovery_base.html#angr.analyses.variable_recovery.variable_recovery_base.VariableRecoveryStateBase "angr.analyses.variable_recovery.variable_recovery_base.VariableRecoveryStateBase")

The abstract state of variable recovery analysis.

Variables:  
**variable_manager** (*angr.knowledge.variable_manager.VariableManager*) – The variable manager.

\_\_init\_\_(*project*, *block_addr*, *analysis*, *arch*, *func*, *concrete_states*, *\**, *tv_manager*, *stack_region=None*, *register_region=None*)  
Parameters:  
- **project** ([*Project*](https://docs.angr.io/en/latest/api/angr.project.html#angr.project.Project "angr.project.Project"))

- **block_addr** ([*int*](https://docs.python.org/3/library/functions.html#int "(in Python v3.14)"))

- **arch** ([*Arch*](https://docs.angr.io/projects/archinfo/en/latest/api.html#archinfo.arch.Arch "(in archinfo)"))

- **func** ([*Function*](https://docs.angr.io/en/latest/api/angr.knowledge_plugins.functions.function.html#angr.knowledge_plugins.functions.function.Function "angr.knowledge_plugins.functions.function.Function"))

- **tv_manager** ([*TypeVariableManager*](https://docs.angr.io/en/latest/api/angr.analyses.typehoon.typevars.html#angr.analyses.typehoon.typevars.TypeVariableManager "angr.analyses.typehoon.typevars.TypeVariableManager"))

property concrete_states  

get_concrete_state(*addr*)  
Parameters:  
**addr**

Returns:  

copy()  

register_callbacks(*concrete_states*)  
Parameters:  
**concrete_states**

Returns:  

merge(*others*, *successor=None*)  
Merge two abstract states.

Parameters:  
**others** ([`tuple`](https://docs.python.org/3/library/stdtypes.html#tuple "(in Python v3.14)")\[[`VariableRecoveryState`](#angr.analyses.variable_recovery.variable_recovery.VariableRecoveryState "angr.analyses.variable_recovery.variable_recovery.VariableRecoveryState"), [`...`](https://docs.python.org/3/library/constants.html#Ellipsis "(in Python v3.14)")\]) – Other abstract states to merge.

Returns:  
The merged abstract state.

Return type:  
[`tuple`](https://docs.python.org/3/library/stdtypes.html#tuple "(in Python v3.14)")\[[`VariableRecoveryState`](#angr.analyses.variable_recovery.variable_recovery.VariableRecoveryState "angr.analyses.variable_recovery.variable_recovery.VariableRecoveryState"), [`bool`](https://docs.python.org/3/library/functions.html#bool "(in Python v3.14)")\]

&nbsp;

class angr.analyses.variable_recovery.variable_recovery.VariableRecovery  
Bases: [`ForwardAnalysis`](https://docs.angr.io/en/latest/api/angr.analyses.forward_analysis.forward_analysis.html#angr.analyses.forward_analysis.forward_analysis.ForwardAnalysis "angr.analyses.forward_analysis.forward_analysis.ForwardAnalysis"), [`VariableRecoveryBase`](https://docs.angr.io/en/latest/api/angr.analyses.variable_recovery.variable_recovery_base.html#angr.analyses.variable_recovery.variable_recovery_base.VariableRecoveryBase "angr.analyses.variable_recovery.variable_recovery_base.VariableRecoveryBase")

Recover “variables” from a function using forced execution.

While variables play a very important role in programming, it does not really exist after compiling. However, we can still identify and recovery their counterparts in binaries. It is worth noting that not every variable in source code can be identified in binaries, and not every recognized variable in binaries have a corresponding variable in the original source code. In short, there is no guarantee that the variables we identified/recognized in a binary are the same variables in its source code.

This analysis uses heuristics to identify and recovers the following types of variables: - Register variables. - Stack variables. - Heap variables. (not implemented yet) - Global variables. (not implemented yet)

This analysis takes a function as input, and performs a data-flow analysis on nodes. It runs concrete execution on every statement and hooks all register/memory accesses to discover all places that are accessing variables. It is slow, but has a more accurate analysis result. For a fast but inaccurate variable recovery, you may consider using VariableRecoveryFast.

This analysis follows SSA, which means every write creates a new variable in registers or memory (statck, heap, etc.). Things may get tricky when overlapping variable (in memory, as you cannot really have overlapping accesses to registers) accesses exist, and in such cases, a new variable will be created, and this new variable will overlap with one or more existing variables. A decision procedure (which is pretty much TODO) is required at the end of this analysis to resolve the conflicts between overlapping variables.

\_\_init\_\_(*func*, *max_iterations=20*, *store_live_variables=False*)  
Parameters:  
**func** (*knowledge.Function*) – The function to analyze.