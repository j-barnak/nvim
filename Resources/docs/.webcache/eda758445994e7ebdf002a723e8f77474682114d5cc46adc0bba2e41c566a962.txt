[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[linux](https://doc.rust-lang.org/std/os/linux/index.html)::[net](https://doc.rust-lang.org/std/os/linux/net/index.html)

# Trait SocketAddrExt Copy item path

1.70.0 · [Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/addr.rs.html#7-69)

``` rust
pub trait SocketAddrExt {
    // Required methods
    fn from_abstract_name<N>(name: N) -> Result<SocketAddr>
       where N: AsRef<[u8]>;
    fn as_abstract_name(&self) -> Option<&[u8]>;
}
```

ⓘ *This trait cannot be implemented outside `std`.*

Available on **Android or Cygwin or Linux** only.

Expand description

Platform-specific extensions to [`SocketAddr`](https://doc.rust-lang.org/std/os/unix/net/struct.SocketAddr.html "struct std::os::unix::net::SocketAddr").

## Required Methods

1.70.0 · [Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/addr.rs.html#43-45)

Creates a Unix socket address in the abstract namespace.

The abstract namespace is a Linux-specific extension that allows Unix sockets to be bound without creating an entry in the filesystem. Abstract sockets are unaffected by filesystem layout or permissions, and no cleanup is necessary when the socket is closed.

An abstract socket address name may contain any bytes, including zero.

##### Errors

Returns an error if the name is longer than `SUN_LEN - 1`.

##### Examples

``` rust
use std::os::unix::net::{UnixListener, SocketAddr};
#[cfg(target_os = "linux")]
use std::os::linux::net::SocketAddrExt;
#[cfg(target_os = "android")]
use std::os::android::net::SocketAddrExt;

fn main() -> std::io::Result<()> {
    let addr = SocketAddr::from_abstract_name(b"hidden")?;
    let listener = match UnixListener::bind_addr(&addr) {
        Ok(sock) => sock,
        Err(err) => {
            println!("Couldn't bind: {err:?}");
            return Err(err);
        }
    };
    Ok(())
}
```

1.70.0 · [Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/addr.rs.html#68)

Returns the contents of this address if it is in the abstract namespace.

##### Examples

``` rust
use std::os::unix::net::{UnixListener, SocketAddr};
#[cfg(target_os = "linux")]
use std::os::linux::net::SocketAddrExt;
#[cfg(target_os = "android")]
use std::os::android::net::SocketAddrExt;

fn main() -> std::io::Result<()> {
    let name = b"hidden";
    let name_addr = SocketAddr::from_abstract_name(name)?;
    let socket = UnixListener::bind_addr(&name_addr)?;
    let local_addr = socket.local_addr().expect("Couldn't get local address");
    assert_eq!(local_addr.as_abstract_name(), Some(&name[..]));
    Ok(())
}
```

## Dyn Compatibility

This trait is **not** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

1.70.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/addr.rs.html#258-288)

Available on **Unix** only.