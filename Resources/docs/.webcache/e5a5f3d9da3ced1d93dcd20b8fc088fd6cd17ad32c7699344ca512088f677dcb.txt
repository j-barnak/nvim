# [`pwnlib.protocols.adb`](#module-pwnlib.protocols.adb "pwnlib.protocols.adb") — Protocol implementations

Implementation of the Android Debug Bridge (ADB) protocol.

Documentation is available [here](https://android.googlesource.com/platform/system/core/+/master/adb/protocol.txt).

*class *pwnlib.protocols.adb.AdbClient(*level=None*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L80-L631)  
ADB Client

\_\_init\_\_(*level=None*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L82-L90)  

\_autoclose()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L120-L130)  
Decorator which automatically closes the connection to the ADB server after calling the decorated function.

\_sync()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L373-L381)  
Decorator which enters ‘sync:’ mode to the selected transport, then invokes the decorated funciton.

\_with_transport()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L132-L143)  
Decorator which automatically selects a device transport before calling the decorated function, and closes the connection afterward.

devices(*long=False*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L204-L221)  
Parameters:  
**long** ([*bool*](https://docs.python.org/3/library/functions.html#bool "(in Python v3.14)")) – If `True`, fetch the long-format listing.

Returns:  
String representation of all available devices.

execute(*argv*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L270-L293)  
Executes a program on the device.

Returns:  
A [`pwnlib.tubes.tube.tube`](https://docs.pwntools.com/en/stable/tubes.html#pwnlib.tubes.tube.tube "pwnlib.tubes.tube.tube") which is connected to the process.

Examples:

     >>> pwnlib.protocols.adb.AdbClient().execute(['echo','hello']).recvall()
     b'hello\n'

kill()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L160-L183)  
Kills the remote ADB server”

     >>> c=pwnlib.protocols.adb.AdbClient()
     >>> c.kill()

The server is automatically re-started on the next request, if the default host/port are used.

     >>> c.version() > (4,0)
     True
     >>> c.wait_for_device() # ensure doctests alive

list(*path*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L383-L430)  
Execute the `LIST` command of the `SYNC` API.

Parameters:  
**path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path of the directory to list.

Returns:  
A dictionary, where the keys are relative filenames, and the values are a dictionary containing the same values as `stat()` supplies.

Note

In recent releases of Android (e.g. 7.0), the domain that adbd executes from does not have access to everything that the shell user does.

Because of this, while the shell user can get listings of e.g. the root directory (‘/’), adbd cannot.

The SYNC APIs are executed within the adbd context, not the shell user context.

This issue is not a problem if the phone is rooted via ‘adb root’, since adbd then runs in the `su` domain.

Examples:

     >>> _ = AdbClient().root()
     >>> AdbClient().wait_for_device()
     >>> pprint(AdbClient().list('/data/user'))
     {'0': {'mode': 41471, 'size': 10, 'time': ...}}
     >>> AdbClient().list('/does/not/exist')
     Traceback (most recent call last):
     ...
     PwnlibException: Cannot list directory '/does/not/exist': Does not exist

read(*path*, *filesize=0*, *callback=\<function AdbClient.\<lambda\>\>*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L559-L620)  
Execute the `READ` command of the `SYNC` API.

Parameters:  
- **path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path to the file to read

- **filesize** ([*int*](https://docs.python.org/3/library/functions.html#int "(in Python v3.14)")) – Size of the file, in bytes. Optional.

- **callback** (*callable*) –

  Callback function invoked as data becomes available. Arguments provided are:

  - File path

  - All data

  - Expected size of all data

  - Current chunk

  - Expected size of chunk

Returns:  
The data received as a string.

recvl()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L155-L158)  
Receives a length-prefixed data buffer from the ADB server

send(*\*a*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L145-L149)  
Sends data to the ADB server

stat(*path*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L466-L502)  
Execute the STAT command of the SYNC API.

Parameters:  
**path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path to the file to stat.

Returns:  
On success, a dictionary mapping the values returned. If the file cannot be stat() ed, None is returned.

Example:

     >>> expected = {'mode': 16749, 'size': 0, 'time': 0}
     >>> pwnlib.protocols.adb.AdbClient().stat('/proc')           == expected
     True
     >>> pwnlib.protocols.adb.AdbClient().stat('/does/not/exist') is None
     True

track_devices()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L223-L235)  
Returns:  
Generator which returns a short-format listing of available devices each time a device state changes.

transport(*serial=None*, *try_again=True*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L237-L268)  
Sets the Transport on the remote device.

Examples:

     >>> pwnlib.protocols.adb.AdbClient().transport()

unpack(*\*a*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L151-L153)  
Receives a hex-ascii packed integer from the ADB server

version()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L185-L202)  
Returns:  
Tuple containing the `(major,`` ``minor)` version from the ADB server

Example:

     >>> pwnlib.protocols.adb.AdbClient().version()
     (4, 36)

write(*path*, *data*, *mode=493*, *timestamp=None*, *callback=None*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L504-L528)  
Execute the `WRITE` command of the `SYNC` API.

Parameters:  
- **path** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Path to the file to write

- **data** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")) – Data to write to the file

- **mode** ([*int*](https://docs.python.org/3/library/functions.html#int "(in Python v3.14)")) – File mode to set (e.g. `0o755`)

- **timestamp** ([*int*](https://docs.python.org/3/library/functions.html#int "(in Python v3.14)")) – Unix timestamp to set the file date to

- **callback** (*callable*) –

  Callback function invoked as data is written. Arguments provided are:

  - File path

  - All data

  - Expected size of all data

  - Current chunk

  - Expected size of chunk

*property *c[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L92-L118)  
AdbClient’s connection to the ADB server

&nbsp;

*class *pwnlib.protocols.adb.Connection(*host*, *port*, *level=None*, *\*a*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L51-L75)  
Connection to the ADB server

\_\_init\_\_(*host*, *port*, *level=None*, *\*a*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L53-L60)  

close()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L62-L64)  
Closes the tube.

&nbsp;

*class *pwnlib.protocols.adb.Message(*string*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L40-L49)  
An ADB hex-length-prefixed message

\_\_init\_\_(*string*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L42-L43)  

\_\_str\_\_()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L46-L47)  
Return str(self).

\_\_weakref\_\_[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py)  
list of weak references to the object

&nbsp;

*class *pwnlib.protocols.adb.Process(*host*, *port*, *level=None*, *\*a*, *\*\*kw*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L77-L78)  
Duck-typed `tubes.remote` object to add properties of a `tubes.process`

&nbsp;

pwnlib.protocols.adb.proxy(*port=9999*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/protocols/adb.py#L633-L638)  
Starts an ADB proxy on the specified port, for debugging purposes.