# miasm.ir.ir

## Module variables

- `ESCAPE_CHARS = re.compile('[' + re.escape('{}[]') + '&|<>' + ']')`

## Classes

### class `TranslatorHtml(Translator)`

- **`_fix_chars(token)`**
- **`__init__(self, loc_db=None, **kwargs)`**
- **`str_protected_child(self, child, parent)`**
- **`from_ExprInt(self, expr)`**
- **`from_ExprId(self, expr)`**
- **`from_ExprLoc(self, expr)`**
- **`from_ExprMem(self, expr)`**
- **`from_ExprSlice(self, expr)`**
- **`from_ExprCompose(self, expr)`**
- **`from_ExprCond(self, expr)`**
- **`from_ExprOp(self, expr)`**
- **`from_ExprAssign(self, expr)`**

### class `AssignBlock(object)`

Represent parallel IR assignment, such as:
EAX = EBX
EBX = EAX

-> Exchange between EBX and EAX

AssignBlock can be seen as a dictionary where keys are the destinations
(ExprId or ExprMem), and values their corresponding sources.

Also provides common manipulation on this assignments.

- **`__init__(self, irs=None, instr=None)`** - Create a new AssignBlock
- **`instr(self)`** - Return the associated instruction, if any
- **`_set(self, dst, src)`** - Special cases:
- **`__setitem__(self, dst, src)`**
- **`__getitem__(self, key)`**
- **`__contains__(self, key)`**
- **`iteritems(self)`**
- **`items(self)`**
- **`itervalues(self)`**
- **`keys(self)`**
- **`values(self)`**
- **`__iter__(self)`**
- **`__delitem__(self, _)`**
- **`update(self, _)`**
- **`__eq__(self, other)`**
- **`__ne__(self, other)`**
- **`__len__(self)`**
- **`get(self, key, default)`**
- **`get_modified_slice(dst, src)`** - Return an Expr list of extra expressions needed during the
- **`get_w(self)`** - Return a set of elements written
- **`get_rw(self, mem_read=False, cst_read=False)`** - Return a dictionary associating written expressions to a set of
- **`get_r(self, mem_read=False, cst_read=False)`** - Return a set of elements reads
- **`__str__(self)`**
- **`dst2ExprAssign(self, dst)`** - Return an ExprAssign corresponding to @dst equation
- **`simplify(self, simplifier)`** - Return a new AssignBlock with expression simplified
- **`to_string(self, loc_db=None)`**

### class `IRBlock(object)`

Intermediate representation block object.

Stand for an intermediate representation  basic block.

- **`__init__(self, loc_db, loc_key, assignblks)`** - @loc_key: LocKey of the IR basic block
- **`__eq__(self, other)`**
- **`__ne__(self, other)`**
- **`get_label(self)`**
- **`assignblks(self)`**
- **`irs(self)`**
- **`__iter__(self)`** - Iterate on assignblks
- **`__getitem__(self, index)`** - Getitem on assignblks
- **`__len__(self)`** - Length of assignblks
- **`is_dst_set(self)`**
- **`cache_dst(self)`**
- **`dst(self)`** - Return the value of IRDst for the IRBlock
- **`set_dst(self, value)`** - Generate a new IRBlock with a dst (IRBlock) fixed to @value
- **`dst_linenb(self)`** - Line number of the IRDst setting statement in the current irs
- **`to_string(self)`**
- **`__str__(self)`**
- **`modify_exprs(self, mod_dst=None, mod_src=None)`** - Generate a new IRBlock with its AssignBlock expressions modified
- **`simplify(self, simplifier)`** - Simplify expressions in each assignblock

### class `irbloc(IRBlock)`

DEPRECATED object
Use IRBlock instead of irbloc

- **`__init__(self, loc_key, irs, lines=None)`**

### class `IRCFG(DiGraph)`

DiGraph for IR instances

- **`__init__(self, irdst, loc_db, blocks=None, *args, **kwargs)`** - Instantiate a IRCFG
- **`IRDst(self)`**
- **`blocks(self)`**
- **`add_irblock(self, irblock)`** - Add the @irblock to the current IRCFG
- **`escape_text(self, text)`**
- **`node2lines(self, node)`**
- **`edge_attr(self, src, dst)`**
- **`node_attr(self, node)`**
- **`dot(self, offset=False)`** - @offset: (optional) if set, add the corresponding line number in each
- **`get_loc_key(self, addr)`** - Transforms an ExprId/ExprInt/loc_key/int into a loc_key
- **`get_or_create_loc_key(self, addr)`** - Transforms an ExprId/ExprInt/loc_key/int into a loc_key
- **`get_block(self, addr)`** - Returns the irbloc associated to an ExprId/ExprInt/loc_key/int
- **`getby_offset(self, offset)`** - Return the set of loc_keys of irblocks containing @offset
- **`simplify(self, simplifier)`** - Simplify expressions in each irblocks
- **`_extract_dst(self, todo, done)`** - Naive extraction of @todo destinations
- **`dst_trackback(self, irb)`** - Naive backtracking of IRDst

### class `DiGraphIR(IRCFG)`

DEPRECATED object
Use IRCFG instead of DiGraphIR

- **`__init__(self, *args, **kwargs)`**

### class `Lifter(object)`

Intermediate representation object

Allow native assembly to intermediate representation traduction

- **`__init__(self, arch, attrib, loc_db)`**
- **`get_ir(self, instr)`**
- **`new_ircfg(self, *args, **kwargs)`** - Return a new instance of IRCFG
- **`new_ircfg_from_asmcfg(self, asmcfg, *args, **kwargs)`** - Return a new instance of IRCFG from an @asmcfg
- **`instr2ir(self, instr)`**
- **`add_instr_to_ircfg(self, instr, ircfg, loc_key=None, gen_pc_updt=False)`** - Add the native instruction @instr to the @ircfg
- **`gen_pc_update(self, assignments, instr)`**
- **`add_instr_to_current_state(self, instr, block, assignments, ir_blocks_all, gen_pc_updt)`** - Add the IR effects of an instruction to the current state.
- **`add_asmblock_to_ircfg(self, block, ircfg, gen_pc_updt=False)`** - Add a native block to the current IR
- **`add_block(self, block, gen_pc_updt=False)`** - DEPRECATED function
- **`add_bloc(self, block, gen_pc_updt=False)`** - DEPRECATED function
- **`get_next_loc_key(self, instr)`**
- **`get_loc_key_for_instr(self, instr)`** - Returns the loc_key associated to an instruction
- **`gen_loc_key_and_expr(self, size)`** - Return a loc_key and it's corresponding ExprLoc
- **`expr_fix_regs_for_mode(self, expr, *args, **kwargs)`**
- **`expraff_fix_regs_for_mode(self, expr, *args, **kwargs)`**
- **`irbloc_fix_regs_for_mode(self, irblock, *args, **kwargs)`**
- **`is_pc_written(self, block)`** - Return the first Assignblk of the @block in which PC is written
- **`set_empty_dst_to_next(self, block, ir_blocks)`**
- **`post_add_asmblock_to_ircfg(self, block, ircfg, ir_blocks)`**

### class `IntermediateRepresentation(Lifter)`

DEPRECATED object
Use Lifter instead of IntermediateRepresentation

- **`__init__(self, arch, attrib, loc_db)`**

### class `ir(Lifter)`

DEPRECATED object
Use Lifter instead of ir

- **`__init__(self, loc_key, irs, lines=None)`**

## Functions

### `_expr_loc_to_symb(expr, loc_db)`

### `color_expr_html(expr, loc_db)`

### `slice_rest(expr)`

Return the completion of the current slice
