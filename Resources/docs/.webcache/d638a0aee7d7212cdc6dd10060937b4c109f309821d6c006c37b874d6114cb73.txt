[std](https://doc.rust-lang.org/std/index.html)::[fs](https://doc.rust-lang.org/std/fs/index.html)

# Struct Dir Copy item path

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#187-189)

``` rust
pub struct Dir { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`dirfd` [\#120426](https://github.com/rust-lang/rust/issues/120426))

Expand description

An object providing access to a directory on the filesystem.

Directories are automatically closed when they go out of scope. Errors detected on closing are ignored by the implementation of `Drop`.

## Platform-specific behavior

On supported systems (including Windows and some UNIX-based OSes), this function acquires a handle/file descriptor for the directory. This allows functions like [`Dir::open_file`](https://doc.rust-lang.org/std/fs/struct.Dir.html#method.open_file "method std::fs::Dir::open_file") to avoid [TOCTOU](https://doc.rust-lang.org/std/fs/index.html#time-of-check-to-time-of-use-toctou "mod std::fs") errors when the directory itself is being moved.

On other systems, it stores an absolute path (see [`canonicalize()`](https://doc.rust-lang.org/std/fs/fn.canonicalize.html "fn std::fs::canonicalize")). In the latter case, no [TOCTOU](https://doc.rust-lang.org/std/fs/index.html#time-of-check-to-time-of-use-toctou "mod std::fs") guarantees are made.

## Examples

Opens a directory and then a file inside it.

``` rust
#![feature(dirfd)]
use std::{fs::Dir, io};

fn main() -> std::io::Result<()> {
    let dir = Dir::open("foo")?;
    let mut file = dir.open_file("bar.txt")?;
    let contents = io::read_to_string(file)?;
    assert_eq!(contents, "Hello, world!");
    Ok(())
}
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#1545-1619)

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#1568-1571)

🔬This is a nightly-only experimental API. (`dirfd` [\#120426](https://github.com/rust-lang/rust/issues/120426))

Attempts to open a directory at `path` in read-only mode.

##### Errors

This function will return an error if `path` does not point to an existing directory. Other errors may also be returned according to [`OpenOptions::open`](https://doc.rust-lang.org/std/fs/struct.OpenOptions.html#method.open "method std::fs::OpenOptions::open").

##### Examples

``` rust
#![feature(dirfd)]
use std::{fs::Dir, io};

fn main() -> std::io::Result<()> {
    let dir = Dir::open("foo")?;
    let mut f = dir.open_file("bar.txt")?;
    let contents = io::read_to_string(f)?;
    assert_eq!(contents, "Hello, world!");
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#1595-1599)

🔬This is a nightly-only experimental API. (`dirfd` [\#120426](https://github.com/rust-lang/rust/issues/120426))

Attempts to open a file in read-only mode relative to this directory.

##### Errors

This function will return an error if `path` does not point to an existing file. Other errors may also be returned according to [`OpenOptions::open`](https://doc.rust-lang.org/std/fs/struct.OpenOptions.html#method.open "method std::fs::OpenOptions::open").

##### Examples

``` rust
#![feature(dirfd)]
use std::{fs::Dir, io};

fn main() -> std::io::Result<()> {
    let dir = Dir::open("foo")?;
    let mut f = dir.open_file("bar.txt")?;
    let contents = io::read_to_string(f)?;
    assert_eq!(contents, "Hello, world!");
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#1616-1618)

🔬This is a nightly-only experimental API. (`dirfd` [\#120426](https://github.com/rust-lang/rust/issues/120426))

Queries metadata about the underlying directory.

##### Examples

``` rust
#![feature(dirfd)]
use std::fs::Dir;

fn main() -> std::io::Result<()> {
    let dir = Dir::open("foo")?;
    let metadata = dir.metadata()?;
    Ok(())
}
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#106-110)

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#107-109)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

Borrows the file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.AsFd.html#tymethod.as_fd)

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#85-89)

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#86-88)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

Extracts the raw file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.AsRawFd.html#tymethod.as_raw_fd)

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#1639-1643)

[Source](https://doc.rust-lang.org/src/std/fs.rs.html#1640-1642)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#113-117)

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#114-116)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#120-124)

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#121-123)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#99-103)

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#100-102)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

Constructs a new instance of `Self` from the given raw file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.FromRawFd.html#tymethod.from_raw_fd)

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#92-96)

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#93-95)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

Consumes this object, returning the raw underlying file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.IntoRawFd.html#tymethod.into_raw_fd)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.