[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)::[mpsc](https://doc.rust-lang.org/std/sync/mpsc/index.html)

# Struct SyncSender Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/sync/mpsc.rs.html#395-397)

``` rust
pub struct SyncSender<T> { /* private fields */ }
```

Expand description

The sending-half of Rust’s synchronous [`sync_channel`](https://doc.rust-lang.org/std/sync/mpsc/fn.sync_channel.html "fn std::sync::mpsc::sync_channel") type.

Messages can be sent through this channel with [`send`](https://doc.rust-lang.org/std/sync/mpsc/struct.SyncSender.html#method.send "method std::sync::mpsc::SyncSender::send") or [`try_send`](https://doc.rust-lang.org/std/sync/mpsc/struct.SyncSender.html#method.try_send "method std::sync::mpsc::SyncSender::try_send").

[`send`](https://doc.rust-lang.org/std/sync/mpsc/struct.SyncSender.html#method.send "method std::sync::mpsc::SyncSender::send") will block if there is no space in the internal buffer.

## Examples

``` rust
use std::sync::mpsc::sync_channel;
use std::thread;

// Create a sync_channel with buffer size 2
let (sync_sender, receiver) = sync_channel(2);
let sync_sender2 = sync_sender.clone();

// First thread owns sync_sender
thread::spawn(move || {
    sync_sender.send(1).unwrap();
    sync_sender.send(2).unwrap();
});

// Second thread owns sync_sender2
thread::spawn(move || {
    sync_sender2.send(3).unwrap();
    // thread will now block since the buffer is full
    println!("Thread unblocked!");
});

let mut msg;

msg = receiver.recv().unwrap();
println!("message {msg} received");

// "Thread unblocked!" will be printed now

msg = receiver.recv().unwrap();
println!("message {msg} received");

msg = receiver.recv().unwrap();

println!("message {msg} received");
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/sync/mpsc.rs.html#664-773)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/sync/mpsc.rs.html#702-704)

Sends a value on this synchronous channel.

This function will *block* until space in the internal buffer becomes available or a receiver is available to hand off the message to.

Note that a successful send does *not* guarantee that the receiver will ever see the data if there is a buffer on this channel. Items may be enqueued in the internal buffer for the receiver to receive at a later time. If the buffer size is 0, however, the channel becomes a rendezvous channel and it guarantees that the receiver has indeed received the data if this function returns success.

This function will never panic, but it may return [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") if the [`Receiver`](https://doc.rust-lang.org/std/sync/mpsc/struct.Receiver.html "struct std::sync::mpsc::Receiver") has disconnected and is no longer able to receive information.

##### Examples

``` rust
use std::sync::mpsc::sync_channel;
use std::thread;

// Create a rendezvous sync_channel with buffer size 0
let (sync_sender, receiver) = sync_channel(0);

thread::spawn(move || {
   println!("sending message...");
   sync_sender.send(1).unwrap();
   // Thread is now blocked until the message is received

   println!("...message received!");
});

let msg = receiver.recv().unwrap();
assert_eq!(1, msg);
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/sync/mpsc.rs.html#760-762)

Attempts to send a value on this channel without blocking.

This method differs from [`send`](https://doc.rust-lang.org/std/sync/mpsc/struct.SyncSender.html#method.send "method std::sync::mpsc::SyncSender::send") by returning immediately if the channel’s buffer is full or no receiver is waiting to acquire some data. Compared with [`send`](https://doc.rust-lang.org/std/sync/mpsc/struct.SyncSender.html#method.send "method std::sync::mpsc::SyncSender::send"), this function has two failure cases instead of one (one for disconnection, one for a full buffer).

See [`send`](https://doc.rust-lang.org/std/sync/mpsc/struct.SyncSender.html#method.send "method std::sync::mpsc::SyncSender::send") for notes about guarantees of whether the receiver has received the data or not if this function is successful.

##### Examples

``` rust
use std::sync::mpsc::sync_channel;
use std::thread;

// Create a sync_channel with buffer size 1
let (sync_sender, receiver) = sync_channel(1);
let sync_sender2 = sync_sender.clone();

// First thread owns sync_sender
let handle1 = thread::spawn(move || {
    sync_sender.send(1).unwrap();
    sync_sender.send(2).unwrap();
    // Thread blocked
});

// Second thread owns sync_sender2
let handle2 = thread::spawn(move || {
    // This will return an error and send
    // no message if the buffer is full
    let _ = sync_sender2.try_send(3);
});

let mut msg;
msg = receiver.recv().unwrap();
println!("message {msg} received");

msg = receiver.recv().unwrap();
println!("message {msg} received");

// Third message may have never been sent
match receiver.try_recv() {
    Ok(msg) => println!("message {msg} received"),
    Err(_) => println!("the third message was never sent"),
}

// Wait for threads to complete
handle1.join().unwrap();
handle2.join().unwrap();
```

## Trait Implementations

1.0.0 · [Source](https://doc.rust-lang.org/src/std/sync/mpsc.rs.html#776-780)

[Source](https://doc.rust-lang.org/src/std/sync/mpsc.rs.html#777-779)

Returns a duplicate of the value. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#tymethod.clone)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/142757 "Tracking issue for const_clone")) · [Source](https://doc.rust-lang.org/src/core/clone.rs.html#245-247)

Performs copy-assignment from `source`. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#method.clone_from)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/sync/mpsc.rs.html#786-790)

[Source](https://doc.rust-lang.org/src/std/sync/mpsc.rs.html#787-789)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/sync/mpsc.rs.html#400)

[Source](https://doc.rust-lang.org/src/std/sync/mpsc.rs.html#783)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#389)

🔬This is a nightly-only experimental API. (`share_trait` [\#156756](https://github.com/rust-lang/rust/issues/156756))

Creates another alias to the same underlying resource or shared state. [Read more](https://doc.rust-lang.org/std/clone/trait.Share.html#method.share)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.