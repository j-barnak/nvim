# Thread Safety

Only one thread at a time should access the same `Program` (including `Object`, `Type`, `StackTrace`, etc. from that program). It is safe to use different `Program`s from concurrent threads.
