[std](https://doc.rust-lang.org/std/index.html)::[process](https://doc.rust-lang.org/std/process/index.html)

# Struct Stdio Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1544)

``` rust
pub struct Stdio(/* private fields */);
```

Expand description

Describes what to do with a standard I/O stream for a child process when passed to the [`stdin`](https://doc.rust-lang.org/std/process/struct.Command.html#method.stdin "method std::process::Command::stdin"), [`stdout`](https://doc.rust-lang.org/std/process/struct.Command.html#method.stdout "method std::process::Command::stdout"), and [`stderr`](https://doc.rust-lang.org/std/process/struct.Command.html#method.stderr "method std::process::Command::stderr") methods of [`Command`](https://doc.rust-lang.org/std/process/struct.Command.html "struct std::process::Command").

## Implementations

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1546-1694)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1595-1597)

A new pipe should be arranged to connect the parent and child processes.

##### Examples

With stdout:

``` rust
use std::process::{Command, Stdio};

let output = Command::new("echo")
    .arg("Hello, world!")
    .stdout(Stdio::piped())
    .output()
    .expect("Failed to execute command");

assert_eq!(String::from_utf8_lossy(&output.stdout), "Hello, world!\n");
// Nothing echoed to console
```

With stdin:

``` rust
use std::io::Write;
use std::process::{Command, Stdio};

let mut child = Command::new("rev")
    .stdin(Stdio::piped())
    .stdout(Stdio::piped())
    .spawn()
    .expect("Failed to spawn child process");

let mut stdin = child.stdin.take().expect("Failed to open stdin");
std::thread::spawn(move || {
    stdin.write_all("Hello, world!".as_bytes()).expect("Failed to write to stdin");
});

let output = child.wait_with_output().expect("Failed to read stdout");
assert_eq!(String::from_utf8_lossy(&output.stdout), "!dlrow ,olleH");
```

Writing more than a pipe buffer’s worth of input to stdin without also reading stdout and stderr at the same time may cause a deadlock. This is an issue when running any program that doesn’t guarantee that it reads its entire stdin before writing more than a pipe buffer’s worth of output. The size of a pipe buffer varies on different targets.

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1635-1637)

The child inherits from the corresponding parent descriptor.

##### Examples

With stdout:

``` rust
use std::process::{Command, Stdio};

let output = Command::new("echo")
    .arg("Hello, world!")
    .stdout(Stdio::inherit())
    .output()
    .expect("Failed to execute command");

assert_eq!(String::from_utf8_lossy(&output.stdout), "");
// "Hello, world!" echoed to console
```

With stdin:

``` rust
use std::process::{Command, Stdio};
use std::io::{self, Write};

let output = Command::new("rev")
    .stdin(Stdio::inherit())
    .stdout(Stdio::piped())
    .output()?;

print!("You piped in the reverse of: ");
io::stdout().write_all(&output.stdout)?;
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1675-1677)

This stream will be ignored. This is the equivalent of attaching the stream to `/dev/null`.

##### Examples

With stdout:

``` rust
use std::process::{Command, Stdio};

let output = Command::new("echo")
    .arg("Hello, world!")
    .stdout(Stdio::null())
    .output()
    .expect("Failed to execute command");

assert_eq!(String::from_utf8_lossy(&output.stdout), "");
// Nothing echoed to console
```

With stdin:

``` rust
use std::process::{Command, Stdio};

let output = Command::new("rev")
    .stdin(Stdio::null())
    .stdout(Stdio::piped())
    .output()
    .expect("Failed to execute command");

assert_eq!(String::from_utf8_lossy(&output.stdout), "");
// Ignores any piped-in input
```

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1691-1693)

🔬This is a nightly-only experimental API. (`stdio_makes_pipe` [\#98288](https://github.com/rust-lang/rust/issues/98288))

Returns `true` if this requires [`Command`](https://doc.rust-lang.org/std/process/struct.Command.html "struct std::process::Command") to create a new pipe.

##### Example

``` rust
#![feature(stdio_makes_pipe)]
use std::process::Stdio;

let io = Stdio::piped();
assert_eq!(io.makes_pipe(), true);
```

## Trait Implementations

1.16.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1703-1707)

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1704-1706)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.20.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1768-1796)

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1793-1795)

Converts a [`ChildStderr`](https://doc.rust-lang.org/std/process/struct.ChildStderr.html "struct std::process::ChildStderr") into a [`Stdio`](https://doc.rust-lang.org/std/process/struct.Stdio.html "struct std::process::Stdio").

##### Examples

``` rust
use std::process::{Command, Stdio};

let reverse = Command::new("rev")
    .arg("non_existing_file.txt")
    .stderr(Stdio::piped())
    .spawn()
    .expect("failed reverse command");

let cat = Command::new("cat")
    .arg("-")
    .stdin(reverse.stderr.unwrap()) // Converted into a Stdio here
    .output()
    .expect("failed echo command");

assert_eq!(
    String::from_utf8_lossy(&cat.stdout),
    "rev: cannot open non_existing_file.txt: No such file or directory\n"
);
```

1.20.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1710-1736)

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1733-1735)

Converts a [`ChildStdin`](https://doc.rust-lang.org/std/process/struct.ChildStdin.html "struct std::process::ChildStdin") into a [`Stdio`](https://doc.rust-lang.org/std/process/struct.Stdio.html "struct std::process::Stdio").

##### Examples

`ChildStdin` will be converted to `Stdio` using `Stdio::from` under the hood.

``` rust
use std::process::{Command, Stdio};

let reverse = Command::new("rev")
    .stdin(Stdio::piped())
    .spawn()
    .expect("failed reverse command");

let _echo = Command::new("echo")
    .arg("Hello, world!")
    .stdout(reverse.stdin.unwrap()) // Converted into a Stdio here
    .output()
    .expect("failed echo command");

// "!dlrow ,olleH" echoed to console
```

1.20.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1739-1765)

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1762-1764)

Converts a [`ChildStdout`](https://doc.rust-lang.org/std/process/struct.ChildStdout.html "struct std::process::ChildStdout") into a [`Stdio`](https://doc.rust-lang.org/std/process/struct.Stdio.html "struct std::process::Stdio").

##### Examples

`ChildStdout` will be converted to `Stdio` using `Stdio::from` under the hood.

``` rust
use std::process::{Command, Stdio};

let hello = Command::new("echo")
    .arg("Hello, world!")
    .stdout(Stdio::piped())
    .spawn()
    .expect("failed echo command");

let reverse = Command::new("rev")
    .stdin(hello.stdout.unwrap())  // Converted into a Stdio here
    .output()
    .expect("failed reverse command");

assert_eq!(reverse.stdout, b"!dlrow ,olleH\n");
```

1.20.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1799-1823)

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1820-1822)

Converts a [`File`](https://doc.rust-lang.org/std/fs/struct.File.html "struct std::fs::File") into a [`Stdio`](https://doc.rust-lang.org/std/process/struct.Stdio.html "struct std::process::Stdio").

##### Examples

`File` will be converted to `Stdio` using `Stdio::from` under the hood.

``` rust
use std::fs::File;
use std::process::Command;

// With the `foo.txt` file containing "Hello, world!"
let file = File::open("foo.txt")?;

let reverse = Command::new("rev")
    .stdin(file)  // Implicit File conversion into a Stdio
    .output()?;

assert_eq!(reverse.stdout, b"!dlrow ,olleH");
```

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#593-602)

Available on **Unix** only.

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#597-601)

Takes ownership of a file descriptor and returns a [`Stdio`](https://doc.rust-lang.org/std/process/struct.Stdio.html "struct std::process::Stdio") that can attach a stream to it.

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#25-33)

Available on **Windows** only.

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#28-32)

Takes ownership of a handle and returns a [`Stdio`](https://doc.rust-lang.org/std/process/struct.Stdio.html "struct std::process::Stdio") that can attach a stream to it.

1.87.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1893-1897)

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1894-1896)

Converts to this type from the input type.

1.87.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1886-1890)

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1887-1889)

Converts to this type from the input type.

1.74.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1857-1883)

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1880-1882)

Redirect command stdout/stderr to our stderr

##### Examples

``` rust
#![feature(exit_status_error)]
use std::io;
use std::process::Command;

let output = Command::new("whoami")
    .stdout(io::stderr())
    .output()?;
output.status.exit_ok()?;
assert!(output.stdout.is_empty());
```

1.74.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1826-1854)

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1851-1853)

Redirect command stdout/stderr to our stdout

##### Examples

``` rust
#![feature(exit_status_error)]
use std::io;
use std::process::Command;

let output = Command::new("whoami")
    .stdout(io::stdout())
    .output()?;
output.status.exit_ok()?;
assert!(output.stdout.is_empty());
```

1.2.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#583-590)

Available on **Unix** only.

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#585-589)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

Constructs a new instance of `Self` from the given raw file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.FromRawFd.html#tymethod.from_raw_fd)

1.2.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#16-22)

Available on **Windows** only.

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#17-21)

Constructs a new I/O object from the specified raw handle. [Read more](https://doc.rust-lang.org/std/os/windows/io/trait.FromRawHandle.html#tymethod.from_raw_handle)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.