# events

Model and Manager for Event resources.

*class* EventsManager(*client: APIClient*)[\[source\]](https://podman-py.readthedocs.io/en/latest/_modules/podman/domain/events.html#EventsManager)  
Bases: `object`

Specialized Manager for Event resources.

Initialize EventManager object.

Parameters:  
**client** – Connection to Podman service.

list(*since: datetime \| int \| None = None*, *until: datetime \| int \| None = None*, *filters: dict\[str, Any\] \| None = None*, *decode: bool = False*) → Iterator\[str \| dict\[str, Any\]\][\[source\]](https://podman-py.readthedocs.io/en/latest/_modules/podman/domain/events.html#EventsManager.list)  
Report on networks.

Parameters:  
- **decode** – When True, decode stream into dict’s. Default: False

- **filters** – Criteria for including events.

- **since** – Get events newer than this time.

- **until** – Get events older than this time.

Yields:  
When decode is True, Iterator\[dict\[str, Any\]\]

When decode is False, Iterator\[str\]
