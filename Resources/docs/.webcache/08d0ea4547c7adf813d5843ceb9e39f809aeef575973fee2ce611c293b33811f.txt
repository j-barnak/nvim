[std](https://doc.rust-lang.org/std/index.html)::[simd](https://doc.rust-lang.org/std/simd/index.html)

# Trait Select Copy item path

[Source](https://doc.rust-lang.org/src/core/portable-simd/crates/core_simd/src/select.rs.html#51)

``` rust
pub trait Select<T> {
    // Required method
    fn select(self, true_values: T, false_values: T) -> T;
}
```

🔬This is a nightly-only experimental API. (`portable_simd` [\#86656](https://github.com/rust-lang/rust/issues/86656))

Expand description

Choose elements from two vectors using a mask.

For each element in the mask, choose the corresponding element from `true_values` if that element mask is true, and `false_values` if that element mask is false.

If the mask is `u64`, it’s treated as a bitmask with the least significant bit corresponding to the first element.

## Examples

### Selecting values from `Simd`

``` rust
let a = Simd::from_array([0, 1, 2, 3]);
let b = Simd::from_array([4, 5, 6, 7]);
let mask = Mask::<i32, 4>::from_array([true, false, false, true]);
let c = mask.select(a, b);
assert_eq!(c.to_array(), [0, 5, 6, 3]);
```

### Selecting values from `Mask`

``` rust
let a = Mask::<i32, 4>::from_array([true, true, false, false]);
let b = Mask::<i32, 4>::from_array([false, false, true, true]);
let mask = Mask::<i32, 4>::from_array([true, false, false, true]);
let c = mask.select(a, b);
assert_eq!(c.to_array(), [true, false, true, false]);
```

### Selecting with a bitmask

``` rust
let a = Mask::<i32, 4>::from_array([true, true, false, false]);
let b = Mask::<i32, 4>::from_array([false, false, true, true]);
let mask = 0b1001;
let c = mask.select(a, b);
assert_eq!(c.to_array(), [true, false, true, false]);
```

## Required Methods

[Source](https://doc.rust-lang.org/src/core/portable-simd/crates/core_simd/src/select.rs.html#53)

🔬This is a nightly-only experimental API. (`portable_simd` [\#86656](https://github.com/rust-lang/rust/issues/86656))

Choose elements

## Dyn Compatibility

This trait **is** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

[Source](https://doc.rust-lang.org/src/core/portable-simd/crates/core_simd/src/select.rs.html#128-131)

[Source](https://doc.rust-lang.org/src/core/portable-simd/crates/core_simd/src/select.rs.html#56-59)

[Source](https://doc.rust-lang.org/src/core/portable-simd/crates/core_simd/src/select.rs.html#143-145)

[Source](https://doc.rust-lang.org/src/core/portable-simd/crates/core_simd/src/select.rs.html#73-75)