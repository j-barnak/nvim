[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_lt Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#310)

``` rust
pub const unsafe fn simd_lt<T, U>(x: T, y: T) -> U
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Tests if `x` is less than `y`, elementwise.

`T` must be a vector of integers or floats.

`U` must be a vector of integers with the same number of elements and element size as `T`.

Returns `0` for false and `!0` for true.