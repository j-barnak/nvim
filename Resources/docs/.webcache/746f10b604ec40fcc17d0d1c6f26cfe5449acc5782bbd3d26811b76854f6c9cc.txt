[std](https://doc.rust-lang.org/std/index.html)::[panic](https://doc.rust-lang.org/std/panic/index.html)

# Type Alias PanicInfo Copy item path

1.10.0 · [Source](https://doc.rust-lang.org/src/std/panic.rs.html#21)

``` rust
pub type PanicInfo<'a> = PanicHookInfo<'a>;
```

👎Deprecated since 1.82.0:

use `PanicHookInfo` instead

Expand description

A struct providing information about a panic.

`PanicInfo` has been renamed to [`PanicHookInfo`](https://doc.rust-lang.org/std/panic/struct.PanicHookInfo.html "struct std::panic::PanicHookInfo") to avoid confusion with [`core::panic::PanicInfo`](https://doc.rust-lang.org/core/panic/panic_info/struct.PanicInfo.html "struct core::panic::panic_info::PanicInfo").

## Aliased Type

``` rust
pub struct PanicInfo<'a> { /* private fields */ }
```