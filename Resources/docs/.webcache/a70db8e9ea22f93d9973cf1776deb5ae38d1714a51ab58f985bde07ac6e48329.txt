[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[unix](https://doc.rust-lang.org/std/os/unix/index.html)::[net](https://doc.rust-lang.org/std/os/unix/net/index.html)

# Struct UnixStream Copy item path

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#74)

``` rust
pub struct UnixStream(/* private fields */);
```

Available on **Unix** only.

Expand description

A Unix stream socket.

## Examples

``` rust
use std::os::unix::net::UnixStream;
use std::io::prelude::*;

fn main() -> std::io::Result<()> {
    let mut stream = UnixStream::connect("/path/to/my/socket")?;
    stream.write_all(b"hello world")?;
    let mut response = String::new();
    stream.read_to_string(&mut response)?;
    println!("{response}");
    Ok(())
}
```

## `SOCK_CLOEXEC`

On platforms that support it, we pass the close-on-exec flag to atomically create the socket and set it as CLOEXEC. On Linux, this was added in 2.6.27. See [`socket(2)`](https://www.man7.org/linux/man-pages/man2/socket.2.html#:~:text=SOCK_CLOEXEC) for more information.

## `SIGPIPE`

Writes to the underlying socket in `SOCK_STREAM` mode are made with `MSG_NOSIGNAL` flag. This suppresses the emission of the `SIGPIPE` signal when writing to disconnected socket. In some cases getting a `SIGPIPE` would trigger process termination.

## Implementations

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#91-608)

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#108-116)

Connects to the socket named by `path`.

##### Examples

``` rust
use std::os::unix::net::UnixStream;

let socket = match UnixStream::connect("/tmp/sock") {
    Ok(sock) => sock,
    Err(e) => {
        println!("Couldn't connect: {e:?}");
        return
    }
};
```

1.70.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#142-152)

Connects to the socket specified by [`address`](https://doc.rust-lang.org/std/os/unix/net/struct.SocketAddr.html "struct std::os::unix::net::SocketAddr").

##### Examples

``` rust
use std::os::unix::net::{UnixListener, UnixStream};

fn main() -> std::io::Result<()> {
    let listener = UnixListener::bind("/path/to/the/socket")?;
    let addr = listener.local_addr()?;

    let sock = match UnixStream::connect_addr(&addr) {
        Ok(sock) => sock,
        Err(e) => {
            println!("Couldn't connect: {e:?}");
            return Err(e)
        }
    };
    Ok(())
}
```

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#172-175)

Creates an unnamed pair of connected sockets.

Returns two `UnixStream`s which are connected to each other.

##### Examples

``` rust
use std::os::unix::net::UnixStream;

let (sock1, sock2) = match UnixStream::pair() {
    Ok((sock1, sock2)) => (sock1, sock2),
    Err(e) => {
        println!("Couldn't create a pair of sockets: {e:?}");
        return
    }
};
```

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#196-198)

Creates a new independently owned handle to the underlying socket.

The returned `UnixStream` is a reference to the same stream that this object references. Both handles will read and write the same stream of data, and options set on one stream will be propagated to the other stream.

##### Examples

``` rust
use std::os::unix::net::UnixStream;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    let sock_copy = socket.try_clone().expect("Couldn't clone socket");
    Ok(())
}
```

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#214-216)

Returns the socket address of the local half of this connection.

##### Examples

``` rust
use std::os::unix::net::UnixStream;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    let addr = socket.local_addr().expect("Couldn't get local address");
    Ok(())
}
```

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#232-234)

Returns the socket address of the remote half of this connection.

##### Examples

``` rust
use std::os::unix::net::UnixStream;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    let addr = socket.peer_addr().expect("Couldn't get peer address");
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#262-264)

🔬This is a nightly-only experimental API. (`peer_credentials_unix_socket` [\#42839](https://github.com/rust-lang/rust/issues/42839))

Available on **Android or Apple or Cygwin or DragonFly BSD or FreeBSD or Linux or NetBSD or OpenBSD or QNX SDP 7.x** only.

Gets the peer credentials for this Unix domain socket.

##### Examples

``` rust
#![feature(peer_credentials_unix_socket)]
use std::os::unix::net::UnixStream;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    let peer_cred = socket.peer_cred().expect("Couldn't get peer credentials");
    Ok(())
}
```

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#304-306)

Sets the read timeout for the socket.

If the provided value is [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None"), then [`read`](https://doc.rust-lang.org/std/io/trait.Read.html#tymethod.read "method std::io::Read::read") calls will block indefinitely. An [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") is returned if the zero [`Duration`](https://doc.rust-lang.org/std/time/struct.Duration.html "struct std::time::Duration") is passed to this method.

##### Examples

``` rust
use std::os::unix::net::UnixStream;
use std::time::Duration;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    socket.set_read_timeout(Some(Duration::new(1, 0))).expect("Couldn't set read timeout");
    Ok(())
}
```

An [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") is returned if the zero [`Duration`](https://doc.rust-lang.org/std/time/struct.Duration.html "struct std::time::Duration") is passed to this method:

``` rust
use std::io;
use std::os::unix::net::UnixStream;
use std::time::Duration;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    let result = socket.set_read_timeout(Some(Duration::new(0, 0)));
    let err = result.unwrap_err();
    assert_eq!(err.kind(), io::ErrorKind::InvalidInput);
    Ok(())
}
```

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#347-349)

Sets the write timeout for the socket.

If the provided value is [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None"), then [`write`](https://doc.rust-lang.org/std/macro.write.html "macro std::write") calls will block indefinitely. An [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") is returned if the zero [`Duration`](https://doc.rust-lang.org/std/time/struct.Duration.html "struct std::time::Duration") is passed to this method.

##### Examples

``` rust
use std::os::unix::net::UnixStream;
use std::time::Duration;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    socket.set_write_timeout(Some(Duration::new(1, 0)))
        .expect("Couldn't set write timeout");
    Ok(())
}
```

An [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") is returned if the zero [`Duration`](https://doc.rust-lang.org/std/time/struct.Duration.html "struct std::time::Duration") is passed to this method:

``` rust
use std::io;
use std::os::unix::net::UnixStream;
use std::time::Duration;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    let result = socket.set_write_timeout(Some(Duration::new(0, 0)));
    let err = result.unwrap_err();
    assert_eq!(err.kind(), io::ErrorKind::InvalidInput);
    Ok(())
}
```

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#367-369)

Returns the read timeout of this socket.

##### Examples

``` rust
use std::os::unix::net::UnixStream;
use std::time::Duration;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    socket.set_read_timeout(Some(Duration::new(1, 0))).expect("Couldn't set read timeout");
    assert_eq!(socket.read_timeout()?, Some(Duration::new(1, 0)));
    Ok(())
}
```

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#388-390)

Returns the write timeout of this socket.

##### Examples

``` rust
use std::os::unix::net::UnixStream;
use std::time::Duration;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    socket.set_write_timeout(Some(Duration::new(1, 0)))
        .expect("Couldn't set write timeout");
    assert_eq!(socket.write_timeout()?, Some(Duration::new(1, 0)));
    Ok(())
}
```

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#406-408)

Moves the socket into or out of nonblocking mode.

##### Examples

``` rust
use std::os::unix::net::UnixStream;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    socket.set_nonblocking(true).expect("Couldn't set nonblocking");
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#431-433)

🔬This is a nightly-only experimental API. (`unix_set_mark` [\#96467](https://github.com/rust-lang/rust/issues/96467))

Available on **FreeBSD or Linux or OpenBSD** only.

Set the id of the socket for network filtering purpose

``` rust
#![feature(unix_set_mark)]
use std::os::unix::net::UnixStream;

fn main() -> std::io::Result<()> {
    let sock = UnixStream::connect("/tmp/sock")?;
    sock.set_mark(32)?;
    Ok(())
}
```

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#454-456)

Returns the value of the `SO_ERROR` option.

##### Examples

``` rust
use std::os::unix::net::UnixStream;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    if let Ok(Some(err)) = socket.take_error() {
        println!("Got error: {err:?}");
    }
    Ok(())
}
```

##### Platform specific

On Redox this always returns `None`.

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#477-479)

Shuts down the read, write, or both halves of this connection.

This function will cause all pending and future I/O calls on the specified portions to immediately return with an appropriate value (see the documentation of [`Shutdown`](https://doc.rust-lang.org/std/net/enum.Shutdown.html "enum std::net::Shutdown")).

##### Examples

``` rust
use std::os::unix::net::UnixStream;
use std::net::Shutdown;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    socket.shutdown(Shutdown::Both).expect("shutdown function failed");
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#503-505)

🔬This is a nightly-only experimental API. (`unix_socket_peek` [\#76923](https://github.com/rust-lang/rust/issues/76923))

Receives data on the socket from the remote address to which it is connected, without removing that data from the queue. On success, returns the number of bytes peeked.

Successive calls return the same data. This is accomplished by passing `MSG_PEEK` as a flag to the underlying `recv` system call.

##### Examples

``` rust
#![feature(unix_socket_peek)]

use std::os::unix::net::UnixStream;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    let mut buf = [0; 10];
    let len = socket.peek(&mut buf).expect("peek failed");
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#552-560)

🔬This is a nightly-only experimental API. (`unix_socket_ancillary_data` [\#76915](https://github.com/rust-lang/rust/issues/76915))

Available on **Android or Cygwin or Linux** only.

Receives data and ancillary data from socket.

On success, returns the number of bytes read.

##### Examples

``` rust
#![feature(unix_socket_ancillary_data)]
use std::os::unix::net::{UnixStream, SocketAncillary, AncillaryData};
use std::io::IoSliceMut;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    let mut buf1 = [1; 8];
    let mut buf2 = [2; 16];
    let mut buf3 = [3; 8];
    let mut bufs = &mut [
        IoSliceMut::new(&mut buf1),
        IoSliceMut::new(&mut buf2),
        IoSliceMut::new(&mut buf3),
    ][..];
    let mut fds = [0; 8];
    let mut ancillary_buffer = [0; 128];
    let mut ancillary = SocketAncillary::new(&mut ancillary_buffer[..]);
    let size = socket.recv_vectored_with_ancillary(bufs, &mut ancillary)?;
    println!("received {size}");
    for ancillary_result in ancillary.messages() {
        if let AncillaryData::ScmRights(scm_rights) = ancillary_result.unwrap() {
            for fd in scm_rights {
                println!("receive file descriptor: {fd}");
            }
        }
    }
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#601-607)

🔬This is a nightly-only experimental API. (`unix_socket_ancillary_data` [\#76915](https://github.com/rust-lang/rust/issues/76915))

Available on **Android or Cygwin or Linux** only.

Sends data and ancillary data on the socket.

On success, returns the number of bytes written.

##### Examples

``` rust
#![feature(unix_socket_ancillary_data)]
use std::os::unix::net::{UnixStream, SocketAncillary};
use std::io::IoSlice;

fn main() -> std::io::Result<()> {
    let socket = UnixStream::connect("/tmp/sock")?;
    let buf1 = [1; 8];
    let buf2 = [2; 16];
    let buf3 = [3; 8];
    let bufs = &[
        IoSlice::new(&buf1),
        IoSlice::new(&buf2),
        IoSlice::new(&buf3),
    ][..];
    let fds = [0, 1, 2];
    let mut ancillary_buffer = [0; 128];
    let mut ancillary = SocketAncillary::new(&mut ancillary_buffer[..]);
    ancillary.add_fds(&fds[..]);
    socket.send_vectored_with_ancillary(bufs, &mut ancillary)
        .expect("send_vectored_with_ancillary function failed");
    Ok(())
}
```

## Trait Implementations

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#716-721)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#718-720)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

Borrows the file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.AsFd.html#tymethod.as_fd)

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#692-697)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#694-696)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

Extracts the raw file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.AsRawFd.html#tymethod.as_raw_fd)

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#77-89)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#78-88)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#733-738)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#735-737)

Converts to this type from the input type.

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#724-730)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#727-729)

Takes ownership of a [`UnixStream`](https://doc.rust-lang.org/std/os/unix/net/struct.UnixStream.html "struct std::os::unix::net::UnixStream")’s socket file descriptor.

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#700-705)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#702-704)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

Constructs a new instance of `Self` from the given raw file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.FromRawFd.html#tymethod.from_raw_fd)

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#708-713)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#710-712)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

Consumes this object, returning the raw underlying file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.IntoRawFd.html#tymethod.into_raw_fd)

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#611-628)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#612-614)

Pull some bytes from this source into the specified buffer, returning how many bytes were read. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#tymethod.read)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#616-618)

🔬This is a nightly-only experimental API. (`read_buf` [\#78485](https://github.com/rust-lang/rust/issues/78485))

Pull some bytes from this source into the specified buffer. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_buf)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#620-622)

Like `read`, except that it reads into a slice of buffers. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_vectored)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#625-627)

🔬This is a nightly-only experimental API. (`can_vector` [\#69941](https://github.com/rust-lang/rust/issues/69941))

Determines if this `Read`er has an efficient `read_vectored` implementation. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.is_read_vectored)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#922-924)

Reads all bytes until EOF in this source, placing them into `buf`. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_to_end)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#978-980)

Reads all bytes until EOF in this source, appending them to `buf`. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_to_string)

1.6.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1031-1033)

Reads the exact number of bytes required to fill `buf`. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_exact)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1067-1069)

🔬This is a nightly-only experimental API. (`read_buf` [\#78485](https://github.com/rust-lang/rust/issues/78485))

Reads the exact number of bytes required to fill `cursor`. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_buf_exact)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1106-1111)

Creates a “by reference” adapter for this instance of `Read`. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.by_ref)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1149-1154)

Transforms this `Read` instance to an [`Iterator`](https://doc.rust-lang.org/std/iter/trait.Iterator.html "trait std::iter::Iterator") over its bytes. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.bytes)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1187-1192)

Creates an adapter which will chain this stream with another. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.chain)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1226-1231)

Creates an adapter which will read at most `limit` bytes from it. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.take)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1261-1271)

🔬This is a nightly-only experimental API. (`read_array` [\#148848](https://github.com/rust-lang/rust/issues/148848))

Read and return a fixed array of bytes from this source. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_array)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1299-1304)

🔬This is a nightly-only experimental API. (`read_le` [\#156983](https://github.com/rust-lang/rust/issues/156983))

Read and return a type (e.g. an integer) in little-endian order. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_le)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1332-1337)

🔬This is a nightly-only experimental API. (`read_le` [\#156983](https://github.com/rust-lang/rust/issues/156983))

Read and return a type (e.g. an integer) in big-endian order. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_be)

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#631-648)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#632-634)

Pull some bytes from this source into the specified buffer, returning how many bytes were read. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#tymethod.read)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#636-638)

🔬This is a nightly-only experimental API. (`read_buf` [\#78485](https://github.com/rust-lang/rust/issues/78485))

Pull some bytes from this source into the specified buffer. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_buf)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#640-642)

Like `read`, except that it reads into a slice of buffers. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_vectored)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#645-647)

🔬This is a nightly-only experimental API. (`can_vector` [\#69941](https://github.com/rust-lang/rust/issues/69941))

Determines if this `Read`er has an efficient `read_vectored` implementation. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.is_read_vectored)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#922-924)

Reads all bytes until EOF in this source, placing them into `buf`. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_to_end)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#978-980)

Reads all bytes until EOF in this source, appending them to `buf`. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_to_string)

1.6.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1031-1033)

Reads the exact number of bytes required to fill `buf`. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_exact)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1067-1069)

🔬This is a nightly-only experimental API. (`read_buf` [\#78485](https://github.com/rust-lang/rust/issues/78485))

Reads the exact number of bytes required to fill `cursor`. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_buf_exact)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1106-1111)

Creates a “by reference” adapter for this instance of `Read`. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.by_ref)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1149-1154)

Transforms this `Read` instance to an [`Iterator`](https://doc.rust-lang.org/std/iter/trait.Iterator.html "trait std::iter::Iterator") over its bytes. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.bytes)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1187-1192)

Creates an adapter which will chain this stream with another. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.chain)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1226-1231)

Creates an adapter which will read at most `limit` bytes from it. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.take)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1261-1271)

🔬This is a nightly-only experimental API. (`read_array` [\#148848](https://github.com/rust-lang/rust/issues/148848))

Read and return a fixed array of bytes from this source. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_array)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1299-1304)

🔬This is a nightly-only experimental API. (`read_le` [\#156983](https://github.com/rust-lang/rust/issues/156983))

Read and return a type (e.g. an integer) in little-endian order. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_le)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1332-1337)

🔬This is a nightly-only experimental API. (`read_le` [\#156983](https://github.com/rust-lang/rust/issues/156983))

Read and return a type (e.g. an integer) in big-endian order. [Read more](https://doc.rust-lang.org/std/io/trait.Read.html#method.read_be)

[Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/socket.rs.html#57-65)

Available on **Android or Cygwin or Linux** only.

[Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/socket.rs.html#58-60)

🔬This is a nightly-only experimental API. (`unix_socket_ancillary_data` [\#76915](https://github.com/rust-lang/rust/issues/76915))

Query the current setting of socket option `SO_PASSCRED`.

[Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/socket.rs.html#62-64)

🔬This is a nightly-only experimental API. (`unix_socket_ancillary_data` [\#76915](https://github.com/rust-lang/rust/issues/76915))

Enable or disable socket option `SO_PASSCRED`. [Read more](https://doc.rust-lang.org/std/os/linux/net/trait.UnixSocketExt.html#tymethod.set_passcred)

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#651-668)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#652-654)

Writes a buffer into this writer, returning how many bytes were written. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.write)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#656-658)

Like [`write`](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.write "method std::io::Write::write"), except that it writes from a slice of buffers. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_vectored)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#661-663)

🔬This is a nightly-only experimental API. (`can_vector` [\#69941](https://github.com/rust-lang/rust/issues/69941))

Determines if this `Write`r has an efficient [`write_vectored`](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_vectored "method std::io::Write::write_vectored") implementation. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.is_write_vectored)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#665-667)

Flushes this output stream, ensuring that all intermediately buffered contents reach their destination. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.flush)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1611-1623)

Attempts to write an entire buffer into this writer. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_all)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1673-1688)

🔬This is a nightly-only experimental API. (`write_all_vectored` [\#70436](https://github.com/rust-lang/rust/issues/70436))

Attempts to write multiple buffers into this writer. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_all_vectored)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1726-1732)

Writes a formatted string into this writer, returning any error encountered. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_fmt)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1756-1761)

Creates a “by reference” adapter for this instance of `Write`. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.by_ref)

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#671-689)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#672-674)

Writes a buffer into this writer, returning how many bytes were written. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.write)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#676-678)

Like [`write`](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.write "method std::io::Write::write"), except that it writes from a slice of buffers. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_vectored)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#681-683)

🔬This is a nightly-only experimental API. (`can_vector` [\#69941](https://github.com/rust-lang/rust/issues/69941))

Determines if this `Write`r has an efficient [`write_vectored`](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_vectored "method std::io::Write::write_vectored") implementation. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.is_write_vectored)

[Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#686-688)

Flushes this output stream, ensuring that all intermediately buffered contents reach their destination. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.flush)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1611-1623)

Attempts to write an entire buffer into this writer. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_all)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1673-1688)

🔬This is a nightly-only experimental API. (`write_all_vectored` [\#70436](https://github.com/rust-lang/rust/issues/70436))

Attempts to write multiple buffers into this writer. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_all_vectored)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1726-1732)

Writes a formatted string into this writer, returning any error encountered. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_fmt)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1756-1761)

Creates a “by reference” adapter for this instance of `Write`. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.by_ref)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.