[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[windows](https://doc.rust-lang.org/std/os/windows/index.html)::[io](https://doc.rust-lang.org/std/os/windows/io/index.html)

# Trait FromRawHandle Copy item path

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#45-73)

``` rust
pub trait FromRawHandle {
    // Required method
    unsafe fn from_raw_handle(handle: RawHandle) -> Self;
}
```

Available on **Windows** only.

Expand description

Constructs I/O objects from raw handles.

## Required Methods

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#72)

Constructs a new I/O object from the specified raw handle.

This function is typically used to **consume ownership** of the handle given, passing responsibility for closing the handle to the returned object. When used in this way, the returned object will take responsibility for closing it when the object goes out of scope.

However, consuming ownership is not strictly required. Use a `From<OwnedHandle>::from` implementation for an API which strictly consumes ownership.

##### Safety

The `handle` passed in must:

- be an [owned handle](https://doc.rust-lang.org/std/io/index.html#io-safety "mod std::io"); in particular, it must be open.
- be a handle for a resource that may be freed via [`CloseHandle`](https://docs.microsoft.com/en-us/windows/win32/api/handleapi/nf-handleapi-closehandle) (as opposed to `RegCloseKey` or other close functions).

Note that the handle *may* have the value `INVALID_HANDLE_VALUE` (-1), which is sometimes a valid handle value. See [here](https://devblogs.microsoft.com/oldnewthing/20040302-00/?p=40443) for the full story.

## Dyn Compatibility

This trait is **not** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#156-166)

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/handle.rs.html#322-327)

1.87.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#322-326)

1.87.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#343-347)

1.2.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#16-22)