[std](https://doc.rust-lang.org/std/index.html)::[mem](https://doc.rust-lang.org/std/mem/index.html)

# Function min_align_of_val Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/core/mem/mod.rs.html#511)

``` rust
pub fn min_align_of_val<T>(val: &T) -> usizewhere
    T: ?Sized,
```

👎Deprecated since 1.2.0:

use `align_of_val` instead

Expand description

Returns the [ABI](https://en.wikipedia.org/wiki/Application_binary_interface)-required minimum alignment of the type of the value that `val` points to in bytes.

Every reference to a value of the type `T` must be a multiple of this number.

## Examples

``` rust
use std::mem;

assert_eq!(4, mem::min_align_of_val(&5i32));
```