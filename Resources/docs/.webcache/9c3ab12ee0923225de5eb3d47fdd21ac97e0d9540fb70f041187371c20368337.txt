# Go with the flow: basic

- [1 intro](https://beautifulracket.com/basic/intro.html)
- [2 specification and setup](https://beautifulracket.com/basic/specification-and-setup.html)
- [3 the lexer](https://beautifulracket.com/basic/the-lexer.html)
- [4 the tokenizer](https://beautifulracket.com/basic/the-tokenizer.html)
- [5 the parser](https://beautifulracket.com/basic/the-parser.html)
- [6 the reader](https://beautifulracket.com/basic/the-reader.html)
- [7 the expander](https://beautifulracket.com/basic/the-expander.html)
- [8 testing the language](https://beautifulracket.com/basic/testing-the-language.html)
- [9 recap](https://beautifulracket.com/basic/recap.html)
- [10 source listing](https://beautifulracket.com/basic/source-listing.html)

In previous tokenizer modules—for instance, those for [bf](https://beautifulracket.com/bf/the-tokenizer-and-reader.html) and [jsonic](https://beautifulracket.com/jsonic/the-tokenizer.html)—we made one function, make-tokenizer. It returned a function called next-token that successively retrieved tokens from an input port using a character-matching function called a [lexer](https://beautifulracket.com/appendix/glossary.html#lexer).

Since then, we’ve learned that a lexer can also be useful for [syntax coloring](https://beautifulracket.com/jsonic-2/syntax-coloring.html). So this time, we’ll separate our lexer from our tokenizer. That way, we can reuse it later for the syntax colorer.

Rather than start with "basic/tokenizer.rkt", we create a new "basic/lexer.rkt" module:

basic/lexer.rkt

    #lang br
    (require brag/support)

    (define basic-lexer
      ···)

    (provide basic-lexer)

Previously we used [lexer](http://docs.racket-lang.org/br-parser-tools/Lexers.html#(form._((lib._br-parser-tools/lex..rkt)._lexer))) to set up our matching rules. That worked fine, but when we wanted to add [source locations](https://beautifulracket.com/jsonic-2/source-locations.html), we found ourselves doing a lot of housekeeping—we had to manually fill in the source-location fields for the tokens generated on the right-hand side.

This time, we switch to [lexer-srcloc](http://docs.racket-lang.org/br-parser-tools/Lexers.html#(form._((lib._br-parser-tools/lex..rkt)._lexer-srcloc))), a variant of lexer that automatically gathers source-location information and attaches it to our tokens. We get the same source locations as before, but with zero manual labor.

basic/lexer.rkt

    #lang br
    (require brag/support)

    (define basic-lexer
      (lexer-srcloc
       ···))

    (provide basic-lexer)

To make this happen, lexer-srcloc returns instances of a structure called a [srcloc-token](http://docs.racket-lang.org/br-parser-tools/Lexers.html#(def._((lib._br-parser-tools/lex..rkt)._srcloc-token))). This structure holds two values: a token (that is, whatever we put on the right-hand side of the rule, which will usually be a string or an instance of the [token](http://docs.racket-lang.org/brag/index.html#(def._((lib._brag/support..rkt)._token))) structure) and a source location (which is itself an instance of the [srcloc](http://docs.racket-lang.org/reference/exns.html#(def._((lib._racket/private/base..rkt)._srcloc))) structure).

But we don’t need to get bogged down in the details. lexer-srcloc will automatically create the correct srcloc and srcloc-token structures. In turn, any parser made with #lang brag can take these srcloc-token structures as input. So the output from lexer-srcloc can be passed to our parser without any extra housekeeping.

By the way, since a lexer takes an input port as an argument, why aren’t we declaring this function as (basic-lexer port)? If we look at our previous examples, for instance [the jsonic tokenizer](https://beautifulracket.com/jsonic/the-tokenizer.html), we also didn’t declare the lexer with an argument. The reason is that both lexer and lexer-srcloc return a [lambda](http://docs.racket-lang.org/reference/lambda.html#(form._((lib._racket/private/base..rkt)._lambda))) that takes one argument, so we omit the argument from the left-hand side of the declaration.

For the same reason, these two declarations are the same:

    (define (f x) (+ x x))
    (define f (lambda (x) (+ x x))

In the second case, because the right-hand side is a lambda expression, we don’t declare the x argument on the left. In fact, the lambda notation on the bottom is the “real” way functions are handled in Racket—the top notation is a convenience that’s transformed into the bottom notation. (For more about lambda notation, see [functions](https://beautifulracket.com/explainer/functions.html).)

If we wanted, we could declare basic-lexer like so:

    (define (basic-lexer port)
      (define the-lexer
        (lexer-srcloc
         ···))
      (the-lexer port))

But that’s just a wordier way of saying the same thing.

### [Lexer rules](#lexer-rules)

First, we handle whitespace. Most whitespace in BASIC is ignored. But newlines have special status, because each line of the program has to be on a separate line of the file. This program is valid:

    10 print "hello world"
    20 goto 10

But this version, with the newline removed, is not:

    10 print "hello world" 20 goto 10

When we write [the parser](https://beautifulracket.com/basic/the-parser.html), we’ll need to keep these two cases separate. We can only do that if we preserve the newline as a token. So our first whitespace-lexing rule will match the "\n" character, and put it inside a [token](http://docs.racket-lang.org/brag/index.html#(def._((lib._brag/support..rkt)._token))) structure named NEWLINE:

basic/lexer.rkt

    #lang br
    (require brag/support)

    (define basic-lexer
      (lexer-srcloc
       ["\n" (token 'NEWLINE lexeme)]))

    (provide basic-lexer)

But all the other whitespace, we can ignore. In previous lexers, we ignored whitespace or comments by recursively calling the lexer to fetch the next token. This shortcut doesn’t work with lexer-srcloc, because each recursive call also recursively wraps more layers of source-location information. That’s not what we want.

Let’s learn an alternate technique:

basic/lexer.rkt

    #lang br
    (require brag/support)

    (define basic-lexer
      (lexer-srcloc
       ["\n" (token 'NEWLINE lexeme)]
       [whitespace (token lexeme #:skip? #t)]))

    (provide basic-lexer)

Rather than ignoring certain source characters within the lexer, we can mark the tokens as “skippable” so that the parser ignores them. We do this by wrapping them in a [token](http://docs.racket-lang.org/brag/index.html#(def._((lib._brag/support..rkt)._token))) structure as usual, but we make the token skippable by passing the #:skip? keyword argument with a #t value. The parser will ignore this token, as if it had never been there in the first place. Problem solved.

A rem at the beginning of a line marks a line comment:

basic/lexer.rkt

    #lang br
    (require brag/support)

    (define basic-lexer
      (lexer-srcloc
       ["\n" (token 'NEWLINE lexeme)]
       [whitespace (token lexeme #:skip? #t)]
       [(from/stop-before "rem" "\n") (token 'REM lexeme)]))

    (provide basic-lexer)

Recall from [specification and setup](https://beautifulracket.com/basic/specification-and-setup.html) that a rem doesn’t remove the line entirely from the program—it can still be the target of a goto statement. So we only want to soak up the characters on the right side of the rem statement (not the line number).

Furthermore, though the "\n" marks the end of the rem, we don’t want to absorb the "\n" itself (because of its semantic significance in separating lines). So we use [from/stop-before](http://docs.racket-lang.org/brag/index.html#(form._((lib._brag/support..rkt)._from/stop-before))) to capture these characters and put them in a REM token. Unlike [from/to](http://docs.racket-lang.org/brag/index.html#(form._((lib._brag/support..rkt)._from/to))), which captures the left and right boundary characters, from/stop-before captures the left boundary character but not the right (hence “stop before”).

Then we add our other supported statements—print, goto, end—our only supported numerical operation +, and the separators for multiple print arguments ; and multiple statements ::

basic/lexer.rkt

    #lang br
    (require brag/support)

    (define basic-lexer
      (lexer-srcloc
       ["\n" (token 'NEWLINE lexeme)]
       [whitespace (token lexeme #:skip? #t)]
       [(from/stop-before "rem" "\n") (token 'REM lexeme)]
       [(:or "print" "goto" "end"
             "+" ":" ";") (token lexeme lexeme)]))

    (provide basic-lexer)

[:or](http://docs.racket-lang.org/brag/index.html#(form._((lib._brag/support..rkt)._~3aor))) is a lexer function that matches one of a listed set of possibilities.

As the return value, we make a [token](http://docs.racket-lang.org/brag/index.html#(def._((lib._brag/support..rkt)._token))) structure, passing [lexeme](http://docs.racket-lang.org/br-parser-tools/Lexers.html#(form._((lib._br-parser-tools/lex..rkt)._lexeme))) in both the type and value fields. Strictly speaking, we don’t need to return a token structure for this rule. We could return the raw lexeme value, because it’ll be matched literally in [the parser](https://beautifulracket.com/basic/the-parser.html). But when we get to [the syntax colorer](https://beautifulracket.com/basic-2/the-syntax-colorer.html), we’ll find it more convenient to have the lexer results consistently packaged into token structures.

Finally we need to put in our data types: integers, decimals, and strings.

Both integers and decimals need to match a string of digits. To simplify our notation, we use [define-lex-abbrev](http://docs.racket-lang.org/br-parser-tools/Lexers.html#(form._((lib._br-parser-tools/lex..rkt)._define-lex-abbrev))) to make a little named subpattern that we can use in other rules. Our abbreviation digits will match any sequence of digits (where [char-set](http://docs.racket-lang.org/br-parser-tools/Lexers.html#(form._((lib._br-parser-tools/lex..rkt)._char-set))) means “match any character in this string” and [:+](http://docs.racket-lang.org/brag/index.html#(form._((lib._brag/support..rkt)._~3a+))) still means “one or more”):

basic/lexer.rkt

    #lang br
    (require brag/support)

    (define-lex-abbrev digits (:+ (char-set "0123456789")))

    (define basic-lexer
      (lexer-srcloc
       ["\n" (token 'NEWLINE lexeme)]
       [whitespace (token lexeme #:skip? #t)]
       [(from/stop-before "rem" "\n") (token 'REM lexeme)]
       [(:or "print" "goto" "end"
             "+" ":" ";") (token lexeme lexeme)]))

    (provide basic-lexer)

Once we have digits, we can easily write rules to match integers and decimals:

basic/lexer.rkt

    #lang br
    (require brag/support)

    (define-lex-abbrev digits (:+ (char-set "0123456789")))

    (define basic-lexer
      (lexer-srcloc
       ["\n" (token 'LINE-SEP)]
       [whitespace (token lexeme #:skip? #t)]
       [(from/stop-before "rem" "\n") (token 'REM lexeme)]
       [(:or "print" "goto" "end"
             "+" ":" ";") (token lexeme lexeme)]
       [digits (token 'INTEGER (string->number lexeme))]
       [(:or (:seq (:? digits) "." digits)
             (:seq digits "."))
        (token 'DECIMAL (string->number lexeme))]))

    (provide basic-lexer)

An integer is just digits. We label the token with the name 'INTEGER, and use [string->number](http://docs.racket-lang.org/reference/generic-numbers.html#(def._((quote._~23~25kernel)._string-~3enumber))) to pass through the numeric value rather than its string representation.

The decimal rule is a little trickier, because we want to capture three valid possibilities:

       .456
    123.456
    123.

We can use [:seq](http://docs.racket-lang.org/brag/index.html#(form._((lib._brag/support..rkt)._~3aseq))), a lexer function that lets us match a series of patterns in a row, to express these possibilities as patterns, and then use [:or](http://docs.racket-lang.org/brag/index.html#(form._((lib._brag/support..rkt)._~3aor))) to choose among them:

    (:or (:seq "." digits)        ;    .456
         (:seq digits "." digits) ; 123.456
         (:seq digits "."))       ; 123.

But we can combine the first and second cases into a single pattern by using [:?](http://docs.racket-lang.org/brag/index.html#(form._((lib._brag/support..rkt)._~3a~3f))) to make the first digits subpattern optional (again, just like ? in a regular expression).

    (:or (:seq (:? digits) "." digits) ; 123.456 or .456
         (:seq digits "."))            ; 123.

And that becomes the pattern for our rule. Though we shouldn’t get overzealous about streamlining the pattern, because this is wrong:

    (:seq (:? digits) "." (:? digits))

This rule is too broad, because it would also match a naked decimal point. A decimal number always needs digits on one side or the other. So we need two pieces to our pattern.

To finish, we label the token with the name 'DECIMAL, and again use string->number to pass through its numeric value.

Last, we have strings:

basic/lexer.rkt

    #lang br
    (require brag/support)

    (define-lex-abbrev digits (:+ (char-set "0123456789")))

    (define basic-lexer
      (lexer-srcloc
       ["\n" (token 'NEWLINE lexeme)]
       [whitespace (token lexeme #:skip? #t)]
       [(from/stop-before "rem" "\n") (token 'REM lexeme)]
       [(:or "print" "goto" "end"
             "+" ":" ";") (token lexeme lexeme)]
       [digits (token 'INTEGER (string->number lexeme))]
       [(:or (:seq (:? digits) "." digits)
             (:seq digits "."))
        (token 'DECIMAL (string->number lexeme))]
       [(:or (from/to "\"" "\"") (from/to "'" "'"))
        (token 'STRING
               (substring lexeme
                          1 (sub1 (string-length lexeme))))]))

    (provide basic-lexer)

For these, we can use [from/to](http://docs.racket-lang.org/brag/index.html#(form._((lib._brag/support..rkt)._from/to))) to match anything delimited by either single or double quotes. When we make the token, we don’t need the quote marks, so we use [substring](http://docs.racket-lang.org/reference/strings.html#(def._((quote._~23~25kernel)._substring))) to drop the first and last characters of the matched lexeme. Then we package this into a [token](http://docs.racket-lang.org/brag/index.html#(def._((lib._brag/support..rkt)._token))) called 'STRING.

And that’s our finished lexer.

### [Testing the lexer](#testing-the-lexer)

Well, almost finished. No function is really done until it’s thoroughly tested. We could put tests inside "basic/lexer.rkt" module using a test submodule, as [we’ve done before](https://beautifulracket.com/jsonic-2/unit-tests.html). But here’s how we could make a set of tests for the lexer in a separate file:

basic/lexer-test.rkt

    #lang br
    (require "lexer.rkt" brag/support rackunit)

    (define (lex str)
      (apply-port-proc basic-lexer str))

    ···

First, we define lex, which is a helper function that will take a string and lex it into a list of tokens using basic-lexer and the helper function [apply-port-proc](http://docs.racket-lang.org/brag/index.html#(def._((lib._brag/support..rkt)._apply-port-proc))), which comes from brag/support. apply-port-proc applies a function to an input port (or string) repeatedly until it reaches the end, and then returns the results in a list.

Then we use testing functions from rackunit like [check-equal?](http://docs.racket-lang.org/rackunit/api.html#(def._((lib._rackunit/main..rkt)._check-equal~3f))) and [check-exn](http://docs.racket-lang.org/rackunit/api.html#(def._((lib._rackunit/main..rkt)._check-exn))) to compare the results of lex with lists of [srcloc-token](http://docs.racket-lang.org/br-parser-tools/Lexers.html#(def._((lib._br-parser-tools/lex..rkt)._srcloc-token))) structures. Here are some simple sample tests that cover every lexing rule we wrote for basic-lexer (but feel free to add others):

basic/lexer-test.rkt

    #lang br
    (require "lexer.rkt" brag/support rackunit)

    (define (lex str)
      (apply-port-proc basic-lexer str))

    (check-equal? (lex "") empty)
    (check-equal?
     (lex " ")
     (list (srcloc-token (token " " #:skip? #t)
                         (srcloc 'string 1 0 1 1))))
    (check-equal?
     (lex "rem ignored\n")
     (list (srcloc-token (token 'REM "rem ignored")
                         (srcloc 'string 1 0 1 11))
           (srcloc-token (token 'NEWLINE "\n")
                         (srcloc 'string 1 11 12 1))))
    (check-equal?
     (lex "print")
     (list (srcloc-token (token "print" "print")
                         (srcloc 'string 1 0 1 5))))
    (check-equal?
     (lex "goto")
     (list (srcloc-token (token "goto" "goto")
                         (srcloc 'string 1 0 1 4))))
    (check-equal?
     (lex "end")
     (list (srcloc-token (token "end" "end")
                         (srcloc 'string 1 0 1 3))))
    (check-equal?
     (lex "+")
     (list (srcloc-token (token "+" "+")
                         (srcloc 'string 1 0 1 1))))
    (check-equal?
     (lex ";")
     (list (srcloc-token (token ";" ";")
                         (srcloc 'string 1 0 1 1))))
    (check-equal?
     (lex ":")
     (list (srcloc-token (token ":" ":")
                         (srcloc 'string 1 0 1 1))))
    (check-equal?
     (lex "12")
     (list (srcloc-token (token 'INTEGER 12)
                         (srcloc 'string 1 0 1 2))))
    (check-equal?
     (lex "1.2")
     (list (srcloc-token (token 'DECIMAL 1.2)
                         (srcloc 'string 1 0 1 3))))
    (check-equal?
     (lex "12.")
     (list (srcloc-token (token 'DECIMAL 12.)
                         (srcloc 'string 1 0 1 3))))
    (check-equal?
     (lex ".12")
     (list (srcloc-token (token 'DECIMAL .12)
                         (srcloc 'string 1 0 1 3))))
    (check-equal?
     (lex "\"foo\"")
     (list (srcloc-token (token 'STRING "foo")
                         (srcloc 'string 1 0 1 5))))
    (check-equal?
     (lex "'foo'")
     (list (srcloc-token (token 'STRING "foo")
                         (srcloc 'string 1 0 1 5))))
    (check-exn exn:fail:read? (lambda () (lex "x")))

- [1 intro](https://beautifulracket.com/basic/intro.html)
- [2 specification and setup](https://beautifulracket.com/basic/specification-and-setup.html)
- [3 the lexer](https://beautifulracket.com/basic/the-lexer.html)
- [4 the tokenizer](https://beautifulracket.com/basic/the-tokenizer.html)
- [5 the parser](https://beautifulracket.com/basic/the-parser.html)
- [6 the reader](https://beautifulracket.com/basic/the-reader.html)
- [7 the expander](https://beautifulracket.com/basic/the-expander.html)
- [8 testing the language](https://beautifulracket.com/basic/testing-the-language.html)
- [9 recap](https://beautifulracket.com/basic/recap.html)
- [10 source listing](https://beautifulracket.com/basic/source-listing.html)