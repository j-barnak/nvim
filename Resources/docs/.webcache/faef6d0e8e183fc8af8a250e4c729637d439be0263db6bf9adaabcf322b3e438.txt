[std](https://doc.rust-lang.org/std/index.html)

# Module primitive Copy item path

1.43.0 · [Source](https://doc.rust-lang.org/src/core/lib.rs.html#344)

Expand description

This module reexports the primitive types to allow usage that is not possibly shadowed by other declared types.

This is normally only useful in macro generated code.

An example of this is when generating a new struct and an impl for it:

[ⓘ](# "This example deliberately fails to compile")

``` rust
pub struct bool;

impl QueryId for bool {
    const SOME_PROPERTY: bool = true;
}
```

Note that the `SOME_PROPERTY` associated constant would not compile, as its type `bool` refers to the struct, rather than to the primitive bool type.

A correct implementation could look like:

``` rust
pub struct bool;

impl QueryId for bool {
    const SOME_PROPERTY: ::core::primitive::bool = true;
}
```

We also used `::core` instead of `core`, because `core` can be shadowed, too. Paths, starting with `::`, are searched in the [extern prelude](https://doc.rust-lang.org/nightly/reference/names/preludes.html#extern-prelude) since Edition 2018.

## Re-exports

`pub use `[`bool`](https://doc.rust-lang.org/std/primitive.bool.html)`;`

`pub use `[`char`](https://doc.rust-lang.org/std/primitive.char.html)`;`

`pub use `[`f32`](https://doc.rust-lang.org/std/primitive.f32.html)`;`

`pub use `[`f64`](https://doc.rust-lang.org/std/primitive.f64.html)`;`

`pub use `[`i8`](https://doc.rust-lang.org/std/primitive.i8.html)`;`

`pub use `[`i16`](https://doc.rust-lang.org/std/primitive.i16.html)`;`

`pub use `[`i32`](https://doc.rust-lang.org/std/primitive.i32.html)`;`

`pub use `[`i64`](https://doc.rust-lang.org/std/primitive.i64.html)`;`

`pub use `[`i128`](https://doc.rust-lang.org/std/primitive.i128.html)`;`

`pub use `[`isize`](https://doc.rust-lang.org/std/primitive.isize.html)`;`

`pub use `[`str`](https://doc.rust-lang.org/std/primitive.str.html)`;`

`pub use `[`u8`](https://doc.rust-lang.org/std/primitive.u8.html)`;`

`pub use `[`u16`](https://doc.rust-lang.org/std/primitive.u16.html)`;`

`pub use `[`u32`](https://doc.rust-lang.org/std/primitive.u32.html)`;`

`pub use `[`u64`](https://doc.rust-lang.org/std/primitive.u64.html)`;`

`pub use `[`u128`](https://doc.rust-lang.org/std/primitive.u128.html)`;`

`pub use `[`usize`](https://doc.rust-lang.org/std/primitive.usize.html)`;`