# [`pwnlib.tubes.buffer`](#module-pwnlib.tubes.buffer "pwnlib.tubes.buffer") — buffer implementation for tubes

*class *pwnlib.tubes.buffer.Buffer(*buffer_fill_size=None*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/tubes/buffer.py#L7-L191)  
List of strings with some helper routines.

Example

    >>> b = Buffer()
    >>> b.add(b"A" * 10)
    >>> b.add(b"B" * 10)
    >>> len(b)
    20
    >>> b.get(1)
    b'A'
    >>> len(b)
    19
    >>> b.get(9999)
    b'AAAAAAAAABBBBBBBBBB'
    >>> len(b)
    0
    >>> b.get(1)
    b''

Implementation Details:

> Implemented as a list. Strings are added onto the end. The `0th` item in the buffer is the oldest item, and will be received first.

\_\_contains\_\_(*x*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/tubes/buffer.py#L55-L68)  
    >>> b = Buffer()
    >>> b.add(b'asdf')
    >>> b'x' in b
    False
    >>> b.add(b'x')
    >>> b'x' in b
    True

\_\_init\_\_(*buffer_fill_size=None*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/tubes/buffer.py#L35-L38)  

\_\_len\_\_()[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/tubes/buffer.py#L40-L50)  
    >>> b = Buffer()
    >>> b.add(b'lol')
    >>> len(b) == 3
    True
    >>> b.add(b'foobar')
    >>> len(b) == 9
    True

add(*data*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/tubes/buffer.py#L85-L100)  
Adds data to the buffer.

Parameters:  
**data** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")*,*[*Buffer*](#pwnlib.tubes.buffer.Buffer "pwnlib.tubes.buffer.Buffer")) – Data to add

get(*want=inf*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/tubes/buffer.py#L127-L175)  
Retrieves bytes from the buffer.

Parameters:  
**want** ([*int*](https://docs.python.org/3/library/functions.html#int "(in Python v3.14)")) – Maximum number of bytes to fetch

Returns:  
Data as string

Example

    >>> b = Buffer()
    >>> b.add(b'hello')
    >>> b.add(b'world')
    >>> b.get(1)
    b'h'
    >>> b.get()
    b'elloworld'

get_fill_size(*size=None*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/tubes/buffer.py#L177-L191)  
Retrieves the default fill size for this buffer class.

Parameters:  
**size** ([*int*](https://docs.python.org/3/library/functions.html#int "(in Python v3.14)")) – (Optional) If set and not None, returns the size variable back.

Returns:  
Fill size as integer if size is None, else size.

index(*x*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/tubes/buffer.py#L70-L83)  
    >>> b = Buffer()
    >>> b.add(b'asdf')
    >>> b.add(b'qwert')
    >>> b.index(b't') == len(b) - 1
    True

unget(*data*)[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/tubes/buffer.py#L102-L125)  
Places data at the front of the buffer.

Parameters:  
**data** ([*str*](https://docs.python.org/3/library/stdtypes.html#str "(in Python v3.14)")*,*[*Buffer*](#pwnlib.tubes.buffer.Buffer "pwnlib.tubes.buffer.Buffer")) – Data to place at the beginning of the buffer.

Example

    >>> b = Buffer()
    >>> b.add(b"hello")
    >>> b.add(b"world")
    >>> b.get(5)
    b'hello'
    >>> b.unget(b"goodbye")
    >>> b.get()
    b'goodbyeworld'

\_\_weakref\_\_[\[source\]](https://github.com/Gallopsled/pwntools/blob/7e5e79d00f/pwnlib/tubes/buffer.py)  
list of weak references to the object