# Extend a data format: jsonic

- [1 intro](https://beautifulracket.com/jsonic/intro.html)
- [2 specification](https://beautifulracket.com/jsonic/specification.html)
- [3 setup](https://beautifulracket.com/jsonic/setup.html)
- [4 the reader](https://beautifulracket.com/jsonic/the-reader.html)
- [5 the tokenizer](https://beautifulracket.com/jsonic/the-tokenizer.html)
- [6 the parser](https://beautifulracket.com/jsonic/the-parser.html)
- [7 the expander](https://beautifulracket.com/jsonic/the-expander.html)
- [8 testing the language](https://beautifulracket.com/jsonic/testing-the-language.html)
- [9 recap](https://beautifulracket.com/jsonic/recap.html)
- [10 source listing](https://beautifulracket.com/jsonic/source-listing.html)

### [Booting the reader](#booting-the-reader)

When we invoke #lang jsonic, Racket will look for a "main.rkt" module within our jsonic directory. Within that module, it will expect to find a reader submodule that provides our read-syntax function. We’ll use [require](http://docs.racket-lang.org/reference/require.html#(form._((lib._racket/private/base..rkt)._require))) and [provide](http://docs.racket-lang.org/reference/require.html#(form._((lib._racket/private/base..rkt)._provide))) to fetch this function from a "reader.rkt" module that we’ll create next. For now, let’s put a "main.rkt" in the jsonic directory that looks like this:

jsonic/main.rkt

    #lang br/quicklang
    (module reader br
      (require "reader.rkt")
      (provide read-syntax))

### [The reader](#the-reader)

One more time—let’s recap the role of the reader:

-

  The [reader](https://beautifulracket.com/appendix/glossary.html#reader) converts the source code of our language from a string of characters into Racket-style parenthesized forms, also known as [S-expressions](https://beautifulracket.com/appendix/glossary.html#s-expression).

By convention, Racket expects the name of the main reader function to be read-syntax. This read-syntax function must return one value: code for a module expression, packaged as a [syntax object](https://beautifulracket.com/appendix/glossary.html#syntax-object). This syntax object must have no [bindings](https://beautifulracket.com/appendix/glossary.html#binding).

In [bf](https://beautifulracket.com/bf/), we saw that we could make a read-syntax function by relying on a [tokenizer](https://beautifulracket.com/appendix/glossary.html#tokenizer) and a [parser](https://beautifulracket.com/appendix/glossary.html#parser) to do the heavy lifting.

We’ll do the same here. In fact, we’ll just take [the read-syntax from bf](https://beautifulracket.com/bf/source-listing.html) and reuse it here, changing the expander name from bf/expander to jsonic/expander. Our new "reader.rkt" ends up like this:

jsonic/reader.rkt

    #lang br/quicklang
    (require "tokenizer.rkt" "parser.rkt")

    (define (read-syntax path port)
      (define parse-tree (parse path (make-tokenizer port)))
      (define module-datum `(module jsonic-module jsonic/expander
                              ,parse-tree))
      (datum->syntax #f module-datum))
    (provide read-syntax)

By the way, we’re using parse-tree and module-datum as intermediate values for readability. We could also write this function more compactly like so:

jsonic/reader.rkt

    #lang br/quicklang
    (require "tokenizer.rkt" "parser.rkt")

    (define (read-syntax path port)
      (datum->syntax #f `(module jsonic-mod jsonic/expander
                           ,(parse path (make-tokenizer port)))))
    (provide read-syntax)

Because Racket is an expression-based language, it’s usually possible—and therefore tempting—to just keep nesting lines of code inside each other. But it’s not a virtuous habit.

First, it makes the code harder to read—for others, but especially for the future version of ourselves. What seems like cleverly compact notation today will just be an annoying brain-teaser next week.

Second, it doesn’t make the code faster. Before Racket runs a program, it optimizes the code. Most of these simple notational “improvements” will be done automatically by Racket anyhow. So we’re usually better off maximizing the readability of our code.

Our read-syntax function won’t work yet, because it relies on "tokenizer.rkt" and "parser.rkt". We’ll add those next.

- [1 intro](https://beautifulracket.com/jsonic/intro.html)
- [2 specification](https://beautifulracket.com/jsonic/specification.html)
- [3 setup](https://beautifulracket.com/jsonic/setup.html)
- [4 the reader](https://beautifulracket.com/jsonic/the-reader.html)
- [5 the tokenizer](https://beautifulracket.com/jsonic/the-tokenizer.html)
- [6 the parser](https://beautifulracket.com/jsonic/the-parser.html)
- [7 the expander](https://beautifulracket.com/jsonic/the-expander.html)
- [8 testing the language](https://beautifulracket.com/jsonic/testing-the-language.html)
- [9 recap](https://beautifulracket.com/jsonic/recap.html)
- [10 source listing](https://beautifulracket.com/jsonic/source-listing.html)