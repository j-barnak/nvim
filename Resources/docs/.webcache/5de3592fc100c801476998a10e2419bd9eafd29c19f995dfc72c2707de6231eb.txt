[std](https://doc.rust-lang.org/std/index.html)

# Module range Copy item path

1.96.0 · [Source](https://doc.rust-lang.org/src/core/lib.rs.html#313)

Expand description

## Replacement range types

The types within this module are meant to replace the legacy `Range`, `RangeInclusive`, `RangeToInclusive` and `RangeFrom` types in a future edition.

``` rust
use core::range::{Range, RangeFrom, RangeInclusive, RangeToInclusive};

let arr = [0, 1, 2, 3, 4];
assert_eq!(arr[                        ..   ], [0, 1, 2, 3, 4]);
assert_eq!(arr[                        .. 3 ], [0, 1, 2      ]);
assert_eq!(arr[RangeToInclusive::from( ..=3)], [0, 1, 2, 3   ]);
assert_eq!(arr[       RangeFrom::from(1..  )], [   1, 2, 3, 4]);
assert_eq!(arr[           Range::from(1..3 )], [   1, 2      ]);
assert_eq!(arr[  RangeInclusive::from(1..=3)], [   1, 2, 3   ]);
```

## Modules

[legacy](https://doc.rust-lang.org/std/range/legacy/index.html "mod std::range::legacy")  
Legacy range types

## Structs

[Range](https://doc.rust-lang.org/std/range/struct.Range.html "struct std::range::Range")  
A (half-open) range bounded inclusively below and exclusively above.

[RangeFrom](https://doc.rust-lang.org/std/range/struct.RangeFrom.html "struct std::range::RangeFrom")  
A range only bounded inclusively below.

[RangeFromIter](https://doc.rust-lang.org/std/range/struct.RangeFromIter.html "struct std::range::RangeFromIter")  
By-value [`RangeFrom`](https://doc.rust-lang.org/std/range/struct.RangeFrom.html "struct std::range::RangeFrom") iterator.

[RangeFull](https://doc.rust-lang.org/std/range/struct.RangeFull.html "struct std::range::RangeFull")  
An unbounded range (`..`).

[RangeInclusive](https://doc.rust-lang.org/std/range/struct.RangeInclusive.html "struct std::range::RangeInclusive")  
A range bounded inclusively below and above.

[RangeInclusiveIter](https://doc.rust-lang.org/std/range/struct.RangeInclusiveIter.html "struct std::range::RangeInclusiveIter")  
By-value [`RangeInclusive`](https://doc.rust-lang.org/std/range/struct.RangeInclusive.html "struct std::range::RangeInclusive") iterator.

[RangeIter](https://doc.rust-lang.org/std/range/struct.RangeIter.html "struct std::range::RangeIter")  
By-value [`Range`](https://doc.rust-lang.org/std/range/struct.Range.html "struct std::range::Range") iterator.

[RangeTo](https://doc.rust-lang.org/std/range/struct.RangeTo.html "struct std::range::RangeTo")  
A range only bounded exclusively above (`..end`).

[RangeToInclusive](https://doc.rust-lang.org/std/range/struct.RangeToInclusive.html "struct std::range::RangeToInclusive")  
A range only bounded inclusively above.