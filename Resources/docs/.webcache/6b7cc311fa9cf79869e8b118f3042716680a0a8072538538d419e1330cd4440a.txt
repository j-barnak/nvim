[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_with_exposed_provenance Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#690)

``` rust
pub const unsafe fn simd_with_exposed_provenance<T, U>(addr: T) -> U
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Creates a vector of pointers from a vector of addresses.

`T` must be a vector of `usize`.

`U` must be a vector of pointers, with the same length as `T`.