# miasm.arch.ppc.arch

## Module variables

- `LPARENTHESIS = Suppress(Literal('('))`
- `RPARENTHESIS = Suppress(Literal(')'))`

## Classes

### class `ppc_arg(m_arg)`

- **`asm_ast_to_expr(self, arg, loc_db)`**

### class `additional_info(object)`

- **`__init__(self)`**

### class `instruction_ppc(instruction)`

- **`__init__(self, *args, **kargs)`**
- **`arg2str(e, pos=None, loc_db=None)`**
- **`arg2html(e, pos=None, loc_db=None)`**
- **`is_conditional_jump(s)`**
- **`dstflow(self)`**
- **`dstflow2label(self, loc_db)`**
- **`breakflow(self)`**
- **`is_subcall(self)`**
- **`getdstflow(self, loc_db)`**
- **`splitflow(self)`**
- **`get_symbol_size(self, symbol, loc_db)`**
- **`fixDstOffset(self)`**
- **`get_args_expr(self)`**
- **`get_asm_offset(self, x)`**

### class `mn_ppc(cls_mn)`

- **`getpc(cls, attrib=None)`**
- **`getsp(cls, attrib=None)`**
- **`additional_info(self)`**
- **`getbits(cls, bs, attrib, start, n)`**
- **`endian_offset(cls, attrib, offset)`**
- **`check_mnemo(cls, fields)`**
- **`getmn(cls, name)`**
- **`mod_fields(cls, fields)`**
- **`gen_modes(cls, subcls, name, bases, dct, fields)`**
- **`post_dis(self)`**
- **`value(self, mode)`**
- **`get_symbol_size(self, symbol, loc_db, mode)`**

### class `ppc_reg(reg_noarg, ppc_arg)`

### class `ppc_gpreg_noarg(reg_noarg)`

### class `ppc_gpreg_or_0_noarg(reg_noarg)`

- **`decode(self, v)`**

### class `ppc_gpreg(ppc_reg)`

### class `ppc_gpreg_or_0(ppc_reg)`

- **`decode(self, v)`**

### class `ppc_crfreg_noarg(reg_noarg)`

### class `ppc_crfreg(ppc_reg)`

### class `ppc_imm(imm_noarg, ppc_arg)`

### class `ppc_s14imm_branch(ppc_imm)`

- **`decode(self, v)`**
- **`encode(self)`**

### class `ppc_s24imm_branch(ppc_imm)`

- **`decode(self, v)`**
- **`encode(self)`**

### class `ppc_s16imm(ppc_imm)`

- **`decode(self, v)`**
- **`encode(self)`**

### class `ppc_u16imm(ppc_imm)`

- **`decode(self, v)`**
- **`encode(self)`**

### class `ppc_spr(ppc_imm)`

- **`decode(self, v)`**
- **`encode(self, e)`**

### class `ppc_tbr(ppc_imm)`

- **`decode(self, v)`**
- **`encode(self, e)`**

### class `ppc_u08imm(ppc_u16imm)`

### class `ppc_u05imm(ppc_u16imm)`

### class `ppc_u04imm(ppc_u16imm)`

### class `ppc_u02imm_noarg(imm_noarg)`

### class `ppc_float(ppc_reg)`

### class `ppc_vex(ppc_reg)`

### class `ppc_divert_conditional_branch(bs_divert)`

- **`divert(self, i, candidates)`**

### class `ppc_deref32(ppc_arg)`

- **`decode(self, v)`**
- **`encode(self)`**

### class `bs_mod_name_prio4(bs_mod_name)`

### class `bs_mod_name_prio5(bs_mod_name)`

### class `bs_mod_name_prio6(bs_mod_name)`

## Functions

### `cb_deref_imm_reg(tokens)`

### `ppc_swap_10(v)`

### `ppc_bo_bi_to_mnemo(bo, bi, prefer_taken=True, default_taken=True)`

### `ppc_all_bo_bi()`

### `ppcop(name, fields, args=None, alias=False)`
