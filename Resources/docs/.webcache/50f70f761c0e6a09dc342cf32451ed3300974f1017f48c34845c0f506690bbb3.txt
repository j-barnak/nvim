# miasm.jitter.jitcore_gcc

## Classes

### class `JitCore_Gcc(JitCore_Cc_Base)`

JiT management, using a C compiler as backend

- **`__init__(self, lifter, bin_stream)`**
- **`deleteCB(self, offset)`** - Free the state associated to @offset and delete it
- **`load_code(self, label, fname_so)`**
- **`add_block(self, block)`** - Add a block to JiT and JiT it.
- **`gen_C_source(lifter, func_code)`**
