[std](https://doc.rust-lang.org/std/index.html)::[fmt](https://doc.rust-lang.org/std/fmt/index.html)

# Struct DebugList Copy item path

1.2.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#706)

``` rust
pub struct DebugList<'a, 'b>where
    'b: 'a,{ /* private fields */ }
```

Expand description

A struct to help with [`fmt::Debug`](https://doc.rust-lang.org/std/fmt/trait.Debug.html "trait std::fmt::Debug") implementations.

This is useful when you wish to output a formatted list of items as a part of your [`Debug::fmt`](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt "method std::fmt::Debug::fmt") implementation.

This can be constructed by the [`Formatter::debug_list`](https://doc.rust-lang.org/std/fmt/struct.Formatter.html#method.debug_list "method std::fmt::Formatter::debug_list") method.

## Examples

``` rust
use std::fmt;

struct Foo(Vec<i32>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        fmt.debug_list().entries(self.0.iter()).finish()
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![10, 11])),
    "[10, 11]",
);
```

## Implementations

[Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#715)

1.2.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#740)

Adds a new entry to the list output.

##### Examples

``` rust
use std::fmt;

struct Foo(Vec<i32>, Vec<u32>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        fmt.debug_list()
           .entry(&self.0) // We add the first "entry".
           .entry(&self.1) // We add the second "entry".
           .finish()
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![10, 11], vec![12, 13])),
    "[[10, 11], [12, 13]]",
);
```

[Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#750-752)

🔬This is a nightly-only experimental API. (`debug_closure_helpers` [\#117729](https://github.com/rust-lang/rust/issues/117729))

Adds a new entry to the list output.

This method is equivalent to [`DebugList::entry`](https://doc.rust-lang.org/std/fmt/struct.DebugList.html#method.entry "method std::fmt::DebugList::entry"), but formats the entry using a provided closure rather than by calling [`Debug::fmt`](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt "method std::fmt::Debug::fmt").

1.2.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#782-785)

Adds the contents of an iterator of entries to the list output.

##### Examples

``` rust
use std::fmt;

struct Foo(Vec<i32>, Vec<u32>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        fmt.debug_list()
           .entries(self.0.iter())
           .entries(self.1.iter())
           .finish()
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![10, 11], vec![12, 13])),
    "[10, 11, 12, 13]",
);
```

1.83.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#822)

Marks the list as non-exhaustive, indicating to the reader that there are some other elements that are not shown in the debug representation.

##### Examples

``` rust
use std::fmt;

struct Foo(Vec<i32>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        // Print at most two elements, abbreviate the rest
        let mut f = fmt.debug_list();
        let mut f = f.entries(self.0.iter().take(2));
        if self.0.len() > 2 {
            f.finish_non_exhaustive()
        } else {
            f.finish()
        }
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![1, 2, 3, 4])),
    "[1, 2, ..]",
);
```

1.2.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#863)

Finishes output and returns any error encountered.

##### Examples

``` rust
use std::fmt;

struct Foo(Vec<i32>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        fmt.debug_list()
           .entries(self.0.iter())
           .finish() // Ends the list formatting.
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![10, 11])),
    "[10, 11]",
);
```

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.