[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)

# Struct OnceLock Copy item path

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#109-132)

``` rust
pub struct OnceLock<T> { /* private fields */ }
```

Expand description

A synchronization primitive which can nominally be written to only once.

This type is a thread-safe [`OnceCell`](https://doc.rust-lang.org/std/cell/struct.OnceCell.html "struct std::cell::OnceCell"), and can be used in statics. In many simple cases, you can use [`LazyLock<T, F>`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html "struct std::sync::LazyLock") instead to get the benefits of this type with less effort: `LazyLock<T, F>` “looks like” `&T` because it initializes with `F` on deref! Where OnceLock shines is when LazyLock is too simple to support a given case, as LazyLock doesn’t allow additional inputs to its function after you call [`LazyLock::new(|| ...)`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.new "associated function std::sync::LazyLock::new").

A `OnceLock` can be thought of as a safe abstraction over uninitialized data that becomes initialized once written.

Unlike [`Mutex`](https://doc.rust-lang.org/std/sync/struct.Mutex.html "struct std::sync::Mutex"), `OnceLock` is never poisoned on panic.

## Examples

Writing to a `OnceLock` from a separate thread:

``` rust
use std::sync::OnceLock;

static CELL: OnceLock<usize> = OnceLock::new();

// `OnceLock` has not been written to yet.
assert!(CELL.get().is_none());

// Spawn a thread and write to `OnceLock`.
std::thread::spawn(|| {
    let value = CELL.get_or_init(|| 12345);
    assert_eq!(value, &12345);
})
.join()
.unwrap();

// `OnceLock` now contains the value.
assert_eq!(
    CELL.get(),
    Some(&12345),
);
```

You can use `OnceLock` to implement a type that requires “append-only” logic:

``` rust
use std::sync::{OnceLock, atomic::{AtomicU32, Ordering}};
use std::thread;

struct OnceList<T> {
    data: OnceLock<T>,
    next: OnceLock<Box<OnceList<T>>>,
}
impl<T> OnceList<T> {
    const fn new() -> OnceList<T> {
        OnceList { data: OnceLock::new(), next: OnceLock::new() }
    }
    fn push(&self, value: T) {
        // FIXME: this impl is concise, but is also slow for long lists or many threads.
        // as an exercise, consider how you might improve on it while preserving the behavior
        if let Err(value) = self.data.set(value) {
            let next = self.next.get_or_init(|| Box::new(OnceList::new()));
            next.push(value)
        };
    }
    fn contains(&self, example: &T) -> bool
    where
        T: PartialEq,
    {
        self.data.get().map(|item| item == example).filter(|v| *v).unwrap_or_else(|| {
            self.next.get().map(|next| next.contains(example)).unwrap_or(false)
        })
    }
}

// Let's exercise this new Sync append-only list by doing a little counting
static LIST: OnceList<u32> = OnceList::new();
static COUNTER: AtomicU32 = AtomicU32::new(0);

const LEN: u32 = 1000;
thread::scope(|s| {
    for _ in 0..thread::available_parallelism().unwrap().get() {
        s.spawn(|| {
            while let i @ 0..LEN = COUNTER.fetch_add(1, Ordering::Relaxed) {
                LIST.push(i);
            }
        });
    }
});

for i in 0..LEN {
    assert!(LIST.contains(&i));
}
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#134-576)

1.70.0 (const: 1.70.0) · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#140-146)

Creates a new uninitialized cell.

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#155-162)

Gets the reference to the underlying value.

Returns `None` if the cell is uninitialized, or being initialized. This method never blocks.

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#173-180)

Gets the mutable reference to the underlying value.

Returns `None` if the cell is uninitialized.

This method never blocks. Since it borrows the `OnceLock` mutably, it is statically guaranteed that no active borrows to the `OnceLock` exist, including from other threads.

1.86.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#203-207)

Blocks the current thread until the cell is initialized.

##### Example

Waiting for a computation on another thread to finish:

``` rust
use std::thread;
use std::sync::OnceLock;

let value = OnceLock::new();

thread::scope(|s| {
    s.spawn(|| value.set(1 + 1));

    let result = value.wait();
    assert_eq!(result, &2);
})
```

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#238-243)

Initializes the contents of the cell to `value`.

May block if another thread is currently attempting to initialize the cell. The cell is guaranteed to contain a value when `set` returns, though not necessarily the one provided.

Returns `Ok(())` if the cell was uninitialized and `Err(value)` if the cell was already initialized.

##### Examples

``` rust
use std::sync::OnceLock;

static CELL: OnceLock<i32> = OnceLock::new();

fn main() {
    assert!(CELL.get().is_none());

    std::thread::spawn(|| {
        assert_eq!(CELL.set(92), Ok(()));
    }).join().unwrap();

    assert_eq!(CELL.set(62), Err(62));
    assert_eq!(CELL.get(), Some(&92));
}
```

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#278-285)

🔬This is a nightly-only experimental API. (`once_cell_try_insert` [\#116693](https://github.com/rust-lang/rust/issues/116693))

Initializes the contents of the cell to `value` if the cell was uninitialized, then returns a reference to it.

May block if another thread is currently attempting to initialize the cell. The cell is guaranteed to contain a value when `try_insert` returns, though not necessarily the one provided.

Returns `Ok(&value)` if the cell was uninitialized and `Err((&current_value, value))` if it was already initialized.

##### Examples

``` rust
#![feature(once_cell_try_insert)]

use std::sync::OnceLock;

static CELL: OnceLock<i32> = OnceLock::new();

fn main() {
    assert!(CELL.get().is_none());

    std::thread::spawn(|| {
        assert_eq!(CELL.try_insert(92), Ok(&92));
    }).join().unwrap();

    assert_eq!(CELL.try_insert(62), Err((&92, 62)));
    assert_eq!(CELL.get(), Some(&92));
}
```

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#317-324)

Gets the contents of the cell, initializing it to `f()` if the cell was uninitialized.

Many threads may call `get_or_init` concurrently with different initializing functions, but it is guaranteed that only one function will be executed if the function doesn’t panic.

##### Panics

If `f()` panics, the panic is propagated to the caller, and the cell remains uninitialized.

It is an error to reentrantly initialize the cell from `f`. The exact outcome is unspecified. Current implementation deadlocks, but this may be changed to a panic in the future.

##### Examples

``` rust
use std::sync::OnceLock;

let cell = OnceLock::new();
let value = cell.get_or_init(|| 92);
assert_eq!(value, &92);
let value = cell.get_or_init(|| unreachable!());
assert_eq!(value, &92);
```

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#357-364)

🔬This is a nightly-only experimental API. (`once_cell_get_mut` [\#121641](https://github.com/rust-lang/rust/issues/121641))

Gets the mutable reference of the contents of the cell, initializing it to `f()` if the cell was uninitialized.

This method never blocks. Since it borrows the `OnceLock` mutably, it is statically guaranteed that no active borrows to the `OnceLock` exist, including from other threads.

##### Panics

If `f()` panics, the panic is propagated to the caller, and the cell remains uninitialized.

##### Examples

``` rust
#![feature(once_cell_get_mut)]

use std::sync::OnceLock;

let mut cell = OnceLock::new();
let value = cell.get_mut_or_init(|| 92);
assert_eq!(*value, 92);

*value += 2;
assert_eq!(*value, 94);

let value = cell.get_mut_or_init(|| unreachable!());
assert_eq!(*value, 94);
```

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#398-414)

🔬This is a nightly-only experimental API. (`once_cell_try` [\#109737](https://github.com/rust-lang/rust/issues/109737))

Gets the contents of the cell, initializing it to `f()` if the cell was uninitialized. If the cell was uninitialized and `f()` failed, an error is returned.

##### Panics

If `f()` panics, the panic is propagated to the caller, and the cell remains uninitialized.

It is an error to reentrantly initialize the cell from `f`. The exact outcome is unspecified. Current implementation deadlocks, but this may be changed to a panic in the future.

##### Examples

``` rust
#![feature(once_cell_try)]

use std::sync::OnceLock;

let cell = OnceLock::new();
assert_eq!(cell.get_or_try_init(|| Err(())), Err(()));
assert!(cell.get().is_none());
let value = cell.get_or_try_init(|| -> Result<i32, ()> {
    Ok(92)
});
assert_eq!(value, Ok(&92));
assert_eq!(cell.get(), Some(&92))
```

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#449-459)

🔬This is a nightly-only experimental API. (`once_cell_get_mut` [\#121641](https://github.com/rust-lang/rust/issues/121641))

Gets the mutable reference of the contents of the cell, initializing it to `f()` if the cell was uninitialized. If the cell was uninitialized and `f()` failed, an error is returned.

This method never blocks. Since it borrows the `OnceLock` mutably, it is statically guaranteed that no active borrows to the `OnceLock` exist, including from other threads.

##### Panics

If `f()` panics, the panic is propagated to the caller, and the cell remains uninitialized.

##### Examples

``` rust
#![feature(once_cell_get_mut)]

use std::sync::OnceLock;

let mut cell: OnceLock<u32> = OnceLock::new();

// Failed attempts to initialize the cell do not change its contents
assert!(cell.get_mut_or_try_init(|| "not a number!".parse()).is_err());
assert!(cell.get().is_none());

let value = cell.get_mut_or_try_init(|| "1234".parse());
assert_eq!(value, Ok(&mut 1234));
*value.unwrap() += 2;
assert_eq!(cell.get(), Some(&1236))
```

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#478-480)

Consumes the `OnceLock`, returning the wrapped value. Returns `None` if the cell was uninitialized.

##### Examples

``` rust
use std::sync::OnceLock;

let cell: OnceLock<String> = OnceLock::new();
assert_eq!(cell.into_inner(), None);

let cell = OnceLock::new();
cell.set("hello".to_string()).unwrap();
assert_eq!(cell.into_inner(), Some("hello".to_string()));
```

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#504-514)

Takes the value out of this `OnceLock`, moving it back to an uninitialized state.

Has no effect and returns `None` if the `OnceLock` was uninitialized.

Since this method borrows the `OnceLock` mutably, it is statically guaranteed that no active borrows to the `OnceLock` exist, including from other threads.

##### Examples

``` rust
use std::sync::OnceLock;

let mut cell: OnceLock<String> = OnceLock::new();
assert_eq!(cell.take(), None);

let mut cell = OnceLock::new();
cell.set("hello".to_string()).unwrap();
assert_eq!(cell.take(), Some("hello".to_string()));
assert_eq!(cell.get(), None);
```

## Trait Implementations

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#626-631)

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#628-630)

Returns a duplicate of the value. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#tymethod.clone)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/142757 "Tracking issue for const_clone")) · [Source](https://doc.rust-lang.org/src/core/clone.rs.html#245-247)

Performs copy-assignment from `source`. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#method.clone_from)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#614-623)

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#615-622)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.70.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143894 "Tracking issue for const_default")) · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#595-611)

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#608-610)

Creates a new uninitialized cell.

##### Example

``` rust
use std::sync::OnceLock;

fn main() {
    assert_eq!(OnceLock::<()>::new(), OnceLock::default());
}
```

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#692-702)

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#694-701)

Executes the destructor for this type. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop)

[Source](https://doc.rust-lang.org/src/core/ops/drop.rs.html#267)

🔬This is a nightly-only experimental API. (`pin_ergonomics` [\#130494](https://github.com/rust-lang/rust/issues/130494))

Execute the destructor for this type, but different to [`Drop::drop`](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop "method std::ops::Drop::drop"), it requires `self` to be pinned. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.pin_drop)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#689)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#634-658)

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#651-657)

Creates a new cell with its contents set to `value`.

##### Example

``` rust
use std::sync::OnceLock;

let a = OnceLock::from(3);
let b = OnceLock::new();
b.set(3)?;
assert_eq!(a, b);
Ok(())
```

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#661-686)

[Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#683-685)

Equality for two `OnceLock`s.

Two `OnceLock`s are equal if they either both contain values and their values are equal, or if neither contains a value.

##### Examples

``` rust
use std::sync::OnceLock;

let five = OnceLock::new();
five.set(5).unwrap();

let also_five = OnceLock::new();
also_five.set(5).unwrap();

assert!(five == also_five);

assert!(OnceLock::<u32>::new() == OnceLock::<u32>::new());
```

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#263)

Tests for `!=`. The default implementation is almost always sufficient, and should not be overridden without very good reason.

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#589)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#586)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#584)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/sync/once_lock.rs.html#591)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#804)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#805)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.