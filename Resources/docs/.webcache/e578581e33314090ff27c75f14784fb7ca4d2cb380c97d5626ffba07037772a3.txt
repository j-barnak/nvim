[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_arith_offset Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#665)

``` rust
pub const unsafe fn simd_arith_offset<T, U>(ptr: T, offset: U) -> T
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Calculates the offset from a pointer vector elementwise, potentially wrapping.

`T` must be a vector of pointers.

`U` must be a vector of `isize` or `usize` with the same number of elements as `T`.

Operates as if by `<ptr>::wrapping_offset`.