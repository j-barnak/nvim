[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[unix](https://doc.rust-lang.org/std/os/unix/index.html)::[fs](https://doc.rust-lang.org/std/os/unix/fs/index.html)

# Function mkfifo Copy item path

[Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#1222-1224)

``` rust
pub fn mkfifo<P: AsRef<Path>>(path: P, permissions: Permissions) -> Result<()>
```

🔬This is a nightly-only experimental API. (`unix_mkfifo` [\#139324](https://github.com/rust-lang/rust/issues/139324))

Available on **Unix** only.

Expand description

Create a FIFO special file at the specified path with the specified mode.

## Examples

``` rust
mkfifo("/tmp/fifo", Permissions::from_mode(0o774))?;

let mut wx = File::options().read(true).write(true).open("/tmp/fifo")?;
let mut rx = File::open("/tmp/fifo")?;

wx.write_all(b"hello, world!")?;
drop(wx);

let mut s = String::new();
rx.read_to_string(&mut s)?;

assert_eq!(s, "hello, world!");
```