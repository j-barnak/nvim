[std](https://doc.rust-lang.org/std/index.html)::[ops](https://doc.rust-lang.org/std/ops/index.html)

# Trait OneSidedRange Copy item path

[Source](https://doc.rust-lang.org/src/core/ops/range.rs.html#1321)

``` rust
pub trait OneSidedRange<T>: RangeBounds<T> {
    // Required method
    fn bound(self) -> (OneSidedRangeBound, T);
}
```

🔬This is a nightly-only experimental API. (`one_sided_range` [\#69780](https://github.com/rust-lang/rust/issues/69780))

Expand description

`OneSidedRange` is implemented for built-in range types that are unbounded on one side. For example, `a..`, `..b` and `..=c` implement `OneSidedRange`, but `..`, `d..e`, and `f..=g` do not.

Types that implement `OneSidedRange<T>` must return `Bound::Unbounded` from one of `RangeBounds::start_bound` or `RangeBounds::end_bound`.

## Required Methods

[Source](https://doc.rust-lang.org/src/core/ops/range.rs.html#1324)

🔬This is a nightly-only experimental API. (`one_sided_range` [\#69780](https://github.com/rust-lang/rust/issues/69780))

An internal-only helper function for `split_off` and `split_off_mut` that returns the bound of the one-sided range.

## Dyn Compatibility

This trait is **not** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

[Source](https://doc.rust-lang.org/src/core/ops/range.rs.html#1340-1342)

[Source](https://doc.rust-lang.org/src/core/range.rs.html#587-589)

[Source](https://doc.rust-lang.org/src/core/ops/range.rs.html#1329-1331)

[Source](https://doc.rust-lang.org/src/core/ops/range.rs.html#1351-1353)

[Source](https://doc.rust-lang.org/src/core/range.rs.html#748-750)