[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)

# Struct ReentrantLock Copy item path

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#81-86)

``` rust
pub struct ReentrantLock<T: ?Sized> { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`reentrant_lock` [\#121440](https://github.com/rust-lang/rust/issues/121440))

Expand description

A re-entrant mutual exclusion lock

This lock will block *other* threads waiting for the lock to become available. The thread which has already locked the mutex can lock it multiple times without blocking, preventing a common source of deadlocks.

## Examples

Allow recursively calling a function needing synchronization from within a callback (this is how [`StdoutLock`](https://doc.rust-lang.org/std/io/struct.StdoutLock.html "struct std::io::StdoutLock") is currently implemented):

``` rust
#![feature(reentrant_lock)]

use std::cell::RefCell;
use std::sync::ReentrantLock;

pub struct Log {
    data: RefCell<String>,
}

impl Log {
    pub fn append(&self, msg: &str) {
        self.data.borrow_mut().push_str(msg);
    }
}

static LOG: ReentrantLock<Log> = ReentrantLock::new(Log { data: RefCell::new(String::new()) });

pub fn with_log<R>(f: impl FnOnce(&Log) -> R) -> R {
    let log = LOG.lock();
    f(&*log)
}

with_log(|log| {
    log.append("Hello");
    with_log(|log| log.append(" there!"));
});
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#221-256)

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#232-239)

🔬This is a nightly-only experimental API. (`reentrant_lock` [\#121440](https://github.com/rust-lang/rust/issues/121440))

Creates a new re-entrant lock in an unlocked state ready for use.

##### Examples

``` rust
#![feature(reentrant_lock)]
use std::sync::ReentrantLock;

let lock = ReentrantLock::new(0);
```

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#253-255)

🔬This is a nightly-only experimental API. (`reentrant_lock` [\#121440](https://github.com/rust-lang/rust/issues/121440))

Consumes this lock, returning the underlying data.

##### Examples

``` rust
#![feature(reentrant_lock)]

use std::sync::ReentrantLock;

let lock = ReentrantLock::new(0);
assert_eq!(lock.into_inner(), 0);
```

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#259-368)

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#284-300)

🔬This is a nightly-only experimental API. (`reentrant_lock` [\#121440](https://github.com/rust-lang/rust/issues/121440))

Acquires the lock, blocking the current thread until it is able to do so.

This function will block the caller until it is available to acquire the lock. Upon returning, the thread is the only thread with the lock held. When the thread calling this method already holds the lock, the call succeeds without blocking.

##### Examples

``` rust
#![feature(reentrant_lock)]
use std::cell::Cell;
use std::sync::{Arc, ReentrantLock};
use std::thread;

let lock = Arc::new(ReentrantLock::new(Cell::new(0)));
let c_lock = Arc::clone(&lock);

thread::spawn(move || {
    c_lock.lock().set(10);
}).join().expect("thread::spawn failed");
assert_eq!(lock.lock().get(), 10);
```

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#318-320)

🔬This is a nightly-only experimental API. (`reentrant_lock` [\#121440](https://github.com/rust-lang/rust/issues/121440))

Returns a mutable reference to the underlying data.

Since this call borrows the `ReentrantLock` mutably, no actual locking needs to take place – the mutable borrow statically guarantees no locks exist.

##### Examples

``` rust
#![feature(reentrant_lock)]
use std::sync::ReentrantLock;

let mut lock = ReentrantLock::new(0);
*lock.get_mut() = 10;
assert_eq!(*lock.lock(), 10);
```

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#358-360)

🔬This is a nightly-only experimental API. (`reentrant_lock_data_ptr` [\#140368](https://github.com/rust-lang/rust/issues/140368))

Returns a raw pointer to the underlying data.

The returned pointer is always non-null and properly aligned, but it is the user’s responsibility to ensure that any reads through it are properly synchronized to avoid data races, and that it is not read through after the lock is dropped.

## Trait Implementations

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#371-380)

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#372-379)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#383-387)

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#384-386)

Returns the “default value” for a type. [Read more](https://doc.rust-lang.org/std/default/trait.Default.html#tymethod.default)

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#390-394)

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#391-393)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#190)

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#182)

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#184)

[Source](https://doc.rust-lang.org/src/std/sync/reentrant_lock.rs.html#188)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#804)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#805)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.