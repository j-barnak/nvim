[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)

# Function rotate_left Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/mod.rs.html#2033)

``` rust
pub const fn rotate_left<T>(x: T, shift: u32) -> Twhere
    T: FunnelShift,
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Performs rotate left.

Note that, unlike most intrinsics, this is safe to call; it does not require an `unsafe` block. Therefore, implementations must not require the user to uphold any safety invariants.

The stabilized versions of this intrinsic are available on the integer primitives via the `rotate_left` method. For example, [`u32::rotate_left`](https://doc.rust-lang.org/std/primitive.u32.html#method.rotate_left "method u32::rotate_left")