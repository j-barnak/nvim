[std](https://doc.rust-lang.org/std/index.html)::[mem](https://doc.rust-lang.org/std/mem/index.html)

# Struct MaybeDangling Copy item path

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#73)

``` rust
pub struct MaybeDangling<P>(/* private fields */)
where
    P: ?Sized;
```

🔬This is a nightly-only experimental API. (`maybe_dangling` [\#118166](https://github.com/rust-lang/rust/issues/118166))

Expand description

Allows wrapped [references](https://doc.rust-lang.org/std/primitive.reference.html "primitive reference") and [boxes](https://doc.rust-lang.org/std/boxed/struct.Box.html) to dangle.

That is, if a reference (or a `Box`) is wrapped in `MaybeDangling` (including when in a (nested) field of a compound type wrapped in `MaybeDangling`), it does not have to follow pointer aliasing rules or be dereferenceable.

This can be useful when the value can become dangling while the function holding it is still executing (particularly in concurrent code). As a somewhat absurd example, consider this code:

``` rust
let mut boxed = Box::new(0_u32);
let ptr = Box::as_mut_ptr(&mut boxed);

// Safety: the pointer comes from a box and thus was allocated before; `box` is not used afterwards
unsafe { dealloc(ptr.cast(), Layout::new::<u32>()) };

mem::forget(boxed); // <-- this is UB!
```

Even though the `Box`’s destructor is not run (and thus we don’t have a double free bug), this code is still UB. This is because when moving `boxed` into `forget`, its validity invariants are asserted, causing UB since the `Box` is dangling. The safety comment is as such wrong, as moving the `boxed` variable as part of the `forget` call *is* a use.

To fix this we could use `MaybeDangling`:

``` rust
#![feature(maybe_dangling)]

let mut boxed = MaybeDangling::new(Box::new(0_u32));
let ptr = Box::as_mut_ptr(boxed.as_mut());

// Safety: the pointer comes from a box and thus was allocated before; `box` is not used afterwards
unsafe { dealloc(ptr.cast(), Layout::new::<u32>()) };

mem::forget(boxed); // <-- this is OK!
```

Note that the bit pattern must still be valid for the wrapped type. That is, [references](https://doc.rust-lang.org/std/primitive.reference.html "primitive reference") (and [boxes](https://doc.rust-lang.org/std/boxed/struct.Box.html)) still must be aligned and non-null.

Additionally note that safe code can still assume that the inner value in a `MaybeDangling` is **not** dangling – functions like [`as_ref`](https://doc.rust-lang.org/std/mem/struct.MaybeDangling.html#method.as_ref "method std::mem::MaybeDangling::as_ref") and [`into_inner`](https://doc.rust-lang.org/std/mem/struct.MaybeDangling.html#method.into_inner "method std::mem::MaybeDangling::into_inner") are safe. It is not sound to return a dangling reference in a `MaybeDangling` to safe code. However, it *is* sound to hold such values internally inside your code – and there’s no way to do that without this type. Note that other types can use this type and thus get the same effect; in particular, [`ManuallyDrop`](https://doc.rust-lang.org/std/mem/struct.ManuallyDrop.html "struct std::mem::ManuallyDrop") will use `MaybeDangling`.

Note that `MaybeDangling` doesn’t prevent drops from being run, which can lead to UB if the drop observes a dangling value. If you need to prevent drops from being run use [`ManuallyDrop`](https://doc.rust-lang.org/std/mem/struct.ManuallyDrop.html "struct std::mem::ManuallyDrop") instead.

## Implementations

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#75)

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#77-79)

🔬This is a nightly-only experimental API. (`maybe_dangling` [\#118166](https://github.com/rust-lang/rust/issues/118166))

Wraps a value in a `MaybeDangling`, allowing it to dangle.

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#87)

🔬This is a nightly-only experimental API. (`maybe_dangling` [\#118166](https://github.com/rust-lang/rust/issues/118166))

Returns a reference to the inner value.

Note that this is UB if the inner value is currently dangling.

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#94)

🔬This is a nightly-only experimental API. (`maybe_dangling` [\#118166](https://github.com/rust-lang/rust/issues/118166))

Returns a mutable reference to the inner value.

Note that this is UB if the inner value is currently dangling.

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#101-103)

🔬This is a nightly-only experimental API. (`maybe_dangling` [\#118166](https://github.com/rust-lang/rust/issues/118166))

Extracts the value from the `MaybeDangling` container.

Note that this is UB if the inner value is currently dangling.

## Trait Implementations

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#71)

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#71)

Returns a duplicate of the value. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#tymethod.clone)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/142757 "Tracking issue for const_clone")) · [Source](https://doc.rust-lang.org/src/core/clone.rs.html#245-247)

Performs copy-assignment from `source`. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#method.clone_from)

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#71)

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#71)

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#71)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#71)

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#71)

Returns the “default value” for a type. [Read more](https://doc.rust-lang.org/std/default/trait.Default.html#tymethod.default)

[Source](https://doc.rust-lang.org/src/core/mem/maybe_dangling.rs.html#113)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.