# miasm.ir.symbexec

## Module variables

- `INTERNAL_INTBASE_NAME = '__INTERNAL_INTBASE__'`

## Classes

### class `StateEngine(object)`

Stores an Engine state

- **`merge(self, other)`** - Generate a new state, representing the merge of self and @other

### class `SymbolicState(StateEngine)`

Stores a SymbolicExecutionEngine state

- **`__init__(self, dct)`**
- **`__hash__(self)`**
- **`__eq__(self, other)`**
- **`__ne__(self, other)`**
- **`__iter__(self)`**
- **`iteritems(self)`** - Iterate on stored memory/values
- **`merge(self, other)`** - Merge two symbolic states
- **`symbols(self)`** - Return the dictionary of known symbols

### class `MemArray(MutableMapping)`

Link between base and its (offset, Expr)

Given an expression (say *base*), this structure will store every memory
content relatively to an integer offset from *base*.

The value associated to a given offset is a description of the slice of a
stored expression. The slice size depends on the configuration of the
MemArray. For example, for a slice size of 8 bits, the assignment:
- @32[EAX+0x10] = EBX

will store for the base EAX:
- 0x10: (EBX, 0)
- 0x11: (EBX, 1)
- 0x12: (EBX, 2)
- 0x13: (EBX, 3)

If the *base* is EAX+EBX, this structure can store the following contents:
- @32[EAX+EBX]
- @8[EAX+EBX+0x100]
But not:
- @32[EAX+0x10] (which is stored in another MemArray based on EAX)
- @32[EAX+EBX+ECX] (which is stored in another MemArray based on
  EAX+EBX+ECX)

- **`__init__(self, base, expr_simp=expr_simp_explicit)`**
- **`base(self)`** - Expression representing the symbolic base address
- **`mask(self)`** - Mask offset
- **`__contains__(self, offset)`**
- **`__getitem__(self, offset)`**
- **`__setitem__(self, offset, value)`**
- **`__delitem__(self, offset)`**
- **`__iter__(self)`**
- **`__len__(self)`**
- **`__repr__(self)`**
- **`copy(self)`** - Copy object instance
- **`offset_to_ptr(base, offset)`** - Return an expression representing the @base + @offset
- **`read(self, offset, size)`** - Return memory at @offset with @size as an Expr list
- **`write(self, offset, expr)`** - Write @expr at @offset
- **`_get_variable_parts(self, index, known_offsets, forward=True)`** - Find consecutive memory parts representing the same variable. The part
- **`_build_value_at_offset(self, value, offset, start, length)`** - Return a couple. The first element is the memory Expression representing
- **`memory(self)`** - Iterate on stored memory/values
- **`dump(self)`** - Display MemArray content

### class `MemSparse(object)`

Link a symbolic memory pointer to its MemArray.

For each symbolic memory object, this object will extract the memory pointer
*ptr*. It then splits *ptr* into a symbolic and an integer part. For
example, the memory @[ESP+4] will give ESP+4 for *ptr*. *ptr* is then split
into its base ESP and its offset 4. Each symbolic base address uses a
different MemArray.

Example:
- @32[EAX+EBX]
- @8[EAX+EBX+0x100]
Will be stored in the same MemArray with a EAX+EBX base

- **`__init__(self, addrsize, expr_simp=expr_simp_explicit)`** - @addrsize: size (in bits) of the addresses manipulated by the MemSparse
- **`__contains__(self, expr)`** - Return True if the whole @expr is present
- **`contains_partial(self, expr)`** - Return True if a part of @expr is present in memory
- **`clear(self)`** - Reset the current object content
- **`copy(self)`** - Copy the current object instance
- **`__delitem__(self, expr)`** - Delete a value @expr *fully* present in memory
- **`delete_partial(self, expr)`** - Delete @expr from memory. Skip parts of @expr which are not present in
- **`read(self, ptr, size)`** - Return the value associated with the Expr at address @ptr
- **`write(self, ptr, expr)`** - Update the corresponding Expr @expr at address @ptr
- **`iteritems(self)`** - Iterate on stored memory variables and their values.
- **`items(self)`** - Return stored memory variables and their values.
- **`dump(self)`** - Display MemSparse content
- **`__repr__(self)`**

### class `SymbolMngr(object)`

Symbolic store manager (IDs and MEMs)

- **`__init__(self, init=None, addrsize=None, expr_simp=expr_simp_explicit)`**
- **`__contains__(self, expr)`**
- **`__getitem__(self, expr)`**
- **`__setitem__(self, expr, value)`**
- **`__delitem__(self, expr)`**
- **`copy(self)`** - Copy object instance
- **`clear(self)`** - Forget every variables values
- **`read(self, src)`** - Return the value corresponding to Expr @src
- **`write(self, dst, src)`** - Update @dst with @src expression
- **`dump(self, ids=True, mems=True)`** - Display memory content
- **`__repr__(self)`**
- **`iteritems(self)`** - ExprId/ExprMem iteritems of the current state
- **`items(self)`** - Return variables/values of the current state
- **`__iter__(self)`**
- **`ids(self)`** - Iterate on variables and their values.
- **`memory(self)`** - Iterate on memory variables and their values.
- **`keys(self)`** - Variables of the current state

### class `SymbolicExecutionEngine(object)`

Symbolic execution engine
Allow IR code emulation in symbolic domain


Examples:
    from miasm.ir.symbexec import SymbolicExecutionEngine
    from miasm.ir.ir import AssignBlock

    lifter = Lifter_X86_32()

    init_state = {
        lifter.arch.regs.EAX: lifter.arch.regs.EBX,
        ExprMem(id_x+ExprInt(0x10, 32), 32): id_a,
    }

    sb_exec = SymbolicExecutionEngine(lifter, init_state)

    >>> sb_exec.dump()
    EAX                = a
    @32[x + 0x10]      = a
    >>> sb_exec.dump(mems=False)
    EAX                = a

    >>> print sb_exec.eval_expr(lifter.arch.regs.EAX + lifter.arch.regs.ECX)
    EBX + ECX

Inspecting state:
    - dump
    - modified
State manipulation:
    - '.state' (rw)

Evaluation (read only):
    - eval_expr
    - eval_assignblk
Evaluation with state update:
    - eval_updt_expr
    - eval_updt_assignblk
    - eval_updt_irblock

Start a symbolic execution based on provisioned '.lifter' blocks:
    - run_block_at
    - run_at

- **`__init__(self, lifter, state=None, sb_expr_simp=expr_simp_explicit)`**
- **`ir_arch(self)`**
- **`get_state(self)`** - Return the current state of the SymbolicEngine
- **`set_state(self, state)`** - Restaure the @state of the engine
- **`eval_expr_visitor(self, expr, cache=None)`** - [DEV]: Override to change the behavior of an Expr evaluation.
- **`eval_exprint(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprInt using the current state
- **`eval_exprid(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprId using the current state
- **`eval_exprloc(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprLoc using the current state
- **`eval_exprmem(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprMem using the current state
- **`eval_exprcond(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprCond using the current state
- **`eval_exprslice(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprSlice using the current state
- **`eval_exprop(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprOp using the current state
- **`eval_exprcompose(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprCompose using the current state
- **`eval_expr(self, expr, eval_cache=None)`** - Evaluate @expr
- **`modified(self, init_state=None, ids=True, mems=True)`** - Return the modified variables.
- **`dump(self, ids=True, mems=True)`** - Display modififed variables
- **`eval_assignblk(self, assignblk)`** - Evaluate AssignBlock using the current state
- **`apply_change(self, dst, src)`** - Apply @dst = @src on the current state WITHOUT evaluating both side
- **`eval_updt_assignblk(self, assignblk)`** - Apply an AssignBlock on the current state
- **`eval_updt_irblock(self, irb, step=False)`** - Symbolic execution of the @irb on the current state
- **`run_block_at(self, ircfg, addr, step=False)`** - Symbolic execution of the block at @addr
- **`run_at(self, ircfg, addr, lbl_stop=None, step=False)`** - Symbolic execution starting at @addr
- **`del_mem_above_stack(self, stack_ptr)`** - Remove all stored memory values with following properties:
- **`eval_updt_expr(self, expr)`** - Evaluate @expr and apply side effect if needed (ie. if expr is an
- **`mem_read(self, expr)`** - [DEV]: Override to modify the effective memory reads
- **`mem_write(self, dst, src)`** - [DEV]: Override to modify the effective memory writes

## Functions

### `get_block(lifter, ircfg, mdis, addr)`

Get IRBlock at address @addr

### `get_expr_base_offset(expr)`

Return a couple representing the symbolic/concrete part of an addition
expression.

If there is no symbolic part, ExprId(INTERNAL_INTBASE_NAME) is used
If there is not concrete part, 0 is used
@expr: Expression instance

### `merge_ptr_read(known, ptrs)`

Merge common memory parts in a multiple byte memory.
@ptrs: memory bytes list
@known: ptrs' associated boolean for present/unpresent memory part in the
store
