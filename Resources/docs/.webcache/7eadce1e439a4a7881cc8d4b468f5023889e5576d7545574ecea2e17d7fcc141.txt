# miasm.core.graph

## Classes

### class `DiGraph(object)`

Implementation of directed graph

- **`__init__(self)`**
- **`__repr__(self)`**
- **`nodes(self)`**
- **`edges(self)`**
- **`merge(self, graph)`** - Merge the current graph with @graph
- **`__add__(self, graph)`** - Wrapper on `.merge`
- **`copy(self)`** - Copy the current graph instance
- **`__eq__(self, graph)`**
- **`__ne__(self, other)`**
- **`add_node(self, node)`** - Add the node @node to the graph.
- **`del_node(self, node)`** - Delete the @node of the graph; Also delete every edge to/from this
- **`add_edge(self, src, dst)`**
- **`add_uniq_edge(self, src, dst)`** - Add an edge from @src to @dst if it doesn't already exist
- **`del_edge(self, src, dst)`**
- **`discard_edge(self, src, dst)`** - Remove edge between @src and @dst if it exits
- **`predecessors_iter(self, node)`**
- **`predecessors(self, node)`**
- **`successors_iter(self, node)`**
- **`successors(self, node)`**
- **`leaves_iter(self)`**
- **`leaves(self)`**
- **`heads_iter(self)`**
- **`heads(self)`**
- **`find_path(self, src, dst, cycles_count=0, done=None)`** - Searches for paths from @src to @dst
- **`find_path_from_src(self, src, dst, cycles_count=0, done=None)`** - This function does the same as function find_path.
- **`nodeid(self, node)`** - Returns uniq id for a @node
- **`node2lines(self, node)`** - Returns an iterator on cells of the dot @node.
- **`node_attr(self, node)`** - Returns a dictionary of the @node's attributes
- **`edge_attr(self, src, dst)`** - Return a dictionary of attributes for the edge between @src and @dst
- **`_fix_chars(token)`**
- **`_attr2str(default_attr, attr)`**
- **`escape_text(self, text)`**
- **`dot(self)`** - Render dot graph with HTML
- **`graphviz(self)`**
- **`_reachable_nodes(head, next_cb)`** - Generic algorithm to compute all nodes reachable from/to node
- **`predecessors_stop_node_iter(self, node, head)`**
- **`reachable_sons(self, head)`** - Compute all nodes reachable from node @head. Each son is an
- **`reachable_parents(self, leaf)`** - Compute all parents of node @leaf. Each parent is an immediate
- **`reachable_parents_stop_node(self, leaf, head)`** - Compute all parents of node @leaf. Each parent is an immediate
- **`_compute_generic_dominators(head, reachable_cb, prev_cb, next_cb)`** - Generic algorithm to compute either the dominators or postdominators
- **`compute_dominators(self, head)`** - Compute the dominators of the graph
- **`compute_postdominators(self, leaf)`** - Compute the postdominators of the graph
- **`compute_dominator_tree(self, head)`** - Computes the dominator tree of a graph
- **`_walk_generic_dominator(node, gen_dominators, succ_cb)`** - Generic algorithm to return an iterator of the ordered list of
- **`walk_dominators(self, node, dominators)`** - Return an iterator of the ordered list of @node's dominators
- **`walk_postdominators(self, node, postdominators)`** - Return an iterator of the ordered list of @node's postdominators
- **`compute_immediate_dominators(self, head)`** - Compute the immediate dominators of the graph
- **`compute_immediate_postdominators(self, tail)`** - Compute the immediate postdominators of the graph
- **`compute_dominance_frontier(self, head)`** - Compute the dominance frontier of the graph
- **`_walk_generic_first(self, head, flag, succ_cb)`** - Generic algorithm to compute breadth or depth first search
- **`walk_breadth_first_forward(self, head)`** - Performs a breadth first search on the graph from @head
- **`walk_depth_first_forward(self, head)`** - Performs a depth first search on the graph from @head
- **`walk_breadth_first_backward(self, head)`** - Performs a breadth first search on the reversed graph from @head
- **`walk_depth_first_backward(self, head)`** - Performs a depth first search on the reversed graph from @head
- **`has_loop(self)`** - Return True if the graph contains at least a cycle
- **`compute_natural_loops(self, head)`** - Computes all natural loops in the graph.
- **`compute_back_edges(self, head)`** - Computes all back edges from a node to a
- **`_compute_natural_loop_body(self, head, leaf)`** - Computes the body of a natural loop by a depth-first
- **`compute_strongly_connected_components(self)`** - Partitions the graph into strongly connected components.
- **`compute_weakly_connected_components(self)`** - Return the weakly connected components
- **`replace_node(self, node, new_node)`** - Replace @node by @new_node

### class `DiGraphSimplifier(object)`

Wrapper on graph simplification passes.

Instance handle passes lists.

- **`__init__(self)`**
- **`enable_passes(self, passes)`** - Add @passes to passes to applied
- **`apply_simp(self, graph)`** - Apply enabled simplifications on graph @graph
- **`__call__(self, graph)`** - Wrapper on 'apply_simp'

### class `MatchGraphJoker(object)`

MatchGraphJoker are joker nodes of MatchGraph, that is to say nodes which
stand for any node. Restrictions can be added to jokers.

If j1, j2 and j3 are MatchGraphJoker, one can quickly build a matcher for
the pattern:
                                     |
                                +----v----+
                                |  (j1)   |
                                +----+----+
                                     |
                                +----v----+
                                |  (j2)   |<---+
                                +----+--+-+    |
                                     |  +------+
                                +----v----+
                                |  (j3)   |
                                +----+----+
                                     |
                                     v
Using:
>>> matcher = j1 >> j2 >> j3
>>> matcher += j2 >> j2
Or:
>>> matcher = j1 >> j2 >> j2 >> j3

- **`__init__(self, restrict_in=True, restrict_out=True, filt=None, name=None)`** - Instantiate a MatchGraphJoker, with restrictions
- **`__rshift__(self, joker)`** - Helper for describing a MatchGraph from @joker
- **`__str__(self)`**

### class `MatchGraph(DiGraph)`

MatchGraph intends to be the counterpart of match_expr, but for DiGraph

This class provides API to match a given DiGraph pattern, with addidionnal
restrictions.
The implemented algorithm is a naive approach.

The recommended way to instantiate a MatchGraph is the use of
MatchGraphJoker.

- **`__init__(self, *args, **kwargs)`**
- **`__rshift__(self, joker)`** - Construction helper, adding @joker to the current graph as a son of
- **`__add__(self, graph)`** - Construction helper, merging @graph with self
- **`_check_node(self, candidate, expected, graph, partial_sol=None)`** - Check if @candidate can stand for @expected in @graph, given @partial_sol
- **`_propagate_sol(self, node, partial_sol, graph, todo, propagator)`** - Try to extend the current @partial_sol by propagating the solution using
- **`_propagate_successors(graph, node)`** - Propagate through @node successors in @graph
- **`_propagate_predecessors(graph, node)`** - Propagate through @node predecessors in @graph
- **`match(self, graph)`** - Naive subgraph matching between graph and self.
