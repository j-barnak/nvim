[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)

# Function fadd_fast Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/mod.rs.html#1578)

``` rust
pub unsafe fn fadd_fast<T>(a: T, b: T) -> Twhere
    T: FloatPrimitive,
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Float addition that allows optimizations based on algebraic rules. Requires that inputs and output of the operation are finite, causing UB otherwise.

This intrinsic does not have a stable counterpart.