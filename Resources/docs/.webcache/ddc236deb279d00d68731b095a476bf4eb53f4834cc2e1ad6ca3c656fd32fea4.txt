[std](https://doc.rust-lang.org/std/index.html)::[mem](https://doc.rust-lang.org/std/mem/index.html)

# Struct Assume Copy item path

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#151)

``` rust
pub struct Assume {
    pub alignment: bool,
    pub lifetimes: bool,
    pub safety: bool,
    pub validity: bool,
}
```

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

Expand description

Configurable proof assumptions of [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom").

When `false`, the respective proof obligation belongs to the compiler. When `true`, the onus of the safety proof belongs to the programmer.

## Fields

`alignment: `[`bool`](https://doc.rust-lang.org/std/primitive.bool.html)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

When `false`, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") is not implemented for transmutations that might violate the alignment requirements of references; e.g.:

[ⓘ](# "This example deliberately fails to compile")

``` rust
#![feature(transmutability)]
use core::mem::TransmuteFrom;

assert_eq!(align_of::<[u8; 2]>(), 1);
assert_eq!(align_of::<u16>(), 2);

let src: &[u8; 2] = &[0xFF, 0xFF];

// SAFETY: No safety obligations.
let dst: &u16 = unsafe {
    <_ as TransmuteFrom<_>>::transmute(src)
};
```

When `true`, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") assumes that *you* have ensured that references in the transmuted value satisfy the alignment requirements of their referent types; e.g.:

``` rust
#![feature(pointer_is_aligned_to, transmutability)]
use core::mem::{Assume, TransmuteFrom};

let src: &[u8; 2] = &[0xFF, 0xFF];

let maybe_dst: Option<&u16> = if <*const _>::is_aligned_to(src, align_of::<u16>()) {
    // SAFETY: We have checked above that the address of `src` satisfies the
    // alignment requirements of `u16`.
    Some(unsafe {
        <_ as TransmuteFrom<_, { Assume::ALIGNMENT }>>::transmute(src)
    })
} else {
    None
};

assert!(matches!(maybe_dst, Some(&u16::MAX) | None));
```

`lifetimes: `[`bool`](https://doc.rust-lang.org/std/primitive.bool.html)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

When `false`, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") is not implemented for transmutations that extend the lifetimes of references.

When `true`, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") assumes that *you* have ensured that references in the transmuted value do not outlive their referents.

`safety: `[`bool`](https://doc.rust-lang.org/std/primitive.bool.html)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

When `false`, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") is not implemented for transmutations that might violate the library safety invariants of the destination type; e.g.:

[ⓘ](# "This example deliberately fails to compile")

``` rust
#![feature(transmutability)]
use core::mem::TransmuteFrom;

let src: u8 = 3;

struct EvenU8 {
    // SAFETY: `val` must be an even number.
    val: u8,
}

// SAFETY: No safety obligations.
let dst: EvenU8 = unsafe {
    <_ as TransmuteFrom<_>>::transmute(src)
};
```

When `true`, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") assumes that *you* have ensured that undefined behavior does not arise from using the transmuted value; e.g.:

``` rust
#![feature(transmutability)]
use core::mem::{Assume, TransmuteFrom};

let src: u8 = 42;

struct EvenU8 {
    // SAFETY: `val` must be an even number.
    val: u8,
}

let maybe_dst: Option<EvenU8> = if src % 2 == 0 {
    // SAFETY: We have checked above that the value of `src` is even.
    Some(unsafe {
        <_ as TransmuteFrom<_, { Assume::SAFETY }>>::transmute(src)
    })
} else {
    None
};

assert!(matches!(maybe_dst, Some(EvenU8 { val: 42 })));
```

`validity: `[`bool`](https://doc.rust-lang.org/std/primitive.bool.html)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

When `false`, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") is not implemented for transmutations that might violate the language-level bit-validity invariant of the destination type; e.g.:

[ⓘ](# "This example deliberately fails to compile")

``` rust
#![feature(transmutability)]
use core::mem::TransmuteFrom;

let src: u8 = 3;

// SAFETY: No safety obligations.
let dst: bool = unsafe {
    <_ as TransmuteFrom<_>>::transmute(src)
};
```

When `true`, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") assumes that *you* have ensured that the value being transmuted is a bit-valid instance of the transmuted value; e.g.:

``` rust
#![feature(transmutability)]
use core::mem::{Assume, TransmuteFrom};

let src: u8 = 1;

let maybe_dst: Option<bool> = if src == 0 || src == 1 {
    // SAFETY: We have checked above that the value of `src` is a bit-valid
    // instance of `bool`.
    Some(unsafe {
        <_ as TransmuteFrom<_, { Assume::VALIDITY }>>::transmute(src)
    })
} else {
    None
};

assert_eq!(maybe_dst, Some(true));
```

## Implementations

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#295)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#300)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

With this, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") does not assume you have ensured any safety obligations are met, and relies only upon its own analysis to (dis)prove transmutability.

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#307)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

With this, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") assumes only that you have ensured that references in the transmuted value satisfy the alignment requirements of their referent types. See [`Assume::alignment`](https://doc.rust-lang.org/std/mem/struct.Assume.html#structfield.alignment "field std::mem::Assume::alignment") for examples.

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#313)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

With this, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") assumes only that you have ensured that references in the transmuted value do not outlive their referents. See [`Assume::lifetimes`](https://doc.rust-lang.org/std/mem/struct.Assume.html#structfield.lifetimes "field std::mem::Assume::lifetimes") for examples.

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#319)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

With this, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") assumes only that you have ensured that undefined behavior does not arise from using the transmuted value. See [`Assume::safety`](https://doc.rust-lang.org/std/mem/struct.Assume.html#structfield.safety "field std::mem::Assume::safety") for examples.

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#325)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

With this, [`TransmuteFrom`](https://doc.rust-lang.org/std/mem/trait.TransmuteFrom.html "trait std::mem::TransmuteFrom") assumes only that you have ensured that the value being transmuted is a bit-valid instance of the transmuted value. See [`Assume::validity`](https://doc.rust-lang.org/std/mem/struct.Assume.html#structfield.validity "field std::mem::Assume::validity") for examples.

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#375)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

Combine the assumptions of `self` and `other_assumptions`.

This is especially useful for extending [`Assume`](https://doc.rust-lang.org/std/mem/struct.Assume.html "struct std::mem::Assume") in generic contexts; e.g.:

``` rust
#![feature(
    adt_const_params,
    generic_const_exprs,
    pointer_is_aligned_to,
    transmutability,
)]
#![allow(incomplete_features)]
use core::mem::{Assume, TransmuteFrom};

/// Attempts to transmute `src` to `&Dst`.
///
/// Returns `None` if `src` violates the alignment requirements of `&Dst`.
///
/// # Safety
///
/// The caller guarantees that the obligations required by `ASSUME`, except
/// alignment, are satisfied.
unsafe fn try_transmute_ref<'a, Src, Dst, const ASSUME: Assume>(src: &'a Src) -> Option<&'a Dst>
where
    &'a Dst: TransmuteFrom<&'a Src, { ASSUME.and(Assume::ALIGNMENT) }>,
{
    if <*const _>::is_aligned_to(src, align_of::<Dst>()) {
        // SAFETY: By the above dynamic check, we have ensured that the address
        // of `src` satisfies the alignment requirements of `&Dst`. By contract
        // on the caller, the safety obligations required by `ASSUME` have also
        // been satisfied.
        Some(unsafe {
            <_ as TransmuteFrom<_, { ASSUME.and(Assume::ALIGNMENT) }>>::transmute(src)
        })
    } else {
        None
    }
}

let src: &[u8; 2] = &[0xFF, 0xFF];

// SAFETY: No safety obligations.
let maybe_dst: Option<&u16> = unsafe {
    try_transmute_ref::<_, _, { Assume::NOTHING }>(src)
};
```

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#399)

🔬This is a nightly-only experimental API. (`transmutability` [\#99571](https://github.com/rust-lang/rust/issues/99571))

Remove `other_assumptions` the obligations of `self`; e.g.:

``` rust
#![feature(transmutability)]
use core::mem::Assume;

let assumptions = Assume::ALIGNMENT.and(Assume::SAFETY);
let to_be_removed = Assume::SAFETY.and(Assume::VALIDITY);

assert_eq!(
    assumptions.but_not(to_be_removed),
    Assume::ALIGNMENT,
);
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#412)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#413)

The resulting type after applying the `+` operator.

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#415)

Performs the `+` operation. [Read more](https://doc.rust-lang.org/std/ops/trait.Add.html#tymethod.add)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#150)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#150)

Returns a duplicate of the value. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#tymethod.clone)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/142757 "Tracking issue for const_clone")) · [Source](https://doc.rust-lang.org/src/core/clone.rs.html#245-247)

Performs copy-assignment from `source`. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#method.clone_from)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#293)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#150)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#150)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#150)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#150)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#150)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#150)

Tests for `self` and `other` values to be equal, and is used by `==`.

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#263)

Tests for `!=`. The default implementation is almost always sufficient, and should not be overridden without very good reason.

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#150)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#423)

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#424)

The resulting type after applying the `-` operator.

[Source](https://doc.rust-lang.org/src/core/mem/transmutability.rs.html#426)

Performs the `-` operation. [Read more](https://doc.rust-lang.org/std/ops/trait.Sub.html#tymethod.sub)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.