[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_carryless_mul Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#180)

``` rust
pub unsafe fn simd_carryless_mul<T>(a: T, b: T) -> T
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Compute the carry-less product.

This is similar to long multiplication except that the carry is discarded.

This operation can be used to model multiplication in `GF(2)[X]`, the polynomial ring over `GF(2)`.

`T` must be a vector of integers.