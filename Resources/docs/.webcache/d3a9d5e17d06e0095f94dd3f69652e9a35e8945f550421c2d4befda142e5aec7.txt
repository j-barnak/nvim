# Follow the grammar: bf

- [1 intro](https://beautifulracket.com/bf/intro.html)
- [2 grammars and parsers](https://beautifulracket.com/bf/grammars-and-parsers.html)
- [3 grammar notation](https://beautifulracket.com/bf/grammar-notation.html)
- [4 the parser](https://beautifulracket.com/bf/the-parser.html)
- [5 the tokenizer and reader](https://beautifulracket.com/bf/the-tokenizer-and-reader.html)
- [6 an imperative expander](https://beautifulracket.com/bf/an-imperative-expander.html)
- [7 a functional expander](https://beautifulracket.com/bf/a-functional-expander.html)
- [8 packaging our language](https://beautifulracket.com/bf/packaging-our-language.html)
- [9 recap](https://beautifulracket.com/bf/recap.html)
- [10 source listing](https://beautifulracket.com/bf/source-listing.html)

### [Writing a grammar for BF](#writing-a-grammar-for-bf)

Back to the task at hand—writing a grammar for bf so we can parse source code like this:

    ++++++[>++++++++++++<-]>.
    >++++++++++[>++++++++++<-]>+.
    +++++++..+++.>++++[>+++++++++++<-]>.
    <+++[>----<-]>.<<<<<+++[>+++++<-]>.
    >>.+++.------.--------.>>+.

It turns out, however, that bf has a grammar even simpler than our fictitious M-expressions.

Let’s recall [how bf works](https://beautifulracket.com/bf/intro.html#how-bf-works):

-

  There are six operators that manipulate a byte array: > < + - . ,.

-

  There’s also a looping construct [···] that can contain operators or other loops.

-

  Whitespace and other characters have to be handled too—but we’ll leave those out of the grammar, because in the next section, we’ll learn a shortcut for dealing with them.

Let’s build up our grammar line by line. The first production rule represents the top element, which we’ll call bf-program.

    bf-program :

What’s the pattern for a bf-program? We know that bf is just a list of operations and loops. Let’s call them bf-op and bf-loop respectively. Thus our pattern for bf-program is a list of any number of bf-ops or bf-loops:

    bf-program : (bf-op | bf-loop)*

To recap, (bf-op | bf-loop) means “either a bf-op or a bf-loop” and * means “zero or more of the preceding item”.

If we stopped here, our grammar wouldn’t work, because we’ve referred to bf-op and bf-loop without defining them. We’ll do that next:

    bf-program : (bf-op | bf-loop)*
    bf-op      :
    bf-loop    :

So what’s a bf-op? As we noted, it’s one of the characters > < + - . ,, which we notate like so:

    bf-program : (bf-op | bf-loop)*
    bf-op      : ">" | "<" | "+" | "-" | "." | ","
    bf-loop    :

Finally, what’s a bf-loop? It’s a pair of square brackets with another list of bf-ops or bf-loops inside:

    bf-program : (bf-op | bf-loop)*
    bf-op      : ">" | "<" | "+" | "-" | "." | ","
    bf-loop    : "[" (bf-op | bf-loop)* "]"

That’s it—we have a complete grammar for bf. Meaning, bf may be a crazy language, but no matter how big or complex a bf program someone handed us, we could parse it using just these three rules.

Is this the only valid grammar for bf? No. We could recursively reuse our bf-program element like so:

    bf-program : (bf-op | bf-loop)*
    bf-op      : ">" | "<" | "+" | "-" | "." | ","
    bf-loop    : "[" bf-program "]"

We could also create a separate element, call it bf-exprs, to represent a list of bf-ops and bf-loops:

    bf-program : bf-exprs
    bf-exprs   : (bf-op | bf-loop)*
    bf-op      : ">" | "<" | "+" | "-" | "." | ","
    bf-loop    : "[" bf-exprs "]"

A case could be made for either of these alternatives. The tiebreaker is convenience. For reasons that will become clear when we write our [expander](https://beautifulracket.com/appendix/glossary.html#expander), the easiest grammar to work with will be the first one, because bf-program only appears once, and it otherwise uses the fewest possible rules.

### [Converting a grammar to a parser](#converting-a-grammar-to-a-parser)

Our small investment in making a grammar will pay an immediate dividend. Because now that we’ve made our bf grammar:

    bf-program : (bf-op | bf-loop)*
    bf-op      : ">" | "<" | "+" | "-" | "." | ","
    bf-loop    : "[" (bf-op | bf-loop)* "]"

We need to convert it to an actual bf parser. How?

Consistent with the idea of using a [domain-specific language](https://beautifulracket.com/appendix/glossary.html#domain-specific-language) to solve specialized problems, we’ll use a parser-generating language called brag that takes a list of production rules as its source code, and turns those rules into a working parser.

Here’s how we use it. We make a new file called "parser.rkt" like so:

parser.rkt

    #lang brag
    bf-program : (bf-op | bf-loop)*
    bf-op      : ">" | "<" | "+" | "-" | "." | ","
    bf-loop    : "[" (bf-op | bf-loop)* "]"

All we’ve done is take our grammar and add a #lang brag line to the top. When we import this module into our bf reader, we’ll get a function called [parse](http://docs.racket-lang.org/brag/index.html#(def._((lib._brag/main..rkt)._parse))) that implements this grammar, and another called [parse-to-datum](http://docs.racket-lang.org/brag/index.html#(def._((lib._brag/main..rkt)._parse-to-datum))) that will let us check the generated parse tree.

Let’s do that now, by making another file in the same directory, and pasting in the source code from our [demo program](https://beautifulracket.com/bf/intro.html#how-bf-works) as the input to parse-to-datum:

parser-tester.rkt

    #lang br
    (require "parser.rkt")
    (parse-to-datum "++++-+++-++-++[>++++-+++-++-++<-]>.")

When we run "parser-tester.rkt", we’ll get this:

    '(bf-program
      (bf-op "+")
      (bf-op "+")
      (bf-op "+")
      (bf-op "+")
      (bf-op "-")
      (bf-op "+")
      (bf-op "+")
      (bf-op "+")
      (bf-op "-")
      (bf-op "+")
      (bf-op "+")
      (bf-op "-")
      (bf-op "+")
      (bf-op "+")
      (bf-loop
       "["
       (bf-op ">")
       (bf-op "+")
       (bf-op "+")
       (bf-op "+")
       (bf-op "+")
       (bf-op "-")
       (bf-op "+")
       (bf-op "+")
       (bf-op "+")
       (bf-op "-")
       (bf-op "+")
       (bf-op "+")
       (bf-op "-")
       (bf-op "+")
       (bf-op "+")
       (bf-op "<")
       (bf-op "-")
       "]")
      (bf-op ">")
      (bf-op "."))

Once again, all the characters in our source code appear in a node of the parse tree, and these nodes correspond to the names and patterns of the production rules. (The curious can change the source string in "parser-tester.rkt" and see how the parse tree changes, though take care not to use any characters other than the eight handled by our grammar.)

Not bad—with four lines of code, the heavy lifting for the parser is now done. To finish our parser, we need to build one helper function that will take care of whitespace and other characters.

- [1 intro](https://beautifulracket.com/bf/intro.html)
- [2 grammars and parsers](https://beautifulracket.com/bf/grammars-and-parsers.html)
- [3 grammar notation](https://beautifulracket.com/bf/grammar-notation.html)
- [4 the parser](https://beautifulracket.com/bf/the-parser.html)
- [5 the tokenizer and reader](https://beautifulracket.com/bf/the-tokenizer-and-reader.html)
- [6 an imperative expander](https://beautifulracket.com/bf/an-imperative-expander.html)
- [7 a functional expander](https://beautifulracket.com/bf/a-functional-expander.html)
- [8 packaging our language](https://beautifulracket.com/bf/packaging-our-language.html)
- [9 recap](https://beautifulracket.com/bf/recap.html)
- [10 source listing](https://beautifulracket.com/bf/source-listing.html)