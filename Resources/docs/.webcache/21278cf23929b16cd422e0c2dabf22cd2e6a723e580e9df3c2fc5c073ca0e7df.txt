[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)

# Function size_of_val Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/mod.rs.html#2869)

``` rust
pub const unsafe fn size_of_val<T>(ptr: *const T) -> usizewhere
    T: ?Sized,
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

The size of the referenced value in bytes.

The stabilized version of this intrinsic is [`core::mem::size_of_val`](https://doc.rust-lang.org/std/mem/fn.size_of_val.html "fn std::mem::size_of_val").

## Safety

See [`crate::mem::size_of_val_raw`](https://doc.rust-lang.org/std/mem/fn.size_of_val_raw.html "fn std::mem::size_of_val_raw") for safety conditions.