# kAFL: Hardware-Assisted Feedback Fuzzing for OS Kernels (2017)

   kAFL: Hardware-Assisted Feedback Fuzzing
                for OS Kernels
Sergej Schumilo, Cornelius Aschermann, and Robert Gawlik, Ruhr-Universität Bochum;
             Sebastian Schinzel, Münster University of Applied Sciences;
                      Thorsten Holz, Ruhr-Universität Bochum
   https://www.usenix.org/conference/usenixsecurity17/technical-sessions/presentation/schumilo




             This paper is included in the Proceedings of the
                    26th USENIX Security Symposium
                      August 16–18, 2017 • Vancouver, BC, Canada
                                     ISBN 978-1-931971-40-9




                                                 Open access to the Proceedings of the
                                                  26th USENIX Security Symposium
                                                       is sponsored by USENIX
            kAFL: Hardware-Assisted Feedback Fuzzing for OS Kernels

         Sergej Schumilo                     Cornelius Aschermann                     Robert Gawlik
     Ruhr-Universität Bochum                Ruhr-Universität Bochum              Ruhr-Universität Bochum
                        Sebastian Schinzel                                  Thorsten Holz
               Münster University of Applied Sciences                  Ruhr-Universität Bochum


                       Abstract                              free vulnerabilities, are known threats for programs run-
   Many kinds of memory safety vulnerabilities have          ning in user mode as well as for the operating system
been endangering software systems for decades.               (OS) core itself. Past experience has shown that attack-
Amongst other approaches, fuzzing is a promising             ers typically focus on user mode applications. This is
technique to unveil various software faults. Recently,       likely because vulnerabilities in user mode programs are
feedback-guided fuzzing demonstrated its power, pro-         notoriously easier and more reliable to exploit. How-
ducing a steady stream of security-critical software bugs.   ever, with the appearance of different kinds of exploit
Most fuzzing efforts—especially feedback fuzzing—are         defense mechanisms – especially in user mode, it has
limited to user space components of an operating system      become much harder nowadays to exploit known vul-
(OS), although bugs in kernel components are more            nerabilities. Due to those advanced defense mechanisms
severe, because they allow an attacker to gain access        in user mode, the kernel has become even more appeal-
to a system with full privileges. Unfortunately, kernel      ing to an attacker since most kernel defense mechanisms
components are difficult to fuzz as feedback mechanisms      are not widely deployed in practice. This is due to more
(i.e., guided code coverage) cannot be easily applied.       complex implementations, which may affect the system
Additionally, non-determinism due to interrupts, kernel      performance. Furthermore, some of them are not part
threads, statefulness, and similar mechanisms poses          of the official mainline code base or even require sup-
problems. Furthermore, if a process fuzzes its own           port for the latest CPU extension (e.g., SMAP / SMEP
kernel, a kernel crash highly impacts the performance of     on x86-64). Additionally, when compromising the OS,
the fuzzer as the OS needs to reboot.                        an attacker typically gains full access to the system re-
   In this paper, we approach the problem of coverage-       sources (except for virtualized systems). Kernel-level
guided kernel fuzzing in an OS-independent and               vulnerabilities are usually used for privilege escalation
hardware-assisted way: We utilize a hypervisor and In-       or to gain persistence for kernel-based rootkits.
tel’s Processor Trace (PT) technology. This allows us           For a long time, fuzzing has been a critical compo-
to remain independent of the target OS as we just re-        nent in testing and establishing the quality of software.
quire a small user space component that interacts with       However, with the development of American Fuzzy Lop
the targeted OS. As a result, our approach introduces        (AFL), smarter fuzzers have gained significant traction
almost no performance overhead, even in cases where          in the industry [1] as well as in research [8, 14, 35, 37].
the OS crashes, and performs up to 17,000 executions         This trend was further amplified by Google’s OSS Fuzz
per second on an off-the-shelf laptop. We developed a        project that successfully found—and continues to find—
framework called kernel-AFL (kAFL) to assess the secu-       a significant number of critical bugs in highly security-
rity of Linux, macOS, and Windows kernel components.         relevant software. Finally, DARPA’s Cyber Grand Chal-
Among many crashes, we uncovered several flaws in the        lenge showed that fuzzing remains highly relevant for
ext4 driver for Linux, the HFS and APFS file system of       the state-of-the-art in bug finding. The latest generation
macOS, and the NTFS driver of Windows.                       of feedback-driven fuzzers generally uses mechanisms
                                                             to learn which inputs are interesting and which are not.
1   Introduction                                             Interesting inputs are used to produce more inputs that
                                                             may trigger new execution paths in the target. Inputs that
Several vulnerability classes such as memory corrup-         did not trigger interesting behavior in the program are
tions, race-conditional memory accesses, and use-after-      discarded. Thus, the fuzzer is able to “learn” the input



USENIX Association                                                           26th USENIX Security Symposium         167
format. This greatly improves efficiency and usability          • Hardware-assisted feedback: Our fuzzing ap-
of fuzzers, especially by reducing the need for an oracle         proach utilizes Intel’s Processor Trace (PT) tech-
which generates semi-valid inputs or an extensive corpus          nology, and thus has a very small performance over-
that covers most paths in the target.                             head. Additionally, our PT-decoder is up to 30 times
   Unfortunately, AFL is limited to user space applica-           faster than Intel’s ptxed decoder. Thereby, we ob-
tions and lacks kernel support. Fuzzing kernels has a             tain complete trace information that we use to guide
set of additional challenges when compared to userland            our evolutionary fuzzing algorithm to maximize test
(or ring 3) fuzzing: First, crashes and timeouts mandate          coverage.
the use of virtualization to be able to catch faults and
                                                                • Extensible and modular design: Our modular de-
continue gracefully. Second, kernel-level code has sig-
                                                                  sign separates the fuzzer, the tracing engine, and
nificantly more non-determinism than the average ring 3
                                                                  the target to fuzz. This allows to support additional
program—mostly due to interrupts, kernel threads, state-
                                                                  x86 operating systems’ kernel space and user space
fulness, and similar mechanisms. This makes fuzzing
                                                                  components, without the need to develop a system
kernel code challenging. Furthermore, there is no equiv-
                                                                  driver for the target OS.
alent to command line arguments or stdin to interact
with kernels or drivers in a generic way except for plain       • kernel-AFL: We incorporated our design con-
interrupt or sysenter instructions. In addition, the Win-         cepts and developed a prototype called kernel-AFL
dows kernel and many relevant drivers and core compo-             (kAFL) which was able to find several vulnerabili-
nents (for Windows, macOS and even Linux) are closed              ties in kernel components of different operating sys-
source and cannot be instrumented by common tech-                 tems. To foster research on this topic, we make the
niques without a significant performance overhead.                source code of our prototype implementation avail-
   Previous approaches to kernel fuzzing were not                 able at https://github.com/RUB-SysSec/kAFL.
portable because they relied on certain drivers or recom-
pilation [10, 34], were very slow due to emulation to       2     Technical Background
gather feedback [7], or simply were not feedback-driven
at all [11].                                                We use the Intel Processor Trace (Intel PT) extension of
   In this paper, we introduce a new technique that al-     IA-32 CPUs to obtain coverage information for ring 0
lows applying feedback fuzzing to arbitrary (even closed    execution of arbitrary (even closed-source) OS code. To
source) x86-64 based kernels, without any custom ring       facilitate efficient and OS-independent fuzzing, we also
0 target code or even OS-specific code at all. We           make use of Intel’s hardware virtualization features (In-
discuss the design and implementation of kernel-AFL         tel VT-x). Hence, our approach requires a CPU that sup-
(kAFL), our prototype implementation of the proposed        ports both Intel VT-x and Intel PT. This section provides a
techniques. The overhead for feedback generation is very    brief overview of these hardware features and establishes
small (less than 5%) due to a new CPU feature: Intel’s      the technical foundation for the later sections.
Processor Trace (PT) technology provides control flow
information on running code. We use this information        2.1    x86-64 Virtual Memory Layouts
to construct a feedback mechanism similar to AFL’s in-      Every commonly used x86-64 OS uses a split virtual
strumentation. This allows us to obtain up to 17,000 ex-    memory layout: The kernel is commonly located at the
ecutions per second on an off-the-shelf laptop (Thinkpad    upper half of each virtual memory space, whereas each
T460p, i7-6700HQ and 32 GB RAM) for simple target           user mode process memory is located in the lower half.
drivers. Additionally, we describe an efficient way for     For example, the virtual memory space of Linux is typ-
dealing with the non-determinisms that occur during ker-    ically split into kernel space (upper half) and user space
nel fuzzing. Due to the modular design, kAFL is exten-      (lower half) each with a size of 247 due to the 48-bit
sible to fuzz any x86/x86-64 OS. We have applied kAFL       virtual address limit of current x86-64 CPUs. Hence,
to Linux, macOS, and Windows and found multiple pre-        the kernel memory is mapped to any virtual address
viously unknown bugs in kernel drivers in those OSs.        space and therefore it is located always at the same
   In summary, our contributions in this paper are:         virtual address. If an user mode process executes the
                                                            syscall/sysenter instruction for kernel interaction or
  • OS independence: We show that feedback-driven           causes an exception that has to be handled by the OS, the
    fuzzing of closed-source kernel mode components         OS will keep the current CR3 value and thus does not
    is possible in an (almost) OS-independent manner        switch the virtual memory address space. Instead, the
    by harnessing the hypervisor (VMM) to produce           current virtual memory address space is reused and the
    coverage. This allows targeting any x86 operating       kernel handles the current user mode process related task
    system kernel or user space component of interest.      within the same address space.



168   26th USENIX Security Symposium                                                             USENIX Association
2.2    Intel VT-x                                              and control the operations performed with a small per-
                                                               formance overhead.
The kernel fuzzing approach introduced in this paper re-          To create, launch, and control a VM, the VMM has to
lies on modern x86-64 hardware virtualization technol-         use a virtual machine control structure (VMCS) for each
ogy. Hence, we provide a brief overview of Intel’s hard-       vCPU [28]. The VMCS contains all essential informa-
ware virtualization technology, Intel VT-x.                    tion about the current state and how to perform VMX
                                                               transitions of the vCPU.
   We differentiate between three kinds of CPUs: phys-
ical CPUs, logical CPUs, and virtual CPUs (vCPUs). A
physical CPU is a CPU that is implemented in hardware.         2.3    Intel Processor Trace
Most modern CPUs support mechanisms to increase
multithreading performance without additional physical         With the fifth generation of Intel Core processors (Broad-
CPU cores on the die (e.g., Intel Hyper-Threading). In         well architecture), Intel has introduced a new processor
this case, there are multiple logical CPUs on one phys-        feature called Intel Processor Trace (Intel PT) to provide
ical CPU. These different logical CPUs share the phys-         execution and branch tracing information. Unlike other
ical CPU and, thus, only one of them can be active at          branch tracing technologies such as Intel Last Branch
a time. However, the execution of the different logical        Record (LBR), the size of the output buffer is no longer
CPUs is interleaved by the hardware and therefore the          strictly limited by special registers. Instead, it is only
available resources can be utilized more efficiently (e.g.,    limited by the size of the main memory. If the output
one logical CPU uses the arithmetic logic unit while an-       target is repeatedly and timely emptied, we can create
other logical CPU waits for a data fetch) and the oper-        traces of arbitrary length. The processor’s output format
ating system can reduce the scheduling overhead. Each          is packet-oriented and separated into two different types:
logical CPU is usually treated like a whole CPU by the         general execution information and control flow informa-
operating system. Finally, it is possible to create multiple   tion packets. Intel PT produces various types of con-
hardware-supported virtual machines (VMs) on a single          trol flow related packet types during runtime. To obtain
logical CPU. In this case, each VM has a set of its own        control-flow information from the trace data, we require
vCPUs.                                                         a decoder. The decoder needs the traced software to in-
                                                               terpret the packets that contain the addresses of condi-
   The virtualization role model is divided into two com-      tional branches.
ponents: the virtual machine monitor (VMM) and the                Intel specifies five types of control flow affecting in-
VM. The VMM, also named hypervisor or host, is priv-           structions called Change of Flow Instruction (CoFI). The
ileged software that has full control over the physical        execution of different CoFI types results in different se-
CPU and provides virtualized guests with restricted ac-        quences of flow information packets. The three CoFI
cess to physical resources. The VM, also termed guest, is      types relevant to our work are:
a piece of software that is transparently executed within
the virtualized context provided by the VMM.                    1. Taken-Not-Taken (TNT): If the processor exe-
   To provide full hardware-assisted virtualization sup-           cutes any conditional jump, the decision whether
port, Intel VT-x adds two additional execution modes               this jump was taken or not is encoded in a TNT
to the well-known protection ring based standard mode              packet.
of execution. The default mode of executions is called
VMX OFF. It does not implement any hardware virtual-            2. Target IP (TIP): If the processor executes an indi-
ization support. When using hardware-supported virtual-            rect jump or transfer instruction, the decoder will
ization, the CPU switches into the VMX ON state and dis-           not be able to recover the control flow. There-
tinguishes between two different execution modes: the              fore, the processor produces a TIP packet upon the
higher-privileged mode of the hypervisor (VMX root or              execution of an instruction of the type indirect
VMM), and the lower privileged execution mode of the               branch, near ret or far transfer. These TIP
virtual machine guest (VMX non-root or VM).                        packets store the corresponding target instruction
                                                                   pointer executed by the processor after the transfer
    When running in guest mode, several privileged ac-             or jump has occurred.
tions or reasons (execution of restricted instructions, ex-
pired VMX-preemption timer, or access to certain em-            3. Flow Update Packets (FUP): Another case where
ulated devices) in the VM guest will trigger a VM-Exit             the processor must produce a hint packet for the
event and transfer control to the hypervisor. This way,            software decoder are asynchronous events such as
it is possible to run arbitrary software that expects priv-        interrupts or traps. These events are recorded as
ileged access to the hardware (such as an OS) inside a             FUPs and usually followed by a TIP to indicate the
VM. At the same time, a higher authority can meditate              following instruction.



USENIX Association                                                             26th USENIX Security Symposium         169
   To limit the amount of trace data generated, Intel PT
provides multiple options for runtime filtering. Depend-
ing on the given processor, it might be possible to con-
figure multiple ranges for instruction-pointer filtering (IP
Filter). In general, these filter ranges only affect virtual
addresses if paging is enabled; this is always the case
in x86-64 long-mode. Therefore, it is possible to limit
trace generation to selected ranges and thus avoid huge
amounts of superfluous trace data. In accordance to the
IP filtering mechanism, it is possible to filter traces by
the current privilege level (CPL) of the protection ring        Figure 1: High-level overview of the kAFL architecture.
model (e.g ring 0 or ring 3). This filter allows us to select   The setup process ( 1 - 3 ) is not shown.
only the user mode (CPL > 0) or kernel mode (CPL = 0)
activity. kAFL utilizes this filter option to limit tracing
explicitly to kernel mode execution. In most cases, the         ecution once the request has been handled. A overview
focus of tracing is not the whole OS within all user mode       of the architecture can be seen in Figure 1.
processes and their kernel interactions. To limit trace             We now outline the events and communication that
data generation to one specific virtual memory address          take place during a fuzz run, as depicted in Figure 2.
space, software can use the CR3 Filter. Intel PT will only      When the VM is started, the first part of the user mode
produce trace data if the CR3 value matches the config-         agent (the loader) uses the hypercall HC_SUBMIT_PANIC
ured filter value. The CR3 register contains the pointer to     to submit the address of the kernel panic handler (or the
the current page table. The value of the CR3 register can       BugCheck kernel address in Windows) to QEMU-PT 1 .
thus be used to filter code executed on behalf of a certain     QEMU-PT then patches a hypercall calling routine at the
ring 3 process, even in ring 0 mode.                            address of the panic handler. This allows us to get noti-
   Intel PT supports various configurable target domains        fied and react fast to crashes in the VM (instead of wait-
for output data. kAFL focuses on the Table of Physical          ing for timeouts / reboots).
Addresses (ToPA) mechanism that enables us to specify               Then the loader uses the hypercall HC_GET_PROGRAM to
multiple output regions: Every ToPA table contains mul-         request the actual user mode agent and starts it 2 . Now
tiple ToPA entries, which in turn contain the physical ad-      the loader setup is complete and the fuzzer begins its ini-
dress of the associated memory chunk used to store trace        tialization. The agent triggers a HC_SUBMIT_CR3 hyper-
data. Each ToPA entry contains the physical address, a          call that will be handled by KVM-PT. The hypervisor
size specifier for the referred memory chunk in physical        extracts the CR3 value of the currently running process
memory, and multiple type bits. These type bits specify         and hands it over to QEMU-PT for filtering 3 . Finally,
the CPU’s behavior on access of the ToPA entry and how          the agent uses the hypercall HC_SUBMIT_BUFFER to in-
to deal with filled output regions.                             form the host at which address it expects its inputs. The
                                                                fuzzer setup is now finished and the main fuzzing loop
3     System Overview                                           starts.
                                                                    During the main loop, the agent requests a new input
We now provide a high-level overview of the design of an        using the HC_GET_INPUT hypercall 4 . The fuzzing logic
OS-independent and hardware-assisted feedback fuzzer            produces a new input and sends it to QEMU-PT. Since
before presenting the implementation details of our tool        QEMU-PT has full access to the guest’s memory space,
called kAFL in Section 4.                                       it can simply copy the input into the buffer specified by
   Our system is split into three components: the               the agent. Then it performs a VM-Entry to continue ex-
fuzzing logic, the VM infrastructure (modified versions         ecuting the VM 5 . At the same time, this VM-Entry
of QEMU and KVM denoted by QEMU-PT and KVM-                     event enables the PT tracing mechanism. The agent now
PT), and the user mode agent. The fuzzing logic runs            consumes the input and interacts with the kernel (e.g.,
as a ring 3 process on the host OS. This logic is also re-      it interprets the input as a file system image and tries to
ferred to as kAFL. The VM infrastructure consists of a          mount it 6 ). While the kernel is being fuzzed, QEMU-
ring 3 component (QEMU-PT) and a ring 0 component               PT decodes the trace data and updates the bitmap on de-
(KVM-PT). This facilitates communication between the            mand. Once the interaction is finished and the kernel
other two components and makes the Intel PT trace data          handed control back to the agent, the agent notifies the
available to the fuzzing logic. In general, the guest only      hypervisor via a HC_FINISHED hypercall. The resulting
communicates with the host via hypercalls. The host can         VM-Exit stops the tracing and QEMU-PT decodes the
then read and write guest memory and continues VM ex-           remaining trace data 7 . The resulting bitmap is passed



170    26th USENIX Security Symposium                                                                USENIX Association
                                    Figure 2: Overview of the kAFL hypercall interaction.


to the logic for further processing 8 . Afterwards, the             3.2    User Mode Agent
agent can continue to run any untraced clean-up routines
before issuing another HC_GET_INPUT to start the next               We expect a user mode agent to run inside the virtual-
loop iteration.                                                     ized target OS. In principle, this component only has to
                                                                    synchronize and gather new inputs by the fuzzing logic
                                                                    via the hypercall interface and use it to interact with the
                                                                    guest’s kernel. Example agents are programs that try to
3.1    Fuzzing Logic                                                mount inputs as file system images, pass specific files
                                                                    such as certificates to kernel parser or even execute a
The fuzzing logic is the command and controlling com-
                                                                    chain of various syscalls.
ponent of kAFL. It manages the queue of interesting
                                                                       In theory, we only need one such component. In prac-
inputs, creates mutated inputs, and schedules them for
                                                                    tice, we use two different components: The first program
evaluation. In most aspects, it is based on the algo-
                                                                    is the loader component. Its job is to accept an arbitrary
rithms used by AFL. Similarly to AFL, we use a bitmap
                                                                    binary via the hypercall interface. This binary represents
to store basic block transitions. We gather the AFL
                                                                    the user mode agent and is executed by the loader com-
bitmap from the VMs through an interface to QEMU-PT
                                                                    ponent. Additionally, the loader component will check
and decide which inputs triggered interesting behaviour.
                                                                    if the agent has crashed (which happens often in case of
The fuzzing logic also coordinates the number of VMs
                                                                    syscall fuzzing) and restarts it if necessary. This setup
spawned in parallel. One of the bigger design differences
                                                                    has the advantage that we can pass any binary to the
to AFL is that kAFL makes extensive use of multipro-
                                                                    VM and reuse VM snapshots for different fuzzing com-
cessing and parallelism, where AFL simply spawns mul-
                                                                    ponents.
tiple independent fuzzers which synchronize their input
queues sporadically1 . In contrast, kAFL executes the de-
terministic stage in parallel, and all threads work on the          3.3    Virtualization Infrastructure
most interesting input. A significant amount of time is
                                                                    The fuzzing logic uses QEMU-PT to interact with KVM-
spent in tasks that are not CPU-bound (such as guests
                                                                    PT to spawn the target VMs. KVM-PT allows us to trace
that delay execution). Therefore, using many parallel
                                                                    individual vCPUs instead of logical CPUs. This com-
processes (upto 5-6 per CPU core) drastically improves
                                                                    ponent configures and enables Intel PT on the respec-
performance of the fuzzing process due to a higher CPU
                                                                    tive logical CPU before the CPU switches to guest ex-
load per core. Lastly, the fuzzing logic communicates
                                                                    ecution and disables tracing during the VM-Exit tran-
with the user interface to display current statistics in reg-
                                                                    sition. This way, the associated CPU will only pro-
ular intervals.
                                                                    vide trace data of the virtualized kernel itself. QEMU-
   1 AFL recently added experimental support for distributing the   PT is used to interact with the KVM-PT interface to
deterministic stage, see https://github.com/mirrorer/afl/blob/      configure and toggle Intel PT from user space and ac-
master/docs/parallel_fuzzing.txt#L60-L66.                           cess the output buffer to decode the trace data. The



USENIX Association                                                                  26th USENIX Security Symposium         171
decoded trace data is directly translated into a stream        3.5    Hypercalls
of addresses of executed conditional branch instruc-
tions. Moreover, QEMU-PT also filters the stream of            Hypercalls are a feature introduced by virtualization. On
executed addresses—based on previous knowledge of              Intel platforms, hypercalls are triggered by the vmcall
non-deterministic basic blocks—to prevent false-positive       instruction. Hypercalls are to VMMs as syscalls are to
fuzzing results, and makes those available to the fuzzing      kernels. If any ring 3 process or the kernel in the VM ex-
logic as AFL-compatible bitmaps. We use our own cus-           ecutes a vmcall instruction, a VM-Exit event is triggered
tom Intel PT decoder to cache disassembly results, which       and the VMM can decide how to process the hypercall.
leads to significant performance gains compared to the         We patched KVM-PT to pass through our own set of hy-
off-the-shelf solution provided by Intel.                      percalls to the fuzzing logic if a magic value is passed
                                                               in rax and the appropriate hypercall-ID is set in rbx.
                                                               Additionally, we also patched KVM-PT to accept hyper-
                                                               calls from ring 3. Arguments for specific hypercalls are
3.4    Stateful and Non-Deterministic Code                     passed through rcx. We use this mechanism to define
                                                               an interface that user mode agent can use to communi-
Tracing operating systems results in a significant amount      cate with the fuzzing logic. One example hypercall is
of non-determinism.        The largest source of non-          HC_SUBMIT_BUFFER. Its argument is a guest pointer that
deterministic basic block transitions are interrupts, which    is stored in rcx. Upon executing the vmcall instruction,
can occur at any point in time. Additionally, our imple-       a VM-Exit is triggered and QEMU-PT stores the buffer
mentation does not reset the whole state after each exe-       pointer that was passed. It will later copy the new input
cution since reloading the VM from a memory snapshot           data into this buffer (see step 5 in Figure 2). Finally, the
is costly. Thus we have to deal with the stateful and asyn-    execution of the VM is continued.
chronous aspects of the kernel. An example for stateful        cli
code might be a simple call to kmalloc(): Depending            mov rax , KAFL_MAGIC_VALUE
                                                               mov rbx , HC_CRASH
on the number of previous allocations, kmalloc() might         mov rcx , 0 x0
simply return a fresh pointer or map a whole range of          vmcall
pages and update a significant amount of metadata. We
                                                                          Listing 1: Hypercall crash notifier.
use two techniques to deal with these challenges.
   The first one is to filter out interrupts and the transi-      Another use case for this interface is to notify the
tion caused while handling interrupts. This is possible        fuzzing logic when a crash occurs in the target OS kernel.
using the Intel PT trace data. If an interrupt occurs, the     In order to do so, we overwrite the kernel crash handler
processor emits a TIP instruction since the transfer is not    of the OS with a simple hypercall routine. The injected
visible in the code. To avoid confusion during an inter-       code is shown in Listing 1 and displays how the hyper-
rupt occurring at an indirect control flow instruction, the    call interface is used on the assembly level. The cli in-
TIP packet is marked with FUP (flow update packet) to          struction disables all interrupts to avoid any kind of asyn-
indicate an asynchronous event. After identifying such a       chronous interference during the hypercall routine.
signature, the decoder will drop all basic blocks visited
until the corresponding iret instruction is encountered.
To link the interrupts with their corresponding iret, we       4     Implementation Details
track all interrupts on a simple call stack. This mecha-
nism is necessary since the interrupt handler may itself       Based on the design outlined in the previous section, we
be interrupted by another interrupt.                           built a prototype of our approach called kAFL. In the fol-
   The second mechanism is to blacklist any basic block        lowing, we describe several implementation details. The
that occurs non-deterministically. Each time we en-            source code of our reference implementation is available
counter a new bit in the AFL bitmap, we re-run the in-         at https://github.com/RUB-SysSec/kAFL.
put several times in a row. Every basic block that does
not show up in all of the trials will be marked as non-        4.1    KVM-PT
deterministic and filtered from further processing. For
fast access, the results are stored in a bitmap of black-      Intel PT allows us to trace branch transitions without
listed basic block addresses. During the AFL bitmap            patching or recompiling the targeted kernel. To the best
translation, any transition hash value—which combines          of our knowledge, no publicly available driver is able to
the current basic block address and the previous ba-           trace only guest executions of a single vCPU using In-
sic block address—involving a blacklisted block will be        tel PT for long periods of time. For instance, Simple-PT
skipped.                                                       [29] does not support long-term tracing by design. The



172   26th USENIX Security Symposium                                                                 USENIX Association
perf-subsystem [5] supports tracing of VM guest oper-
ations and long-term tracing. However, it is designed to
trace logical CPUs, not vCPUs. Even if VMX execution
is traced, the data would be associated with logical CPUs
and not with vCPUs. Hence, the VMX context must be
reassembled, which is a costly task.                                     Figure 3: KVM-PT ToPA configuration.
   To address these shortcomings, we developed KVM-
PT. It allows us to trace vCPUs for an indefinite amount
of time without any scheduling side effects or any loss        resulting in incomplete traces) or it can raise an inter-
of trace data due to overflowing output regions. The ex-       rupt. This interrupt causes a VM-exit since it is not mask-
tension provides a fast and reliable trace mechanism for       able. We catch the interrupt on the host and consume the
KVM vCPUs. Moreover, this extension exposes, much              trace data. Finally, we reset the buffers and continue with
like KVM, an extensive user mode interface to access           the VM execution. Unfortunately, this interrupt might be
this tracing feature from user space. QEMU-PT utilizes         raised at an unspecified time after the buffer was filled2 .
this novel interface to interact with KVM-PT and to ac-        Our configuration of the ToPA entries can be seen in Fig-
cess the resulting trace data.                                 ure 3. To avoid losing trace data, we use two different
                                                               ToPA entries. The first one is the main buffer 1 . Its
                                                               overflow behavior is to trigger the interrupt. Once the
                                                               main buffer is filled, a second entry is used until the in-
4.1.1   vCPU Specific Traces
                                                               terrupt is actually delivered. The ToPA specifies another
To enable Intel PT, software that runs within ring             smaller buffer 2 . Overflowing the second buffer would
0 (in our case KVM-PT) has to set the corre-                   lead to the stop of the tracing. To avoid the resulting
sponding bit of a model specific register (MSR)                data loss, we chose the second buffer to be about four
(IA32_RTIT_CTL_MSR.TraceEn) [28]. After tracing is             times larger than the largest overflowing trace we have
enabled, the logical CPU will trace any executed code if       ever seen in our tests (4 KB).
it satisfies the configured filter options. The modification      In case the second buffer also overflows, the following
has to be done before the CPU switches from the host           trace will contain a packet indicating that some data is
context to the VM operation; otherwise the CPU will ex-        missing. In that case the size of the second buffer can
ecute guest code and is technically unable to modify any       simply be increased. This way, we manage to obtain pre-
host MSRs. The inverse procedure is required after the         cise traces for any amount of trace data.
CPU has left the guest context. However, enabling or dis-
abling Intel PT manually will also yield a trace contain-      4.2     QEMU-PT
ing the manual MSR modification. To prevent the collec-
tion of unwanted trace data within the VMM, we use the         To make use of the KVM extension KVM-PT, an user
MSR autoload capabilities of Intel VT-x. MSR autoload-         space counterpart is required. QEMU-PT is an extension
ing can be enabled by modifying the corresponding en-          of QEMU and provides full support for KVM-PT’s user
tries in the VMCS (e.g., VM_ENTRY_CONTROL_MSR                  space interface. This interface provides mechanisms to
for VM-entries). This forces the CPU to load a list of pre-    enable, disable, and configure Intel PT at runtime as well
configured values for defined MSRs after either a VM-          as a periodic ToPA status check to avoid overruns. KVM-
entry or VM-exit has occurred. By enabling tracing via         PT is accessible from user mode via ioctl() commands
MSR autoloading, we only gather Intel PT trace data for        and an mmap() interface.
one specific vCPU.                                                In addition to being a userland interface to KVM-PT,
                                                               QEMU-PT includes a component that decodes trace data
                                                               into a form more suitable for the fuzzing logic: We de-
4.1.2   Continuous Tracing                                     code the Intel PT packets and turn them into an AFL-like
                                                               bitmap.
Once we have enabled Intel PT, the CPU will write the
resulting trace data into a memory buffer until it is full.
The physical addresses of this buffer and how to han-          4.2.1    PT Decoder
dle full buffers is specified by an array of data structures   Extensive kernel fuzzing may generate several hundreds
called Table of Physical Addresses (ToPA) entries.             of megabytes of trace data per second. To deal with
   The array can contain multiple entries and has to be           2 This is due to the current implementation of this interrupt. Intel
terminated by a single END entry 3 . There are two             specifies the interrupt as not precise, which means it is likely that fur-
different ways the CPU can handle an overflow: It can          ther data will be written to the next buffer or tracing will be terminated
stop the tracing (while continuing the execution—thus          and data will be discarded.




USENIX Association                                                                 26th USENIX Security Symposium                   173
                   Figure 4: Overview of the pipeline that converts Intel PT traces to kAFL bitmaps.


such large amounts of incoming data, the decoder must                  (id(A)/2 ⊕ id(B)) % SIZE_OF_BITMAP
be implemented with a focus on efficiency. Otherwise,
the decoder may become the major bottleneck during                 Instead of the compile-time random, kAFL uses the
the fuzzing process. Nevertheless, the decoder must also       addresses of the basic blocks. Each time the transition is
be precise, as inaccuracies during the decoding process        observed, the corresponding byte in the bitmap is incre-
would result in further errors. This is due to the nature of   mented. After finishing the fuzzing iteration, each entry
Intel PT decoding since the decoding process is sequen-        of the bitmap is rounded such that only the highest bit re-
tial and is affected by previously decoded packets.            mains set. Then the bitmap is compared with the global
                                                               static bitmap to see if any new bit was found. If a new bit
   To ease efforts to implement an Intel PT software
                                                               was found, it is added to the global bitmap and the input
decoder, Intel provides its own decoding engine called
                                                               that triggered the new bit is added to the queue. When
libipt [4]. libipt is a general-purpose Intel PT decod-
                                                               a new interesting input is found, a deterministic stage is
ing engine. However, it does not fit our purposes very
                                                               executed that tries to mutate each byte individually.
well because libipt decodes trace data in order to pro-
                                                                   Once the deterministic stage is finished, the non-
vide execution data and flow information. Furthermore,
                                                               deterministic phase is started.         During this non-
libipt does not cache disassembled instructions and has
                                                               deterministic phase, multiple mutations are performed at
performed poorly in our use cases.
                                                               random locations. If the deterministic phase finds new
   Since kAFL only relies on flow information and the
                                                               inputs, the non-deterministic phase will be delayed un-
fuzzing process is repeatedly applied to the same code,
                                                               til all deterministic phases of all interesting inputs have
it is possible to optimize the decoding process. Our In-
                                                               been performed. If an input triggers an entirely new tran-
tel PT software decoder acts like a just-in-time decoder,
                                                               sition (as opposed to a change in the number of times the
which means that code sections are only considered if
                                                               transition was taken), it will be favored and fuzzed with
they are executed according to the decoded trace data.
                                                               a higher priority.
To optimize further look-ups, all disassembled code sec-
tions are cached. In addition, we simply ignore packets
that are not relevant for our use case.                        5   Evaluation
   Since our PT decoder is part of QEMU-PT, trace data
is directly processed if the ToPA base region is filled.       Based on our implementation, we now describe the
The decoding process is applied in-place since the buffer      different fuzzing campaigns we performed to evaluate
is directly accessible from user space via mmap(). Un-         kAFL. We evaluate kAFL’s fuzzing performance across
like other Intel PT drivers, we do not need to store large     different platforms. Section 5.5 provides an overview of
amounts of trace data in memory or on storage devices          all reported vulnerabilities, crashes, and bugs that were
for post-mortem decoding. Eventually, the decoded trace        found during the development process of kAFL. We also
data is translated to the AFL bitmap format.                   evaluate kAFL’s ability to find a previously known vul-
                                                               nerability. Finally, in Section 5.6 the overall fuzzing
                                                               performance of kAFL is compared to ProjectTriforce,
4.3    AFL Fuzzing Logic                                       the only other OS-independent feedback fuzzer avail-
                                                               able. TriforceAFL is based on the emulation backend
We give a brief description of the fuzzing parts of AFL        of QEMU instead of hardware-assisted virtualization and
because the logic we use to perform scheduling and mu-         Intel PT. The performance overhead of KVM-PT is dis-
tations closely follows that of AFL. The most important        cussed in Section 5.7. Additionally, a performance com-
aspect of AFL is the bitmap used to trace which basic          parison of our PT decoder and an Intel implementation
block transitions where encountered. Each basic block          of a software decoder is given in Section 5.8.
has a randomly assigned ID, and each transition from ba-          If not stated otherwise, the benchmarks were per-
sic block A to another basic block B is assigned an offset     formed on a desktop system with an Intel i7-6700 pro-
into the bitmap according to the following formula:            cessor and 32GB DDR4 RAM. To avoid distortions due



174   26th USENIX Security Symposium                                                                USENIX Association
to poor I/O performance, all benchmarks are performed
on a RAM disk. Similar to AFL, we consider a crash-
ing input to be unique if it triggered at least one basic
block transition which has not been triggered by any pre-
vious crash (i.e., the bitmap contains at least one new
bit). Note this does not imply that the underlying bugs
are truly unique.



5.1    Fuzzing Windows
                                                                 Figure 5: Fuzzing the ext4 kernel module for 32 hours.
We implemented a small Windows 10 specific user mode
agent that mounts any data chunk (fuzzed payload) as
NTFS-partitioned volume (289 lines of C code). We
used the Virtual Hard Disk (VHD) API and various                 5.2    Fuzzing Linux
IOCTLS to mount and unmount volumes programmat-
ically [31, 32]. Unfortunately, mounting volumes is a            We implemented a similar agent for Linux, which
slow operation under Windows and we only managed                 mounts data as ext4 volumes (66 lines of C code). We
to achieve a throughput of 20 executions per second.             started the fuzz campaign with a minimal 64KB ext4 im-
Nonetheless, kAFL managed to find a crash in the NTFS            age as initial input. However, we configured the fuzzer
driver. The fuzzer ran for 4 days and 14 hours and re-           such that it only fuzzes the first two kilobytes during
ported 59 unique crashes, all of which were division by          the deterministic phase. In contrast to Windows, the
zero crashes. After manual investigation we suspect that         Linux mount process is very fast, and we reached 1,000
there is only one unique bug. While it does not allow            to 2,000 tests per second on a Thinkpad laptop with a
code execution, it is still a denial-of-service vulnerability,   i7-6700HQ@2.6GHz CPU and 32GB RAM. Due to this
as for example, a USB stick with that malicious NTFS             high performance, we obtained significantly better cov-
volume plugged into a critical system will crash that sys-       erage and managed to discover 160 unique crashes and
tem with a blue screen. It seems that we only scratched          multiple (confirmed) bugs in the ext4 driver during a
the surface and NTFS was not thoroughly fuzzed yet.              twelve-day fuzzing campaign. Figure 5 shows the first
Hence, we assume that the NTFS driver under Windows              32 hours of another fuzzing run. The fuzzing process was
is a valuable target for coverage-based feedback fuzzing.        still finding new paths and crashes on a fairly regular ba-
   Furthermore, we implemented a generic system call             sis after 32 hours. An interesting observation is that there
(syscall) fuzzing agent that simply passes a block of data       was no new coverage produced between hours 16 and 25,
to a syscall by setting all registers and the top stack re-      yet the number of inputs increased due a higher number
gion (55 lines of C and 46 lines of assembly code). This         of loop iterations. After hour 25, a truly new input was
allows to set parameters for a syscall with a fuzzing pay-       found that unlocked significant parts of the codebase.
load independent of the OS ABI. The same fuzzer can
be used to attack syscalls on different operation sys-
tems such as Linux or macOS. However, we evaluated
it against the Windows kernel given the proprietary na-          5.3    Fuzzing macOS
ture of this OS. We did not find any bugs in 13 hours
of fuzzing with approx 6.3M executions since many                Similarly to Windows and Linux, we targeted multiple
syscalls cause the userspace agent to terminate: Due             file systems for macOS. So far, we found approximately
to the coverage-guided feedback, kAFL quickly learned            150 crashes in the HFS driver and manually confirmed
how to generate payloads to execute valid syscalls, and          that at least three of them are unique bugs that lead to
this led to the unexpected execution of user mode call-          a kernel panic. Those bugs can be triggered by unprivi-
backs via the kernel within the fuzzing agent. These             leged users and, therefore, could very well be abused for
crashes require rather expensive restarts of the agent and       local denial-of-service attacks. One of these bugs seems
therefore we only achieved approx. 134 executions per            to be a use-after-free vulnerability that leads to full con-
second, while normally kAFL achieves a throughput of             trol of the rip register. Additionally, kAFL found 220
1,000 to 4,000 tests per second (see Section 5.2). Ad-           unique crashes in the APFS kernel extension. All 3 HFS
ditionally, the Windows syscall interface has already re-        vulnerabilities and multiple APFS flaws have been re-
ceived much attention by the security community.                 ported to Apple.



USENIX Association                                                               26th USENIX Security Symposium         175
5.4    Rediscovery of Known Bugs                                 system, a mainline patch was proposed. The last re-
                                                                 ported Linux vulnerability, which calls in the ext4 error
We evaluated kAFL on the keyctl interface, which al-
                                                                 handling routine panic() and hence results in a kernel
lows a user space program to store and manage vari-
                                                                 panic, was at the time of writing not investigated any fur-
ous kinds of key material in the kernel. More specif-
                                                                 ther. The NTFS bug in Windows 10 is a non-recoverable
ically, it features a DER (see RFC5280) parser to load
                                                                 error condition which leads to a blue screen. This bug
certificates. This functionality had a known bug (CVE-
                                                                 was reported to Microsoft, but has not been confirmed
2016-07583 ). We tested kAFL against the same interface
                                                                 yet. Similarly, Apple has not yet verified or confirmed
on a vulnerable kernel (version 4.3.2). kAFL was able
                                                                 our reported macOS bugs.
to uncover the same problem and one additional previ-
ously unknown bug that was assigned CVE-2016-86504 .
kAFL managed to trigger 17 unique KASan reports and              5.6    Fuzzing Performance
15 unique panics in just one hour of execution time. Dur-        We compare the overall performance of kAFL across dif-
ing this experiment, kAFL generated over 34 million in-          ferent operating systems. To ensure comparable results,
puts, found 295 interesting inputs, and performed nearly         we created a simple driver that contains a JSON parser
9,000 executions per second. This experiment was per-            based on jsmn11 for each aforementioned operating sys-
formed while running 8 processes in parallel.                    tem and used it to decode user input (see Listing 2). If
                                                                 the user input is a JSON string starting with "KAFL", a
5.5    Detected Vulnerabilities                                  crash is triggered. We traced both the JSON parser as
                                                                 well as the final check. This way kAFL was able to learn
During the evaluation, kAFL found more than a thou-              correct JSON syntax. We measured the time used to find
sand unique crashes. We evaluated some manually and              the crash, the number of executions per second, and the
found multiple security vulnerabilities in all tested op-        speed for new paths to be discovered on all three target
erating systems such as Linux, Windows, and macOS.               operating systems.
So far, eight bugs were reported and three of them were
                                                            1    jsmn_parser parser ;
confirmed by the maintainers:                               2    jsmntok_t tokens [5];
                                                            3    jsmn_init (& parser ) ;
  • Linux: keyctl Null Pointer Dereference5 (CVE-           4
                                                            5    int res = jsmn_parse (& parser , input , size , tokens , 5) ;
    2016-86506 )                                            6    if ( res >= 2) {
                                                            7          if ( tokens [0]. type == JSMN_STRING ) {
  • Linux: ext4 Memory Corruption7                          8                int json_len = tokens [0]. end - tokens [0].
                                                                                    start ;
                                                            9                int s = tokens [0]. start ;
  • Linux: ext4 Error Handling8                             10               if ( json_len > 0 && input [ s +0] == 'K ') {
                                                            11               if ( json_len > 1 && input [ s +1] == 'A ') {
                                                                             if ( json_len > 2 && input [ s +2] == 'F ') {
  • Windows: NTFS Div-by-Zero9                              12
                                                            13               if ( json_len > 3 && input [ s +3] == 'L ') {
                                                            14                     panic ( KERN_INFO " KAFL ...\ n " ) ;
  • macOS: HFS Div-by-Zero10                                15         }}}}}
                                                            16   }
  • macOS: HFS Assertion Fail10
                                                                 Listing 2: The JSON parser kernel module used for the
  • macOS: HFS Use-After-Free10                                  coverage benchmarks.

  • macOS: APFS Memory Corruption10                                 We performed five repeated experiments for each op-
                                                                 erating system. Additionally we tested TriforceAFL with
   Red Hat has assigned a CVE number for the first re-           the Linux target driver. TriforceAFL is unable to fuzz
ported security flaw, which triggers a null pointer def-         Windows and macOS. To compare TriforceAFL with
erence and a partial memory corruption in the kernel             kAFL, the associated TriforceLinuxSyscallFuzzer was
ASN.1 parser if an RSA certificate with a zero expo-             slightly modified to work with our vulnerable Linux ker-
nent is presented. For the second reported vulnerabil-           nel module. Unfortunately, it was not possible to com-
ity, which triggers a memory corruption in the ext4 file         pare kAFL against Oracle’s file system fuzzer [34] due
                                                                 to technical issues with its setup.
  3 https://access.redhat.com/security/cve/cve-2016-0758
  4 https://access.redhat.com/security/cve/cve-2016-8650
                                                                    During each run, we fuzzed the JSON parser for 30
  5 http://seclists.org/fulldisclosure/2016/Nov/76               minutes. The averaged and rounded results are displayed
  6 https://access.redhat.com/security/cve/cve-2016-8650         in Table 1. As we can see, the performance of kAFL
  7 http://seclists.org/fulldisclosure/2016/Nov/75
                                                                 is very similar across different systems. It should be re-
  8 http://seclists.org/bugtraq/2016/Nov/1
  9 Reported to Microsoft Security.
                                                                 marked that the variance in this experiment is rather high
 10 Reported to Apple Product Security.                            11 http://zserge.com/jsmn.html




176   26th USENIX Security Symposium                                                                     USENIX Association
                             Execs/Sec         Execs/Sec         Time to Crash        Time to Crash     Paths/Min      Paths/Min
                            (1 Process)      (8 Processes)         (1 Process)         (8 Processes)   (1 Process)   (8 Processes)
 TriforceAFL                150              320                 -a                   -a               10.08         -b
 Linux (initramfs)          3000             5700                7:50                 6:00             15.84         15.62
 Debian 8                   3000             5700                4:55                 6:30             16.20         16.00
 Debian 8 (KASan)           4300             5700                9:20                 6:00             16.22         15.90
 macOS (10.12.4)            5100             8100                7:43                 5:10             14.50         15.06
 Windows 10                 4300             8700                4:14                 4:50             11.50         12.02
a Not found during 30-minute experiments.
b This value cannot be obtained since TriforceAFL does not synchronize in such short time frames.


                     Table 1: kAFL and TriforceAFL fuzzing performance on the JSON sample driver.




                                                                             Figure 7: Raw execution performance comparison.
Figure 6: Coverage comparison of kAFL (initramfs) and
TriforceAFL. kAFL takes less than 3 minutes to find the
same number of paths as TriforceAFL does in 30 minutes                    reach for TriforceAFL. kAFL mostly stops finding new
(each running 1 process).                                                 paths around the 10-15 minute mark, because the target
                                                                          driver simply doesn’t contain any more paths to be un-
                                                                          covered. Therefore, the coverage value in Table 1 (stated
and explains some of the surprising results. This is due to               as Paths/Min) is limited to the first 10 minutes of each
the stochastic nature of fuzzing, since each fuzzer finds                 30-minute run.
vastly different paths, some of which may take signifi-                      We also compare raw execution performance instead
cantly longer to process, especially crashing paths and                   of overall fuzzing performance, which is biased because
loops. One example for high variance is the fact that on                  of the execution of different paths, the sampling process
Debian 8 (initramfs), the multiprocessing configuration                   for the non-determinism-filter, and various synchroniza-
on average needed more time to find the crash than one                    tion mechanisms. Especially on smaller inputs, these
process.                                                                  factors disproportionately affect the overall fuzzing per-
                                                                          formance. To avoid this, we compared the performance
                                                                          during the first havoc stage. Figure 7 shows the raw ex-
TriforceAFL We used the JSON driver to compare
                                                                          ecution performance of kAFL compared to TriforceAFL
kAFL and TriforceAFL with respect to execution speed
                                                                          during this havoc phase. kAFL provides up to 54 times
and code coverage. However, the results where biased
                                                                          better performance compared to TriforceAFL’s QEMU
heavily in two ways: TriforceAFL did not manage to find
                                                                          CPU emulation. Slightly lower performance boosts are
a path that triggers the crash within 30 minutes (usually
                                                                          seen in single-process execution (48 times faster).
it takes approximately 2 hours), making it very hard to
compare the code coverage of kAFL and TriforceAFL.
The number of discovered paths is not a good indica-                      syzkaller We did not perform a performance compari-
tor for the amount of coverage: With increasing running                   son against syzkaller [10]. This has two reasons: First of
time, it becomes more difficult to discover new paths.                    all, syzkaller is a highly specific syscall fuzzer that en-
Secondly, the number of executions per second is also bi-                 codes a significant amount of domain knowledge and is
ased by slower and harder to reach paths and especially                   therefore not applicable to other domains such as filesys-
crashing inputs. The coverage reached over time can be                    tem images. On the other hand, syzkaller would most
seen in Figure 6. It is obvious from the figure that kAFL                 likely generate a significantly higher code coverage even
found a significant number of paths that are very hard to                 without any feedback since it knows how to generate



USENIX Association                                                                           26th USENIX Security Symposium      177
                                                             Figure 9: kAFL and ptxed decoding time on multiple
Figure 8: Overhead for compiling QEMU-2.6.0 in a             copies of the same trace (kAFL is up to 30 times faster).
traced VM.

                                                             describes a performance penalty of < 5 % compared to
valid syscalls and hence is able to trigger valid paths      execution without enabled Intel PT [30]. Accordingly,
without any learning. Therefore, the coverage compar-        we expect approximately 5% of kernel overhead. In
ison would be highly misleading unless we implement          the first experiment, the traces were discarded without
the same syscall logic; a task that is out of the scope of   further analysis (KVM-PT). In the second experiment
this paper. Additionally, the coverage collection via kcov   (KVM-PT & ToPA Check), we enabled repeated check-
is highly specific to Linux and not applicable to closed-    ing and clearing of the ToPA buffers. In the final ex-
source targets.                                              periment (KVM-PT & PT decoder), we tested the whole
                                                             pipeline including our own decoder and conversion to an
                                                             AFL bitmap.
5.7   KVM-PT Overhead                                           During our benchmarks, an overhead between 1% –
                                                             4% was measured empirically. Since the resulting over-
Our KVM extension KVM-PT adds overhead to the raw            head is small, we do not expect it to have a major influ-
execution of KVM. Therefore, the performance overhead        ence on the overall fuzzing performance.
was compared with several KVM-PT setups on an i5-
6500@3.2Ghz desktop system with 8GB DDR4 RAM.
This includes KVM-PT in combination with the PT de-          5.8   Decoder Engine
coder, KVM-PT without the PT decoder but processing
                                                             In contrast to KVM-PT, the decoder has significant in-
frequent ToPA state checks, and KVM-PT without any
                                                             fluence on the overall performance of the fuzzing pro-
ToPA consideration. For this benchmark, a 13MB ker-
                                                             cess since the decoding process is—other than Intel PT
nel code range was configured via IP filtering ranges and
                                                             and hence KVM-PT—not hardware-accelerated. There-
traced with one of the aforementioned setups of KVM-
                                                             fore, this process is costly and has to be as efficient as
PT. These benchmarks consider only the kernel core, but
                                                             possible. Consequently, the performance of our devel-
neither considers any kernel module. During KVM-PT
                                                             oped PT decoder was compared to that of ptxed. This
execution only supervisor mode was traced.
                                                             decoder is Intel’s example implementation of an Intel PT
   To generate Intel PT load, QEMU-2.6.0 was com-
                                                             software decoder and is based on libipt. To compare
piled within a traced VM using the ./configure option
                                                             both decoder engines, a small Intel PT trace sample was
--target-list=x86_64-softmmu. We restricted trac-
                                                             generated by executing
ing to the whole kernel address space. This benchmark
was executed on a single vCPU. The resulting compile                  find / > /dev/null 2> /dev/null
time was measured and compared. The following figure
illustrates the relative overhead compared to KVM ex-        within a Linux VM (Linux debian 4.8.0-1-amd64)
ecution without KVM-PT (see Figure 8). We ran three          traced by KVM-PT. This performance benchmark was
experiments to determine the overhead of the different       processed on an i5-6500@3.2Ghz desktop system with
components. In each experiment, we measured three dif-       8GB DDR4 RAM. Only code execution in supervisor
ferent overheads: wall-clock time, user, and kernel. The     mode was traced. The generated sample is 9.4MB in
difference in overall time is denoted by the wall-clock      size and contains over 431, 650 TNT packets, each repre-
overhead. Additionally, we measured how much more            senting up to 7 branch transitions. The sample also con-
time is spent in the kernel and how much time is spend       tains over 100, 045 TIPs. We sanitized the sample by re-
only in user space. Since we only trace the kernel, we       moving anything but flow information packets (see Sec-
expect the users space overhead to be insignificant. Intel   tion 2.3) to avoid any influence of decoding large amount



178   26th USENIX Security Symposium                                                             USENIX Association
of execution information packets, since those are not con-     to infer the whole input grammar from program traces
sidered by our PT decoder. The result is a 5.3MB trace         [13, 24, 38]. The selection of more interesting inputs was
file. To test the effectiveness of the caching approach of     optimized by Rebert et al. [36]. Similar approaches have
our PT decoder, we created cases containing 1, 5, 10,          been used to optimize the mutation rate [17, 40].
50, and 250 copies of the trace. This is a realistic test
case, since during fuzzing we see the same (or very sim-
ilar) paths repeatedly. Figure 9 illustrates the measured
                                                               6.2   White-Box fuzzers
speedup of our PT decoder compared to ptxed.                   To reduce the burden on the tester, techniques where in-
   The figure also shows that our PT decoder easily out-       troduced that apply insights from program analysis to
performs the Intel decoder implementation, even if the         find more interesting inputs. Tools like SAGE [23],
PT decoder processes data for the very first time. This is     DART [22], KLEE [15], SmartFuzz [33], or Mayhem
most likely due to the fact that even a single trace already   [16] try to enumerate complex paths by using techniques
contains a significant amount of loops. Another possible       such as symbolic execution and constraint solving. Tools
factor is the use of Capstone [2] as the instruction decod-    like TaintScope [39], BuzzFuzz [21] and Vuzzer [35]
ing backend. As we decode more and more copies of the          utilize taint tracing and similar dynamic analysis tech-
same trace, it can be seen that our decoder becomes in-        niques to uncover new paths. These tools are often able
creasingly faster (only using 56 times as much time to         to find very complicated code paths that are hidden be-
decode 250 times that amount of data). The caching ap-         hind checksums, magic constants, and other constraints
proach outperforms Intel’s implementation and is up to         that are very unlikely to be satisfied by random inputs.
25 to 30 times faster.                                         Another approach is to use the same kind of information
                                                               to bias the search towards dangerous behavior instead of
6     Related Work                                             new code paths [26].
                                                                  The downside is that these techniques are often signif-
Fuzzers are often classified according to the amount           icantly harder to implement, scale to large programs, and
of interaction with the target program. For black-box          parallelize. To the best of our knowledge, there are no
fuzzers, the fuzzer does not use any information about         such tools for operating system fuzzing.
the target program at all. White-box fuzzers typically use
advanced program analysis techniques to uncover inter-         6.3   Gray-Box Fuzzers
esting properties of the target. Somewhere in the mid-
dle are so called gray-box fuzzers that will typically use     Gray-box fuzzers try to retain the high throughput and
some kind of feedback from the target (such as cover-          simplicity of black-box fuzzers while gaining some of
age information) to guide their search, without analyzing      the additional coverage provided by the advanced me-
the logic of the target program itself. In this section, we    chanics in white-box fuzzing. The prime example for
provide a brief overview of the work performed in the          gray-box fuzzing is AFL, which uses coverage informa-
corresponding areas of fuzzing.                                tion to guide its search. This way, AFL voids spend-
                                                               ing additional time on inputs that do not trigger new
                                                               behaviors. Similar techniques are used by many other
6.1    Black-Box Fuzzers                                       fuzzers [8, 25].
The oldest class of fuzzers are black-box fuzzers. These          To further increase the effectiveness of gray-box
fuzzers typically have no interaction with the target pro-     fuzzing, many of the tricks already used in black-box
gram beyond executing it on newly generated inputs. To         fuzzing can be applied. Böhme et al. [14] showed how to
increase effectiveness, a number of assumptions are usu-       use the insight gained from modelling gray-box fuzzing
ally made: Either a large corpus of good coverage inputs       as a walk on a Markov chain to increase the performance
get mutated and recombined repeatedly. Examples for            of gray-box fuzzing by up to an order of magnitude.
this class are Radamsa [3] or zzuf [12]. Or, the pro-
grammer needs to specify how to generate new semi-             6.4   Coverage-Guided Kernel Fuzzers
valid input files that almost look like real files. Exam-
ples including tools like Peach [6] or Sulley [9]. Both        A project called syzkaller was released by Vyukov; it
approaches have one very important drawback: It is a           is the first publicly available gray-box coverage-guided
time-consuming task to use these tools.                        kernel fuzzer [10]. Nossum and Casanovas demonstrate
   To improve the performance of black-box fuzzers,            that most Linux file system drivers are vulnerable to
many techniques have been proposed. Holler et al.              feedback-driven fuzzing by using an adapted version of
[27] introduced learning interesting parts of the input        AFL [34]. This modified AFL version is based on glue
grammar from old crashing inputs. Others even sought           code to the kernel consisting of a driver interface to



USENIX Association                                                            26th USENIX Security Symposium         179
measure feedback during fuzzing file system drivers of         Supported CPUs. Due to the usage of Intel PT and
the kernel and expose this data to the user space. This        Intel VT-x, our approach is limited to certain Intel CPUs
fuzzer runs inside the targeted OS; a crash terminates the     supporting these extensions. Virtually all modern Intel
fuzzing session.                                               CPUs support Intel VT-x. Unfortunately, Intel is rather
   In 2016, Hertz and Newsham released a modified ver-         vague as to which CPUs exactly support process trace
sion of AFL called TriforceAFL [7]. Their work is based        inside of VMs and various other extensions (such as IP
on a modification of QEMU and utilizes the correspond-         filtering and multi-entry ToPA). We tested our system
ing emulation backend to measure fuzzing progress by           on the following CPU models: Intel Core i5-6500, In-
determining the current instruction pointer after a control    tel Core i7-6700HQ, and Intel Core i5-6600. We believe
flow altering instruction has been executed. In theory,        that at the time of writing, most Skylake and Kabylake
their fuzzer is able to fuzz any OS emulated in QEMU. In       CPUs have the necessary hardware support.
practice, the TriforceAFL fuzzer is limited to operating
systems that are able to boot from read-only file systems,
                                                               Just-In-Time Code. Intel PT does not provide a com-
which narrows down the candidates to classic UNIX-like
                                                               plete list of executed instruction pointers. Instead, Intel
operating systems such as Linux, FreeBSD, NetBSD, or
                                                               PT generates as little information as necessary to reduce
OpenBSD. Therefore, TriforceAFL is currently not able
                                                               the amount of data produced by the processor. Con-
to fuzz closed-source operating systems such as macOS
                                                               sequently, the Intel PT software decoder does not only
or Windows.
                                                               require control flow information to reconstruct the con-
                                                               trol flow but also needs the program that was executed
7     Discussion                                               during tracing. If the program is modified during run-
                                                               time, as often done by just-in-time (JIT) compilers in
Even though our approach is general, fast and mostly in-
                                                               user and kernel mode, the decoder is unable to exactly
dependent of the underlying OS, there are some limita-
                                                               restore the runtime control flow. To bypass this limita-
tions we want to discuss in this section.
                                                               tion, the decoder requires information about all modi-
                                                               fications applied to the program instead of an ordinary
OS-Specific Code. We use a small amount (usually               memory dump or the executable file. As Deng et al. [18]
less than 150 lines) of OS-dependent ring 3 code that per-     have shown, this is possible by making use of EPT viola-
forms three tasks. First, it interacts with the OS to trans-   tions when executing written pages. Another, somewhat
late the inputs from the fuzzing engine to interactions        more old-fashioned, method to achieve the same is to use
with the OS (e.g., mount the data as a partition). Second,     shadow page tables [19]. Once one it is possible to hook
it obtains the address of the crash handler of the OS such     the execution of modified code, self-modifying code can
that we can detect crashes faster than it would take to        be dumped. Reimplementing this technique was out of
wait for the timeout. Third, it can return the addresses of    the scope of this work. It should be noted though that
certain drivers. These addresses can be used to limit trac-    fuzzing kernel JIT code is a very interesting topic since
ing to the activity of said drivers, which improves perfor-    kernel JIT components, such as the BPF JIT in Linux,
mance when only fuzzing individual drivers.                    have often been part of serious vulnerabilities.
   None of these functions are necessary and only im-
prove performance in some cases. The first use case
                                                               Multibyte Compares. Similar to AFL, we are unable
can be avoided by using generic syscall fuzzing. In that
                                                               to effectively bypass checks for large magic values in
case a single standard C program which does not use any
                                                               the inputs. However, we support specifying dictionaries
platform-specific API would suffice to trigger sysenter/
                                                               of interesting constants to improve performance if such
syscall instructions. We do not strictly need the address
                                                               magic values are known in advance (e.g., from RFCs,
of the crash handler, since there are numerous other ways
                                                               source code, or disassembly). Some solutions involving
to detect whether the VM crashed. It would also be quite
                                                               techniques such as concolic execution (e.g., Driller [37])
easy to obtain crash handlers dynamically by introduc-
                                                               or taint tracking (e.g., Vuzzer [35]) have been proposed.
ing faults and analyzing the obtained traces. Finally, we
                                                               However, none of these techniques can easily be adapted
can always trace the whole kernel, taking a slight perfor-
                                                               to closed-source operating system kernels. Therefore it
mance hit (mostly introduced by the increased amount of
                                                               remains an open research problem how to deal with those
non-determinism). In cases such as syscall fuzzing, we
                                                               situations on the kernel level.
need to trace the whole kernel, therefore syscall fuzzing
would not be impacted if this ability was missing. In
summary, this is the first approach that can fuzz arbitrary    Ring 3 Fuzzing. We only demonstrated this technique
x86-64 kernels without any customization and a near-           against kernel-level code. However, the exact same tech-
native performance.                                            nique can be used to fuzz closed-source ring 3 code as



180    26th USENIX Security Symposium                                                               USENIX Association
well. Since our approach has a very modest tracing over-        [5] Linux 4.8, perf Documentation.     https:
head, we expect that this technique will outperform cur-            //git.kernel.org/cgit/linux/kernel/
rent dynamic binary instrumentation based techniques                git/torvalds/linux.git/plain/tools/perf/
for feedback fuzzing of closed-source ring 3 programs               Documentation/intel-pt.txt?id=refs/tags/
such as winAFL [20].                                                v4.8. Accessed: 2017-06-29.

8   Conclusion                                                  [6] Peach. http://www.peachfuzzer.com/.            Ac-
                                                                    cessed: 2017-06-29.
The latest generation of feedback-driven fuzzing meth-          [7] Project Triforce: Run AFL on Everything!
ods has proven to be an effective approach to find vul-             https://www.nccgroup.trust/us/about-
nerabilities in an automated and comprehensive fashion.             us/newsroom-and-events/blog/2016/june/
Recent work has also demonstrated that such techniques              project-triforce-run-afl-on-everything/.
can be applied to kernel space. While previous feedback-            Accessed: 2017-06-29.
driven kernel fuzzers were able to find a large amount of
security flaws in certain operating systems, their benefit      [8] Security oriented fuzzer with powerful analysis op-
was either limited by poor performance due to CPU emu-              tions. https://github.com/google/honggfuzz.
lation or a lack of portability due to the need for compile-        Accessed: 2017-06-29.
time instrumentations.
                                                                [9] Sulley. https://github.com/OpenRCE/sulley.
   In this paper, we presented a novel mechanism to uti-
                                                                    Accessed: 2017-06-29.
lize the latest CPU features for a feedback-driven kernel
fuzzer. As shown in the evaluation, combining all com-         [10] syzkaller: Linux syscall fuzzer. https://github.
ponents provides the ability to apply kernel fuzz testing           com/google/syzkaller. Accessed: 2017-06-29.
to any target OS with significantly better performance
than the alternative approaches.                               [11] Trinity: Linux system call fuzzer.  https:
                                                                    //github.com/kernelslacker/trinity.    Ac-
                                                                    cessed: 2017-06-29.
Acknowledgment
                                                               [12] zzuf. https://github.com/samhocevar/zzuf.
This work was supported by the German Federal
                                                                    Accessed: 2017-06-29.
Ministry of Education and Research (BMBF Grant
16KIS0592K HWSec). We would like to thank our shep-            [13] O. Bastani, R. Sharma, A. Aiken, and P. Liang.
herd Suman Jana for his support in finalizing this paper            Synthesizing program input grammars. In ACM
and the anonymous reviewers for their constructive and              SIGPLAN Conference on Programming Language
valuable comments. Furthermore, we would also like                  Design and Implementation (PLDI), 2017.
to thank Ralf Spenneberg and Hendrik Schwartke from
OpenSource Security for supporting this research. Fi-          [14] M. Böhme, V.-T. Pham, and A. Roychoudhury.
nally, we would like to thank Ali Abbasi, Tim Blazytko,             Coverage-based greybox fuzzing as markov chain.
Teemu Rytilahti and Christine Utz for their valuable                In ACM Conference on Computer and Communica-
feedback.                                                           tions Security (CCS), 2016.
                                                               [15] C. Cadar, D. Dunbar, and D. R. Engler. Klee: Unas-
References                                                          sisted and automatic generation of high-coverage
                                                                    tests for complex systems programs. In Symposium
 [1] Announcing oss-fuzz: Continuous fuzzing for                    on Operating Systems Design and Implementation
     open source software.     https://testing.                     (OSDI), 2008.
     googleblog.com/2016/12/announcing-oss-
                                                               [16] S. K. Cha, T. Avgerinos, A. Rebert, and D. Brum-
     fuzz-continuous-fuzzing.html.     Accessed:
                                                                    ley. Unleashing Mayhem on Binary Code. In IEEE
     2017-06-29.
                                                                    Symposium on Security and Privacy, 2012.
 [2] Capstone disassembly framework. http://www.
                                                               [17] S. K. Cha, M. Woo, and D. Brumley. Program-
     capstone-engine.org/. Accessed: 2017-06-29.
                                                                    adaptive mutational fuzzing. In IEEE Symposium
 [3] A general-purpose fuzzer. https://github.com/                  on Security and Privacy, 2015.
     aoh/radamsa. Accessed: 2017-06-29.
                                                               [18] Z. Deng, X. Zhang, and D. Xu. Spider: Stealthy
 [4] Intel Processor Trace Decoder Library. https:                  binary program instrumentation and debugging via
     //github.com/01org/processor-trace.       Ac-                  hardware virtualization. In Annual Computer Secu-
     cessed: 2017-06-29.                                            rity Applications Conference (ACSAC), 2013.



USENIX Association                                                            26th USENIX Security Symposium       181
[19] A. Dinaburg, P. Royal, M. Sharif, and W. Lee.         [33] D. Molnar, X. C. Li, and D. Wagner. Dynamic
     Ether: malware analysis via hardware virtualization        Test Generation to Find Integer Bugs in x86 Binary
     extensions. In ACM Conference on Computer and              Linux Programs. In USENIX Security Symposium,
     Communications Security (CCS), 2008.                       2009.
[20] Fratric, Ivan.  WinAFL: A fork of AFL for             [34] V. Nossum and Q. Casasnovas. Filesystem Fuzzing
     fuzzing Windows binaries. https://github.com/              with American Fuzzy Lop. Vault 2016, 2016.
     ivanfratric/winafl, 2017.
                                                           [35] S. Rawat, V. Jain, A. Kumar, L. Cojocar, C. Giuf-
[21] V. Ganesh, T. Leek, and M. Rinard. Taint-based             frida, and H. Bos. Vuzzer: Application-aware evo-
     directed whitebox fuzzing. In International Con-           lutionary fuzzing. In Symposium on Network and
     ference on Software Engineering (ICSE), 2009.              Distributed System Security (NDSS), 2017.
[22] P. Godefroid, N. Klarlund, and K. Sen. DART: Di-      [36] A. Rebert, S. K. Cha, T. Avgerinos, J. M. Foote,
     rected Automated Random Testing. In ACM SIG-               D. Warren, G. Grieco, and D. Brumley. Optimiz-
     PLAN Conference on Programming Language De-                ing seed selection for fuzzing. In USENIX Security
     sign and Implementation (PLDI), 2005.                      Symposium, 2014.
[23] P. Godefroid, M. Y. Levin, and D. Molnar. SAGE:       [37] N. Stephens, J. Grosen, C. Salls, A. Dutcher,
     Whitebox Fuzzing for Security Testing. Queue,              R. Wang, J. Corbetta, Y. Shoshitaishvili,
     10(1):20, 2012.                                            C. Kruegel, and G. Vigna. Driller: Augment-
[24] P. Godefroid, H. Peleg, and R. Singh. Learn&fuzz:          ing fuzzing through selective symbolic execution.
     Machine learning for input fuzzing. Technical re-          In Symposium on Network and Distributed System
     port, January 2017.                                        Security (NDSS), 2016.

[25] P. Goodman. Shin GRR: Make Fuzzing Fast               [38] J. Viide, A. Helin, M. Laakso, P. Pietikäinen,
     Again. https://blog.trailofbits.com/2016/                  M. Seppänen, K. Halunen, R. Puuperä, and J. Rön-
     11/02/shin-grr-make-fuzzing-fast-again/.                   ing. Experiences with model inference assisted
     Accessed: 2017-06-29.                                      fuzzing. In USENIX Workshop on Offensive Tech-
                                                                nologies (WOOT), 2008.
[26] I. Haller, A. Slowinska, M. Neugschwandtner, and
     H. Bos. Dowsing for overflows: A guided fuzzer to     [39] T. Wang, T. Wei, G. Gu, and W. Zou. TaintScope:
     find buffer boundary violations. In USENIX Secu-           A checksum-aware directed fuzzing tool for auto-
     rity Symposium, 2013.                                      matic software vulnerability detection. In IEEE
                                                                Symposium on Security and Privacy, 2010.
[27] C. Holler, K. Herzig, and A. Zeller. Fuzzing with
     code fragments. In USENIX Security Symposium,         [40] M. Woo, S. K. Cha, S. Gottlieb, and D. Brumley.
     2012.                                                      Scheduling black-box mutational fuzzing. In ACM
                                                                Conference on Computer and Communications Se-
[28] Intel. Intel® 64 and IA-32 Architectures Soft-
                                                                curity (CCS), 2013.
     ware Developer’s Manual (Order number: 325384-
     058US, April 2016).
[29] A. Kleen. simple-pt: Simple Intel CPU pro-
     cessor tracing on Linux. https://github.com/
     andikleen/simple-pt.
[30] A. Kleen and B. Strong. Intel Processor Trace on
     Linux. Tracing Summit 2015, 2015.
[31] Microsoft.    FSCTL_DISMOUNT_VOLUME.
     https://msdn.microsoft.com/en-us/
     library/windows/desktop/aa364562(v=
     vs.85).aspx, 2017.
[32] Microsoft.    VHD Reference.       https:
     //msdn.microsoft.com/en-us/library/
     windows/desktop/dd323700(v=vs.85).aspx,
     2017.



182   26th USENIX Security Symposium                                                         USENIX Association
