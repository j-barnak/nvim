### 25.4 Defining Signal Handlers

This section describes how to write a signal handler function that can be established with the `signal` or `sigaction` functions.

A signal handler is just a function that you compile together with the rest of the program. Instead of directly invoking the function, you use `signal` or `sigaction` to tell the operating system to call it when a signal arrives. This is known as *establishing* the handler. See [Specifying Signal Actions](https://sourceware.org/glibc/manual/latest/html_node/Signal-Actions.html).

There are two basic strategies you can use in signal handler functions:

- You can have the handler function note that the signal arrived by tweaking some global data structures, and then return normally.
- You can have the handler function terminate the program or transfer control to a point where it can recover from the situation that caused the signal.

You need to take special care in writing handler functions because they can be called asynchronously. That is, a handler might be called at any point in the program, unpredictably. If two signals arrive during a very short interval, one handler can run within another. This section describes what your handler should do, and what you should avoid.

- [Signal Handlers that Return](https://sourceware.org/glibc/manual/latest/html_node/Handler-Returns.html)
- [Handlers That Terminate the Process](https://sourceware.org/glibc/manual/latest/html_node/Termination-in-Handler.html)
- [Nonlocal Control Transfer in Handlers](https://sourceware.org/glibc/manual/latest/html_node/Longjmp-in-Handler.html)
- [Signals Arriving While a Handler Runs](https://sourceware.org/glibc/manual/latest/html_node/Signals-in-Handler.html)
- [Signals Close Together Merge into One](https://sourceware.org/glibc/manual/latest/html_node/Merged-Signals.html)
- [Signal Handling and Nonreentrant Functions](https://sourceware.org/glibc/manual/latest/html_node/Nonreentrancy.html)
- [Atomic Data Access and Signal Handling](https://sourceware.org/glibc/manual/latest/html_node/Atomic-Data-Access.html)
