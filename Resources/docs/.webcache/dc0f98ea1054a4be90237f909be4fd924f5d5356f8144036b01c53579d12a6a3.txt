[std](https://doc.rust-lang.org/std/index.html)::[net](https://doc.rust-lang.org/std/net/index.html)

# Struct TcpListener Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#98)

``` rust
pub struct TcpListener(/* private fields */);
```

Expand description

A TCP socket server, listening for connections.

After creating a `TcpListener` by [`bind`](https://doc.rust-lang.org/std/net/struct.TcpListener.html#method.bind "associated function std::net::TcpListener::bind")ing it to a socket address, it listens for incoming TCP connections. These can be accepted by calling [`accept`](https://doc.rust-lang.org/std/net/struct.TcpListener.html#method.accept "method std::net::TcpListener::accept") or by iterating over the [`Incoming`](https://doc.rust-lang.org/std/net/struct.Incoming.html "struct std::net::Incoming") iterator returned by [`incoming`](https://doc.rust-lang.org/std/net/struct.TcpListener.html#method.incoming "method std::net::TcpListener::incoming").

The socket will be closed when the value is dropped.

The Transmission Control Protocol is specified in [IETF RFC 793](https://tools.ietf.org/html/rfc793).

## Examples

``` rust
use std::net::{TcpListener, TcpStream};

fn handle_client(stream: TcpStream) {
    // ...
}

fn main() -> std::io::Result<()> {
    let listener = TcpListener::bind("127.0.0.1:80")?;

    // accept connections and process them serially
    for stream in listener.incoming() {
        handle_client(stream?);
    }
    Ok(())
}
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#784-1113)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#834-836)

Creates a new `TcpListener` which will be bound to the specified address.

The returned listener is ready for accepting connections.

Binding with a port number of 0 will request that the OS assigns a port to this listener. The port allocated can be queried via the [`TcpListener::local_addr`](https://doc.rust-lang.org/std/net/struct.TcpListener.html#method.local_addr "method std::net::TcpListener::local_addr") method.

The address type can be any implementor of [`ToSocketAddrs`](https://doc.rust-lang.org/std/net/trait.ToSocketAddrs.html "trait std::net::ToSocketAddrs") trait. See its documentation for concrete examples.

If `addr` yields multiple addresses, `bind` will be attempted with each of the addresses until one succeeds and returns the listener. If none of the addresses succeed in creating a listener, the error returned from the last attempt (the last address) is returned.

##### Examples

Creates a TCP listener bound to `127.0.0.1:80`:

``` rust
use std::net::TcpListener;

let listener = TcpListener::bind("127.0.0.1:80").unwrap();
```

Creates a TCP listener bound to `127.0.0.1:80`. If that fails, create a TCP listener bound to `127.0.0.1:443`:

``` rust
use std::net::{SocketAddr, TcpListener};

let addrs = [
    SocketAddr::from(([127, 0, 0, 1], 80)),
    SocketAddr::from(([127, 0, 0, 1], 443)),
];
let listener = TcpListener::bind(&addrs[..]).unwrap();
```

Creates a TCP listener bound to a port assigned by the operating system at `127.0.0.1`.

``` rust
use std::net::TcpListener;

let socket = TcpListener::bind("127.0.0.1:0").unwrap();
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#850-852)

Returns the local socket address of this listener.

##### Examples

``` rust
use std::net::{Ipv4Addr, SocketAddr, SocketAddrV4, TcpListener};

let listener = TcpListener::bind("127.0.0.1:8080").unwrap();
assert_eq!(listener.local_addr().unwrap(),
           SocketAddr::V4(SocketAddrV4::new(Ipv4Addr::new(127, 0, 0, 1), 8080)));
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#869-871)

Creates a new independently owned handle to the underlying socket.

The returned [`TcpListener`](https://doc.rust-lang.org/std/net/struct.TcpListener.html "struct std::net::TcpListener") is a reference to the same socket that this object references. Both handles can be used to accept incoming connections and options set on one listener will affect the other.

##### Examples

``` rust
use std::net::TcpListener;

let listener = TcpListener::bind("127.0.0.1:8080").unwrap();
let listener_clone = listener.try_clone().unwrap();
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#914-919)

Accept a new incoming connection from this listener.

This function will block the calling thread until a new TCP connection is established. When established, the corresponding [`TcpStream`](https://doc.rust-lang.org/std/net/struct.TcpStream.html "struct std::net::TcpStream") and the remote peer’s address will be returned.

##### Errors

Some errors this function returns do not indicate a problem with the listener itself, and a program serving a long-lived listener will usually want to handle them and keep accepting connections rather than treat them as fatal. These include, but are not limited to:

- An error specific to a single incoming connection that failed before it could be accepted, such as one aborted by the peer ([`ConnectionAborted`](https://doc.rust-lang.org/std/io/enum.ErrorKind.html#variant.ConnectionAborted "variant std::io::ErrorKind::ConnectionAborted")). A later call may succeed immediately.
- An error from reaching the per-process or system-wide open file descriptor limit. The call can be retried once other file descriptors have been closed, typically after a short delay.
- An error from failing to allocate memory while accepting a connection ([`OutOfMemory`](https://doc.rust-lang.org/std/io/enum.ErrorKind.html#variant.OutOfMemory "variant std::io::ErrorKind::OutOfMemory")).

Which errors can occur is platform-specific. On Unix, [`Interrupted`](https://doc.rust-lang.org/std/io/enum.ErrorKind.html#variant.Interrupted "variant std::io::ErrorKind::Interrupted") errors are retried internally rather than being returned.

##### Examples

``` rust
use std::net::TcpListener;

let listener = TcpListener::bind("127.0.0.1:8080").unwrap();
match listener.accept() {
    Ok((_socket, addr)) => println!("new client: {addr:?}"),
    Err(e) => println!("couldn't get client: {e:?}"),
}
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#957-959)

Returns an iterator over the connections being received on this listener.

The returned iterator will never return [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") and will also not yield the peer’s [`SocketAddr`](https://doc.rust-lang.org/std/net/enum.SocketAddr.html "enum std::net::SocketAddr") structure. Iterating over it is equivalent to calling [`TcpListener::accept`](https://doc.rust-lang.org/std/net/struct.TcpListener.html#method.accept "method std::net::TcpListener::accept") in a loop.

##### Errors

Each connection yielded by the iterator can fail for the same reasons as [`TcpListener::accept`](https://doc.rust-lang.org/std/net/struct.TcpListener.html#method.accept "method std::net::TcpListener::accept"); see its documentation for details.

##### Examples

``` rust
use std::net::{TcpListener, TcpStream};

fn handle_connection(stream: TcpStream) {
   //...
}

fn main() -> std::io::Result<()> {
    let listener = TcpListener::bind("127.0.0.1:80")?;

    for stream in listener.incoming() {
        match stream {
            Ok(stream) => {
                handle_connection(stream);
            }
            Err(e) => { /* connection failed */ }
        }
    }
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#994-996)

🔬This is a nightly-only experimental API. (`tcplistener_into_incoming` [\#88373](https://github.com/rust-lang/rust/issues/88373))

Turn this into an iterator over the connections being received on this listener.

The returned iterator will never return [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") and will also not yield the peer’s [`SocketAddr`](https://doc.rust-lang.org/std/net/enum.SocketAddr.html "enum std::net::SocketAddr") structure. Iterating over it is equivalent to calling [`TcpListener::accept`](https://doc.rust-lang.org/std/net/struct.TcpListener.html#method.accept "method std::net::TcpListener::accept") in a loop.

##### Errors

Each connection yielded by the iterator can fail for the same reasons as [`TcpListener::accept`](https://doc.rust-lang.org/std/net/struct.TcpListener.html#method.accept "method std::net::TcpListener::accept"); see its documentation for details.

##### Examples

``` rust
#![feature(tcplistener_into_incoming)]
use std::net::{TcpListener, TcpStream};

fn listen_on(port: u16) -> impl Iterator<Item = TcpStream> {
    let listener = TcpListener::bind(("127.0.0.1", port)).unwrap();
    listener.into_incoming()
        .filter_map(Result::ok) /* Ignore failed connections */
}

fn main() -> std::io::Result<()> {
    for stream in listen_on(80) {
        /* handle the connection here */
    }
    Ok(())
}
```

1.9.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#1012-1014)

Sets the value for the `IP_TTL` option on this socket.

This value sets the time-to-live field that is used in every packet sent from this socket.

##### Examples

``` rust
use std::net::TcpListener;

let listener = TcpListener::bind("127.0.0.1:80").unwrap();
listener.set_ttl(100).expect("could not set TTL");
```

1.9.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#1030-1032)

Gets the value of the `IP_TTL` option for this socket.

For more information about this option, see [`TcpListener::set_ttl`](https://doc.rust-lang.org/std/net/struct.TcpListener.html#method.set_ttl "method std::net::TcpListener::set_ttl").

##### Examples

``` rust
use std::net::TcpListener;

let listener = TcpListener::bind("127.0.0.1:80").unwrap();
listener.set_ttl(100).expect("could not set TTL");
assert_eq!(listener.ttl().unwrap_or(0), 100);
```

1.9.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#1037-1039)

👎Deprecated since 1.16.0:

this option can only be set before the socket is bound

1.9.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#1044-1046)

👎Deprecated since 1.16.0:

this option can only be set before the socket is bound

1.9.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#1063-1065)

Gets the value of the `SO_ERROR` option on this socket.

This will retrieve the stored error in the underlying socket, clearing the field in the process. This can be useful for checking errors between calls.

##### Examples

``` rust
use std::net::TcpListener;

let listener = TcpListener::bind("127.0.0.1:80").unwrap();
listener.take_error().expect("No error was expected");
```

1.9.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#1110-1112)

Moves this TCP stream into or out of nonblocking mode.

This will result in the `accept` operation becoming nonblocking, i.e., immediately returning from their calls. If the IO operation is successful, `Ok` is returned and no further action is required. If the IO operation could not be completed and needs to be retried, an error with kind [`io::ErrorKind::WouldBlock`](https://doc.rust-lang.org/std/io/enum.ErrorKind.html#variant.WouldBlock "variant std::io::ErrorKind::WouldBlock") is returned.

On most Unix platforms, calling this method corresponds to calling `ioctl` `FIONBIO`. On Windows, calling this method corresponds to calling `ioctlsocket` `FIONBIO`.

##### Examples

Bind a TCP listener to an address, listen for connections, and read bytes in nonblocking mode:

``` rust
use std::io;
use std::net::TcpListener;

let listener = TcpListener::bind("127.0.0.1:7878").unwrap();
listener.set_nonblocking(true).expect("Cannot set non-blocking");

for stream in listener.incoming() {
    match stream {
        Ok(s) => {
            // do something with the TcpStream
            handle_connection(s);
        }
        Err(ref e) if e.kind() == io::ErrorKind::WouldBlock => {
            // wait until network socket is ready, typically implemented
            // via platform-specific APIs such as epoll or IOCP
            wait_for_fd();
            continue;
        }
        Err(e) => panic!("encountered IO error: {e}"),
    }
}
```

## Trait Implementations

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/owned.rs.html#375-380)

Available on **(Hermit or Motor OS or Trusty or Unix or WASI) and non-Trusty** only.

[Source](https://doc.rust-lang.org/src/std/os/fd/owned.rs.html#377-379)

Borrows the file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.AsFd.html#tymethod.as_fd)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/net.rs.html#17)

Available on **(Hermit or Motor OS or Trusty or Unix or WASI) and non-Trusty** only.

[Source](https://doc.rust-lang.org/src/std/os/fd/net.rs.html#17)

Extracts the raw file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.AsRawFd.html#tymethod.as_raw_fd)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#245-250)

Available on **Windows** only.

[Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#247-249)

Extracts the raw socket. [Read more](https://doc.rust-lang.org/std/os/windows/io/trait.AsRawSocket.html#tymethod.as_raw_socket)

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/socket.rs.html#330-335)

Available on **Windows** only.

[Source](https://doc.rust-lang.org/src/std/os/windows/io/socket.rs.html#332-334)

Borrows the socket.

1.0.0 · [Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#1163-1167)

[Source](https://doc.rust-lang.org/src/std/net/tcp.rs.html#1164-1166)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/owned.rs.html#394-401)

Available on **(Hermit or Motor OS or Trusty or Unix or WASI) and non-Trusty** only.

[Source](https://doc.rust-lang.org/src/std/os/fd/owned.rs.html#396-400)

Converts to this type from the input type.

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/socket.rs.html#347-352)

Available on **Windows** only.

[Source](https://doc.rust-lang.org/src/std/os/windows/io/socket.rs.html#349-351)

Converts to this type from the input type.

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/socket.rs.html#338-344)

Available on **Windows** only.

[Source](https://doc.rust-lang.org/src/std/os/windows/io/socket.rs.html#341-343)

Takes ownership of a [`TcpListener`](https://doc.rust-lang.org/std/net/struct.TcpListener.html "struct std::net::TcpListener")’s socket.

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/owned.rs.html#384-390)

Available on **(Hermit or Motor OS or Trusty or Unix or WASI) and non-Trusty** only.

[Source](https://doc.rust-lang.org/src/std/os/fd/owned.rs.html#387-389)

Takes ownership of a [`TcpListener`](https://doc.rust-lang.org/std/net/struct.TcpListener.html "struct std::net::TcpListener")’s socket file descriptor.

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/net.rs.html#33)

Available on **(Hermit or Motor OS or Trusty or Unix or WASI) and non-Trusty** only.

[Source](https://doc.rust-lang.org/src/std/os/fd/net.rs.html#33)

Constructs a new instance of `Self` from the given raw file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.FromRawFd.html#tymethod.from_raw_fd)

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#270-278)

Available on **Windows** only.

[Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#272-277)

Constructs a new I/O object from the specified raw socket. [Read more](https://doc.rust-lang.org/std/os/windows/io/trait.FromRawSocket.html#tymethod.from_raw_socket)

1.4.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/net.rs.html#46)

Available on **(Hermit or Motor OS or Trusty or Unix or WASI) and non-Trusty** only.

[Source](https://doc.rust-lang.org/src/std/os/fd/net.rs.html#46)

Consumes this object, returning the raw underlying file descriptor. [Read more](https://doc.rust-lang.org/std/os/fd/trait.IntoRawFd.html#tymethod.into_raw_fd)

1.4.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#299-304)

Available on **Windows** only.

[Source](https://doc.rust-lang.org/src/std/os/windows/io/raw.rs.html#301-303)

Consumes this object, returning the raw underlying socket. [Read more](https://doc.rust-lang.org/std/os/windows/io/trait.IntoRawSocket.html#tymethod.into_raw_socket)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.