[std](https://doc.rust-lang.org/std/index.html)::[thread](https://doc.rust-lang.org/std/thread/index.html)

# Function park_timeout_ms Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/thread/functions.rs.html#553-555)

``` rust
pub fn park_timeout_ms(ms: u32)
```

👎Deprecated since 1.6.0:

replaced by `std::thread::park_timeout`

Expand description

Uses [`park_timeout`](https://doc.rust-lang.org/std/thread/fn.park_timeout.html "fn std::thread::park_timeout").

Blocks unless or until the current thread’s token is made available or the specified duration has been reached (may wake spuriously).

The semantics of this function are equivalent to [`park`](https://doc.rust-lang.org/std/thread/fn.park.html "fn std::thread::park") except that the thread will be blocked for roughly no longer than `dur`. This method should not be used for precise timing due to anomalies such as preemption or platform differences that might not cause the maximum amount of time waited to be precisely `ms` long.

See the [park documentation](https://doc.rust-lang.org/std/thread/fn.park.html "fn std::thread::park") for more detail.