[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[windows](https://doc.rust-lang.org/std/os/windows/index.html)::[process](https://doc.rust-lang.org/std/os/windows/process/index.html)

# Trait ExitCodeExt Copy item path

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#454-463)

``` rust
pub trait ExitCodeExt {
    // Required method
    fn from_raw(raw: u32) -> Self;
}
```

ⓘ *This trait cannot be implemented outside `std`.*

🔬This is a nightly-only experimental API. (`windows_process_exit_code_from` [\#111688](https://github.com/rust-lang/rust/issues/111688))

Available on **Windows** only.

Expand description

Windows-specific extensions to [`process::ExitCode`](https://doc.rust-lang.org/std/process/struct.ExitCode.html "struct std::process::ExitCode").

## Required Methods

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#462)

🔬This is a nightly-only experimental API. (`windows_process_exit_code_from` [\#111688](https://github.com/rust-lang/rust/issues/111688))

Creates a new `ExitCode` from the raw underlying `u32` return value of a process.

The exit code should not be 259, as this conflicts with the `STILL_ACTIVE` macro returned from the `GetExitCodeProcess` function to signal that the process has yet to run to completion.

## Dyn Compatibility

This trait is **not** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#466-470)