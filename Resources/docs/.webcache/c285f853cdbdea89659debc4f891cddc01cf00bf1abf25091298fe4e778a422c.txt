[std](https://doc.rust-lang.org/std/index.html)::[process](https://doc.rust-lang.org/std/process/index.html)

# Struct Command Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#592-594)

``` rust
pub struct Command { /* private fields */ }
```

Expand description

A process builder, providing fine-grained control over how a new process should be spawned.

A default configuration can be generated using `Command::new(program)`, where `program` gives a path to the program to be executed. Additional builder methods allow the configuration to be changed (for example, by adding arguments) prior to spawning:

``` rust
use std::process::Command;

let output = if cfg!(target_os = "windows") {
    Command::new("cmd")
        .args(["/C", "echo hello"])
        .output()
        .expect("failed to execute process")
} else {
    Command::new("sh")
        .arg("-c")
        .arg("echo hello")
        .output()
        .expect("failed to execute process")
};

let hello = output.stdout;
```

`Command` can be reused to spawn multiple processes. The builder methods change the command without needing to immediately spawn the process.

``` rust
use std::process::Command;

let mut echo_hello = Command::new("sh");
echo_hello.arg("-c").arg("echo hello");
let hello_1 = echo_hello.output().expect("failed to execute process");
let hello_2 = echo_hello.output().expect("failed to execute process");
```

Similarly, you can call builder methods after spawning a process and then spawn a new process with the modified settings.

``` rust
use std::process::Command;

let mut list_dir = Command::new("ls");

// Execute `ls` in the current directory of the program.
list_dir.status().expect("process failed to execute");

println!();

// Change `ls` to execute in the root directory.
list_dir.current_dir("/");

// And then execute `ls` again but in the root directory.
list_dir.status().expect("process failed to execute");
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/process.rs.html#596-1337)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#686-688)

Constructs a new `Command` for launching the program at path `program`, with the following default configuration:

- No arguments to the program
- Inherit the current process’s environment
- Inherit the current process’s working directory
- Inherit stdin/stdout/stderr for [`spawn`](https://doc.rust-lang.org/std/process/struct.Command.html#method.spawn "method std::process::Command::spawn") or [`status`](https://doc.rust-lang.org/std/process/struct.Command.html#method.status "method std::process::Command::status"), but create pipes for [`output`](https://doc.rust-lang.org/std/process/struct.Command.html#method.output "method std::process::Command::output")

Builder methods are provided to change these defaults and otherwise configure the process.

If `program` is not an absolute path, the `PATH` environment variable will be searched in an OS-defined way.

##### Platform-specific behavior

The details below describe the current behavior, but these details may change in future versions of Rust.

On Unix, the `PATH` searched comes from the child’s environment:

- If the environment is unmodified, the child inherits the parent’s `PATH` and that is what is searched.
- If `PATH` is explicitly set via [`env`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env "method std::process::Command::env"), that new value is searched.
- If [`env_clear`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_clear "method std::process::Command::env_clear") or [`env_remove`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_remove "method std::process::Command::env_remove") removes `PATH` without a replacement, `execvp` falls back to an OS-defined default (typically `/bin:/usr/bin`), **not** the parent’s `PATH`. This may fail to find programs that rely on the parent’s `PATH`.

To avoid surprises, use an absolute path or explicitly set `PATH` on the `Command` when modifying the child’s environment.

On Windows, Rust resolves the executable path before spawning, rather than passing the name to `CreateProcessW` for resolution. When `program` is not an absolute path, the following locations are searched in order:

1.  The child’s `PATH`, if explicitly set via [`env`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env "method std::process::Command::env").
2.  The directory of the current executable.
3.  The system directory (`GetSystemDirectoryW`).
4.  The Windows directory (`GetWindowsDirectoryW`).
5.  The parent process’s `PATH`.

Note: when `PATH` is cleared via [`env_clear`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_clear "method std::process::Command::env_clear") or [`env_remove`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_remove "method std::process::Command::env_remove") on Windows, step 1 is skipped but the parent process’s `PATH` is still searched at step 5, unlike on Unix.

For executable files, the `.exe` extension may be omitted. Files with other extensions must include the extension, otherwise they will not be found. Note that this behavior has some known limitations (see issue \#37519).

##### Examples

``` rust
use std::process::Command;

Command::new("sh")
    .spawn()
    .expect("sh command failed to start");
```

##### Caveats

[`Command::new`](https://doc.rust-lang.org/std/process/struct.Command.html#method.new "associated function std::process::Command::new") is only intended to accept the path of the program. If you pass a program path along with arguments like `Command::new("ls -l").spawn()`, it will try to search for `ls -l` literally. The arguments need to be passed separately, such as via [`arg`](https://doc.rust-lang.org/std/process/struct.Command.html#method.arg "method std::process::Command::arg") or [`args`](https://doc.rust-lang.org/std/process/struct.Command.html#method.args "method std::process::Command::args").

``` rust
use std::process::Command;

Command::new("ls")
    .arg("-l") // arg passed separately
    .spawn()
    .expect("ls command failed to start");
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#749-752)

Adds an argument to pass to the program.

Only one argument can be passed per use. So instead of:

``` rust
.arg("-C /path/to/repo")
```

usage would be:

``` rust
.arg("-C")
.arg("/path/to/repo")
```

To pass multiple arguments see [`args`](https://doc.rust-lang.org/std/process/struct.Command.html#method.args "method std::process::Command::args").

Note that the argument is not passed through a shell, but given literally to the program. This means that shell syntax like quotes, escaped characters, word splitting, glob patterns, variable substitution, etc. have no effect.

On Windows, use caution with untrusted inputs. Most applications use the standard convention for decoding arguments passed to them. These are safe to use with `arg`. However, some applications such as `cmd.exe` and `.bat` files use a non-standard way of decoding arguments. They are therefore vulnerable to malicious input.

In the case of `cmd.exe` this is especially important because a malicious argument can potentially run arbitrary shell commands.

See [Windows argument splitting](https://doc.rust-lang.org/std/process/index.html#windows-argument-splitting "mod std::process") for more details or [`raw_arg`](https://doc.rust-lang.org/std/os/windows/process/trait.CommandExt.html#tymethod.raw_arg "method std::os::windows::process::CommandExt::raw_arg") for manually implementing non-standard argument encoding.

##### Examples

``` rust
use std::process::Command;

Command::new("ls")
    .arg("-l")
    .arg("-a")
    .spawn()
    .expect("ls command failed to start");
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#795-804)

Adds multiple arguments to pass to the program.

To pass a single argument see [`arg`](https://doc.rust-lang.org/std/process/struct.Command.html#method.arg "method std::process::Command::arg").

Note that the arguments are not passed through a shell, but given literally to the program. This means that shell syntax like quotes, escaped characters, word splitting, glob patterns, variable substitution, etc. have no effect.

On Windows, use caution with untrusted inputs. Most applications use the standard convention for decoding arguments passed to them. These are safe to use with `arg`. However, some applications such as `cmd.exe` and `.bat` files use a non-standard way of decoding arguments. They are therefore vulnerable to malicious input.

In the case of `cmd.exe` this is especially important because a malicious argument can potentially run arbitrary shell commands.

See [Windows argument splitting](https://doc.rust-lang.org/std/process/index.html#windows-argument-splitting "mod std::process") for more details or [`raw_arg`](https://doc.rust-lang.org/std/os/windows/process/trait.CommandExt.html#tymethod.raw_arg "method std::os::windows::process::CommandExt::raw_arg") for manually implementing non-standard argument encoding.

##### Examples

``` rust
use std::process::Command;

Command::new("ls")
    .args(["-l", "-a"])
    .spawn()
    .expect("ls command failed to start");
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#831-838)

Inserts or updates an explicit environment variable mapping.

This method allows you to add an environment variable mapping to the spawned process or overwrite a previously set value. You can use [`Command::envs`](https://doc.rust-lang.org/std/process/struct.Command.html#method.envs "method std::process::Command::envs") to set multiple environment variables simultaneously.

Child processes will inherit environment variables from their parent process by default. Environment variables explicitly set using [`Command::env`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env "method std::process::Command::env") take precedence over inherited variables. You can disable environment variable inheritance entirely using [`Command::env_clear`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_clear "method std::process::Command::env_clear") or for a single key using [`Command::env_remove`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_remove "method std::process::Command::env_remove").

Note that environment variable names are case-insensitive (but case-preserving) on Windows and case-sensitive on all other platforms.

##### Examples

``` rust
use std::process::Command;

Command::new("ls")
    .env("PATH", "/bin")
    .spawn()
    .expect("ls command failed to start");
```

1.19.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#875-885)

Inserts or updates multiple explicit environment variable mappings.

This method allows you to add multiple environment variable mappings to the spawned process or overwrite previously set values. You can use [`Command::env`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env "method std::process::Command::env") to set a single environment variable.

Child processes will inherit environment variables from their parent process by default. Environment variables explicitly set using [`Command::envs`](https://doc.rust-lang.org/std/process/struct.Command.html#method.envs "method std::process::Command::envs") take precedence over inherited variables. You can disable environment variable inheritance entirely using [`Command::env_clear`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_clear "method std::process::Command::env_clear") or for a single key using [`Command::env_remove`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_remove "method std::process::Command::env_remove").

Note that environment variable names are case-insensitive (but case-preserving) on Windows and case-sensitive on all other platforms.

##### Examples

``` rust
use std::process::{Command, Stdio};
use std::env;
use std::collections::HashMap;

let filtered_env : HashMap<String, String> =
    env::vars().filter(|&(ref k, _)|
        k == "TERM" || k == "TZ" || k == "LANG" || k == "PATH"
    ).collect();

Command::new("printenv")
    .stdin(Stdio::null())
    .stdout(Stdio::inherit())
    .env_clear()
    .envs(&filtered_env)
    .spawn()
    .expect("printenv failed to start");
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#915-918)

Removes an explicitly set environment variable and prevents inheriting it from a parent process.

This method will remove the explicit value of an environment variable set via [`Command::env`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env "method std::process::Command::env") or [`Command::envs`](https://doc.rust-lang.org/std/process/struct.Command.html#method.envs "method std::process::Command::envs"). In addition, it will prevent the spawned child process from inheriting that environment variable from its parent process.

After calling [`Command::env_remove`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_remove "method std::process::Command::env_remove"), the value associated with its key from [`Command::get_envs`](https://doc.rust-lang.org/std/process/struct.Command.html#method.get_envs "method std::process::Command::get_envs") will be [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None").

To clear all explicitly set environment variables and disable all environment variable inheritance, you can use [`Command::env_clear`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_clear "method std::process::Command::env_clear").

##### Examples

Prevent any inherited `GIT_DIR` variable from changing the target of the `git` command, while allowing all other variables, like `GIT_AUTHOR_NAME`.

``` rust
use std::process::Command;

Command::new("git")
    .arg("commit")
    .env_remove("GIT_DIR")
    .spawn()?;
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#947-950)

Clears all explicitly set environment variables and prevents inheriting any parent process environment variables.

This method will remove all explicitly added environment variables set via [`Command::env`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env "method std::process::Command::env") or [`Command::envs`](https://doc.rust-lang.org/std/process/struct.Command.html#method.envs "method std::process::Command::envs"). In addition, it will prevent the spawned child process from inheriting any environment variable from its parent process.

After calling [`Command::env_clear`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_clear "method std::process::Command::env_clear"), the iterator from [`Command::get_envs`](https://doc.rust-lang.org/std/process/struct.Command.html#method.get_envs "method std::process::Command::get_envs") will be empty.

You can use [`Command::env_remove`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_remove "method std::process::Command::env_remove") to clear a single mapping.

##### Examples

The behavior of `sort` is affected by `LANG` and `LC_*` environment variables. Clearing the environment makes `sort`‘s behavior independent of the parent processes’ language.

``` rust
use std::process::Command;

Command::new("sort")
    .arg("file.txt")
    .env_clear()
    .spawn()?;
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#975-978)

Sets the working directory for the child process.

##### Platform-specific behavior

If the program path is relative (e.g., `"./script.sh"`), it’s ambiguous whether it should be interpreted relative to the parent’s working directory or relative to `current_dir`. The behavior in this case is platform specific and unstable, and it’s recommended to use [`canonicalize`](https://doc.rust-lang.org/std/fs/fn.canonicalize.html "fn std::fs::canonicalize") to get an absolute program path instead.

##### Examples

``` rust
use std::process::Command;

Command::new("ls")
    .current_dir("/bin")
    .spawn()
    .expect("ls command failed to start");
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1002-1005)

Configuration for the child process’s standard input (stdin) handle.

Defaults to [`inherit`](https://doc.rust-lang.org/std/process/struct.Stdio.html#method.inherit "associated function std::process::Stdio::inherit") when used with [`spawn`](https://doc.rust-lang.org/std/process/struct.Command.html#method.spawn "method std::process::Command::spawn") or [`status`](https://doc.rust-lang.org/std/process/struct.Command.html#method.status "method std::process::Command::status"), and defaults to [`piped`](https://doc.rust-lang.org/std/process/struct.Stdio.html#method.piped "associated function std::process::Stdio::piped") when used with [`output`](https://doc.rust-lang.org/std/process/struct.Command.html#method.output "method std::process::Command::output").

##### Examples

``` rust
use std::process::{Command, Stdio};

Command::new("ls")
    .stdin(Stdio::null())
    .spawn()
    .expect("ls command failed to start");
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1029-1032)

Configuration for the child process’s standard output (stdout) handle.

Defaults to [`inherit`](https://doc.rust-lang.org/std/process/struct.Stdio.html#method.inherit "associated function std::process::Stdio::inherit") when used with [`spawn`](https://doc.rust-lang.org/std/process/struct.Command.html#method.spawn "method std::process::Command::spawn") or [`status`](https://doc.rust-lang.org/std/process/struct.Command.html#method.status "method std::process::Command::status"), and defaults to [`piped`](https://doc.rust-lang.org/std/process/struct.Stdio.html#method.piped "associated function std::process::Stdio::piped") when used with [`output`](https://doc.rust-lang.org/std/process/struct.Command.html#method.output "method std::process::Command::output").

##### Examples

``` rust
use std::process::{Command, Stdio};

Command::new("ls")
    .stdout(Stdio::null())
    .spawn()
    .expect("ls command failed to start");
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1056-1059)

Configuration for the child process’s standard error (stderr) handle.

Defaults to [`inherit`](https://doc.rust-lang.org/std/process/struct.Stdio.html#method.inherit "associated function std::process::Stdio::inherit") when used with [`spawn`](https://doc.rust-lang.org/std/process/struct.Command.html#method.spawn "method std::process::Command::spawn") or [`status`](https://doc.rust-lang.org/std/process/struct.Command.html#method.status "method std::process::Command::status"), and defaults to [`piped`](https://doc.rust-lang.org/std/process/struct.Stdio.html#method.piped "associated function std::process::Stdio::piped") when used with [`output`](https://doc.rust-lang.org/std/process/struct.Command.html#method.output "method std::process::Command::output").

##### Examples

``` rust
use std::process::{Command, Stdio};

Command::new("ls")
    .stderr(Stdio::null())
    .spawn()
    .expect("ls command failed to start");
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1095-1097)

Executes the command as a child process, returning a handle to it.

By default, stdin, stdout and stderr are inherited from the parent.

##### Errors

This method returns an [`io::Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error") if the child process could not be spawned. Common reasons include:

- the program could not be found (for example, it does not exist, or, when given a bare name, it is not present in the `PATH`);
- the current process does not have permission to execute the program (for example, the file is not marked executable, or execution is denied by a security policy such as `seccomp`);
- the operating system could not create the new process because of resource exhaustion (for example, a limit on the number of processes was reached).

An error is only returned for failures that occur while the child is being spawned. Once the child has started successfully, anything that happens to it afterwards — including being terminated by a signal — is reported through its [`ExitStatus`](https://doc.rust-lang.org/std/process/struct.ExitStatus.html "struct std::process::ExitStatus") rather than as an error from the spawning method.

##### Examples

``` rust
use std::process::Command;

Command::new("ls")
    .spawn()
    .expect("ls command failed to start");
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1138-1141)

Executes the command as a child process, waiting for it to finish and collecting all of its output.

By default, stdout and stderr are captured (and used to provide the resulting output). Stdin is not inherited from the parent and any attempt by the child process to read from the stdin stream will result in the stream immediately closing.

##### Errors

Like [`spawn`](https://doc.rust-lang.org/std/process/struct.Command.html#method.spawn "method std::process::Command::spawn"), this method returns an [`io::Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error") if the child process could not be spawned; see [`spawn`](https://doc.rust-lang.org/std/process/struct.Command.html#method.spawn "method std::process::Command::spawn") for the common reasons. It may also return an error if reading the child’s output or waiting on the child fails.

Note that this method does **not** return an error if the child runs and then exits unsuccessfully, or is terminated by a signal. In those cases it still returns [`Ok`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Ok "variant std::result::Result::Ok"), and the outcome is reflected in the [`ExitStatus`](https://doc.rust-lang.org/std/process/struct.ExitStatus.html "struct std::process::ExitStatus") stored in the returned [`Output`](https://doc.rust-lang.org/std/process/struct.Output.html "struct std::process::Output").

##### Examples

[ⓘ](# "This example panics")

``` rust
use std::process::Command;
use std::io::{self, Write};
let output = Command::new("/bin/cat")
    .arg("file.txt")
    .output()?;

println!("status: {}", output.status);
io::stdout().write_all(&output.stdout)?;
io::stderr().write_all(&output.stderr)?;

assert!(output.status.success());
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1176-1181)

Executes a command as a child process, waiting for it to finish and collecting its status.

By default, stdin, stdout and stderr are inherited from the parent.

##### Errors

Like [`spawn`](https://doc.rust-lang.org/std/process/struct.Command.html#method.spawn "method std::process::Command::spawn"), this method returns an [`io::Error`](https://doc.rust-lang.org/std/io/struct.Error.html "struct std::io::Error") if the child process could not be spawned; see [`spawn`](https://doc.rust-lang.org/std/process/struct.Command.html#method.spawn "method std::process::Command::spawn") for the common reasons. It may also return an error if waiting on the child fails.

Note that this method does **not** return an error if the child runs and then exits unsuccessfully, or is terminated by a signal. In those cases it still returns [`Ok`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Ok "variant std::result::Result::Ok"), and the outcome is reflected in the returned [`ExitStatus`](https://doc.rust-lang.org/std/process/struct.ExitStatus.html "struct std::process::ExitStatus").

##### Examples

[ⓘ](# "This example panics")

``` rust
use std::process::Command;

let status = Command::new("/bin/cat")
    .arg("file.txt")
    .status()
    .expect("failed to execute process");

println!("process finished with: {status}");

assert!(status.success());
```

1.57.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1195-1197)

Returns the path to the program that was given to [`Command::new`](https://doc.rust-lang.org/std/process/struct.Command.html#method.new "associated function std::process::Command::new").

##### Examples

``` rust
use std::process::Command;

let cmd = Command::new("echo");
assert_eq!(cmd.get_program(), "echo");
```

1.57.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1217-1219)

Returns an iterator of the arguments that will be passed to the program.

This does not include the path to the program as the first argument; it only includes the arguments specified with [`Command::arg`](https://doc.rust-lang.org/std/process/struct.Command.html#method.arg "method std::process::Command::arg") and [`Command::args`](https://doc.rust-lang.org/std/process/struct.Command.html#method.args "method std::process::Command::args").

##### Examples

``` rust
use std::ffi::OsStr;
use std::process::Command;

let mut cmd = Command::new("echo");
cmd.arg("first").arg("second");
let args: Vec<&OsStr> = cmd.get_args().collect();
assert_eq!(args, &["first", "second"]);
```

1.57.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1253-1255)

Returns an iterator of the environment variables explicitly set for the child process.

Environment variables explicitly set using [`Command::env`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env "method std::process::Command::env"), [`Command::envs`](https://doc.rust-lang.org/std/process/struct.Command.html#method.envs "method std::process::Command::envs"), and [`Command::env_remove`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_remove "method std::process::Command::env_remove") can be retrieved with this method.

Note that this output does not include environment variables inherited from the parent process. To see the full list of environment variables, including those inherited from the parent process, use [`Command::get_resolved_envs`](https://doc.rust-lang.org/std/process/struct.Command.html#method.get_resolved_envs "method std::process::Command::get_resolved_envs").

Each element is a tuple key/value pair `(&OsStr, Option<&OsStr>)`. A [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") value indicates its key was explicitly removed via [`Command::env_remove`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_remove "method std::process::Command::env_remove"). The associated key for the [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") value will no longer inherit from its parent process.

An empty iterator can indicate that no explicit mappings were added or that [`Command::env_clear`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_clear "method std::process::Command::env_clear") was called. After calling [`Command::env_clear`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_clear "method std::process::Command::env_clear"), the child process will not inherit any environment variables from its parent process.

##### Examples

``` rust
use std::ffi::OsStr;
use std::process::Command;

let mut cmd = Command::new("ls");
cmd.env("TERM", "dumb").env_remove("TZ");
let envs: Vec<(&OsStr, Option<&OsStr>)> = cmd.get_envs().collect();
assert_eq!(envs, &[
    (OsStr::new("TERM"), Some(OsStr::new("dumb"))),
    (OsStr::new("TZ"), None)
]);
```

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1289-1291)

🔬This is a nightly-only experimental API. (`command_resolved_envs` [\#149070](https://github.com/rust-lang/rust/issues/149070))

Returns an iterator of the environment variables that will be set when the process is spawned.

This returns the environment as it would be if the command were executed at the time of calling this method. The returned environment includes:

- All inherited environment variables from the parent process (unless [`Command::env_clear`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_clear "method std::process::Command::env_clear") was called)
- All environment variables explicitly set via [`Command::env`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env "method std::process::Command::env") or [`Command::envs`](https://doc.rust-lang.org/std/process/struct.Command.html#method.envs "method std::process::Command::envs")
- Excluding any environment variables removed via [`Command::env_remove`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_remove "method std::process::Command::env_remove")

Note that the returned environment is a snapshot at the time this method is called and will not reflect any subsequent changes to the `Command` or the parent process’s environment. Additionally, it will not reflect changes made in a `pre_exec` hook (on Unix platforms).

Each element is a tuple `(OsString, OsString)` representing an environment variable key and value.

##### Examples

``` rust
#![feature(command_resolved_envs)]
use std::process::Command;
use std::ffi::{OsString, OsStr};
use std::env;
use std::collections::HashMap;

let mut cmd = Command::new("ls");
cmd.env("TZ", "UTC");
unsafe { env::set_var("EDITOR", "vim"); }

let resolved: HashMap<OsString, OsString> = cmd.get_resolved_envs().collect();
assert_eq!(resolved.get(OsStr::new("TZ")), Some(&OsString::from("UTC")));
assert_eq!(resolved.get(OsStr::new("EDITOR")), Some(&OsString::from("vim")));
```

1.57.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1310-1312)

Returns the working directory for the child process.

This returns [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") if the working directory will not be changed.

##### Examples

``` rust
use std::path::Path;
use std::process::Command;

let mut cmd = Command::new("ls");
assert_eq!(cmd.get_current_dir(), None);
cmd.current_dir("/bin");
assert_eq!(cmd.get_current_dir(), Some(Path::new("/bin")));
```

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1334-1336)

🔬This is a nightly-only experimental API. (`command_resolved_envs` [\#149070](https://github.com/rust-lang/rust/issues/149070))

Returns whether the environment will be cleared for the child process.

This returns `true` if [`Command::env_clear`](https://doc.rust-lang.org/std/process/struct.Command.html#method.env_clear "method std::process::Command::env_clear") was called, and `false` otherwise. When `true`, the child process will not inherit any environment variables from its parent process.

##### Examples

``` rust
#![feature(command_resolved_envs)]
use std::process::Command;

let mut cmd = Command::new("ls");
assert_eq!(cmd.get_env_clear(), false);

cmd.env_clear();
assert_eq!(cmd.get_env_clear(), true);
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/std/os/linux/process.rs.html#211-216)

Available on **Linux** only.

[Source](https://doc.rust-lang.org/src/std/os/linux/process.rs.html#212-215)

🔬This is a nightly-only experimental API. (`linux_pidfd` [\#82971](https://github.com/rust-lang/rust/issues/82971))

Sets whether a [`PidFd`](https://doc.rust-lang.org/std/os/linux/process/struct.PidFd.html "struct std::os::linux::process::PidFd") should be created for the [`Child`](https://doc.rust-lang.org/std/process/struct.Child.html "struct std::process::Child") spawned by this [`Command`](https://doc.rust-lang.org/std/process/struct.Command.html "struct std::process::Command"). By default, no pidfd will be created. [Read more](https://doc.rust-lang.org/std/os/linux/process/trait.CommandExt.html#tymethod.create_pidfd)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#223-275)

Available on **Unix** only.

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#224-227)

Sets the child process’s user ID. This translates to a `setuid` call in the child process. Failure in the `setuid` call will cause the spawn to fail. [Read more](https://doc.rust-lang.org/std/os/unix/process/trait.CommandExt.html#tymethod.uid)

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#229-232)

Similar to `uid`, but sets the group ID of the child process. This has the same semantics as the `uid` field.

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#234-237)

🔬This is a nightly-only experimental API. (`setgroups` [\#90747](https://github.com/rust-lang/rust/issues/90747))

Sets the supplementary group IDs for the calling process. Translates to a `setgroups` call in the child process.

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#239-245)

Schedules a closure to be run just before the `exec` function is invoked. [Read more](https://doc.rust-lang.org/std/os/unix/process/trait.CommandExt.html#tymethod.pre_exec)

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#247-251)

Performs all the required setup by this `Command`, followed by calling the `execvp` syscall. [Read more](https://doc.rust-lang.org/std/os/unix/process/trait.CommandExt.html#tymethod.exec)

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#253-259)

Set executable argument [Read more](https://doc.rust-lang.org/std/os/unix/process/trait.CommandExt.html#tymethod.arg0)

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#261-264)

Sets the process group ID (PGID) of the child process. Equivalent to a `setpgid` call in the child process, but may be more efficient. [Read more](https://doc.rust-lang.org/std/os/unix/process/trait.CommandExt.html#tymethod.process_group)

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#266-269)

🔬This is a nightly-only experimental API. (`process_chroot` [\#141298](https://github.com/rust-lang/rust/issues/141298))

Set the root of the child process. This calls `chroot` in the child process before executing the command. [Read more](https://doc.rust-lang.org/std/os/unix/process/trait.CommandExt.html#tymethod.chroot)

[Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#271-274)

🔬This is a nightly-only experimental API. (`process_setsid` [\#105376](https://github.com/rust-lang/rust/issues/105376))

1.15.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#127-132)

👎Deprecated since 1.37.0:

should be unsafe, use `pre_exec` instead

Schedules a closure to be run just before the `exec` function is invoked. [Read more](https://doc.rust-lang.org/std/os/unix/process/trait.CommandExt.html#method.before_exec)

1.16.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#378-436)

Available on **Windows** only.

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#379-382)

Sets the [process creation flags](https://docs.microsoft.com/en-us/windows/win32/procthread/process-creation-flags) to be passed to `CreateProcess`. [Read more](https://doc.rust-lang.org/std/os/windows/process/trait.CommandExt.html#tymethod.creation_flags)

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#384-387)

🔬This is a nightly-only experimental API. (`windows_process_extensions_show_window` [\#127544](https://github.com/rust-lang/rust/issues/127544))

Sets the field `wShowWindow` of [STARTUPINFO](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/ns-processthreadsapi-startupinfow) that is passed to `CreateProcess`. Allowed values are the ones listed in <https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-showwindow>

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#389-392)

🔬This is a nightly-only experimental API. (`windows_process_extensions_force_quotes` [\#82227](https://github.com/rust-lang/rust/issues/82227))

Forces all arguments to be wrapped in quote (`"`) characters. [Read more](https://doc.rust-lang.org/std/os/windows/process/trait.CommandExt.html#tymethod.force_quotes)

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#394-397)

Append literal text to the command line without any quoting or escaping. [Read more](https://doc.rust-lang.org/std/os/windows/process/trait.CommandExt.html#tymethod.raw_arg)

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#399-406)

🔬This is a nightly-only experimental API. (`windows_process_extensions_async_pipes` [\#98289](https://github.com/rust-lang/rust/issues/98289))

When [`process::Command`](https://doc.rust-lang.org/std/process/struct.Command.html "struct std::process::Command") creates pipes, request that our side is always async. [Read more](https://doc.rust-lang.org/std/os/windows/process/trait.CommandExt.html#tymethod.async_pipes)

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#408-415)

🔬This is a nightly-only experimental API. (`windows_process_extensions_raw_attribute` [\#114854](https://github.com/rust-lang/rust/issues/114854))

Executes the command as a child process with the given [`ProcThreadAttributeList`](https://doc.rust-lang.org/std/os/windows/process/struct.ProcThreadAttributeList.html "struct std::os::windows::process::ProcThreadAttributeList"), returning a handle to it. [Read more](https://doc.rust-lang.org/std/os/windows/process/trait.CommandExt.html#tymethod.spawn_with_attributes)

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#417-420)

🔬This is a nightly-only experimental API. (`windows_process_extensions_startupinfo` [\#141010](https://github.com/rust-lang/rust/issues/141010))

When true, sets the `STARTF_RUNFULLSCREEN` flag on the [STARTUPINFO](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/ns-processthreadsapi-startupinfoa) struct before passing it to `CreateProcess`.

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#422-425)

🔬This is a nightly-only experimental API. (`windows_process_extensions_startupinfo` [\#141010](https://github.com/rust-lang/rust/issues/141010))

When true, sets the `STARTF_UNTRUSTEDSOURCE` flag on the [STARTUPINFO](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/ns-processthreadsapi-startupinfoa) struct before passing it to `CreateProcess`.

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#427-430)

🔬This is a nightly-only experimental API. (`windows_process_extensions_startupinfo` [\#141010](https://github.com/rust-lang/rust/issues/141010))

When specified, sets the following flags on the [STARTUPINFO](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/ns-processthreadsapi-startupinfoa) struct before passing it to `CreateProcess`: [Read more](https://doc.rust-lang.org/std/os/windows/process/trait.CommandExt.html#tymethod.startupinfo_force_feedback)

[Source](https://doc.rust-lang.org/src/std/os/windows/process.rs.html#432-435)

🔬This is a nightly-only experimental API. (`windows_process_extensions_inherit_handles` [\#146407](https://github.com/rust-lang/rust/issues/146407))

If this flag is set to `true`, each inheritable handle in the calling process is inherited by the new process. If the flag is `false`, the handles are not inherited. [Read more](https://doc.rust-lang.org/std/os/windows/process/trait.CommandExt.html#tymethod.inherit_handles)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/process.rs.html#1340-1356)

[Source](https://doc.rust-lang.org/src/std/process.rs.html#1353-1355)

Format the program and arguments of a Command for display. Any non-utf8 data is lossily converted using the utf8 replacement character.

The default format approximates a shell invocation of the program along with its arguments. It does not include most of the other command properties. The output is not guaranteed to work (e.g. due to lack of shell-escaping or differences in path resolution). On some platforms you can use [the alternate syntax](https://doc.rust-lang.org/std/fmt/index.html#sign0 "mod std::fmt") to show more fields.

Note that the debug implementation is platform-specific.

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.