[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[unix](https://doc.rust-lang.org/std/os/unix/index.html)::[fs](https://doc.rust-lang.org/std/os/unix/fs/index.html)

# Trait DirBuilderExt Copy item path

1.6.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#1069-1084)

``` rust
pub trait DirBuilderExt {
    // Required method
    fn mode(&mut self, mode: u32) -> &mut Self;
}
```

Available on **Unix** only.

Expand description

Unix-specific extensions to [`fs::DirBuilder`](https://doc.rust-lang.org/std/fs/struct.DirBuilder.html "struct std::fs::DirBuilder").

## Required Methods

1.6.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#1083)

Sets the mode to create new directories with. This option defaults to 0o777.

##### Examples

``` rust
use std::fs::DirBuilder;
use std::os::unix::fs::DirBuilderExt;

let mut builder = DirBuilder::new();
builder.mode(0o755);
```

## Dyn Compatibility

This trait is **not** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

1.6.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#1087-1092)