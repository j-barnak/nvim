# miasm.jitter.emulatedsymbexec

## Classes

### class `EmulatedSymbExec(SymbolicExecutionEngine)`

Symbolic exec instance linked with a jitter

- **`__init__(self, cpu, vm, *args, **kwargs)`** - Instantiate an EmulatedSymbExec, associated to CPU @cpu and bind
- **`reset_regs(self)`** - Set registers value to 0. Ignore register aliases
- **`mem_read(self, expr_mem)`** - Memory read wrapper for symbolic execution
- **`mem_write(self, dest, data)`** - Memory write wrapper for symbolic execution
- **`update_cpu_from_engine(self)`** - Updates @cpu instance according to new CPU values
- **`update_engine_from_cpu(self)`** - Updates CPU values according to @cpu instance
- **`_simp_handle_segm(self, e_s, expr)`** - Handle 'segm' operation
- **`_simp_handle_x86_cpuid(self, e_s, expr)`** - From miasm/jitter/op_semantics.h: x86_cpuid
- **`enable_emulated_simplifications(self)`** - Enable simplifications needing a CPU instance on associated
