[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[unix](https://doc.rust-lang.org/std/os/unix/index.html)::[fs](https://doc.rust-lang.org/std/os/unix/fs/index.html)

# Function symlink Copy item path

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#1063-1065)

``` rust
pub fn symlink<P: AsRef<Path>, Q: AsRef<Path>>(
    original: P,
    link: Q,
) -> Result<()>
```

Available on **Unix** only.

Expand description

Creates a new symbolic link on the filesystem.

The `link` path will be a symbolic link pointing to the `original` path.

## Examples

``` rust
use std::os::unix::fs;

fn main() -> std::io::Result<()> {
    fs::symlink("a.txt", "b.txt")?;
    Ok(())
}
```