[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)

# Function type_of Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/mod.rs.html#2904)

``` rust
pub const fn type_of(_id: TypeId) -> Type
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Compute the type information of a concrete type. It can only be called at compile time, the backends do not implement it.