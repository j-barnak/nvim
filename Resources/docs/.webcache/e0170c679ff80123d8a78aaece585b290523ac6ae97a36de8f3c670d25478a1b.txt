# Lecture 3: Cache Attacks

Practical Cache Attacks
Mengjia Yan
Spring 2026

A Communication Model
secret-dependent
microarchitectural
behaviors
Domain of victim

Transmitter

Domain of attacker
Channel

Receiver

Secret

Kiriansky et al; DAWG: A Defense Against Cache Timing Attacks in Speculative Execution Processors
Bourgeat et al; CaSA: End-to-end quantitative security analysis of randomly mapped caches

Secret

2

Leak Crypto Library #1: RSA
• Square-and-Multiply Exponentiation
Input :
base b
modulo m
exponent e = (en−1 ...e0 )2

Output:
be mod m

When b = 3, e = 2256

How many multiplications do you
need to do to compute:
be

Naïve: 2256
Repeated Squaring: 256
3

Leak Crypto Library #1: RSA
• Square-and-Multiply Exponentiation
Input :
base b
modulo m
exponent e = (en−1 ...e0 )2

r = 1
for i = n-1 to 0 do
r = sqr(r)
r

= mod(r, m)

if ei == 1 then

Output:
be mod m

r = mul(r, b)
r = mod(r, m)
end
end
4

Leak Crypto Library #2: AES
Input :
Plaintext
Secret key
Lookup Tables
Output:
Ciphertext

Repeat 10, 12, 14 times
depending on key size.
5

Leak Crypto Library #2: AES T-Table
// Td0, Td1, Td2, Td3 are 4 lookup tables
// s0..s3, t0..t3 are 32-bit integer
for (

;

;

) {

t0 = Td0[(s0>>24)] ^ Td1[(s3>>16)&0xff] ^ Td2[(s2>>8)&0xff] ^ Td3[s1&0xff] ^ rk[4];
t1 = Td0[(s1>>24)] ^ Td1[(s0>>16)&0xff] ^ Td2[(s3>>8)&0xff] ^ Td3[s2&0xff] ^ rk[5];
t2 = Td0[(s2>>24)] ^ Td1[(s1>>16)&0xff] ^ Td2[(s0>>8)&0xff] ^ Td3[s3&0xff] ^ rk[6];
t3 = Td0[(s3>>24)] ^ Td1[(s2>>16)&0xff] ^ Td2[(s1>>8)&0xff] ^ Td3[s0&0xff] ^ rk[7];
rk += 8;
if (--r == 0) {
break;
}
...
6

Observation: Secret-dependent memory accesses.

The attacker’s goal:
Monitor access patterns at cache line granularity.

Why Cache?
• Large attack surface. Shared across cores/sockets.
• Fast. Can be used to build high-bandwidth channels
• Many states. Can encode secrets spatially to further improve bandwidth
and precision.
• There exist many cache-like structures. The same attack concepts and
tricks will apply.

8

Cache Attack Plan
Attacker:
Reset
cache state

Victim:
Access /
No Access

Attacker:
Observe whether access
has happened
Time

① How to reset cache state?
② How to monitor cache state?

9

Attack Strategy #1: Flush+Reload
• The flush instructions allow explicit control of cache states
• In X86, clflush vaddr
• In ARM, DC CIVAC vaddr

• What are these flush instructions used for except for attacks?
• For coherence, in the case when the data in the cache is inconsistent
with the data in the DRAM.
• 1) old time, incoherent DMA
• 2) nowadays, Non-volatile memory for crash recovery

10

Something might
seem weird here…
(threat model)

Flush+Reload
Victim

A shared cache line

Attacker

Attacker:
Flush

Cache

Victim:
Access

Attacker:
Reload ->
low latency
Time

Attacker:
Flush

Victim:
No Access

Attacker:
Reload ->
high latency

DRAM
11

Page Mapping
Process 1

Page Table
per process
4KB

Physical Address Space
(limited by DRAM size)

VA
PA

4KB

Process 2

4KB
4KB

Virtual Address Space
(Programmer's View)

12

Transparent Page Deduplication
Process 1

Page Table
per process
4KB

Physical Address Space
(limited by DRAM size)

VA
PA

4KB

Process 2
4KB
the same
content
Virtual Address Space
(Programmer's View)

13

The Attack Monitor Code
x1 = read cycle;

perform the load;

x2 = read cycle;

latency = x2 – x1;

14

The Attack Monitor Code
mfence
rdtsc
x1 =%eax,
read %edi
cycle;
mov
mov (<vaddr>), %rsi
perform the load;

In x86, 8 GPR:
• rax, rbx, rcx, rdx
• rsp, rbp
• rsi, rdi
“r” means 64-bit
replacing “r” with “e” means the lower 32 bits.

mfence
x2 = read cycle;
rdtsc

rdtsc:
• Read Time-Stamp Counter
• edx:eax := TimeStampCounter;

sub
%edi,= %eax
latency
x2 – x1;

mfence:
• Memory Fence
• Performs a serializing operation on all memory instructions
15

A Demo

Access one element

Sender

Figure out which element
has been accessed

Receiver

16

Attack Strategy #2: ?
• Cache state manipulation instructions
• In X86, clflush vaddr
• In ARM, DC CIVAC vaddr

• What if these instructions are not available in user space?
• Apple devices
• “Except ARMv8-A CPUs, ARM processors do not support a flush instruction”
from ARMageddon: Cache Attacks on Mobile Devices (USENIX’16)

17

Attack Strategy #2: Evict+Reload
Victim

A shared cache line

Attacker
Attacker:
Access a
large buffer

Cache

Victim:
Access

Attacker:
Reload ->
low latency
Time

Attacker:
Access a
large buffer

Victim:
No Access

Attacker:
Reload ->
high latency

DRAM
18

Lessons Learnt So Far
So the fundamental problem:
shared memory between
different security domains.

Source: https://kb.vmware.com/s/article/2080735
19

No more shared memory.
Can we still attack?

Attack Strategy #3: Prime+Probe
Sender’s line

Victim

Attacker

Receiver’s line
Attacker:
Prime a
cache set

Cache

Victim:
Access
Time

Attacker:
Prime a
cache set

Victim:
No Access

DRAM
21

Attack Strategy #3: Prime+Probe
Sender’s line

Victim

Attacker

Receiver’s line
Attacker:
Prime a
cache set

Cache

Victim:
Access
Time

Attacker:
Prime a
cache set

Victim:
No Access

DRAM
22

Attack Strategy #3: Prime+Probe
Sender’s line

Victim

Attacker

Receiver’s line
Attacker:
Prime a
cache set

Cache

Victim:
Access

Attacker:
Probe ->
high latency
Time

Attacker:
Prime a
cache set
DRAM

Victim:
No Access

Attacker:
Probe ->
low latency

Question: How to know which cache
set an address maps to?
23

N-way Set-Associative Cache
INCOMING ADDRESS

Index

Tag

Data

Tag

Data

Tag

Data

Tag

Data

SET

8 sets

Tag

Block offset

WAY
4 ways
24

A 6.191/6.004 Quiz Question
• I have a virtual address: 0xAAAA
• The cache parameter is as below
• Cache size: 64KB
• Line size/Block size: 64B
• Associativity: 8

Question 1:
What is the cache set index?

Question 2:
What is the next address that map to
the same cache set as this one
but not the same cache line?

25

A 6.191/6.004 Quiz Question
• I have a virtual address: 0xAAAA
• The cache parameter is as below
• Cache size: 64KB
• Line size/Block size: 64B
• Associativity: 8

Question 1:
What is the cache set index?

Question 2:
What is the next address that map to
the same cache set as this one
but not the same cache line?

26

Address Translation (4KB page)
47

Virtual Address (48bit):

12

11
Page offset
(12 bits)

Virtual page number (VPN)

Copy
page offset

Page
Table

31

Physical Address (32bit):

0

12

Physical page number (PPN)

11

0
Page offset
(12 bits)
27

Why system designers
introduce huge pages?

Using Huge Pages
47

Virtual Address (48bit):

12
Virtual page number (VPN)

11

0
Page offset
(12 bits)

The red bit is NOT under attacker’s control

Cache mapping:
(256 sets)

Virtual Address :
2MB page

Set Index
(7 bits)

Tag

47
48

21

Virtual page number

Line offset
(6 bits)

20

0
Page offset
(21 bits)

This green bit IS under attacker’s control
28

There are still many other practical challenges.
If you tackle all of them, you can ...

Review RSA Vulnerability
• Square-and-Multiply Exponentiation
Input :
base b
modulo m
exponent e = (en−1 ...e0 )2

r = 1
for i = n-1 to 0 do
r = sqr(r)

r

= mod(r, m)

if ei == 1 then
r = mul(r, b)

Output:
be mod m

r = mod(r, m)
end
end
30

The Multiply Function

31

Raw Trace

Access latencies measured in the probe operation in Prime+Probe on a cache line
inside the multiplication function.
A sequence of “01010111011001” can be deduced as part of the exponent.
32

Takeaways
• Practical challenges in implementing a reliable cache attack
• Page sharing
• Noise due to prefetchers
• Uncertainty due to page mapping
• Replacement policy
• etc.

• Hardware and software optimizations make attacks easier
• Transparent page sharing
• Copy-on-write
• Huge pages
• Virtually-indexed and physically-tagged caches
33

Next:
Cache Attack Recitation

Using Caches with Virtual Memory
Virtually-Addressed Cache

CPU

Cache

TLB

Main
memory

Physically-Addressed Cache

CPU

TLB

Cache

Main
memory

• FAST: No virtual→physical translation
on cache hits

• Avoids stale cache data after context
switch

• Problem: Must flush cache after
context switch

• SLOW: virtual→physical translation
before every cache access

35

Best of Both Worlds (L1 Cache):
Virtually-Indexed, Physically-Tagged Cache (VIPT)

TLB
Main
memory

CPU
Cache index comes entirely from address
bits in page offset – don’t need to wait
for TLB to start cache lookup!

Cache

36

