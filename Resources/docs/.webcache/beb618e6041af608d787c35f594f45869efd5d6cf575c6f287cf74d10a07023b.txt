[std](https://doc.rust-lang.org/std/index.html)::[io](https://doc.rust-lang.org/std/io/index.html)

# Struct BufWriter Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#67-78)

``` rust
pub struct BufWriter<W: ?Sized + Write> { /* private fields */ }
```

Expand description

Wraps a writer and buffers its output.

It can be excessively inefficient to work directly with something that implements [`Write`](https://doc.rust-lang.org/std/io/trait.Write.html "trait std::io::Write"). For example, every call to [`write`](https://doc.rust-lang.org/std/net/struct.TcpStream.html#method.write "method std::net::TcpStream::write") on [`TcpStream`](https://doc.rust-lang.org/std/net/struct.TcpStream.html "struct std::net::TcpStream") results in a system call. A `BufWriter<W>` keeps an in-memory buffer of data and writes it to an underlying writer in large, infrequent batches.

`BufWriter<W>` can improve the speed of programs that make *small* and *repeated* write calls to the same file or network socket. It does not help when writing very large amounts at once, or writing just one or a few times. It also provides no advantage when writing to a destination that is in memory, like a [`Vec`](https://doc.rust-lang.org/std/vec/struct.Vec.html "struct std::vec::Vec")`<u8>`.

It is critical to call [`flush`](https://doc.rust-lang.org/std/io/struct.BufWriter.html#method.flush "method std::io::BufWriter::flush") before `BufWriter<W>` is dropped. Though dropping will attempt to flush the contents of the buffer, any errors that happen in the process of dropping will be ignored. Calling [`flush`](https://doc.rust-lang.org/std/io/struct.BufWriter.html#method.flush "method std::io::BufWriter::flush") ensures that the buffer is empty and thus dropping will not even attempt file operations.

## Examples

Let’s write the numbers one through ten to a [`TcpStream`](https://doc.rust-lang.org/std/net/struct.TcpStream.html "struct std::net::TcpStream"):

``` rust
use std::io::prelude::*;
use std::net::TcpStream;

let mut stream = TcpStream::connect("127.0.0.1:34254").unwrap();

for i in 0..10 {
    stream.write(&[i+1]).unwrap();
}
```

Because we’re not buffering, we write each one in turn, incurring the overhead of a system call per byte written. We can fix this with a `BufWriter<W>`:

``` rust
use std::io::prelude::*;
use std::io::BufWriter;
use std::net::TcpStream;

let mut stream = BufWriter::new(TcpStream::connect("127.0.0.1:34254").unwrap());

for i in 0..10 {
    stream.write(&[i+1]).unwrap();
}
stream.flush().unwrap();
```

By wrapping the stream with a `BufWriter<W>`, these ten writes are all grouped together by the buffer and will all be written out in one system call when the `stream` is flushed.

## Implementations

[Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#80-185)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#93-95)

Creates a new `BufWriter<W>` with a default buffer capacity. The default is currently 8 KiB, but may change in the future.

##### Examples

``` rust
use std::io::BufWriter;
use std::net::TcpStream;

let mut buffer = BufWriter::new(TcpStream::connect("127.0.0.1:34254").unwrap());
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#121-123)

Creates a new `BufWriter<W>` with at least the specified buffer capacity.

##### Examples

Creating a buffer with a buffer of at least a hundred bytes.

``` rust
use std::io::BufWriter;
use std::net::TcpStream;

let stream = TcpStream::connect("127.0.0.1:34254").unwrap();
let mut buffer = BufWriter::with_capacity(100, stream);
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#145-150)

Unwraps this `BufWriter<W>`, returning the underlying writer.

The buffer is written out before returning the writer.

##### Errors

An [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") will be returned if an error occurs while flushing the buffer.

##### Examples

``` rust
use std::io::BufWriter;
use std::net::TcpStream;

let mut buffer = BufWriter::new(TcpStream::connect("127.0.0.1:34254").unwrap());

// unwrap the TcpStream and flush the buffer
let stream = buffer.into_inner().unwrap();
```

1.56.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#175-184)

Disassembles this `BufWriter<W>`, returning the underlying writer, and any buffered but unwritten data.

If the underlying writer panicked, it is not known what portion of the data was written. In this case, we return `WriterPanicked` for the buffered data (from which the buffer contents can still be recovered).

`into_parts` makes no attempt to flush data and cannot fail.

##### Examples

``` rust
use std::io::{BufWriter, Write};

let mut buffer = [0u8; 10];
let mut stream = BufWriter::new(buffer.as_mut());
write!(stream, "too much data").unwrap();
stream.flush().expect_err("it doesn't fit");
let (recovered_writer, buffered_data) = stream.into_parts();
assert_eq!(recovered_writer.len(), 0);
assert_eq!(&buffered_data.unwrap(), b"ata");
```

[Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#187-468)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#296-298)

Gets a reference to the underlying writer.

##### Examples

``` rust
use std::io::BufWriter;
use std::net::TcpStream;

let mut buffer = BufWriter::new(TcpStream::connect("127.0.0.1:34254").unwrap());

// we can use reference just like buffer
let reference = buffer.get_ref();
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#316-318)

Gets a mutable reference to the underlying writer.

It is inadvisable to directly write to the underlying writer.

##### Examples

``` rust
use std::io::BufWriter;
use std::net::TcpStream;

let mut buffer = BufWriter::new(TcpStream::connect("127.0.0.1:34254").unwrap());

// we can use reference just like buffer
let reference = buffer.get_mut();
```

1.37.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#334-336)

Returns a reference to the internally buffered data.

##### Examples

``` rust
use std::io::BufWriter;
use std::net::TcpStream;

let buf_writer = BufWriter::new(TcpStream::connect("127.0.0.1:34254").unwrap());

// See how many bytes are currently buffered
let bytes_buffered = buf_writer.buffer().len();
```

1.46.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#366-368)

Returns the number of bytes the internal buffer can hold without flushing.

##### Examples

``` rust
use std::io::BufWriter;
use std::net::TcpStream;

let buf_writer = BufWriter::new(TcpStream::connect("127.0.0.1:34254").unwrap());

// Check the capacity of the inner buffer
let capacity = buf_writer.capacity();
// Calculate how many bytes can be written without flushing
let without_flush = capacity - buf_writer.buffer().len();
```

## Trait Implementations

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#663-673)

[Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#667-672)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#687-694)

[Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#688-693)

Executes the destructor for this type. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop)

[Source](https://doc.rust-lang.org/src/core/ops/drop.rs.html#267)

🔬This is a nightly-only experimental API. (`pin_ergonomics` [\#130494](https://github.com/rust-lang/rust/issues/130494))

Execute the destructor for this type, but different to [`Drop::drop`](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop "method std::ops::Drop::drop"), it requires `self` to be pinned. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.pin_drop)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#676-684)

[Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#680-683)

Seek to the offset, in bytes, in the underlying writer.

Seeking always writes out the internal buffer before seeking.

1.55.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1840-1843)

Rewind to the beginning of a stream. [Read more](https://doc.rust-lang.org/std/io/trait.Seek.html#method.rewind)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1880-1882)

🔬This is a nightly-only experimental API. (`seek_stream_len` [\#59359](https://github.com/rust-lang/rust/issues/59359))

Returns the length of this stream (in bytes). [Read more](https://doc.rust-lang.org/std/io/trait.Seek.html#method.stream_len)

1.51.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1908-1910)

Returns the current seek position from the start of the stream. [Read more](https://doc.rust-lang.org/std/io/trait.Seek.html#method.stream_position)

1.80.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1936-1939)

Seeks relative to the current position. [Read more](https://doc.rust-lang.org/std/io/trait.Seek.html#method.seek_relative)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#530-660)

[Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#532-545)

Writes a buffer into this writer, returning how many bytes were written. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.write)

[Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#548-561)

Attempts to write an entire buffer into this writer. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_all)

[Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#563-650)

Like [`write`](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.write "method std::io::Write::write"), except that it writes from a slice of buffers. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_vectored)

[Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#652-654)

🔬This is a nightly-only experimental API. (`can_vector` [\#69941](https://github.com/rust-lang/rust/issues/69941))

Determines if this `Write`r has an efficient [`write_vectored`](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_vectored "method std::io::Write::write_vectored") implementation. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.is_write_vectored)

[Source](https://doc.rust-lang.org/src/std/io/buffered/bufwriter.rs.html#656-659)

Flushes this output stream, ensuring that all intermediately buffered contents reach their destination. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.flush)

[Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1673-1688)

🔬This is a nightly-only experimental API. (`write_all_vectored` [\#70436](https://github.com/rust-lang/rust/issues/70436))

Attempts to write multiple buffers into this writer. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_all_vectored)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1726-1732)

Writes a formatted string into this writer, returning any error encountered. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_fmt)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1756-1761)

Creates a “by reference” adapter for this instance of `Write`. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.by_ref)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.