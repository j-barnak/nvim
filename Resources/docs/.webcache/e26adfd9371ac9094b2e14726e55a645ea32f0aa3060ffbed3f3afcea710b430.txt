[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[linux](https://doc.rust-lang.org/std/os/linux/index.html)::[net](https://doc.rust-lang.org/std/os/linux/net/index.html)

# Trait UnixSocketExt Copy item path

[Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/socket.rs.html#13-43)

``` rust
pub trait UnixSocketExt {
    // Required methods
    fn passcred(&self) -> Result<bool>;
    fn set_passcred(&self, passcred: bool) -> Result<()>;
}
```

ⓘ *This trait cannot be implemented outside `std`.*

🔬This is a nightly-only experimental API. (`unix_socket_ancillary_data` [\#76915](https://github.com/rust-lang/rust/issues/76915))

Available on **Android or Cygwin or Linux** only.

Expand description

Linux-specific functionality for `AF_UNIX` sockets [`UnixDatagram`](https://doc.rust-lang.org/std/os/unix/net/struct.UnixDatagram.html "struct std::os::unix::net::UnixDatagram") and [`UnixStream`](https://doc.rust-lang.org/std/os/unix/net/struct.UnixStream.html "struct std::os::unix::net::UnixStream").

## Required Methods

[Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/socket.rs.html#16)

🔬This is a nightly-only experimental API. (`unix_socket_ancillary_data` [\#76915](https://github.com/rust-lang/rust/issues/76915))

Query the current setting of socket option `SO_PASSCRED`.

[Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/socket.rs.html#42)

🔬This is a nightly-only experimental API. (`unix_socket_ancillary_data` [\#76915](https://github.com/rust-lang/rust/issues/76915))

Enable or disable socket option `SO_PASSCRED`.

This option enables the credentials of the sending process to be received as a control message in [`AncillaryData`](https://doc.rust-lang.org/std/os/unix/net/enum.AncillaryData.html "enum std::os::unix::net::AncillaryData").

##### Examples

``` rust
#![feature(unix_socket_ancillary_data)]
#[cfg(target_os = "linux")]
use std::os::linux::net::UnixSocketExt;
#[cfg(target_os = "android")]
use std::os::android::net::UnixSocketExt;
use std::os::unix::net::UnixDatagram;

fn main() -> std::io::Result<()> {
    let sock = UnixDatagram::unbound()?;
    sock.set_passcred(true).expect("set_passcred failed");
    Ok(())
}
```

## Dyn Compatibility

This trait **is** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

[Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/socket.rs.html#46-54)

[Source](https://doc.rust-lang.org/src/std/os/net/linux_ext/socket.rs.html#57-65)