[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)::[mpmc](https://doc.rust-lang.org/std/sync/mpmc/index.html)

# Struct Receiver Copy item path

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#727-729)

``` rust
pub struct Receiver<T> { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Expand description

The receiving half of Rust’s [`channel`](https://doc.rust-lang.org/std/sync/mpmc/fn.channel.html "fn std::sync::mpmc::channel") (or [`sync_channel`](https://doc.rust-lang.org/std/sync/mpmc/fn.sync_channel.html "fn std::sync::mpmc::sync_channel")) type. Different threads can share this [`Receiver`](https://doc.rust-lang.org/std/sync/mpmc/struct.Receiver.html "struct std::sync::mpmc::Receiver") by cloning it.

Messages sent to the channel can be retrieved using [`recv`](https://doc.rust-lang.org/std/sync/mpmc/struct.Receiver.html#method.recv "method std::sync::mpmc::Receiver::recv").

## Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::channel;
use std::thread;
use std::time::Duration;

let (send, recv) = channel();

let tx_thread = thread::spawn(move || {
    send.send("Hello world!").unwrap();
    thread::sleep(Duration::from_secs(2)); // block for two seconds
    send.send("Delayed for 2 seconds").unwrap();
});

let (rx1, rx2) = (recv.clone(), recv.clone());
let rx_thread_1 = thread::spawn(move || {
    println!("{}", rx1.recv().unwrap()); // Received immediately
});
let rx_thread_2 = thread::spawn(move || {
    println!("{}", rx2.recv().unwrap()); // Received after 2 seconds
});

tx_thread.join().unwrap();
rx_thread_1.join().unwrap();
rx_thread_2.join().unwrap();
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#915-1196)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#945-951)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Attempts to receive a message from the channel without blocking.

This method will never block the caller in order to wait for data to become available. Instead, this will always return immediately with a possible option of pending data on the channel.

If called on a zero-capacity channel, this method will receive a message only if there happens to be a send operation on the other side of the channel at the same time.

This is useful for a flavor of “optimistic check” before deciding to block on a receiver.

Compared with [`recv`](https://doc.rust-lang.org/std/sync/mpmc/struct.Receiver.html#method.recv "method std::sync::mpmc::Receiver::recv"), this function has two failure cases instead of one (one for disconnection, one for an empty buffer).

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::{Receiver, channel};

let (_, receiver): (_, Receiver<i32>) = channel();

assert!(receiver.try_recv().is_err());
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1011-1018)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Attempts to wait for a value on this receiver, returning an error if the corresponding channel has hung up.

This function will always block the current thread if there is no data available and it’s possible for more data to be sent (at least one sender still exists). Once a message is sent to the corresponding [`Sender`](https://doc.rust-lang.org/std/sync/mpmc/struct.Sender.html "struct std::sync::mpmc::Sender"), this receiver will wake up and return that message.

If the corresponding [`Sender`](https://doc.rust-lang.org/std/sync/mpmc/struct.Sender.html "struct std::sync::mpmc::Sender") has disconnected, or it disconnects while this call is blocking, this call will wake up and return [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") to indicate that no more messages can ever be received on this channel. However, since channels are buffered, messages sent before the disconnect will still be properly received.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;
use std::thread;

let (send, recv) = mpmc::channel();
let handle = thread::spawn(move || {
    send.send(1u8).unwrap();
});

handle.join().unwrap();

assert_eq!(Ok(1), recv.recv());
```

Buffering behavior:

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;
use std::thread;
use std::sync::mpmc::RecvError;

let (send, recv) = mpmc::channel();
let handle = thread::spawn(move || {
    send.send(1u8).unwrap();
    send.send(2).unwrap();
    send.send(3).unwrap();
    drop(send);
});

// wait for the thread to join so we ensure the sender is dropped
handle.join().unwrap();

assert_eq!(Ok(1), recv.recv());
assert_eq!(Ok(2), recv.recv());
assert_eq!(Ok(3), recv.recv());
assert_eq!(Err(RecvError), recv.recv());
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1079-1085)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Attempts to wait for a value on this receiver, returning an error if the corresponding channel has hung up, or if it waits more than `timeout`.

This function will always block the current thread if there is no data available and it’s possible for more data to be sent (at least one sender still exists). Once a message is sent to the corresponding [`Sender`](https://doc.rust-lang.org/std/sync/mpmc/struct.Sender.html "struct std::sync::mpmc::Sender"), this receiver will wake up and return that message.

If the corresponding [`Sender`](https://doc.rust-lang.org/std/sync/mpmc/struct.Sender.html "struct std::sync::mpmc::Sender") has disconnected, or it disconnects while this call is blocking, this call will wake up and return [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") to indicate that no more messages can ever be received on this channel. However, since channels are buffered, messages sent before the disconnect will still be properly received.

##### Examples

Successfully receiving value before encountering timeout:

``` rust
#![feature(mpmc_channel)]

use std::thread;
use std::time::Duration;
use std::sync::mpmc;

let (send, recv) = mpmc::channel();

thread::spawn(move || {
    send.send('a').unwrap();
});

assert_eq!(
    recv.recv_timeout(Duration::from_millis(400)),
    Ok('a')
);
```

Receiving an error upon reaching timeout:

``` rust
#![feature(mpmc_channel)]

use std::thread;
use std::time::Duration;
use std::sync::mpmc;

let (send, recv) = mpmc::channel();

thread::spawn(move || {
    thread::sleep(Duration::from_millis(800));
    send.send('a').unwrap();
});

assert_eq!(
    recv.recv_timeout(Duration::from_millis(400)),
    Err(mpmc::RecvTimeoutError::Timeout)
);
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1146-1152)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Attempts to wait for a value on this receiver, returning an error if the corresponding channel has hung up, or if `deadline` is reached.

This function will always block the current thread if there is no data available and it’s possible for more data to be sent. Once a message is sent to the corresponding [`Sender`](https://doc.rust-lang.org/std/sync/mpmc/struct.Sender.html "struct std::sync::mpmc::Sender"), then this receiver will wake up and return that message.

If the corresponding [`Sender`](https://doc.rust-lang.org/std/sync/mpmc/struct.Sender.html "struct std::sync::mpmc::Sender") has disconnected, or it disconnects while this call is blocking, this call will wake up and return [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") to indicate that no more messages can ever be received on this channel. However, since channels are buffered, messages sent before the disconnect will still be properly received.

##### Examples

Successfully receiving value before reaching deadline:

``` rust
#![feature(mpmc_channel)]

use std::thread;
use std::time::{Duration, Instant};
use std::sync::mpmc;

let (send, recv) = mpmc::channel();

thread::spawn(move || {
    send.send('a').unwrap();
});

assert_eq!(
    recv.recv_deadline(Instant::now() + Duration::from_millis(400)),
    Ok('a')
);
```

Receiving an error upon reaching deadline:

``` rust
#![feature(mpmc_channel)]

use std::thread;
use std::time::{Duration, Instant};
use std::sync::mpmc;

let (send, recv) = mpmc::channel();

thread::spawn(move || {
    thread::sleep(Duration::from_millis(800));
    send.send('a').unwrap();
});

assert_eq!(
    recv.recv_deadline(Instant::now() + Duration::from_millis(400)),
    Err(mpmc::RecvTimeoutError::Timeout)
);
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1193-1195)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns an iterator that will attempt to yield all pending values. It will return `None` if there are no more pending values or if the channel has hung up. The iterator will never [`panic!`](https://doc.rust-lang.org/std/macro.panic.html "macro std::panic") or block the user by waiting for values.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::channel;
use std::thread;
use std::time::Duration;

let (sender, receiver) = channel();

// nothing is in the buffer yet
assert!(receiver.try_iter().next().is_none());

thread::spawn(move || {
    thread::sleep(Duration::from_secs(1));
    sender.send(1).unwrap();
    sender.send(2).unwrap();
    sender.send(3).unwrap();
});

// nothing is in the buffer yet
assert!(receiver.try_iter().next().is_none());

// block for two seconds
thread::sleep(Duration::from_secs(2));

let mut iter = receiver.try_iter();
assert_eq!(iter.next(), Some(1));
assert_eq!(iter.next(), Some(2));
assert_eq!(iter.next(), Some(3));
assert_eq!(iter.next(), None);
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1198-1408)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1224-1230)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns `true` if the channel is empty.

Note: Zero-capacity channels are always empty.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;
use std::thread;

let (send, recv) = mpmc::channel();

assert!(recv.is_empty());

let handle = thread::spawn(move || {
    send.send(1u8).unwrap();
});

handle.join().unwrap();

assert!(!recv.is_empty());
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1257-1263)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns `true` if the channel is full.

Note: Zero-capacity channels are always full.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;
use std::thread;

let (send, recv) = mpmc::sync_channel(1);

assert!(!recv.is_full());

let handle = thread::spawn(move || {
    send.send(1u8).unwrap();
});

handle.join().unwrap();

assert!(recv.is_full());
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1288-1294)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns the number of messages in the channel.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;
use std::thread;

let (send, recv) = mpmc::channel();

assert_eq!(recv.len(), 0);

let handle = thread::spawn(move || {
    send.send(1u8).unwrap();
});

handle.join().unwrap();

assert_eq!(recv.len(), 1);
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1319-1325)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

If the channel is bounded, returns its capacity.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;
use std::thread;

let (send, recv) = mpmc::sync_channel(3);

assert_eq!(recv.capacity(), Some(3));

let handle = thread::spawn(move || {
    send.send(1u8).unwrap();
});

handle.join().unwrap();

assert_eq!(recv.capacity(), Some(3));
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1343-1350)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns `true` if receivers belong to the same channel.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;

let (_, rx1) = mpmc::channel::<i32>();
let (_, rx2) = mpmc::channel::<i32>();

assert!(rx1.same_channel(&rx1));
assert!(!rx1.same_channel(&rx2));
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1378-1380)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns an iterator that will block waiting for messages, but never [`panic!`](https://doc.rust-lang.org/std/macro.panic.html "macro std::panic"). It will return [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") when the channel has hung up.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::channel;
use std::thread;

let (send, recv) = channel();

thread::spawn(move || {
    send.send(1).unwrap();
    send.send(2).unwrap();
    send.send(3).unwrap();
});

let mut iter = recv.iter();
assert_eq!(iter.next(), Some(1));
assert_eq!(iter.next(), Some(2));
assert_eq!(iter.next(), Some(3));
assert_eq!(iter.next(), None);
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1401-1407)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns `true` if the channel is disconnected.

Note that a return value of `false` does not guarantee the channel will remain connected. The channel may be disconnected immediately after this method returns, so a subsequent [`Receiver::recv`](https://doc.rust-lang.org/std/sync/mpmc/struct.Receiver.html#method.recv "method std::sync::mpmc::Receiver::recv") may still fail with [`RecvError`](https://doc.rust-lang.org/std/sync/mpsc/struct.RecvError.html "struct std::sync::mpsc::RecvError").

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::channel;

let (tx, rx) = channel::<i32>();
assert!(!rx.is_disconnected());
drop(tx);
assert!(rx.is_disconnected());
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1424-1434)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1425-1433)

Returns a duplicate of the value. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#tymethod.clone)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/142757 "Tracking issue for const_clone")) · [Source](https://doc.rust-lang.org/src/core/clone.rs.html#245-247)

Performs copy-assignment from `source`. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#method.clone_from)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1437-1441)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1438-1440)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1411-1421)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#1412-1420)

Executes the destructor for this type. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop)

[Source](https://doc.rust-lang.org/src/core/ops/drop.rs.html#267)

🔬This is a nightly-only experimental API. (`pin_ergonomics` [\#130494](https://github.com/rust-lang/rust/issues/130494))

Execute the destructor for this type, but different to [`Drop::drop`](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop "method std::ops::Drop::drop"), it requires `self` to be pinned. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.pin_drop)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#866-873)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#867)

The type of the elements being iterated over.

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#868)

Which kind of iterator are we turning this into?

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#870-872)

Creates an iterator from a value. [Read more](https://doc.rust-lang.org/std/iter/trait.IntoIterator.html#tymethod.into_iter)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#884-891)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#885)

The type of the elements being iterated over.

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#886)

Which kind of iterator are we turning this into?

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#888-890)

Creates an iterator from a value. [Read more](https://doc.rust-lang.org/std/iter/trait.IntoIterator.html#tymethod.into_iter)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#913)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#906)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#908)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#911)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.