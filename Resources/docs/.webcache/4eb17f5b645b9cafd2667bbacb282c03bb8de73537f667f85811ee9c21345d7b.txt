[std](https://doc.rust-lang.org/std/index.html)::[cmp](https://doc.rust-lang.org/std/cmp/index.html)

# Function min_by Copy item path

1.53.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1605-1609)

``` rust
pub fn min_by<T, F>(v1: T, v2: T, compare: F) -> Twhere
    F: FnOnce(&T, &T) -> Ordering,
```

Expand description

Returns the minimum of two values with respect to the specified comparison function.

Returns the first argument if the comparison determines them to be equal.

The parameter order is preserved when calling the `compare` function, i.e. `v1` is always passed as the first argument and `v2` as the second.

## Examples

``` rust
use std::cmp;

let abs_cmp = |x: &i32, y: &i32| x.abs().cmp(&y.abs());

let result = cmp::min_by(2, -1, abs_cmp);
assert_eq!(result, -1);

let result = cmp::min_by(2, -3, abs_cmp);
assert_eq!(result, 2);

let result = cmp::min_by(1, -1, abs_cmp);
assert_eq!(result, 1);
```