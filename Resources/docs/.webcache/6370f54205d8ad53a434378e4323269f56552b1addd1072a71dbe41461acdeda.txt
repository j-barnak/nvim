[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)::[nonpoison](https://doc.rust-lang.org/std/sync/nonpoison/index.html)

# Struct RwLock Copy item path

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#43-48)

``` rust
pub struct RwLock<T: ?Sized> { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`nonpoison_rwlock` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Expand description

A reader-writer lock that does not keep track of lock poisoning.

For more information about reader-writer locks, check out the documentation for the poisoning variant of this lock (which can be found at [`poison::RwLock`](https://doc.rust-lang.org/std/sync/struct.RwLock.html "struct std::sync::RwLock")).

## Examples

``` rust
#![feature(nonpoison_rwlock)]

use std::sync::nonpoison::RwLock;

let lock = RwLock::new(5);

// many reader locks can be held at once
{
    let r1 = lock.read();
    let r2 = lock.read();
    assert_eq!(*r1, 5);
    assert_eq!(*r2, 5);
} // read locks are dropped at this point

// only one write lock may be held, however
{
    let mut w = lock.write();
    *w += 1;
    assert_eq!(*w, 6);
} // write lock is dropped here
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#192-282)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#206-208)

🔬This is a nightly-only experimental API. (`nonpoison_rwlock` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Creates a new instance of an `RwLock<T>` which is unlocked.

##### Examples

``` rust
#![feature(nonpoison_rwlock)]

use std::sync::nonpoison::RwLock;

let lock = RwLock::new(5);
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#226-231)

🔬This is a nightly-only experimental API. (`lock_value_accessors` [\#133407](https://github.com/rust-lang/rust/issues/133407))

Returns the contained value by cloning it.

##### Examples

``` rust
#![feature(nonpoison_rwlock)]
#![feature(lock_value_accessors)]

use std::sync::nonpoison::RwLock;

let mut lock = RwLock::new(7);

assert_eq!(lock.get_cloned(), 7);
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#251-259)

🔬This is a nightly-only experimental API. (`lock_value_accessors` [\#133407](https://github.com/rust-lang/rust/issues/133407))

Sets the contained value.

##### Examples

``` rust
#![feature(nonpoison_rwlock)]
#![feature(lock_value_accessors)]

use std::sync::nonpoison::RwLock;

let mut lock = RwLock::new(7);

assert_eq!(lock.get_cloned(), 7);
lock.set(11);
assert_eq!(lock.get_cloned(), 11);
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#278-281)

🔬This is a nightly-only experimental API. (`lock_value_accessors` [\#133407](https://github.com/rust-lang/rust/issues/133407))

Replaces the contained value with `value`, and returns the old contained value.

##### Examples

``` rust
#![feature(nonpoison_rwlock)]
#![feature(lock_value_accessors)]

use std::sync::nonpoison::RwLock;

let mut lock = RwLock::new(7);

assert_eq!(lock.replace(11), 7);
assert_eq!(lock.get_cloned(), 11);
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#284-563)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#322-327)

🔬This is a nightly-only experimental API. (`nonpoison_rwlock` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Locks this `RwLock` with shared read access, blocking the current thread until it can be acquired.

The calling thread will be blocked until there are no more writers which hold the lock. There may be other readers currently inside the lock when this method returns. This method does not provide any guarantees with respect to the ordering of whether contentious readers or writers will acquire the lock first.

Returns an RAII guard which will release this thread’s shared access once it is dropped.

##### Panics

This function might panic when called if the lock is already held by the current thread.

##### Examples

``` rust
#![feature(nonpoison_rwlock)]

use std::sync::Arc;
use std::sync::nonpoison::RwLock;
use std::thread;

let lock = Arc::new(RwLock::new(1));
let c_lock = Arc::clone(&lock);

let n = lock.read();
assert_eq!(*n, 1);

thread::spawn(move || {
    let r = c_lock.read();
}).join().unwrap();
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#361-365)

🔬This is a nightly-only experimental API. (`nonpoison_rwlock` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Attempts to acquire this `RwLock` with shared read access.

If the access could not be granted at this time, then `Err` is returned. Otherwise, an RAII guard is returned which will release the shared access when it is dropped.

This function does not block.

This function does not provide any guarantees with respect to the ordering of whether contentious readers or writers will acquire the lock first.

##### Errors

This function will return the [`WouldBlock`](https://doc.rust-lang.org/std/sync/nonpoison/struct.WouldBlock.html "struct std::sync::nonpoison::WouldBlock") error if the `RwLock` could not be acquired because it was already locked exclusively.

##### Examples

``` rust
#![feature(nonpoison_rwlock)]

use std::sync::nonpoison::RwLock;

let lock = RwLock::new(1);

match lock.try_read() {
    Ok(n) => assert_eq!(*n, 1),
    Err(_) => unreachable!(),
};
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#396-401)

🔬This is a nightly-only experimental API. (`nonpoison_rwlock` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Locks this `RwLock` with exclusive write access, blocking the current thread until it can be acquired.

This function will not return while other writers or other readers currently have access to the lock.

Returns an RAII guard which will drop the write access of this `RwLock` when dropped.

##### Panics

This function might panic when called if the lock is already held by the current thread.

##### Examples

``` rust
#![feature(nonpoison_rwlock)]

use std::sync::nonpoison::RwLock;

let lock = RwLock::new(1);

let mut n = lock.write();
*n = 2;

assert!(lock.try_read().is_err());
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#437-441)

🔬This is a nightly-only experimental API. (`nonpoison_rwlock` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Attempts to lock this `RwLock` with exclusive write access.

If the lock could not be acquired at this time, then `Err` is returned. Otherwise, an RAII guard is returned which will release the lock when it is dropped.

This function does not block.

This function does not provide any guarantees with respect to the ordering of whether contentious readers or writers will acquire the lock first.

##### Errors

This function will return the [`WouldBlock`](https://doc.rust-lang.org/std/sync/nonpoison/struct.WouldBlock.html "struct std::sync::nonpoison::WouldBlock") error if the `RwLock` could not be acquired because it was already locked.

##### Examples

``` rust
#![feature(nonpoison_rwlock)]

use std::sync::nonpoison::RwLock;

let lock = RwLock::new(1);

let n = lock.read();
assert_eq!(*n, 1);

assert!(lock.try_write().is_err());
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#460-465)

🔬This is a nightly-only experimental API. (`nonpoison_rwlock` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Consumes this `RwLock`, returning the underlying data.

##### Examples

``` rust
#![feature(nonpoison_rwlock)]

use std::sync::nonpoison::RwLock;

let lock = RwLock::new(String::new());
{
    let mut s = lock.write();
    *s = "modified".to_owned();
}
assert_eq!(lock.into_inner(), "modified");
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#486-488)

🔬This is a nightly-only experimental API. (`nonpoison_rwlock` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Returns a mutable reference to the underlying data.

Since this call borrows the `RwLock` mutably, no actual locking needs to take place – the mutable borrow statically guarantees no new locks can be acquired while this reference exists. Note that this method does not clear any previously abandoned locks (e.g., via [`forget()`](https://doc.rust-lang.org/std/mem/fn.forget.html "fn std::mem::forget") on a [`RwLockReadGuard`](https://doc.rust-lang.org/std/sync/nonpoison/struct.RwLockReadGuard.html "struct std::sync::nonpoison::RwLockReadGuard") or [`RwLockWriteGuard`](https://doc.rust-lang.org/std/sync/nonpoison/struct.RwLockWriteGuard.html "struct std::sync::nonpoison::RwLockWriteGuard")).

##### Examples

``` rust
#![feature(nonpoison_rwlock)]

use std::sync::nonpoison::RwLock;

let mut lock = RwLock::new(0);
*lock.get_mut() = 10;
assert_eq!(*lock.read(), 10);
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#498-500)

🔬This is a nightly-only experimental API. (`rwlock_data_ptr` [\#140368](https://github.com/rust-lang/rust/issues/140368))

Returns a raw pointer to the underlying data.

The returned pointer is always non-null and properly aligned, but it is the user’s responsibility to ensure that any reads and writes through it are properly synchronized to avoid data races, and that it is not read or written through after the lock is dropped.

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#523-528)

🔬This is a nightly-only experimental API. (`lock_value_accessors` [\#133407](https://github.com/rust-lang/rust/issues/133407))

Locks this `RwLock` with shared read access to the underlying data by passing a reference to the given closure.

This method acquires the lock, calls the provided closure with a reference to the data, and returns the result of the closure. The lock is released after the closure completes, even if it panics.

##### Examples

``` rust
#![feature(lock_value_accessors, nonpoison_rwlock)]

use std::sync::nonpoison::RwLock;

let rwlock = RwLock::new(2);
let result = rwlock.with(|data| *data + 3);

assert_eq!(result, 5);
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#557-562)

🔬This is a nightly-only experimental API. (`lock_value_accessors` [\#133407](https://github.com/rust-lang/rust/issues/133407))

Locks this `RwLock` with exclusive write access to the underlying data by passing a mutable reference to the given closure.

This method acquires the lock, calls the provided closure with a mutable reference to the data, and returns the result of the closure. The lock is released after the closure completes, even if it panics.

##### Examples

``` rust
#![feature(lock_value_accessors, nonpoison_rwlock)]

use std::sync::nonpoison::RwLock;

let rwlock = RwLock::new(2);

let result = rwlock.with_mut(|data| {
    *data += 3;

    *data + 5
});

assert_eq!(*rwlock.read(), 5);
assert_eq!(result, 10);
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#566-579)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#567-578)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#582-587)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#584-586)

Creates a new `RwLock<T>`, with the `Default` value for T.

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#590-596)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#593-595)

Creates a new instance of an `RwLock<T>` which is unlocked. This is equivalent to [`RwLock::new`](https://doc.rust-lang.org/std/sync/nonpoison/struct.RwLock.html#method.new "associated function std::sync::nonpoison::RwLock::new").

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#51)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#54)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#804)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#805)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.