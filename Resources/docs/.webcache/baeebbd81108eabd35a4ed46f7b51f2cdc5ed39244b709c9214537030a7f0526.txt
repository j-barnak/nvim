# miasm.analysis.outofssa

## Classes

### class `Varinfo(object)`

Store liveness information for a variable

- **`__init__(self, live_index, loc_key, index)`**

### class `UnSSADiGraph(object)`

Implements unssa algorithm
Revisiting Out-of-SSA Translation for Correctness, Code Quality, and
Efficiency

- **`__init__(self, ssa, head, cfg_liveness)`**
- **`insert_parallel_copy(self)`** - Naive Out-of-SSA from CSSA (without coalescing for now)
- **`create_copy_var(self, var)`** - Generate a new var standing for @var
- **`isolate_phi_nodes_block(self)`** - Init structures and virtually insert parallel copy before/after each phi
- **`init_phis_merge_state(self)`** - Generate trivial coalescing of phi variable and itself
- **`order_ssa_var_dom(self)`** - Compute dominance order of each ssa variable
- **`ssa_def_dominates(self, node_a, node_b)`** - Return living index order of @node_a and @node_b
- **`merge_set_sort(self, merge_set)`** - Return a sorted list of (live_index, var) from @merge_set in dominance
- **`ssa_def_is_live_at(self, node_a, node_b, parent)`** - Return True if @node_a is live during @node_b definition
- **`merge_nodes_interfere(self, node_a, node_b, parent)`** - Return True if @node_a and @node_b interfere
- **`merge_sets_interfere(self, merge_a, merge_b, parent)`** - Return True if no variable in @merge_a and @merge_b interferes.
- **`aggressive_coalesce_parallel_copy(self, parallel_copies, parent)`** - Try to coalesce variables each dst/src couple together from
- **`aggressive_coalesce_block(self)`** - Try to coalesce phi var with their pre/post variables
- **`get_best_merge_set_name(self, merge_set)`** - For a given @merge_set, prefer an original SSA variable instead of a
- **`replace_merge_sets(self)`** - In the graph, replace all variables from merge state by their
- **`remove_phi(self)`** - Remove phi operators in @ifcfg
- **`remove_assign_eq(self)`** - Remove trivial expressions (a=a) in the current graph
