[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_insert_dyn Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#42)

``` rust
pub const unsafe fn simd_insert_dyn<T, U>(x: T, idx: u32, val: U) -> T
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Inserts an element into a vector, returning the updated vector.

`T` must be a vector with element type `U`.

If the index is `const`, [`simd_insert`](https://doc.rust-lang.org/std/intrinsics/simd/fn.simd_insert.html "fn std::intrinsics::simd::simd_insert") may emit better assembly.

## Safety

`idx` must be in-bounds of the vector.