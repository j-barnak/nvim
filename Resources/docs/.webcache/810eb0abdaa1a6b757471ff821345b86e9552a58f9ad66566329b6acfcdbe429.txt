[std](https://doc.rust-lang.org/std/index.html)::[fmt](https://doc.rust-lang.org/std/fmt/index.html)

# Struct DebugMap Copy item path

1.2.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#897)

``` rust
pub struct DebugMap<'a, 'b>where
    'b: 'a,{ /* private fields */ }
```

Expand description

A struct to help with [`fmt::Debug`](https://doc.rust-lang.org/std/fmt/trait.Debug.html "trait std::fmt::Debug") implementations.

This is useful when you wish to output a formatted map as a part of your [`Debug::fmt`](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt "method std::fmt::Debug::fmt") implementation.

This can be constructed by the [`Formatter::debug_map`](https://doc.rust-lang.org/std/fmt/struct.Formatter.html#method.debug_map "method std::fmt::Formatter::debug_map") method.

## Examples

``` rust
use std::fmt;

struct Foo(Vec<(String, i32)>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        fmt.debug_map().entries(self.0.iter().map(|&(ref k, ref v)| (k, v))).finish()
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![("A".to_string(), 10), ("B".to_string(), 11)])),
    r#"{"A": 10, "B": 11}"#,
);
```

## Implementations

[Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#911)

1.2.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#935)

Adds a new entry to the map output.

##### Examples

``` rust
use std::fmt;

struct Foo(Vec<(String, i32)>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        fmt.debug_map()
           .entry(&"whole", &self.0) // We add the "whole" entry.
           .finish()
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![("A".to_string(), 10), ("B".to_string(), 11)])),
    r#"{"whole": [("A", 10), ("B", 11)]}"#,
);
```

1.42.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#971)

Adds the key part of a new entry to the map output.

This method, together with `value`, is an alternative to `entry` that can be used when the complete entry isn’t known upfront. Prefer the `entry` method when it’s possible to use.

##### Panics

`key` must be called before `value` and each call to `key` must be followed by a corresponding call to `value`. Otherwise this method will panic.

##### Examples

``` rust
use std::fmt;

struct Foo(Vec<(String, i32)>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        fmt.debug_map()
           .key(&"whole").value(&self.0) // We add the "whole" entry.
           .finish()
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![("A".to_string(), 10), ("B".to_string(), 11)])),
    r#"{"whole": [("A", 10), ("B", 11)]}"#,
);
```

[Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#980-982)

🔬This is a nightly-only experimental API. (`debug_closure_helpers` [\#117729](https://github.com/rust-lang/rust/issues/117729))

Adds the key part of a new entry to the map output.

This method is equivalent to [`DebugMap::key`](https://doc.rust-lang.org/std/fmt/struct.DebugMap.html#method.key "method std::fmt::DebugMap::key"), but formats the key using a provided closure rather than by calling [`Debug::fmt`](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt "method std::fmt::Debug::fmt").

1.42.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#1047)

Adds the value part of a new entry to the map output.

This method, together with `key`, is an alternative to `entry` that can be used when the complete entry isn’t known upfront. Prefer the `entry` method when it’s possible to use.

##### Panics

`key` must be called before `value` and each call to `key` must be followed by a corresponding call to `value`. Otherwise this method will panic.

##### Examples

``` rust
use std::fmt;

struct Foo(Vec<(String, i32)>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        fmt.debug_map()
           .key(&"whole").value(&self.0) // We add the "whole" entry.
           .finish()
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![("A".to_string(), 10), ("B".to_string(), 11)])),
    r#"{"whole": [("A", 10), ("B", 11)]}"#,
);
```

[Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#1056-1058)

🔬This is a nightly-only experimental API. (`debug_closure_helpers` [\#117729](https://github.com/rust-lang/rust/issues/117729))

Adds the value part of a new entry to the map output.

This method is equivalent to [`DebugMap::value`](https://doc.rust-lang.org/std/fmt/struct.DebugMap.html#method.value "method std::fmt::DebugMap::value"), but formats the value using a provided closure rather than by calling [`Debug::fmt`](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt "method std::fmt::Debug::fmt").

1.2.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#1105-1109)

Adds the contents of an iterator of entries to the map output.

##### Examples

``` rust
use std::fmt;

struct Foo(Vec<(String, i32)>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        fmt.debug_map()
           // We map our vec so each entries' first field will become
           // the "key".
           .entries(self.0.iter().map(|&(ref k, ref v)| (k, v)))
           .finish()
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![("A".to_string(), 10), ("B".to_string(), 11)])),
    r#"{"A": 10, "B": 11}"#,
);
```

1.83.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#1150)

Marks the map as non-exhaustive, indicating to the reader that there are some other entries that are not shown in the debug representation.

##### Examples

``` rust
use std::fmt;

struct Foo(Vec<(String, i32)>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        // Print at most two elements, abbreviate the rest
        let mut f = fmt.debug_map();
        let mut f = f.entries(self.0.iter().take(2).map(|&(ref k, ref v)| (k, v)));
        if self.0.len() > 2 {
            f.finish_non_exhaustive()
        } else {
            f.finish()
        }
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![
        ("A".to_string(), 10),
        ("B".to_string(), 11),
        ("C".to_string(), 12),
    ])),
    r#"{"A": 10, "B": 11, ..}"#,
);
```

1.2.0 · [Source](https://doc.rust-lang.org/src/core/fmt/builders.rs.html#1199)

Finishes output and returns any error encountered.

##### Panics

`key` must be called before `value` and each call to `key` must be followed by a corresponding call to `value`. Otherwise this method will panic.

##### Examples

``` rust
use std::fmt;

struct Foo(Vec<(String, i32)>);

impl fmt::Debug for Foo {
    fn fmt(&self, fmt: &mut fmt::Formatter<'_>) -> fmt::Result {
        fmt.debug_map()
           .entries(self.0.iter().map(|&(ref k, ref v)| (k, v)))
           .finish() // Ends the map formatting.
    }
}

assert_eq!(
    format!("{:?}", Foo(vec![("A".to_string(), 10), ("B".to_string(), 11)])),
    r#"{"A": 10, "B": 11}"#,
);
```

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.