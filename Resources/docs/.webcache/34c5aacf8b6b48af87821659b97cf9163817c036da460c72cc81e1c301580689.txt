[std](https://doc.rust-lang.org/std/index.html)::[u64](https://doc.rust-lang.org/std/u64/index.html)

# Constant MIN Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/core/num/shells/legacy_int_modules.rs.html#69)

``` rust
pub const MIN: u64 = u64::MIN; // 0u64
```

👎Deprecating in a future version:

replaced by the `MIN` associated constant on this type

Expand description

The smallest value that can be represented by this integer type. Use [`u64::MIN`](https://doc.rust-lang.org/std/primitive.u64.html#associatedconstant.MIN "associated constant u64::MIN") instead.

## Examples

``` rust
// deprecated way
let min = std::u64::MIN;

// intended way
let min = u64::MIN;
```