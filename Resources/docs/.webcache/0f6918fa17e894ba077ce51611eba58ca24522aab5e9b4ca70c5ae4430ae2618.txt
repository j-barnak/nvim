# miasm.loader.pe_init

## Classes

### class `ContentManager(object)`

- **`__get__(self, owner, _)`**
- **`__set__(self, owner, new_content)`**
- **`__delete__(self, owner)`**

### class `ContectRva(object)`

- **`__init__(self, parent)`**
- **`get(self, rva_start, rva_stop=None)`** - Get data in RVA view starting at @rva_start, stopping at @rva_stop
- **`set(self, rva, data)`** - Set @data in RVA view starting at @start
- **`__getitem__(self, item)`**
- **`__setitem__(self, item, data)`**

### class `ContentVirtual(object)`

- **`__init__(self, parent)`**
- **`__getitem__(self, item)`**
- **`__setitem__(self, item, data)`**
- **`__call__(self, ad_start, ad_stop=None, ad_step=None)`**
- **`get(self, virt_start, virt_stop=None)`** - Get data in VIRTUAL view starting at @virt_start, stopping at @virt_stop
- **`set(self, addr, data)`** - Set @data in VIRTUAL view starting at @start
- **`max_addr(self)`**
- **`find(self, pattern, start=0, end=None)`**
- **`rfind(self, pattern, start=0, end=None)`**
- **`is_addr_in(self, addr)`**

### class `PE(object)`

- **`__init__(self, pestr=None, loadfrommem=False, parse_resources=True, parse_delay=True, parse_reloc=True, wsize=32, **kwargs)`**
- **`isPE(self)`**
- **`parse_content(self, parse_resources=True, parse_delay=True, parse_reloc=True)`**
- **`resize(self, old, new)`**
- **`__getitem__(self, item)`**
- **`__setitem__(self, item, data)`**
- **`getsectionbyrva(self, rva)`**
- **`getsectionbyvad(self, vad)`**
- **`getsectionbyoff(self, off)`**
- **`getsectionbyname(self, name)`**
- **`is_rva_ok(self, rva)`**
- **`rva2off(self, rva)`**
- **`off2rva(self, off)`**
- **`virt2rva(self, addr)`** - Return rva of virtual address @addr; None if addr is below ImageBase
- **`rva2virt(self, rva)`**
- **`virt2off(self, addr)`** - Return offset of virtual address @addr
- **`off2virt(self, off)`**
- **`is_in_virt_address(self, addr)`**
- **`get_drva(self)`**
- **`get_rva(self)`**
- **`get_virt(self)`**
- **`build_content(self)`**
- **`__bytes__(self)`**
- **`__str__(self)`**
- **`export_funcs(self)`**
- **`reloc_to(self, imgbase)`**

## Functions

### `compute_crc(raw, olds)`
