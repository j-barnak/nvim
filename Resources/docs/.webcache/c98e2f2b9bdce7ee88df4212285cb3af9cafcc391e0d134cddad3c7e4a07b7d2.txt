# miasm.loader.minidump_init

High-level abstraction of Minidump file

## Classes

### class `MemorySegment(object)`

Stand for a segment in memory with additional information

- **`__init__(self, offset, memory_desc, module=None, memory_info=None)`**
- **`address(self)`**
- **`size(self)`**
- **`name(self)`**
- **`content(self)`**
- **`protect(self)`**
- **`pretty_protect(self)`**

### class `Minidump(object)`

Stand for a Minidump file

Here is a few limitation:
 - only < 4GB Minidump are supported (LocationDescriptor handling)
 - only Stream relative to memory mapping are implemented

Official description is available on MSDN:
https://msdn.microsoft.com/en-us/library/ms680378(VS.85).aspx

- **`__init__(self, minidump_str)`**
- **`parse_content(self)`** - Build structures corresponding to current content
- **`build_memory(self)`** - Build an easier to use memory view based on ModuleList and
- **`get(self, virt_start, virt_stop)`** - Return the content at the (virtual addresses)
