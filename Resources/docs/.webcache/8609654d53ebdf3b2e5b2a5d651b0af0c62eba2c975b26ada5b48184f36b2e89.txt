# NAME

podman-image - Manage images

# SYNOPSIS

**podman image** *subcommand*

# DESCRIPTION

The image command allows the management of images

# COMMANDS

| Command | Man Page | Description |
|----|----|----|
| build | [podman-build(1)](https://docs.podman.io/en/latest/markdown/podman-build.1.html) | Build a container using a Dockerfile. |
| diff | [podman-image-diff(1)](https://docs.podman.io/en/latest/markdown/podman-image-diff.1.html) | Inspect changes on an image’s filesystem. |
| exists | [podman-image-exists(1)](https://docs.podman.io/en/latest/markdown/podman-image-exists.1.html) | Check if an image exists in local storage. |
| history | [podman-history(1)](https://docs.podman.io/en/latest/markdown/podman-history.1.html) | Show the history of an image. |
| import | [podman-import(1)](https://docs.podman.io/en/latest/markdown/podman-import.1.html) | Import a tarball and save it as a filesystem image. |
| inspect | [podman-image-inspect(1)](https://docs.podman.io/en/latest/markdown/podman-image-inspect.1.html) | Display an image’s configuration. |
| list | [podman-images(1)](https://docs.podman.io/en/latest/markdown/podman-images.1.html) | List the container images on the system.(alias ls) |
| load | [podman-load(1)](https://docs.podman.io/en/latest/markdown/podman-load.1.html) | Load an image from the docker archive. |
| mount | [podman-image-mount(1)](https://docs.podman.io/en/latest/markdown/podman-image-mount.1.html) | Mount an image’s root filesystem. |
| prune | [podman-image-prune(1)](https://docs.podman.io/en/latest/markdown/podman-image-prune.1.html) | Remove all unused images from the local store. |
| pull | [podman-pull(1)](https://docs.podman.io/en/latest/markdown/podman-pull.1.html) | Pull an image from a registry. |
| push | [podman-push(1)](https://docs.podman.io/en/latest/markdown/podman-push.1.html) | Push an image from local storage to elsewhere. |
| rm | [podman-rmi(1)](https://docs.podman.io/en/latest/markdown/podman-rmi.1.html) | Remove one or more locally stored images. |
| save | [podman-save(1)](https://docs.podman.io/en/latest/markdown/podman-save.1.html) | Save an image to docker-archive or oci. |
| scp | [podman-image-scp(1)](https://docs.podman.io/en/latest/markdown/podman-image-scp.1.html) | Securely copy an image from one host to another. |
| search | [podman-search(1)](https://docs.podman.io/en/latest/markdown/podman-search.1.html) | Search a registry for an image. |
| sign | [podman-image-sign(1)](https://docs.podman.io/en/latest/markdown/podman-image-sign.1.html) | Create a signature for an image. |
| tag | [podman-tag(1)](https://docs.podman.io/en/latest/markdown/podman-tag.1.html) | Add an additional name to a local image. |
| tree | [podman-image-tree(1)](https://docs.podman.io/en/latest/markdown/podman-image-tree.1.html) | Print layer hierarchy of an image in a tree format. |
| trust | [podman-image-trust(1)](https://docs.podman.io/en/latest/markdown/podman-image-trust.1.html) | Manage container registry image trust policy. |
| unmount | [podman-image-unmount(1)](https://docs.podman.io/en/latest/markdown/podman-image-unmount.1.html) | Unmount an image’s root filesystem. |
| untag | [podman-untag(1)](https://docs.podman.io/en/latest/markdown/podman-untag.1.html) | Remove one or more names from a locally-stored image. |

# SEE ALSO

**[podman(1)](https://docs.podman.io/en/latest/markdown/podman.1.html)**
