[std](https://doc.rust-lang.org/std/index.html)::[panic](https://doc.rust-lang.org/std/panic/index.html)

# Function resume_unwind Copy item path

1.9.0 · [Source](https://doc.rust-lang.org/src/std/panic.rs.html#390-392)

``` rust
pub fn resume_unwind(payload: Box<dyn Any + Send>) -> !
```

Expand description

Triggers a panic without invoking the panic hook.

This is designed to be used in conjunction with [`catch_unwind`](https://doc.rust-lang.org/std/panic/fn.catch_unwind.html "fn std::panic::catch_unwind") to, for example, carry a panic across a layer of C code.

## Notes

Note that panics in Rust are not always implemented via unwinding, but they may be implemented by aborting the process. If this function is called when panics are implemented this way then this function will abort the process, not trigger an unwind.

## Examples

[ⓘ](# "This example panics")

``` rust
use std::panic;

let result = panic::catch_unwind(|| {
    if 1 != 2 {
        panic!("oh no!");
    }
});

if let Err(err) = result {
    panic::resume_unwind(err);
}
```