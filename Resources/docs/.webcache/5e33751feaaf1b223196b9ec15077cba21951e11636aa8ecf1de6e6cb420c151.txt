# angr.engines.soot.values.strref

class angr.engines.soot.values.strref.SimSootValue_StringRef  
Bases: [`SimSootValue`](https://docs.angr.io/en/latest/api/angr.engines.soot.values.base.html#angr.engines.soot.values.base.SimSootValue "angr.engines.soot.values.base.SimSootValue")

\_\_init\_\_(*heap_alloc_id*)  

id  

type  

classmethod from_sootvalue(*soot_value*, *state*)  

static new_string(*state*, *value*)  
Allocate and initialize a new string in the context of the state passed.

The method returns the reference to the newly allocated string

Parameters:  
- **state** ([*SimState*](https://docs.angr.io/en/latest/api/angr.sim_state.html#angr.sim_state.SimState "angr.sim_state.SimState")) – angr state where we want to allocate the string

- **value** (*claripy.String*) – value of the string to initialize

Returns:  
SimSootValue_StringRef