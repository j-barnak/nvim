# angr.analyses.variable_recovery.variable_recovery_fast

class angr.analyses.variable_recovery.variable_recovery_fast.VariableRecoveryFastState  
Bases: [`VariableRecoveryStateBase`](https://docs.angr.io/en/latest/api/angr.analyses.variable_recovery.variable_recovery_base.html#angr.analyses.variable_recovery.variable_recovery_base.VariableRecoveryStateBase "angr.analyses.variable_recovery.variable_recovery_base.VariableRecoveryStateBase")

The abstract state of variable recovery analysis.

Variables:  
- **stack_region** ([*KeyedRegion*](https://docs.angr.io/en/latest/api/angr.keyed_region.html#angr.keyed_region.KeyedRegion "angr.keyed_region.KeyedRegion")) – The stack store.

- **register_region** ([*KeyedRegion*](https://docs.angr.io/en/latest/api/angr.keyed_region.html#angr.keyed_region.KeyedRegion "angr.keyed_region.KeyedRegion")) – The register store.

\_\_init\_\_(*block_addr*, *analysis*, *arch*, *func*, *project*, *stack_region=None*, *register_region=None*, *global_region=None*, *typevars=None*, *type_constraints=None*, *func_typevar=None*, *delayed_type_constraints=None*, *stack_offset_typevars=None*, *ret_val_size=None*, *\**, *tv_manager*)  
Parameters:  
**tv_manager** ([*TypeVariableManager*](https://docs.angr.io/en/latest/api/angr.analyses.typehoon.typevars.html#angr.analyses.typehoon.typevars.TypeVariableManager "angr.analyses.typehoon.typevars.TypeVariableManager"))

copy()  

merge(*others*, *successor=None*)  
Merge two abstract states.

For any node A whose dominance frontier that the current node (at the current program location) belongs to, we create a phi variable V’ for each variable V that is defined in A, and then replace all existence of V with V’ in the merged abstract state.

Parameters:  
**others** ([`tuple`](https://docs.python.org/3/library/stdtypes.html#tuple "(in Python v3.14)")\[[`VariableRecoveryFastState`](#angr.analyses.variable_recovery.variable_recovery_fast.VariableRecoveryFastState "angr.analyses.variable_recovery.variable_recovery_fast.VariableRecoveryFastState"), [`...`](https://docs.python.org/3/library/constants.html#Ellipsis "(in Python v3.14)")\]) – Other abstract states to merge.

Return type:  
[`tuple`](https://docs.python.org/3/library/stdtypes.html#tuple "(in Python v3.14)")\[[`VariableRecoveryFastState`](#angr.analyses.variable_recovery.variable_recovery_fast.VariableRecoveryFastState "angr.analyses.variable_recovery.variable_recovery_fast.VariableRecoveryFastState"), [`bool`](https://docs.python.org/3/library/functions.html#bool "(in Python v3.14)")\]

Returns:  
The merged abstract state.

&nbsp;

class angr.analyses.variable_recovery.variable_recovery_fast.VariableRecoveryFast  
Bases: [`ForwardAnalysis`](https://docs.angr.io/en/latest/api/angr.analyses.forward_analysis.forward_analysis.html#angr.analyses.forward_analysis.forward_analysis.ForwardAnalysis "angr.analyses.forward_analysis.forward_analysis.ForwardAnalysis"), [`VariableRecoveryBase`](https://docs.angr.io/en/latest/api/angr.analyses.variable_recovery.variable_recovery_base.html#angr.analyses.variable_recovery.variable_recovery_base.VariableRecoveryBase "angr.analyses.variable_recovery.variable_recovery_base.VariableRecoveryBase")

Recover “variables” from a function by keeping track of stack pointer offsets and pattern matching VEX statements.

If calling conventions are recovered prior to running VariableRecoveryFast, variables can be recognized more accurately. However, it is not a requirement. In this case, the function graph you pass must contain information indicating the call-out sites inside the analyzed function. These graph edges must be annotated with either `"type":`` ``"call"` or `"outside":`` ``True`.