[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)::[mpmc](https://doc.rust-lang.org/std/sync/mpmc/index.html)

# Struct Sender Copy item path

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#301-303)

``` rust
pub struct Sender<T> { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Expand description

The sending-half of Rust’s synchronous [`channel`](https://doc.rust-lang.org/std/sync/mpmc/fn.channel.html "fn std::sync::mpmc::channel") type.

Messages can be sent through this channel with [`send`](https://doc.rust-lang.org/std/sync/mpmc/struct.Sender.html#method.send "method std::sync::mpmc::Sender::send").

Note: all senders (the original and its clones) need to be dropped for the receiver to stop blocking to receive messages with [`Receiver::recv`](https://doc.rust-lang.org/std/sync/mpmc/struct.Receiver.html#method.recv "method std::sync::mpmc::Receiver::recv").

## Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::channel;
use std::thread;

let (sender, receiver) = channel();
let sender2 = sender.clone();

// First thread owns sender
thread::spawn(move || {
    sender.send(1).unwrap();
});

// Second thread owns sender2
thread::spawn(move || {
    sender2.send(2).unwrap();
});

let msg = receiver.recv().unwrap();
let msg2 = receiver.recv().unwrap();

assert_eq!(3, msg + msg2);
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#327-405)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#348-354)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Attempts to send a message into the channel without blocking.

This method will either send a message into the channel immediately or return an error if the channel is full or disconnected. The returned error contains the original message.

If called on a zero-capacity channel, this method will send the message only if there happens to be a receive operation on the other side of the channel at the same time.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::{channel, Receiver, Sender};

let (sender, _receiver): (Sender<i32>, Receiver<i32>) = channel();

assert!(sender.try_send(1).is_ok());
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#394-404)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Attempts to send a value on this channel, returning it back if it could not be sent.

A successful send occurs when it is determined that the other end of the channel has not hung up already. An unsuccessful send would be one where the corresponding receiver has already been deallocated. Note that a return value of [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") means that the data will never be received, but a return value of [`Ok`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Ok "variant std::result::Result::Ok") does *not* mean that the data will be received. It is possible for the corresponding receiver to hang up immediately after this function returns [`Ok`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Ok "variant std::result::Result::Ok"). However, if the channel is zero-capacity, it acts as a rendezvous channel and a return value of [`Ok`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Ok "variant std::result::Result::Ok") means that the data has been received.

If the channel is full and not disconnected, this call will block until the send operation can proceed. If the channel becomes disconnected, this call will wake up and return an error. The returned error contains the original message.

If called on a zero-capacity channel, this method will wait for a receive operation to appear on the other side of the channel.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::channel;

let (tx, rx) = channel();

// This send is always successful
tx.send(1).unwrap();

// This send will fail because the receiver is gone
drop(rx);
assert!(tx.send(1).is_err());
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#407-654)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#430-436)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Waits for a message to be sent into the channel, but only for a limited time.

If the channel is full and not disconnected, this call will block until the send operation can proceed or the operation times out. If the channel becomes disconnected, this call will wake up and return an error. The returned error contains the original message.

If called on a zero-capacity channel, this method will wait for a receive operation to appear on the other side of the channel.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::channel;
use std::time::Duration;

let (tx, rx) = channel();

tx.send_timeout(1, Duration::from_millis(400)).unwrap();
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#461-467)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Waits for a message to be sent into the channel, but only until a given deadline.

If the channel is full and not disconnected, this call will block until the send operation can proceed or the operation times out. If the channel becomes disconnected, this call will wake up and return an error. The returned error contains the original message.

If called on a zero-capacity channel, this method will wait for a receive operation to appear on the other side of the channel.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::channel;
use std::time::{Duration, Instant};

let (tx, rx) = channel();

let t = Instant::now() + Duration::from_millis(400);
tx.send_deadline(1, t).unwrap();
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#497-503)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns `true` if the channel is empty.

Note: Zero-capacity channels are always empty.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;
use std::thread;

let (send, _recv) = mpmc::channel();

let tx1 = send.clone();
let tx2 = send.clone();

assert!(tx1.is_empty());

let handle = thread::spawn(move || {
    tx2.send(1u8).unwrap();
});

handle.join().unwrap();

assert!(!tx1.is_empty());
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#531-537)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns `true` if the channel is full.

Note: Zero-capacity channels are always full.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;
use std::thread;

let (send, _recv) = mpmc::sync_channel(1);

let (tx1, tx2) = (send.clone(), send.clone());
assert!(!tx1.is_full());

let handle = thread::spawn(move || {
    tx2.send(1u8).unwrap();
});

handle.join().unwrap();

assert!(tx1.is_full());
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#563-569)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns the number of messages in the channel.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;
use std::thread;

let (send, _recv) = mpmc::channel();
let (tx1, tx2) = (send.clone(), send.clone());

assert_eq!(tx1.len(), 0);

let handle = thread::spawn(move || {
    tx2.send(1u8).unwrap();
});

handle.join().unwrap();

assert_eq!(tx1.len(), 1);
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#595-601)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

If the channel is bounded, returns its capacity.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;
use std::thread;

let (send, _recv) = mpmc::sync_channel(3);
let (tx1, tx2) = (send.clone(), send.clone());

assert_eq!(tx1.capacity(), Some(3));

let handle = thread::spawn(move || {
    tx2.send(1u8).unwrap();
});

handle.join().unwrap();

assert_eq!(tx1.capacity(), Some(3));
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#619-626)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns `true` if senders belong to the same channel.

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc;

let (tx1, _) = mpmc::channel::<i32>();
let (tx2, _) = mpmc::channel::<i32>();

assert!(tx1.same_channel(&tx1));
assert!(!tx1.same_channel(&tx2));
```

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#647-653)

🔬This is a nightly-only experimental API. (`mpmc_channel` [\#126840](https://github.com/rust-lang/rust/issues/126840))

Returns `true` if the channel is disconnected.

Note that a return value of `false` does not guarantee the channel will remain connected. The channel may be disconnected immediately after this method returns, so a subsequent [`Sender::send`](https://doc.rust-lang.org/std/sync/mpmc/struct.Sender.html#method.send "method std::sync::mpmc::Sender::send") may still fail with [`SendError`](https://doc.rust-lang.org/std/sync/mpsc/struct.SendError.html "struct std::sync::mpsc::SendError").

##### Examples

``` rust
#![feature(mpmc_channel)]

use std::sync::mpmc::channel;

let (tx, rx) = channel::<i32>();
assert!(!tx.is_disconnected());
drop(rx);
assert!(tx.is_disconnected());
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#670-680)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#671-679)

Returns a duplicate of the value. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#tymethod.clone)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/142757 "Tracking issue for const_clone")) · [Source](https://doc.rust-lang.org/src/core/clone.rs.html#245-247)

Performs copy-assignment from `source`. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#method.clone_from)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#683-687)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#684-686)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#657-667)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#658-666)

Executes the destructor for this type. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop)

[Source](https://doc.rust-lang.org/src/core/ops/drop.rs.html#267)

🔬This is a nightly-only experimental API. (`pin_ergonomics` [\#130494](https://github.com/rust-lang/rust/issues/130494))

Execute the destructor for this type, but different to [`Drop::drop`](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop "method std::ops::Drop::drop"), it requires `self` to be pinned. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.pin_drop)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#325)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#318)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#320)

[Source](https://doc.rust-lang.org/src/std/sync/mpmc/mod.rs.html#323)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.