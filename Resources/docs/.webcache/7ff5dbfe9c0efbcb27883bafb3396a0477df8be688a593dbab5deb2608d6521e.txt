[std](https://doc.rust-lang.org/std/index.html)::[task](https://doc.rust-lang.org/std/task/index.html)

# Struct LocalWaker Copy item path

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#741)

``` rust
pub struct LocalWaker { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`local_waker` [\#118959](https://github.com/rust-lang/rust/issues/118959))

Expand description

A `LocalWaker` is analogous to a [`Waker`](https://doc.rust-lang.org/std/task/struct.Waker.html "struct std::task::Waker"), but it does not implement [`Send`](https://doc.rust-lang.org/std/marker/trait.Send.html "trait std::marker::Send") or [`Sync`](https://doc.rust-lang.org/std/marker/trait.Sync.html "trait std::marker::Sync").

This handle encapsulates a [`RawWaker`](https://doc.rust-lang.org/std/task/struct.RawWaker.html "struct std::task::RawWaker") instance, which defines the executor-specific wakeup behavior.

Local wakers can be requested from a `Context` with the [`local_waker`](https://doc.rust-lang.org/std/task/struct.Context.html#method.local_waker "method std::task::Context::local_waker") method.

The typical life of a `LocalWaker` is that it is constructed by an executor, wrapped in a [`Context`](https://doc.rust-lang.org/std/task/struct.Context.html "struct std::task::Context") using [`ContextBuilder`](https://doc.rust-lang.org/std/task/struct.ContextBuilder.html "struct std::task::ContextBuilder"), then passed to [`Future::poll()`](https://doc.rust-lang.org/std/future/trait.Future.html#tymethod.poll "method std::future::Future::poll"). Then, if the future chooses to return [`Poll::Pending`](https://doc.rust-lang.org/std/task/enum.Poll.html#variant.Pending "variant std::task::Poll::Pending"), it must also store the waker somehow and call [`LocalWaker::wake()`](https://doc.rust-lang.org/std/task/struct.LocalWaker.html#method.wake "method std::task::LocalWaker::wake") when the future should be polled again.

Implements [`Clone`](https://doc.rust-lang.org/std/clone/trait.Clone.html "trait std::clone::Clone"), but neither [`Send`](https://doc.rust-lang.org/std/marker/trait.Send.html "trait std::marker::Send") nor [`Sync`](https://doc.rust-lang.org/std/marker/trait.Sync.html "trait std::marker::Sync"); therefore, a local waker may not be moved to other threads. In general, when deciding to use wakers or local wakers, local wakers are preferable unless the waker needs to be sent across threads. This is because wakers can incur in additional cost related to memory synchronization.

Note that it is preferable to use `local_waker.clone_from(&new_waker)` instead of `*local_waker = new_waker.clone()`, as the former will avoid cloning the waker unnecessarily if the two wakers [wake the same task](https://doc.rust-lang.org/std/task/struct.LocalWaker.html#method.will_wake "method std::task::LocalWaker::will_wake").

## Examples

Usage of a local waker to implement a future analogous to `std::thread::yield_now()`.

``` rust
#![feature(local_waker)]
use std::future::{Future, poll_fn};
use std::task::Poll;

// a future that returns pending once.
fn yield_now() -> impl Future<Output=()> + Unpin {
    let mut yielded = false;
    poll_fn(move |cx| {
        if !yielded {
            yielded = true;
            cx.local_waker().wake_by_ref();
            return Poll::Pending;
        }
        return Poll::Ready(())
    })
}

yield_now().await;
```

## Implementations

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#748)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#768)

🔬This is a nightly-only experimental API. (`local_waker` [\#118959](https://github.com/rust-lang/rust/issues/118959))

Wakes up the task associated with this `LocalWaker`.

As long as the executor keeps running and the task is not finished, it is guaranteed that each invocation of [`wake()`](https://doc.rust-lang.org/std/task/struct.LocalWaker.html#method.wake "method std::task::LocalWaker::wake") (or [`wake_by_ref()`](https://doc.rust-lang.org/std/task/struct.LocalWaker.html#method.wake_by_ref "method std::task::LocalWaker::wake_by_ref")) will be followed by at least one [`poll()`](https://doc.rust-lang.org/std/future/trait.Future.html#tymethod.poll "method std::future::Future::poll") of the task to which this `LocalWaker` belongs. This makes it possible to temporarily yield to other tasks while running potentially unbounded processing loops.

Note that the above implies that multiple wake-ups may be coalesced into a single [`poll()`](https://doc.rust-lang.org/std/future/trait.Future.html#tymethod.poll "method std::future::Future::poll") invocation by the runtime.

Also note that yielding to competing tasks is not guaranteed: it is the executor’s choice which task to run and the executor may choose to run the current task again.

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#788)

🔬This is a nightly-only experimental API. (`local_waker` [\#118959](https://github.com/rust-lang/rust/issues/118959))

Wakes up the task associated with this `LocalWaker` without consuming the `LocalWaker`.

This is similar to [`wake()`](https://doc.rust-lang.org/std/task/struct.LocalWaker.html#method.wake "method std::task::LocalWaker::wake"), but may be slightly less efficient in the case where an owned `Waker` is available. This method should be preferred to calling `waker.clone().wake()`.

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#808)

🔬This is a nightly-only experimental API. (`local_waker` [\#118959](https://github.com/rust-lang/rust/issues/118959))

Returns `true` if this `LocalWaker` and another `LocalWaker` would awake the same task.

This function works on a best-effort basis, and may return false even when the `Waker`s would awaken the same task. However, if this function returns `true`, it is guaranteed that the `Waker`s will awaken the same task.

This function is primarily used for optimization purposes — for example, this type’s [`clone_from`](https://doc.rust-lang.org/std/task/struct.LocalWaker.html#method.clone_from "method std::task::LocalWaker::clone_from") implementation uses it to avoid cloning the waker when they would wake the same task anyway.

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#836)

🔬This is a nightly-only experimental API. (`local_waker` [\#118959](https://github.com/rust-lang/rust/issues/118959))

Creates a new `LocalWaker` from the provided `data` pointer and `vtable`.

The `data` pointer can be used to store arbitrary data as required by the executor. This could be e.g. a type-erased pointer to an `Arc` that is associated with the task. The value of this pointer will get passed to all functions that are part of the `vtable` as the first parameter.

The `vtable` customizes the behavior of a `LocalWaker`. For each operation on the `LocalWaker`, the associated function in the `vtable` will be called.

##### Safety

The behavior of the returned `Waker` is undefined if the contract defined in [`RawWakerVTable`](https://doc.rust-lang.org/std/task/struct.RawWakerVTable.html "struct std::task::RawWakerVTable")’s documentation is not upheld.

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#848)

🔬This is a nightly-only experimental API. (`local_waker` [\#118959](https://github.com/rust-lang/rust/issues/118959))

Creates a new `LocalWaker` from [`RawWaker`](https://doc.rust-lang.org/std/task/struct.RawWaker.html "struct std::task::RawWaker").

The behavior of the returned `LocalWaker` is undefined if the contract defined in [`RawWaker`](https://doc.rust-lang.org/std/task/struct.RawWaker.html "struct std::task::RawWaker")’s and [`RawWakerVTable`](https://doc.rust-lang.org/std/task/struct.RawWakerVTable.html "struct std::task::RawWakerVTable")’s documentation is not upheld. Therefore this method is unsafe.

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#885)

🔬This is a nightly-only experimental API. (`local_waker` [\#118959](https://github.com/rust-lang/rust/issues/118959))

Returns a reference to a `LocalWaker` that does nothing when used.

This is mostly useful for writing tests that need a [`Context`](https://doc.rust-lang.org/std/task/struct.Context.html "struct std::task::Context") to poll some futures, but are not expecting those futures to wake the waker or do not need to do anything specific if it happens.

More generally, using `LocalWaker::noop()` to poll a future means discarding the notification of when the future should be polled again, So it should only be used when such a notification will not be needed to make progress.

If an owned `LocalWaker` is needed, `clone()` this one.

##### Examples

``` rust
#![feature(local_waker)]
use std::future::Future;
use std::task::{ContextBuilder, LocalWaker, Waker, Poll};

let mut cx = ContextBuilder::from_waker(Waker::noop())
    .local_waker(LocalWaker::noop())
    .build();

let mut future = Box::pin(async { 10 });
assert_eq!(future.as_mut().poll(&mut cx), Poll::Ready(10));
```

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#894)

🔬This is a nightly-only experimental API. (`local_waker` [\#118959](https://github.com/rust-lang/rust/issues/118959))

Gets the `data` pointer used to create this `LocalWaker`.

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#902)

🔬This is a nightly-only experimental API. (`local_waker` [\#118959](https://github.com/rust-lang/rust/issues/118959))

Gets the `vtable` pointer used to create this `LocalWaker`.

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#910)

🔬This is a nightly-only experimental API. (`waker_from_fn_ptr` [\#148457](https://github.com/rust-lang/rust/issues/148457))

Constructs a `LocalWaker` from a function pointer.

## Trait Implementations

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#980)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#982)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#950)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#951)

Converts this type into a shared reference of the (usually inferred) input type.

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#929)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#931)

Returns a duplicate of the value. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#tymethod.clone)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#941)

Performs copy-assignment from `source`. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#method.clone_from)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#969)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#970)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#958)

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#960)

Executes the destructor for this type. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop)

[Source](https://doc.rust-lang.org/src/core/ops/drop.rs.html#267)

🔬This is a nightly-only experimental API. (`pin_ergonomics` [\#130494](https://github.com/rust-lang/rust/issues/130494))

Execute the destructor for this type, but different to [`Drop::drop`](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop "method std::ops::Drop::drop"), it requires `self` to be pinned. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.pin_drop)

[Source](https://doc.rust-lang.org/src/alloc/task.rs.html#331)

[Source](https://doc.rust-lang.org/src/alloc/task.rs.html#335)

Use a `Wake`-able type as a `LocalWaker`.

No heap allocations or atomic operations are used for this conversion.

[Source](https://doc.rust-lang.org/src/core/task/wake.rs.html#746)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.