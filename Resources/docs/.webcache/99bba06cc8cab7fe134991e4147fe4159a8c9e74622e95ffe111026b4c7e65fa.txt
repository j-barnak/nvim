[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)

# Constant ONCE_INIT Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/sync/once.rs.html#75)

``` rust
pub const ONCE_INIT: Once;
```

👎Deprecated since 1.38.0:

the `Once::new()` function is now preferred

Expand description

Initialization value for static [`Once`](https://doc.rust-lang.org/std/sync/struct.Once.html "struct std::sync::Once") values.

## Examples

``` rust
use std::sync::{Once, ONCE_INIT};

static START: Once = ONCE_INIT;
```