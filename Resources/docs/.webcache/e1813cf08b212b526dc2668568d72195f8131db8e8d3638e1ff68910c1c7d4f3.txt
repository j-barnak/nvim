[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)

# Struct UniqueArc Copy item path

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4377-4380)

``` rust
pub struct UniqueArc<T, A = Global>where
    A: Allocator,
    T: ?Sized,{ /* private fields */ }
```

🔬This is a nightly-only experimental API. (`unique_rc_arc` [\#112566](https://github.com/rust-lang/rust/issues/112566))

Expand description

A uniquely owned [`Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html "struct std::sync::Arc").

This represents an `Arc` that is known to be uniquely owned – that is, have exactly one strong reference. Multiple weak pointers can be created, but attempts to upgrade those to strong references will fail unless the `UniqueArc` they point to has been converted into a regular `Arc`.

Because it is uniquely owned, the contents of a `UniqueArc` can be freely mutated. A common use case is to have an object be mutable during its initialization phase but then have it become immutable and converted to a normal `Arc`.

This can be used as a flexible way to create cyclic data structures, as in the example below.

``` rust
#![feature(unique_rc_arc)]
use std::sync::{Arc, Weak, UniqueArc};

struct Gadget {
    me: Weak<Gadget>,
}

fn create_gadget() -> Option<Arc<Gadget>> {
    let mut rc = UniqueArc::new(Gadget {
        me: Weak::new(),
    });
    rc.me = UniqueArc::downgrade(&rc);
    Some(UniqueArc::into_arc(rc))
}

create_gadget().unwrap();
```

An advantage of using `UniqueArc` over [`Arc::new_cyclic`](https://doc.rust-lang.org/std/sync/struct.Arc.html#method.new_cyclic "associated function std::sync::Arc::new_cyclic") to build cyclic data structures is that [`Arc::new_cyclic`](https://doc.rust-lang.org/std/sync/struct.Arc.html#method.new_cyclic "associated function std::sync::Arc::new_cyclic")’s `data_fn` parameter cannot be async or return a [`Result`](https://doc.rust-lang.org/std/result/enum.Result.html "enum std::result::Result"). As shown in the previous example, `UniqueArc` allows for more flexibility in the construction of cyclic data, including fallible or async constructors.

## Implementations

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4622)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4632)

🔬This is a nightly-only experimental API. (`unique_rc_arc` [\#112566](https://github.com/rust-lang/rust/issues/112566))

Creates a new `UniqueArc`.

Weak references to this `UniqueArc` can be created with [`UniqueArc::downgrade`](https://doc.rust-lang.org/std/sync/struct.UniqueArc.html#method.downgrade "associated function std::sync::UniqueArc::downgrade"). Upgrading these weak references will fail before the `UniqueArc` has been converted into an [`Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html "struct std::sync::Arc"). After converting the `UniqueArc` into an [`Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html "struct std::sync::Arc"), any weak references created beforehand will point to the new [`Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html "struct std::sync::Arc").

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4659)

🔬This is a nightly-only experimental API. (`smart_pointer_try_map` [\#144419](https://github.com/rust-lang/rust/issues/144419))

Maps the value in a `UniqueArc`, reusing the allocation if possible.

`f` is called on a reference to the value in the `UniqueArc`, and the result is returned, also in a `UniqueArc`.

Note: this is an associated function, which means that you have to call it as `UniqueArc::map(u, f)` instead of `u.map(f)`. This is so that there is no conflict with a method on the inner type.

##### Examples

``` rust
#![feature(smart_pointer_try_map)]
#![feature(unique_rc_arc)]

use std::sync::UniqueArc;

let r = UniqueArc::new(7);
let new = UniqueArc::map(r, |i| i + 7);
assert_eq!(*new, 14);
```

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4700-4706)

🔬This is a nightly-only experimental API. (`smart_pointer_try_map` [\#144419](https://github.com/rust-lang/rust/issues/144419))

Attempts to map the value in a `UniqueArc`, reusing the allocation if possible.

`f` is called on a reference to the value in the `UniqueArc`, and if the operation succeeds, the result is returned, also in a `UniqueArc`.

Note: this is an associated function, which means that you have to call it as `UniqueArc::try_map(u, f)` instead of `u.try_map(f)`. This is so that there is no conflict with a method on the inner type.

##### Examples

``` rust
#![feature(smart_pointer_try_map)]
#![feature(unique_rc_arc)]

use std::sync::UniqueArc;

let b = UniqueArc::new(7);
let new = UniqueArc::try_map(b, u32::try_from).unwrap();
assert_eq!(*new, 7);
```

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4759)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4770)

🔬This is a nightly-only experimental API. (`unique_rc_arc` [\#112566](https://github.com/rust-lang/rust/issues/112566))

Creates a new `UniqueArc` in the provided allocator.

Weak references to this `UniqueArc` can be created with [`UniqueArc::downgrade`](https://doc.rust-lang.org/std/sync/struct.UniqueArc.html#method.downgrade "associated function std::sync::UniqueArc::downgrade"). Upgrading these weak references will fail before the `UniqueArc` has been converted into an [`Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html "struct std::sync::Arc"). After converting the `UniqueArc` into an [`Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html "struct std::sync::Arc"), any weak references created beforehand will point to the new [`Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html "struct std::sync::Arc").

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4785)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4795)

🔬This is a nightly-only experimental API. (`unique_rc_arc` [\#112566](https://github.com/rust-lang/rust/issues/112566))

Converts the `UniqueArc` into a regular [`Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html "struct std::sync::Arc").

This consumes the `UniqueArc` and returns a regular [`Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html "struct std::sync::Arc") that contains the `value` that is passed to `into_arc`.

Any weak references created before this method is called can now be upgraded to strong references.

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4846)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4853)

🔬This is a nightly-only experimental API. (`unique_rc_arc` [\#112566](https://github.com/rust-lang/rust/issues/112566))

Creates a new weak reference to the `UniqueArc`.

Attempting to upgrade this weak reference will fail before the `UniqueArc` has been converted to a [`Arc`](https://doc.rust-lang.org/std/sync/struct.Arc.html "struct std::sync::Arc") using [`UniqueArc::into_arc`](https://doc.rust-lang.org/std/sync/struct.UniqueArc.html#method.into_arc "associated function std::sync::UniqueArc::into_arc").

## Trait Implementations

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4450)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4451)

Converts this type into a mutable reference of the (usually inferred) input type.

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4443)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4444)

Converts this type into a shared reference of the (usually inferred) input type.

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4429)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4430)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4436)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4437)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4398-4399)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4415)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4416)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4883)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4884)

The resulting type after dereferencing.

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4886)

Dereferences the value.

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4897)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4898)

Mutably dereferences the value.

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4911)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4405)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4408)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4409)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Display.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4914)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4915)

Executes the destructor for this type. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop)

[Source](https://doc.rust-lang.org/src/core/ops/drop.rs.html#267)

🔬This is a nightly-only experimental API. (`pin_ergonomics` [\#130494](https://github.com/rust-lang/rust/issues/130494))

Execute the destructor for this type, but different to [`Drop::drop`](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop "method std::ops::Drop::drop"), it requires `self` to be pinned. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.pin_drop)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4613)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4458)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4460)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4616)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4617)

Feeds this value into the given [`Hasher`](https://doc.rust-lang.org/std/hash/trait.Hasher.html "trait std::hash::Hasher"). [Read more](https://doc.rust-lang.org/std/hash/trait.Hash.html#tymethod.hash)

1.3.0 · [Source](https://doc.rust-lang.org/src/core/hash/mod.rs.html#234-236)

Feeds a slice of this type into the given [`Hasher`](https://doc.rust-lang.org/std/hash/trait.Hasher.html "trait std::hash::Hasher"). [Read more](https://doc.rust-lang.org/std/hash/trait.Hash.html#method.hash_slice)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4590)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4607)

Comparison for two `UniqueArc`s.

The two are compared by calling `cmp()` on their inner values.

##### Examples

``` rust
#![feature(unique_rc_arc)]
use std::sync::UniqueArc;
use std::cmp::Ordering;

let five = UniqueArc::new(5);

assert_eq!(Ordering::Less, five.cmp(&UniqueArc::new(6)));
```

1.21.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1034-1036)

Compares and returns the maximum of two values. [Read more](https://doc.rust-lang.org/std/cmp/trait.Ord.html#method.max)

1.21.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1073-1075)

Compares and returns the minimum of two values. [Read more](https://doc.rust-lang.org/std/cmp/trait.Ord.html#method.min)

1.50.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1099-1101)

Restrict a value to a certain interval. [Read more](https://doc.rust-lang.org/std/cmp/trait.Ord.html#method.clamp)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4469)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4485)

Equality for two `UniqueArc`s.

Two `UniqueArc`s are equal if their inner values are equal.

##### Examples

``` rust
#![feature(unique_rc_arc)]
use std::sync::UniqueArc;

let five = UniqueArc::new(5);

assert!(five == UniqueArc::new(5));
```

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#263)

Tests for `!=`. The default implementation is almost always sufficient, and should not be overridden without very good reason.

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4491)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4508)

Partial comparison for two `UniqueArc`s.

The two are compared by calling `partial_cmp()` on their inner values.

##### Examples

``` rust
#![feature(unique_rc_arc)]
use std::sync::UniqueArc;
use std::cmp::Ordering;

let five = UniqueArc::new(5);

assert_eq!(Some(Ordering::Less), five.partial_cmp(&UniqueArc::new(6)));
```

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4527)

Less-than comparison for two `UniqueArc`s.

The two are compared by calling `<` on their inner values.

##### Examples

``` rust
#![feature(unique_rc_arc)]
use std::sync::UniqueArc;

let five = UniqueArc::new(5);

assert!(five < UniqueArc::new(6));
```

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4546)

‘Less than or equal to’ comparison for two `UniqueArc`s.

The two are compared by calling `<=` on their inner values.

##### Examples

``` rust
#![feature(unique_rc_arc)]
use std::sync::UniqueArc;

let five = UniqueArc::new(5);

assert!(five <= UniqueArc::new(5));
```

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4565)

Greater-than comparison for two `UniqueArc`s.

The two are compared by calling `>` on their inner values.

##### Examples

``` rust
#![feature(unique_rc_arc)]
use std::sync::UniqueArc;

let five = UniqueArc::new(5);

assert!(five > UniqueArc::new(4));
```

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4584)

‘Greater than or equal to’ comparison for two `UniqueArc`s.

The two are compared by calling `>=` on their inner values.

##### Examples

``` rust
#![feature(unique_rc_arc)]
use std::sync::UniqueArc;

let five = UniqueArc::new(5);

assert!(five >= UniqueArc::new(5));
```

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4894)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4422)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4423)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Pointer.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4391)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4394)

[Source](https://doc.rust-lang.org/src/alloc/sync.rs.html#4466)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#804)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#805)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/ops/deref.rs.html#379-381)

[Source](https://doc.rust-lang.org/src/core/ops/deref.rs.html#383)

🔬This is a nightly-only experimental API. (`arbitrary_self_types` [\#44874](https://github.com/rust-lang/rust/issues/44874))

The target type on which the method may be called.

[Source](https://doc.rust-lang.org/src/alloc/string.rs.html#2918)

[Source](https://doc.rust-lang.org/src/alloc/string.rs.html#2920)

Converts the given value to a `String`. [Read more](https://doc.rust-lang.org/std/string/trait.ToString.html#tymethod.to_string)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.