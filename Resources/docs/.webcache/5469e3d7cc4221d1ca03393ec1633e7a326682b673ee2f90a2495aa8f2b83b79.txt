[std](https://doc.rust-lang.org/std/index.html)::[io](https://doc.rust-lang.org/std/io/index.html)

# Macro const_error Copy item path

[Source](https://doc.rust-lang.org/src/core/io/error.rs.html#193)

``` rust
pub macro const_error($kind:expr, $message:expr $(,)?) {
    ...
}
```

🔬This is a nightly-only experimental API. (`io_const_error` [\#133448](https://github.com/rust-lang/rust/issues/133448))

Expand description

Creates a new I/O error from a known kind of error and a string literal.

Contrary to [`Error::new`](https://doc.rust-lang.org/alloc/io/struct.Error.html#method.new), this macro does not allocate and can be used in `const` contexts.

## Example

``` rust
#![feature(io_const_error)]
use std::io::{const_error, Error, ErrorKind};

const FAIL: Error = const_error!(ErrorKind::Unsupported, "tried something that never works");

fn not_here() -> Result<(), Error> {
    Err(FAIL)
}
```