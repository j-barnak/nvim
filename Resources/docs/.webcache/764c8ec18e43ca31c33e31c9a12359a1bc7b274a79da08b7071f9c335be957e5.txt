# The ART of Practical Synchronization

                                  The ART of Practical Synchronization

                       Viktor Leis                 Florian Scheibner                          Alfons Kemper            Thomas Neumann
                                                                    Technische Universität München
                                                   {leis,scheibnf,kemper,neumann}@in.tum.de



ABSTRACT                                                                                             scalability
The performance of transactional database systems is critically de-
pendent on the efficient synchronization of in-memory data struc-
tures. The traditional approach, fine-grained locking, does not scale                                                  ROWEX
on modern hardware. Lock-free data structures, in contrast, scale                                          lock-free
very well but are extremely difficult to implement and often require                                                                             HTM
additional indirections. In this work, we argue for a middle ground,                                                           optimistic
i.e., synchronization protocols that use locking, but only sparingly.                                                        lock coupling
We synchronize the Adaptive Radix Tree (ART) using two such
protocols, Optimistic Lock Coupling and Read-Optimized Write
EXclusion (ROWEX). Both perform and scale very well while be-
ing much easier to implement than lock-free techniques.                                                                        ﬁne-grained locking
                                                                                                                                 (lock coupling)
1.     INTRODUCTION
   In traditional database systems, most data structures are pro-                                                                        ease of use
tected by fine-grained locks1 . This approach worked well in the
past, since these locks were only acquired for a short time and                                       Figure 1: Overview of synchronization paradigms
disk I/O dominated overall execution time. On modern servers with
many cores and where most data resides in RAM, synchronization
itself often becomes a major scalability bottleneck. And with the                                  We focus on synchronizing the Adaptive Radix Tree (ART) [12],
increasing number of CPU cores—Intel’s current server platform                                  a general-purpose, order-preserving index structure for main-memory
Broadwell EP supports up to 24 cores per socket—efficient syn-                                  database systems. ART is an interesting case study, because it is a
chronization will become even more important.                                                   non-trivial data structure that was not designed with concurrency
   Figure 1 gives an overview over the synchronization paradigms                                in mind rather with high single-threaded performance. We present
discussed in this paper. Besides traditional fine-grained locking,                              a number of synchronization protocols for ART and compare them
which is known to scale badly on modern CPUs, the figure shows                                  experimentally.
the lock-free paradigm, which offers strong theoretical guarantees,                                Our main goal in this paper, however, is to distill general prin-
and Hardware Transactional Memory (HTM), which requires spe-                                    ciples for synchronizing data structures in general. This is impor-
cial hardware support. When designing a data structure, so far, one                             tant for two reasons. First, besides index structures, database sys-
had to decide between the extreme difficulty of the lock-free ap-                               tems also require other data structures that must be concurrently ac-
proach, special hardware support of HTM, and poor scalability of                                cessible like tuple storage, buffer management data structures, job
locking. In this paper, we present two additional points in the de-                             queues, etc. Second, concurrent programs are very hard to write
sign space that fill the void in between. Optimistic Lock Coupling                              and even harder to debug. We therefore present our ideas, which,
and ROWEX are much easier to use than lock-free synchronization                                 as we discuss in Section 6, are not entirely new, as general building
but offer similar scalability without special hardware support.                                 blocks that can be applied to other other data structures.
1                                                                                                  The rest of this work is organized as follows. We first present
  In this paper, we always use the term “lock” instead of “latch”                               necessary background about the Adaptive Radix Tree in Section 2.
since we focus on low-level data structure synchronization, not
high-level concurrency control.                                                                 The two new synchronization paradigms Optimistic Lock Coupling
                                                                                                and Read-Optimized Write EXclusion are introduced in Section 3
                                                                                                and Section 4. Section 5 evaluates the presented mechanisms. Fi-
Permission to make digital or hard copies of all or part of this work for personal or
classroom use is granted without fee provided that copies are not made or distributed           nally, after discussing related work in Section 6, we present our
for profit or commercial advantage and that copies bear this notice and the full cita-          conclusions in Section 7.
tion on the first page. Copyrights for components of this work owned by others than
ACM must be honored. Abstracting with credit is permitted. To copy otherwise, or re-
publish, to post on servers or to redistribute to lists, requires prior specific permission
and/or a fee. Request permissions from permissions@acm.org.                                     2.   THE ADAPTIVE RADIX TREE (ART)
DaMoN’16, June 26-July 01 2016, San Francisco, CA, USA                                            Trie data structures, which are also known as radix trees and
c 2016 ACM. ISBN 978-1-4503-4319-0/16/06. . . $15.00                                            prefix trees, have been shown to outperform classical in-memory
DOI: http://dx.doi.org/10.1145/2933349.2933352                                                  search trees [12, 10]. At the same time, and in contrast to hash
                     preﬁxCount             count type                         preﬁx
Header                                                                                                    propagate up to more than 1 level. Lock coupling can therefore be
(in front of                                                                                              applied to ART even more easily than to B-trees.
                               4              3       N4 0 0 0 0
each node)                                                                                                   The left-hand-side of Figure 3 shows the necessary changes for
Node4                key                                     child pointer                                synchronizing the lookup operation of ART with lock coupling.
                                                                                                          The pseudo code uses read-write locks to allow for concurrent read-
         0       1         2       3          0             1              2            3
                                                                                                          ers. Insert and delete operations can also initially acquire read locks
         13 129130                                                                                        before upgrading them to write locks if necessary. This allows to
                                                                                                          avoid exclusively locking nodes near the root, which greatly en-
                                                                                                          hances concurrency because most updates only affect nodes far
Node48 child index                                                      child pointer
                                                                                                          from the root.
 0       1       2       3              255           0             1              2                 47      Lock coupling is simple and seems to allow for a high degree
                                   …                                                        …             of parallelism. However, as we show in Section 5, it performs very
                                                                                                          badly on modern multi-core CPUs even if the locks do not logically
                                                                                                          conflict at all, for example in read-only workloads. The reason is
Node16           key                                                child pointer
                                                                                                          that concurrent locking of tree structures causes many unnecessary
     0       1       2                 15         0             1              2                15        cache misses: Each time a core acquires a read lock for a node
     3       8       9         … 255                        TID                         …                 (by writing to that node), all copies of that cache line are invali-
                                                                                                          dated in the caches of all other cores. Threads, in effect, “fight” for
                                                                                                          exclusive ownership of the cache line holding the lock. The root
Node256                                           child pointer
                                                                                                          node and other nodes close to it become contention points. There-
     0               1                 2              3             4              5             255      fore, other synchronization mechanisms are necessary to fully uti-
                                   TID            TID             TID                       …    TID      lize modern multi-core CPUs.
                                                                                                             Optimistic Lock Coupling is similar to “normal” lock coupling,
                                                                                                          but offers dramatically better scalability. Instead of preventing con-
             Figure 2: The internal data structures of ART
                                                                                                          current modifications of nodes (as locks do), the basic idea is to
                                                                                                          optimistically assume that there will be no concurrent modification.
                                                                                                          Modifications are detected after the fact using version counters, and
tables, they are order-preserving, making them very attractive in-                                        the operation is restarted if necessary. From a performance stand-
dexing structures for main-memory database systems.                                                       point, this optimism makes a huge difference, because it dramati-
   What distinguishes ART [12] from most other trie variants is that                                      cally reduces the number of writes to shared memory locations.
it uses an adaptive node structure. ART dynamically chooses the
internal representation of each node from multiple data structures.
The four available node types are illustrated in Figure 2. Initially,                                     3.1    Optimistic Locks
the smallest node type (Node4) is selected, and, as entries are in-                                          Figure 3 compares the pseudo code for lookup in ART using lock
serted into that node, it is replaced with a larger node type. In                                         coupling and Optimistic Lock Coupling (the code for insert can be
the figure, if two more entries would be inserted into the (Node4),                                       found in Appendix B). Clearly, the two versions are very similar.
which currently holds 3 entries, it would be replaced by a (Node16).                                      The difference is encapsulated in the readLockOrRestart and
   Another important feature of ART is path compression, which                                            readUnlockOrRestart functions, which mimic a traditional
collapses nodes with only a single child pointer into the first node                                      locking interface but are implemented differently. This interface
with more than one child. To implement this, each node stores a                                           makes it possible for the programmer to reason (almost) as if she
prefix of key bytes in its header. This allows indexing long keys                                         was using normal read-write locks.
(e.g., strings) effectively, because the optimization significantly re-                                      Internally, an optimistic lock consists of a lock and a version
duces the height of the tree. The example header shown in Figure 2                                        counter. For writers, optimistic locks work mostly like normal
stores a prefix of 4 zero bytes thus reducing the height by 4 levels.                                     locks, i.e., they provide mutual exclusion by physically acquir-
   In HyPer, where ART is the default indexing structure [8], ART                                         ing (by writing into) the lock. Additionally, each writeUnlock
maps arbitrary keys to tuple identifiers (TIDs). As the figure                                            operation causes the version counter associated with the lock
shows, the TIDs are stored directly inside the pointers. Pointers                                         to be incremented. Read operations, in contrast, do not actu-
and TIDs are distinguished using “pointer tagging”, i.e., by using                                        ally acquire or release locks. readLockOrRestart merely
one of the pointer’s bits.                                                                                waits until the lock is free, before returning the current version.
   Adaptive node types and path compression reduce space con-                                             readUnlockOrRestart, which takes a version as an argument,
sumption while allowing for nodes with high fanout and thus en-                                           makes sure the lock is still free and that the version (returned by
sure high overall performance. At the same time, these features                                           readLockOrRestart) did not change. If a change occurred,
are also the main challenges for synchronizing ART. Tries without                                         the lookup operation is restarted from the root of tree. In our im-
these features are easier to synchronize, which makes ART a more                                          plementation we encode the lock and the version counter (and an
interesting case study for synchronizing non-trivial data structures.                                     obsolete flag) in a single 64 bit word that is updated atomically and
                                                                                                          is stored in the header of each ART node. A full description of the
                                                                                                          optimistic lock primitives can be found in Appendix A.
3.       OPTIMISTIC LOCK COUPLING                                                                            Let us mention that, for debugging purposes, it is possible to
   Lock coupling [1], i.e., holding at most 2 locks at a time during                                      map the optimistic primitives to their pessimistic variants (e.g.,
traversal, is the standard method for synchronizing B-trees. One                                          pthread_rwlock). This enables thread analysis tools like
interesting property of ART is that modifications affect at most two                                      Helgrind to detect synchronization bugs. Version/lock combi-
nodes: the node where the value is inserted or deleted, and poten-                                        nations have been used in the past for synchronizing data struc-
tially its parent node if the node must grow (during insert) or shrink                                    tures (e.g., [4, 2, 18]), usually in combination with additional, data
(during deletion). In contrast to B-trees, a modification will never                                      structure-specific tricks that reduce the number of restarts. Opti-
lookup(key, node, level, parent)                                     1 lookupOpt(key, node, level, parent, versionParent)
   readLock(node)                                                    2    version = readLockOrRestart(node)
   if parent != null                                                 3    if parent != null
      unlock(parent)                                                 4       readUnlockOrRestart(parent, versionParent)
   // check if prefix matches, may increment level                   5    // check if prefix matches, may increment level
   if !prefixMatches(node, key, level)                               6    if !prefixMatches(node, key, level)
      unlock(node)                                                   7       readUnlockOrRestart(node, version)
      return null // key not found                                   8       return null // key not found
   // find child                                                     9    // find child
   nextNode = node.findChild(key[level])                            10    nextNode = node.findChild(key[level])
                                                                    11    checkOrRestart(node, version)
   if isLeaf(nextNode)                                              12    if isLeaf(nextNode)
      value = getLeafValue(nextNode)                                13       value = getLeafValue(nextNode)
      unlock(node)                                                  14       readUnlockOrRestart(node, version)
      return value // key found                                     15       return value // key found
   if nextNode == null                                              16    if nextNode == null
      unlock(node)                                                  17       readUnlockOrRestart(node, version)
      return null // key not found                                  18       return null // key not found
   // recurse to next level                                         19    // recurse to next level
   return lookup(key, nextNode, level+1, node)                      20    return lookupOpt(key, nextNode, level+1, node, version)

Figure 3: Pseudo code for a lookup operation that is synchronized using lock coupling (left) vs. Optimistic Lock Coupling (right).
The necessary changes for synchronization are highlighted


mistic Lock Coupling is indeed a very general technique that al-           4.    READ-OPTIMIZED WRITE EXCLUSION
lows consistent snapshots over multiple nodes (e.g., 2 at a time for          Like many optimistic concurrency control schemes, Optimistic
lock coupling). The region can even involve more than 2 nodes, but         Lock Coupling performs very well as long as there are few con-
should obviously be as small as possible to reduce conflicts.              flicts. The big disadvantage is, however, that all operations may
                                                                           restart. Restarts are particularly undesirable for reads, because they
                                                                           are predominant in many workloads. We therefore present a sec-
3.2    Assumptions of Optimistic Lock Coupling                             ond synchronization technique that still uses locks for writes, but
   To make Optimistic Lock Coupling work correctly, there                  where reads always succeed and never block or restart. We call this
are certain properties that an algorithm must fulfill. After               technique Read-Optimized Write EXclusion (ROWEX).
readLockOrRestart has been called, a reader may see inter-
mediate, inconsistent states of the data structure. An incorrect state     4.1    General Idea
will be detected later by readUnlockOrRestart, but, in some                   ROWEX is a synchronization paradigm that lies between tra-
cases, can still cause problems. In particular, care must be taken to      ditional locking and lock-free techniques (cf. Figure 1). It pro-
avoid (1) infinite loops and (2) invalid pointers. For ART, an infinite    vides the guarantee that reads are non-blocking and always suc-
loop cannot occur. To protect against invalid pointers, it is neces-       ceed. Synchronizing an existing data structure with ROWEX is
sary to add an additional version check (line 11 in Figure 3). With-       harder than with (optimistic) lock coupling, but generally still eas-
out this check, nextNode might be an invalid pointer, which may            ier (i.e., at least possible) than designing a similar lock-free data
cause the program to crash. These additional checks are needed             structure. The main tool of ROWEX is the write lock, which pro-
before an optimistically read pointer is dereferenced.                     vides additional leverage in comparison with lock-free approaches
   Another aspect that needs some care is deletion of nodes. A node        that must confine themselves with primitive atomic operations like
must not be immediately reclaimed after removing it from the tree          compare-and-swap. The write locks are acquired only infrequently
because readers might still be active. We use epoch-based memory           by writers and never by readers.
reclamation to defer freeing such nodes. Additionally, when a node            The basic idea with ROWEX is that, before modifying a node,
has been logically deleted, we mark it as obsolete when unlocking          the lock for that node is first acquired. This lock only provides
the node (cf. writeUnlockObsolete in Appendix A). This al-                 exclusion relative to other writers, but not readers, which never ac-
lows other writers to detect this situation and trigger a restart.         quire any locks (and never check any versions). The consequence
   One theoretical problem with Optimistic Lock Coupling is that—          is that writers must ensure that reads are always consistent by using
in pathological cases—a read operation may be restarted repeat-            atomic operations. As we describe in the following, ROWEX gen-
edly. A simple solution for ensuring forward progress is to limit          erally requires some modifications to the internal data structures.
the number of optimistic restarts. After this limit has been reached,         Although the name ROWEX indicates that reads are fast, writes
the lookup operation can acquire write locks instead.                      are not slow either (despite requiring locks). The reason is that writ-
   To summarize, Optimistic Lock Coupling is remarkably simple,            ers only acquire local locks at the nodes that are actually changed,
requires few changes to the underlying data structure, and, as we          i.e., where physical conflicts are likely. Even lock-free designs
show in Section 5, performs very well. The technique is also quite         will often have cache line invalidations that impair scalability when
general and can be applied to other data structures (e.g., B-trees).       writing to the same node. ROWEX thus provides very good scala-
                                                                           bility while still being realistically applicable to many existing data
                                                                           structures.
                                                                           level preﬁx                     level preﬁx                           level preﬁx
4.2     ROWEX for ART
   One important invariant of ART is that every insertion/deletion           0                                 0                                   0
order results in the same tree because there are no rebalancing op-                              I
                                                                                                                           A            I                      A       I
erations. Each key, therefore, has a deterministic physical location.
                                                                                             I                                      I
In B-link trees [11], in contrast, a key may have been moved to a                                                                                                  I
node on the right (and never to the left due to deliberately omitting                A                  level preﬁx                            level preﬁx
underflow handling).                                                                                       1                                     1
   To synchronize ART using ROWEX, we first discuss local mod-                           1. install
ifications within the 4 node types before describing how node re-                        new node                      R            S                     R            S
placement and finally path compression are handled.
                                                                           level preﬁx                                          AS                                 AS
   To allow concurrent local modifications, accesses to fields that                                   level preﬁx                           level preﬁx
                                                                            2    R
may be read concurrently must be atomic. These fields include                                          2       R                             2
the key bytes (in Node4 and Node16), the child indexes (in
                                                                                     E           T                 E            T                         E         T
Node48), and the child pointers (in all node types). In C++ 11
this is done by using the std::atomic type, which ensures that                                                                     2. update
                                                                                   ARE      ART                    ARE         ART   preﬁx             ARE         ART
appropriate memory barriers are inserted2 . These changes are al-
ready sufficient for the Node48 and Node256 types and allow
adding (removing) children to (from) these nodes. For the lin-                   Figure 4: Path compression changes for inserting “AS”
ear node types (Node4 and Node16), which are structurally very
similar to each other, some additional conceptual changes are nec-
essary. In the original design, the keys in linear nodes are sorted        8 byte value, which can be changed atomically3 .
(cf. Figure 2) to simplify range scans. To allow for concurrent mod-          The main difficulty with path compression is that it is not pos-
ifications while reads are active, we avoid maintaining the sorted         sible to install a new node and truncate the prefix in a single oper-
order and append new keys at the end of the node instead. Deletions        ation. As a consequence, a reader may see the intermediate state
simply set the child pointer to null, and slots are reclaimed lazily       in Figure 4. To solve this problem, we augment each node with
by replacing the node with a new one. With this change, lookups            a level field, which stores the height of the node including the
must check all keys (i.e., 4 for Node4 and 16 for Node16). How-            prefix and which never changes after a node has been created. With
ever, this is not a problem since SIMD instructions can be used to         this additional information, the intermediate state in Figure 4 is
perform the comparison.                                                    safe, because a reader will detect that the prefix at the blue node
   Node replacement can become necessary due to (1) the node               has to be skipped. Similarly, it is also possible that a reader sees
becoming too full to encompass another insert or due to (2) the node       the final state of the blue node without having seen the green node
becoming underfull. In both cases, the required steps for replacing        before. In that situation, the reader can detect the missing prefix
a node are the same:                                                       using level field and retrieve the missing key from the database.
                                                                              To summarize, whereas in Optimistic Lock Coupling readers de-
   1. Both the node and its parent are locked.                             tect changes and restart, with ROWEX it is the responsibility of
                                                                           writers to ensure that reads are safe. Thus ROWEX is not a simple
   2. A new node of the appropriate type is created and initialized        recipe that can be applied to any data structure, but must be care-
      by copying over all entries from the old node.                       fully adapted to it. In some cases, it might even prove impossible
                                                                           without major changes to the underlying data structure. Neverthe-
   3. The location within the parent pointing to the old node is           less, we believe that ROWEX is an interesting design point between
      changed to the new node using an atomic store.                       lock-free techniques and locking.

   4. The old node is unlocked and marked as obsolete. The parent
      node is unlocked.                                                    5.     EVALUATION
                                                                              In this section we experimentally compare a number of ART
Like with Optimistic Lock Coupling, once a node is not reachable           variants: unmodified ART without support for concurrent modi-
in the most recent version of the tree (after step 3), the node must       fications, lock coupling with read-write spinlocks, Optimistic Lock
be marked as obsolete before being unlocked. Any writer waiting            Coupling, ROWEX, and hardware transactional memory (HTM)
for that lock will detect that the node is obsolete and will restart the   with 20 restarts using a global, elided lock as fallback (as described
entire insert or delete operation. For readers, in contrast, it is safe    in [14]). The additional space consumption per node is 4 bytes for
to read from obsolete nodes.                                               lock coupling, 8 bytes for Optimistic Lock Coupling, and 12 bytes
   Path compression is illustrated in Figure 4. In the 2-level tree on     for ROWEX. As a competitor we chose Masstree [18], which, to
the left-hand side, the blue node contains the “R” prefix. In order to     the best of our knowledge, is the fastest publicly available4 , syn-
insert the new key “AS”, it is necessary to (1) install a new (green)      chronized, order-preserving data structure. Note that the compar-
node and (2) truncate the prefix (at the blue node). Individually,         ison between ART and Masstree is not really “apples-to-apples”,
both steps can be performed atomically: Installing the new node            as both the synchronization protocol and the data structures them-
can be done by a single (atomic) store. To change the prefix of the        selves differ. All implementations use the jemalloc memory
node atomically, we store both the prefix and its length in a single       allocator and, when required, low-overhead epoch-based memory
2                                                                          3
   The std::atomic type does not change the physical layout.                 In comparison with the original ART implementation this decision
Also note that on x86 an atomic load adds very little overhead, as         reduces the maximum prefix length from 9 to 4. On x86, 16 byte
it does not introduce a memory barrier. Making the fields atomic           values can also be accessed atomically, which would also allow
merely prevents the compiler from reordering instructions but does         storing 12 byte prefixes.
                                                                           4
not introduce additional instructions for readers.                           https://github.com/kohler/masstree-beta
                                      lookup                     insert                                            remove
                      100

                                                                                                                                                    no sync.
M operations/second


                      75
                                                                                                                                                    lock coupling

                      50                                                                                                                            Opt. Lock Coupling
                                                                                                                                                    ROWEX
                      25
                                                                                                                                                    HTM
                                                                                                                                                    Masstree
                       0
                                  5       10   15   20     5      10    15       20                            5       10        15        20
                                                                threads

                                                         Figure 5: Scalability (50M 8 byte integers)

                                                                                                                       lookup                       insert + remove
reclamation. We use a Haswell EP system with an Intel Xeon E5-
                                                                                                      25
2687W v3 CPU, which has 10 cores (20 “Hyper-Threads”) and
25 MB of L3 cache.
                                                                                                      20




                                                                                M operations/second
                                                                                                                                ROWEX
                                                                                                      15                                                       HTM
5.1                         Scalability
   In our first experiment we investigate the scalability using 50M                                                         Opt. Lock Coupling                        ROWEX
random (sparse) 8-byte integers. This is a low contention work-                                       10           HTM
load, because conflicts are unlikely with random keys. Figure 5                                                             Masstree                                    OLC
shows the results for individually executing lookup, insert, and re-                                   5                                                   Masstree
move. As expected, the variant without synchronization, which                                                                    lock coupling                 lock coupling
is only shown for the read-only experiment, performs best. Lock                                        0
coupling does not scale well, even in the lookup experiment and                                            0       1             2         3 0         1       2          3
although we use read-write locks. Optimistic Lock Coupling and                                                                    Zipf parameter (skew)
ROWEX, in contrast, scale very well and have very similar perfor-
mance, with Optimistic Lock Coupling being slightly (around 7%)                      Figure 6: Performance under contention (1 lookup thread and
faster for lookup. The HTM variant also performs very well on the                    1 insert+remove thread)
lookup experiment, but is slightly slower for insert and significantly
slower for remove. We verified that the reason for this is (unneces-
sary) contention in the memory allocator causing frequent aborts.                    5.2                   Contention
Masstree scales well but with short keys its overall performance is                     In order to show the effect of contention, we measured simulta-
significantly lower than with ART.                                                   neous lookup and update operations (insert+remove) in a tree with
   To better understand the lookup results in Figure 5, we measured                  10M dense 8 byte integer keys. We used 1 lookup thread and 1 up-
some important CPU statistics:                                                       date thread and varied the skewness of the keys going from uniform
                           1 thread / 20 threads [per lookup]                        to extremely skewed (reading and modifying the same key 83% of
                         cycles instruct. L1 misses L3 misses                        the time). The lookup results (left-hand side of Figure 6) show
no sync               211 / 381 123 / 124      4.3 / 4.5 1.7 / 1.8                   that with higher skew most variants initially become faster (due to
lock coupling       418 / 2787 242 / 243       5.2 / 9.0 1.8 / 2.0                   better cache locality), before eventually slowing down under very
Opt. Lock Coup. 348 / 418 187 / 187            5.3 / 5.7 1.8 / 2.0                   high contention. ROWEX is the only exception, as its lookup per-
ROWEX                 375 / 427 248 / 249      5.6 / 5.8 1.9 / 1.9                   formance stays very high even under extreme contention due to the
HTM                   347 / 428 132 / 135      4.3 / 4.5 1.7 / 2.1                   non-blocking reads. The performance of the update thread (right-
Masstree            982 / 1231 897 / 897 20.5 / 21.1 6.5 / 7.1                       hand side of Figure 6) is generally similar to the lookup perfor-
   Single-threaded, the overhead of Optimistic Lock Coupling in                      mance. One exception is HTM, which has higher performance for
comparison with the non-synchronized variant is around 65%,                          the update thread than for the lookup thread, because Intel’s trans-
mostly due to additional instructions. With 20 threads, the overhead                 actional memory implementation favors writers over reader [17].
is reduced to only 10%, likely due to longer delays in the memory
controller. With lock coupling, because of cache line invalidations                  5.3                   Strings
caused by lock acquisitions, the number of L1 misses (highlighted)                      Besides integer keys, we also measured the lookup performance
increases significantly when going from 1 to 20 threads and the                      for three real-world string data sets. The “genome” data set has
CPU cycles increase by a factor of 6.6×. The slowdown would                          256K strings of average length 10.0, “Wikipedia” contains 16M ar-
be even larger on multi-socket systems, since such systems do                        ticle titles of average length 22.4, and “URL” has 6.4M URLs of
not have a shared cache for inter-thread communication. The CPU                      average length 63.3. The lookup and insert performance with 20
statistics also explain why ART is significantly faster than Masstree                threads is shown in Figure 7. Masstree closes its performance gap
with integer keys. The Optimistic Lock Coupling variant of ART,                      to ART with longer keys. Again, lock coupling is very slow and the
for example, requires 4.8× fewer instructions and 3.6× fewer L3                      synchronized ART variants are slightly slower than the unsynchro-
misses than Masstree.                                                                nized variant.
                                   lookup                               insert                        FOEDUS system [9]. The goal of this paper has been to consoli-
                                                                                                      date these ideas and to present them as general building blocks as
                      75                                                                              opposed to clever tricks used to synchronize individual data struc-
                                                                                                      tures.




                                                                                          genome
                      50                                                                                  The traditional method of synchronizing B-trees, lock coupling,
                                                                                                      was proposed by Bayer and Schkolnick [1]. Graefe’s surveys [6, 5]
                      25
                                                                                                      on low-level synchronization of B-trees, which summarize decades
                       0                                                                              of accumulate wisdom in the database community, focus on fine-
                                                                                                      grained locking. Unfortunately, as was already observed by Cha
M operations/second




                      30                                                                              et al. [4] in 2001, lock coupling simply does not scale on modern
                                                                                                      multi- and many-core CPUs. Since then, the problem has become




                                                                                          Wikipedia
                      20                                                                              even more pronounced and the trend is likely to continue. Like
                                                                                                      Optimistic Lock Coupling, Cha et al.’s solution (OLFIT) uses ver-
                      10                                                                              sions to detect changes within a node. In contrast to Optimistic
                                                                                                      Lock Coupling, OLFIT does not keep track of versions across mul-
                       0
                                                                                                      tiple nodes, but uses the B-link tree idea [11] to support concurrent
                      40
                                                                                                      structure modification operations.
                      30                                                                                  Both the Bw-Tree [15] and Masstree [18] are two B-tree pro-
                                                                                                      posals published in 2012, and both eschew traditional lock cou-


                                                                                          URL
                      20                                                                              pling. The Bw-Tree is latch-free and its nodes can be, due to their
                      10
                                                                                                      relatively large size, moved to disk/SSD [16]. To allow concur-
                                                                                                      rent modifications, delta records are pre-pended to a list of existing
                       0                                                                              changes and the (immutable) B-tree node at the end of the list itself.
                                                                                                      Nodes do not store physical pointers but offsets into a mapping ta-
                           co c.

                                     g

                             O .
                                   EX

                                            TM

                                                   e


                                                          g

                                                                p.

                                                                     EX

                                                                             TM


                                                                                      e
                                    p
                          oc in




                                                 re


                                                        lin




                                                                                     re
                                   n




                                                                                                      bles that points to the delta lists and allows one to atomically add
                                ou




                                                               ou
                               sy

                                pl



                               W




                                                                    W
                                        H

                                             st




                                                                            H

                                                                                  st
                                                       up
                              C




                                                              C
                              u




                                             as




                                                                                 as
                                                                    O
                      no




                                                   co




                                                                                                      new delta records. After a number of updates the deltas and the
                            k




                                                            k
                           R




                                                                R
                                            M




                                                                                 M
                                                          oc
                           k




                                                  ck
                          c
                       .L




                                                        .L
                       lo




                                                 lo




                                                                                                      node are consolidated to a new node. The Bw-Tree has been de-
                            pt




                                                       pt
                           O




                                                      O




                                                                                                      signed with concurrency in mind and its synchronization protocol is
                       Figure 7: Performance for string data with 20 threads                          highly sophisticated; structure modification operations (e.g., node
                                                                                                      splits) are very complex operations involving multiple steps.
                                                                                                          Masstree [18] is a hybrid B-tree/trie data structure and, like ART,
5.4                        Code Complexity                                                            exclusively focuses on the in-memory case. Like the Bw-Tree,
                                                                                                      Masstree was designed with concurrency in mind, but the design-
   To give a rough indication for the relative complexity of the im-                                  ers took different design decisions. The synchronization protocol
plementations, we counted the core algorithmic C++ code lines ex-                                     of Masstree relies on a mix of local locks, clever use of atomic
cluding the lock implementations, garbage collection, comments,                                       operations, and hand-over-hand locking (the idea underlying Opti-
and empty lines:                                                                                      mistic Lock Coupling). Like Optimistic Lock Coupling, but unlike
                                                      lookup    insert      remove                    ROWEX, Masstree lookups must, in some cases, be restarted when
                      no synchronization                  29       95           87                    a conflict with a structure-modifying operation is detected.
                      HTM                                 30       96           88                        Previous work has shown that Hardware Transactional Memory
                      lock coupling                       41      136          139                    can be used to synchronize ART [13, 14] and B-trees [7] with very
                      Optimistic Lock Coupling            44      148          143                    little effort using elided coarse-grained HTM locks. Makreshan-
                      ROWEX                               34      200          156                    ski et al. [17] confirmed these findings in an in-depth experimental
                                                                                                      study but found that performance with HTM can degrade with large
   Using HTM is quite trivial, it merely requires wrapping each                                       tuples or heavy contention. Cervini et al. [3] found that replacing
transaction in a hardware transaction, which we implemented with                                      fine-grained locks with (elided) HTM locks at the same granularity
a Transaction object that starts the HTM transaction in the con-                                      does not improve performance. HTM also has the obvious dis-
structor and commits the transaction in the destructor. The two                                       advantage of requiring special hardware support, which is not yet
lock coupling variants require more changes, with the optimistic                                      widespread.
variant being only marginally larger. ROWEX requires most ad-
ditional code, in particular for the insert and remove operations.
The ROWEX numbers actually underestimate the complexity of
                                                                                                      7.    SUMMARY AND DISCUSSION
ROWEX, since its protocol is fairly subtle in comparison with the                                        We presented two synchronization protocols for ART that have
other variants.                                                                                       good scalability despite relying on locks. The first protocol, Opti-
                                                                                                      mistic Lock Coupling is very simple, requires few changes to the
                                                                                                      underlying data structure, and performs very well as long as con-
6.                         RELATED WORK                                                               flicts are not too frequent. The Read-Optimized Write EXclusion
   Concurrency control is one of the major areas in the database                                      (ROWEX) protocol is more complex, but has the advantage that
field. Most prior work, however, focuses on high-level user transac-                                  reads never block. ROWEX generally requires changes to the data
tions, and not so much on low-level synchronization of data struc-                                    structure itself, as opposed to simply adding a lock at each node.
tures, which is what we study in this work.                                                           However, in our experience, while synchronizing an existing, non-
   Hand-over-hand locking, i.e., the idea underlying Optimistic                                       trivial data structure using ROWEX may be non-trivial, it is, at
Lock Coupling, was used to synchronize binary search trees [2].                                       least, realistic. Truly lock-free algorithms, in contrast, are much
ROWEX-like ideas have also been used before, for example by the                                       more complicated. They also generally require radical changes and
additional indirections to the underlying data structure. The Bw-         [17] D. Makreshanski, J. J. Levandoski, and R. Stutsman. To lock,
Tree, for example, requires an indirection table that causes addi-             swap, or elide: On the interplay of hardware transactional
tional cache misses whenever a node is accessed. Similarly, the                memory and lock-free indexing. PVLDB, 8(11), 2015.
state-of-the-art lock-free hash table, the split-ordered list [19], re-   [18] Y. Mao, E. Kohler, and R. T. Morris. Cache craftiness for fast
quires “dummy” nodes—again at the price of more cache misses.                  multicore key-value storage. In EuroSys, 2012.
   It is an open question how a lock-free variant of ART would            [19] O. Shalev and N. Shavit. Split-ordered lists: Lock-free
look like and how well it would perform. We speculate that it                  extensible hash tables. J. ACM, 53(3):379–405, 2006.
would likely be significantly slower than the Optimistic Lock Cou-
pling and ROWEX implementations. We therefore argue that one
should not discount the use of locks as long as these locks are infre-
                                                                          APPENDIX
quently acquired like in our two protocols. Optimistic Lock Cou-          A. Implementation of Optimistic Locks
pling and ROWEX are two pragmatic paradigms that result in very           The following pseudo code implements optimistic locks. Each
good overall performance. We believe both to be highly practical          node header stores a 64 bit version field that is read and writ-
and generally applicable.                                                 ten atomically. The two least significant bits indicate if the node is
                                                                          obsolete or if the node is locked, respectively. The remaining bits
8.    REFERENCES                                                          store the update counter.
 [1] R. Bayer and M. Schkolnick. Concurrency of operations on
                                                                          struct Node
     B-trees. Acta Informatica, 9, 1977.                                     atomic<uint64_t> version
 [2] N. G. Bronson, J. Casper, H. Chafi, and K. Olukotun. A                  ...
     practical concurrent binary search tree. In PPOPP, pages
                                                                          uint64_t readLockOrRestart(Node node)
     257–268, 2010.                                                          uint64_t version = awaitNodeUnlocked(node)
 [3] D. Cervini, D. Porobic, P. Tözün, and A. Ailamaki. Applying             if isObsolete(version)
     HTM to an OLTP system: No free lunch. In DaMoN, 2015.                      restart()
                                                                             return version
 [4] S. K. Cha, S. Hwang, K. Kim, and K. Kwon.
     Cache-conscious concurrency control of main-memory                   void checkOrRestart(Node node, uint64_t version)
     indexes on shared-memory multiprocessor systems. In                     readUnlockOrRestart(node, version)
     VLDB, 2001.                                                          void readUnlockOrRestart(Node node, uint64_t version)
 [5] G. Graefe. A survey of B-tree locking techniques. ACM                   if version != node.version.load()
     Trans. Database Syst., 35(3), 2010.                                        restart()

 [6] G. Graefe. Modern B-tree techniques. Foundations and                 void readUnlockOrRestart(Node node, uint64_t version,
     Trends in Databases, 3(4):203–402, 2011.                                                      Node lockedNode)
                                                                             if version != node.version.load()
 [7] T. Karnagel, R. Dementiev, R. Rajwar, K. Lai, T. Legler,                   writeUnlock(lockedNode)
     B. Schlegel, and W. Lehner. Improving in-memory database                   restart()
     index performance with Intel transactional synchronization
                                                                          void upgradeToWriteLockOrRestart(Node node, uint64_t version)
     extensions. In HPCA, 2014.                                              if !node.version.CAS(version, setLockedBit(version))
 [8] A. Kemper, T. Neumann, J. Finis, F. Funke, V. Leis,                        restart()
     H. Mühe, T. Mühlbauer, and W. Rödiger. Transaction
                                                                          void upgradeToWriteLockOrRestart(Node node, uint64_t version,
     processing in the hybrid OLTP & OLAP main-memory                                                      Node lockedNode)
     database system HyPer. IEEE Data Eng. Bull., 36(2):41–47,               if !node.version.CAS(version, setLockedBit(version))
     2013.                                                                      writeUnlock(lockedNode)
                                                                                restart()
 [9] H. Kimura. FOEDUS: OLTP engine for a thousand cores and
     NVRAM. In SIGMOD, pages 691–706, 2015.                               void writeLockOrRestart(Node node)
[10] T. Kissinger, B. Schlegel, D. Habich, and W. Lehner.                    uint64_t version
                                                                             do
     KISS-Tree: Smart latch-free in-memory indexing on modern                   version = readLockOrRestart(node)
     architectures. In DaMoN, 2012.                                          while !upgradeToWriteLockOrRestart(node, version)
[11] P. L. Lehman and S. B. Yao. Efficient locking for concurrent         void writeUnlock(Node node)
     operations on B-trees. ACM Trans. Database Syst.,                       // reset locked bit and overflow into version
     6(4):650–670, 1981.                                                     node.version.fetch_add(2)
[12] V. Leis, A. Kemper, and T. Neumann. The adaptive radix               void writeUnlockObsolete(Node node)
     tree: ARTful indexing for main-memory databases. In ICDE,               // set obsolete, reset locked, overflow into version
     2013.                                                                   node.version.fetch_add(3)
[13] V. Leis, A. Kemper, and T. Neumann. Exploiting hardware              // Helper functions
     transactional memory in main-memory databases. In ICDE,              uint64_t awaitNodeUnlocked(Node node)
     2014.                                                                   uint64_t version = node.version.load()
                                                                             while (version & 2) == 2 // spinlock
[14] V. Leis, A. Kemper, and T. Neumann. Scaling                                pause()
     HTM-supported database transactions to many cores. IEEE                    version = node.version.load()
     Trans. Knowl. Data Eng., 28(2), 2016.                                   return version
[15] J. J. Levandoski, D. B. Lomet, and S. Sengupta. The Bw-tree:         uint64_t setLockedBit(uint64_t version)
     A B-tree for new hardware platforms. In ICDE, 2013.                     return version + 2
[16] J. J. Levandoski, D. B. Lomet, and S. Sengupta. LLAMA: a             bool isObsolete(uint64_t version)
     cache/storage subsystem for modern hardware. PVLDB,                     return (version & 1) == 1
     6(10):877–888, 2013.
B. Insert with Optimistic Lock Coupling
The following pseudo code implements the insert operation using
optimistic lock coupling. Initially, the traversal proceed like in the
lookup case without acquiring write locks. Once the node that
needs to be modified is found, it is locked. In cases where the
node must grow the parent is also locked.

 1 insertOpt(key, value, node, level, parent, parentVersion)
 2    version = readLockOrRestart(node)
 3    if !prefixMatches(node, key, level)
 4       upgradeToWriteLockOrRestart(parent, parentVersion)
 5       upgradeToWriteLockOrRestart(node, version, parent)
 6       insertSplitPrefix(key, value, node, level, parent)
 7       writeUnlock(node)
 8       writeUnlock(parent)
 9       return
10    nextNode = node.findChild(key[level])
11    checkOrRestart(node, version)
12    if nextNode == null
13       if node.isFull()
14          upgradeToWriteLockOrRestart(parent, parentVersion)
15          upgradeToWriteLockOrRestart(node, version, parent)
16          insertAndGrow(key, value, node, parent)
17          writeUnlockObsolete(node)
18          writeUnlock(parent)
19       else
20          upgradeToWriteLockOrRestart(node, version)
21          readUnlockOrRestart(parent, parentVersion, node)
22          node.insert(key, value)
23          writeUnlock(node)
24       return
25    if parent != null
26        readUnlockOrRestart(parent, parentVersion)
27    if isLeaf(nextNode)
28        upgradeToWriteLockOrRestart(node, version)
29        insertExpandLeaf(key, value, nextNode, node, parent)
30        writeUnlock(node)
31        return
32    // recurse to next level
33    insertOpt(key, value, nextNode, level+1, node, version)
34    return
