[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_rem Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#108)

``` rust
pub const unsafe fn simd_rem<T>(lhs: T, rhs: T) -> T
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Returns remainder of two vectors elementwise.

`T` must be a vector of integers or floats.

## Safety

For integers, `rhs` must not contain any zero elements. Additionally for signed integers, `<int>::MIN / -1` is undefined behavior.