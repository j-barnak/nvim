[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_select Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#637)

``` rust
pub const unsafe fn simd_select<M, T>(mask: M, if_true: T, if_false: T) -> T
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Selects elements from a mask.

`T` must be a vector.

`M` must be an integer vector with the same length as `T` (but any element size).

For each element, if the corresponding value in `mask` is `!0`, select the element from `if_true`. If the corresponding value in `mask` is `0`, select the element from `if_false`.

## Safety

`mask` must only contain `0` and `!0`.