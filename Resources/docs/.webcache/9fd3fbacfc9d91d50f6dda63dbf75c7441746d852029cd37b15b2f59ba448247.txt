[std](https://doc.rust-lang.org/std/index.html)::[future](https://doc.rust-lang.org/std/future/index.html)

# Function pending Copy item path

1.48.0 · [Source](https://doc.rust-lang.org/src/core/future/pending.rs.html#33)

``` rust
pub fn pending<T>() -> Pending<T> ⓘ
```

Expand description

Creates a future which never resolves, representing a computation that never finishes.

## Examples

``` rust
use std::future;

let future = future::pending();
let () = future.await;
unreachable!();
```