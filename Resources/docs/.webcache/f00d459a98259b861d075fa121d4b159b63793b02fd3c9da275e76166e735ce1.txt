# miasm.ir.symbexec_top

## Module variables

- `TOPSTR = 'TOP'`

## Classes

### class `SymbolicStateTop(StateEngine)`

- **`__init__(self, dct, regstop)`**
- **`__hash__(self)`**
- **`__str__(self)`**
- **`__eq__(self, other)`**
- **`__ne__(self, other)`**
- **`__iter__(self)`**
- **`merge(self, other)`** - Merge two symbolic states
- **`symbols(self)`** - Return the dictionary of known symbols
- **`regstop(self)`** - Return the set of expression with TOP values

### class `SymbExecTopNoMem(SymbolicExecutionEngine)`

Symbolic execution, include TOP value.
ExprMem are not propagated.
Any computation involving a TOP will generate TOP.

- **`__init__(self, lifter, state, regstop, sb_expr_simp=expr_simp)`**
- **`get_state(self)`** - Return the current state of the SymbolicEngine
- **`eval_expr(self, expr, eval_cache=None)`**
- **`manage_mem(self, expr, state, cache, level)`**
- **`eval_exprid(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprId using the current state
- **`eval_exprloc(self, expr, **kwargs)`**
- **`eval_exprcond(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprCond using the current state
- **`eval_exprslice(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprSlice using the current state
- **`eval_exprop(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprOp using the current state
- **`eval_exprcompose(self, expr, **kwargs)`** - [DEV]: Evaluate an ExprCompose using the current state
- **`apply_change(self, dst, src)`**

### class `SymbExecTop(SymbExecTopNoMem)`

Symbolic execution, include TOP value.
ExprMem are propagated.
Any computation involving a TOP will generate TOP.
WARNING: avoid memory aliases here!

- **`manage_mem(self, expr, state, cache, level)`**

## Functions

### `exprid_top(expr)`

Return a TOP expression (ExprId("TOP") of size @expr.size
@expr: expression to replace with TOP
