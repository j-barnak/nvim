[std](https://doc.rust-lang.org/std/index.html)::[io](https://doc.rust-lang.org/std/io/index.html)

# Trait IsTerminal Copy item path

1.70.0 · [Source](https://doc.rust-lang.org/src/std/io/stdio.rs.html#1198-1249)

``` rust
pub trait IsTerminal {
    // Required method
    fn is_terminal(&self) -> bool;
}
```

ⓘ *This trait cannot be implemented outside `std`.*

Expand description

Trait to determine if a descriptor/handle refers to a terminal/tty.

## Required Methods

1.70.0 · [Source](https://doc.rust-lang.org/src/std/io/stdio.rs.html#1248)

Returns `true` if the descriptor/handle refers to a terminal/tty.

On platforms where Rust does not know how to detect a terminal yet, this will return `false`. This will also return `false` if an unexpected error occurred, such as from passing an invalid file descriptor.

##### Platform-specific behavior

On Windows, in addition to detecting consoles, this currently uses some heuristics to detect older msys/cygwin/mingw pseudo-terminals based on device name: devices with names starting with `msys-` or `cygwin-` and ending in `-pty` will be considered terminals. Note that this [may change in the future](https://doc.rust-lang.org/std/io/index.html#platform-specific-behavior "mod std::io").

##### Examples

An example of a type for which `IsTerminal` is implemented is [`Stdin`](https://doc.rust-lang.org/std/io/struct.Stdin.html "struct std::io::Stdin"):

``` rust
use std::io::{self, IsTerminal, Write};

fn main() -> io::Result<()> {
    let stdin = io::stdin();

    // Indicate that the user is prompted for input, if this is a terminal.
    if stdin.is_terminal() {
        print!("> ");
        io::stdout().flush()?;
    }

    let mut name = String::new();
    let _ = stdin.read_line(&mut name)?;

    println!("Hello {}", name.trim_end());

    Ok(())
}
```

The example can be run in two ways:

- If you run this example by piping some text to it, e.g. `echo "foo" | path/to/executable` it will print: `Hello foo`.
- If you instead run the example interactively by running `path/to/executable` directly, it will prompt for input.

## Dyn Compatibility

This trait **is** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

1.70.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/owned.rs.html#250)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

1.70.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/handle.rs.html#418)

Available on **Windows** only.

1.70.0 · [Source](https://doc.rust-lang.org/src/std/io/stdio.rs.html#1263)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/owned.rs.html#250)

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

1.70.0 · [Source](https://doc.rust-lang.org/src/std/os/windows/io/handle.rs.html#418)

Available on **Windows** only.

1.70.0 · [Source](https://doc.rust-lang.org/src/std/io/stdio.rs.html#1263)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/io/stdio.rs.html#1263)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/io/stdio.rs.html#1263)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/io/stdio.rs.html#1263)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/io/stdio.rs.html#1263)

1.70.0 · [Source](https://doc.rust-lang.org/src/std/io/stdio.rs.html#1263)