|                                                                                          |     |
|------------------------------------------------------------------------------------------|-----|
| This page or section is a stub . You can help the wiki by accurately contributing to it. |     |

| Executable Formats                                         |
|------------------------------------------------------------|
| Microsoft                                                  |
| 16 bit: COM MZ NE Mixed (16/32 bit): LE 32/64 bit: PE COFF |
| \*nix                                                      |
| a.out ELF                                                  |
| Apple                                                      |
| Mach-O                                                     |

*Not to be confused with Microsoft's [Component Object Model](https://wiki.osdev.org/index.php?title=Component_Object_Model&action=edit&redlink=1 "Component Object Model (page does not exist)")*.

## Introduction

COM files are extremely simple executable files. They are useful for loading programs without complications like relocating symbols, reading file headers, etc. However, their simplicity can make their usefulness limited, so you may want to support something more complex but useful, like [ELF](https://wiki.osdev.org/ELF "ELF"), once your OS is advanced.

COM files were used by MS-DOS. They are raw binaries, meaning there is no header data. Also, COM files are restricted to the size of one segment (a real-address mode segment, 64kb), minus 256 bytes. When a COM file is loaded, it is assumed that all code and data fits in one segment, and it is loaded to offset `0x100`.

MS-DOS creates and stores an info structure starting at offset 0 and ending just before 0x100, called the *Program Segment Prefix* (PSP). The PSP is made just before starting the COM program. More info about the PSP can be found [here](https://web.archive.org/web/20061101031637/http://heim.ifi.uio.no/~stanisls/helppc/program_segment_prefix.html). The PSP is only required for running DOS programs.

COM files usually terminate with a `RET` instruction. On DOS, the `RET` pops a empty word from the stack, which results in a interrupt `0x20` (DOS API Exit) being called.

## Loading

To load a COM file you need to load it to a low memory location (conventional memory), point `DS` and `ES` to the start of the file with an `org 0x100`, and then jump to the start, with offset `0x100`.

## See Also

### Articles

- [MZ](https://wiki.osdev.org/MZ "MZ")
- [NE](https://wiki.osdev.org/NE "NE")
- [PE](https://wiki.osdev.org/PE "PE")
