# miasm.analysis.modularintervals

Intervals with a maximum size, supporting modular arithmetic

## Classes

### class `ModularIntervals(object)`

Intervals with a maximum size, supporting modular arithmetic

- **`__init__(self, size, intervals=None)`** - Instantiate a ModularIntervals of size @size
- **`_range2interval(func)`** - Convert a function taking 2 ranges to a function taking a ModularIntervals
- **`_range2integer(func)`** - Convert a function taking 1 range and optional arguments to a function
- **`_promote(func)`** - Check and promote the second argument from integer to
- **`_unsigned2signed(self, value)`** - Return the signed value of @value, based on self.size
- **`_signed2unsigned(self, value)`** - Return the unsigned value of @value, based on self.size
- **`_range_add(self, x_min, x_max, y_min, y_max)`** - Bounds interval for x + y, with
- **`_range_minus_uniq(self, x_min, x_max)`** - Bounds interval for -x, with
- **`_range_or_min(self, x_min, x_max, y_min, y_max)`** - Interval min for x | y, with
- **`_range_or_max(self, x_min, x_max, y_min, y_max)`** - Interval max for x | y, with
- **`_range_or(self, x_min, x_max, y_min, y_max)`** - Interval bounds for x | y, with
- **`_range_and_min(self, x_min, x_max, y_min, y_max)`** - Interval min for x & y, with
- **`_range_and_max(self, x_min, x_max, y_min, y_max)`** - Interval max for x & y, with
- **`_range_and(self, x_min, x_max, y_min, y_max)`** - Interval bounds for x & y, with
- **`_range_xor(self, x_min, x_max, y_min, y_max)`** - Interval bounds for x ^ y, with
- **`_range_mul(self, x_min, x_max, y_min, y_max)`** - Interval bounds for x * y, with
- **`_range_mod_uniq(self, x_min, x_max, mod)`** - Interval bounds for x % @mod, with
- **`_range_shift_uniq(self, x_min, x_max, shift, op)`** - Bounds interval for x @op @shift with
- **`_interval_shift(self, operation, shifter)`** - Apply the shifting operation @operation with a shifting
- **`_range_rotate_uniq(self, x_min, x_max, shift, op)`** - Bounds interval for x @op @shift with
- **`_interval_rotate(self, operation, shifter)`** - Apply the rotate operation @operation with a shifting
- **`__add__(self, to_add)`** - Add @to_add to the current intervals
- **`__or__(self, to_or)`** - Bitwise OR @to_or to the current intervals
- **`__and__(self, to_and)`** - Bitwise AND @to_and to the current intervals
- **`__xor__(self, to_xor)`** - Bitwise XOR @to_xor to the current intervals
- **`__mul__(self, to_mul)`** - Multiply @to_mul to the current intervals
- **`__rshift__(self, to_shift)`** - Logical shift right the current intervals of @to_shift
- **`__lshift__(self, to_shift)`** - Logical shift left the current intervals of @to_shift
- **`arithmetic_shift_right(self, to_shift)`** - Arithmetic shift right the current intervals of @to_shift
- **`__neg__(self)`** - Negate the current intervals
- **`__mod__(self, modulo)`** - Apply % @modulo on the current intervals
- **`rotation_right(self, to_rotate)`** - Right rotate the current intervals of @to_rotate
- **`rotation_left(self, to_rotate)`** - Left rotate the current intervals of @to_rotate
- **`mask(self)`** - Return the mask corresponding to the instance size
- **`__iter__(self)`**
- **`length(self)`**
- **`__contains__(self, other)`**
- **`__str__(self)`**
- **`size_update(self, new_size)`** - Update the instance size to @new_size
- **`union(self, to_union)`** - Union set operation with @to_union
- **`update(self, to_union)`** - Union set operation in-place with @to_union
- **`intersection(self, to_intersect)`** - Intersection set operation with @to_intersect
- **`intersection_update(self, to_intersect)`** - Intersection set operation in-place with @to_intersect
