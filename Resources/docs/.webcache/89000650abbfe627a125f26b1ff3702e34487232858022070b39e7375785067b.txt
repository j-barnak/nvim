# Crate alloc Copy item path

1.36.0 · [Source](https://doc.rust-lang.org/src/alloc/lib.rs.html#1-266)

Expand description

## The Rust core allocation and collections library

This library provides smart pointers and collections for managing heap-allocated values.

This library, like core, normally doesn’t need to be used directly since its contents are re-exported in the [`std` crate](https://doc.rust-lang.org/std/index.html). Crates that use the `#![no_std]` attribute however will typically not depend on `std`, so they’d use this crate instead.

### Boxed values

The [`Box`](https://doc.rust-lang.org/alloc/boxed/index.html "mod alloc::boxed") type is a smart pointer type. There can only be one owner of a [`Box`](https://doc.rust-lang.org/alloc/boxed/index.html "mod alloc::boxed"), and the owner can decide to mutate the contents, which live on the heap.

This type can be sent among threads efficiently as the size of a `Box` value is the same as that of a pointer. Tree-like data structures are often built with boxes because each node often has only one owner, the parent.

### Reference counted pointers

The [`Rc`](https://doc.rust-lang.org/alloc/rc/index.html "mod alloc::rc") type is a non-threadsafe reference-counted pointer type intended for sharing memory within a thread. An [`Rc`](https://doc.rust-lang.org/alloc/rc/index.html "mod alloc::rc") pointer wraps a type, `T`, and only allows access to `&T`, a shared reference.

This type is useful when inherited mutability (such as using [`Box`](https://doc.rust-lang.org/alloc/boxed/index.html "mod alloc::boxed")) is too constraining for an application, and is often paired with the [`Cell`](https://doc.rust-lang.org/core/cell/index.html "mod core::cell") or [`RefCell`](https://doc.rust-lang.org/core/cell/index.html "mod core::cell") types in order to allow mutation.

### Atomically reference counted pointers

The [`Arc`](https://doc.rust-lang.org/alloc/sync/index.html "mod alloc::sync") type is the threadsafe equivalent of the [`Rc`](https://doc.rust-lang.org/alloc/rc/index.html "mod alloc::rc") type. It provides all the same functionality of [`Rc`](https://doc.rust-lang.org/alloc/rc/index.html "mod alloc::rc"), except it requires that the contained type `T` is shareable. Additionally, [`Arc<T>`](https://doc.rust-lang.org/alloc/sync/index.html "mod alloc::sync") is itself sendable while [`Rc<T>`](https://doc.rust-lang.org/alloc/rc/index.html "mod alloc::rc") is not.

This type allows for shared access to the contained data, and is often paired with synchronization primitives such as mutexes to allow mutation of shared resources.

### Collections

Implementations of the most common general purpose data structures are defined in this library. They are re-exported through the [standard collections library](https://doc.rust-lang.org/std/collections/index.html).

### Heap interfaces

The [`alloc`](https://doc.rust-lang.org/alloc/alloc/index.html) module defines the low-level interface to the default global allocator. It is not compatible with the libc allocator API.

## Modules

[alloc](https://doc.rust-lang.org/alloc/alloc/index.html "mod alloc::alloc")  
Memory allocation APIs

[borrow](https://doc.rust-lang.org/alloc/borrow/index.html "mod alloc::borrow")  
A module for working with borrowed data.

[boxed](https://doc.rust-lang.org/alloc/boxed/index.html "mod alloc::boxed")  
The `Box<T>` type for heap allocation.

[collections](https://doc.rust-lang.org/alloc/collections/index.html "mod alloc::collections")  
Collection types.

[ffi](https://doc.rust-lang.org/alloc/ffi/index.html "mod alloc::ffi")  
Utilities related to FFI bindings.

[fmt](https://doc.rust-lang.org/alloc/fmt/index.html "mod alloc::fmt")  
Utilities for formatting and printing `String`s.

[rc](https://doc.rust-lang.org/alloc/rc/index.html "mod alloc::rc")  
Single-threaded reference-counting pointers. ‘Rc’ stands for ‘Reference Counted’.

[slice](https://doc.rust-lang.org/alloc/slice/index.html "mod alloc::slice")  
Utilities for the slice primitive type.

[str](https://doc.rust-lang.org/alloc/str/index.html "mod alloc::str")  
Utilities for the `str` primitive type.

[string](https://doc.rust-lang.org/alloc/string/index.html "mod alloc::string")  
A UTF-8–encoded, growable string.

[sync](https://doc.rust-lang.org/alloc/sync/index.html "mod alloc::sync")  
Thread-safe reference-counting pointers.

[task](https://doc.rust-lang.org/alloc/task/index.html "mod alloc::task")  
Types and Traits for working with asynchronous tasks.

[vec](https://doc.rust-lang.org/alloc/vec/index.html "mod alloc::vec")  
A contiguous growable array type with heap-allocated contents, written `Vec<T>`.

[bstr](https://doc.rust-lang.org/alloc/bstr/index.html "mod alloc::bstr")Experimental  
The `ByteStr` and `ByteString` types and trait implementations.

[intrinsics](https://doc.rust-lang.org/alloc/intrinsics/index.html "mod alloc::intrinsics")Experimental  
Intrinsics that cannot be moved to `core` because they depend on `alloc` types.

[io](https://doc.rust-lang.org/alloc/io/index.html "mod alloc::io")Experimental  
Traits, helpers, and type definitions for core I/O functionality.

## Macros

[format](https://doc.rust-lang.org/alloc/macro.format.html "macro alloc::format")  
Creates a `String` using interpolation of runtime expressions.

[vec](https://doc.rust-lang.org/alloc/macro.vec.html "macro alloc::vec")  
Creates a [`Vec`](https://doc.rust-lang.org/alloc/vec/struct.Vec.html "struct alloc::vec::Vec") containing the arguments.