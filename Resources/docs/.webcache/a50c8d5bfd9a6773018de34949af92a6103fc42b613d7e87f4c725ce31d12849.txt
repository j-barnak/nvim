[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[unix](https://doc.rust-lang.org/std/os/unix/index.html)::[fs](https://doc.rust-lang.org/std/os/unix/fs/index.html)

# Trait DirEntryExt2 Copy item path

[Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#1017-1039)

``` rust
pub trait DirEntryExt2 {
    // Required method
    fn file_name_ref(&self) -> &OsStr;
}
```

ⓘ *This trait cannot be implemented outside `std`.*

🔬This is a nightly-only experimental API. (`dir_entry_ext2` [\#85573](https://github.com/rust-lang/rust/issues/85573))

Available on **Unix** only.

Expand description

Unix-specific extension methods for [`fs::DirEntry`](https://doc.rust-lang.org/std/fs/struct.DirEntry.html "struct std::fs::DirEntry").

## Required Methods

[Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#1038)

🔬This is a nightly-only experimental API. (`dir_entry_ext2` [\#85573](https://github.com/rust-lang/rust/issues/85573))

Returns a reference to the underlying `OsStr` of this entry’s filename.

##### Examples

``` rust
#![feature(dir_entry_ext2)]
use std::os::unix::fs::DirEntryExt2;
use std::{fs, io};

fn main() -> io::Result<()> {
    let mut entries = fs::read_dir(".")?.collect::<Result<Vec<_>, io::Error>>()?;
    entries.sort_unstable_by(|a, b| a.file_name_ref().cmp(b.file_name_ref()));

    for p in entries {
        println!("{p:?}");
    }

    Ok(())
}
```

## Dyn Compatibility

This trait **is** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

[Source](https://doc.rust-lang.org/src/std/os/unix/fs.rs.html#1042-1046)