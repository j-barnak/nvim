[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)::[poison](https://doc.rust-lang.org/std/sync/poison/index.html)

# Type Alias TryLockResult Copy item path

[Source](https://doc.rust-lang.org/src/std/sync/poison.rs.html#240)

``` rust
pub type TryLockResult<Guard> = Result<Guard, TryLockError<Guard>>;
```

🔬This is a nightly-only experimental API. (`sync_poison_mod` [\#134646](https://github.com/rust-lang/rust/issues/134646))

Expand description

A type alias for the result of a nonblocking locking method.

For more information, see [`LockResult`](https://doc.rust-lang.org/std/sync/type.LockResult.html "type std::sync::LockResult"). A `TryLockResult` doesn’t necessarily hold the associated guard in the [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") type as the lock might not have been acquired for other reasons.

## Aliased Type

``` rust
pub enum TryLockResult<Guard> {
    Ok(Guard),
    Err(TryLockError<Guard>),
}
```

## Variants

🔬This is a nightly-only experimental API. (`sync_poison_mod` [\#134646](https://github.com/rust-lang/rust/issues/134646))

Contains the success value

🔬This is a nightly-only experimental API. (`sync_poison_mod` [\#134646](https://github.com/rust-lang/rust/issues/134646))

Contains the error value