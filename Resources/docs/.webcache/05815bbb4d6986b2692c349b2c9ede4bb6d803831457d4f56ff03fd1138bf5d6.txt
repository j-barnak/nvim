[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[mir](https://doc.rust-lang.org/std/intrinsics/mir/index.html)

# Function CastTransmute Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/mir.rs.html#478-485)

``` rust
pub fn CastTransmute<T, U>(operand: T) -> U
```

🔬This is a nightly-only experimental API. (`custom_mir`)

Expand description

Emits a `CastKind::Transmute` cast.

Needed to test the UB when `sizeof(T) != sizeof(U)`, which can’t be generated via the normal `mem::transmute`.