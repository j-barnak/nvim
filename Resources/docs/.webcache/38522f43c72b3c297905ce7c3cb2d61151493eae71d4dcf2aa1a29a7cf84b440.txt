[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)

# Function ptr_guaranteed_cmp Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/mod.rs.html#2297)

``` rust
pub const fn ptr_guaranteed_cmp<T>(ptr: *const T, other: *const T) -> u8
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

See documentation of `<*const T>::guaranteed_eq` for details. Returns `2` if the result is unknown. Returns `1` if the pointers are guaranteed equal. Returns `0` if the pointers are guaranteed inequal.