[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)

# Function volatile_store Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/mod.rs.html#1005)

``` rust
pub unsafe fn volatile_store<T>(dst: *mut T, val: T)
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Performs a volatile store to the `dst` pointer.

The stabilized version of this intrinsic is [`core::ptr::write_volatile`](https://doc.rust-lang.org/std/ptr/fn.write_volatile.html "fn std::ptr::write_volatile").