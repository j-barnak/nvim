[std](https://doc.rust-lang.org/std/index.html)::[cell](https://doc.rust-lang.org/std/cell/index.html)

# Struct Ref Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/core/cell.rs.html#1614)

``` rust
pub struct Ref<'b, T>where
    T: 'b + ?Sized,{ /* private fields */ }
```

Expand description

Wraps a borrowed reference to a value in a `RefCell` box. A wrapper type for an immutably borrowed value from a `RefCell<T>`.

See the [module-level documentation](https://doc.rust-lang.org/std/cell/index.html "mod std::cell") for more.

## Implementations

[Source](https://doc.rust-lang.org/src/core/cell.rs.html#1637)

1.15.0 (const: [unstable](https://github.com/rust-lang/rust/issues/137844 "Tracking issue for const_ref_cell")) · [Source](https://doc.rust-lang.org/src/core/cell.rs.html#1650)

Copies a `Ref`.

The `RefCell` is already immutably borrowed, so this cannot fail.

This is an associated function that needs to be used as `Ref::clone(...)`. A `Clone` implementation or a method would interfere with the widespread use of `r.borrow().clone()` to clone the contents of a `RefCell`.

1.8.0 · [Source](https://doc.rust-lang.org/src/core/cell.rs.html#1674-1676)

Makes a new `Ref` for a component of the borrowed data.

The `RefCell` is already immutably borrowed, so this cannot fail.

This is an associated function that needs to be used as `Ref::map(...)`. A method would interfere with methods of the same name on the contents of a `RefCell` used through `Deref`.

##### Examples

``` rust
use std::cell::{RefCell, Ref};

let c = RefCell::new((5, 'b'));
let b1: Ref<'_, (u32, char)> = c.borrow();
let b2: Ref<'_, u32> = Ref::map(b1, |t| &t.0);
assert_eq!(*b2, 5)
```

1.63.0 · [Source](https://doc.rust-lang.org/src/core/cell.rs.html#1703-1705)

Makes a new `Ref` for an optional component of the borrowed data. The original guard is returned as an `Err(..)` if the closure returns `None`.

The `RefCell` is already immutably borrowed, so this cannot fail.

This is an associated function that needs to be used as `Ref::filter_map(...)`. A method would interfere with methods of the same name on the contents of a `RefCell` used through `Deref`.

##### Examples

``` rust
use std::cell::{RefCell, Ref};

let c = RefCell::new(vec![1, 2, 3]);
let b1: Ref<'_, Vec<u32>> = c.borrow();
let b2: Result<Ref<'_, u32>, _> = Ref::filter_map(b1, |v| v.get(1));
assert_eq!(*b2.unwrap(), 2);
```

[Source](https://doc.rust-lang.org/src/core/cell.rs.html#1744-1747)

🔬This is a nightly-only experimental API. (`refcell_try_map` [\#143801](https://github.com/rust-lang/rust/issues/143801))

Tries to makes a new `Ref` for a component of the borrowed data. On failure, the original guard is returned alongside with the error returned by the closure.

The `RefCell` is already immutably borrowed, so this cannot fail.

This is an associated function that needs to be used as `Ref::try_map(...)`. A method would interfere with methods of the same name on the contents of a `RefCell` used through `Deref`.

##### Examples

``` rust
#![feature(refcell_try_map)]
use std::cell::{RefCell, Ref};
use std::str::{from_utf8, Utf8Error};

let c = RefCell::new(vec![0xF0, 0x9F, 0xA6 ,0x80]);
let b1: Ref<'_, Vec<u8>> = c.borrow();
let b2: Result<Ref<'_, str>, _> = Ref::try_map(b1, |v| from_utf8(v));
assert_eq!(&*b2.unwrap(), "🦀");

let c = RefCell::new(vec![0xF0, 0x9F, 0xA6]);
let b1: Ref<'_, Vec<u8>> = c.borrow();
let b2: Result<_, (Ref<'_, Vec<u8>>, Utf8Error)> = Ref::try_map(b1, |v| from_utf8(v));
let (b3, e) = b2.unwrap_err();
assert_eq!(*b3, vec![0xF0, 0x9F, 0xA6]);
assert_eq!(e.valid_up_to(), 0);
```

1.35.0 · [Source](https://doc.rust-lang.org/src/core/cell.rs.html#1776-1778)

Splits a `Ref` into multiple `Ref`s for different components of the borrowed data.

The `RefCell` is already immutably borrowed, so this cannot fail.

This is an associated function that needs to be used as `Ref::map_split(...)`. A method would interfere with methods of the same name on the contents of a `RefCell` used through `Deref`.

##### Examples

``` rust
use std::cell::{Ref, RefCell};

let cell = RefCell::new([1, 2, 3, 4]);
let borrow = cell.borrow();
let (begin, end) = Ref::map_split(borrow, |slice| slice.split_at(2));
assert_eq!(*begin, [1, 2]);
assert_eq!(*end, [3, 4]);
```

[Source](https://doc.rust-lang.org/src/core/cell.rs.html#1814)

🔬This is a nightly-only experimental API. (`cell_leak` [\#69099](https://github.com/rust-lang/rust/issues/69099))

Converts into a reference to the underlying data.

The underlying `RefCell` can never be mutably borrowed from again and will always appear already immutably borrowed. It is not a good idea to leak more than a constant number of references. The `RefCell` can be immutably borrowed again if only a smaller number of leaks have occurred in total.

This is an associated function that needs to be used as `Ref::leak(...)`. A method would interfere with methods of the same name on the contents of a `RefCell` used through `Deref`.

##### Examples

``` rust
#![feature(cell_leak)]
use std::cell::{RefCell, Ref};
let cell = RefCell::new(0);

let value = Ref::leak(cell.borrow());
assert_eq!(*value, 0);

assert!(cell.try_borrow().is_ok());
assert!(cell.try_borrow_mut().is_err());
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/core/cell.rs.html#1826)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/fmt/mod.rs.html#3168)

[Source](https://doc.rust-lang.org/src/core/fmt/mod.rs.html#3169)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143773 "Tracking issue for const_convert")) · [Source](https://doc.rust-lang.org/src/core/cell.rs.html#1624)

[Source](https://doc.rust-lang.org/src/core/cell.rs.html#1625)

The resulting type after dereferencing.

[Source](https://doc.rust-lang.org/src/core/cell.rs.html#1628)

Dereferences the value.

[Source](https://doc.rust-lang.org/src/core/cell.rs.html#1635)

1.20.0 · [Source](https://doc.rust-lang.org/src/core/cell.rs.html#1829)

[Source](https://doc.rust-lang.org/src/core/cell.rs.html#1830)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Display.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/core/cell.rs.html#2722)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/ops/deref.rs.html#379-381)

[Source](https://doc.rust-lang.org/src/core/ops/deref.rs.html#383)

🔬This is a nightly-only experimental API. (`arbitrary_self_types` [\#44874](https://github.com/rust-lang/rust/issues/44874))

The target type on which the method may be called.

[Source](https://doc.rust-lang.org/src/alloc/string.rs.html#2918)

[Source](https://doc.rust-lang.org/src/alloc/string.rs.html#2920)

Converts the given value to a `String`. [Read more](https://doc.rust-lang.org/std/string/trait.ToString.html#tymethod.to_string)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.