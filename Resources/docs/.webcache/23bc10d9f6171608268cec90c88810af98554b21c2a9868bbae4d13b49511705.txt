[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)::[oneshot](https://doc.rust-lang.org/std/sync/oneshot/index.html)

# Struct Receiver Copy item path

[Source](https://doc.rust-lang.org/src/std/sync/oneshot.rs.html#174-177)

``` rust
pub struct Receiver<T> { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`oneshot_channel` [\#143674](https://github.com/rust-lang/rust/issues/143674))

Expand description

The receiving half of a oneshot channel.

## Examples

``` rust
#![feature(oneshot_channel)]
use std::sync::oneshot;
use std::thread;
use std::time::Duration;

let (sender, receiver) = oneshot::channel();

thread::spawn(move || {
    thread::sleep(Duration::from_millis(100));
    sender.send("Hello after delay!").unwrap();
});

println!("Waiting for message...");
println!("{}", receiver.recv().unwrap());
```

`Receiver` cannot be sent between threads if it is receiving non-`Send` types.

[ⓘ](# "This example deliberately fails to compile")

``` rust
let (sender, receiver) = oneshot::channel();

struct NotSend(*mut ());
sender.send(NotSend(ptr::null_mut()));

thread::spawn(move || {
    let reply = receiver.try_recv().unwrap();
});
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/sync/oneshot.rs.html#185-322)

[Source](https://doc.rust-lang.org/src/std/sync/oneshot.rs.html#209-211)

🔬This is a nightly-only experimental API. (`oneshot_channel` [\#143674](https://github.com/rust-lang/rust/issues/143674))

Receives the value from the sending end, blocking the calling thread until it gets it.

Can only fail if the corresponding [`Sender<T>`](https://doc.rust-lang.org/std/sync/oneshot/struct.Sender.html "struct std::sync::oneshot::Sender") has been dropped.

##### Examples

``` rust
#![feature(oneshot_channel)]
use std::sync::oneshot;
use std::thread;
use std::time::Duration;

let (tx, rx) = oneshot::channel();

thread::spawn(move || {
    thread::sleep(Duration::from_millis(500));
    tx.send("Done!").unwrap();
});

// This will block until the message arrives.
println!("{}", rx.recv().unwrap());
```

[Source](https://doc.rust-lang.org/src/std/sync/oneshot.rs.html#250-255)

🔬This is a nightly-only experimental API. (`oneshot_channel` [\#143674](https://github.com/rust-lang/rust/issues/143674))

Attempts to return a pending value on this receiver without blocking.

##### Examples

``` rust
#![feature(oneshot_channel)]
use std::sync::oneshot;
use std::thread;
use std::time::Duration;

let (sender, mut receiver) = oneshot::channel();

thread::spawn(move || {
    thread::sleep(Duration::from_millis(100));
    sender.send(42).unwrap();
});

// Keep trying until we get the message, doing other work in the process.
loop {
    match receiver.try_recv() {
        Ok(value) => {
            assert_eq!(value, 42);
            break;
        }
        Err(oneshot::TryRecvError::Empty(rx)) => {
            // Retake ownership of the receiver.
            receiver = rx;
            do_other_work();
        }
        Err(oneshot::TryRecvError::Disconnected) => panic!("Sender disconnected"),
    }
}
```

[Source](https://doc.rust-lang.org/src/std/sync/oneshot.rs.html#283-288)

🔬This is a nightly-only experimental API. (`oneshot_channel` [\#143674](https://github.com/rust-lang/rust/issues/143674))

Attempts to wait for a value on this receiver, returning an error if the corresponding [`Sender`](https://doc.rust-lang.org/std/sync/oneshot/struct.Sender.html "struct std::sync::oneshot::Sender") half of this channel has been dropped, or if it waits more than `timeout`.

##### Examples

``` rust
#![feature(oneshot_channel)]
use std::sync::oneshot;
use std::thread;
use std::time::Duration;

let (sender, receiver) = oneshot::channel();

thread::spawn(move || {
    thread::sleep(Duration::from_millis(500));
    sender.send("Success!").unwrap();
});

// Wait up to 1 second for the message
match receiver.recv_timeout(Duration::from_secs(1)) {
    Ok(msg) => println!("Received: {}", msg),
    Err(oneshot::RecvTimeoutError::Timeout(_)) => println!("Timed out!"),
    Err(oneshot::RecvTimeoutError::Disconnected) => println!("Sender dropped!"),
}
```

[Source](https://doc.rust-lang.org/src/std/sync/oneshot.rs.html#316-321)

🔬This is a nightly-only experimental API. (`oneshot_channel` [\#143674](https://github.com/rust-lang/rust/issues/143674))

Attempts to wait for a value on this receiver, returning an error if the corresponding [`Sender`](https://doc.rust-lang.org/std/sync/oneshot/struct.Sender.html "struct std::sync::oneshot::Sender") half of this channel has been dropped, or if `deadline` is reached.

##### Examples

``` rust
#![feature(oneshot_channel)]
use std::sync::oneshot;
use std::thread;
use std::time::{Duration, Instant};

let (sender, receiver) = oneshot::channel();

thread::spawn(move || {
    thread::sleep(Duration::from_millis(100));
    sender.send("Just in time!").unwrap();
});

let deadline = Instant::now() + Duration::from_millis(500);
match receiver.recv_deadline(deadline) {
    Ok(msg) => println!("Received: {}", msg),
    Err(oneshot::RecvTimeoutError::Timeout(_)) => println!("Missed deadline!"),
    Err(oneshot::RecvTimeoutError::Disconnected) => println!("Sender dropped!"),
}
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/std/sync/oneshot.rs.html#325-329)

[Source](https://doc.rust-lang.org/src/std/sync/oneshot.rs.html#326-328)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/std/sync/oneshot.rs.html#183)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.