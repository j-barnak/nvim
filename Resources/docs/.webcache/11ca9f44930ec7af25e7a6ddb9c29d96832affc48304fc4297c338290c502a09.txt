# angr.misc.loggers

class angr.misc.loggers.Loggers  
Bases: [`object`](https://docs.python.org/3/library/functions.html#object "(in Python v3.14)")

Implements a loggers manager for angr.

\_\_init\_\_(*default_level=30*)  

default_level  

profiling_enabled  

handler  

IN_SCOPE = ('angr', 'claripy', 'cle', 'pyvex', 'archinfo', 'tracer', 'driller', 'rex', 'patcherex', 'identifier')  

load_all_loggers()  
A dumb and simple way to conveniently aggregate all loggers.

Adds attributes to this instance of each registered logger, replacing ‘.’ with ‘\_’

enable_root_logger()  
Enable angr’s default logger

disable_root_logger()  
Disable angr’s default logger

static setall(*level*)  

&nbsp;

class angr.misc.loggers.CuteFormatter  
Bases: [`Formatter`](https://docs.python.org/3/library/logging.html#logging.Formatter "(in Python v3.14)")

A log formatter that can print log messages with colors.

&nbsp;

angr.misc.loggers.is_enabled_for(*logger*, *level*)  