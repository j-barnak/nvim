[std](https://doc.rust-lang.org/std/index.html)::[usize](https://doc.rust-lang.org/std/usize/index.html)

# Constant MIN Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/core/num/shells/legacy_int_modules.rs.html#71)

``` rust
pub const MIN: usize = usize::MIN; // 0usize
```

👎Deprecating in a future version:

replaced by the `MIN` associated constant on this type

Expand description

The smallest value that can be represented by this integer type. Use [`usize::MIN`](https://doc.rust-lang.org/std/primitive.usize.html#associatedconstant.MIN "associated constant usize::MIN") instead.

## Examples

``` rust
// deprecated way
let min = std::usize::MIN;

// intended way
let min = usize::MIN;
```