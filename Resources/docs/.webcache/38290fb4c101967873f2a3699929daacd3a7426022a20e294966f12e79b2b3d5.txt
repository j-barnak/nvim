[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)::[nonpoison](https://doc.rust-lang.org/std/sync/nonpoison/index.html)

# Struct RwLockWriteGuard Copy item path

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#107-110)

``` rust
pub struct RwLockWriteGuard<'rwlock, T: ?Sized + 'rwlock> { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`nonpoison_rwlock` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Expand description

RAII structure used to release the exclusive write access of a lock when dropped.

This structure is created by the [`write`](https://doc.rust-lang.org/std/sync/nonpoison/struct.RwLock.html#method.write "method std::sync::nonpoison::RwLock::write") and [`try_write`](https://doc.rust-lang.org/std/sync/nonpoison/struct.RwLock.html#method.try_write "method std::sync::nonpoison::RwLock::try_write") methods on [`RwLock`](https://doc.rust-lang.org/std/sync/nonpoison/struct.RwLock.html "struct std::sync::nonpoison::RwLock").

## Implementations

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#678-835)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#754-766)

🔬This is a nightly-only experimental API. (`nonpoison_rwlock` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Downgrades a write-locked `RwLockWriteGuard` into a read-locked [`RwLockReadGuard`](https://doc.rust-lang.org/std/sync/nonpoison/struct.RwLockReadGuard.html "struct std::sync::nonpoison::RwLockReadGuard").

Since we have the `RwLockWriteGuard`, the [`RwLock`](https://doc.rust-lang.org/std/sync/nonpoison/struct.RwLock.html "struct std::sync::nonpoison::RwLock") must already be locked for writing, so this method cannot fail.

After downgrading, other readers will be allowed to read the protected data.

##### Examples

`downgrade` takes ownership of the `RwLockWriteGuard` and returns a [`RwLockReadGuard`](https://doc.rust-lang.org/std/sync/nonpoison/struct.RwLockReadGuard.html "struct std::sync::nonpoison::RwLockReadGuard").

``` rust
#![feature(nonpoison_rwlock)]

use std::sync::nonpoison::{RwLock, RwLockWriteGuard};

let rw = RwLock::new(0);

let mut write_guard = rw.write();
*write_guard = 42;

let read_guard = RwLockWriteGuard::downgrade(write_guard);
assert_eq!(42, *read_guard);
```

`downgrade` will *atomically* change the state of the [`RwLock`](https://doc.rust-lang.org/std/sync/nonpoison/struct.RwLock.html "struct std::sync::nonpoison::RwLock") from exclusive mode into shared mode. This means that it is impossible for another writing thread to get in between a thread calling `downgrade` and any reads it performs after downgrading.

``` rust
#![feature(nonpoison_rwlock)]

use std::sync::Arc;
use std::sync::nonpoison::{RwLock, RwLockWriteGuard};

let rw = Arc::new(RwLock::new(1));

// Put the lock in write mode.
let mut main_write_guard = rw.write();

let rw_clone = rw.clone();
let evil_handle = std::thread::spawn(move || {
    // This will not return until the main thread drops the `main_read_guard`.
    let mut evil_guard = rw_clone.write();

    assert_eq!(*evil_guard, 2);
    *evil_guard = 3;
});

*main_write_guard = 2;

// Atomically downgrade the write guard into a read guard.
let main_read_guard = RwLockWriteGuard::downgrade(main_write_guard);

// Since `downgrade` is atomic, the writer thread cannot have changed the protected data.
assert_eq!(*main_read_guard, 2, "`downgrade` was not atomic");
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#783-795)

🔬This is a nightly-only experimental API. (`mapped_lock_guards` [\#117108](https://github.com/rust-lang/rust/issues/117108))

Makes a [`MappedRwLockWriteGuard`](https://doc.rust-lang.org/std/sync/nonpoison/struct.MappedRwLockWriteGuard.html "struct std::sync::nonpoison::MappedRwLockWriteGuard") for a component of the borrowed data, e.g. an enum variant.

The `RwLock` is already locked for writing, so this cannot fail.

This is an associated function that needs to be used as `RwLockWriteGuard::map(...)`. A method would interfere with methods of the same name on the contents of the `RwLockWriteGuard` used through `Deref`.

##### Panics

If the closure panics, the guard will be dropped (unlocked).

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#813-834)

🔬This is a nightly-only experimental API. (`mapped_lock_guards` [\#117108](https://github.com/rust-lang/rust/issues/117108))

Makes a [`MappedRwLockWriteGuard`](https://doc.rust-lang.org/std/sync/nonpoison/struct.MappedRwLockWriteGuard.html "struct std::sync::nonpoison::MappedRwLockWriteGuard") for a component of the borrowed data. The original guard is returned as an `Err(...)` if the closure returns `None`.

The `RwLock` is already locked for writing, so this cannot fail.

This is an associated function that needs to be used as `RwLockWriteGuard::filter_map(...)`. A method would interfere with methods of the same name on the contents of the `RwLockWriteGuard` used through `Deref`.

##### Panics

If the closure panics, the guard will be dropped (unlocked).

## Trait Implementations

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#113)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#1097-1101)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#1098-1100)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#1031-1038)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#1032)

The resulting type after dereferencing.

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#1034-1037)

Dereferences the value.

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#1041-1046)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#1042-1045)

Mutably dereferences the value.

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#1104-1108)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#1105-1107)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Display.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#987-994)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#988-993)

Executes the destructor for this type. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop)

[Source](https://doc.rust-lang.org/src/core/ops/drop.rs.html#267)

🔬This is a nightly-only experimental API. (`pin_ergonomics` [\#130494](https://github.com/rust-lang/rust/issues/130494))

Execute the destructor for this type, but different to [`Drop::drop`](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop "method std::ops::Drop::drop"), it requires `self` to be pinned. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.pin_drop)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/rwlock.rs.html#116)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/ops/deref.rs.html#379-381)

[Source](https://doc.rust-lang.org/src/core/ops/deref.rs.html#383)

🔬This is a nightly-only experimental API. (`arbitrary_self_types` [\#44874](https://github.com/rust-lang/rust/issues/44874))

The target type on which the method may be called.

[Source](https://doc.rust-lang.org/src/alloc/string.rs.html#2918)

[Source](https://doc.rust-lang.org/src/alloc/string.rs.html#2920)

Converts the given value to a `String`. [Read more](https://doc.rust-lang.org/std/string/trait.ToString.html#tymethod.to_string)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.