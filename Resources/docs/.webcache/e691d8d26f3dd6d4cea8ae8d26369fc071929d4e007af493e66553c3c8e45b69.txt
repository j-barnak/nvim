# Lecture 11: Hardware Support for Software Security

Hardware Support for Memory Safety
              Mengjia Yan
              Spring 2026
Overview
• What attacker can do with software bugs?
   • Demos, variations, real-world examples

• Hardware mitigations: what are the design tradeoffs?




                                    Szekeres et al. SoK: Eternal War in Memory. SP’13

                                                                                        2
The Problem: Software Bugs
• Low-level Language Basics (C/C++/Assembly)
   + Efficient, programmers have more control
   - Bugs
   - Programming productivity

• Widely used in production systems and
  legacy systems
   • Operating systems, web browsers, etc.
   • Large CVE numbers every year


                                                3
The Problems of Using Pointers
• Pointer = Address of variables:
   • An 64-bit integer to indicate memory location where variable is stored
• It is programmers’ responsibility to do pointer check, e.g. NULL, out-
  of-bound, use-after-free




                                                                              4
Buffer Overflow Example
char * buffer1 = (char*)malloc(buffer1_size);
char * buffer2 = (char*)malloc(buffer2_size);

// fill up buffer2 with ‘B’
for (int i=0; i<buffer2_size; i++){
   buffer2[i] = 'B';
 }

// fill up buffer1 with ‘A’
for (int i=0; i< 2buffer1_size
                   * buffer1_size ; i++){
   buffer1[i] = 'A';
 }

                                                5
Use-after-free Example
char * buffer1 = (char*)malloc(buffer1_size);
char * buffer2 = (char*)malloc(buffer2_size);
// fill buffer1 and buffer2 with ‘A’ and ‘B’ respectively


 free(buffer1);
 char *buffer3 = (char*)malloc(buffer3_size);
 for (int i=0; i<buffer3_size; i++){
     buffer3[i] = 'C';
 }
// print buffer 1 2 3

                                                            6
Memory Corruption Vulnerabilities
• Spatial safety:
   • out-of-bound (inter-object, intra-object)
   • Can happen on heap and stack


• Temporal safety:
   • Use-after-free
   • Use before initialization

• Why Python (and other high-level programming language) does not have
  these problems?
   • out-of-bound access => emit runtime checks
   • use-after-free => garbage collection
                                                                         7
From Bugs to Exploitations
Via manipulating function pointers




                                     8
Recap Stack Operations
                             TEXT (code)


      int hello(){
          int x = 100;          stack
          return x;
      }

      int main(){
          int a;
          int *p = &a;
          int b = hello();
 ra       b = b+3;
          return 0;
      }


                                           9
Stack Smash: Manipulate the return address (ra)
        // victim code
        int func (char *str) {                Shell code:
            char buffer[12];
                                              PUSH “/bin/sh”
            strncpy(buffer, str, len(str));   CALL system
            return 1;
        }

                                              Input str:
        int main() {
          …                                   Shell code
            func (input);                     .. Some padding..
   ra                                         Address of buffer
            …
        }
                                                                  10
Stack Smash / Code Injection Attack
              TEXT (code)   TEXT (code)



                stack         stack




                buffer      Shell code


                                …
                   …
              Return addr   Return addr



                                          11
                 Make a pointer
                go out of bounds


Mitigations       Use pointer to
                  write (or free)


  TEXT (code)
                                       Modify a
                                    code pointer …

    stack                      … to the address of
                                                                   ASLR
                                shellcode/gadget



                                                   Use pointer by         Stack canary,
   Shell code                                    return instruction       Shadow stack



       …                                             Execute injected     Non-executable
                                                        shellcode          Data (W⊕X)
  Return addr


                                     Control flow
                                     hijack attack                                        12
   Return-Oriented Programming (ROP)
     TEXT (code)        !writable &   executable          TEXT (code)          Gadget example:
                                                                                 pop rdi
                                                                                 ret
        stack                                                stack




      Shell code
                        writable & !executable
          …
     Return addr                                          return addr

                                                         return addr 2..

    Stack Smash:                                             ROP:
execute injected code     Blocked by “W⊕X”         execute pre-existing code                     13
     An Attack Puzzle                                      Jump-oriented programming


                                    int main(int argc, char *argv[]){
0x4011b6: int func0(u_int64_t s);       // an array with two function pointers
……                                      // to emulate a vtable
0x4011f2: int func1(u_int64_t s);       int (*func_ptr[2])(u_int64_t) = {&func0, &func1};
……                                      u_int64_t val;
                                        int choice;
0x40122e: int hack(u_int64_t s);
……
                                        // obtain args from input; attacker-controlled
                                        choice = atoi(argv[1]);
                                        val = strtol(argv[2], NULL, 16);
                                        // index into the array to make a function call
                                        (*func_ptr[choice])(val);
                                    }
                                                                                       14
Hardware Supported Mitigations

                                 15
                                                 Make a pointer                   Make a pointer
                                                go out of bounds                 become dangling




                                                   Use pointer to                 Use pointer to
                                                   write (or free)                    read



     Modify a                Modify code …                              Modify a                             Modify a         Output data
    data pointer                                                     code pointer …                       data variable …      variable


                            … to the attacker                   … to the address of                      … to the attacker    Interpret the
                             specified code                      shellcode/gadget                         specified value      output data



                                                  Use pointer by                    Use pointer by       Use corrupted data
                                                indirect call/jump                return instruction          variable



                                                Execute available                     Execute injected
                                                gadgets/functions                        shellcode
L. Szekeres, M. Payer, T. Wei and
D. Song, "SoK: Eternal War in
Memory,” S&P’2013

                            Code corruption                           Control flow                           Data-only        Information
                                attack                                hijack attack                           attack              leak16
  Memory Safety
• Strongest security property that tries to address the problem at the root.

• Idea: include metadata and perform security checks at runtime
   • Spatial safety (bound information)
   • Temporal safety (allocation/de-allocation information)

• Software solutions
   • Problem #1: performance overhead, extra instructions to perform the check
   • Problem #2: where to store metadata? -> in shadow memory

           SoftBound: Highly Compatible and Complete Spatial Memory Safety for C; Nagarakatte et al; PLDI’09
                                                                                                               17
SoftBound
                                                       shadow memory
Creating a pointer:            0xBFABCD   table[ptr]   base, bound     [0x3000, 0x3040]
   int array[100];
   ptr = &array;
   ptr_base = &array[0];
   ptr_bound = &array[100];
   table[ptr]={base, bound};          0xFFABCD   ptr      array        array=0x3000




                                            array[0]

                                                                       array[100]


                                                                                18
SoftBound
                                                            shadow memory
Creating a pointer:                 0xBFABCD table[ptr]                     [0x3000, 0x3040]
                                                            base, bound
   int array[100];
                                          table[newptr]     base, bound     [0x3000, 0x3040]
   ptr = &array;                    0xBFACCD
   ptr_base = &array[0];
   ptr_bound = &array[100];
   table[ptr]={base, bound};               0xFFABCD   ptr      array        array=0x3000


Pointer arithmetic:                      0xFFACCD newptr      array_p2      array_p2=0x3010

   int* array_p2 = &array[10];                  array[0]
   newptr_base = table[ptr].base;                                           array[100]
   newptr_bound = table[ptr].bound;
   table[newptr]={base, bound};
                                                                                     19
                                       How many memory
                                     accesses do we need to
SoftBound                             do one array access?
                                                              shadow memory
Creating a pointer:                          table[ptr]       base, bound
   int array[100];
                                        table[new_ptr]        base, bound
   ptr = &array;
   ptr_base = &array[0];
   ptr_bound = &array[100];
   table[ptr]={base, bound};                          ptr        array

Check a pointer:                                  newptr        array_p2
   newptr = &array_p2;
                                                array[0]
   {base, bound} = table[newptr];
   if (base > array_p2 || bound …)                                            array[100]
       go to err;
   int* array_p2 = 0xFF;
                                                                                   20
HW Support for Memory Safety
A lot of work. The key is to understand the design trade-offs.

                           Intel MPX                                 ARM MTE
                  (Memory Protection Extension)               (Memory Tagging Extension)
                                                   Introduced in ARM-8.5 in 2018. In 2019, Google
                Announced in 2013, produced in
   History                                         announced that it is adopting Arm’s MTE in Android.
                2015, now not supported anymore.
                                                   Apple will ship it soon.
  Security

Performance

Compatibility



                                                                                                         21
Intel MPX (Memory Protection Extension)
4 bound registers (bnd0-3)
• Bndmk: create base and bound metadata
• Bndldx/bndstx: load/store metadata from/to bound tables
• Bndcl/bndcu: check pointer with lower and upper bounds
Dedicated hardware organized as tables to store metadata



    int array[100];                                               int array[100];
    ptr = &array;                                                 ptr = &array;
    ptr_base = &array[0];                                         bnd0 = bndmk(ptr, 100)
    ptr_bound = &array[100];
    table[ptr]={base, bound};                                     bndstx(&ptr,ptr,bnd0)



       Intel MPX Explained: A Cross-layer Analysis of the Intel MPX System Stack; OLEKSENKO et al; SIGMETRICS’18   22
Intel MPX (Memory Protection Extension)
4 bound registers (bnd0-3)
• Bndmk: create base and bound metadata                                               Pros and cons?
• Bndldx/bndstx: load/store metadata from/to bound tables
• Bndcl/bndcu: check pointer with lower and upper bounds
Dedicated hardware organized as tables to store metadata



 newptr = &array_p2;                                               newptr = &array_p2;
 {base, bound} = table[newptr];                                    bnd1 = bndldx(&newptr, newptr)
 if (base > array_p2 || bound …)                                   bndcu(&newptr, bnd1)
     go to err;                                                    bndcl(&newptr, bnd1)
 int* array_p2 = 0xFF;                                             int* array_p2 = 0xFF;



       Intel MPX Explained: A Cross-layer Analysis of the Intel MPX System Stack; OLEKSENKO et al; SIGMETRICS’18   23
 ARM MTE (Memory Tagging Extension)
1: char *ptr = new char[16];



                                    • Memory locations are tagged by adding 4 bits of
                                      metadata (16 different colors)
                                    • Each pointer also comes with 4 bit metadata (keys)

                   Color mismatch   • Help with both spatial and temporal memory safety
2: ptr[17] = 42;




                                     Armv8.5-A Memory Tagging Extension White paper
                                     https://security.googleblog.com/2019/08/adopting-arm-memory-tagging-extension.html

                                                                                                          24
 ARM MTE (Memory Tagging Extension)
1: char *ptr = new char[16];



                                  • Memory locations are tagged by adding 4 bits of
                                    metadata (16 different colors)
                                  • Each pointer also comes with 4 bit metadata (keys)

                                  • Help with both spatial and temporal memory safety
2: ptr[17] = 42;


3: delete [] ptr;
            Color mismatch
4: ptr[3] = 0; //use after free    Armv8.5-A Memory Tagging Extension White paper
                                   https://security.googleblog.com/2019/08/adopting-arm-memory-tagging-extension.html

                                                                                                        25
 Analysis of ARM MTE
1: char *ptr = new char[16];
                                  • Where to store tags (key and lock)?
                                      • Pointer tag is stored in top unused bits inside the
                                        pointer (no extra register needed)
                                      • Physical memory tag is stored in hardware (new
                                        hardware needed for both DRAM and cache)
                                  • Limited tag bits
                                      • Cannot ensure two allocations have different colors
2: ptr[17] = 42;
                                      • But can ensure that the tags of sequential
                                        allocations are always different
3: delete [] ptr;
            Color mismatch
4: ptr[3] = 0; //use after free      Armv8.5-A Memory Tagging Extension White paper
                                     https://security.googleblog.com/2019/08/adopting-arm-memory-tagging-extension.html

                                                                                                          26
HW Support for Memory Safety
A lot of work. The key is to understand the design trade-offs.

                           Intel MPX                                 ARM MTE
                  (Memory Protection Extension)               (Memory Tagging Extension)
                                                   Introduced in ARM-8.5 in 2018. In 2019, Google
                Announced in 2013, produced in
   History                                         announced that it is adopting Arm’s MTE in Android.
                2015, now not supported anymore.
                                                   Apple will ship it soon.
  Security

Performance

Compatibility



                                                                                                         27
Control-flow Integrity
• To maintain code pointer integrity
• Naïve idea:
   • Make pointer immutable (read-only)
   • Only work for global offset table and virtual function tables

• How about other pointers?
   • Return address?
   • Programmer-defined function pointers
   • Change function pointers after changing vtable pointer


                                                                     28
29
   ARM PA (Pointer Authentication)

• Widely used in Apple processors
• Motivation:
   • 64-bit pointer, but 48-bit virtual address space
   • Unused high bits
   • Integrity check for pointers
• Hash: A tweakable message authentication code
    • PAC = hash(key, context, pointer)
                                                        Before function call   Before function return
    • Context can be sp or vtable ptr




                                                                                               30
Summary
• Memory corruption problems: An eternal war

• Attack variations and mitigations

• Trade-off in hardware support




                                               31
Enjoy the Spring Break!
