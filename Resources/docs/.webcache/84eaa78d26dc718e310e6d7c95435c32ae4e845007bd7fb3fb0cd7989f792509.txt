### 12.21 Other Kinds of Streams

The GNU C Library provides ways for you to define additional kinds of streams that do not necessarily correspond to an open file.

One such type of stream takes input from or writes output to a string. These kinds of streams are used internally to implement the `sprintf` and `sscanf` functions. You can also create such a stream explicitly, using the functions described in [String Streams](https://sourceware.org/glibc/manual/latest/html_node/String-Streams.html).

More generally, you can define streams that do input/output to arbitrary objects using functions supplied by your program. This protocol is discussed in [Programming Your Own Custom Streams](https://sourceware.org/glibc/manual/latest/html_node/Custom-Streams.html).

**Portability Note:** The facilities described in this section are specific to GNU. Other systems or C implementations might or might not provide equivalent functionality.

- [String Streams](https://sourceware.org/glibc/manual/latest/html_node/String-Streams.html)
- [Programming Your Own Custom Streams](https://sourceware.org/glibc/manual/latest/html_node/Custom-Streams.html)
