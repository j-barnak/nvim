# 13 Query Execution I (slides)

Query Execution
15-445/645 FALL 2025

LECTURE # 13

15- 445/645 FALL 2025

– Pt.1

PROF. ANDY PAVLO

PROF. ANDY PAVLO

ADMINISTRIVIA
Project #2 is due Sunday Oct 26th @ 11:59pm

→ See Recitation Video (@158)
→ Office Hours: Saturday Oct 25th @ 3:00-5:00pm in GHC 5207

Mid-term exam grades posted

→ Come to Andy's OH to view your grade and solution.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

2

UPCOMING DATABASE TALKS
Columnar (DB Seminar)

→ Monday Oct 20th @ 4:30pm ET
→ Zoom

Astronomer Tech Talk

→ Tuesday Oct 21st @ 12:00pm
→ GHC 8115

SingleStore (DB Seminar)
→ Monday Oct 27th @ 12:00pm
→ Zoom
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

3

LAST CLASS
We discussed different join operator algorithms.
We now know how to implement basic query operators
→ Sorting, Aggregations, Joins

The next two weeks are about how put all the pieces
together to execute queries…

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

4

5

QUERY EXECUTION
A query plan is a DAG of operators.
A pipeline is a sequence of operators
where tuples continuously flow
between them without intermediate
storage.
A pipeline breaker is an operator
that cannot finish until all its children
emit all their tuples.
→ Joins (Build Side), Subqueries, Order By
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

Pipeline #2



R.id, S.cdate

⨝
R
Pipeline #1

R.id=S.id



value>100

S

TODAY'S AGENDA
Processing Models
Access Methods
Modification Queries
Expression Evaluation

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

6

PROCESSING MODEL
A DBMS's processing model defines how the system
executes a query plan and moves data from one
operator to the next.
→ Different trade-offs for workloads (OLTP vs. OLAP).

Each processing model is comprised of two types of
execution paths:
→ Control Flow: How the DBMS invokes an operator.
→ Data Flow: How an operator sends its results.

The output of an operator can be either whole tuples
(NSM) or subsets of columns (DSM).
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

7

8

PROCESSING MODEL
Approach #1: Iterator Model

Most Common

Approach #2: Materialization Model
Approach #3: Vectorized / Batch Model

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

Rare
Common

ITERATOR MODEL
Each query plan operator implements a Next()
function.

→ On each invocation, the operator returns either a single tuple
or a EOF marker if there are no more tuples.
→ The operator implements a loop that calls Next() on its
children to retrieve their tuples and then process them.

Each operator implementation also has Open() and
Close() functions.
→ Analogous to constructors/destructors, but for operators.

Also called Volcano or Pipeline Model.
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

9

10

ITERATOR MODEL
Control Flow
Data Flow

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

Next() for t in child.Next():
emit(projection(t))

Next() for t1 in left.Next():

buildHashTable(t1)
for t2 in right.Next():
if probe(t2): emit(t1⨝t2)



Next() for t in child.Next():

⨝

if evalPred(t): emit(t)

Next() for t in R:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

Next() for t in S:
emit(t)

R.id, S.cdate

R

R.id=S.id



value>100

S

10

ITERATOR MODEL
Control Flow
Data Flow

1

for t in child.Next():
emit(projection(t))
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): emit(t1⨝t2)

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



⨝

for t in child.Next():
if evalPred(t): emit(t)
for t in R:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

for t in S:
emit(t)

R.id, S.cdate

R

R.id=S.id



value>100

S

10

ITERATOR MODEL
Control Flow
Data Flow

1
2

for t in child.Next():
emit(projection(t))
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): emit(t1⨝t2)

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



⨝

for t in child.Next():
if evalPred(t): emit(t)

3

for t in R:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

for t in S:
emit(t)

R.id, S.cdate

R

R.id=S.id



value>100

S

10

ITERATOR MODEL
Control Flow
Data Flow

1
2

for t in child.Next():
emit(projection(t))
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): emit(t1⨝t2)

Single Tuple

3

for t in R:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



⨝

for t in child.Next():
if evalPred(t): emit(t)
for t in S:
emit(t)

R.id, S.cdate

R

R.id=S.id



value>100

S

10

ITERATOR MODEL
Control Flow
Data Flow

1
2

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

for t in child.Next():
emit(projection(t))
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): emit(t1⨝t2)



for t in child.Next():
if evalPred(t): emit(t)

3

for t in R:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

for t in S:
emit(t)

5

R.id, S.cdate

⨝

4

R

R.id=S.id



value>100

S

10

ITERATOR MODEL
Control Flow
Data Flow

1
2

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

for t in child.Next():
emit(projection(t))
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): emit(t1⨝t2)



for t in child.Next():
if evalPred(t): emit(t)

3

for t in R:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

for t in S:
emit(t)

5

R.id, S.cdate

⨝

4

R

R.id=S.id



value>100

S

10

ITERATOR MODEL
Control Flow
Data Flow

1
2

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

for t in child.Next():
emit(projection(t))
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): emit(t1⨝t2)



for t in child.Next():
if evalPred(t): emit(t)

3

for t in R:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

for t in S:
emit(t)

5

R.id, S.cdate

⨝

4

R
Pipeline #1

R.id=S.id



value>100

S

10

ITERATOR MODEL
Control Flow
Data Flow

1
2

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

for t in child.Next():
emit(projection(t))
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): emit(t1⨝t2)
for t in child.Next():
if evalPred(t): emit(t)

3

for t in R:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

for t in S:
emit(t)

5

Pipeline #2



4

⨝
R
Pipeline #1

R.id, S.cdate
R.id=S.id



value>100

S

ITERATOR MODEL
The Iterator model is used in almost every DBMS.
→ Easy to implement / debug.
→ Output control works easily with this approach.

Allows for pipelining where the DBMS tries to
process each tuple through as many operators as
possible before retrieving the next tuple.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

11

MATERIALIZATION MODEL
Each operator processes its input all at once and then
emits its output all at once.
→ The operator "materializes" its output as a single result.
→ The DBMS can push down hints (e.g., LIMIT) to avoid
scanning too many tuples.
→ Can send either a materialized row or a single column.

The output can be either whole tuples (NSM) or subsets
of columns (DSM).
→ Originally developed by MonetDB in the 1990s to process
entire columns at a time instead of single tuples.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

12

13

MATERIALIZATION MODEL
Control Flow
Data Flow

1

out = [ ]
for t in child.Output():
out.add(projection(t))
return out
out = [ ]
for t1 in left.Output():
buildHashTable(t1)
for t2 in right.Output():
if probe(t2): out.add(t1⨝t2)
return out
out = [ ]
for t in child.Output():
if evalPred(t): out.add(t)
return out

DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in R:
out.add(t)
return out

DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in S:
out.add(t)
return out

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

13

MATERIALIZATION MODEL
Control Flow
Data Flow

1
2

out = [ ]
for t in child.Output():
out.add(projection(t))
return out
out = [ ]
for t1 in left.Output():
buildHashTable(t1)
for t2 in right.Output():
if probe(t2): out.add(t1⨝t2)
return out
out = [ ]
for t in child.Output():
if evalPred(t): out.add(t)
return out

3
DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in R:
out.add(t)
return out

DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in S:
out.add(t)
return out

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

13

MATERIALIZATION MODEL
Control Flow
Data Flow

1
2

out = [ ]
for t in child.Output():
out.add(projection(t))
return out
out = [ ]
for t1 in left.Output():
buildHashTable(t1)
for t2 in right.Output():
if probe(t2): out.add(t1⨝t2)
return out

All Tuples

3
DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in R:
out.add(t)
return out

DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in child.Output():
if evalPred(t): out.add(t)
return out
out = [ ]
for t in S:
out.add(t)
return out

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

13

MATERIALIZATION MODEL
Control Flow
Data Flow

1
2

out = [ ]
for t1 in left.Output():
buildHashTable(t1)
for t2 in right.Output():
if probe(t2): out.add(t1⨝t2)
return out
out = [ ]
for t in child.Output():
if evalPred(t): out.add(t)
return out

3
DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in R:
out.add(t)
return out

DATABASE SYSTEMS (FALL 2025)

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

out = [ ]
for t in child.Output():
out.add(projection(t))
return out

out = [ ]
for t in S:
out.add(t)
return out



R.id, S.cdate

⨝
4

R

R.id=S.id



value>100

S

13

MATERIALIZATION MODEL
Control Flow
Data Flow

1
2

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

out = [ ]
for t in child.Output():
out.add(projection(t))
return out
out = [ ]
for t1 in left.Output():
buildHashTable(t1)
for t2 in right.Output():
if probe(t2): out.add(t1⨝t2)
return out



out = [ ]
for t in child.Output():
if evalPred(t): out.add(t)
return out

3
DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in R:
out.add(t)
return out

DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in S:
out.add(t)
return out

5

R.id, S.cdate

⨝
4

R

R.id=S.id



value>100

S

13

MATERIALIZATION MODEL
Control Flow
Data Flow

1
2

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

out = [ ]
for t in child.Output():
out.add(projection(t))
return out
out = [ ]
for t1 in left.Output():
buildHashTable(t1)
for t2 in right.Output():
if probe(t2): out.add(t1⨝t2)
return out



out = [ ]
for t in child.Output():
if evalPred(t): out.add(t)
return out

3
DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in R:
out.add(t)
return out

DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in S:
out.add(t)
return out

5

R.id, S.cdate

⨝
4

R

R.id=S.id



value>100

S

13

MATERIALIZATION MODEL
Control Flow
Data Flow

1
2

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

out = [ ]
for t in child.Output():
out.add(projection(t))
return out
out = [ ]
for t1 in left.Output():
buildHashTable(t1)
for t2 in right.Output():
if probe(t2): out.add(t1⨝t2)
return out



out = [ ]
for t in child.Output():
if evalPred(t): out.add(t)
return out

3
DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in R:
out.add(t)
return out

DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in S:
out.add(t)
return out

5

R.id, S.cdate

⨝
4

R

R.id=S.id



value>100

S

13

MATERIALIZATION MODEL
Control Flow
Data Flow

1
2

out = [ ]
for t in child.Output():
out.add(projection(t))
return out
out = [ ]
for t1 in left.Output():
buildHashTable(t1)
for t2 in right.Output():
if probe(t2): out.add(t1⨝t2)
return out
out = [ ]
for t in S:
if evalPred(t): out.add(t)
return out

3
DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in R:
out.add(t)
return out

DATABASE SYSTEMS (FALL 2025)

Operator Fusion

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

MATERIALIZATION MODEL
Better for OLTP workloads because queries only access
a small number of tuples at a time.
→ Lower execution / coordination overhead.
→ Fewer function calls.

Not ideal for OLAP queries with large intermediate
results because DBMS must allocate buffers.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

14

VECTORIZATION MODEL
Like the Iterator Model where each operator
implements a Next() function, but…
Each operator emits a batch of tuples instead of a single
tuple.
→ The operator's internal loop processes multiple tuples at a time.
→ The size of the batch can vary based on hardware or query
properties.
→ Each batch will contain one or more columns each their own
null bitmaps.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

15

16

VECTORIZATION MODEL
Control Flow
Data Flow

out = [ ]
for t in child.Next():
out.add(projection(t))
if |out|>n: emit(out)

2

1

out = [ ]
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): out.add(t1⨝t2)
if |out|>n: emit(out)

out = [ ]
for t in child.Next():
if evalPred(t): out.add(t)
if |out|>n: emit(out)
out = [ ]

3 for t in R:
DATABASE SYSTEMS (FALL 2025)

out.add(t)
if |out|>n: emit(out)

DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in S:
out.add(t)
if |out|>n: emit(out)

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

16

VECTORIZATION MODEL
Control Flow
Data Flow

out = [ ]
for t in child.Next():
out.add(projection(t))
if |out|>n: emit(out)

2

1

out = [ ]
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): out.add(t1⨝t2)
if |out|>n: emit(out)

out = [ ]
for t in child.Next():
if evalPred(t): out.add(t)
if |out|>n: emit(out)

3
DATABASE SYSTEMS (FALL 2025)

Tuple Batch
out = [ ]
for t in R:
out.add(t)
if |out|>n: emit(out)

DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t in S:
out.add(t)
if |out|>n: emit(out)

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

16

VECTORIZATION MODEL
Control Flow
Data Flow

out = [ ]
for t in child.Next():
out.add(projection(t))
if |out|>n: emit(out)

2

3
DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): out.add(t1⨝t2)
if |out|>n: emit(out)

Tuple Batch
out = [ ]
for t in R:
out.add(t)
if |out|>n: emit(out)

DATABASE SYSTEMS (FALL 2025)

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

1



out = [ ]
for t in child.Next():
if evalPred(t): out.add(t)
if |out|>n: emit(out)

4

out = [ ]
for t in S:
out.add(t)
if |out|>n: emit(out)

5

R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

16

VECTORIZATION MODEL
Control Flow
Data Flow

out = [ ]
for t in child.Next():
out.add(projection(t))
if |out|>n: emit(out)

2

3
DATABASE SYSTEMS (FALL 2025)

out = [ ]
for t1 in left.Next():
buildHashTable(t1)
for t2 in right.Next():
if probe(t2): out.add(t1⨝t2)
if |out|>n: emit(out)

Tuple Batch
out = [ ]
for t in R:
out.add(t)
if |out|>n: emit(out)

DATABASE SYSTEMS (FALL 2025)

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100

1



out = [ ]
for t in child.Next():
if evalPred(t): out.add(t)
if |out|>n: emit(out)

4

out = [ ]
for t in S:
out.add(t)
if |out|>n: emit(out)

5

R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

VECTORIZATION MODEL
Ideal for OLAP queries because it greatly reduces the
number of invocations per operator.
Allows an out-of-order CPU to efficiently execute
operators over batches of tuples.

→ Operators perform work in tight for-loops over arrays, which
compilers know how to optimize / vectorize.
→ No data or control dependencies.
→ Hot instruction cache.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

17

VECTORIZATION MODEL
Ideal for OLAP queries because it greatly reduces the
number of invocations per operator.
Allows an out-of-order CPU to efficiently execute
operators over batches of tuples.

→ Operators perform work in tight for-loops over arrays, which
compilers know how to optimize / vectorize.
→ No data or control dependencies.
→ Hot instruction cache.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

17

OBSERVATION
In the previous examples, the DBMS starts executing a
query by invoking Next() at the root of the query plan
and pulling data up from leaf operators.
This is the how most DBMSs implement their
execution engine.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

18

PLAN PROCESSING DIRECTION
Approach #1: Top-to-Bottom (Pull)

→ Start with the root and "pull" data up from its children.
→ Tuples are always passed between operators using function calls
(unless it's a pipeline breaker).

Approach #2: Bottom-to-Top (Push)

→ Start with leaf nodes and "push" data to their parents.
→ Can "fuse" operators together within a for-loop to
minimize intermediate result staging.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

19

PLAN PROCESSING DIRECTION
Approach #1: Top-to-Bottom (Pull)

→ Start with the root and "pull" data up from its children.
→ Tuples are always passed between operators using function calls
(unless it's a pipeline breaker).

Approach #2: Bottom-to-Top (Push)

→ Start with leaf nodes and "push" data to their parents.
→ Can "fuse" operators together within a for-loop to
minimize intermediate result staging.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

19

PLAN PROCESSING DIRECTION
Approach #1: Top-to-Bottom (Pull)

→ Start with the root and "pull" data up from its children.
→ Tuples are always passed between operators using function calls
(unless it's a pipeline breaker).

Approach #2: Bottom-to-Top (Push)

→ Start with leaf nodes and "push" data to their parents.
→ Can "fuse" operators together within a for-loop to
minimize intermediate result staging.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

19

20

PUSH-BASED ITERATOR MODEL
Control Flow
Data Flow

2

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100
for t2 in S:
if evalPred(t):
if probeHashTable(t2):
emit(projection(t1⨝t2))

Pipeline #2

1
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

R.id, S.cdate

⨝

Operator Fusion
for t1 in R:
buildHashTable(t1)



R
Pipeline #1

R.id=S.id



value>100

S

20

PUSH-BASED ITERATOR MODEL
Control Flow
Data Flow

Scheduler

2

1

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



for t2 in S:
if evalPred(t):
if probeHashTable(t2):
emit(projection(t1⨝t2))

for t1 in R:
buildHashTable(t1)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

20

PUSH-BASED ITERATOR MODEL
Control Flow
Data Flow

Scheduler

2

1

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



for t2 in S:
if evalPred(t):
if probeHashTable(t2):
emit(projection(t1⨝t2))

for t1 in R:
buildHashTable(t1)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

20

PUSH-BASED ITERATOR MODEL
Control Flow
Data Flow

Scheduler

2

1

Output Buffers

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



for t2 in S:
if evalPred(t):
if probeHashTable(t2):
emit(projection(t1⨝t2))

for t1 in R:
buildHashTable(t1)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

20

PUSH-BASED ITERATOR MODEL
Control Flow
Data Flow

Scheduler

2

1

Output Buffers

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



for t2 in S:
if evalPred(t):
if probeHashTable(t2):
emit(projection(t1⨝t2))

for t1 in R:
buildHashTable(t1)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

20

PUSH-BASED ITERATOR MODEL
Control Flow
Data Flow

Scheduler

2

1

Output Buffers

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



for t2 in S:
if evalPred(t):
if probeHashTable(t2):
emit(projection(t1⨝t2))

for t1 in R:
buildHashTable(t1)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

20

PUSH-BASED ITERATOR MODEL
Control Flow
Data Flow

Scheduler

2

1

Output Buffers

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



for t2 in S:
if evalPred(t):
if probeHashTable(t2):
emit(projection(t1⨝t2))

for t1 in R:
buildHashTable(t1)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

20

PUSH-BASED ITERATOR MODEL
Control Flow
Data Flow

Scheduler

2

1

Output Buffers

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



for t2 in S:
if evalPred(t):
if probeHashTable(t2):
emit(projection(t1⨝t2))

for t1 in R:
buildHashTable(t1)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

R.id, S.cdate

⨝
R

R.id=S.id



value>100

S

PLAN PROCESSING DIRECTION
Approach #1: Top-to-Bottom (Pull)

Most Common

Approach #2: Bottom-to-Top (Push)

Rare-ish

→ Easy to control output via LIMIT.
→ Parent operator blocks until its child returns with a tuple.
→ Additional overhead because operators' Next() functions are
implemented as virtual functions.
→ Branching costs on each Next() invocation.
→ Allows for tighter control of caches/registers in pipelines.
→ May not have exact control of intermediate result sizes.
→ Difficult to implement some operators (Sort-Merge Join).

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

21

22

ACCESS METHODS
An access method is the how the
DBMS retrieves data stored in a table.
→ There is not a specific operator for this
defined in relational algebra.

SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100



Three basic approaches:

→ Sequential Scan.
→ Index Scan (many variants).
→ Multi-Index Scan.

⨝
R

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

R.id, S.cdate
R.id=S.id



value>100

S

SEQUENTIAL SCAN
For each page in the table:

→ Retrieve it from the buffer pool.
→ Iterate over each tuple and check whether
to emit it to the next operator.

The DBMS maintains an internal
cursor that tracks the last page / slot it
examined.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

for page in table.pages:
for t in page.tuples:

// Do Something!

23

SEQUENTIAL SCAN: OPTIMIZATIONS
Data Encoding / Compression
Lecture #06 Prefetching / Scan Sharing / Buffer Bypass
Lecture #14 Task Parallelization / Multi-threading
Lecture #08 Clustering / Sorting
Lecture #12 Late Materialization
Materialized Views / Result Caching
Data Skipping
Lecture #14 Data Parallelization / Vectorization
Code Specialization / Compilation
Lecture #05

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

24

DATA SKIPPING
Approach #1: Approximate Queries (Lossy)

→ Execute queries on a sampled subset of the entire table to
produce approximate results.
→ Examples: BlinkDB, Redshift, ComputeDB, XDB, Oracle,
Snowflake, Google BigQuery, DataBricks

Approach #2: Zone Maps (Lossless)

→ Pre-compute columnar aggregations per page that allow the
DBMS to check whether queries need to access it.
→ Trade-off between page size vs. filter efficacy.
→ Examples: Oracle, Vertica, SingleStore, Netezza, Snowflake,
Google BigQuery
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

25

26

ZONE MAPS
Pre-computed aggregates for the attribute values in a
page. DBMS checks the zone map first to decide
whether it wants to access the page.
Original Data

SELECT * FROM table
WHERE val > 600

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

val
100
200
300
400
400

Zone Map
type

MIN
MAX
AVG
SUM
COUNT

val
100
400
280
1400
5

26

ZONE MAPS
Pre-computed aggregates for the attribute values in a
page. DBMS checks the zone map first to decide
whether it wants to access the page.
Original Data

SELECT * FROM table
WHERE val > 600

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

val
100
200
300
400
400

Zone Map
type

MIN
MAX
AVG
SUM
COUNT

val
100
400
280
1400
5

INDEX SCAN
The DBMS picks an index to find the tuples that the
query needs.
Which index to use depends on:

→ What attributes the index contains
→ What attributes the query references
→ The attribute's value domains
→ Predicate composition
→ Whether the index has unique or non-unique keys

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

27

28

INDEX SCAN
Suppose that we have a single table
with 100 tuples and two indexes:
→ Index #1: age
→ Index #2: dept

Scenario #1
There are 99 people
under the age of 30 but
only 2 people in the CS
department.
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

SELECT * FROM students
WHERE age < 30
AND dept = 'CS'
AND country = 'US'

Scenario #2
There are 99 people in
the CS department but
only 2 people under the
age of 30.

MULTI-INDEX SCAN
If there are multiple indexes available for a query, the
DBMS does not have to pick only one:

→ Compute sets of Record IDs using each matching index.
→ Combine these sets based on the query’s predicates (union vs.
intersect).
→ Retrieve the records and apply any remaining predicates.

Examples:

→ DB2 Multi-Index Scan
→ PostgreSQL Bitmap Scan
→ MySQL Index Merge

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

29

MULTI-INDEX SCAN
Given the following query on a
database with an index #1 on age and
an index #2 on dept:

→ We can retrieve the Record IDs satisfying
age<30 using index #1.
→ Then retrieve the Record IDs satisfying
dept='CS' using index #2.
→ Take their intersection.
→ Retrieve records and check
country='US'.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

30

SELECT * FROM students
WHERE age < 30
AND dept = 'CS'
AND country = 'US'

MULTI-INDEX SCAN
Compute set intersection of matching
record ids from multiple indexes using
bitmaps or hash tables.

age<30

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

dept='CS'

31

SELECT * FROM students
WHERE age < 30
AND dept = 'CS'
AND country = 'US'

MULTI-INDEX SCAN
Compute set intersection of matching
record ids from multiple indexes using
bitmaps or hash tables.

age<30
record ids

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

dept='CS'

31

SELECT * FROM students
WHERE age < 30
AND dept = 'CS'
AND country = 'US'

MULTI-INDEX SCAN
Compute set intersection of matching
record ids from multiple indexes using
bitmaps or hash tables.

age<30
record ids

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

dept='CS'
record ids

31

SELECT * FROM students
WHERE age < 30
AND dept = 'CS'
AND country = 'US'

MULTI-INDEX SCAN
Compute set intersection of matching
record ids from multiple indexes using
bitmaps or hash tables.

age<30

dept='CS'

record ids

record ids
fetch records

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

country='US'

31

SELECT * FROM students
WHERE age < 30
AND dept = 'CS'
AND country = 'US'

MODIFICATION QUERIES
Operators that modify the database (INSERT, UPDATE,
DELETE) are responsible for modifying the target table
and its indexes.
→ Constraint checks can either happen immediately inside of
operator or deferred until later in query/transaction.

The output of these operators can either be Record Ids
or tuple data (i.e., RETURNING).

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

32

MODIFICATION QUERIES
UPDATE/DELETE:

→ Child operators pass Record IDs for target tuples.
→ Must keep track of previously seen tuples.

INSERT:

→ Choice #1: Materialize tuples inside of the operator.
→ Choice #2: Operator inserts any tuple passed in from child
operators.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

33

UPDATE QUERY PROBLEM
Control Flow
Data Flow

for t in child.Next():
removeFromIndex(idx_salary, t.salary, t)
updateTuple(t.salary = t.salary + 100)
insertIntoIndex(idx_salary, t.salary, t)

for t in Indexpeople:
if t.salary < 1100:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

CREATE INDEX idx_salary
ON people (salary);

UPDATE people
SET salary = salary + 100
WHERE salary < 1100
Index(people.salary)

34

UPDATE QUERY PROBLEM
Control Flow
Data Flow

for t in child.Next():
removeFromIndex(idx_salary, t.salary, t)
updateTuple(t.salary = t.salary + 100)
insertIntoIndex(idx_salary, t.salary, t)

for t in Indexpeople:
if t.salary < 1100:
emit(t)

CREATE INDEX idx_salary
ON people (salary);

UPDATE people
SET salary = salary + 100
WHERE salary < 1100
Index(people.salary)

(999,Andy)
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

34

UPDATE QUERY PROBLEM
Control Flow
Data Flow

(999,Andy)
for t in child.Next():
removeFromIndex(idx_salary, t.salary, t)
updateTuple(t.salary = t.salary + 100)
insertIntoIndex(idx_salary, t.salary, t)

for t in Indexpeople:
if t.salary < 1100:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

CREATE INDEX idx_salary
ON people (salary);

UPDATE people
SET salary = salary + 100
WHERE salary < 1100
Index(people.salary)

34

UPDATE QUERY PROBLEM
Control Flow
Data Flow

(999,Andy)
for t in child.Next():
removeFromIndex(idx_salary, t.salary, t)
updateTuple(t.salary = t.salary + 100)
insertIntoIndex(idx_salary, t.salary, t)

for t in Indexpeople:
if t.salary < 1100:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

CREATE INDEX idx_salary
ON people (salary);

UPDATE people
SET salary = salary + 100
WHERE salary < 1100
Index(people.salary)

34

UPDATE QUERY PROBLEM
Control Flow
Data Flow

for t in child.Next():
removeFromIndex(idx_salary, t.salary, t)
updateTuple(t.salary = t.salary + 100)
insertIntoIndex(idx_salary, t.salary, t)

for t in Indexpeople:
if t.salary < 1100:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

CREATE INDEX idx_salary
ON people (salary);

UPDATE people
SET salary = salary + 100
WHERE salary < 1100
Index(people.salary)

34

34

UPDATE QUERY PROBLEM
Control Flow
Data Flow

for t in child.Next():
removeFromIndex(idx_salary, t.salary, t)
updateTuple(t.salary = t.salary + 100)
insertIntoIndex(idx_salary, t.salary, t)

for t in Indexpeople:
if t.salary < 1100:
emit(t)

CREATE INDEX idx_salary
ON people (salary);

UPDATE people
SET salary = salary + 100
WHERE salary < 1100
Index(people.salary)

(1099,Andy)
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

UPDATE QUERY PROBLEM
Control Flow
Data Flow

(1099,Andy)
for t in child.Next():
removeFromIndex(idx_salary, t.salary, t)
updateTuple(t.salary = t.salary + 100)
insertIntoIndex(idx_salary, t.salary, t)

for t in Indexpeople:
if t.salary < 1100:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

CREATE INDEX idx_salary
ON people (salary);

UPDATE people
SET salary = salary + 100
WHERE salary < 1100
Index(people.salary)

34

UPDATE QUERY PROBLEM
Control Flow
Data Flow

(1199,Andy)
for t in child.Next():
removeFromIndex(idx_salary, t.salary, t)
updateTuple(t.salary = t.salary + 100)
insertIntoIndex(idx_salary, t.salary, t)

for t in Indexpeople:
if t.salary < 1100:
emit(t)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

CREATE INDEX idx_salary
ON people (salary);

UPDATE people
SET salary = salary + 100
WHERE salary < 1100
Index(people.salary)

34

HALLOWEEN PROBLEM
Anomaly where an update operation changes the
physical location of a tuple, which causes a scan
operator to visit the tuple multiple times.
→ Can occur on clustered tables or index scans.

First discovered by IBM researchers while working on
System R on Halloween day in 1976.
Solution: Track modified record ids per query.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

35

36

EXPRESSION EVALUATION
SELECT R.id, S.cdate
FROM R JOIN S
ON R.id = S.id
WHERE S.value > 100;

The DBMS represents a WHERE clause
as an expression tree.
The nodes in the tree represent
different expression types:

→ Comparisons (=, <, >, !=)
→ Conjunction (AND), Disjunction (OR)
→ Arithmetic Operators (+, -, *, /, %)
→ Constant Values
→ Tuple Attribute References
→ Functions

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

AND

=

Attribute(R.id)

>

Attribute(S.id)

Attribute(value)

Constant(100)

EXPRESSION EVALUATION
PREPARE xxx AS
SELECT * FROM S
WHERE S.val = $1 + 9
EXECUTE xxx(991)

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

37

37

EXPRESSION EVALUATION
PREPARE xxx AS
SELECT * FROM S
WHERE S.val = $1 + 9
EXECUTE xxx(991)

Execution Context
Current Tuple
(123, 1000)

Query Parameters
(int:991)

Table Schema
S→(int:id, int:val)

=

Attribute(S.val)

Parameter($1)
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

+

Constant(9)

37

EXPRESSION EVALUATION
PREPARE xxx AS
SELECT * FROM S
WHERE S.val = $1 + 9
EXECUTE xxx(991)

Execution Context
Current Tuple
(123, 1000)

Query Parameters
(int:991)

Table Schema
S→(int:id, int:val)

=

Attribute(S.val)
1000
Parameter($1)
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

+

Constant(9)

37

EXPRESSION EVALUATION
PREPARE xxx AS
SELECT * FROM S
WHERE S.val = $1 + 9
EXECUTE xxx(991)

Execution Context
Current Tuple
(123, 1000)

Query Parameters
(int:991)

Table Schema
S→(int:id, int:val)

=

Attribute(S.val)
1000
Parameter($1)
991
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

+

Constant(9)

37

EXPRESSION EVALUATION
PREPARE xxx AS
SELECT * FROM S
WHERE S.val = $1 + 9
EXECUTE xxx(991)

Execution Context
Current Tuple
(123, 1000)

Query Parameters
(int:991)

Table Schema
S→(int:id, int:val)

=

Attribute(S.val)
1000
Parameter($1)
991
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

+

Constant(9)
9

37

EXPRESSION EVALUATION
PREPARE xxx AS
SELECT * FROM S
WHERE S.val = $1 + 9
EXECUTE xxx(991)

Execution Context
Current Tuple
(123, 1000)

Query Parameters
(int:991)

Table Schema
S→(int:id, int:val)

=

Attribute(S.val)
1000
Parameter($1)
991
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

+
1000
Constant(9)
9

37

EXPRESSION EVALUATION
PREPARE xxx AS
SELECT * FROM S
WHERE S.val = $1 + 9
EXECUTE xxx(991)

Execution Context
Current Tuple
(123, 1000)

Query Parameters
(int:991)

Table Schema
S→(int:id, int:val)

=
true

Attribute(S.val)
1000
Parameter($1)
991
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

+
1000
Constant(9)
9

38

EXPRESSION EVALUATION
Evaluating predicates by traversing a
tree is terrible for the CPU.

→ The DBMS traverses the tree and for each
node that it visits, it must figure out what
the operator needs to do.

A better approach is to evaluate the
expression directly.
An even better approach is to
vectorize it evaluate a batch of tuples
at the same time…
DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

SELECT * WHERE s.val = 1;
=
Attribute(s.val)

Constant(1)

bool check(val) {
return (val == 1);
}
gcc, Clang, LLVM, …

Machine Code

39

EXPRESSION EVALUATION: OPTIMIZATIONS
Constant Folding:

→ Identify redundant / unnecessary
operations that are wasteful.
→ Compute a sub-expression on a constant
value once and reuse result per tuple.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

WHERE UPPER(col1) = UPPER('wutang');
=
UPPER()
Attribute(col1)

UPPER()
Constant('wutang')

39

EXPRESSION EVALUATION: OPTIMIZATIONS
Constant Folding:

→ Identify redundant / unnecessary
operations that are wasteful.
→ Compute a sub-expression on a constant
value once and reuse result per tuple.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

WHERE UPPER(col1) = UPPER('wutang');
=
UPPER()
Attribute(col1)

Constant('WUTANG')

39

EXPRESSION EVALUATION: OPTIMIZATIONS
WHERE UPPER(col1) = UPPER('wutang');

Constant Folding:

=

→ Identify redundant / unnecessary
operations that are wasteful.
→ Compute a sub-expression on a constant
value once and reuse result per tuple.

Common Sub-Expr. Elimination:

UPPER()
Attribute(col1)

WHERE STRPOS('x', col1) < 2
OR STRPOS('x', col1) > 8

→ Identify repeated sub-expressions that can
be shared across expression tree.
→ Compute once and then reuse result.

OR
Op(<)

STRPOS()

Constant('x')

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

Constant('WUTANG')

Op(>)

Constant(2)

Attribute(col1)

STRPOS()

Constant('x')

Constant(8)

Attribute(col1)

39

EXPRESSION EVALUATION: OPTIMIZATIONS
WHERE UPPER(col1) = UPPER('wutang');

Constant Folding:

=

→ Identify redundant / unnecessary
operations that are wasteful.
→ Compute a sub-expression on a constant
value once and reuse result per tuple.

Common Sub-Expr. Elimination:

UPPER()
Attribute(col1)

WHERE STRPOS('x', col1) < 2
OR STRPOS('x', col1) > 8

→ Identify repeated sub-expressions that can
be shared across expression tree.
→ Compute once and then reuse result.

OR
Op(<)

STRPOS()

Constant('x')

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

Constant('WUTANG')

Constant(2)

Attribute(col1)

Op(>)

Constant(8)

39

EXPRESSION EVALUATION: OPTIMIZATIONS
WHERE UPPER(col1) = UPPER('wutang');

Constant Folding:

=

→ Identify redundant / unnecessary
operations that are wasteful.
→ Compute a sub-expression on a constant
value once and reuse result per tuple.

Common Sub-Expr. Elimination:

UPPER()
Attribute(col1)

WHERE STRPOS('x', col1) < 2
OR STRPOS('x', col1) > 8

→ Identify repeated sub-expressions that can
be shared across expression tree.
→ Compute once and then reuse result.

OR
Op(<)

STRPOS()

Constant('x')

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

Constant('WUTANG')

Constant(2)

Attribute(col1)

Op(>)

Constant(8)

CONCLUSION
The same query plan can be executed in multiple
different ways.
OLTP DBMSs want to use index scans as much as
possible.
Expression trees are flexible but slow.
JIT compilation can (sometimes) speed them up.

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

40

NEXT CLASS
Parallel Query Execution

DATABASE SYSTEMS (FALL 2025)

DATABASE SYSTEMS (FALL 2025)

41

