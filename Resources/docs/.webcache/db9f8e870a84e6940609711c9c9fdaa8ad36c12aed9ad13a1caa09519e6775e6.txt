[std](https://doc.rust-lang.org/std/index.html)::[ffi](https://doc.rust-lang.org/std/ffi/index.html)

# Type Alias c_char Copy item path

1.64.0 · [Source](https://doc.rust-lang.org/src/core/ffi/primitives.rs.html#21)

``` rust
pub type c_char = i8;
```

Expand description

Equivalent to C’s `char` type.

[C’s `char` type](https://en.wikipedia.org/wiki/C_data_types#Basic_types) is completely unlike [Rust’s `char` type](https://doc.rust-lang.org/std/primitive.char.html "primitive char"); while Rust’s type represents a unicode scalar value, C’s `char` type is just an ordinary integer. On modern architectures this type will always be either [`i8`](https://doc.rust-lang.org/std/primitive.i8.html "primitive i8") or [`u8`](https://doc.rust-lang.org/std/primitive.u8.html "primitive u8"), as they use byte-addressed memory with 8-bit bytes.

C chars are most commonly used to make C strings. Unlike Rust, where the length of a string is included alongside the string, C strings mark the end of a string with the character `'\0'`. See `CStr` for more information.