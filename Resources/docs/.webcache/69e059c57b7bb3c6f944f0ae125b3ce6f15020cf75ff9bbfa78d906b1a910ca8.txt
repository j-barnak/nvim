[std](https://doc.rust-lang.org/std/index.html)::[time](https://doc.rust-lang.org/std/time/index.html)

# Struct Instant Copy item path

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#156)

``` rust
pub struct Instant(/* private fields */);
```

Expand description

A measurement of a monotonically nondecreasing clock. Opaque and useful only with [`Duration`](https://doc.rust-lang.org/std/time/struct.Duration.html "struct std::time::Duration").

Instants are always guaranteed, barring [platform bugs](https://doc.rust-lang.org/std/time/struct.Instant.html#monotonicity "struct std::time::Instant"), to be no less than any previously measured instant when created, and are often useful for tasks such as measuring benchmarks or timing how long an operation takes.

Note, however, that instants are **not** guaranteed to be **steady**. In other words, each tick of the underlying clock might not be the same length (e.g. some seconds may be longer than others). An instant may jump forwards or experience time dilation (slow down or speed up), but it will never go backwards. As part of this non-guarantee it is also not specified whether system suspends count as elapsed time or not. The behavior varies across platforms and Rust versions.

Instants are opaque types that can only be compared to one another. There is no method to get “the number of seconds” from an instant. Instead, it only allows measuring the duration between two instants (or comparing two instants).

The size of an `Instant` struct may vary depending on the target operating system.

Example:

``` rust
use std::time::{Duration, Instant};
use std::thread::sleep;

fn main() {
   let now = Instant::now();

   // we sleep for 2 seconds
   sleep(Duration::new(2, 0));
   // it prints '2'
   println!("{}", now.elapsed().as_secs());
}
```

## OS-specific behaviors

An `Instant` is a wrapper around system-specific types and it may behave differently depending on the underlying operating system. For example, the following snippet is fine on Linux but panics on macOS:

``` rust
use std::time::{Instant, Duration};

let now = Instant::now();
let days_per_10_millennia = 365_2425;
let solar_seconds_per_day = 60 * 60 * 24;
let millennium_in_solar_seconds = 31_556_952_000;
assert_eq!(millennium_in_solar_seconds, days_per_10_millennia * solar_seconds_per_day / 10);

let duration = Duration::new(millennium_in_solar_seconds, 0);
println!("{:?}", now + duration);
```

For cross-platform code, you can comfortably use durations of up to around one hundred years.

## Underlying System calls

The following system calls are [currently](https://doc.rust-lang.org/std/io/index.html#platform-specific-behavior "mod std::io") being used by `now()` to find out the current time:

| Platform | System call                                                                                                                                                                                                                          |
|----------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| SGX      | [insecure_time usercall](https://edp.fortanix.com/docs/api/fortanix_sgx_abi/struct.Usercalls.html#method.insecure_time) . More information on [timekeeping in SGX](https://edp.fortanix.com/docs/concepts/rust-std/#codestdtimecode) |
| UNIX     | [clock_gettime](https://pubs.opengroup.org/onlinepubs/9799919799/functions/clock_getres.html) with CLOCK_MONOTONIC                                                                                                                   |
| WASI     | [clock_gettime](https://pubs.opengroup.org/onlinepubs/9799919799/functions/clock_getres.html) with CLOCK_MONOTONIC                                                                                                                   |
| Darwin   | [clock_gettime](https://pubs.opengroup.org/onlinepubs/9799919799/functions/clock_getres.html) with CLOCK_UPTIME_RAW                                                                                                                  |
| VXWorks  | [clock_gettime](https://pubs.opengroup.org/onlinepubs/9799919799/functions/clock_getres.html) with CLOCK_MONOTONIC                                                                                                                   |
| SOLID    | `get_tim`                                                                                                                                                                                                                            |
| Windows  | [QueryPerformanceCounter](https://docs.microsoft.com/en-us/windows/win32/api/profileapi/nf-profileapi-queryperformancecounter)                                                                                                       |

**Disclaimer:** These system calls might change over time.

> Note: mathematical operations like [`add`](https://doc.rust-lang.org/std/time/struct.Instant.html#method.add "method std::time::Instant::add") may panic if the underlying structure cannot represent the new point in time.

### Monotonicity

On all platforms `Instant` will try to use an OS API that guarantees monotonic behavior if available, which is the case for all [tier 1](https://doc.rust-lang.org/rustc/platform-support.html) platforms. In practice such guarantees are – under rare circumstances – broken by hardware, virtualization or operating system bugs. To work around these bugs and platforms not offering monotonic clocks [`duration_since`](https://doc.rust-lang.org/std/time/struct.Instant.html#method.duration_since "method std::time::Instant::duration_since"), [`elapsed`](https://doc.rust-lang.org/std/time/struct.Instant.html#method.elapsed "method std::time::Instant::elapsed") and [`sub`](#impl-Sub-for-Instant) saturate to zero. In older Rust versions this lead to a panic instead. [`checked_duration_since`](https://doc.rust-lang.org/std/time/struct.Instant.html#method.checked_duration_since "method std::time::Instant::checked_duration_since") can be used to detect and handle situations where monotonicity is violated, or `Instant`s are subtracted in the wrong order.

This workaround obscures programming errors where earlier and later instants are accidentally swapped. For this reason future Rust versions may reintroduce panics.

## Implementations

[Source](https://doc.rust-lang.org/src/std/time.rs.html#272-416)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#285-287)

Returns an instant corresponding to “now”.

##### Examples

``` rust
use std::time::Instant;

let now = Instant::now();
```

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#314-316)

Returns the amount of time elapsed from another instant to this one, or zero duration if that instant is later than this one.

##### Panics

Previous Rust versions panicked when `earlier` was later than `self`. Currently this method saturates. Future versions may reintroduce the panic in some circumstances. See [Monotonicity](https://doc.rust-lang.org/std/time/struct.Instant.html#monotonicity "struct std::time::Instant").

##### Examples

``` rust
use std::time::{Duration, Instant};
use std::thread::sleep;

let now = Instant::now();
sleep(Duration::new(1, 0));
let new_now = Instant::now();
println!("{:?}", new_now.duration_since(now));
println!("{:?}", now.duration_since(new_now)); // 0ns
```

1.39.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#340-342)

Returns the amount of time elapsed from another instant to this one, or None if that instant is later than this one.

Due to [monotonicity bugs](https://doc.rust-lang.org/std/time/struct.Instant.html#monotonicity "struct std::time::Instant"), even under correct logical ordering of the passed `Instant`s, this method can return `None`.

##### Examples

``` rust
use std::time::{Duration, Instant};
use std::thread::sleep;

let now = Instant::now();
sleep(Duration::new(1, 0));
let new_now = Instant::now();
println!("{:?}", new_now.checked_duration_since(now));
println!("{:?}", now.checked_duration_since(new_now)); // None
```

1.39.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#361-363)

Returns the amount of time elapsed from another instant to this one, or zero duration if that instant is later than this one.

##### Examples

``` rust
use std::time::{Duration, Instant};
use std::thread::sleep;

let now = Instant::now();
sleep(Duration::new(1, 0));
let new_now = Instant::now();
println!("{:?}", new_now.saturating_duration_since(now));
println!("{:?}", now.saturating_duration_since(new_now)); // 0ns
```

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#388-390)

Returns the amount of time elapsed since this instant.

##### Panics

Previous Rust versions panicked when the current time was earlier than self. Currently this method returns a Duration of zero in that case. Future versions may reintroduce the panic. See [Monotonicity](https://doc.rust-lang.org/std/time/struct.Instant.html#monotonicity "struct std::time::Instant").

##### Examples

``` rust
use std::thread::sleep;
use std::time::{Duration, Instant};

let instant = Instant::now();
let three_secs = Duration::from_secs(3);
sleep(three_secs);
assert!(instant.elapsed() >= three_secs);
```

1.34.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#396-398)

Returns `Some(t)` where `t` is the time `self + duration` if `t` can be represented as `Instant` (which means it’s inside the bounds of the underlying data structure), `None` otherwise.

1.34.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#404-406)

Returns `Some(t)` where `t` is the time `self - duration` if `t` can be represented as `Instant` (which means it’s inside the bounds of the underlying data structure), `None` otherwise.

## Trait Implementations

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#419-430)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#427-429)

##### Panics

This function may panic if the resulting point in time cannot be represented by the underlying data structure. See [`Instant::checked_add`](https://doc.rust-lang.org/std/time/struct.Instant.html#method.checked_add "method std::time::Instant::checked_add") for a version without panic.

[Source](https://doc.rust-lang.org/src/std/time.rs.html#420)

The resulting type after applying the `+` operator.

1.9.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#433-437)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#434-436)

Performs the `+=` operation. [Read more](https://doc.rust-lang.org/std/ops/trait.AddAssign.html#tymethod.add_assign)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

Returns a duplicate of the value. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#tymethod.clone)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/142757 "Tracking issue for const_clone")) · [Source](https://doc.rust-lang.org/src/core/clone.rs.html#245-247)

Performs copy-assignment from `source`. [Read more](https://doc.rust-lang.org/std/clone/trait.Clone.html#method.clone_from)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#476-480)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#477-479)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

Feeds this value into the given [`Hasher`](https://doc.rust-lang.org/std/hash/trait.Hasher.html "trait std::hash::Hasher"). [Read more](https://doc.rust-lang.org/std/hash/trait.Hash.html#tymethod.hash)

1.3.0 · [Source](https://doc.rust-lang.org/src/core/hash/mod.rs.html#234-236)

Feeds a slice of this type into the given [`Hasher`](https://doc.rust-lang.org/std/hash/trait.Hasher.html "trait std::hash::Hasher"). [Read more](https://doc.rust-lang.org/std/hash/trait.Hash.html#method.hash_slice)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

This method returns an [`Ordering`](https://doc.rust-lang.org/std/cmp/enum.Ordering.html "enum std::cmp::Ordering") between `self` and `other`. [Read more](https://doc.rust-lang.org/std/cmp/trait.Ord.html#tymethod.cmp)

1.21.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1034-1036)

Compares and returns the maximum of two values. [Read more](https://doc.rust-lang.org/std/cmp/trait.Ord.html#method.max)

1.21.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1073-1075)

Compares and returns the minimum of two values. [Read more](https://doc.rust-lang.org/std/cmp/trait.Ord.html#method.min)

1.50.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1099-1101)

Restrict a value to a certain interval. [Read more](https://doc.rust-lang.org/std/cmp/trait.Ord.html#method.clamp)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

Tests for `self` and `other` values to be equal, and is used by `==`.

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#263)

Tests for `!=`. The default implementation is almost always sufficient, and should not be overridden without very good reason.

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

This method returns an ordering between `self` and `other` values if one exists. [Read more](https://doc.rust-lang.org/std/cmp/trait.PartialOrd.html#tymethod.partial_cmp)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1410)

Tests less than (for `self` and `other`) and is used by the `<` operator. [Read more](https://doc.rust-lang.org/std/cmp/trait.PartialOrd.html#method.lt)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1428)

Tests less than or equal to (for `self` and `other`) and is used by the `<=` operator. [Read more](https://doc.rust-lang.org/std/cmp/trait.PartialOrd.html#method.le)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1446)

Tests greater than (for `self` and `other`) and is used by the `>` operator. [Read more](https://doc.rust-lang.org/std/cmp/trait.PartialOrd.html#method.gt)

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143800 "Tracking issue for const_cmp")) · [Source](https://doc.rust-lang.org/src/core/cmp.rs.html#1464)

Tests greater than or equal to (for `self` and `other`) and is used by the `>=` operator. [Read more](https://doc.rust-lang.org/std/cmp/trait.PartialOrd.html#method.ge)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#153)

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#457-473)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#470-472)

Returns the amount of time elapsed from another instant to this one, or zero duration if that instant is later than this one.

##### Panics

Previous Rust versions panicked when `other` was later than `self`. Currently this method saturates. Future versions may reintroduce the panic in some circumstances. See [Monotonicity](https://doc.rust-lang.org/std/time/struct.Instant.html#monotonicity "struct std::time::Instant").

[Source](https://doc.rust-lang.org/src/std/time.rs.html#458)

The resulting type after applying the `-` operator.

1.8.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#440-447)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#441)

The resulting type after applying the `-` operator.

[Source](https://doc.rust-lang.org/src/std/time.rs.html#444-446)

Performs the `-` operation. [Read more](https://doc.rust-lang.org/std/ops/trait.Sub.html#tymethod.sub)

1.9.0 · [Source](https://doc.rust-lang.org/src/std/time.rs.html#450-454)

[Source](https://doc.rust-lang.org/src/std/time.rs.html#451-453)

Performs the `-=` operation. [Read more](https://doc.rust-lang.org/std/ops/trait.SubAssign.html#tymethod.sub_assign)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#648)

[Source](https://doc.rust-lang.org/src/core/clone.rs.html#650)

🔬This is a nightly-only experimental API. (`clone_to_uninit` [\#126799](https://github.com/rust-lang/rust/issues/126799))

Performs copy-assignment from `self` to `dest`. [Read more](https://doc.rust-lang.org/std/clone/trait.CloneToUninit.html#tymethod.clone_to_uninit)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#72-74)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#76)

The resulting type after obtaining ownership.

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#77)

Creates owned data from borrowed data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#tymethod.to_owned)

[Source](https://doc.rust-lang.org/src/alloc/borrow.rs.html#81)

Uses borrowed data to replace owned data, usually by cloning. [Read more](https://doc.rust-lang.org/std/borrow/trait.ToOwned.html#method.clone_into)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.