# Stringlike types

Racket’s stringlike types include *strings*, *symbols*, *identifiers*, *keywords*, and *path strings*. They are “stringlike” in the sense that they all consist of a sequence of Unicode characters, and can easily be converted among one another.

### [Strings](#strings)

A standard *string* is enclosed in "double quotes". A literal double-quote character can be escaped as \.

    "one string"
    "two \"strings\""

By default, strings will print as literal expressions (meaning, their printed form can be reused as input). To print strings in the usual friendly way, use [display](http://docs.racket-lang.org/reference/Writing.html#(def._((quote._~23~25kernel)._display))) or [displayln](http://docs.racket-lang.org/reference/Writing.html#(def._((lib._racket/private/misc..rkt)._displayln))). To print strings in the raw style used on [the REPL](https://beautifulracket.com/explainer/the-repls.html), use [print](http://docs.racket-lang.org/reference/Writing.html#(def._((quote._~23~25kernel)._print))) or [println](http://docs.racket-lang.org/reference/Writing.html#(def._((lib._racket/private/misc..rkt)._println))):

    "one literal string"
    "two literal \"strings\""
    (display "one displayed string\n")
    (displayln "two displayed \"strings\"")
    (print "one printed string\n")
    (println "two printed \"strings\"")

    "one literal string"
    "two literal \"strings\""
    one displayed string
    two displayed "strings"
    "one printed string\n""two printed \"strings\""

Strings can also include the usual escape characters to represent ASCII whitespace, like \t for tab and \n for linefeed, as well as special classes of escape characters like \x for hex numbers and \u for Unicode characters. (See the Racket docs for [the full list](http://docs.racket-lang.org/reference/reader.html?q=here%20string#%28part._parse-string%29).)

Racket also has the usual string-manipulation functions—[string-split](http://docs.racket-lang.org/reference/strings.html#(def._((lib._racket/string..rkt)._string-split))), [string-join](http://docs.racket-lang.org/reference/strings.html#(def._((lib._racket/string..rkt)._string-join))), [string-append](http://docs.racket-lang.org/reference/strings.html#(def._((quote._~23~25kernel)._string-append))), [format](http://docs.racket-lang.org/reference/Writing.html#(def._((quote._~23~25kernel)._format))), etc.—that do what you’d expect.

### [Further](#further)

-

  [Strings](http://docs.racket-lang.org/guide/strings.html) in the Racket Guide

-

  [Strings](http://docs.racket-lang.org/reference/strings.html) in the Racket Reference

### [Here strings & @-expressions](#here-strings-and--expressions)

To make multiline strings inside a program, two options.

You can use a *here string*, which is introduced by #<< and a delimiter name, followed by the string starting on the next line, ending with the delimiter on its own line:

    (define str #<<here-string-delimiter
    A wonderful
    multiline string
    that goes on
    seemingly
    forever.
    here-string-delimiter
    ) ; contrary to usual Racket style,
      ; the closing paren of a here string must go on a new line

    (display str)

    A wonderful
    multiline string
    that goes on
    seemingly
    forever.

You can also use an *@-expression*, which is a special textual kind of S-expression, by adding the at-exp metalanguage to the #lang line:

    #lang at-exp br ; note `at-exp` here

    (define str @string-append{
    A wonderful
    multiline string
    that goes on
    seemingly
    forever.})

    (display str) ; same result as above

A nice advantage of @-expressions over here strings is that they can include normally evaluated subexpressions, the results of which are spliced into the string. These subexpressions are prefixed with another @:

    #lang at-exp br

    (define base 2)
    (define pow 8)

    (define str @string-append{
    A wonderful
    multiline string
    that goes on
    @(number->string (expt base pow)) years.})

    (display str)

    A wonderful
    multiline string
    that goes on
    256 years.

### [Further](#further)

-

  [@-expression syntax](http://docs.racket-lang.org/scribble/reader.html) in the Scribble docs

### [Symbols](#symbols)

A *symbol* is like a string, but written with a single ' prefix rather than surrounding double quotes. A symbol cannot contain spaces unless the whole symbol is surrounded with | vertical bars |:

    "string"
    'symbol
    "two-word string"
    '|two-word symbol|

Symbols have one special property that strings do not: they are *[interned](https://beautifulracket.com/appendix/glossary.html#interned)*, meaning Racket guarantees that all instances of a particular symbol refer to only one object in memory. Therefore, it’s a good habit to use symbols when you need high-speed equality checking, because symbols can be compared with [eq?](http://docs.racket-lang.org/reference/Equality.html#(def._((quote._~23~25kernel)._eq~3f))), which is much faster than the [equal?](http://docs.racket-lang.org/reference/Equality.html#(def._((quote._~23~25kernel)._equal~3f))) required by strings:

    (equal? "string" "string") ; slower
    (equal? 'symbol 'symbol) ; slower
    (eq? 'symbol 'symbol) ; wicked fast

[string->symbol](http://docs.racket-lang.org/reference/symbols.html#(def._((quote._~23~25kernel)._string-~3esymbol))) and [symbol->string](http://docs.racket-lang.org/reference/symbols.html#(def._((quote._~23~25kernel)._symbol-~3estring))) are available for explicit conversions.

### [Further](#further)

-

  [Equality](https://beautifulracket.com/explainer/equality.html) explainer

-

  [Symbols](http://docs.racket-lang.org/guide/symbols.html) in the Racket Guide

### [Identifiers](#identifiers)

An *identifier* is a name that appears in a program. For its behavior, see [identifiers](https://beautifulracket.com/explainer/identifiers.html). Within a [datum](https://beautifulracket.com/appendix/glossary.html#datum), an identifier is represented with a symbol:

    (define foo "see")
    (define bar "saw")
    (string-append foo bar) ; "seesaw"
    '(string-append foo bar) ; '(string-append foo bar)
    (andmap symbol? '(string-append foo bar)) ; #t

Here, (string-append foo bar) is an S-expression consisting of a list containing three identifiers. When evaluated normally, it becomes "seesaw". When converted to a datum, it becomes a list of three symbols (= 'string-append, 'foo, 'bar).

Conversely, when a datum is evaluated, symbols inside are treated as identifiers. For instance, when the three symbols 'cons, 'foo, and 'bar are grouped into a list and evaluated, they are treated as identifiers:

    (define foo "see")
    (define bar "saw")
    (define datum (list 'cons 'foo 'bar))
    (evaluate datum) ; '("see" . "saw")

In these situations, if you really want a symbol in the result, then put two ' prefixes on the original item:

    (define foo "see")
    (define bar "saw")
    (define datum (list 'cons ''foo ''bar))
    (evaluate datum) ; '(foo . bar)

Most strings can be converted into identifiers, though there are some [reserved characters](http://docs.racket-lang.org/guide/symbols.html) and special conventions. For instance, strings representing numbers cannot be used as identifiers.

### [Further](#further)

-

  [Identifiers](http://docs.racket-lang.org/guide/syntax-overview.html?q=identifiers#%28part._.Identifiers%29) in the Racket Guide

### [Keywords](#keywords)

A keyword is used to introduce a [keyword argument](https://beautifulracket.com/appendix/glossary.html#keyword-argument) either in a function definition or a call to that function. Below, #:base and #:pow are keywords:

    (define (f #:base x #:pow y)
      (expt x y))
    (f #:base 2 #:pow 8) ; 256

It’s usually only necessary to think about keywords as data if you’re writing macros or other special operations that programmatically manipulate keyword arguments, in which case you can use [string->keyword](http://docs.racket-lang.org/reference/keywords.html#(def._((quote._~23~25kernel)._string-~3ekeyword))) and [keyword->string](http://docs.racket-lang.org/reference/keywords.html#(def._((quote._~23~25kernel)._keyword-~3estring))) to convert.

    (define (f #:base x #:pow y)
      (expt x y))
    (f #:base 2 #:pow 8) ; 256
    (keyword-apply f '(#:base #:pow) '(2 8) empty) ; 256
    (keyword-apply f (list
                      (string->keyword "base")
                      (string->keyword "pow")) '(2 8) empty) ; 256

For more about the use of keyword arguments, see [functions](https://beautifulracket.com/explainer/functions.html).

### [Further](#further)

-

  [Keywords](http://docs.racket-lang.org/guide/keywords.html) in the Racket Guide

-

  [Keywords](http://docs.racket-lang.org/reference/keywords.html) in the Racket Reference

A line comment begins with ;. A multiline comment is delimited with #| and |#. A single [S-expression](https://beautifulracket.com/appendix/glossary.html#s-expression) can be commented with #;.

    ; comment
    "not a comment"
    #|
    a nice
    multiline
    comment
    |#
    (list 1 2 #;(list 'comment) 3 4)

    "not a comment"
    '(1 2 3 4)

### [Path strings](#path-strings)

A *path string* is not a distinct type, but rather the subset of strings that can be converted into a valid path. Many Racket functions that manipulate paths will accept either a path or a path string, so it’s often not necessary to manually convert the input argument with [string->path](http://docs.racket-lang.org/reference/Manipulating_Paths.html#(def._((quote._~23~25kernel)._string-~3epath))):

    (file-exists? (string->path "/usr/bin/racket")) ; #t
    (file-exists? "/usr/bin/racket") ; #t

### [Paths](#paths)

A *path* represents a file location. Paths are similar to strings. But it’s slightly misleading to categorize them as “stringlike”, because they’re made of a sequence of bytes rather than Unicode characters. In Racket, this is called a [byte string](http://docs.racket-lang.org/guide/bytestrings.html?q=byte%20string#%28tech._byte._string%29). It’s a distinct data type from an ordinary Unicode string.

In practice, many paths can be converted to an ordinary string and back to a path. So as a convenience, Racket offers [path->string](http://docs.racket-lang.org/reference/Manipulating_Paths.html#(def._((quote._~23~25kernel)._path-~3estring))) and [string->path](http://docs.racket-lang.org/reference/Manipulating_Paths.html#(def._((quote._~23~25kernel)._string-~3epath))):

    "/usr/bin/racket"
    (string->path "/usr/bin/racket")
    (path->string (string->path "/usr/bin/racket"))

    "/usr/bin/racket"
    #<path:/usr/bin/racket>
    "/usr/bin/racket"

But some paths can’t be converted to a string without losing data. In general, path->string is intended for making user-readable versions of paths. It’s unwise to manipulate paths by converting them to strings and back again. Better to use Racket’s dedicated path-manipulation functions.

### [Further](#further)

-

  [Paths](http://docs.racket-lang.org/reference/pathutils.html) in the Racket Reference