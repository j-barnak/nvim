[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[fd](https://doc.rust-lang.org/std/os/fd/index.html)

# Trait FromRawFd Copy item path

1.66.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/raw.rs.html#75-114)

``` rust
pub trait FromRawFd {
    // Required method
    unsafe fn from_raw_fd(fd: RawFd) -> Self;
}
```

Available on **Hermit or Motor OS or Trusty or Unix or WASI** only.

Expand description

A trait to express the ability to construct an object from a raw file descriptor.

## Required Methods

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/raw.rs.html#113)

Constructs a new instance of `Self` from the given raw file descriptor.

This function is typically used to **consume ownership** of the specified file descriptor. When used in this way, the returned object will take responsibility for closing it when the object goes out of scope.

However, consuming ownership is not strictly required. Use a [`From<OwnedFd>::from`](https://doc.rust-lang.org/std/convert/trait.From.html#tymethod.from "associated function std::convert::From::from") implementation for an API which strictly consumes ownership.

##### Safety

The `fd` passed in must be an [owned file descriptor](https://doc.rust-lang.org/std/io/index.html#io-safety "mod std::io"); in particular, it must be open.

##### Example

``` rust
use std::fs::File;
#[cfg(any(unix, target_os = "wasi"))]
use std::os::fd::{FromRawFd, IntoRawFd, RawFd};

let f = File::open("foo.txt")?;
let raw_fd: RawFd = f.into_raw_fd();
// SAFETY: no other functions should call `from_raw_fd`, so there
// is only one owner for the file descriptor.
let f = unsafe { File::from_raw_fd(raw_fd) };
```

## Dyn Compatibility

This trait is **not** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

[Source](https://doc.rust-lang.org/src/std/sys/fs/unix/dir.rs.html#99-103)

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/raw.rs.html#180-185)

Available on **non-Trusty** only.

1.63.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/owned.rs.html#175-193)

[Source](https://doc.rust-lang.org/src/std/os/linux/process.rs.html#116-120)

Available on **Linux** only.

1.87.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/raw.rs.html#302-306)

Available on **non-Trusty** only.

1.87.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/raw.rs.html#326-330)

Available on **non-Trusty** only.

1.48.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/raw.rs.html#163-168)

1.2.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/process.rs.html#583-590)

Available on **Unix** only.

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/net.rs.html#33)

Available on **non-Trusty** only.

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/net.rs.html#33)

Available on **non-Trusty** only.

1.1.0 · [Source](https://doc.rust-lang.org/src/std/os/fd/net.rs.html#33)

Available on **non-Trusty** only.

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/datagram.rs.html#970-975)

Available on **Unix** only.

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/listener.rs.html#319-324)

Available on **Unix** only.

1.10.0 · [Source](https://doc.rust-lang.org/src/std/os/unix/net/stream.rs.html#700-705)

Available on **Unix** only.