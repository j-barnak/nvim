[std](https://doc.rust-lang.org/std/index.html)::[io](https://doc.rust-lang.org/std/io/index.html)

# Struct LineWriter Copy item path

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#68-70)

``` rust
pub struct LineWriter<W: ?Sized + Write> { /* private fields */ }
```

Expand description

Wraps a writer and buffers output to it, flushing whenever a newline (`0x0a`, `'\n'`) is detected.

The [`BufWriter`](https://doc.rust-lang.org/std/io/struct.BufWriter.html "struct std::io::BufWriter") struct wraps a writer and buffers its output. But it only does this batched write when it goes out of scope, or when the internal buffer is full. Sometimes, you’d prefer to write each line as it’s completed, rather than the entire buffer at once. Enter `LineWriter`. It does exactly that.

Like [`BufWriter`](https://doc.rust-lang.org/std/io/struct.BufWriter.html "struct std::io::BufWriter"), a `LineWriter`’s buffer will also be flushed when the `LineWriter` goes out of scope or when its internal buffer is full.

If there’s still a partial line in the buffer when the `LineWriter` is dropped, it will flush those contents.

## Examples

We can use `LineWriter` to write one line at a time, significantly reducing the number of actual writes to the file.

``` rust
use std::fs::{self, File};
use std::io::prelude::*;
use std::io::LineWriter;

fn main() -> std::io::Result<()> {
    let road_not_taken = b"I shall be telling this with a sigh
Somewhere ages and ages hence:
Two roads diverged in a wood, and I -
I took the one less traveled by,
And that has made all the difference.";

    let file = File::create("poem.txt")?;
    let mut file = LineWriter::new(file);

    file.write_all(b"I shall be telling this with a sigh")?;

    // No bytes are written until a newline is encountered (or
    // the internal buffer is filled).
    assert_eq!(fs::read_to_string("poem.txt")?, "");
    file.write_all(b"\n")?;
    assert_eq!(
        fs::read_to_string("poem.txt")?,
        "I shall be telling this with a sigh\n",
    );

    // Write the rest of the poem.
    file.write_all(b"Somewhere ages and ages hence:
Two roads diverged in a wood, and I -
I took the one less traveled by,
And that has made all the difference.")?;

    // The last line of the poem doesn't end in a newline, so
    // we have to flush or drop the `LineWriter` to finish
    // writing.
    file.flush()?;

    // Confirm the whole poem was written.
    assert_eq!(fs::read("poem.txt")?, &road_not_taken[..]);
    Ok(())
}
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#72-165)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#88-91)

Creates a new `LineWriter`.

##### Examples

``` rust
use std::fs::File;
use std::io::LineWriter;

fn main() -> std::io::Result<()> {
    let file = File::create("poem.txt")?;
    let file = LineWriter::new(file);
    Ok(())
}
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#109-111)

Creates a new `LineWriter` with at least the specified capacity for the internal buffer.

##### Examples

``` rust
use std::fs::File;
use std::io::LineWriter;

fn main() -> std::io::Result<()> {
    let file = File::create("poem.txt")?;
    let file = LineWriter::with_capacity(100, file);
    Ok(())
}
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#134-136)

Gets a mutable reference to the underlying writer.

Caution must be taken when calling methods on the mutable reference returned as extra writes could corrupt the output stream.

##### Examples

``` rust
use std::fs::File;
use std::io::LineWriter;

fn main() -> std::io::Result<()> {
    let file = File::create("poem.txt")?;
    let mut file = LineWriter::new(file);

    // we can use reference just like file
    let reference = file.get_mut();
    Ok(())
}
```

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#162-164)

Unwraps this `LineWriter`, returning the underlying writer.

The internal buffer is written out before returning the writer.

##### Errors

An [`Err`](https://doc.rust-lang.org/std/result/enum.Result.html#variant.Err "variant std::result::Result::Err") will be returned if an error occurs while flushing the buffer.

##### Examples

``` rust
use std::fs::File;
use std::io::LineWriter;

fn main() -> std::io::Result<()> {
    let file = File::create("poem.txt")?;

    let writer: LineWriter<File> = LineWriter::new(file);

    let file: File = writer.into_inner()?;
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#167-188)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#185-187)

Gets a reference to the underlying writer.

##### Examples

``` rust
use std::fs::File;
use std::io::LineWriter;

fn main() -> std::io::Result<()> {
    let file = File::create("poem.txt")?;
    let file = LineWriter::new(file);

    let reference = file.get_ref();
    Ok(())
}
```

## Trait Implementations

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#222-235)

[Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#226-234)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#191-219)

[Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#192-194)

Writes a buffer into this writer, returning how many bytes were written. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.write)

[Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#196-198)

Flushes this output stream, ensuring that all intermediately buffered contents reach their destination. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.flush)

[Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#200-202)

Like [`write`](https://doc.rust-lang.org/std/io/trait.Write.html#tymethod.write "method std::io::Write::write"), except that it writes from a slice of buffers. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_vectored)

[Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#204-206)

🔬This is a nightly-only experimental API. (`can_vector` [\#69941](https://github.com/rust-lang/rust/issues/69941))

Determines if this `Write`r has an efficient [`write_vectored`](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_vectored "method std::io::Write::write_vectored") implementation. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.is_write_vectored)

[Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#208-210)

Attempts to write an entire buffer into this writer. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_all)

[Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#212-214)

🔬This is a nightly-only experimental API. (`write_all_vectored` [\#70436](https://github.com/rust-lang/rust/issues/70436))

Attempts to write multiple buffers into this writer. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_all_vectored)

[Source](https://doc.rust-lang.org/src/std/io/buffered/linewriter.rs.html#216-218)

Writes a formatted string into this writer, returning any error encountered. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.write_fmt)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/io/mod.rs.html#1756-1761)

Creates a “by reference” adapter for this instance of `Write`. [Read more](https://doc.rust-lang.org/std/io/trait.Write.html#method.by_ref)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.