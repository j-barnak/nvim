[std](https://doc.rust-lang.org/std/index.html)::[iter](https://doc.rust-lang.org/std/iter/index.html)

# Trait DoubleEndedIterator Copy item path

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/92476 "Tracking issue for const_iter")) · [Source](https://doc.rust-lang.org/src/core/iter/traits/double_ended.rs.html#44)

``` rust
pub trait DoubleEndedIterator: Iterator {
    // Required method
    fn next_back(&mut self) -> Option<Self::Item>;

    // Provided methods
    fn next_chunk_back<const N: usize>(
        &mut self,
    ) -> Result<[Self::Item; N], IntoIter<Self::Item, N>>
       where Self: Sized { ... }
    fn advance_back_by(&mut self, n: usize) -> Result<(), NonZero<usize>> { ... }
    fn nth_back(&mut self, n: usize) -> Option<Self::Item> { ... }
    fn try_rfold<B, F, R>(&mut self, init: B, f: F) -> R
       where Self: Sized,
             F: FnMut(B, Self::Item) -> R,
             R: Try<Output = B> { ... }
    fn rfold<B, F>(self, init: B, f: F) -> B
       where Self: Sized,
             F: FnMut(B, Self::Item) -> B { ... }
    fn rfind<P>(&mut self, predicate: P) -> Option<Self::Item>
       where Self: Sized,
             P: FnMut(&Self::Item) -> bool { ... }
}
```

Expand description

An iterator able to yield elements from both ends.

Something that implements `DoubleEndedIterator` has one extra capability over something that implements [`Iterator`](https://doc.rust-lang.org/std/iter/trait.Iterator.html "trait std::iter::Iterator"): the ability to also take `Item`s from the back, as well as the front.

It is important to note that both back and forth work on the same range, and do not cross: iteration is over when they meet in the middle.

In a similar fashion to the [`Iterator`](https://doc.rust-lang.org/std/iter/trait.Iterator.html "trait std::iter::Iterator") protocol, once a `DoubleEndedIterator` returns [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") from a [`next_back()`](https://doc.rust-lang.org/std/iter/trait.DoubleEndedIterator.html#tymethod.next_back "method std::iter::DoubleEndedIterator::next_back"), calling it again may or may not ever return [`Some`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.Some "variant std::option::Option::Some") again. [`next()`](https://doc.rust-lang.org/std/iter/trait.Iterator.html#tymethod.next "method std::iter::Iterator::next") and [`next_back()`](https://doc.rust-lang.org/std/iter/trait.DoubleEndedIterator.html#tymethod.next_back "method std::iter::DoubleEndedIterator::next_back") are interchangeable for this purpose.

## Examples

Basic usage:

``` rust
let numbers = vec![1, 2, 3, 4, 5, 6];

let mut iter = numbers.iter();

assert_eq!(Some(&1), iter.next());
assert_eq!(Some(&6), iter.next_back());
assert_eq!(Some(&5), iter.next_back());
assert_eq!(Some(&2), iter.next());
assert_eq!(Some(&3), iter.next());
assert_eq!(Some(&4), iter.next());
assert_eq!(None, iter.next());
assert_eq!(None, iter.next_back());
```

## Required Methods

1.0.0 (const: [unstable](https://github.com/rust-lang/rust/issues/92476 "Tracking issue for const_iter")) · [Source](https://doc.rust-lang.org/src/core/iter/traits/double_ended.rs.html#97)

Removes and returns an element from the end of the iterator.

Returns `None` when there are no more elements.

The [trait-level](https://doc.rust-lang.org/std/iter/trait.DoubleEndedIterator.html "trait std::iter::DoubleEndedIterator") docs contain more details.

##### Examples

Basic usage:

``` rust
let numbers = vec![1, 2, 3, 4, 5, 6];

let mut iter = numbers.iter();

assert_eq!(Some(&1), iter.next());
assert_eq!(Some(&6), iter.next_back());
assert_eq!(Some(&5), iter.next_back());
assert_eq!(Some(&2), iter.next());
assert_eq!(Some(&3), iter.next());
assert_eq!(Some(&4), iter.next());
assert_eq!(None, iter.next());
assert_eq!(None, iter.next_back());
```

##### Remarks

The elements yielded by `DoubleEndedIterator`’s methods may differ from the ones yielded by [`Iterator`](https://doc.rust-lang.org/std/iter/trait.Iterator.html "trait std::iter::Iterator")’s methods:

``` rust
let vec = vec![(1, 'a'), (1, 'b'), (1, 'c'), (2, 'a'), (2, 'b')];
let uniq_by_fst_comp = || {
    let mut seen = std::collections::HashSet::new();
    vec.iter().copied().filter(move |x| seen.insert(x.0))
};

assert_eq!(uniq_by_fst_comp().last(), Some((2, 'a')));
assert_eq!(uniq_by_fst_comp().next_back(), Some((2, 'b')));

assert_eq!(
    uniq_by_fst_comp().fold(vec![], |mut v, x| {v.push(x); v}),
    vec![(1, 'a'), (2, 'a')]
);
assert_eq!(
    uniq_by_fst_comp().rfold(vec![], |mut v, x| {v.push(x); v}),
    vec![(2, 'b'), (1, 'c')]
);
```

## Provided Methods

[Source](https://doc.rust-lang.org/src/core/iter/traits/double_ended.rs.html#137-141)

🔬This is a nightly-only experimental API. (`iter_next_chunk` [\#98326](https://github.com/rust-lang/rust/issues/98326))

Advances from the back of the iterator and returns an array containing the next `N` values in sequence.

If there are not enough elements to fill the array then `Err` is returned containing an iterator over the remaining elements.

Note: This is not equivalent to doing `iter.rev().next_chunk()` as this method takes elements from the back of the iterator and preserves the order that the elements were seen in the original iterator.

##### Examples

Basic usage:

``` rust
#![feature(iter_next_chunk)]

let mut iter = "lorem".chars();

assert_eq!(iter.next_chunk_back().unwrap(), ['e', 'm']);              // N is inferred as 2
assert_eq!(iter.next_chunk_back().unwrap(), ['l', 'o', 'r']);         // N is inferred as 3
assert_eq!(iter.next_chunk_back::<4>().unwrap_err().as_slice(), &[]); // N is explicitly 4
```

Split a string and get the last three items in sequence.

``` rust
#![feature(iter_next_chunk)]

let quote = "not all those who wander are lost";
let [first, second, third] = quote.split_whitespace().next_chunk_back().unwrap();
assert_eq!(first, "wander");
assert_eq!(second, "are");
assert_eq!(third, "lost");
```

[Source](https://doc.rust-lang.org/src/core/iter/traits/double_ended.rs.html#189)

🔬This is a nightly-only experimental API. (`iter_advance_by` [\#77404](https://github.com/rust-lang/rust/issues/77404))

Advances the iterator from the back by `n` elements.

`advance_back_by` is the reverse version of [`advance_by`](https://doc.rust-lang.org/std/iter/trait.Iterator.html#method.advance_by "method std::iter::Iterator::advance_by"). This method will eagerly skip `n` elements starting from the back by calling [`next_back`](https://doc.rust-lang.org/std/iter/trait.DoubleEndedIterator.html#tymethod.next_back "method std::iter::DoubleEndedIterator::next_back") up to `n` times until [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") is encountered.

`advance_back_by(n)` will return `Ok(())` if the iterator successfully advances by `n` elements, or a `Err(NonZero<usize>)` with value `k` if [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") is encountered, where `k` is remaining number of steps that could not be advanced because the iterator ran out. If `self` is empty and `n` is non-zero, then this returns `Err(n)`. Otherwise, `k` is always less than `n`.

Calling `advance_back_by(0)` can do meaningful work, for example [`Flatten`](https://doc.rust-lang.org/std/iter/struct.Flatten.html "struct std::iter::Flatten") can advance its outer iterator until it finds an inner iterator that is not empty, which then often allows it to return a more accurate `size_hint()` than in its initial state.

##### Examples

Basic usage:

``` rust
#![feature(iter_advance_by)]

use std::num::NonZero;

let a = [3, 4, 5, 6];
let mut iter = a.iter();

assert_eq!(iter.advance_back_by(2), Ok(()));
assert_eq!(iter.next_back(), Some(&4));
assert_eq!(iter.advance_back_by(0), Ok(()));
assert_eq!(iter.advance_back_by(100), Err(NonZero::new(99).unwrap())); // only `&3` was skipped
```

1.37.0 (const: [unstable](https://github.com/rust-lang/rust/issues/92476 "Tracking issue for const_iter")) · [Source](https://doc.rust-lang.org/src/core/iter/traits/double_ended.rs.html#243)

Returns the `n`th element from the end of the iterator.

This is essentially the reversed version of [`Iterator::nth()`](https://doc.rust-lang.org/std/iter/trait.Iterator.html#method.nth "method std::iter::Iterator::nth"). Although like most indexing operations, the count starts from zero, so `nth_back(0)` returns the first value from the end, `nth_back(1)` the second, and so on.

Note that all elements between the end and the returned element will be consumed, including the returned element. This also means that calling `nth_back(0)` multiple times on the same iterator will return different elements.

`nth_back()` will return [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") if `n` is greater than or equal to the length of the iterator.

##### Examples

Basic usage:

``` rust
let a = [1, 2, 3];
assert_eq!(a.iter().nth_back(2), Some(&1));
```

Calling `nth_back()` multiple times doesn’t rewind the iterator:

``` rust
let a = [1, 2, 3];

let mut iter = a.iter();

assert_eq!(iter.nth_back(1), Some(&2));
assert_eq!(iter.nth_back(1), None);
```

Returning `None` if there are less than `n + 1` elements:

``` rust
let a = [1, 2, 3];
assert_eq!(a.iter().nth_back(10), None);
```

1.27.0 (const: [unstable](https://github.com/rust-lang/rust/issues/92476 "Tracking issue for const_iter")) · [Source](https://doc.rust-lang.org/src/core/iter/traits/double_ended.rs.html#282-286)

This is the reverse version of [`Iterator::try_fold()`](https://doc.rust-lang.org/std/iter/trait.Iterator.html#method.try_fold "method std::iter::Iterator::try_fold"): it takes elements starting from the back of the iterator.

##### Examples

Basic usage:

``` rust
let a = ["1", "2", "3"];
let sum = a.iter()
    .map(|&s| s.parse::<i32>())
    .try_rfold(0, |acc, x| x.and_then(|y| Ok(acc + y)));
assert_eq!(sum, Ok(6));
```

Short-circuiting:

``` rust
let a = ["1", "rust", "3"];
let mut it = a.iter();
let sum = it
    .by_ref()
    .map(|&s| s.parse::<i32>())
    .try_rfold(0, |acc, x| x.and_then(|y| Ok(acc + y)));
assert!(sum.is_err());

// Because it short-circuited, the remaining elements are still
// available through the iterator.
assert_eq!(it.next_back(), Some(&"1"));
```

1.27.0 (const: [unstable](https://github.com/rust-lang/rust/issues/92476 "Tracking issue for const_iter")) · [Source](https://doc.rust-lang.org/src/core/iter/traits/double_ended.rs.html#353-356)

An iterator method that reduces the iterator’s elements to a single, final value, starting from the back.

This is the reverse version of [`Iterator::fold()`](https://doc.rust-lang.org/std/iter/trait.Iterator.html#method.fold "method std::iter::Iterator::fold"): it takes elements starting from the back of the iterator.

`rfold()` takes two arguments: an initial value, and a closure with two arguments: an ‘accumulator’, and an element. The closure returns the value that the accumulator should have for the next iteration.

The initial value is the value the accumulator will have on the first call.

After applying this closure to every element of the iterator, `rfold()` returns the accumulator.

This operation is sometimes called ‘reduce’ or ‘inject’.

Folding is useful whenever you have a collection of something, and want to produce a single value from it.

Note: `rfold()` combines elements in a *right-associative* fashion. For associative operators like `+`, the order the elements are combined in is not important, but for non-associative operators like `-` the order will affect the final result. For a *left-associative* version of `rfold()`, see [`Iterator::fold()`](https://doc.rust-lang.org/std/iter/trait.Iterator.html#method.fold "method std::iter::Iterator::fold").

##### Examples

Basic usage:

``` rust
let a = [1, 2, 3];

// the sum of all of the elements of a
let sum = a.iter()
           .rfold(0, |acc, &x| acc + x);

assert_eq!(sum, 6);
```

This example demonstrates the right-associative nature of `rfold()`: it builds a string, starting with an initial value and continuing with each element from the back until the front:

``` rust
let numbers = [1, 2, 3, 4, 5];

let zero = "0".to_string();

let result = numbers.iter().rfold(zero, |acc, &x| {
    format!("({x} + {acc})")
});

assert_eq!(result, "(1 + (2 + (3 + (4 + (5 + 0)))))");
```

1.27.0 (const: [unstable](https://github.com/rust-lang/rust/issues/92476 "Tracking issue for const_iter")) · [Source](https://doc.rust-lang.org/src/core/iter/traits/double_ended.rs.html#419-422)

Searches for an element of an iterator from the back that satisfies a predicate.

`rfind()` takes a closure that returns `true` or `false`. It applies this closure to each element of the iterator, starting at the end, and if any of them return `true`, then `rfind()` returns [`Some(element)`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.Some "variant std::option::Option::Some"). If they all return `false`, it returns [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None").

`rfind()` is short-circuiting; in other words, it will stop processing as soon as the closure returns `true`.

Because `rfind()` takes a reference, and many iterators iterate over references, this leads to a possibly confusing situation where the argument is a double reference. You can see this effect in the examples below, with `&&x`.

##### Examples

Basic usage:

``` rust
let a = [1, 2, 3];

assert_eq!(a.into_iter().rfind(|&x| x == 2), Some(2));
assert_eq!(a.into_iter().rfind(|&x| x == 5), None);
```

Iterating over references:

``` rust
let a = [1, 2, 3];

// `iter()` yields references i.e. `&i32` and `rfind()` takes a
// reference to each element.
assert_eq!(a.iter().rfind(|&&x| x == 2), Some(&2));
assert_eq!(a.iter().rfind(|&&x| x == 5), None);
```

Stopping at the first `true`:

``` rust
let a = [1, 2, 3];

let mut iter = a.iter();

assert_eq!(iter.rfind(|&&x| x == 2), Some(&2));

// we can still use `iter`, as there are more elements.
assert_eq!(iter.next_back(), Some(&1));
```

## Dyn Compatibility

This trait **is** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

1.12.0 · [Source](https://doc.rust-lang.org/src/std/env.rs.html#912-916)

1.12.0 · [Source](https://doc.rust-lang.org/src/std/env.rs.html#1000-1010)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#364)

1.6.0 · [Source](https://doc.rust-lang.org/src/alloc/string.rs.html#3608)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/ascii.rs.html#142)

[Source](https://doc.rust-lang.org/src/alloc/string.rs.html#3492)

[Source](https://doc.rust-lang.org/src/core/char/mod.rs.html#522-535)

1.59.0 · [Source](https://doc.rust-lang.org/src/core/char/mod.rs.html#507-520)

[Source](https://doc.rust-lang.org/src/core/char/mod.rs.html#492-505)

1.59.0 · [Source](https://doc.rust-lang.org/src/core/char/mod.rs.html#477-490)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/option.rs.html#2549)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/option.rs.html#2599)

1.1.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/cloned.rs.html#75-78)

1.36.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/copied.rs.html#112-115)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/traits/double_ended.rs.html#436)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/map.rs.html#1771)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/map.rs.html#1846)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/map.rs.html#2026)

1.17.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/map.rs.html#2485)

1.17.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/map.rs.html#2529)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/map.rs.html#2084)

1.10.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/map.rs.html#2329)

1.5.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#1078-1094)

1.2.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#1135-1151)

1.5.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#1078-1094)

1.2.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#1135-1151)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#786-802)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#850-866)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#786-802)

1.51.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#1443-1444)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#850-866)

1.77.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#3060-3062)

1.77.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#3155-3157)

1.27.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#989-991)

1.27.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#1086-1088)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#492-494)

1.51.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#633-635)

1.51.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#877-879)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#758-760)

1.94.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#2244)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#1571)

1.31.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#1938)

1.31.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#2095)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#1751)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/result.rs.html#1998)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#140-148)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/binary_heap/mod.rs.html#1655)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#1793)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/linked_list.rs.html#1227)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/vec_deque/iter.rs.html#162)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/result.rs.html#2047)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#369)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/linked_list.rs.html#1295)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/vec_deque/iter_mut.rs.html#226)

1.31.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#2398)

1.31.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#2752)

1.31.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#2919)

1.31.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#2563)

1.17.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#1911)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/slice/iter.rs.html#1414)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#213)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#123)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/path.rs.html#975-1020)

1.60.0 · [Source](https://doc.rust-lang.org/src/core/slice/ascii.rs.html#364)

1.0.0 · [Source](https://doc.rust-lang.org/src/std/path.rs.html#909-914)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#1185)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#1248)

1.34.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#1375)

1.1.0 · [Source](https://doc.rust-lang.org/src/core/str/iter.rs.html#1319)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/chain.rs.html#205-208)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/zip.rs.html#116-119)

1.43.0 · [Source](https://doc.rust-lang.org/src/core/iter/sources/once_with.rs.html#104)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/option.rs.html#2641)

[Source](https://doc.rust-lang.org/src/core/option.rs.html#2734)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/range.rs.html#1113)

1.26.0 · [Source](https://doc.rust-lang.org/src/core/iter/range.rs.html#1490)

1.95.0 · [Source](https://doc.rust-lang.org/src/core/range/iter.rs.html#259)

1.96.0 · [Source](https://doc.rust-lang.org/src/core/range/iter.rs.html#136)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/sources/repeat.rs.html#126)

1.82.0 · [Source](https://doc.rust-lang.org/src/core/iter/sources/repeat_n.rs.html#175)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/filter_map.rs.html#151-153)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/map.rs.html#143-145)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/boxed/iter.rs.html#61)

[Source](https://doc.rust-lang.org/src/alloc/collections/vec_deque/splice.rs.html#47)

1.21.0 · [Source](https://doc.rust-lang.org/src/alloc/vec/splice.rs.html#43)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/inspect.rs.html#107-109)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/filter.rs.html#170-172)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/flatten.rs.html#107-110)

1.29.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/flatten.rs.html#270-273)

[Source](https://doc.rust-lang.org/src/core/iter/adapters/array_chunks.rs.html#112-114)

[Source](https://doc.rust-lang.org/src/core/iter/adapters/by_ref_sized.rs.html#59)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/enumerate.rs.html#175-177)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/fuse.rs.html#127-129)

1.38.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/peekable.rs.html#121-123)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/rev.rs.html#96-98)

1.9.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/skip.rs.html#191-193)

1.38.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/step_by.rs.html#102-104)

1.38.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/take.rs.html#151-153)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/map.rs.html#1974)

1.54.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/map.rs.html#2394)

1.54.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/map.rs.html#2448)

1.6.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/binary_heap/mod.rs.html#1865)

1.6.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/vec_deque/drain.rs.html#258)

1.6.0 · [Source](https://doc.rust-lang.org/src/alloc/vec/drain.rs.html#165)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/btree/set.rs.html#1840)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/binary_heap/mod.rs.html#1718)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/linked_list.rs.html#2021)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/collections/vec_deque/into_iter.rs.html#180)

1.0.0 · [Source](https://doc.rust-lang.org/src/alloc/vec/into_iter.rs.html#424)

1.40.0 · [Source](https://doc.rust-lang.org/src/core/array/iter.rs.html#295)

1.2.0 · [Source](https://doc.rust-lang.org/src/core/iter/sources/empty.rs.html#53)

1.0.0 · [Source](https://doc.rust-lang.org/src/core/result.rs.html#2093)

1.2.0 · [Source](https://doc.rust-lang.org/src/core/iter/sources/once.rs.html#84)

1.82.0 · [Source](https://doc.rust-lang.org/src/core/iter/adapters/take.rs.html#322)