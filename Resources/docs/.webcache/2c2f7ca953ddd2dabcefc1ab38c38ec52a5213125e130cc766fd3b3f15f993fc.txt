[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)

# Struct LazyLock Copy item path

1.80.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#81-85)

``` rust
pub struct LazyLock<T, F = fn() -> T> { /* private fields */ }
```

Expand description

A value which is initialized on the first access.

This type is a thread-safe [`LazyCell`](https://doc.rust-lang.org/std/cell/struct.LazyCell.html "struct std::cell::LazyCell"), and can be used in statics. Since initialization may be called from multiple threads, any dereferencing call will block the calling thread if another initialization routine is currently running.

## Poisoning

If the initialization closure passed to [`LazyLock::new`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.new "associated function std::sync::LazyLock::new") panics, the lock will be poisoned. Once the lock is poisoned, any threads that attempt to access this lock (via a dereference or via an explicit call to [`force()`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.force "associated function std::sync::LazyLock::force")) will panic.

This concept is similar to that of poisoning in the [`std::sync::poison`](https://doc.rust-lang.org/std/sync/poison/index.html "mod std::sync::poison") module. A key difference, however, is that poisoning in `LazyLock` is *unrecoverable*. All future accesses of the lock from other threads will panic, whereas a type in [`std::sync::poison`](https://doc.rust-lang.org/std/sync/poison/index.html "mod std::sync::poison") like [`std::sync::poison::Mutex`](https://doc.rust-lang.org/std/sync/struct.Mutex.html "struct std::sync::Mutex") allows recovery via [`PoisonError::into_inner()`](https://doc.rust-lang.org/std/sync/struct.PoisonError.html#method.into_inner "method std::sync::PoisonError::into_inner").

## Examples

Initialize static variables with `LazyLock`.

``` rust
use std::sync::LazyLock;

// Note: static items do not call [`Drop`] on program termination, so this won't be deallocated.
// this is fine, as the OS can deallocate the terminated program faster than we can free memory
// but tools like valgrind might report "memory leaks" as it isn't obvious this is intentional.
static DEEP_THOUGHT: LazyLock<String> = LazyLock::new(|| {
    // M3 Ultra takes about 16 million years in --release config
    another_crate::great_question()
});

// The `String` is built, stored in the `LazyLock`, and returned as `&String`.
let _ = &*DEEP_THOUGHT;
```

Initialize fields with `LazyLock`.

``` rust
use std::sync::LazyLock;

#[derive(Debug)]
struct UseCellLock {
    number: LazyLock<u32>,
}
fn main() {
    let lock: LazyLock<u32> = LazyLock::new(|| 0u32);

    let data = UseCellLock { number: lock };
    println!("{}", *data.number);
}
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#87-263)

1.80.0 (const: 1.80.0) · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#104-106)

Creates a new lazy value with the given initializing function.

##### Examples

``` rust
use std::sync::LazyLock;

let hello = "Hello, World!".to_string();

let lazy = LazyLock::new(|| hello.to_uppercase());

assert_eq!(&*lazy, "HELLO, WORLD!");
```

[Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#131-149)

🔬This is a nightly-only experimental API. (`lazy_cell_into_inner` [\#125623](https://github.com/rust-lang/rust/issues/125623))

Consumes this `LazyLock` returning the stored value.

Returns `Ok(value)` if `Lazy` is initialized and `Err(f)` otherwise.

##### Panics

Panics if the lock is poisoned.

##### Examples

``` rust
#![feature(lazy_cell_into_inner)]

use std::sync::LazyLock;

let hello = "Hello, World!".to_string();

let lazy = LazyLock::new(|| hello.to_uppercase());

assert_eq!(&*lazy, "HELLO, WORLD!");
assert_eq!(LazyLock::into_inner(lazy).ok(), Some("HELLO, WORLD!".to_string()));
```

1.94.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#177-211)

Forces the evaluation of this lazy value and returns a mutable reference to the result.

##### Panics

If the initialization closure panics (the one that is passed to the [`new()`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.new "associated function std::sync::LazyLock::new") method), the panic is propagated to the caller, and the lock becomes poisoned. This will cause all future accesses of the lock (via [`force()`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.force "associated function std::sync::LazyLock::force") or a dereference) to panic.

##### Examples

``` rust
use std::sync::LazyLock;

let mut lazy = LazyLock::new(|| 92);

let p = LazyLock::force_mut(&mut lazy);
assert_eq!(*p, 92);
*p = 44;
assert_eq!(*lazy, 44);
```

1.80.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#241-262)

Forces the evaluation of this lazy value and returns a reference to result. This is equivalent to the `Deref` impl, but is explicit.

This method will block the calling thread if another initialization routine is currently running.

##### Panics

If the initialization closure panics (the one that is passed to the [`new()`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.new "associated function std::sync::LazyLock::new") method), the panic is propagated to the caller, and the lock becomes poisoned. This will cause all future accesses of the lock (via [`force()`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.force "associated function std::sync::LazyLock::force") or a dereference) to panic.

##### Examples

``` rust
use std::sync::LazyLock;

let lazy = LazyLock::new(|| 92);

assert_eq!(LazyLock::force(&lazy), &92);
assert_eq!(&*lazy, &92);
```

[Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#265-321)

1.94.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#283-292)

Returns a mutable reference to the value if initialized. Otherwise (if uninitialized or poisoned), returns `None`.

##### Examples

``` rust
use std::sync::LazyLock;

let mut lazy = LazyLock::new(|| 92);

assert_eq!(LazyLock::get_mut(&mut lazy), None);
let _ = LazyLock::force(&lazy);
*LazyLock::get_mut(&mut lazy).unwrap() = 44;
assert_eq!(*lazy, 44);
```

1.94.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#311-320)

Returns a reference to the value if initialized. Otherwise (if uninitialized or poisoned), returns `None`.

##### Examples

``` rust
use std::sync::LazyLock;

let lazy = LazyLock::new(|| 92);

assert_eq!(LazyLock::get(&lazy), None);
let _ = LazyLock::force(&lazy);
assert_eq!(LazyLock::get(&lazy), Some(&92));
```

## Trait Implementations

1.80.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#388-397)

[Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#389-396)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

1.80.0 (const: [unstable](https://github.com/rust-lang/rust/issues/143894 "Tracking issue for const_default")) · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#379-385)

[Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#382-384)

Creates a new lazy value using `Default` as the initializing function.

1.80.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#339-359)

[Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#356-358)

Dereferences the value.

This method will block the calling thread if another initialization routine is currently running.

##### Panics

If the initialization closure panics (the one that is passed to the [`new()`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.new "associated function std::sync::LazyLock::new") method), the panic is propagated to the caller, and the lock becomes poisoned. This will cause all future accesses of the lock (via [`force()`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.force "associated function std::sync::LazyLock::force") or a dereference) to panic.

[Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#340)

The resulting type after dereferencing.

1.89.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#362-375)

[Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#372-374)

##### Panics

If the initialization closure panics (the one that is passed to the [`new()`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.new "associated function std::sync::LazyLock::new") method), the panic is propagated to the caller, and the lock becomes poisoned. This will cause all future accesses of the lock (via [`force()`](https://doc.rust-lang.org/std/sync/struct.LazyLock.html#method.force "associated function std::sync::LazyLock::force") or a dereference) to panic.

1.80.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#324-336)

[Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#325-335)

Executes the destructor for this type. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop)

[Source](https://doc.rust-lang.org/src/core/ops/drop.rs.html#267)

🔬This is a nightly-only experimental API. (`pin_ergonomics` [\#130494](https://github.com/rust-lang/rust/issues/130494))

Execute the destructor for this type, but different to [`Drop::drop`](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.drop "method std::ops::Drop::drop"), it requires `self` to be pinned. [Read more](https://doc.rust-lang.org/std/ops/trait.Drop.html#method.pin_drop)

1.96.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#400-410)

[Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#404-409)

Constructs a `LazyLock` that starts already initialized with the provided value.

1.80.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#425)

1.80.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#421)

1.80.0 · [Source](https://doc.rust-lang.org/src/std/sync/lazy_lock.rs.html#427)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#804)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#805)

Converts to this type from the input type.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/ops/deref.rs.html#379-381)

[Source](https://doc.rust-lang.org/src/core/ops/deref.rs.html#383)

🔬This is a nightly-only experimental API. (`arbitrary_self_types` [\#44874](https://github.com/rust-lang/rust/issues/44874))

The target type on which the method may be called.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.