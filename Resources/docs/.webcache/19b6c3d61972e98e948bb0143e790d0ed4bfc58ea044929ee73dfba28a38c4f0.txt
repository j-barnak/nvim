[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[windows](https://doc.rust-lang.org/std/os/windows/index.html)::[net](https://doc.rust-lang.org/std/os/windows/net/index.html)

# Struct UnixListener Copy item path

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#47)

``` rust
pub struct UnixListener(/* private fields */);
```

🔬This is a nightly-only experimental API. (`windows_unix_domain_sockets` [\#150487](https://github.com/rust-lang/rust/issues/150487))

Available on **Windows** only.

Expand description

A structure representing a Unix domain socket server.

Under Windows, it will only work starting from Windows 10 17063.

## Examples

``` rust
#![feature(windows_unix_domain_sockets)]
use std::thread;
use std::os::windows::net::{UnixStream, UnixListener};

fn handle_client(stream: UnixStream) {
    // ...
}

fn main() -> std::io::Result<()> {
    let listener = UnixListener::bind("/path/to/the/socket")?;

    // accept connections and process them, spawning a new thread for each one
    for stream in listener.incoming() {
        match stream {
            Ok(stream) => {
                /* connection succeeded */
                thread::spawn(|| handle_client(stream));
            }
            Err(err) => {
                /* connection failed */
                break;
            }
        }
    }
    Ok(())
}
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#59-267)

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#76-79)

🔬This is a nightly-only experimental API. (`windows_unix_domain_sockets` [\#150487](https://github.com/rust-lang/rust/issues/150487))

Creates a new `UnixListener` bound to the specified socket.

##### Examples

``` rust
#![feature(windows_unix_domain_sockets)]
use std::os::windows::net::UnixListener;

let listener = match UnixListener::bind("/path/to/the/socket") {
    Ok(sock) => sock,
    Err(e) => {
        println!("Couldn't connect: {e:?}");
        return
    }
};
```

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#105-113)

🔬This is a nightly-only experimental API. (`windows_unix_domain_sockets` [\#150487](https://github.com/rust-lang/rust/issues/150487))

Creates a new `UnixListener` bound to the specified [`socket address`](https://doc.rust-lang.org/std/os/windows/net/struct.SocketAddr.html "struct std::os::windows::net::SocketAddr").

##### Examples

``` rust
#![feature(windows_unix_domain_sockets)]
use std::os::windows::net::{UnixListener};

fn main() -> std::io::Result<()> {
    let listener1 = UnixListener::bind("path/to/socket")?;
    let addr = listener1.local_addr()?;

    let listener2 = match UnixListener::bind_addr(&addr) {
        Ok(sock) => sock,
        Err(err) => {
            println!("Couldn't bind: {err:?}");
            return Err(err);
        }
    };
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#139-145)

🔬This is a nightly-only experimental API. (`windows_unix_domain_sockets` [\#150487](https://github.com/rust-lang/rust/issues/150487))

Accepts a new incoming connection to this listener.

This function will block the calling thread until a new Unix connection is established. When established, the corresponding [`UnixStream`](https://doc.rust-lang.org/std/os/windows/net/struct.UnixStream.html "struct std::os::windows::net::UnixStream") and the remote peer’s address will be returned.

##### Examples

``` rust
#![feature(windows_unix_domain_sockets)]
use std::os::windows::net::UnixListener;

fn main() -> std::io::Result<()> {
    let listener = UnixListener::bind("/path/to/the/socket")?;

    match listener.accept() {
        Ok((socket, addr)) => println!("Got a client: {addr:?}"),
        Err(e) => println!("accept function failed: {e:?}"),
    }
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#161-163)

🔬This is a nightly-only experimental API. (`windows_unix_domain_sockets` [\#150487](https://github.com/rust-lang/rust/issues/150487))

Returns the local socket address of this listener.

##### Examples

``` rust
#![feature(windows_unix_domain_sockets)]
use std::os::windows::net::UnixListener;

fn main() -> std::io::Result<()> {
    let listener = UnixListener::bind("/path/to/the/socket")?;
    let addr = listener.local_addr().expect("Couldn't get local address");
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#183-185)

🔬This is a nightly-only experimental API. (`windows_unix_domain_sockets` [\#150487](https://github.com/rust-lang/rust/issues/150487))

Creates a new independently owned handle to the underlying socket.

The returned `UnixListener` is a reference to the same socket that this object references. Both handles can be used to accept incoming connections and options set on one listener will affect the other.

##### Examples

``` rust
#![feature(windows_unix_domain_sockets)]
use std::os::windows::net::UnixListener;

fn main() -> std::io::Result<()> {
    let listener = UnixListener::bind("/path/to/the/socket")?;
    let listener_copy = listener.try_clone().expect("try_clone failed");
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#207-209)

🔬This is a nightly-only experimental API. (`windows_unix_domain_sockets` [\#150487](https://github.com/rust-lang/rust/issues/150487))

Moves the socket into or out of nonblocking mode.

This will result in the `accept` operation becoming nonblocking, i.e., immediately returning from their calls. If the IO operation is successful, `Ok` is returned and no further action is required. If the IO operation could not be completed and needs to be retried, an error with kind [`io::ErrorKind::WouldBlock`](https://doc.rust-lang.org/std/io/enum.ErrorKind.html#variant.WouldBlock "variant std::io::ErrorKind::WouldBlock") is returned.

##### Examples

``` rust
#![feature(windows_unix_domain_sockets)]
use std::os::windows::net::UnixListener;

fn main() -> std::io::Result<()> {
    let listener = UnixListener::bind("/path/to/the/socket")?;
    listener.set_nonblocking(true).expect("Couldn't set non blocking");
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#228-230)

🔬This is a nightly-only experimental API. (`windows_unix_domain_sockets` [\#150487](https://github.com/rust-lang/rust/issues/150487))

Returns the value of the `SO_ERROR` option.

##### Examples

``` rust
#![feature(windows_unix_domain_sockets)]
use std::os::windows::net::UnixListener;

fn main() -> std::io::Result<()> {
    let listener = UnixListener::bind("/tmp/sock")?;

    if let Ok(Some(err)) = listener.take_error() {
        println!("Got error: {err:?}");
    }
    Ok(())
}
```

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#264-266)

🔬This is a nightly-only experimental API. (`windows_unix_domain_sockets` [\#150487](https://github.com/rust-lang/rust/issues/150487))

Returns an iterator over incoming connections.

The iterator will never return [`None`](https://doc.rust-lang.org/std/option/enum.Option.html#variant.None "variant std::option::Option::None") and will also not yield the peer’s [`SocketAddr`](https://doc.rust-lang.org/std/os/windows/net/struct.SocketAddr.html "struct std::os::windows::net::SocketAddr") structure.

##### Examples

``` rust
#![feature(windows_unix_domain_sockets)]
use std::thread;
use std::os::windows::net::{UnixStream, UnixListener};

fn handle_client(stream: UnixStream) {
    // ...
}

fn main() -> std::io::Result<()> {
    let listener = UnixListener::bind("/path/to/the/socket")?;

    for stream in listener.incoming() {
        match stream {
            Ok(stream) => {
                thread::spawn(|| handle_client(stream));
            }
            Err(err) => {
                break;
            }
        }
    }
    Ok(())
}
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#316-321)

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#318-320)

Extracts the raw socket. [Read more](https://doc.rust-lang.org/std/os/windows/io/trait.AsRawSocket.html#tymethod.as_raw_socket)

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#49-58)

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#50-57)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#323-328)

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#325-327)

Constructs a new I/O object from the specified raw socket. [Read more](https://doc.rust-lang.org/std/os/windows/io/trait.FromRawSocket.html#tymethod.from_raw_socket)

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#337-344)

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#338)

The type of the elements being iterated over.

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#339)

Which kind of iterator are we turning this into?

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#341-343)

Creates an iterator from a value. [Read more](https://doc.rust-lang.org/std/iter/trait.IntoIterator.html#tymethod.into_iter)

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#330-335)

[Source](https://doc.rust-lang.org/src/std/os/windows/net/listener.rs.html#332-334)

Consumes this object, returning the raw underlying socket. [Read more](https://doc.rust-lang.org/std/os/windows/io/trait.IntoRawSocket.html#tymethod.into_raw_socket)

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.