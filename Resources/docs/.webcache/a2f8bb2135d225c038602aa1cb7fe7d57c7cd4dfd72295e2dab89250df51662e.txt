[std](https://doc.rust-lang.org/std/index.html)::[marker](https://doc.rust-lang.org/std/marker/index.html)

# Trait Destruct Copy item path

[Source](https://doc.rust-lang.org/src/core/marker.rs.html#1063)

``` rust
pub trait Destruct { }
```

🔬This is a nightly-only experimental API. (`const_destruct` [\#133214](https://github.com/rust-lang/rust/issues/133214))

Expand description

A marker for types that can be dropped.

This should be used for `[const]` bounds, as non-const bounds will always hold for every type.

## Dyn Compatibility

This trait is **not** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors