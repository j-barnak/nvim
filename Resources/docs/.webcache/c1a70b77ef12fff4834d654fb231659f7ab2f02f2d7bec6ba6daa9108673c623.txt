# In-Depth: `afl-fuzz` initialization and runtime processes

This in-depth section walks the real `afl-fuzz` code path in two parts, split into the next two chapters:

- **Initializing `afl-fuzz`** — everything from process start to the fuzzing loop.
- **Runtime of `afl-fuzz`** — the mutation/execution/feedback loop itself.
