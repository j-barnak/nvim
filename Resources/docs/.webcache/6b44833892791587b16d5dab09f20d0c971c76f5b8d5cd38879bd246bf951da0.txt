# Bypassing Kernel Barriers: Fuzzing Linux Kernel in Userspace with LKL

*Eugene Rodionov & Xuan Xing (Google) - Linux Security Summit North America 2025. Slides polished to Markdown; navigate slides with `<leader>fs`. Source: https://static.sched.com/hosted_files/lssna2025/01/*

## Slide 1 - Bypassing Kernel Barriers

Fuzzing Linux Kernel in Userspace with LKL

Eugene Rodionov, Xuan Xing

## Slide 2 - A conversation happened long time ago…

- My manager:
  - Maybe we should fuzz the Linux HID driver?
  - Any idea how to do it?
- <if I were an experienced kernel hacker>:
  - syzkaller?
- Actual me:
  - clang -fsanitize=fuzzer drivers/hid/hid-core.c kernel_stubs.c …..
- Clang:
  - fatal error: too many errors emitted, stopping now [-ferror-limit=]
- Matt Alexander (former Googler/RedTeamer):
  - Oh, you want to run kernel code in userspace? try LKL!

## Slide 3 - Agenda

- Introduction
- LKL Basics
- Fuzzing on LKL
- LKL Fuzzers
- Quick Demo
- Conclusion
- Q&A

## Slide 4 - Introduction

Proprietary + Confidential

## Slide 5 - Who are we?

Protect the Android ecosystem through offensive security
- Offensive Security Reviews to verify (break) security assumptions
- Scale through tool development (e.g. continuous fuzzing)
- Develop proof of concepts to demonstrate real-world impact
- Assess the efficacy of security mitigations

## Slide 6 - But what is LKL?

Proprietary + Confidential

## Slide 7 - LKL Application

Shared memory
Threads
with LKL
LKL Overview
LKL syscall API
- Linux Kernel Library (LKL)1 builds Linux kernel as a
user-space library                                                                                Linux kernel

  - Implemented as Linux arch-port                                                Generic        VFS         Binder
  - LKL vs UML                                                                    LKL arch
NET           ...
- LKL building blocks
  - Host environment API -- portability layer                                       LKL Host environment API

  - Linux kernel code                                                                 POSIX                 Win32

  - LKL syscall API exposed to the user-space application
user space
- Run kernel code without launching a VM
kernel space
  - Kernel unit testing
Host kernel
  - Fuzzing!2,3
[1] https://github.com/lkl/linux                                                             Linux         Android       Windows
[2] Xu et al., Fuzzing File Systems via Two-Dimensional Input Space Exploration
[3] https://github.com/atrosinenko/kbdysch

## Slide 8 - Using LKL from your C program

int ret = lkl_start_kernel(&lkl_host_ops, "mem=50M");

lkl_mount_fs("sysfs");
lkl_mount_fs("proc");
lkl_mount_fs("dev");

int binder_fd = lkl_sys_open("/dev/binder", O_RDWR | O_CLOEXEC, 0);
void *binder_map = lkl_sys_mmap(NULL, BINDER_VM_SIZE,
PROT_READ, MAP_PRIVATE, binder_fd, 0);

struct lkl_binder_version version = { 0 };
ret = lkl_sys_ioctl(binder_fd, LKL_BINDER_VERSION, &version);

## Slide 9 - Using LKL from your C program

int ret = lkl_start_kernel(&lkl_host_ops, "mem=50M");

lkl_mount_fs("sysfs");
lkl_mount_fs("proc");
lkl_mount_fs("dev");

int binder_fd = lkl_sys_open("/dev/binder", O_RDWR | O_CLOEXEC, 0);
void *binder_map = lkl_sys_mmap(NULL, BINDER_VM_SIZE,
PROT_READ, MAP_PRIVATE, binder_fd, 0);

struct lkl_binder_version version = { 0 };
ret = lkl_sys_ioctl(binder_fd, LKL_BINDER_VERSION, &version);

## Slide 10 - Using LKL from your C program

int ret = lkl_start_kernel(&lkl_host_ops, "mem=50M");

lkl_mount_fs("sysfs");
lkl_mount_fs("proc");
lkl_mount_fs("dev");

int binder_fd = lkl_sys_open("/dev/binder", O_RDWR | O_CLOEXEC, 0);
void *binder_map = lkl_sys_mmap(NULL, BINDER_VM_SIZE,
PROT_READ, MAP_PRIVATE, binder_fd, 0);

struct lkl_binder_version version = { 0 };
ret = lkl_sys_ioctl(binder_fd, LKL_BINDER_VERSION, &version);

## Slide 11 - Using LKL from your C program

int ret = lkl_start_kernel(&lkl_host_ops, "mem=50M");

lkl_mount_fs("sysfs");
lkl_mount_fs("proc");
lkl_mount_fs("dev");

int binder_fd = lkl_sys_open("/dev/binder", O_RDWR | O_CLOEXEC, 0);
void *binder_map = lkl_sys_mmap(NULL, BINDER_VM_SIZE,
PROT_READ, MAP_PRIVATE, binder_fd, 0);

struct lkl_binder_version version = { 0 };
ret = lkl_sys_ioctl(binder_fd, LKL_BINDER_VERSION, &version);

## Slide 12 - LKL Quirks

- MMU implementation
- Inspired by MMU implementation in UML
- Implements 3-level page tables & flat memory model
- Uses the host's mmap to map/unmap LKL's physical pages at
provided virtual addresses
- Emulates multiple processes
- Implements custom new_thread_group_leader syscall to
modify tgid

## Slide 13 - Applications

- Built-in applications
  - fs2tar: converting file system image to a tar archive
  - cptofs: copies files to/from a file system image
  - lklfuse: mounting filesystem images in userspace, without root privileges
- Kernel fuzzing
  - kBdysch: A collection of kernel fuzzers in userspace
  - Janus: Fuzzing file system using lkl
  - Sample fuzzers: Sample fuzzers developed by Android Red Team (HID, binder)
- Other interesting ideas
  - LKL.js: running Linux kernel on JavaScript directly
  - User Space TCP: a full TCP stack in userspace for better security

## Slide 14 - Enabling Fuzzing on LKL

Proprietary + Confidential

## Slide 15 - Anatomy of LKL fuzzer

LKL enables fuzzing Linux kernel code in                                GNU Linux x86_64 user-space process
user-space
- Use in-process fuzzing engine, such as libFuzzer                               Fuzzing coverage
& crash detection
Advantages
- High fuzzing performance on x86_64
- Ease of custom modifications                            Linux kernel + KASan
  - e.g. mocking hardware, custom scheduler(?)                                                   libFuzzer-based
Binder                               fuzzing harness
device driver
Limitations
- No SMP in LKL
- x86_64 vs aarch64 -- potential false positives, false
Fuzz data
negatives

## Slide 16 - LKL fuzzing limitations

- No SMP support
  - Not easy to test certain concurrency scenarios
- Challenges with coverage build
  - Large binary size
  - Taking too long to build
- Only support x86-64 arch
  - Limited AArch/AArch64 support
  - Drivers specific to non-x86-64 architecture can’t be fuzzed
- Need to mock lower level interfaces
  - Mocking hardware support
  - Using virtio backend
- Not in linux main branch (yet)
  - Requiring effort to be upstreamed

## Slide 17 - LKL Fuzzers

Proprietary + Confidential

## Slide 18 - LKL fuzzers

- Human Interface Device (HID) fuzzer
  - A sample LKL fuzzer targeting HID driver

- Binder fuzzer
  - Targeting Android Binder driver

- Virtio driver fuzzing
  - Used by Android pKVM with special attacking scenario (malicious host against guest VM)
  - Virtio based device drivers

## Slide 19 - Human Interface Device (HID) fuzzing

- Target:
  - Linux HID system                                    user                   hid-fuzzer
  - Complex spec (hid1_11.pdf, hut1_5.pdf)
- Attacking scenario:                                      kernel              Input Subsystem

  - Physical access (juice jacking)                                            HID Core
- Details                                                                               Userspace HID
…
  - Inspired by kBdysch fuzzing project                                                 (UHID)

  - A proof of concept for LKL fuzzing support development
USB     PS/2
- Issues discovered and verified with USB simulation                  Stack   Stack

  - Two bulletin class issues (CVE-2020-0465)
  - 6 stability issues                                  hardware

## Slide 20 - Human Interface Device (HID) fuzzing (cont.)

Host PC                                    Target Device

Test input

kernel                  Input Subsystem
FaceDancer python application
HID Core

Userspace HID
…
(UHID)

USB          PS/2
Stack        Stack

hardware

## Slide 21 - Exploitability of HID bugs

- We didn’t attempt to exploit identified issues in HID driver
- However, …

https://i.blackhat.com/EU-21/Thursday/EU-21-Bogaard-Geist-Achieving-Linux-Kernel-Code-Execution-Through-A-Malicious-USB-Device.pdf

## Slide 22 - DEMO: USB HID Driver issue discovery

Proprietary + Confidential

## Slide 23 - Android Binder Driver Fuzzing

Proprietary + Confidential

## Slide 24 - What is Binder?

- Primary inter-process communication (IPC) channel on Android
- Support passing file descriptors, objects containing pointers, etc.
- Composed of a userspace library (libbinder) and a kernel driver (/dev/binder)
- Provide Remote Procedure Call (RPC) framework for Java and C/C++

## Slide 25 - Binder Threat Model

App               RPC                                 RPC
System Services
System Services               Untrusted App
libbinder                          System Service
(Binder userspace library)

Userspace         ioctl                               ioctl
ioctl                      ioctl

Kernel

/dev/binder (Binder driver)

## Slide 26 - object

Binder Fuzzing Challenges                                      scatter
x_ptr      y_ptr

x

Data dependencies                                struct object {                                                                       y
char *x_ptr;
char *y_ptr;
- Binder commands                              };                                    struct                 struct                 struct

- Scatter-gather data structures                                              binder_buffer_object   binder_buffer_object   binder_buffer_object

(BINDER_TYPE_PTR)
x_ptr      y_ptr               x                      y

gather

State dependencies                                                         Client                                     Client

- Synchronous IPC
  - Cannot send transaction to oneself
call
  - Multiple outstanding transactions not allowed
return
call

## Slide 27 - Binder Fuzzing Challenges

ioctl(binder_fd, BINDER_WRITE_READ, x) // 1.
State dependencies
// y = x->read_buffer->...->buffer
- Some inputs depend on previous                   ioctl(binder_fd, BC_FREE_BUFFER, y )                 // 2.
IOCTL calls
  - e.g. Transaction buffers (BC_FREE_BUFFER)

Binder memory map

Multi-process coordination
Context Manager
- All communication requires a                                             Client
Context Manager                                  1. Send Node                               2. Request Ref

- Node & Ref setup is required to
establish a connection                                    Client                       Client

3. Connection established

## Slide 28 - LKL Binder Fuzzing Harness

Fuzz data
client_1 {
binder_write {
- Simulate IPC interactions between              binder_commands {
transaction {
multiple clients                                   binder_objects { binder { ptr: 0xbeef } }
  - 3 clients (1 Context Manager)             }
}
  - IOCTL calls and data                  }
}
client_2 {
binder_read { ... }
Fuzzing harness                               binder_write {
binder_commands { free_buffer { ... } }
Context Manager
}
Client 2                 }
client_3 { ... }
client_2 { ... }
client_3 { ... }
Client 1                     Client 3   client_1 { ... }

## Slide 29 - Randomized    Client     Client

Fuzz data                Thread     Thread
Scheduler
LKL Randomized Scheduler                                                                          ...

Deterministically simulate thread interleaving                                                  yield

based on fuzz data1
schedule   schedule

Insert yield points before/after synchronization                                                 lock

primitives
...
- spin_lock, spin_unlock
- mutex_lock, mutex_unlock                                                                         unlock
yield

schedule   schedule
lock
[1] Williamson, N., Catch Me If You Can: Deterministic Discovery of
Race Conditions with Fuzzing. Black Hat USA, (2022).

## Slide 30 - LKL Binder Fuzzing Results

- Achieved 68% line coverage
- Discovered CVE-2023-20938 & CVE-2023-21255

## Slide 31 - DEMO: Test case for CVE-2023-20938

Proprietary + Confidential

## Slide 32 - Virtio fuzzing for Android pKVM

Proprietary + Confidential

## Slide 33 - Android Protected KVM Attack Surface

Android Host                              Protected VMs                    Non-Protected VMs

Android virtualization framework (AVF)               Microdroid manager &
application                                  payload

EL0                                                                                             Application 0
Virtualization service                                                                (VM)
virtio    Guest kernel (GKI)

CrosVM              virtio
Microdroid bootloader
virtio
(u-boot)

EL1        Android host kernel (GKI)                      virtio    pvmfw (u-boot)             Arbitrary kernel

Host hypercalls        Protected KVM hypervisor                    Guest hypercalls
EL2
FF-A

EL3                                              EL3 firmware

## Slide 34 - Protected VM virtio attack surface

Android Host                                              Protected Guest VM

CrosVM VMM                       virtio_console       virtio_blk   virtio_fs     vmw_vsock_virtio     device layer

virtio_pci                        virtio_ring
(modern & legacy PCI)         (packed, split & indirect modes)   transport layer
Backend virtio drivers:
PCI, block, console, etc

PCI bus                            virtio bus             bus layer

Legend

Not included
in u-boot
Shared memory
(ring buffers)
Covered
by fuzzing

## Slide 35 - Virtio Front-end Fuzzers

Kernel under test:
virtio_blk:
- Android13-5.10
  - mutates the virtio_blk configuration block
virtio_ring:
- fuzzes ring-buffer processing functionality
- handles both split & packed mode
virtio_console   virtio_blk   virtio_fs       vmw_vsock     device layer

virtio_pci:
virtio_pci                   virtio_ring
- fuzzes PCI configuration space                  (modern & legacy PCI)       (packed, split & indirect)
transport layer

- LKL arch-specific implementation of PCI
bus                                                    PCI bus                          virtio bus         bus layer
- mock-out PCI MMIO in the fuzzer harness

## Slide 36 - Virtio_blk fuzzer finding

int block_read_full_page(struct page *page, get_block_t *get_block)
{
struct buffer_head *bh, *head, *arr[MAX_BUF_PER_PAGE];
...
do {
if (buffer_uptodate(bh))
continue;
if (!buffer_mapped(bh)) {
int err = 0;
...
}
arr[nr++] = bh;   <====== OOB write on stack
} while (i++, iblock++, (bh = bh->b_this_page) != head);
...
}

## Slide 37 - Virtio_blk fuzzer finding

- With the block size 0xe5e5e5e5:
  - `inode->i_blkbits == 32`
  - `1 << READ_ONCE(inode->i_blkbits)` is undefined behavior in C
  - `1 << READ_ONCE(inode->i_blkbits) == 1` on x86 architecture

static struct buffer_head *create_page_buffers(struct page *page, ...)
{
BUG_ON(!PageLocked(page));
if (!page_has_buffers(page))
create_empty_buffers(page, 1 << READ_ONCE(inode->i_blkbits), b_state);
return page_buffers(page);
}

## Slide 38 - Conclusions

Proprietary + Confidential

## Slide 39 - Key takeaways

- LKL can be an effective way to fuzz certain kernel scenarios
- Scenarios requiring complex setup (e.g. hardware-to-kernel attack surface)
- Dependencies to some userspace libraries
- Visual code coverage and debugging can be very useful
- It’s good to have alternatives

- The fact that you don’t know something might be your advantage
- Discovering new approaches from naive ideas
- Learning without predefined mindset

## Slide 40 - Future work

- Enable MMU (https://github.com/lkl/linux/pull/551)
- Upstream Android Binder fuzzer (https://github.com/lkl/linux/pull/564)

Creating more fuzzers and profit!

## Slide 41 - Acknowledgement

- Octavian Purdila and LKL maintainers
- https://github.com/atrosinenko/kbdysch
- Android pKVM team
- Android Binder team

## Slide 42 - Resources

- libFuzzer – a library for coverage-guided fuzz testing
- Achieving Kernel Code Execution through Malicious USB device
- Android Virtualization Framework and pKVM security
- Emulating USB device with Python
- Attacking Android Binder: Analysis and Exploitation of CVE-2023-20938
- Binder Internals

## Slide 43 - Questions?

Proprietary + Confidential
