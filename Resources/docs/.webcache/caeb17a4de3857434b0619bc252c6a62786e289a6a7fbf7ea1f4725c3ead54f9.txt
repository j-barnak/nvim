[std](https://doc.rust-lang.org/std/index.html)::[intrinsics](https://doc.rust-lang.org/std/intrinsics/index.html)::[simd](https://doc.rust-lang.org/std/intrinsics/simd/index.html)

# Function simd_reduce_mul_unordered Copy item path

[Source](https://doc.rust-lang.org/src/core/intrinsics/simd/mod.rs.html#519)

``` rust
pub unsafe fn simd_reduce_mul_unordered<T, U>(x: T) -> U
```

🔬This is a nightly-only experimental API. (`core_intrinsics`)

Expand description

Multiplies elements within a vector in arbitrary order. May also be re-associated with unordered additions on the inputs/outputs.

`T` must be a vector of integers or floats.

`U` must be the element type of `T`.