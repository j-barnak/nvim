[std](https://doc.rust-lang.org/std/index.html)::[os](https://doc.rust-lang.org/std/os/index.html)::[linux](https://doc.rust-lang.org/std/os/linux/index.html)::[process](https://doc.rust-lang.org/std/os/linux/process/index.html)

# Trait ChildExt Copy item path

[Source](https://doc.rust-lang.org/src/std/os/linux/process.rs.html#149-183)

``` rust
pub trait ChildExt {
    // Required methods
    fn pidfd(&self) -> Result<&PidFd>;
    fn into_pidfd(self) -> Result<PidFd, Self>
       where Self: Sized;
}
```

ⓘ *This trait cannot be implemented outside `std`.*

🔬This is a nightly-only experimental API. (`linux_pidfd` [\#82971](https://github.com/rust-lang/rust/issues/82971))

Available on **Linux** only.

Expand description

Os-specific extensions for [`Child`](https://doc.rust-lang.org/std/process/struct.Child.html "struct std::process::Child")

## Required Methods

[Source](https://doc.rust-lang.org/src/std/os/linux/process.rs.html#161)

🔬This is a nightly-only experimental API. (`linux_pidfd` [\#82971](https://github.com/rust-lang/rust/issues/82971))

Obtains a reference to the [`PidFd`](https://doc.rust-lang.org/std/os/linux/process/struct.PidFd.html "struct std::os::linux::process::PidFd") created for this [`Child`](https://doc.rust-lang.org/std/process/struct.Child.html "struct std::process::Child"), if available.

A pidfd will only be available if its creation was requested with [`create_pidfd`](https://doc.rust-lang.org/std/os/linux/process/trait.CommandExt.html#tymethod.create_pidfd "method std::os::linux::process::CommandExt::create_pidfd") when the corresponding [`Command`](https://doc.rust-lang.org/std/process/struct.Command.html "struct std::process::Command") was created.

Even if requested, a pidfd may not be available due to an older version of Linux being in use, or if some other error occurred.

[Source](https://doc.rust-lang.org/src/std/os/linux/process.rs.html#180-182)

🔬This is a nightly-only experimental API. (`linux_pidfd` [\#82971](https://github.com/rust-lang/rust/issues/82971))

Returns the [`PidFd`](https://doc.rust-lang.org/std/os/linux/process/trait.ChildExt.html#tymethod.pidfd "method std::os::linux::process::ChildExt::pidfd") created for this [`Child`](https://doc.rust-lang.org/std/process/struct.Child.html "struct std::process::Child"), if available. Otherwise self is returned.

A pidfd will only be available if its creation was requested with [`create_pidfd`](https://doc.rust-lang.org/std/os/linux/process/trait.CommandExt.html#tymethod.create_pidfd "method std::os::linux::process::CommandExt::create_pidfd") when the corresponding [`Command`](https://doc.rust-lang.org/std/process/struct.Command.html "struct std::process::Command") was created.

Taking ownership of the PidFd consumes the Child to avoid pid reuse races. Use [`pidfd`](https://doc.rust-lang.org/std/os/linux/process/trait.ChildExt.html#tymethod.pidfd "method std::os::linux::process::ChildExt::pidfd") and [`BorrowedFd::try_clone_to_owned`](https://doc.rust-lang.org/std/os/fd/struct.BorrowedFd.html#method.try_clone_to_owned "method std::os::fd::BorrowedFd::try_clone_to_owned") if you don’t want to disassemble the Child yet.

Even if requested, a pidfd may not be available due to an older version of Linux being in use, or if some other error occurred.

## Dyn Compatibility

This trait **is** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

[Source](https://doc.rust-lang.org/src/std/sys/process/unix/unix.rs.html#1299-1316)