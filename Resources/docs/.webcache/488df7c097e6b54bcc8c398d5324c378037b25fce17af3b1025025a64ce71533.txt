[std](https://doc.rust-lang.org/std/index.html)::[slice](https://doc.rust-lang.org/std/slice/index.html)

# Trait Concat Copy item path

[Source](https://doc.rust-lang.org/src/alloc/slice.rs.html#702)

``` rust
pub trait Concat<Item>where
    Item: ?Sized,{
    type Output;

    // Required method
    fn concat(slice: &Self) -> Self::Output;
}
```

🔬This is a nightly-only experimental API. (`slice_concat_trait` [\#27747](https://github.com/rust-lang/rust/issues/27747))

Expand description

Helper trait for [`[T]::concat`](https://doc.rust-lang.org/std/primitive.slice.html#method.concat "method slice::concat").

Note: the `Item` type parameter is not used in this trait, but it allows impls to be more generic. Without it, we get this error:

```
error[E0207]: the type parameter `T` is not constrained by the impl trait, self type, or predica
   --> library/alloc/src/slice.rs:608:6
    |
608 | impl<T: Clone, V: Borrow<[T]>> Concat for [V] {
    |      ^ unconstrained type parameter
```

This is because there could exist `V` types with multiple `Borrow<[_]>` impls, such that multiple `T` types would apply:

``` rust
pub struct Foo(Vec<u32>, Vec<String>);

impl std::borrow::Borrow<[u32]> for Foo {
    fn borrow(&self) -> &[u32] { &self.0 }
}

impl std::borrow::Borrow<[String]> for Foo {
    fn borrow(&self) -> &[String] { &self.1 }
}
```

## Required Associated Types

[Source](https://doc.rust-lang.org/src/alloc/slice.rs.html#705)

🔬This is a nightly-only experimental API. (`slice_concat_trait` [\#27747](https://github.com/rust-lang/rust/issues/27747))

The resulting type after concatenation

## Required Methods

[Source](https://doc.rust-lang.org/src/alloc/slice.rs.html#709)

🔬This is a nightly-only experimental API. (`slice_concat_trait` [\#27747](https://github.com/rust-lang/rust/issues/27747))

Implementation of [`[T]::concat`](https://doc.rust-lang.org/std/primitive.slice.html#method.concat "method slice::concat")

## Dyn Compatibility

This trait is **not** [dyn compatible](https://doc.rust-lang.org/1.98.1/reference/items/traits.html#dyn-compatibility).

*In older versions of Rust, dyn compatibility was called "object safety".*

## Implementors

[Source](https://doc.rust-lang.org/src/alloc/str.rs.html#62)

Note: `str` in `Concat<str>` is not meaningful here. This type parameter of the trait only exists to enable another impl.

[Source](https://doc.rust-lang.org/src/alloc/str.rs.html#63)

[Source](https://doc.rust-lang.org/src/alloc/slice.rs.html#726)

[Source](https://doc.rust-lang.org/src/alloc/slice.rs.html#727)