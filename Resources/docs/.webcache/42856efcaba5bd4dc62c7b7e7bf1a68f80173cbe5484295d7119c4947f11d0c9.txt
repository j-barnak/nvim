[std](https://doc.rust-lang.org/std/index.html)::[sync](https://doc.rust-lang.org/std/sync/index.html)::[nonpoison](https://doc.rust-lang.org/std/sync/nonpoison/index.html)

# Struct Condvar Copy item path

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#49-51)

``` rust
pub struct Condvar { /* private fields */ }
```

🔬This is a nightly-only experimental API. (`nonpoison_condvar` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Expand description

A Condition Variable

For more information about condition variables, check out the documentation for the poisoning variant of this type at [`poison::Condvar`](https://doc.rust-lang.org/std/sync/struct.Condvar.html "struct std::sync::Condvar").

## Examples

Note that this `Condvar` does **not** propagate information about threads that panic while holding a lock. If you need this functionality, see [`poison::Mutex`](https://doc.rust-lang.org/std/sync/struct.Mutex.html "struct std::sync::Mutex") and [`poison::Condvar`](https://doc.rust-lang.org/std/sync/struct.Condvar.html "struct std::sync::Condvar").

``` rust
#![feature(nonpoison_mutex)]
#![feature(nonpoison_condvar)]

use std::sync::nonpoison::{Mutex, Condvar};
use std::sync::Arc;
use std::thread;

let pair = Arc::new((Mutex::new(false), Condvar::new()));
let pair2 = Arc::clone(&pair);

// Inside of our lock, spawn a new thread, and then wait for it to start.
thread::spawn(move || {
    let (lock, cvar) = &*pair2;
    let mut started = lock.lock();
    *started = true;
    // We notify the condvar that the value has changed.
    cvar.notify_one();
});

// Wait for the thread to start up.
let (lock, cvar) = &*pair;
let mut started = lock.lock();
while !*started {
    cvar.wait(&mut started);
}
```

## Implementations

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#53-429)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#67-69)

🔬This is a nightly-only experimental API. (`nonpoison_condvar` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Creates a new condition variable which is ready to be waited on and notified.

##### Examples

``` rust
use std::sync::Condvar;

let condvar = Condvar::new();
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#123-128)

🔬This is a nightly-only experimental API. (`nonpoison_condvar` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Blocks the current thread until this condition variable receives a notification.

This function will atomically unlock the mutex specified (represented by `guard`) and block the current thread. This means that any calls to [`notify_one`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.notify_one "method std::sync::nonpoison::Condvar::notify_one") or [`notify_all`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.notify_all "method std::sync::nonpoison::Condvar::notify_all") which happen logically after the mutex is unlocked are candidates to wake this thread up. When this function call returns, the lock specified will have been re-acquired.

Note that this function is susceptible to spurious wakeups. Condition variables normally have a boolean predicate associated with them, and the predicate must always be checked each time this function returns to protect against spurious wakeups.

##### Panics

This function may [`panic!`](https://doc.rust-lang.org/std/macro.panic.html "macro std::panic") if it is used with more than one mutex over time.

##### Examples

``` rust
#![feature(nonpoison_mutex)]
#![feature(nonpoison_condvar)]

use std::sync::nonpoison::{Mutex, Condvar};
use std::sync::Arc;
use std::thread;

let pair = Arc::new((Mutex::new(false), Condvar::new()));
let pair2 = Arc::clone(&pair);

thread::spawn(move || {
    let (lock, cvar) = &*pair2;
    let mut started = lock.lock();
    *started = true;
    // We notify the condvar that the value has changed.
    cvar.notify_one();
});

// Wait for the thread to start up.
let (lock, cvar) = &*pair;
let mut started = lock.lock();
// As long as the value inside the `Mutex<bool>` is `false`, we wait.
while !*started {
    cvar.wait(&mut started);
}
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#174-181)

🔬This is a nightly-only experimental API. (`nonpoison_condvar` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Blocks the current thread until the provided condition becomes false.

`condition` is checked immediately; if not met (returns `true`), this will [`wait`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.wait "method std::sync::nonpoison::Condvar::wait") for the next notification then check again. This repeats until `condition` returns `false`, in which case this function returns.

This function will atomically unlock the mutex specified (represented by `guard`) and block the current thread. This means that any calls to [`notify_one`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.notify_one "method std::sync::nonpoison::Condvar::notify_one") or [`notify_all`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.notify_all "method std::sync::nonpoison::Condvar::notify_all") which happen logically after the mutex is unlocked are candidates to wake this thread up. When this function call returns, the lock specified will have been re-acquired.

##### Examples

``` rust
#![feature(nonpoison_mutex)]
#![feature(nonpoison_condvar)]

use std::sync::nonpoison::{Mutex, Condvar};
use std::sync::Arc;
use std::thread;

let pair = Arc::new((Mutex::new(true), Condvar::new()));
let pair2 = Arc::clone(&pair);

thread::spawn(move || {
    let (lock, cvar) = &*pair2;
    let mut pending = lock.lock();
    *pending = false;
    // We notify the condvar that the value has changed.
    cvar.notify_one();
});

// Wait for the thread to start up.
let (lock, cvar) = &*pair;
// As long as the value inside the `Mutex<bool>` is `true`, we wait.
let mut guard = lock.lock();
cvar.wait_while(&mut guard, |pending| { *pending });
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#247-257)

🔬This is a nightly-only experimental API. (`nonpoison_condvar` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Waits on this condition variable for a notification, timing out after a specified duration.

The semantics of this function are equivalent to [`wait`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.wait "method std::sync::nonpoison::Condvar::wait") except that the thread will be blocked for roughly no longer than `dur`. This method should not be used for precise timing due to anomalies such as preemption or platform differences that might not cause the maximum amount of time waited to be precisely `dur`.

Note that the best effort is made to ensure that the time waited is measured with a monotonic clock, and not affected by the changes made to the system time. This function is susceptible to spurious wakeups. Condition variables normally have a boolean predicate associated with them, and the predicate must always be checked each time this function returns to protect against spurious wakeups. Furthermore, since the timeout is given relative to the moment this function is called, it needs to be adjusted when this function is called in a loop. The [`wait_timeout_while`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.wait_timeout_while "method std::sync::nonpoison::Condvar::wait_timeout_while") method lets you wait with a timeout while a predicate is true, taking care of all these concerns.

The returned [`WaitTimeoutResult`](https://doc.rust-lang.org/std/sync/struct.WaitTimeoutResult.html "struct std::sync::WaitTimeoutResult") value indicates if the timeout is known to have elapsed.

Like [`wait`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.wait "method std::sync::nonpoison::Condvar::wait"), the lock specified will have been re-acquired when this function returns, regardless of whether the timeout elapsed or not.

##### Examples

``` rust
#![feature(nonpoison_mutex)]
#![feature(nonpoison_condvar)]

use std::sync::nonpoison::{Mutex, Condvar};
use std::sync::Arc;
use std::thread;
use std::time::Duration;

let pair = Arc::new((Mutex::new(false), Condvar::new()));
let pair2 = Arc::clone(&pair);

thread::spawn(move || {
    let (lock, cvar) = &*pair2;
    let mut started = lock.lock();
    *started = true;
    // We notify the condvar that the value has changed.
    cvar.notify_one();
});

// wait for the thread to start up
let (lock, cvar) = &*pair;
let mut started = lock.lock();
// as long as the value inside the `Mutex<bool>` is `false`, we wait
loop {
    let result = cvar.wait_timeout(&mut started, Duration::from_millis(10));
    // 10 milliseconds have passed, or maybe the value changed!
    if *started == true {
        // We received the notification and the value has been updated, we can leave.
        break
    }
}
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#317-338)

🔬This is a nightly-only experimental API. (`nonpoison_condvar` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Waits on this condition variable for a notification, timing out after a specified duration.

The semantics of this function are equivalent to [`wait_while`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.wait_while "method std::sync::nonpoison::Condvar::wait_while") except that the thread will be blocked for roughly no longer than `dur`. This method should not be used for precise timing due to anomalies such as preemption or platform differences that might not cause the maximum amount of time waited to be precisely `dur`.

Note that the best effort is made to ensure that the time waited is measured with a monotonic clock, and not affected by the changes made to the system time.

The returned [`WaitTimeoutResult`](https://doc.rust-lang.org/std/sync/struct.WaitTimeoutResult.html "struct std::sync::WaitTimeoutResult") value indicates if the timeout is known to have elapsed without the condition being met.

Like [`wait_while`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.wait_while "method std::sync::nonpoison::Condvar::wait_while"), the lock specified will have been re-acquired when this function returns, regardless of whether the timeout elapsed or not.

##### Examples

``` rust
#![feature(nonpoison_mutex)]
#![feature(nonpoison_condvar)]

use std::sync::nonpoison::{Mutex, Condvar};
use std::sync::Arc;
use std::thread;
use std::time::Duration;

let pair = Arc::new((Mutex::new(true), Condvar::new()));
let pair2 = Arc::clone(&pair);

thread::spawn(move || {
    let (lock, cvar) = &*pair2;
    let mut pending = lock.lock();
    *pending = false;
    // We notify the condvar that the value has changed.
    cvar.notify_one();
});

// wait for the thread to start up
let (lock, cvar) = &*pair;
let mut guard = lock.lock();
let result = cvar.wait_timeout_while(
    &mut guard,
    Duration::from_millis(100),
    |&mut pending| pending,
);
if result.timed_out() {
    // timed-out without the condition ever evaluating to false.
}
// access the locked mutex via guard
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#382-384)

🔬This is a nightly-only experimental API. (`nonpoison_condvar` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Wakes up one blocked thread on this condvar.

If there is a blocked thread on this condition variable, then it will be woken up from its call to [`wait`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.wait "method std::sync::nonpoison::Condvar::wait") or [`wait_timeout`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.wait_timeout "method std::sync::nonpoison::Condvar::wait_timeout"). Calls to `notify_one` are not buffered in any way.

To wake up all threads, see [`notify_all`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.notify_all "method std::sync::nonpoison::Condvar::notify_all").

##### Examples

``` rust
#![feature(nonpoison_mutex)]
#![feature(nonpoison_condvar)]

use std::sync::nonpoison::{Mutex, Condvar};
use std::sync::Arc;
use std::thread;

let pair = Arc::new((Mutex::new(false), Condvar::new()));
let pair2 = Arc::clone(&pair);

thread::spawn(move || {
    let (lock, cvar) = &*pair2;
    let mut started = lock.lock();
    *started = true;
    // We notify the condvar that the value has changed.
    cvar.notify_one();
});

// Wait for the thread to start up.
let (lock, cvar) = &*pair;
let mut started = lock.lock();
// As long as the value inside the `Mutex<bool>` is `false`, we wait.
while !*started {
    cvar.wait(&mut started);
}
```

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#426-428)

🔬This is a nightly-only experimental API. (`nonpoison_condvar` [\#134645](https://github.com/rust-lang/rust/issues/134645))

Wakes up all blocked threads on this condvar.

This method will ensure that any current waiters on the condition variable are awoken. Calls to `notify_all()` are not buffered in any way.

To wake up only one thread, see [`notify_one`](https://doc.rust-lang.org/std/sync/nonpoison/struct.Condvar.html#method.notify_one "method std::sync::nonpoison::Condvar::notify_one").

##### Examples

``` rust
#![feature(nonpoison_mutex)]
#![feature(nonpoison_condvar)]

use std::sync::nonpoison::{Mutex, Condvar};
use std::sync::Arc;
use std::thread;

let pair = Arc::new((Mutex::new(false), Condvar::new()));
let pair2 = Arc::clone(&pair);

thread::spawn(move || {
    let (lock, cvar) = &*pair2;
    let mut started = lock.lock();
    *started = true;
    // We notify the condvar that the value has changed.
    cvar.notify_all();
});

// Wait for the thread to start up.
let (lock, cvar) = &*pair;
let mut started = lock.lock();
// As long as the value inside the `Mutex<bool>` is `false`, we wait.
while !*started {
    cvar.wait(&mut started);
}
```

## Trait Implementations

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#432-436)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#433-435)

Formats the value using the given formatter. [Read more](https://doc.rust-lang.org/std/fmt/trait.Debug.html#tymethod.fmt)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#439-444)

[Source](https://doc.rust-lang.org/src/std/sync/nonpoison/condvar.rs.html#441-443)

Creates a `Condvar` which is ready to be waited on and notified.

## Auto Trait Implementations

## Blanket Implementations

[Source](https://doc.rust-lang.org/src/core/any.rs.html#141)

[Source](https://doc.rust-lang.org/src/core/any.rs.html#142)

Gets the `TypeId` of `self`. [Read more](https://doc.rust-lang.org/std/any/trait.Any.html#tymethod.type_id)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#212)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#214)

Immutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.Borrow.html#tymethod.borrow)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#221)

[Source](https://doc.rust-lang.org/src/core/borrow.rs.html#222)

Mutably borrows from an owned value. [Read more](https://doc.rust-lang.org/std/borrow/trait.BorrowMut.html#tymethod.borrow_mut)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#787)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#790)

Returns the argument unchanged.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#769-771)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#779)

Calls `U::from(self)`.

That is, this conversion is whatever the implementation of [`From`](https://doc.rust-lang.org/std/convert/trait.From.html "trait std::convert::From")`<T> for U` chooses to do.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#829-831)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#833)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#836)

Performs the conversion.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#813-815)

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#817)

The type returned in the event of a conversion error.

[Source](https://doc.rust-lang.org/src/core/convert/mod.rs.html#820)

Performs the conversion.