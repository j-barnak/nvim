{#ftp}

# Ftp

```cpp
#include <Ftp.hpp>
```

```cpp
class Ftp
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:51

A FTP client.

> Deprecated: Use `[sf::Sftp](sf-Sftp.md#sftp)` if possible.

`[sf::Ftp](#ftp)` is a very simple FTP client that allows you to communicate with a FTP server. The FTP protocol allows you to manipulate a remote file system (list files, upload, download, create, remove, ...).

Using the FTP client consists of 4 parts: 

* Connecting to the FTP server 
* Logging in (either as a registered user or anonymously) 
* Sending commands to the server 
* Disconnecting (this part can be done implicitly by the destructor)

Every command returns a FTP response, which contains the status code as well as a message from the server. Some commands such as `[getWorkingDirectory()](#getworkingdirectory)` and `[getDirectoryListing()](#getdirectorylisting)` return additional data, and use a class derived from `[sf::Ftp::Response](sf-Ftp-Response.md#response)` to provide this data. The most often used commands are directly provided as member functions, but it is also possible to use specific commands with the `[sendCommand()](#sendcommand)` function.

Note that response statuses >= 1000 are not part of the FTP standard, they are generated by SFML when an internal error occurs.

All commands, especially upload and download, may take some time to complete. This is important to know if you don't want to block your application while the server is completing the task.

Usage example: 
```cpp
// Create a new FTP client
sf::Ftp ftp;

// Get the address of the server
const auto addresses = sf::Dns::resolve("ftp.myserver.com");

// Check if we have a valid address
// Robust code should additionally ensure that the address is IPv4
// since sf::Ftp does not support connecting to IPv6 addresses
if (!addresses.has_value() || addresses->empty())
{
    // Handle no valid address...
}

// Connect to the server
sf::Ftp::Response response = ftp.connect(addresses->front());
if (response.isOk())
    std::cout << "Connected" << std::endl;

// Log in
response = ftp.login("laurent", "dF6Zm89D");
if (response.isOk())
    std::cout << "Logged in" << std::endl;

// Print the working directory
sf::Ftp::DirectoryResponse directory = ftp.getWorkingDirectory();
if (directory.isOk())
    std::cout << "Working directory: " << directory.getDirectory() << std::endl;

// Create a new directory
response = ftp.createDirectory("files");
if (response.isOk())
    std::cout << "Created new directory" << std::endl;

// Upload a file to this new directory
response = ftp.upload("local-path/file.txt", "files", sf::Ftp::TransferMode::Ascii);
if (response.isOk())
    std::cout << "File uploaded" << std::endl;

// Send specific commands (here: FEAT to list supported FTP features)
response = ftp.sendCommand("FEAT");
if (response.isOk())
    std::cout << "Feature list:\n" << response.getMessage() << std::endl;

// Disconnect from the server (optional)
ftp.disconnect();
```

## Friends

| Name | Description |
|------|-------------|
| [`DataChannel`](#datachannel)  |  |

---

{#datachannel}

### DataChannel

```cpp
friend class DataChannel
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:549

## Public Methods

| Return | Name | Description |
|--------|------|-------------|
|  | [`Ftp`](#ftp-1)  | Default constructor. |
|  | [`~Ftp`](#ftp-2)  | Destructor. |
|  | [`Ftp`](#ftp-3)  | Deleted copy constructor. |
| [`Ftp`](#ftp) & | [`operator=`](#operator-26)  | Deleted copy assignment. |
| [`Response`](sf-Ftp-Response.md#response) | [`connect`](#connect) `nodiscard` | Connect to the specified FTP server. |
| [`Response`](sf-Ftp-Response.md#response) | [`disconnect`](#disconnect) `nodiscard` | Close the connection with the server. |
| [`Response`](sf-Ftp-Response.md#response) | [`login`](#login) `nodiscard` | Log in using an anonymous account. |
| [`Response`](sf-Ftp-Response.md#response) | [`login`](#login-1) `nodiscard` | Log in using a username and a password. |
| [`Response`](sf-Ftp-Response.md#response) | [`keepAlive`](#keepalive) `nodiscard` | Send a null command to keep the connection alive. |
| [`DirectoryResponse`](sf-Ftp-DirectoryResponse.md#directoryresponse) | [`getWorkingDirectory`](#getworkingdirectory) `nodiscard` | Get the current working directory. |
| [`ListingResponse`](sf-Ftp-ListingResponse.md#listingresponse) | [`getDirectoryListing`](#getdirectorylisting) `nodiscard` | Get the contents of the given directory. |
| [`Response`](sf-Ftp-Response.md#response) | [`changeDirectory`](#changedirectory) `nodiscard` | Change the current working directory. |
| [`Response`](sf-Ftp-Response.md#response) | [`parentDirectory`](#parentdirectory) `nodiscard` | Go to the parent directory of the current one. |
| [`Response`](sf-Ftp-Response.md#response) | [`createDirectory`](#createdirectory) `nodiscard` | Create a new directory. |
| [`Response`](sf-Ftp-Response.md#response) | [`deleteDirectory`](#deletedirectory) `nodiscard` | Remove an existing directory. |
| [`Response`](sf-Ftp-Response.md#response) | [`renameFile`](#renamefile) `nodiscard` | Rename an existing file. |
| [`Response`](sf-Ftp-Response.md#response) | [`deleteFile`](#deletefile) `nodiscard` | Remove an existing file. |
| [`Response`](sf-Ftp-Response.md#response) | [`download`](#download) `nodiscard` | Download a file from the server. |
| [`Response`](sf-Ftp-Response.md#response) | [`upload`](#upload) `nodiscard` | Upload a file to the server. |
| [`Response`](sf-Ftp-Response.md#response) | [`sendCommand`](#sendcommand) `nodiscard` | Send a command to the FTP server. |

---

{#ftp-1}

### Ftp

```cpp
Ftp() = default
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:251

Default constructor.

---

{#ftp-2}

### ~Ftp

```cpp
~Ftp()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:260

Destructor.

Automatically closes the connection with the server if it is still opened.

---

{#ftp-3}

### Ftp

```cpp
Ftp(const Ftp &) = delete
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:266

Deleted copy constructor.

---

{#operator-26}

### operator=

```cpp
Ftp & operator=(const Ftp &) = delete
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:272

Deleted copy assignment.

---

{#connect}

### connect

`nodiscard`

```cpp
[[nodiscard]] Response connect(IpAddress server, unsigned short port = 21, Time timeout = Time::Zero)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:295

Connect to the specified FTP server.

The port has a default value of 21, which is the standard port used by the FTP protocol. You shouldn't use a different value, unless you really know what you do. This function tries to connect to the server so it may take a while to complete, especially if the server is not reachable. To avoid blocking your application for too long, you can use a timeout. The default value, `[Time::Zero](sf-Time.md#zero-1)`, means that the system timeout will be used (which is usually pretty long).

#### Returns
Server response to the request

**See also**: `[disconnect](#disconnect)`

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `server` | [`IpAddress`](sf-IpAddress.md#ipaddress) | Name or address of the FTP server to connect to |
| `port` | `unsigned short` | Port used for the connection |
| `timeout` | [`Time`](sf-Time.md#time) | Maximum time to wait |

---

{#disconnect}

### disconnect

`nodiscard`

```cpp
[[nodiscard]] Response disconnect()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:305

Close the connection with the server.

#### Returns
Server response to the request

**See also**: `[connect](#connect)`

---

{#login}

### login

`nodiscard`

```cpp
[[nodiscard]] Response login()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:316

Log in using an anonymous account.

Logging in is mandatory after connecting to the server. Users that are not logged in cannot perform any operation.

#### Returns
Server response to the request

---

{#login-1}

### login

`nodiscard`

```cpp
[[nodiscard]] Response login(const std::string & name, const std::string & password)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:330

Log in using a username and a password.

Logging in is mandatory after connecting to the server. Users that are not logged in cannot perform any operation.

#### Returns
Server response to the request

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `name` | `const std::string &` | User name |
| `password` | `const std::string &` | Password |

---

{#keepalive}

### keepAlive

`nodiscard`

```cpp
[[nodiscard]] Response keepAlive()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:341

Send a null command to keep the connection alive.

This command is useful because the server may close the connection automatically if no command is sent.

#### Returns
Server response to the request

---

{#getworkingdirectory}

### getWorkingDirectory

`nodiscard`

```cpp
[[nodiscard]] DirectoryResponse getWorkingDirectory()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:354

Get the current working directory.

The working directory is the root path for subsequent operations involving directories and/or filenames.

#### Returns
Server response to the request

**See also**: `[getDirectoryListing](#getdirectorylisting)`, `[changeDirectory](#changedirectory)`, `[parentDirectory](#parentdirectory)`

---

{#getdirectorylisting}

### getDirectoryListing

`nodiscard`

```cpp
[[nodiscard]] ListingResponse getDirectoryListing(const std::string & directory = "")
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:371

Get the contents of the given directory.

This function retrieves the sub-directories and files contained in the given directory. It is not recursive. The `directory` parameter is relative to the current working directory.

#### Returns
Server response to the request

**See also**: `[getWorkingDirectory](#getworkingdirectory)`, `[changeDirectory](#changedirectory)`, `[parentDirectory](#parentdirectory)`

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `directory` | `const std::string &` | Directory to list |

---

{#changedirectory}

### changeDirectory

`nodiscard`

```cpp
[[nodiscard]] Response changeDirectory(const std::string & directory)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:385

Change the current working directory.

The new directory must be relative to the current one.

#### Returns
Server response to the request

**See also**: `[getWorkingDirectory](#getworkingdirectory)`, `[getDirectoryListing](#getdirectorylisting)`, `[parentDirectory](#parentdirectory)`

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `directory` | `const std::string &` | New working directory |

---

{#parentdirectory}

### parentDirectory

`nodiscard`

```cpp
[[nodiscard]] Response parentDirectory()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:395

Go to the parent directory of the current one.

#### Returns
Server response to the request

**See also**: `[getWorkingDirectory](#getworkingdirectory)`, `[getDirectoryListing](#getdirectorylisting)`, `[changeDirectory](#changedirectory)`

---

{#createdirectory}

### createDirectory

`nodiscard`

```cpp
[[nodiscard]] Response createDirectory(const std::string & name)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:410

Create a new directory.

The new directory is created as a child of the current working directory.

#### Returns
Server response to the request

**See also**: `[deleteDirectory](#deletedirectory)`

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `name` | `const std::string &` | Name of the directory to create |

---

{#deletedirectory}

### deleteDirectory

`nodiscard`

```cpp
[[nodiscard]] Response deleteDirectory(const std::string & name)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:427

Remove an existing directory.

The directory to remove must be relative to the current working directory. Use this function with caution, the directory will be removed permanently!

#### Returns
Server response to the request

**See also**: `[createDirectory](#createdirectory)`

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `name` | `const std::string &` | Name of the directory to remove |

---

{#renamefile}

### renameFile

`nodiscard`

```cpp
[[nodiscard]] Response renameFile(const std::filesystem::path & file, const std::filesystem::path & newName)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:443

Rename an existing file.

The file names must be relative to the current working directory.

#### Returns
Server response to the request

**See also**: `[deleteFile](#deletefile)`

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `file` | `const std::filesystem::path &` | File to rename |
| `newName` | `const std::filesystem::path &` | New name of the file |

---

{#deletefile}

### deleteFile

`nodiscard`

```cpp
[[nodiscard]] Response deleteFile(const std::filesystem::path & name)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:460

Remove an existing file.

The file name must be relative to the current working directory. Use this function with caution, the file will be removed permanently!

#### Returns
Server response to the request

**See also**: `[renameFile](#renamefile)`

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `name` | `const std::filesystem::path &` | File to remove |

---

{#download}

### download

`nodiscard`

```cpp
[[nodiscard]] Response download(const std::filesystem::path & remoteFile, const std::filesystem::path & localPath, TransferMode mode = TransferMode::Binary)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:482

Download a file from the server.

The file name of the distant file is relative to the current working directory of the server, and the local destination path is relative to the current directory of your application. If a file with the same file name as the distant file already exists in the local destination path, it will be overwritten.

#### Returns
Server response to the request

**See also**: `[upload](#upload)`

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `remoteFile` | `const std::filesystem::path &` | File name of the distant file to download |
| `localPath` | `const std::filesystem::path &` | The directory in which to put the file on the local computer |
| `mode` | [`TransferMode`](TransferMode.md#transfermode) | Transfer mode |

---

{#upload}

### upload

`nodiscard`

```cpp
[[nodiscard]] Response upload(const std::filesystem::path & localFile, const std::filesystem::path & remotePath, TransferMode mode = TransferMode::Binary, bool append = false)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:507

Upload a file to the server.

The name of the local file is relative to the current working directory of your application, and the remote path is relative to the current directory of the FTP server.

The append parameter controls whether the remote file is appended to or overwritten if it already exists.

#### Returns
Server response to the request

**See also**: `[download](#download)`

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `localFile` | `const std::filesystem::path &` | Path of the local file to upload |
| `remotePath` | `const std::filesystem::path &` | The directory in which to put the file on the server |
| `mode` | [`TransferMode`](TransferMode.md#transfermode) | Transfer mode |
| `append` | `bool` | Pass `true` to append to or `false` to overwrite the remote file if it already exists |

---

{#sendcommand}

### sendCommand

`nodiscard`

```cpp
[[nodiscard]] Response sendCommand(const std::string & command, const std::string & parameter = "")
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:528

Send a command to the FTP server.

While the most often used commands are provided as member functions in the `[sf::Ftp](#ftp)` class, this method can be used to send any FTP command to the server. If the command requires one or more parameters, they can be specified in `parameter`. If the server returns information, you can extract it from the response using `[Response::getMessage()](sf-Ftp-Response.md#getmessage)`.

#### Returns
Server response to the request

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `command` | `const std::string &` | Command to send |
| `parameter` | `const std::string &` | Command parameter |

## Public Types

| Name | Description |
|------|-------------|
| [`TransferMode`](#transfermode)  | Enumeration of transfer modes. |

---

{#transfermode}

### TransferMode

```cpp
enum TransferMode
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:58

Enumeration of transfer modes.

| Value | Description |
|-------|-------------|
| `Binary` | Binary mode (file is transferred as a sequence of bytes) |
| `Ascii` | [Text](sf-Text.md#text-1) mode using ASCII encoding. |
| `Ebcdic` | [Text](sf-Text.md#text-1) mode using EBCDIC encoding. |
## Private Attributes

| Return | Name | Description |
|--------|------|-------------|
| [`TcpSocket`](sf-TcpSocket.md#tcpsocket-1) | [`m_commandSocket`](#m_commandsocket)  | [Socket](sf-Socket.md#socket) holding the control connection with the server. |
| `std::string` | [`m_receiveBuffer`](#m_receivebuffer)  | Received command data that is yet to be processed. |

---

{#m_commandsocket}

### m_commandSocket

```cpp
TcpSocket m_commandSocket
```

Type: [`TcpSocket`](sf-TcpSocket.md#tcpsocket-1)

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:554

[Socket](sf-Socket.md#socket) holding the control connection with the server.

---

{#m_receivebuffer}

### m_receiveBuffer

```cpp
std::string m_receiveBuffer
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:555

Received command data that is yet to be processed.

## Private Methods

| Return | Name | Description |
|--------|------|-------------|
| [`Response`](sf-Ftp-Response.md#response) | [`getResponse`](#getresponse)  | Receive a response from the server. |

---

{#getresponse}

### getResponse

```cpp
Response getResponse()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Network/Ftp.hpp:540

Receive a response from the server.

This function must be called after each call to `sendCommand` that expects a response.

#### Returns
Server response to the request

