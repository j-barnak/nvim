{#playbackdevice}

# PlaybackDevice

## Enumerations

| Name | Description |
|------|-------------|
| [`Notification`](#notification)  | Enumeration of the playback device notifications. |

---

{#notification}

### Notification

```cpp
enum Notification
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:203

Enumeration of the playback device notifications.

| Value | Description |
|-------|-------------|
| `DeviceStarted` | Playback device has been started. |
| `DeviceStopped` | Playback device has been stopped. |
| `DeviceRerouted` | Playback device has been rerouted (Generated on platforms that support automatic stream routing) |
| `DeviceInterruptionBegan` | Playback device interruption has begun (Generated on Apple mobile platforms) |
| `DeviceInterruptionEnded` | Playback device interruption has ended (Generated on Apple mobile platforms) |
| `DeviceUnlocked` | Playback device has been unlocked (Generated by Emscripten/WebAudio) |
## Typedefs

| Return | Name | Description |
|--------|------|-------------|
| std::function< void([`Notification`](Notification.md#notification) notification)> | [`NotificationCallback`](#notificationcallback)  | Callable that is called to notify of changes to the playback device state. |

---

{#notificationcallback}

### NotificationCallback

```cpp
using NotificationCallback = std::function< void(Notification notification)>
```

Type: std::function< void([`Notification`](Notification.md#notification) notification)>

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:217

Callable that is called to notify of changes to the playback device state.

## Functions

| Return | Name | Description |
|--------|------|-------------|
| [`SFML_AUDIO_API`](api.md#sfml_audio_api) std::vector< std::string > | [`getAvailableDevices`](#getavailabledevices-1) `nodiscard` | Get a list of the names of all available audio playback devices. |
| [`SFML_AUDIO_API`](api.md#sfml_audio_api) std::optional< std::string > | [`getDefaultDevice`](#getdefaultdevice-1) `nodiscard` | Get the name of the default audio playback device. |
| [`SFML_AUDIO_API`](api.md#sfml_audio_api) bool | [`setDevice`](#setdevice-1) `nodiscard` | Set the audio playback device. |
| [`SFML_AUDIO_API`](api.md#sfml_audio_api) bool | [`setDeviceToDefault`](#setdevicetodefault) `nodiscard` | Set the audio playback device to the default. |
| [`SFML_AUDIO_API`](api.md#sfml_audio_api) bool | [`setDeviceToNull`](#setdevicetonull) `nodiscard` | Set the audio playback device to the null device. |
| [`SFML_AUDIO_API`](api.md#sfml_audio_api) std::optional< std::string > | [`getDevice`](#getdevice-1) `nodiscard` | Get the name of the current audio playback device. |
| [`SFML_AUDIO_API`](api.md#sfml_audio_api) std::optional< std::uint32_t > | [`getDeviceSampleRate`](#getdevicesamplerate) `nodiscard` | Get the sample rate of the current audio playback device. |
| [`SFML_AUDIO_API`](api.md#sfml_audio_api) bool | [`isDefaultDevice`](#isdefaultdevice) `nodiscard` | Check if the current playback device is the default device. |
| [`SFML_AUDIO_API`](api.md#sfml_audio_api) void | [`setNotificationCallback`](#setnotificationcallback)  | Set a callback that should be called to notify of changes to the playback device state. |

---

{#getavailabledevices-1}

### getAvailableDevices

`nodiscard`

```cpp
[[nodiscard]] SFML_AUDIO_API std::vector< std::string > getAvailableDevices()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:71

Get a list of the names of all available audio playback devices.

This function returns a vector of strings containing the names of all available audio playback devices.

If the operating system reports multiple devices with the same name, a number will be appended to the name of all subsequent devices to distinguish them from each other. This guarantees that every entry returned by this function will represent a unique device.

For example, if the operating system reports multiple devices with the name "Sound Card", the entries returned would be:

* [Sound](sf-Sound.md#sound) Card
* [Sound](sf-Sound.md#sound) Card 2
* [Sound](sf-Sound.md#sound) Card 3
* ...

The default device, if one is marked as such, will be placed at the beginning of the vector.

If no devices are available, this function will return an empty vector.

#### Returns
A vector of strings containing the device names or an empty vector if no devices are available

---

{#getdefaultdevice-1}

### getDefaultDevice

`nodiscard`

```cpp
[[nodiscard]] SFML_AUDIO_API std::optional< std::string > getDefaultDevice()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:88

Get the name of the default audio playback device.

This function returns the name of the default audio playback device. If none is available, `std::nullopt` is returned.

Note that depending on when this function is called, the default device reported by the operating system might change e.g. when a USB audio device is plugged into or unplugged from the system.

#### Returns
The name of the default audio playback device

---

{#setdevice-1}

### setDevice

`nodiscard`

```cpp
[[nodiscard]] SFML_AUDIO_API bool setDevice(const std::string & name)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:108

Set the audio playback device.

This function sets the audio playback device to the device with the given `name`. It can be called on the fly (i.e: while sounds are playing).

If there are sounds playing when the audio playback device is switched, the sounds will continue playing uninterrupted on the new audio playback device.

#### Returns
`true`, if it was able to set the requested device

**See also**: `[getAvailableDevices](#getavailabledevices-1)`, `[getDefaultDevice](#getdefaultdevice-1)`, `[setDeviceToDefault](#setdevicetodefault)`, `[setDeviceToNull](#setdevicetonull)`

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `name` | `const std::string &` | The name of the audio playback device |

---

{#setdevicetodefault}

### setDeviceToDefault

`nodiscard`

```cpp
[[nodiscard]] SFML_AUDIO_API bool setDeviceToDefault()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:147

Set the audio playback device to the default.

This function sets the audio playback device to the default device. It can be called on the fly (i.e: while sounds are playing).

If there are sounds playing when the audio playback device is switched, the sounds will continue playing uninterrupted on the new audio playback device.

When certain backends are used, using the default device will enable automatic stream routing. When automatic stream routing is enabled, audio data is automatically sent to whichever physical audio device is currently marked as the default on the system. If the default device changes due to e.g. a device being added to or removed from the system or the user marking another device as the default, automatic stream routing will seamlessly reroute the audio data to the new default device without any manual intervention.

Automatic stream routing is currently supported when using the WASAPI or DirectSound backend on Windows or the Core Audio backend on macOS and iOS.

Depending on the order in which hardware devices are initialized e.g. after resuming from sleep, the default device might change one or more times in rapid succession before it reverts back to the state in which it was before the system went to sleep.

#### Returns
`true`, if it was able to set the audio playback device to the default device

**See also**: `[getAvailableDevices](#getavailabledevices-1)`, `[getDefaultDevice](#getdefaultdevice-1)`, `[setDevice](#setdevice-1)`, `[setDeviceToNull](#setdevicetonull)`

---

{#setdevicetonull}

### setDeviceToNull

`nodiscard`

```cpp
[[nodiscard]] SFML_AUDIO_API bool setDeviceToNull()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:170

Set the audio playback device to the null device.

This function sets the audio playback device to the null device. It can be called on the fly (i.e: while sounds are playing).

If there are sounds playing when the audio playback device is switched, the sounds will continue playing uninterrupted on the new audio playback device.

Audio data routed to the null device will be discarded by the backend. This can be used to keep sounds playing without having them actually output on a physical audio playback device.

#### Returns
`true`, if it was able to set the audio playback device to the null device

**See also**: `[getAvailableDevices](#getavailabledevices-1)`, `[getDefaultDevice](#getdefaultdevice-1)`, `[setDevice](#setdevice-1)`, `[setDeviceToDefault](#setdevicetodefault)`

---

{#getdevice-1}

### getDevice

`nodiscard`

```cpp
[[nodiscard]] SFML_AUDIO_API std::optional< std::string > getDevice()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:178

Get the name of the current audio playback device.

#### Returns
The name of the current audio playback device or `std::nullopt` if there is none

---

{#getdevicesamplerate}

### getDeviceSampleRate

`nodiscard`

```cpp
[[nodiscard]] SFML_AUDIO_API std::optional< std::uint32_t > getDeviceSampleRate()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:186

Get the sample rate of the current audio playback device.

#### Returns
The sample rate of the current audio playback device or `std::nullopt` if there is none

---

{#isdefaultdevice}

### isDefaultDevice

`nodiscard`

```cpp
[[nodiscard]] SFML_AUDIO_API bool isDefaultDevice()
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:197

Check if the current playback device is the default device.

This function will return `false` if there is no current playback device.

#### Returns
`true`, if the current playback device is the default device

---

{#setnotificationcallback}

### setNotificationCallback

```cpp
SFML_AUDIO_API void setNotificationCallback(NotificationCallback callback)
```

Defined in /home/jared/.local/share/nvim/docs/sfml/master/include/SFML/Audio/PlaybackDevice.hpp:237

Set a callback that should be called to notify of changes to the playback device state.

Warning: Do not attempt to alter the device state from within this callback. This includes changing the device and even creating/destroying sound objects since that could indirectly cause the playback device to be created/destroyed. Also do not attempt to set the notification callback from within this callback. Doing so will result in a deadlock.

When receiving a notification via this callback, store the information somewhere and react on it from another thread e.g. the main thread within the application.

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `callback` | [`NotificationCallback`](#notificationcallback) | The callback that should be called to notify of changes to the playback device state |

