                  Glossary

ABI   Application binary interface.
ACK   TCP acknowledgment.
adaptive mutex A mutex (mutual
exclusion) synchronization lock type.
See Chapter 5, Applications, Section
5.2.5, Concurrency and Parallelism.
address   A memory location.
address space A virtual memory
context. See Chapter 7, Memory.
AMD A processor vendor.
API Application programming
interface.
application   A program, typically
user-level.
ARM    A processor vendor.
ARP   Address resolution protocol.
array A set of values. This is a data type
for programming languages. Low-level
languages typically store arrays in a
contiguous range of memory, and value
index refers to their offset.
ASIC Application-specific integrated
circuit.
associative array A data type for
programming languages where values
are referenced by an arbitrary key or
multiple keys.
AT&T The American Telephone and
Telegraph Company, which included
Bell Laboratories, where Unix was
developed.
816   back end

      back end Refers to data storage and       byte A unit of digital data. This book
      infrastructure components. A web          follows the industry standard where
      server is back-end software. See front    one byte equals eight bits, where a bit is
      end.                                      a zero or a one.
      balanced system A system without a        C The C programming language.
      bottleneck.                               cache hit A request for data that can
      bandwidth The frequency range for         be returned from the contents of the
      a communication channel, measured         cache.
      in Hertz. In computing, bandwidth is      cache miss A request for data that was
      used to describe the maximum transfer     not found in the cache.
      rate for a communication channel. It is
      also frequently misused to describe the   cache warmth See Hot, Cold, and
      current throughput (which is wrong).      Warm Caches in Section 2.3.14,
                                                Caching, in Chapter 2, Methodologies.
      BCC BPF compiler collection. BCC is
      a project that includes a BPF compiler    CDN Content delivery network.
      framework, as well as many BPF            client A consumer of a network service,
      performance tools. See Chapter 15.        referring to either the client host or the
      benchmark In computing, a                 client application.
      benchmark is a tool that performs a       command     A program executed at the
      workload experiment and measures          shell.
      its performance: the benchmark            concurrency See Section 5.2.5,
      result. These are commonly used for       Concurrency and Parallelism, in
      evaluating the performance of different   Chapter 5, Applications.
      options.
                                                contention Competition for a resource.
      BIOS Basic Input/Output System:
      firmware used to initialize computer      core An execution pipeline on a
      hardware and manage the booting           processor. These may be exposed on an
      process.                                  OS as single CPUs, or via hyperthreads
                                                as multiple CPUs.
      bottleneck Something that limits
      performance.                              CPI Cycles per instruction. See
                                                Chapter 6, CPUs.
      BPF Berkeley Packet Filter: a
      lightweight in-kernel technology          CPU Central processing unit. This term
      from 1992 created to improve the          refers to the set of functional units that
      performance of packet filtering.          execute instructions, including the
      Since 2014 it has been extended to        registers and arithmetic logic unit
      become a general-purpose execution        (ALU). It is now often used to refer to
      environment (see eBPF).                   either the processor or a virtual CPU.

      BSD Berkeley Software Distribution, a     CPU cross call A call by a CPU to
      derivative of Unix.                       request work from others on a multi-
                                                CPU system. Cross calls may be made
      buffer A region of memory used for        for system-wide events such as CPU
      temporary data.                           cache coherency calls. See Chapter 6,
                                                CPUs. Linux terms these “SMP calls.”
                                                                                    FPGA    817

CPU cycle A unit of time based on the         is a technology that can instrument
clock rate of the processor: for 2 GHz,       any software event, including function
each cycle is 0.5 ns. A cycle itself is an    calls and returns, by live modification
electrical signal, the rising or falling of   of instruction text and the insertion of
voltage, used to trigger digital logic.       temporary tracing instructions.
cross call See CPU cross call.                dynamic tracing This can refer to the
CSV The Comma Separated Values file           software that implements dynamic
type.                                         instrumentation.

CTSS Compatible Time-Sharing                  eBPF Extended BPF (see BPF). The
System, one of the first time-sharing         eBPF abbreviation originally described
systems.                                      the extended BPF from 2014, which
                                              updated the register size, instruction
daemon A system program that                  set, added map storage, and limited
continually runs to provide a service.        kernel calls. By 2015, it was decided to
datagram    See segment.                      call eBPF just BPF.
debuginfo file A symbol and debug             ECC Error-correcting code. An
information file, used by debuggers and       algorithm for detecting errors and
profilers.                                    fixing some types of errors (usually
DEC   Digital Equipment Corporation.          single-bit errors).

disk A physical storage device. Also see      ELF Executable and Linkable Format:
HDD and SSD.                                  a common file format for executable
                                              programs.
disk controller A component that
manages directly attached disks,              errno A variable containing the last
making them accessible to the system,         error as a number following a standard
either directly or mapped as virtual          (POSIX.1-2001).
disks. Disk controllers may be built into     Ethernet A set of standards for
the system main board, included as            networking at the physical and data
expansion cards, or built into storage        link layers.
arrays. They support one or more              expander card A physical device (card)
storage interface types (e.g., SCSI, SATA,    connected to the system, usually to
SAS) and are also commonly called host        provide an additional I/O controller.
bus adaptors (HBAs), along with the
interface type, for example, SAS HBA.         file descriptor An identifier for a program
                                              to use in referencing an open file.
DNS Domain Name Service.
                                              firmware Embedded device software.
DRAM Dynamic random-access
memory, a type of volatile memory in          flame graph A visualization for a
common use as main memory.                    set of stack traces. See Chapter 2,
                                              Methodologies.
duplex Simultaneous bi-directional
communication.                                FPGA Field-programmable gate
                                              array. A reprogrammable integrated
dynamic instrumentation Dynamic               circuit used in computing to typically
instrumentation or dynamic tracing            accelerate a specific operation.
818   frame

      frame A message at the data link              IO Visor The Linux Foundation
      layer of the OSI networking model (see        project that hosts the bcc and bpftrace
      Section 10.2.3, Protocol Stack).              repositories on GitHub, and facilitates
                                                    collaboration among BPF developers at
      FreeBSD An open source, Unix-like,
                                                    different companies.
      operating system.
                                                    IOPS I/O operations per second, a
      front end Refers to end-user interface
                                                    measure of the rate of I/O.
      and presentation software. A web
      application is front-end software. See        Intel   A processor vendor.
      back end.                                     instance A virtual server. Cloud
      fsck The file system check command            computing provides server instances.
      is used to repair file systems after system   IP Internet Protocol. Main versions are
      failure, such as due to a power outage        IPv4 and IPv6. See Chapter 10, Network.
      or kernel panic. This process can take
      hours.                                        IPC Either means: instructions per
                                                    cycle, a low-level CPU performance
      Gbps    Gigabits per second.                  metric, or inter-process communication,
      GPU Graphics processing unit. These           a means for processes to exchange
      can be used for other workloads as well,      data. Sockets are an inter-process
      such as machine learning.                     communication mechanism.
      GUI Graphical user interface.                 IRIX A Unix-derived operating system
                                                    by Silicon Graphics, Inc. (SGI).
      HDD Hard disk drive, a rotational
      magnetic storage device. See Chapter 9,       IRQ Interrupt request, a hardware
      Disks.                                        signal to the processor to request work.
                                                    See Chapter 3, Operating Systems.
      Hertz (Hz)   Cycles per second.
                                                    Kbytes Kilobytes. The International
      hit ratio Often used to describe cache
                                                    System of Units (SI) defines a kilobyte as
      performance: the ratio of cache hits
                                                    1000 bytes, but in computing a kilobyte
      versus hits plus misses, usually expressed
                                                    is typically 1024 bytes (which SI terms
      as a percentage. Higher is better.
                                                    a kibibyte). Throughout this book, tools
      host A system connected to the                that report Kbytes are usually using the
      network. Also called a network node.          definition of 1024 (210) bytes.
      HTTP    Hyper Text Transfer Protocol.         kernel The core program on a system
      hyperthread Intel’s implementation of         that runs in privileged mode to manage
      SMT. This is a technology for scaling         resources and user-level processes.
      CPUs, allowing the OS to create multiple      kernel-land Kernel software.
      virtual CPUs for one core and schedule
                                                    kernel-level The processor privilege
      work on them, which the processor
                                                    mode that kernel execution uses.
      attempts to process in parallel.
                                                    kernel-space The address space of the
      ICMP Internet Control Message
                                                    kernel.
      Protocol. Used by ping(1) (ICMP echo
      request/reply).                               kprobes A Linux kernel technology for
                                                    kernel-level dynamic instrumentation.
      I/O Input/output.
                                                                              on-CPU    819

latency Time spent waiting. In              Mbytes Megabytes. The International
computing performance it is often used      System of Units (SI) defines a megabyte
to describe resource I/O time. Latency      as 1000000 bytes, but in computing
is important for performance analysis,      a megabyte is typically 1048576
because it is often the most effective      bytes (which SI terms a mebibyte).
measure of a performance issue. Where       Throughout this book, tools that report
exactly it is measured can be ambiguous     Mbytes are usually using the definition
without further qualifiers. For example,    of 1048576 (220) bytes.
“disk latency” could mean time spent        memory    See main memory.
waiting on a disk driver queue only
or, from an application, it could mean      micro-benchmark A benchmark for
the entire time waiting for disk I/O to     measuring single or simple operations.
complete, both queued and service           minor fault A memory access fault that
time. Latency is limited by a lower         was serviced from main memory. See
bound, bandwidth by an upper bound.         Chapter 3, Operating Systems.
local disks Disks that are connected        MMU Memory management unit. This
directly to the server and are managed      is responsible for presenting memory
by the server. These include disks inside   to a CPU and for performing virtual-to-
the server case and those attached          physical address translation.
directly via a storage transport.           mutex A mutual exclusion lock. They
logical processor Another name for a        can become a source of performance
virtual CPU. See Chapter 6, CPUs.           bottlenecks, and are often investigated
LRU Least recently used. See                for performance problems. See
Section 2.3.14, Caching, in Chapter 2,      Chapter 5, Applications.
Methodologies.                              mysqld The daemon for the MySQL
main board The circuit board that           database.
houses the processors and system            NVMe Non-Volatile Memory express:
interconnect; also called the system        a PCIe bus specification for storage
board.                                      devices.
main memory The primary memory              observability The ability for a comput-
storage of a system, usually implemented    ing system to be observed. This includes
as DRAM.                                    the practice and tools used to analyze
major fault A memory access fault           the state of computing systems.
that was serviced from storage devices      off-CPU This term refers to a thread
(disks). See Chapter 3, Operating           that is not currently running on a CPU,
Systems.                                    and so is “off-CPU,” due to either having
malloc Memory allocate. This usually        blocked on I/O, a lock, a voluntary
refers to the function performing           sleep, or other event.
allocation.                                 on-CPU This term refers to a thread
Mbps   Megabits per second.                 that is currently running on a CPU.
820   operation rate

      operation rate Operations per interval     PDP Programmed Data Processor, a
      (e.g., operations per second), which may   minicomputer series made by Digital
      include non-I/O operations.                Equipment Corporation (DEC).
      OS Operating System. The collection        PEBS Precise event-based sampling
      of software including the kernel for       (aka processor event-based sampling), an
      managing resources and user-level          Intel processor technology for use with
      processes.                                 PMCs to provide more precise recording
      packet A network message at the            of CPU state during events.
      network layer of the OSI networking        Performance engineer A technical
      model (see Section 10.2.3).                staff member who works primarily
      page A chunk of memory managed by          on computer performance:
      the kernel and processor. All memory       planning, evaluations, analysis,
      used by the system is broken up into       and improvements. See Chapter 1,
      pages for reference and management.        Introduction, Section 1.3, Activities.
      Typical page sizes include 4 Kbytes and    PID Process identifier. The operating
      2 Mbytes (depending on the processor).     system unique numeric identifier for a
      pagefault A system trap that occurs        process.
      when a program references a memory         PMCs Performance Monitoring
      location where the virtual memory          Counters: special hardware registers on
      is not currently mapped to a physical      the processor that can be programmed
      backing page. This is a normal             to instrument low-level CPU events:
      consequence of an on-demand                cycles, stall cycles, instructions,
      allocation memory model.                   memory loads/stores, etc.
      pagein/pageout Functions performed         POSIX Portable Operating System
      by an operating system (kernel) to move    Interface, a family of related standards
      chunks of memory (pages) to and from       managed by the IEEE to define a Unix
      external storage devices.                  API. This includes a file system interface
      parallel See Section 5.2.5, Concurrency    as used by applications, provided via
      and Parallelism, in Chapter 5,             system calls or system libraries built
      Applications.                              upon system calls.

      PC Program counter, a CPU register         process A running instance of a
      that points to the currently executing     program. Multi-tasked operating
      instruction.                               systems can execute multiple processes
                                                 concurrently, and can run multiple
      PCID Process-context ID, a processor/      instances of the same program as
      MMU feature to tag virtual address         different processes. A process contains
      entries with the process ID to avoid       one or more threads for execution on
      flushing on context switches.              CPUs.
      PCIe Peripheral Component                  processor ring A protection mode for
      Interconnect Express: a bus standard       the CPU.
      commonly used for storage and network
      controllers.
                                                                               Solaris   821

production A term used in technology        sampling An observability method
to describe the workload of real customer   for understanding a target by taking a
requests, and the environment that          subset of measurements: a sample.
processes it. Many companies also have      script In computing, an executable
a “test” environment with synthetic         program, usually short and in a high-
workloads for testing things before         level language.
production deployment.
                                            SCSI Small Computer System
profiling A technique to collect data       Interface. An interface standard for
that characterizes the performance of a     storage devices.
target. A common profiling technique
is timed sampling (see sampling).           sector A data unit size for storage
                                            devices, commonly 512 bytes or
PSI Linux pressure stall information,       4 Kbytes. See Chapter 9, Disks.
used for identifying performance issues
caused by resources.                        segment A message at the transport
                                            layer of the OSI networking model (see
RCU Read-copy-update: a Linux               Section 10.2.3, Protocol Stack).
synchronization mechanism.
                                            server In networking, a network host
real-time workload One that has fixed       that provides a service for network
latency requirements, usually low           clients, such as an HTTP or database
latency.                                    server. The term server can also refer to
registers Small storage locations           a physical system.
on a CPU, used directly from CPU            shell A command-line interpreter and
instructions for data processing.           scripting language.
remote disks Disks (including virtual       SLA   Service level agreement.
disks) that are used by a server but are
connected to a remote system.               SMP Symmetric multiprocessing,
                                            a multiprocessor architecture where
RFC Request For Comments: a public          multiple similar CPUs share the same
document by the Internet Engineering        main memory.
Task Force (IETF) to share networking
standards and best practices. RFCs are      SMT Simultaneous multithreading,
used to define networking protocols:        a processor feature to run multiple
RFC 793 defines the TCP protocol.           threads on cores. See hyperthread.

RSS Resident set size: a measure of         SNMP Simple Network Management
main memory.                                Protocol.

ROI Return on investment, a business        socket A software abstraction
metric.                                     representing a network endpoint for
                                            communication.
run queue A CPU scheduler queue
of tasks waiting their turn to run on       Solaris A Unix-derived operating
a CPU. In reality the queue may be          system originally developed by Sun
implemented as a tree structure, but the    Microsystems, it was known for
term run queue is still used.               scalability and reliability, and was
                                            popular in enterprise environments.
RX   Receive (used in networking).
822   Solaris

      Since the acquisition of Sun by Oracle      struct A structured object, usually
      Corporation, it has been renamed            from the C programming language.
      Oracle Solaris.                             SunOS Sun Microsystems Operating
      SONET Synchronous optical                   System. This was later rebranded as
      networking, a physical layer protocol       Solaris.
      for optical fibers.                         SUT System under test.
      SPARC A processor architecture (from        SVG The Scalable Vector Graphs file
      scalable processor architecture).           format.
      SRE Site reliability engineer: a            SYN TCP synchronize.
      technical staff member focused on
      infrastructure and reliability. SREs work   syscall   See system call.
      on performance as part of incident          system call The interface for processes
      response, under short time constraints.     to request privileged actions from
      SSD Solid-state drive, a storage device     the kernel. See Chapter 3, Operating
      typically based on flash memory. See        Systems.
      Chapter 9, Disks.                           task A Linux runnable entity, which
      SSH Secure Shell. An encrypted              may be a process, a thread from a
      remote shell protocol.                      multithreaded process, or a kernel
                                                  thread. See Chapter 3, Operating
      stack In the context of observability       Systems.
      tools, stack is usually short for “stack
      trace.”                                     TCP Transmission Control Protocol.
                                                  Originally defined in RFC 793. See
      stack frame A data structure                Chapter 10, Network.
      containing function state information,
      including the return address and            TENEX TEN-EXtended operating
      function arguments.                         system, based on TOPS-10 for the
                                                  PDP-10.
      stack trace A call stack composed of
      multiple stack frames spanning the          thread A software abstraction for an
      code path ancestry. These are often         instance of program execution, which
      inspected as part of performance            can be scheduled to run on a CPU.
      analysis, particularly CPU profiling.       The kernel has multiple threads, and
                                                  a process contains one or more. See
      static instrumentation/tracing              Chapter 3, Operating Systems.
      Instrumentation of software with
      precompiled probe points. See               throughput For network
      Chapter 4, Observability Tools.             communication devices, throughput
                                                  commonly refers to the data transfer
      storage array A collection of disks         rate in either bits per second or bytes
      housed in an enclosure, which can           per second. Throughput may also refer
      then be attached to a system. Storage       to I/O completions per second (IOPS)
      arrays typically provide various            when used with statistical analysis,
      features to improve disk reliability and    especially for targets of study.
      performance.
                                                                                ZFS   823

TLB Translation Lookaside Buffer. A       code by the programmer, at locations to
cache for memory translation on virtual   provide useful probes.
memory systems, used by the MMU (see      user-land This refers to user-level
MMU).                                     software and files, including executable
TLS Transport Layer Security: used to     programs in /usr/bin, /lib, etc.
encrypt network requests.                 user-level The processor privilege
TPU Tensor processing unit. An AI         mode that user-land execution uses.
accelerator ASIC for machine learning     This is a lower privilege level than the
developed by Google, and named after      kernel, and one that denies direct
TensorFlow (a software platform for       access to resources, forcing user-level
machine learning).                        software to request access to them via
                                          the kernel.
tracepoints A Linux kernel technology
for providing static instrumentation.     user-space The address space of the
                                          user-level processes.
tracer A tracing tool. See tracing.
                                          variable A named storage object used
tracing Event-based observability.
                                          by programming languages.
tunable   Short for tunable parameter.
                                          vCPU A virtual CPU. Modern processors
TX   Transmit (used in networking).       can expose multiple virtual CPUs per
UDP User Datagram Protocol.               core (e.g., Intel Hyper-Threading).
Originally defined in RFC 768. See        VFS Virtual file system. An abstraction
Chapter 10, Network.                      used by the kernel to support different
uprobes A Linux kernel technology for     file system types.
user-level dynamic instrumentation.       virtual memory An abstraction of main
us Microseconds. This should be           memory that supports multitasking and
abbreviated as μs; however, you will      over-subscription.
often see it as “us,” especially in the   VMS Virtual Memory System, an
output of ASCII-based performance         operating system by DEC.
tools. (Note that vmstat(8)’s output,
                                          workload This describes the requests
included many times in this book,
                                          for a system or resource.
includes a us column, short for user
time.)                                    x86 A processor architecture based on
                                          the Intel 8086.
μs   Microseconds. See “us.”
                                          ZFS A combined file system and
USDT User-land Statically Defined
                                          volume manager created by Sun
Tracing. This involves the placement of
                                          Microsystems.
static instrumentation in application
This page intentionally left blank
