                                                                                            18.1. Supervisor-Level CSRs widened by RVY | Page 149

Chapter 18. "Supervisor-Level ISA (RVY)" Extensions, Version
1.0

18.1. Supervisor-Level CSRs widened by RVY

18.1.1. Supervisor Trap Vector Base Address Capability Register (stvec)

The stvec register is widened to a capability.

When the S-mode execution environment starts, the value is nominally the Root Executable capability.
                 SXLEN-1                                                                                    2    1                             0
           CT                                                              Metadata (WARL)
                                                   BASE [SXLEN-1:2] (WARL)                                                 MODE (WARL)
                                                           SXLEN-2                                                             2

                                      Figure 21. Supervisor trap-vector base-capability register

The handling of stvec (RVY) is identical to mtvec (RVY), but in supervisor mode.

18.1.2. Supervisor Status Registers (sstatus)

   63           62     61     60          59       58                                                                                                48
   SD       WPRI      UYRG   SYRG     YRGE                                                          WPRI

   47                                                                                                                           34        33         32
                                                                   WPRI                                                                    UXL[1:0]

   31                                                      25             24       23        22             20        19        18        17         16
                             WPRI                                         SDT    SPELP              WPRI             MXR       SUM       WPRI      XS[1:0]

   15           14     13     12          11       10          9           8        7           6    5      4                   2         1          0
 XS[1:0]         FS[1:0]           WPRI              VS[1:0]              SPP     WPRI      UBE     SPIE             WPRI                SIE        WPRI

                             Figure 22. Supervisor-mode status (sstatus) register when SXLEN=64

sstatus makes visible the UYRG, SYRG and YRGE fields added to mstatus (RVY).

18.1.3. Supervisor Scratch Capability Register (sscratch)

The sscratch register is widened to a capability.

At the start of the S-mode execution environment, the value of the capability tag of this CSR is zero and the
values of the metadata and address fields are UNSPECIFIED.
                 SXLEN-1                                                                                                                       0
           CT                                                             sscratch (Metadata)
                                                                           sscratch (Address)
                                                                                SXLEN

                                               Figure 23. Supervisor scratch capability register

18.1.4. Supervisor Exception Program Counter Capability (sepc)

The sepc register is widened to a capability.

When the S-mode execution environment starts, the value is nominally the Root Executable capability.

18.2. Supervisor-Level CSRs added by RVY | Page 150

The handling of sepc (RVY) is identical to mepc (RVY), but in supervisor mode (i.e. applying to SRET
(RVY)).
               SXLEN-1                                                                                                               0
          CT                                            sepc (Metadata, WARL)
                                                         sepc (Address, WARL)
                                                                SXLEN

                           Figure 24. Supervisor exception program counter capability register

18.2. Supervisor-Level CSRs added by RVY

18.2.1. Supervisor Thread Identifier Capability (stidc)

The stidc register is used to identify the current software thread in supervisor mode, using the method
defined in the section for the unprivileged utidc CSR.

At the start of the S-mode execution environment, the value of the capability tag of this CSR is zero and the
values of the metadata and address fields are UNSPECIFIED.
               SXLEN-1                                                                                                               0
          CT                                               stidc (Metadata)
                                                            stidc (Address)
                                                                SXLEN

                                 Figure 25. Supervisor thread identifier capability register

18.3. Supervisor-Level CSRs modified by RVY

18.3.1. Supervisor environment configuration register (senvcfg)

Zyhybrid adds the Y bit to senvcfg. The Y bit can be used to disable or enable RVY for U-mode, as described
in Chapter 20, "Zyhybrid for Privileged Architectures" Extension, Version 1.0.

SXLEN-1                                                                             9        8   7       6   5       4   3       1            0
                                     WPRI                                       Y       CBZE     CBCFE       CBIE        WPRI            FIOM
                                    SXLEN-9                                     1        1           1           2           3            1

                           Figure 26. Supervisor environment configuration register (senvcfg)

18.3.2. Supervisor Cause Register (scause)

RVY adds new exception codes for CHERI exceptions as in mcause (RVY).

18.3.3. "Ssstateen" Integration

The TIDC bit in sstateen0 controls access to the utidc CSR. See utidc for a description of the usage.

   31                                                                                                                                         16
                                                            WPRI

   15                                                                                     4              3           2           1            0
                                              WPRI                                                   TIDC        JVT         FCSR             C

                               Figure 27. Supervisor State Enable 0 Register (sstateen0)

                                                                              18.4. Modified SRET Behavior | Page 151

18.4. Modified SRET Behavior
SRET (RVY) reads the full YLEN bits of the sepc (RVY) register and unseals it prior to exception return if it
is sealed as a Sealed entry point capability.

18.4. Modified SRET Behavior | Page 152

18.4.1. SRET (RVY)

See MRET (RVY).

18.4.2. MRET (RVY)

Synopsis
   Trap Return (MRET, SRET)

Mnemonics
   mret
   sret

Encoding

31                                         20 19               15 14      12 11        7   6                    0
                   funct12                            rs1            funct3       rd               opcode
                  12                                   5                3         5                  7
           MRET=001100000010                           0             PRIV=0       0            SYSTEM=1110011
           SRET=000100000010

Description
   Return from machine mode (MRET) or supervisor mode (SRET) trap handler.

   MRET unseals mepc (RVY) and writes the result into pc.

   SRET unseals sepc (RVY) and writes the result into pc.

Exceptions
   An illegal instruction exception is raised when pc does not grant ASR-permission.

                     ASR-permission is required as MRET and SRET require access to privileged CSRs.

Prerequisites (MRET)
   Machine-Level ISA, RVY

Prerequisites (SRET)
   Supervisor-Level ISA, RVY

Operation for MRET

      if       cur_privilege() != Machine
      then { handle_illegal(); RETIRE_FAIL }
      else if not(ext_check_xret_priv (Machine))
      then { ext_fail_xret_priv(); RETIRE_FAIL }
      else {
          set_next_pc(exception_handler(cur_privilege(), CTL_MRET(), PC));
          RETIRE_SUCCESS
      }

Operation for SRET

      let sret_illegal : bool = match cur_privilege() {

                                                                       18.5. Supervisor-mode YLEN CSR Summary | Page 153

            User         => true,
            Supervisor => not(extensionEnabled(Ext_S)) | mstatus[TSR] == 0b1,
            Machine      => not(extensionEnabled(Ext_S))
        };
        if     sret_illegal
        then { handle_illegal(); RETIRE_FAIL }
        else if not(ext_check_xret_priv (Supervisor))
        then { ext_fail_xret_priv(); RETIRE_FAIL }
        else {
            set_next_pc(exception_handler(cur_privilege(), CTL_SRET(), PC));
            RETIRE_SUCCESS
        }

18.5. Supervisor-mode YLEN CSR Summary
Table 60 shows all YLEN Supervisor-mode CSRs added or modified by RVY.

                                      Table 60. All RVY Supervisor-Mode CSRs.
RVY CSR            Extension      Width      Address      Permissions       Reset Value       Description
sscratch (RVY)     S-mode         YLEN       0x140        SRW, ASR-         tag=0,           Supervisor Scratch
                                                          permission        otherwise        Capability
                                                                            specified by the
                                                                            platform
stvec (RVY)        S-mode         YLEN       0x105        SRW, ASR-         Nominally         Supervisor Trap-Vector
                                                          permission        Root              Base-Address Capability
                                                                            Executable
sepc (RVY)         S-mode         YLEN       0x141        SRW, ASR-         Nominally         Supervisor Exception
                                                          permission        Root              Program Counter
                                                                            Executable        Capability
stidc              RVY            YLEN       0x580        Read: S, Write:   tag=0,           Supervisor Thread ID
                                                          S, ASR-           otherwise
                                                          permission        specified by the
                                                                            platform

                   Table 61. Action taken on writing to YLEN-bit and extended Supervisor-Mode CSRs
RVY CSR            Extension      Width              Action on XLEN write                Action on YLEN write
sscratch (RVY)     S-mode         YLEN               Update the CSR using                Direct write
                                                     YADDRW.
stvec (RVY)        S-mode         YLEN               Apply Invalid address            Apply Invalid address
                                                     conversion. Always update the    conversion. Always update the
                                                     CSR with YADDRW even if the CSR with YADDRW even if the
                                                     address didn’t change, including address didn’t change, including
                                                     the MODE field in the address    the MODE field in the address
                                                     for simplicity. Vector range     for simplicity. Vector range
                                                     check if vectored mode is        check if vectored mode is
                                                     programmed.                      programmed.

18.5. Supervisor-mode YLEN CSR Summary | Page 154

RVY CSR           Extension         Width             Action on XLEN write              Action on YLEN write
sepc (RVY)        S-mode            YLEN              Apply Invalid address             Apply Invalid address
                                                      conversion. Always update the     conversion and update the CSR
                                                      CSR with YADDRW even if the       with the result if the address
                                                      address didn’t change.            changed, direct write if address
                                                                                        didn’t change
stidc             RVY               YLEN              Update the CSR using              Direct write
                                                      YADDRW.

Code pointers and data pointers in Table 62 are WARL CSRs that do not need to store full 64-bit addresses
on RV64, and so may be subject to Invalid address conversion on writing.

                   Table 62. YLEN-wide Supervisor-Mode CSRs storing code pointers or data pointers
RVY CSR                  Extension                Code Pointer           Data Pointer             Unseal On Execution
stvec (RVY)              S-mode                   ✔
sepc (RVY)               S-mode                   ✔                                               ✔

