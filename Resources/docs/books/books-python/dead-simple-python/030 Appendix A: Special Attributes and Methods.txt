                                   A
          SPECIAL ATTRIBUTES AND
                 METHODS

              This appendix is a categorized list of all
              the special attributes and methods in the
              Python language. If you’re looking for a
              particular special method by name, see
the index instead.
  Complete documentation can be found at https://docs.python.org/3/refere
nce/datamodel.html.

Special Methods
These methods are declared on a class to add support for various Python
operators, built-in functions, and compound statements. These are all
typically declared as instance methods, unless otherwise indicated.
Table A-1: Conversion and Coercion

Method                    Typical purpose                             Invocation
obj.__bool__(self)        Returns a boolean: True or False.           bool(obj)
                                                                      if obj: pass

obj.__int__(self)         Returns an integer.                         int(obj)

obj.__trunc__(self)       Returns an integer by truncating the        trunc(obj)
                          decimal part.
obj.__floor__(self)       Returns an integer by rounding down.        floor(obj)

obj.__ceil__(self)        Returns an integer by rounding up.          ceil(obj)

obj.__index__(self)       Returns an integer, converted               operator.index(obj)
                          losslessly (without omitting part of the
                          data). Indicates obj is the integer type.
obj.__float__(self)       Returns a floating-point number.            float(obj)

obj.__complex__(self)     Returns a complex number.                   complex(obj)

obj.__round__(self,       Returns a floating-point number,            float(obj)
ndigits=None)             rounded to ndigits precision.               float(obj, ndigits)
                          Returns an integer by rounding if
                          ndigits is None.

obj.__bytes__(self)       Returns a bytes object.                     bytes(obj)

obj.__repr__(self)        Returns a canonical representation as       repr(obj)
                          a string.                                   f"{obj!r}"

obj.__str__(self)         Returns a string.                           str(obj)

obj.__format__(self,      Returns a string, formatted according       format(obj,
format_spec="")           to format_spec.                             format_spec)
Table A-2: Capabilities and Traits

Method                        Typical purpose                       Invocation
obj.__index__(self)           Returns an integer, created           operator.index(obj)
                              losslessly. Indicates an object is
                              an integer.
obj.__call__(self)            Implementing makes obj                obj()
                              callable.
obj.__iter__(self)            Returns an iterator                   iter(obj)
                              corresponding to obj.
obj.__next__(self)            Returns the next value,               next(obj)
                              according to the iterator obj.
obj.__enter__(self)           Enters the runtime context            with obj:
                              related to the context manager        pass
                              obj.

obj.__exit__(self)            Exits the runtime context related     with obj:
                              to the context manager obj.           pass

obj.__len__(self)             Returns the length of obj, such       len(obj)
                              as the number of items in a
                              collection.
obj.__length_hint__(self)     Returns the estimated length for      operator.length_hint(obj)
                              obj as a non-negative integer.
                              Optional.
obj.__getitem__(self,         Returns an item in obj at key.        obj[key]
key)                          Ideally, it should be valid to pass
                              a slice to key.
obj.__setitem__(self,         Sets the value in obj of key to       obj[key] = value
key, value)                   value.

obj.__delitem__(self,         Deletes the value in obj of key.      del obj[key]
key)

obj.__missing__(self)         Called by __getitem__() method
                              of dict subclasses when key is
                              not in the dictionary.
obj.__reversed__(self)        Returns a new iterator object that reversed(obj)
                              iterates over objects in the
                              container, in reverse order.
                              Should only be provided if
                              implementation is faster than
                              using __len__() and
                              __getitem__() to iterate in
                              reverse.
obj.__contains__(self,        Checks if item is contained in        item in obj
item)                         obj, then returns a boolean.
Method                      Typical purpose                        Invocation
obj.__hash__(self)          Returns hash of obj.                   hash(obj)

obj.__get__(self,           Getter for a descriptor.
instance)

obj.__set__(self,           Setter for a descriptor.
instance, value)

obj.__delete__(self,        Deleter for a descriptor.
instance)

obj.__set_name__(self,      Initializer for a descriptor. Called
owner, name)                when the owning class owner is
                            created.
obj.__await__(self)         Makes obj awaitable. Returns an await obj
                            iterator.
obj.__aiter__(self)         Returns an asynchronous                async for i in obj:
                            iterator object.                       pass

obj.__anext__(self)         Returns an awaitable that yields       async for i in obj:
                            the next value of the                  pass
                            asynchronous iterator.
obj.__aenter__(self)        Same as __enter__() but returns async with obj:
                            an awaitable.                   pass

obj.__aexit__(self)         Same as __exit__() but returns         async with obj:
                            an awaitable.                          pass

Table A-3: Comparison Operations

Method                   Typical purpose                                        Invocation
obj.__lt__(self,         Returns True if self < other; or else, returns         self < other
other)                   False.

obj.__le__(self,         Returns True if self <= other; or else, returns        self <=
other)                   False.                                                 other

obj.__eq__(self,         Returns True if self == other; or else, returns        self ==
other)                   False.                                                 other

obj.__ne__(self,         Returns True if self != other; or else, returns        self !=
other)                   False.                                                 other

obj.__gt__(self,         Returns True if self > other; or else, returns         self > other
other)                   False.

obj.__ge__(self,         Returns True if self >= other; or else, returns        self >=
other)                   False.                                                 other
Table A-4: Arithmetic Operations

Method                      Typical purpose                       Invocation
obj.__add__(self, other)    Returns the sum of obj and other      obj + other
                            or concatenates obj and other.        operator.add(obj, other)

obj.__sub__(self, other)    Returns the difference of obj and     obj – other
                            other.                                operator.sub(obj, other)

obj.__mul_(self, other)     Returns the product of obj and        obj * other
                            other.                                operator.mul(obj, other)

obj.__matmul__(self,        Performs matrix multiplication of     obj @ other
other)                      obj and other.                        operator.matmul(obj,
                                                                  other)

obj.__truediv__(self,       Performs true division of obj and     obj / other
other)                      other.                                operator.truediv(obj,
                                                                  other)

obj.__floordiv__(self,      Performs floor division (integer      obj // other
other)                      division) of obj and other.           operator.floordiv(obj,
                                                                  other)

obj.__mod__(self, other)    Returns the remainder of division     obj % other
                            of obj and other.                     operator.mod(obj, other)

obj.__divmod__(self,        Performs both floor division and      divmod(obj, other)
other)                      modulo, then returns results in a
                            tuple. Same as (obj // other,
                            obj % other).

obj.__pow__(self, other,    Raises obj to the power other.        obj ** other
mod=None)                   If a mod is provided, the result is   pow(obj, other)
                            the same as pow(obj, other) %         pow(obj, other, mod)
                            mod, but calculated more              operator.pow(obj, other)
                            efficiently.
obj.__neg__(self)           Negation.                             -obj
                                                                  operator.neg(obj)

obj.__pos__(self)           Ordinarily, just returns self.        +obj
                                                                  operator.pos(obj, other)

obj.__abs__(self, other)    Absolute value.                       abs(obj)
                                                                  operator.abs(obj, other)

obj.__radd__(self, other)   Returns the sum of other and obj. other + obj
                            Reflected operators are only      operator.pos(other, obj)
                            called if other does not support
                            the operation.
obj.__rsub__(self, other)   Returns the difference of other       other – obj
                            and obj. (Reflected)                  operator.sub(other, obj)
Method                      Typical purpose                        Invocation
obj.__rmul__(self, other)   Returns the product of other and       other * obj
                            obj. (Reflected)                       operator.mul(other, obj)

obj.__rmatmul__(self,       Returns the result of matrix           other @ obj
other)                      multiplication of other and obj.       operator.matmul(other,
                            (Reflected)                            obj)

obj.__rtruediv__(self,      Returns the quotient of true           other / obj
other)                      division of other and obj.             operator.truediv(other,
                            (Reflected)                            obj)

obj.__rfloordiv__(self,     Returns the quotient of floor          other // obj
other)                      division (integer division) of other   operator.floordiv(other,
                            and obj. (Reflected)                   obj)

obj.__rmod__(self, other)   Returns the remainder of division      other % obj
                            of other and obj. (Reflected)          operator.mod(other, obj)

obj.__rdivmod__(self,       Performs both floor division and       divmod(other, obj)
other)                      modulo, then returns results in a
                            tuple. Same as (other // obj,
                            other % obj). (Reflected)

obj.__rpow__(self, other,   Raises other to the power obj.         other ** obj
mod=None)                   If a mod is provided, the result is    pow(other, obj)
                            the same as pow(other, obj) %          pow(other, obj, mod)
                            mod, but calculated more               operator.pow(other, obj)
                            efficiently.
obj.__iadd__(self, other)   Stores the sum of obj and other        obj += other
                            in obj.
obj.__isub__(self, other)   Stores the difference of obj and       obj -= other
                            other in obj.

obj.__imul__(self, other)   Stores the product of obj and          obj *= other
                            other in obj.

obj.__imatmul__(self,       Stores the result of matrix            obj @= other
other)                      multiplication of obj and other in
                            obj.

obj.__itruediv__(self,      Stores the quotient of true division obj /= other
other)                      of obj and other in obj.
obj.__ifloordiv__(self,     Stores the quotient of floor           obj //= other
other)                      division (integer division) of obj
                            and other in obj.
obj.__imod__(self, other)   Stores the remainder of division       obj %= other
                            of obj and other in obj.
obj.__ipow__(self, other)   Stores the result of obj, raised to    obj **= other
                            the power other in obj.
Table A-5: Bitwise Operations

Method                   Typical purpose                            Invocation
obj.__invert__(self)     Returns the bitwise inversion of obj.      ~obj
                                                                    operator.invert(obj)

obj.__and__(self,        Returns the bitwise AND of obj and         obj & other
other)                   other.                                     operator.and(obj,
                                                                    other)

obj.__or__(self,         Returns the bitwise OR of obj and          obj | other
other)                   other.                                     operator.or(obj, other)

obj.__xor__(self,        Returns the bitwise XOR of obj and         obj ^ other
other)                   other.                                     operator.xor(obj,
                                                                    other)

obj.__lshift__(self,     Returns the bitwise left shift of obj and obj << other
other)                   other.                                    operator.lshift(obj,
                                                                    other)

obj.__rshift__(self,     Returns the bitwise right shift of obj     obj >> other
other)                   and other.                                 operator.rshift(obj,
                                                                    other)

obj.__rand__(self,       Returns the bitwise AND of other and       other & obj
other)                   obj. Reflected operators are only          operator.and(other,
                         called if other does not support the       obj)
                         operation.
obj.__ror__(self,        Returns the bitwise OR of other and        other | obj
other)                   obj. (Reflected)                           operator.or(other, obj)

obj.__rxor__(self,       Returns the bitwise XOR of other and       other ^ obj
other)                   obj. (Reflected)                           operator.xor(other,
                                                                    obj)

obj.__rlshift__(self,    Returns the bitwise left shift of other    other <<= obj
other)                   and obj. (Reflected)                       operator.lshift(other,
                                                                    obj)

obj.__rrshift__(self,    Returns the bitwise right shift of other   other >> obj
other)                   and obj. (Reflected)                       operator.rshift(other,
                                                                    obj)

obj.__iand__(self,       Stores the result of the bitwise AND of    obj &= other
other)                   obj and other in obj.

obj.__ior__(self,        Stores the result of the bitwise OR of     obj |= other
other)                   obj and other in obj.

obj.__ixor__(self,       Stores the result of the bitwise XOR of obj ^= other
other)                   obj and other in obj.

obj.__ilshift__(self,    Stores the result of the bitwise left      obj <<= other
other)                   shift of obj and other in obj.
Method                  Typical purpose                          Invocation
obj.__irshift__(self,   Stores the result of the bitwise right   obj >>= other
other)                  shift of obj and other in obj.
Table A-6: Objects, Classes, and Metaclasses

Method                           Typical purpose                   Invocation
obj.__new__(cls, ...)            Returns a new instance of         obj = cls()
                                 cls.

obj.__init__(self, ...)          Initializer for the object obj.   obj = cls()

obj.__del__(self)                Finalizer for the object obj.     del obj # indirect only

obj.__import__(self, ...)        DO NOT IMPLEMENT!
obj.__getattr__(self, name)      Returns an attribute name on      obj.name
                                 obj.

obj.__setattr__(self, name,      Assigns a value value to an       obj.name = value
value)                           attribute name on obj.
obj.__delattr__(self, name)      Deletes an attribute name on      del obj.name
                                 obj.

obj.__dir__(self)                Returns a list of valid           dir(obj)
                                 attributes on obj.
@classmethod                     Called when cls is
obj.__init_subclass__(cls)       subclassed. Returns nothing.
cls.__instancecheck__(self,      Returns True if instance is an isinstance(instance,
instance)                        instance of cls or an instance cls)
                                 of one of its subclasses; or
                                 else, returns False.
                                 Returns True if subclass is a
  cls.__subclasscheck__(self,                                        issubclass(instance,
                                 subclass of cls or one of its
subclass)                                                          subclass)
                                 subclasses; or else, returns
                                 False.

  @classmethod
                                 Returns True if subclass          issubclass(instance,

obj.__subclasshook__(cls,
                                 should be considered a            subclass)
                                 subclass of cls; or else,
subclass)
                                 returns False.

@classmethod                     Returns an object that is the
obj.__class_getitem__(cls,       specialization of a generic
key)                             class cls. Mainly used with
                                 generic types. See PEP 484
                                 and PEP 560.
meta.__new__(self, name,         Returns a new class               cls = meta()
bases, namespace)                instantiated from the
                                 metaclass meta.
@classmethod                     Creates and returns the
meta.__prepare__(cls, name,      dictionary storing all the
Method                             Typical purpose              Invocation
bases, ...)                        methods and class attributes
                                   for the class being created.

Special Attributes
Some special attributes that are only interesting or useful to the language’s
internals are omitted here.
Table A-7: Special Attributes on Modules

Method        Typical purpose
__doc__       The docstring on the module. If no docstring is defined, this has the value
              None.

__name__      The name of the module.
__loader__    The loader that loaded the module. Used by the import system.
__package__   The package containing the module. If the module is a package, this is the
              same as __name__. Used by the import system.
__file__      The path to the module file. May be None in some cases. Used by the import
              system.
__path__      The path to the package. Only defined on packages. Used by the import
              system.
__cached__    The path to the compiled Python bytecode representation of the module.
__spec__      The module’s corresponding ModuleSpec object. This is a technical detail
              used by the import system. The object contains much of the same data as
              the other special attributes on the module.
__builtins__ The module containing all of Python’s built-in functions, objects, and
              attributes (everything you never have to import).
Table A-8: Special Attributes on Functions

Method                 Typical purpose
func.__annotations__ A dictionary of type annotations for parameters. The key 'return' is
                       associated with the type annotation for the return value.
func.__closure__       A read-only tuple of cells, special objects containing bindings to
                       values in memory, for all closure variables.
func.__code__          A string representation of the function’s compiled code. (If you want
                       the string representation of the function’s code instead, use
                       inspect.getsource().)

func.__defaults__      A tuple containing default arguments. The value is simply None if
                       there are no default arguments.
func.__dict__          A dictionary containing all function attributes.
func.__doc__           The docstring on the function. If no docstring is defined, this has the
                       value None.
func.__globals__       A read-only reference to the dictionary of global variables.
func.__kwdefaults__    A dictionary of default arguments for keyword-only parameters.
func.__module__        The name of the module where the function was defined, or None if
                       not applicable.
func.__name__          The name of the module, callable, object, or class.
func.__qualname__      The fully qualified name of the object.

Table A-9: Special Attributes on Instances (Objects)

Method          Typical purpose
obj.__class__   The instance’s class.
obj.__dict__    The dictionary of instance attributes.
obj.__doc__     The instance’s docstring.
obj.__module__ The name of the module where the instance was defined, or None if not
                applicable.
Table A-10: Special Attributes on Classes

Method            Typical purpose
cls.__bases__     The tuple of base classes for the class, in the order specified in the
                  inheritance list when the class was defined.
cls.__dict__      The dictionary containing the class attributes and instance methods.
cls.__doc__       The docstring on the module. If no docstring is defined, this has the
                  value None.
cls.__module__    The name of the module where the class was defined, or None if not
                  applicable.
cls.__mro__       The method resolution order of the class, represented as a tuple of
                  classes.
cls.__name__      The name of the class.
cls.__qualname__ The fully qualified name of the class.

cls.__slots__     A sequence of variable names as strings that should be reserved. When
                  defined, this prevents the automatic creation of the __dict__ and
                  __weakref__ attributes, unless these names are specified in __slots__.

cls.__weakref__   An object that references all weak references to the current object. You
                  should never have to work with this attribute directly, but it’s good to be
                  aware of its purpose when working with cls.__slots__ (see that entry).
