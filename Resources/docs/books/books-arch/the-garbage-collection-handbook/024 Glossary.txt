Glossary

A comprehensive glossary can also be found at http://www.memorymanagement.org.

ABA problem the inability of certain ambiguous pointer a value that may or
   atomic operations to distinguish reading     may not be a true pointer to an object; see
   the same value twice from a memory lo-       conservative collection
   cation as ‘nothing changed’ versus some ambiguous root a root that is an ambigu-
   other thread(s) changing the value after     ous pointer
   the first read and then changing it back
   before the second read                     arbitrary compaction a compaction tech-
                                                nique that moves objects without regard
accurate see type-accurate
                                                for their original order in memory or any
activation record a record that saves the       reference relationship; see compaction
   state of computation and the return ad-      order
   dress of a method, also called a (stack)
   frame                                      arraylet a fixed-size chunk representing
                                                some subset of the cells of an array
address conventionally, a fixed-length un-
   signed integer representing the location asymmetric multicore processor a multi-
   of a datum in virtual or physical memory     core processor where the cores are het-
                                                erogeneous, typically in terms of pro-
address space a set of addresses, physical
                                                cessing power (and energy consumption),
   or virtual, available to the program
                                                though possibly also in their instruction
age-based collection a collection tech-         set
   nique that partitions the heap into spaces
   by age                                     atomic the property of a primitive opera-
                                                tion that it appears to execute indivisibly
aging space a subspace of a generation,         and instantaneously with respect to other
   typically the youngest generation, into      primitive operations
   which objects are copied for a few collec-
   tions before promoting them                atomic add, decrement, exchange and in-
                                                crement primitives that atomically mod-
alignment hardware or virtual machine
                                                ify a memory location using the indicated
   constraints may require that objects and
                                                operation; see also compare-and-swap,
   fields be stored only on certain address
                                                compare-and-set, fetch-and-add, fetch-
   boundaries
                                                and-or, load-linked/store-conditionally
allocation the action of finding a suitable     and test-and-set
   free cell and making it available for use
allocation site a location in code that allo- barrier (read, write, etc.) an action (typi-
   cates a cell or object                       cally a sequence of code emitted by the
allocator the memory manager component          compiler) mediating access to an object;
   responsible for creating (allocating) ob-    see also read barrier, write barrier, stack
   jects (but not initialising them)            barrier, load barrier and store barrier

belt a set of increments used by the Belt- branch predictor a unit in the CPU that
  way collector                              tries to predict the target of each branch
best-fit allocation a free-list allocation   in order to avoid stalls in the instruction
  strategy that places an object in a cell   pipeline
  in the heap that most closely matches the breadth-first traversal a traversal of the
  object’s size                               object graph in which an object’s siblings
big bag of pages allocation (BiBoP)       a   are visited before its descendants (refer-
  segregated-fits allocation strategy that    ents)
  places objects with the same attribute break table a table of forwarding ad-
  (such as type) in the same block, thus al-   dresses placed by a mark-compact collec-
  lowing the type to be associated with the    tor in the break between already moved
  block rather than with individual objects    and yet to be moved objects
bit stealing using one or more low- or bucket a subspace used to segregate ob-
  high-order bits of a word to indicate the    jects by age within a step
  type or some other attribute of the word’s bucket brigade an approach to genera-
  value, such as distinguishing pointers       tional collection using buckets
  from other values or indicating the space
                                             buddy system a segregated-fits allocation
  to which a tagged pointer refers
                                               strategy that allocates blocks, typically in
bitmap an array of bits (or often, bytes),     power of two sizes to allow easy splitting
  each associated with an object or granule    and coalescing; see also Fibonacci buddy
bitmapped-fits allocation a sequential fits    system
  allocation strategy that uses a bitmap to   buffered reference counting a form of ref-
  record the free granules in the heap          erence counting in which mutators buffer
black an object is black if the collector has   reference counting operations and sub-
  finished processing it and considers it to    sequently send them for execution to the
  be live (and by extension, a mutator if it    collector
  cannot access white objects); see also tri- bump pointer allocation see sequential al-
  colour abstraction                            location
blacklisting an address range that has byte map see bitmap
  been found to be a target of a false
  pointer may be blacklisted in conserva- cache a fast memory which stores copies
  tive collection to prevent space leaks     of data from frequently accessed memory
block an aligned chunk of a particular size, locations
  usually a power of two                       cache block see cache line
block-based allocation see    big   bag   of   cache coherence the degree to which two
  pages allocation                               or more caches agree on the contents of
                                                 memory
block-structured heap a heap comprised
  of a set of typically contiguous blocks of cache hit a memory access that finds its
  memory, rather than a single contiguous      containing cache line in the cache
  range of addresses                         cache line the unit of memory that can be
boundary tag structures on the boundaries      transferred between the cache and mem-
  of blocks that assist coalescing             ory
bounded mutator utilisation (BMU) the cache miss a memory access that does not
  minimum mutator utilisation observed    find its containing cache line in the cache
  for a given time window or any larger call stack a stack holding the stack frames
  one; BMU is monotonically increasing,   (also called activation records) of the
  whereas MMU is not                      methods being executed by a thread

car (1) the unit of collection in the Mature closure a record consisting of a function
  Object Space (‘Train’) collector; (2) the      and bindings (values) for one or more of
  word in a cons cell that holds or points       the variables it uses; see also thunk
  to the list element, from ‘Content of the
                                               coalesced reference counting a form of
  Address part of the Register’ referring to
                                                 buffered reference counting that avoids
  an early Lisp implementation
                                                 applying redundant reference count op-
card a small, power of two sized and             erations
  aligned area of the heap
                                               coalescing recombining adjacent free cells
card marking a technique used by the mu-         into a single cell; see also segregated-fits
  tator to record the source of pointers of      allocation
  interest to the collector; the write barrier
  updates a card table                         coherence protocol a cache management
causal consistency a store A is said to          protocol for maintaining some memory
  causally precede another store B if the pro-   consistency model
  cess performing store B observed store           collection a single instance of executing a
  A before performing store B; a system is           collector; typically this instance will re-
  causally consistent if whenever some store         claim at least all objects in the space be-
  A causally precedes another store B, all           ing collected that were dead at the time
  processes must observe A and B in that             that the collector was called
  order
                                               collection cycle a complete execution of
cdr the word in a cons cell that holds or        the collector
  points to the next cons cell in the list,
  from ‘Content of the Decrement part of collector a system component responsible
  the Register’ referring to an early Lisp im-   for garbage collection
  plementation                                 compacting relocating marked (live) ob-
cell a contiguous group of granules, which       jects and updating the pointer values of
  may be allocated or free, or even wasted       all live references to objects that have
  or unusable                                    moved; its purpose is to eliminate exter-
channel a single-reader, single-writer com-      nal fragmentation
  munication mechanism, for example, be-    compaction order the order in which a
  tween two threads                           compacting collector rearranges objects;
checkpoint a persistent record of the state   this may be arbitrary, linearising or slid-
  of a heap at a moment in time               ing
Cheney scanning a technique used with compare-and-set an atomic instruction that
  copying collection for tracing live ob-      compares a memory location to an ex-
  jects without using a stack                  pected value old, and if the location’s
chip multiprocessor (CMP) a multiproces-       value equals old, sets the value to new.
  sor that has more than one processor on      In either case it indicates whether or not
  a single integrated circuit chip; see also   it updated the memory location; see also
  multicore processor and many-core pro-       atomic
  cessor                                     compare-and-swap (also called compare-
chunk a large contiguous group of gran-              and-exchange) like compare-and-set but
  ules                                               returns the value of the memory location
circular first-fit allocation see next-fit allo-     observed before any update rather than
   cation                                            returning a boolean truth value; see also
                                                     atomic
class loader An object responsible for lo-
  cating and loading classes, usually on de-       completeness the property of a collector
  mand                                               that guarantees to reclaim all garbage

  eventually; for example, reference count-     crossing map a map that decodes how ob-
  ing is not complete because it cannot re-       jects span areas (typically cards)
  claim cycles of dead objects                  cyclic data structure a self-referential data
compute unit OpenCL term for a set of             structure
  processing elements in a GPU along with
  other elements such as an instruction de- dangling pointer a pointer to an object
  coder, instruction cache and data caches      that has been reclaimed by the memory
  or memory shared by its processing ele-       manager
  ments; called a streaming multiprocessor on
                                              dead an object is dead if it cannot be ac-
  CUDA or a subslice on Intel
                                                cessed at any time in the future execution
concurrent collection execution of the          of the mutator, usually approximated by
  garbage collector concurrently with ex-       the object’s being unreachable
  ecution of mutator threads                  deallocation the action of freeing an allo-
condemned space the space or subspace          cated cell
  chosen to be collected, typically by evac- deferred reference counting a reference
  uating its live contents to another space    counting scheme in which some refer-
connectivity-based (garbage) collection a      ence count operations (typically those on
  collection technique that partitions the     local variables) are deferred to a later time
  heap into spaces based on connectivity     deletion barrier a write barrier that de-
cons cell a double-word cell used by Lisp      tects the removal of a reference; see also
  for storing the spine of a list              snapshot-at-the-beginning
conservative collection a technique for col- dependent load a load from a memory lo-
  lection that receives no assistance from a   cation whose address depends on the re-
  language’s compiler or run-time system       sult of a prior load
  and so must make over-estimates of the depth-first traversal a traversal of the ob-
  set of live objects                          ject graph in which an object’s descen-
consistency model a specification of how       dants (referents) are visited before its sib-
  the memory system will appear to the         lings
  programmer, placing restrictions on the       derived pointer a pointer obtained by
  values that can be returned by a read in        adding an offset to an object reference
  a shared-memory program execution        direct collection a collection algorithm
copy reserve a space reserved as a target    that determines whether an object is live
  for copying in copying collection          simply from that object itself, in contrast
copying see evacuating                       to indirect collection; an example is ref-
                                             erence counting, particularly eager refer-
copying collection collection that evacu-    ence counting
  ates live objects from one semispace to
                                           discrete GPU a card dedicated to the GPU,
  another (after which, the space occupied
                                             in contrast to an integrated GPU
  by the former can be reclaimed)
                                           disentangled a property of a hierarchy of
core (1) a processor of a multicore or
                                             heaplets in which an object may only ref-
  many-core processor; (2) the main mem-
                                             erence another object if it is in the same
  ory of a system, even though magnetic
                                             heaplet or in an ancestor heaplet; thus,
  cores are no longer used
                                             heaplets cannot contain down-pointers
creation space see nursery                   or cross-pointers
critical section a section of code in which a   double mapping a technique that maps the
   thread manipulates a shared resource; see      same physical page at two different vir-
   also mutual exclusion                          tual addresses with different protections

double-ended queue (deque) a data struc-    false sharing the coincidence of different
  ture allowing adding to and removing        processors accessing different memory
  from both the front (head) and back (tail)  locations that happen to lie in the same
durability the ability of changes to a per-   cache line, resulting in increased cache
  sistent heap to survive failures and out-   coherence traffic
  live the execution of a program           fast (slow) path most memory managers
dynamic voltage and frequency scaling         take care to optimise the common case
  (DVFS) adjusting the voltage and clock      (fast path) for allocation or barriers; the
  frequency of a processor while it runs,     fast path is typically inlined code with
  typically to control the tradeoff between   the less common case (slow path) han-
  speed and energy consumed                   dled with a procedure call
                                            fast-fits allocation a sequential fits alloca-
eager reference counting an eager refer-      tion strategy that uses an index to search
  ence counting memory manager applies        for the first or next cell that satisfies the
  reference count increments and decre-       allocation request
  ments immediately, rather than deferring
  them; see also buffered reference count- fat pointer a pointer that also holds addi-
  ing, coalesced reference counting, de-      tional metadata, for example, a version
  ferred reference counting, lazy reference   number
  counting                                  fence see memory fence
eager sweeping sweeping immediately as fetch-and-add and fetch-and-or (also
  opposed to lazy sweeping                      called exchange-and-add) an atomic in-
eden see nursery                                struction that atomically increments the
                                                value of a location by a specified value
en masse promotion generational collec-
                                                (fetch-and-add), or performs a logical or
  tion scheme in which all survivors of a
                                                of the value of the location with a speci-
  minor collection are promoted
                                                fied value (fetch-and-or), and returns the
epoch a period of execution of a reference      location’s original value; see also atomic
  counting collector during which syn-
  chronised operations can be eliminated or Fibonacci buddy system a buddy system
  replaced by unsynchronised operations         in which the size classes form a Fibonacci
                                                sequence
escape analysis an analysis (usually static)
  that determines whether an object may field a component of an object holding a
  become reachable from outside the             reference or scalar value
  method or thread that created it           filler object a pseudo-object allocated in
evacuating moving an object from a con-        the gaps between real objects to support
  demned space to its new location (in to-     heap parsability
  space); see copying collection and mark- finalisation an action performed on an ob-
  compact collection                           ject when it is determined to be no longer
explicit deallocation the action of deallo-    reachable
  cation under the control of the program-
                                            finaliser a method that runs when the col-
  mer, rather than automatically
                                               lector determines that the object is no
external fragmentation space wasted out-       longer reachable
  side any cell; see also internal fragmen-
  tation                                    first-fit allocation a free-list allocation
                                               strategy that places an object in the first
                                               cell in the heap that can contain the ob-
false pointer a value that was falsely as-
                                               ject
  sumed to be a pointer to an object; see
  conservative collection                   first-in, first-out (FIFO) see queue

flip the swapping of fromspace and to-             loop; every GC check-point must be a
   space in copying collection at the start of     GC safe-point, but not necessarily vice
   a collection cycle                              versa
floating garbage garbage that was not re-        GC-point see GC safe-point
   claimed in a previous collection cycle      GC safe-point a point in the mutator code
footprint (memory) the volume of memory          at which it is safe for garbage collection
  that a program uses while running              to occur, although a collection might or
                                                 might not actually be triggered here on
forward (1) cause mutators to refer to the
                                                 a given occasion; certain points in code,
  tospace copy of an object; (2) insert a for-
                                                 such as allocations or where a thread
  warding address
                                                 might suspend, must be GC safe-points
forwarding address the address to which generation a space characterised by the age
  an object has been evacuated (stored in        of the objects it contains
  the fromspace object’s header or in side
  metadata)                                    generational   collection collection that seg-
                                                 regates objects by age into generations
fragmentation the inability to use free          and preferentially collects the youngest
  memory because of the arrangement of           generation
  objects; see also internal fragmentation
                                               generational hypothesis a hypothesis that
  and external fragmentation
                                                 object lifetime is correlated with age; see
frame (1) a power of two sized and aligned       also weak generational hypothesis and
  chunk (typically a discontiguous space         strong generational hypothesis
  comprises a number of frames); (2) see ac-
                                               gibibyte (GiB) standard usage unit mean-
  tivation record
                                                 ing 230 bytes; see also gigabyte
free the state of a cell that is available for gigabyte (GB) common usage unit mean-
  reallocation; see also reclaim                 ing 230 bytes; see also gibibyte
free pointer a pointer to the free granules globalise in a thread-local collector, the ac-
  of a chunk; see sequential allocation          tion of making a local object accessible
free-list allocation a sequential fits alloca-   to other threads, typically by copying it
  tion strategy that uses a data structure to    from a thread-local heaplet to the shared,
  record the location and size of free cells     global heap
fromspace the semispace from which granule the smallest unit of allocation, say
  copying collection copies objects              a word or a double-word
fromspace invariant the invariant that the graphics processing unit (GPU) a spe-
  mutator holds only fromspace references        cialised processor with a large number
                                                 of cores, originally designed to accel-
                                                 erate 3D graphics but now also widely
garbage an object that is not live but whose
                                                 used to accelerate workloads in high per-
  space has not been reclaimed
                                                 formance computing, deep learning and
garbage collection (GC) an automatic ap-         other applications
  proach to memory management that re-
                                               grey an object is grey if the collector has
  claims memory occupied by objects that
                                                 determined that it is reachable but has
  are no longer in use by the program (mu-
                                                 not yet finished processing it (and by ex-
  tator)
                                                 tension, a mutator if it may access white
garbage collector see collector                  objects); see also tricolour abstraction
GC check-point a point in mutator code grey packet a unit of collector work used
 that may trigger garbage collection, but  for load balancing that contains a fixed
 where collection would not otherwise be   number of grey objects for a collector to
 triggered, such as a branch that closes a process; see also tricolour abstraction

guard page a page mapped with no-access immutable data that does not change, in
  protection so as to cause a trap when     constrast to mutable data
  accessing beyond an allowed limit, thus
                                          increment (1) a unit of collection in the
  avoiding the need for an explicit range
                                            Beltway collector; (2) see incremental
  limit check
                                            collection

handle a structure holding a reference to       incremental collection collection in which
  an object, and optionally additional sta-       the mutator performs small quanta of
  tus information; typically, a handle is not     collection work interlaced with its own
  moved by the collector whereas its target       work; see also concurrent collection
  object might be                               incremental update a technique for solv-
handshake a protocol for mutator and col-         ing the lost object problem that informs
  lector threads to rendezvous at garbage         the  collector of incremental changes
  collection phase boundaries                     made  by the mutator to the set of objects
                                                  known to be live
happens-before a requirement on the order
  in which operations occur on memory           incremental update barrier see insertion
                                                  barrier
hard real-time system a real-time system
  in which all deadlines must be met within indirect collection a collection algorithm
  a strict bound; missing a deadline is a crit-   that does not detect garbage per se, but
  ical system failure                             rather identifies all live objects and then
header a part of an object used to store          concludes that anything else must be
  metadata used by the run-time system            garbage

headroom additional space for allocation insertion barrier a write barrier that de-
  required to avoid thrashing tracing col-    tects the insertion of a reference; see also
  lectors and those reference counting col-   incremental update
  lectors that do not reclaim garbage im- integrated GPU a system where the GPU is
  mediately                                   built onto the same die as the CPU, in con-
heap a data structure in which objects may    trast to a discrete GPU
  be allocated or deallocated in any order, interior pointer a derived pointer to a field
  independent of the lifetime of the method   of an object
  invocation that created them
                                            internal fragmentation space wasted in-
heap allocation allocation of an object in    side a cell, for example due to rounding
  the heap                                    up requested sizes; see also external frag-
heap parsability the capability to advance    mentation
  through the heap from one object to the
  next
                                            JVM a virtual machine for the Java lan-
heap reference graph a compact represen-      guage
  tation of the heap, typically intended to
  be better suited to processing by a GPU
                                            kernel (1) a procedure executed on the
heaplet a subset of the heap containing ob-   GPU; (2) the privileged controlling code
  jects accessible to only a single thread    of the operating system
hyperthreading see simultaneous multi-
                                            kibibyte (KiB) standard usage unit mean-
  threading
                                              ing 210 bytes; see also kilobyte

immediacy the degree to which a collector kilobyte    (KB) common usage unit meaning
                                              210 bytes; see also kibibyte
  reclaims garbage shortly after it dies; re-
  lated to promptness

large object space (LOS) a space reserved         of a (concurrent or incremental) collector
   for objects larger than a given threshold,     that it eventually completes its collection
   and typically managed by a non-moving          cycle
   collector                                    load balancing distributing work across
last-in, first-out (LIFO) see stack               available hardware resources in a way
                                                  that minimises the coordination neces-
latency the delay from the start of some ac-
                                                  sary yet keeps all processors as busy as
   tion to its completion, typically from a re-
                                                  possible
   quest delivered to an application until the
   application’s response                       load barrier (also called a loaded value bar-
                                                  rier) a barrier on loads by the mutator
lazy reference counting deferring freeing
                                                  and possibly the collector, distinguished
   of objects with reference counts of zero
                                                  by the fact action may be taken with
   until they are subsequently acquired by
                                                  respect to the value or reference being
   the allocator, at which point their chil-
                                                  loaded rather than the reference being
   dren can be processed
                                                  used; a load barrier may be concerned
lazy sweeping sweeping only on demand             with low-level reference encoding, with
   (when fresh space is required)                 properties such as the space in which the
leak see memory leak                              referent lies, or with both
limit pointer a pointer to the end of a load-linked/store-conditionally (also
   chunk; see sequential allocation               called load-and-reserve/store-conditional
                                                  or load-exclusive/store-exclusive) atomic
line an aligned chunk used by the immix           instruction pair that first reserves a loca-
   collector and its relatives, roughly sized     tion and then updates it only if the pro-
   to match the architecture’s cache line         cessor’s coherence protocol confirms that
linear allocation see sequential allocation       its value has not been changed since the
linearisable an execution history of con-         reservation was made; see also atomic
  current operations that appear to execute      loaded value barrier (LVB) see load bar-
  serially in some non-overlapped way,             rier
  where if two operations do not overlap in      local allocation buffer (LAB) a chunk of
  the history then they must appear to hap-        memory used for allocation by a single
  pen in the order they were invoked               thread
linearisation point the point in time at locality the degree to which items (fields,
   which an operation in a linearisable his-     objects) are accessed together in space or
   tory appears instantaneously to occur         time; see also spatial locality and tempo-
linearising compaction a compaction tech-        ral locality
   nique that attempts to place objects next location an addressable unit of memory,
   to those that they reference; see com-        having a unique address
   paction order
                                               lock a synchronisation mechanism for con-
live an object is live if it could be accessed   trolling access to a resource by multi-
   at some time in the future execution of the   ple concurrent threads; usually only one
   mutator; usually this is approximated by      thread at a time can hold the lock, while
   the object’s being reachable                  all other threads must wait
livelock a situation in which two (or more)      lock-free a guarantee of system-wide
   competing threads prevent progress of           progress, although individual threads
   the other(s) indefinitely                       may fail to make progress; implies
liveness (1) the property of an object that it     obstruction-free; see also non-blocking
   will be accessed at some time in the future   log (1) a record, often sequential, related to
   execution of the mutator; (2) the property      changes made to a data structure, such as

  a persistent heap; see also redo log and      marking all live objects and then sweep-
  undo log; (2) a record related to remem-      ing through the heap, reclaiming the
  bered sets, buffered reference counting       storage of all unmarked, and hence dead,
  and so on                                     objects
lost object problem a situation that can mark/cons ratio a common garbage collec-
  arise when interleaved execution results   tion metric that compares the amount of
  in the mutator’s hiding references from    work done by the collector (‘marking’)
  the collector so that the collector erro-  with the amount of allocation (‘consing’)
  neously reclaims live objects              done; see cons cell
                                            marking recording that an object is live, of-
major collection collection of both the old  ten by setting a mark bit
  generation and young generation; see mature object space (MOS) a space re-
  also generational collection               served for older (mature) objects man-
malloc a function in the C standard library     aged without respect to their age in the
 that allocates memory in the heap              Mature Object Space collector
managed code application code running         mebibyte (MiB) standard usage unit mean-
 on a managed run-time                         ing 220 bytes; see also megabyte
managed language a programming lan- megabyte (MB) common usage unit mean-
 guage that has a managed run-time that  ing 220 bytes; see also mebibyte
 supports managed code                  memory fence an operation on a processor
managed run-time a run-time system that  that prevents certain reorderings of mem-
 provides services such as automatic     ory accesses
 memory management                      memory leak a failure to reclaim memory
many-core processor a kind of multi-     that is no longer in use by the program
 processor that has a large number of memory manager component responsible
 processors on a single integrated circuit     for allocating and reclaiming memory
 chip; sometimes distinguished from a memory order the order of writes (and
 multicore processor in that the cores of      reads) to multiple memory locations at
 a many-core processor are smaller and         caches or memories, and thus as per-
 simpler                                       ceived by other processors; see also pro-
mark bit a bit (stored in the object’s header  gram order
 or on the side in a mark bitmap) record- memory protection keys a hardware fea-
 ing whether an object is live                 ture that associates additional memory
mark stack (or queue) a stack (or queue)       protection restrictions with chosen pages
 used as the work list for the marking pro-    of memory under programmer control
 cess                                         meta-object an object that describes an-
mark-compact collection a tracing collec-      other object, typically providing informa-
 tion that typically operates in three or      tion on the layout of fields
 more phases, first marking all live ob- metadata data used by the virtual machine
 jects and then compacting these objects       or memory manager but not part of the
 and updating references to them to elim-      running application
 inate fragmentation                          minimum mutator utilisation (MMU) the
mark-region collection marking both ob-        minimum mutator utilisation for a given
 jects and lines, used by the immix collec-    time window
 tor                                          minor collection collection of only the
mark-sweep collection a tracing collection     young generation or nursery; see also
 that typically operates in two phases, first  generational collection

mmap a Unix system call that creates a         cell in the heap that can contain the ob-
 mapping for a range of virtual addresses      ject
mostly-concurrent collection a technique     node see object
 for concurrent collection that may pause  non-blocking a guarantee that threads
 all mutator threads briefly                 competing for a shared resource do not
mostly-copying collection a technique for    have their execution delayed indefinitely;
 copying collection that copies most ob-     see also obstruction-free, lock-free, wait-
 jects but does not move others (because     free
 of pinning)                               non-uniform memory access (NUMA) a
moving collection any collection tech-       multiprocessor in which a shared mem-
 nique that moves objects                    ory unit is associated with each proces-
                                             sor, giving that processor faster access to
multi-tasking virtual machine a    virtual
                                             that memory unit
 machine that runs several applications
 (tasks) within a single invocation of the non-volatile memory (1) memory whose
 virtual machine                             state is not volatile, that is, which is
                                             retained without application of external
multicore processor see chip multiproces-
                                             power; (2) more specifically, non-volatile
 sor and many-core processor
                                             byte-addressable main memory, as op-
multiprocessor a computer that provides      posed to external memory accessed via
 more than one processor                     block transfer
multiprogramming the execution of multi-     Not-Marked-Through (NMT) a state of a
 ple processes or threads on a single pro-    reference indicating that it has not been
 cessor                                       traced through by the Pauseless/C4 col-
multitasking the execution of multiple        lector to assure that its target object has
 tasks on a single processor                  been marked
multithreading the execution of multiple     null a distinguished reference value that
 threads on one or more processors             does not refer to any object
                                             nursery a space in which objects are cre-
mutable data that may be changed, in con-
                                               ated, typically by a generational collector
 strast to immutable
mutator the user program, so called be-      object a cell allocated for use by the appli-
 cause from the collector’s point of view      cation
 it simply mutates the graph of objects      object inlining an optimisation technique
mutator utilisation the fraction of CPU        that replaces a reference to a unique ob-
 time used by the mutator, as opposed to       ject with that object’s fields; see also
 the collector                                 scalar replacement
mutual exclusion an approach to avoiding     object table a table of handles, which refer
 concurrent use of a shared resource; see      to objects
 also critical section                       oblet a fixed-size chunk representing some
                                               subset of the cells of an object
nepotism the situation where a dead ob- obstruction-free a guarantee that at any
  ject in an uncollected space preserves an point, a single thread executed in isola-
  otherwise dead object in the condemned    tion (that is, with all obstructing threads
  space                                     suspended) will complete its operation
newspace the space in which objects are     within a bounded number of steps; see
  allocated                                 also non-blocking
next-fit allocation a free-list allocation old generation a space into which objects
  strategy that places an object in the next are promoted or tenured

on-stack replacement a technique for re-          collector-aware); (2) forcing a particular
  placing a method’s code with new code           thread to run on a particular core
  while it still has active invocations    pointer a value that is an address in mem-
on-the-fly collection a technique for con-   ory, typically of an object
  current collection that stops mutator pointer field a field that contains a pointer
  threads at most one at a time              to another object
                                               pointer swizzling The conversion of an ob-
padding extra space inserted by the alloca-
                                                 ject reference between its internal and ex-
  tor to meet alignment constraints
                                                 ternal form
page a block of virtual memory
                                              prefetching fetching a value into the cache
paging the transfer of memory in a virtual      earlier than it would naturally be fetched
  memory system between main memory
                                              prefetching on grey fetching the first cache
  and secondary storage (also called swap-
                                                line of an object when that object is
  ping); if overly active, called thrashing
                                                marked grey
parallel collection use of multiple proces-
                                              pretenuring allocating an object directly
  sors or threads to perform collection; not
                                                into an old generation
  to be confused with concurrent collection
                                              primitive (instruction) an instruction di-
partial tracing tracing only a subset of the
                                                rectly executable by a hardware processor
  graph of objects; typically used to refer
  to a trial deletion algorithm that traces process an instance of a computer program
  a subgraph that is suspected of being         that is executing within its own address
  garbage                                       space; a process may comprise multiple
                                                threads executing concurrently
pause time the time during which muta-
  tors are halted while stop-the-world col- processing element OpenCL term for an
  lection runs                                  Arithmetic-Logic Unit (ALU) on which
                                                work-items execute; the CUDA term is
persistence the ability of a heap and of ob-    streaming processor
  jects within it to outlive the execution of
  a program                                   processor (CPU) a unit of hardware that ex-
                                                ecutes (primitive) machine instructions
persistence by reachability persistence of
  objects by reachability from persistent program order the order of writes (and
  roots                                         reads) to multiple memory locations as
                                                they appear to be executed in the pro-
persistent root a root whose transitive clo-    gram’s machine code; see also memory
  sure is made to persist                       order
phantom reference in Java, the weakest prolific type an object type having many
  kind of reference; phantom references are     instantiations
  typically used for scheduling cleanup ac-
  tions more flexibly than is possible with promote move an object into an old gener-
  finalisation                                  ation in a generational collector or to an
                                                ancestor heaplet in a thread-local collec-
phase a part of a collection cycle that does    tor
  particular work or expects or establishes
  particular invariants                       promptness    the degree to which a collector
                                                reclaims all garbage at each collection cy-
physical memory the memory that is actu-        cle; related to immediacy
  ally available on a given machine; see also
  virtual memory                              queue a first-in, first-out data structure, al-
pinning (1) preventing a collector from           lowing adding to the back (tail) and re-
  moving a particular object (typically be-       moving from the front (head)
  cause it is accessible to code that is not

ragged handshake a handshake that is            the expression be pure: its value must de-
  performed with each mutator indepen-          pend only on its inputs and its evaluation
  dently as opposed to stopping all muta-       must have no side effects
  tors for a handshake at the same time       referrer set the set of locations that point
raw pointer a plain pointer (in contrast to a   to a particular object, notably used in the
  smart pointer)                                Collie collector
reachable the property of an object that it region (1) a space visible to and man-
  can be accessed by following a chain of      aged by the programmer or (typically in-
  references from a set of mutator roots;      ferred automatically by the) compiler in
  typically used as a safe approximation to    a region-based system; a region can typi-
  live                                         cally be made free in constant time; (2) a
                                               set of usually contiguous addresses, often
read barrier a barrier on reference loads by
                                               power-of-two aligned, that are treated in
  the mutator
                                               the same way by a collector, at least dur-
real-time (garbage) collection a technique     ing a given collection
  for concurrent or incremental collection
                                             region inferencing a static analysis that de-
  supporting a real-time system
                                               termines the region into which to place an
real-time system a hardware or software        object in a region-based memory man-
  system that is subject to deadlines from     agement scheme
  event to system response
                                             register a quickly accessible location pro-
reclaim return storage occupied by an ob-      vided by a processor, usually not ad-
  ject to the memory manager for reuse         dressable with a memory address
recursion if this is not clear, see recursion   relaxed consistency a consistency model
                                                  that is weaker than sequential consis-
redo log a record of changes made to a data
                                                  tency
  structure allowing a system to push the
  data structure forward to a new state; see    release consistency a consistency model in
  also log and undo log                           which acquire operations prevent later
                                                  accesses from occurring before the ac-
reference the canonical pointer used to
                                                  quire, but earlier accesses can happen af-
  identify an object
                                                  ter the acquire, and release operations
reference count a count of the number of          prevent earlier accesses from happening
  references that point to an object, typi-       after the release but later accesses can
  cally stored in the object’s header             happen before the release
reference counting a direct collection remembered set (remset) a set of objects
  scheme that manages objects by main-         or fields that the collector must process;
  taining a count of the number of refer-      typically, mutators supported by genera-
  ences to each object                         tional, concurrent or incremental collec-
reference listing a      direct    collection  tion add entries to the remembered set as
  scheme that manages objects by main-         they create pointers of interest to the col-
  taining a list rather than a count of refer- lector
  ences to each object                          remset see remembered set
referent the target of a reference              rendezvous barrier a code point at which
referentially transparent an expression is        each thread waits until all other threads
  called referentially transparent if it can      have reached that point
  be replaced with its corresponding value      replica (in a copying collector) the copy in
  (and vice versa) without changing the           tospace of a fromspace object, or vice
  program’s behaviour; this requires that         versa

replicating collection a technique for con- scheduler an operating system component
  current copying collection that maintains   that chooses which threads to execute on
  two (or more) copies of live objects        which processors at any given time
resilience the ability of a system to survive   scheduling choosing when to execute a
  failures (‘crashes’)                            unit of collection
restricted deque a double-ended queue           segregated-fits allocation an    allocation
  where one of the actions of adding or re-       strategy that partitions the heap by size
  moving is allowed at only one end of the        class in order to minimise fragmentation
  queue
                                                self-healing barrier a barrier that repairs
resurrection an action performed by a fi-         the condition that the barrier is detecting,
  naliser that causes the previously un-          often by writing an updated value back
  reachable object to become reachable            into the slot from which a reference was
root a reference that is directly accessible to   loaded
  the mutator without going through other semispace one of two spaces into which a
  objects                                         copying collection divides the heap
root object an object in the heap referred to semispace copying see copying collection
  directly by a root
                                                sequential allocation an allocation strat-
run-time system the code that supports the
                                                  egy that allocates objects consecutively
  execution of a program, providing ser-
                                                  from one end of a free chunk; often called
  vices such as memory management and
                                                  bump pointer allocation or linear alloca-
  thread scheduling
                                                  tion
                                                sequential consistency consistency model
safety the property of a collector that it
                                                  in which all memory operations appear to
  never reclaims a live object
                                                  execute one at a time, and the operations
scalar a non-reference value                      of each processor appear to execute in its
scalar field a field that contains a scalar       program order
  value                                        sequential fits allocation a free-list alloca-
scalar replacement an optimisation tech-         tion strategy that searches the free-list se-
  nique that replaces a reference to a           quentially for a cell that satisfies the allo-
  unique object with local variables repre-      cation request
  senting its fields; see also object inlining sequential store buffer (SSB) an efficient
scan pointer a pointer to the next location      implementation of a remembered set,
  to be scanned; typically used in Cheney        such as a chain of blocks of slots, which
  scanning                                       allows new entries to be stored quickly
                                                 but does not prevent duplicate entries
scanning processing each pointer field of
  an object or other contiguous set of ad- shade to colour a white object grey; see tri-
  dresses                                        colour abstraction
scavenging picking out live objects from shared pointer a form of smart pointer de-
  the fromspace, typically leaving forward- fined for C++ (shared_ptr), and Rust
  ing addresses behind; see copying collec- (Rc and Arc) based on reference count-
  tion                                      ing
schedulability analysis the analysis of a shell an empty object structure, typically
  set of real-time tasks to decide whether  allocated in tospace by a replicating col-
  they can be scheduled so that none of     lector with the expectation that its fields
  them misses a deadline                    will be filled in later

simultaneous multithreading (SMT) the space a subset of the heap managed by a
  capability of a processor to execute multi-   particular collection policy
  ple independent threads at the same time space utilisation the fraction of heap space
Single Instruction, Multiple Thread             usable for mutators’ data; that is, exclud-
  (SIMT) an execution model used by GPUs        ing garbage collector metadata, other
  whereby instructions of all threads exe-      semispaces, and so on
  cute in lock-step                           spatial locality the degree to which two
size class a logical set of objects that are        items (fields, objects) are likely to be al-
   managed by the same allocation and               located close to each other (for example,
   collection policies and whose size falls         on the same page or cache line)
   within a specific range; the amount al- spin lock a lock where the waiting threads
   located is generally rounded up to the           simply ‘spin’ in a loop until they can ac-
   largest size in the class                        quire the lock
slack-based scheduling a technique for
                                                  splitting dividing a cell into two adjacent
   scheduling real-time collection that per-
                                                    free cells; see also segregated-fits alloca-
   forms collector work when no real-time
                                                    tion
   task is running
                                                  stack (1) a last-in, first-out data structure,
sliding compaction a compaction tech-
                                                    allowing adding and removing only from
   nique that preserves the order in memory
                                                    the front (top); (2) see call stack
   of live objects; see compaction order
                                                  stack allocation allocation of an object in
slot see field
                                                    the stack frame of its allocating method
slow path see fast (slow) path
                                                  stack barrier a barrier on returning (or
smart pointer a form of pointer upon                throwing an exception) beyond a given
   which operations such as copying or              stack frame in a thread’s call stack
   dereferencing are overloaded in order
                                                  stack frame an activation record allocated
   to perform memory management opera-
                                                    in the call stack
   tions
                                                  stack map a data structure indicating
snapshot barrier see deletion barrier
                                                    which addresses in a call stack the col-
snapshot-at-the-beginning a technique for           lector should consider to be references to
   solving the lost object problem that pre-        objects
   serves the set of objects live at the start of
                                                  static allocation allocation of an object at
   the collection cycle
                                                    an address known at program build time
socket (1) a place on a hardware circuit
   board where an integrated circuit com- step a subspace used to segregate objects
   puter chip may be inserted; (2) that chip        by age within a generation

soft real-time system a real-time system sticky reference count a reference count
   (controversially) in which most deadlines        that has been incremented to the maxi-
   must be met within strict bounds to pre-         mum permissible value, not changed by
   serve quality of service; completion of an       subsequent pointer updates
  operation after its deadline results in de-stop-the-world collection a technique for
  graded service                               collection during which all mutator
soft reference in Java, a reference that is    threads  are halted
  stronger than a weak reference but not store barrier a barrier on reference (and
  as strong as a strong reference; soft ref-   possibly value) stores by the mutator and
  erences are typically used to build caches   possibly the collector, distinguished by
  that the garbage collector can reclaim       the fact that it relates to the low-level en-
  when it comes under memory pressure          coding of the datum while a write barrier

  is concerned more with properties such as   temporal locality the degree to which two
  the space in which a referent lies            items (fields, objects) are likely to be ac-
store buffer see write buffer; not to be con-   cessed close to each other in time
  fused with a sequential store buffer        tenuring see promote
strict consistency a consistency model in terabyte (TB) common usage unit meaning
   which every memory access and atomic           240 bytes; see also tebibyte
   operation appears to occur in the same or- test-and-set an atomic instruction that sets
   der everywhere                                 that value of a location to ”1” only if it
strong generational hypothesis assumption         is currently ”0”; in any case, it indicates
   that the older an object is, the less likely   whether it succeeded; see also atomic
   it is to die                                 test-and-set lock see spin lock
strong reference a reference to an object test-and-test-and-set lock lower-cost vari-
   that contributes to its reachability; nor- ant of a test-and-set lock that uses
   mal references are usually strong          (expensive) atomic hardware primitives
strong tricolour invariant a tricolour ab-    only when the lock appears to be free
  straction invariant that no black object      thrash overly frequent collections (or pag-
  ever refers to a white object                   ing), at the expense of mutator activity
survival rate the fraction of objects that thread the smallest unit of processing that
  survive a collection                            can be scheduled for execution by an op-
survivor an object that is live at the time of    erating system; multiple threads may ex-
  a collection and thus not reclaimed             ecute in the same address space; see also
                                                  process
swapping see paging
                                               thread-local collection garbage collection
sweeping reclaiming unmarked (dead)               of only those objects accessible to a sin-
  objects in a linear pass through (a sub-        gle thread
  set) of the heap
                                               threaded compaction a technique for com-
symmetric multiprocessor (SMP) a multi-           pacting that links objects so that all those
  processor with identical separate proces-       originally pointing to a given object can
  sors and shared, possibly non-uniform           be discovered from that object
  memory access, memory
                                               throughput the rate at which data is pro-
synthetic benchmark an artificially con-          cessed by the mutator(s) (alternatively, by
  structed benchmark intended only to             the collector(s))
  evaluate a system; although the be- thunk an unevaluated application of a
  haviour of synthetic benchmarks may be          function to its arguments; see also closure
  stochastic, such benchmarks may not re-
  produce the interactions exhibited by real   tidy  pointer the canonical pointer used as
  programs, or may have working sets too          an object’s reference
  small to exhibit their locality effects      time-based scheduling a technique for
                                                  scheduling real-time collection that re-
tagged pointer a pointer some of whose            serves  a pre-defined portion of execu-
  bits are a tag indicating some attribute of     tion time solely for collector work during
  the pointer or its referent such as which       which  the mutator is stopped
  space it refers to; sometimes called a tospace the semispace to which copying
  coloured pointer                                collection evacuates live objects
task a unit of work performed by a process tospace invariant the invariant that the
  or thread, usually in a real-time system        mutator holds only tospace references
tebibyte (TiB) standard usage unit mean-        tracing visiting the reachable objects by
  ing 240 bytes; see also terabyte                 traversing all or part of an object graph

tracing collection a technique for indirect ulterior reference counting a    reference
   collection that operates by tracing the    counting scheme that manages young
   graph of live objects                      objects by copying and older ones by
train a component of the Mature Object        reference counting, used in the collector
   Space (‘Train’) collector                  of the same name
transaction a collection of reads and writes    undo log a record of changes made to a
   that must appear to execute atomically         data structure allowing a system to roll
                                                  the data structure back to its previous
transaction abort the unsuccessful termi-
                                                  state; see also log and redo log
   nation of a transaction which discards its
   effects                                      uniprocessor a computer that provides a
transaction commit the successful comple-         single processor; contrasted with multi-
   tion of a transaction which ensures that       processor
   its effects are made visible, and possibly     unique pointer a smart pointer that retains
   durable                                          sole ownership of an object through a
transactional memory a mechanism analo-             pointer and destroys the object when the
   gous to database transactions for control-       unique  pointer goes out of scope (called
   ling concurrent access to shared memory;         unique   because two different unique
   it may be implemented in hardware or             pointers cannot  manage the same object);
   software                                         defined for C++  (unique_ptr) and Rust
                                                    (Box)
transitive (referential) closure given a set
   of objects, fields or other locations
   (roots), the transitive referential closure of vector load load several words in a single
   the set of roots is the set of objects that      operation; see also vector store
   can be reached from those roots by fol- vector store store several words in a single
   lowing references                                operation; see also vector load
translation lookaside buffer (TLB) a small virtual address an address in the virtual
   associative memory that caches some          memory
   translations between virtual and physical
                                              virtual machine a run-time system that ab-
   addresses
                                                stracts away details of the underlying
traversal visiting each node in a graph ex-     hardware and operating system
   actly once
                                              virtual memory an idealised abstraction of
trial deletion the temporary deletion of a      the physical memory that is actually
   reference in order to discover whether       available on a given machine; pages in
   this causes an object’s reference count to   virtual memory are mapped to actual
   drop to zero                                 pages in physical memory, not necessar-
tricolour abstraction a characterisation of     ily in a contiguous manner, and at any
   the work of the garbage collector as par-    point some virtual pages’ contents may
   titioning objects into white (not yet vis-   reside in file storage rather than physical
   ited) and black (need not be revisited),     memory
   using grey to represent the remaining volatile (1) subject to change by other
   work (to be revisited)                       threads; (2) in the context of persistence,
type-accurate a property of a garbage col-        not able to survive failures or outlive the
  lector that it can precisely identify every     execution of a program, the opposite of
  slot or root that contains a pointer            durable
type-safe in the context of this book, a pro-
  gramming language is type-safe if it can-     wait-free a guarantee of both system-wide
  not manufacture a reference value from a       (lock-free) and per-thread progress so
  non-reference type                             that a thread will complete its operation

  in a bounded number of steps; see also     work stealing a technique for balanc-
  non-blocking                                ing work among threads where lightly
                                              loaded threads pull work from more
wavefront (1) the boundary, comprising
                                              heavily loaded threads
 grey objects (still to be processed), sepa-
 rating black objects (already processed) work-based scheduling a technique for
 from white objects (not yet processed);     scheduling real-time collection that im-
 (2) the OpenCL term for a group of work-    poses collector work as a tax on units of
 items assigned to a compute unit, execut-   mutator  work
 ing in lock-step — the CUDA term is warp work-item OpenCL term for one of a col-
weak consistency any consistency model       lection of parallel executions of a kernel
 which treats each atomic operation as a     invoked on a device; the CUDA term is
 total memory fence                          thread
                                             workgroup OpenCL term for a collection
weak generational hypothesis assumption
                                              of related work-items executing the same
 that most objects die young
                                              kernel on a single compute unit and shar-
weak head normal formal an expression is      ing local memory; the CUDA term is block
 in weak head normal form when its out-     worst-case execution time (WCET)         the
 ermost part has been evaluated to a data    maximum time an operation can take on
 constructor or a lambda abstraction         a specific hardware platform; knowing
weak reference a reference to an object      worst-case execution times is necessary
 that does not contribute to its reachabil-  for schedulability analysis of a hard real-
 ity; Java for example provides several      time system
 kinds of weak reference (see soft refer- write barrier a barrier on reference stores
 ence and phantom reference)                 by the mutator; see also store barrier
weak tricolour invariant a tricolour ab- write buffer a hardware buffer in a proces-
 straction invariant that any white object   sor that holds pending writes to memory,
 pointed to by a black object must also      also called a store buffer (not to be con-
 be reachable from some grey object ei-      fused with a sequential store buffer)
 ther directly or through a chain of white
 objects                                   young generation see nursery
white an object is white if the collector has
  not processed it so that, at the end of a zero count table (ZCT) a table of objects
  collection cycle, white objects are consid- whose reference counts are zero
  ered dead; see also tricolour abstraction zero-copy transfer an operation which
                                              transfers data from one memory area
wilderness the last free chunk in the heap
                                              to another without having to physically
wilderness preservation a policy of allo-     copy it, typically requiring the CPU and
  cating from the wilderness only as a last   GPU to share a unified address space;
  resort                                      also relevant to access in persistent heaps
Taylor & Francis
Taylor & Francis Group
http://taylorandfrancis.com
