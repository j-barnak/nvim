7     Memory Performance Tools                                                         20
A wide variety of tools is available to help programmers                               18
understand performance characteristics of a program, the                               16

                                                                  Cycles/Instruction
cache and memory use among others. Modern proces-                                      14
sors have performance monitoring hardware that can be                                  12
used. Some events are hard to measure exactly, so there
                                                                                       10
is also room for simulation. When it comes to higher-
level functionality, there are special tools to monitor the                             8
execution of a process. We will introduce a set of com-                                 6
monly used tools available on most Linux systems.                                       4
                                                                                        2
7.1    Memory Operation Profiling                                                       0
                                                                                        210   213 216 219 222 225         228
Profiling memory operations requires collaboration from                                        Working Set Size (Bytes)
the hardware. It is possible to gather some information in
software alone, but this is either coarse-grained or merely
a simulation. Examples of simulation will be shown in             Figure 7.1: Cycles per Instruction (Follow Random)
section 7.2 and 7.5. Here we will concentrate on measur-
able memory effects.                                            measurements were made on a Intel Core 2 processor,
Access to performance monitoring hardware on Linux is           which is multi-scalar and can work on several instruc-
provided by oprofile. Oprofile provides continuous pro-         tions at once. For a program which is not limited by
filing capabilities as first described in [2]; it performs      memory bandwidth, the ratio can be significantly below
statistical, system-wide profiling with an easy-to-use in-      1.0 but, in this case, 1.0 is pretty good.
terface. Oprofile is by no means the only way the per-          Once the L1d is no longer large enough to hold the work-
formance measurement functionality of processors can            ing set, the CPI jumps to just below 3.0. Note that the
be used; Linux developers are working on pfmon which            CPI ratio averages the penalties for accessing L2 over
might at some point be sufficiently widely deployed to          all instructions, not just the memory instructions. Using
warrant being described here, too.                              the cycles for list element data, it can be worked out how
The interface oprofile provides is simple and minimal but       many instructions per list element are needed. If even the
also pretty low-level, even if the optional GUI is used.        L2 cache is not sufficient, the CPI ratio jumps to more
The user has to select among the events the processor           than 20. These are expected results.
can record. The architecture manuals for the processors         But the performance measurement counters are supposed
describe the events but, oftentimes, it requires extensive      to give more insight into what is going on in the pro-
knowledge about the processors themselves to interpret          cessor. For this we need to think about processor im-
the data. Another problem is the interpretation of the          plementations. In this document, we are concerned with
collected data. The performance measurement counters            cache handling details, so we have to look at events re-
are absolute values and can grow arbitrarily. How high is       lated to the caches. These events, their names, and what
too high for a given counter?                                   they count, are processor–specific. This is where opro-
A partial answer to this problem is to avoid looking at the     file is currently hard to use, irrespective of the simple
absolute values and, instead, relate multiple counters to       user interface: the user has to figure out the performance
each other. Processors can monitor more than one event;         counter details by her/himself. In Appendix B we will
the ratio of the collected absolute values can then be ex-      see details about some processors.
amined. This gives nice, comparable results. Often the          For the Core 2 processor the events to look for are L1D_-
divisor is a measure of processing time, the number of          REPL, DTLB_MISSES, and L2_LINES_IN. The latter can
clock cycles or the number of instructions. As an ini-          measure both all misses and misses caused by instruc-
tial stab at program performance, relating just these two       tions instead of hardware prefetching. The results for the
numbers by themselves is useful.                                random “Follow” test can be seen in Figure 7.2.
Figure 7.1 shows the Cycles Per Instruction (CPI) for the       All ratios are computed using the number of retired in-
simple random “Follow” test case for the various work-          structions (INST_RETIRED). This means that to compute
ing set sizes. The names of the events to collect this infor-   the cache miss rate all the load and store instructions a
mation for most Intel processor are CPU_CLK_UNHALTED            substantial number has to be subtracted from the INST_-
and INST_RETIRED. As the names suggest, the former              RETIRED value which means the actual cache miss rate
counts the clock cycles of the CPU and the latter the           of the memory operation is even higher than the numbers
number of instructions. We see a picture similar to the         shown in the graph.
cycles per list element measurements we used. For small
working set sizes the ratio is 1.0 or even lower. These         The L1d misses tower over all the others since an L2 miss

                    22%                                                                                    3.5%
                    20%
                                                                                                             3%
                    18%
 Cache Miss Ratio   16%                                                                                    2.5%

                                                                                        Cache Miss Ratio
                    14%
                    12%                                                                                      2%
                    10%                                                                                    1.5%
                     8%
                     6%                                                                                      1%
                     4%
                                                                                                           0.5%
                     2%
                     0%                                                                                      0%
                       210       213
                                      2  16
                                            2      19
                                                   2    2 22      25
                                                                        228
                                                                                                               210      213 216 219 222 225                   228
                                 Working Set Size (Bytes)                                                               Working Set Size (Bytes)
                    L1D Misses   L2 Misses    L2 Demand Misses   DTLB Misses                               L1D Misses   L2 Misses   L2 Demand Misses   DTLB Misses

 Figure 7.2: Measured Cache Misses (Follow Random)                                    Figure 7.3: Measured Cache Misses (Follow Sequential)

implies, for Intel processors, an L1d miss due to the use                             tion 6.2.1 and the example code in section A.1, we can
of inclusive caches. The processor has 32k of L1d and so                              make use of three more counters. The SSE_HIT_PRE,
we see, as expected, the L1d rate go up from zero at about                            SSE_PRE_MISS, and LOAD_PRE_EXEC counters can be
that working set size (there are other uses of the cache                              used to see how effective the software prefetching is. If
beside the list data structure, which means the increase                              the code in section A.1 is run we get the following re-
happens between the 16k and 32k mark). It is interesting                              sults:
to see that the hardware prefetching can keep the miss
rate at 1% for a working set size up to and including 64k.                                                                   Description         Ratio
After that the L1d rate skyrockets.                                                                                Useful NTA prefetches         2.84%
                                                                                                                    Late NTA prefetches          2.65%
The L2 miss rate stays zero until the L2 is exhausted;
the few misses due to other uses of L2 do not influence
the numbers much. Once the size of L2 (221 bytes) is                                  The low useful NTA (non-temporal aligned) prefetch ra-
exceeded, the miss rates rise. It is important to notice                              tio indicates that many prefetch instructions are executed
that the L2 demand miss rate is nonzero. This indicates                               for cache lines which are already loaded, so no work is
that the hardware prefetcher does not load all the cache                              needed. This means the processor wastes time to decode
lines needed by instructions later. This is expected, the                             the prefetch instruction and look up the cache. One can-
randomness of the accesses prevents perfect prefetching.                              not judge the code too harshly, though. Much depends on
Compare this with the data for the sequential read in Fig-                            the size of the caches of the processor used; the hardware
ure 7.3.                                                                              prefetcher also plays a role.

In this graph we can see that the L2 demand miss rate                                 The low late NTA prefetch ratio is misleading. The ratio
is basically zero (note the scale of this graph is differ-                            means that 2.65% of all prefetch instructions are issued
ent from Figure 7.2). For the sequential access case, the                             too late. The instruction which needs the data is executed
hardware prefetcher works perfectly: almost all L2 cache                              before the data could be prefetched into the cache. It
misses are caused by the prefetcher. The fact that the L1d                            must be kept in mind that only 2.84% + 2.65% = 5.5%
and L2 miss rates are the same shows that all L1d cache                               of the prefetch instructions were of any use. Of the NTA
misses are handled by the L2 cache without further de-                                prefetch instructions which are useful, 48% did not finish
lays. This is the ideal case for all programs but it is, of                           in time. The code therefore can be optimized further:
course, hardly ever achievable.
                                                                                                           • most of the prefetch instructions are not needed.
The fourth line in both graphs is the DTLB miss rate (In-
tel has separate TLBs for code and data, DTLB is the data                                                  • the use of the prefetch instruction can be adjusted
TLB). For the random access case, the DTLB miss rate                                                         to match the hardware better.
is significant and contributes to the delays. What is in-
teresting is that the DTLB penalties set in before the L2
misses. For the sequential access case the DTLB costs                                 It is left as an exercise to the reader to determine the best
are basically zero.                                                                   solution for the available hardware. The exact hardware
                                                                                      specification plays a big role. On Core 2 processors the
Going back to the matrix multiplication example in sec-                               latency of the SSE arithmetic operations is 1 cycle. Older

$ \time ls /etc
[...]
0.00user 0.00system 0:00.02elapsed 17%CPU (0avgtext+0avgdata 0maxresident)k
0inputs+0outputs (1major+335minor)pagefaults 0swaps

                                          Figure 7.4: Output of the time utility

versions had a latency of 2 cycles, meaning that the hard-     page table tree must be modified accordingly. The last
ware prefetcher and the prefetch instructions had more         step requires synchronization with other tasks reading or
time to bring in the data.                                     modifying the page table tree, which might introduce fur-
                                                               ther delays.
To determine where prefetches might be needed–or are
unnecessary–one can use the opannotate program. It lists       The easiest way to retrieve information about the page
the source or assembler code of the program and shows          fault counts is to use the time tool. Note: use the real
the instructions where the event was recognized. Note          tool, not the shell builtin. The output can be seen in Fig-
that there are two sources of vagueness:                       ure 7.4.44
                                                               The interesting part here is the last line. The time tool
     1. Oprofile performs stochastic profiling. Only every     reports one major and 335 minor page faults. The exact
        Nth event (where N is a per-event threshold with       numbers vary; in particular, repeating the run immedi-
        an enforced minimum) is recorded to avoid slow-        ately will likely show that there are now no major page
        ing down operation of the system too much. There       faults at all. If the program performs the same action, and
        might be lines which cause 100 events and yet they     nothing changes in the environment, the total page fault
        might not show up in the report.                       count will be stable.
     2. Not all events are recorded accurately. For exam-      An especially sensitive phase with respect to page faults
        ple, the instruction counter at the time a specific    is program start-up. Each page which is used will pro-
        event was recorded might be incorrect. Processors      duce a page fault; the visible effect (especially for GUI
        being multi-scalar makes it hard to give a 100%        applications) is that the more pages that are used, the
        correct answer. A few events on some processors        longer it takes for the program to start working. In sec-
        are exact, though.                                     tion 7.5 we will see a tool to measure this effect specifi-
                                                               cally.
The annotated listings are useful for more than determin-
                                                               Under the hood, the time tool uses the rusage function-
ing the prefetching information. Every event is recorded
                                                               ality. The wait4 system call fills in a struct rusage
with the instruction pointer; it is therefore also possible
                                                               object when the parent waits for a child to terminate;
to pinpoint other hot spots in the program. Locations
                                                               that is exactly what is needed for the time tool. But it is
which are the source of many INST_RETIRED events are
                                                               also possible for a process to request information about
executed frequently and deserve to be tuned. Locations
                                                               its own resource usage (that is where the name rusage
where many cache misses are reported might warrant a
                                                               comes from) or the resource usage of its terminated chil-
prefetch instruction to avoid the cache miss.
                                                               dren.
One type of event which can be measured without hard-
ware support is page faults. The OS is responsible for re-
solving page faults and, on those occasions, it also counts    #include <sys/resource.h>
them. It distinguishes two kinds of page faults:               int getrusage(__rusage_who_t who,
                                                                             struct rusage *usage)
Minor Page Faults For anonymous (i.e., not backed by
     a file) pages which have not been used so far, for
     copy-on-write pages, and for other pages whose            The who parameter specifies which process the informa-
     content is already in memory somewhere.                   tion is requested for. Currently, only RUSAGE_SELF and
Major Page Faults Resolving them requires access to            RUSAGE_CHILDREN are defined. The resource usage of
     disk to retrieve the file-backed (or swapped-out)         the child processes is accumulated when each child ter-
     data.                                                     minates. It is a total value, not the usage of an individ-
                                                               ual child process. Proposals to allow requesting thread-
                                                               specific information exist, so it is likely that we will see
Obviously, major page faults are significantly more ex-        RUSAGE_THREAD in the near future. The rusage struc-
pensive than minor page faults. But the latter are not         ture is defined to contain all kinds of metrics, including
cheap either. In either case an entry into the kernel is       execution time, the number of IPC messages sent and
necessary, a new page must be found, the page must be
cleared or populated with the appropriate data, and the          44 The leading backslash prevents the use of the built-in command.

==19645== I   refs:      152,653,497
==19645== I1 misses:          25,833
==19645== L2i misses:          2,475
==19645== I1 miss rate:         0.01%
==19645== L2i miss rate:        0.00%
==19645==
==19645== D   refs:       56,857,129 (35,838,721 rd + 21,018,408 wr)
==19645== D1 misses:          14,187 (    12,451 rd +      1,736 wr)
==19645== L2d misses:          7,701 (     6,325 rd +      1,376 wr)
==19645== D1 miss rate:          0.0% (      0.0%   +        0.0% )
==19645== L2d miss rate:         0.0% (      0.0%   +        0.0% )
==19645==
==19645== L2 refs:            40,020 (    38,284 rd +      1,736 wr)
==19645== L2 misses:          10,176 (     8,800 rd +      1,376 wr)
==19645== L2 miss rate:          0.0% (      0.0%   +        0.0% )

                                              Figure 7.5: Cachegrind Summary Output

memory used, and the number of page faults. The lat-                   program on them. This is sometimes (often) not possible.
ter information is available in the ru_minflt and ru_-                 One example is the data from Figure 3.8. To collect such
majflt members of the structure.                                       data with oprofile one would have to have 24 different
                                                                       machines, many of which do not exist.
A programmer who tries to determine where her program
loses performance due to page faults could regularly re-               The data in that graph was collected using a cache simu-
quest the information and then compare the returned val-               lator. This program, cachegrind, uses the valgrind frame-
ues with the previous results.                                         work, which was initially developed to check for memory
                                                                       handling related problems in a program. The valgrind
From the outside, the information is also visible if the               framework simulates the execution of a program and,
requester has the necessary privileges. The pseudo file                while doing this, it allows various extensions, such as
/proc/<PID>/stat, where <PID> is the process ID of
                                                                       cachegrind, to hook into the execution framework. The
the process we are interested in, contains the page fault              cachegrind tool uses this to intercept all uses of memory
numbers in the tenth to fourteenth fields. They are pairs              addresses; it then simulates the operation of L1i, L1d,
of the process’s and its children’s cumulative minor and               and L2 caches with a given size, cache line size, and as-
major page faults, respectively.                                       sociativity.
7.2    Simulating CPU Caches                                           To use the tool a program must be run using valgrind as
                                                                       a wrapper:
While the technical description of how a cache works is
                                                                          valgrind --tool=cachegrind command arg
relatively easy to understand, it is not so easy to see how
an actual program behaves with respect to cache. Pro-
grammers are not directly concerned with the values of                 In this simplest form the program command is executed
addresses, be they absolute nor relative. Addresses are                with the parameter arg while simulating the three caches
determined, in part, by the linker and, in part, at runtime            using sizes and associativity corresponding to that of the
by the dynamic linker and the kernel. The generated as-                processor it is running on. One part of the output is
sembly code is expected to work with all possible ad-                  printed to standard error when the program is running;
dresses and, in the source language, there is not even a               it consists of statistics of the total cache use as can be
hint of absolute address values left. So it can be quite               seen in Figure 7.5. The total number of instructions and
difficult to get a sense for how a program is making use               memory references is given, along with the number of
of memory. 45                                                          misses they produce for the L1i/L1d and L2 cache, the
                                                                       miss rates, etc. The tool is even able to split the L2
CPU-level profiling tools such as oprofile (as described               accesses into instruction and data accesses, and all data
in section 7.1) can help to understand the cache use. The              cache uses are split in read and write accesses.
resulting data corresponds to the actual hardware, and it
can be collected relatively quickly if fine-grained collec-            It becomes even more interesting when the details of the
tion is not needed. As soon as more fine-grained data is               simulated caches are changed and the results compared.
needed, oprofile is not usable anymore; the thread would               Through the use of the --I1, --D1, and --L2 parame-
have to be interrupted too often. Furthermore, to see the              ters, cachegrind can be instructed to disregard the proces-
memory behavior of the program on different processors,                sor’s cache layout and use that specified on the command
one actually has to have such machines and execute the                 line. For example:
  45 When programming close to the hardware this might be different,

but this is of no concern to normal programming and, in any case, is      valgrind --tool=cachegrind \
only possible for special addresses such as memory-mapped devices.          --L2=8388608,8,64 command arg

--------------------------------------------------------------------------------
        Ir I1mr I2mr          Dr D1mr D2mr         Dw D1mw D2mw file:function
--------------------------------------------------------------------------------
53,684,905     9    8 9,589,531     13    3 5,820,373   14    0 ???:_IO_file_xsputn@@GLIBC_2.2.5
36,925,729 6,267 114 11,205,241     74   18 7,123,370   22    0 ???:vfprintf
11,845,373    22    2 3,126,914     46   22 1,563,457    0    0 ???:__find_specmb
 6,004,482    40   10    697,872 1,744 484          0    0    0 ???:strlen
 5,008,448     3    2 1,450,093    370 118          0    0    0 ???:strcmp
 3,316,589    24    4    757,523     0    0   540,952    0    0 ???:_IO_padn
 2,825,541     3    3    290,222     5    1   216,403    0    0 ???:_itoa_word
 2,628,466     9    6    730,059     0    0   358,215    0    0 ???:_IO_file_overflow@@GLIBC_2.2.5
 2,504,211     4    4    762,151     2    0   598,833    3    0 ???:_IO_do_write@@GLIBC_2.2.5
 2,296,142    32    7    616,490    88    0   321,848    0    0 dwarf_child.c:__libdw_find_attr
 2,184,153 2,876   20    503,805    67    0   435,562    0    0 ???:__dcigettext
 2,014,243     3    3    435,512     1    1   272,195    4    0 ???:_IO_file_write@@GLIBC_2.2.5
 1,988,697 2,804    4    656,112   380    0    47,847    1    1 ???:getenv
 1,973,463    27    6    597,768    15    0   420,805    0    0 dwarf_getattrs.c:dwarf_getattrs

                                             Figure 7.6: cg annotate Output

would simulate an 8MB L2 cache with 8-way set asso-           cessor might very well be quite different. cachegrind
ciativity and 64 byte cache line size. Note that the --L2     simulates Least Recently Used (LRU) eviction, which is
option appears on the command line before the name of         likely to be too expensive for caches with large associa-
the program which is simulated.                               tivity. Furthermore, the simulation does not take context
                                                              switches and system calls into account, both of which
This is not all cachegrind can do. Before the process ex-     can destroy large parts of L2 and must flush L1i and L1d.
its it writes out a file named cachegrind.out.XXXXX           This causes the total number of cache misses to be lower
where XXXXX is the PID of the process. This file contains     than experienced in reality. Nevertheless, cachegrind is a
the summary information and detailed information about        nice tool to learn about a program’s memory use and its
the cache use in each function and source file. The data      problems with memory.
can be viewed using the cg annotate program.
The output this program produces contains the cache use       7.3   Measuring Memory Usage
summary which was printed also when the process ter-
minated, along with a detailed summary of the cache line
                                                              Knowing how much memory a program allocates and
use in each function of the program. Generating this per-
                                                              possibly where the allocation happens is the first step to
function data requires that cg annotate is able to match
                                                              optimizing its memory use There are, fortunately, some
addresses to functions. This means debug information
                                                              easy-to-use programs available which do not require that
should be available for best results. Failing that, the ELF
                                                              the program be recompiled or specifically modified.
symbol tables can help a bit but, since internal symbols
are not listed in the dynamic symbol table, the results are   For the first tool, called massif, it is sufficient to not strip
not complete. Figure 7.6 shows part of the output for the     the debug information which the compiler can automat-
same program run as Figure 7.5.                               ically generate. It provides an overview of the accumu-
                                                              lated memory use over time. Figure 7.7 shows an ex-
The Ir, Dr, and Dw columns show the total cache use,
                                                              ample of the generated output. Like cachegrind (sec-
not cache misses, which are shown in the following two
                                                              tion 7.2), massif is a tool using the valgrind infrastruc-
columns. This data can be used to identify the code
                                                              ture. It is started using
which produces the most cache misses. First, one proba-
bly would concentrate on L2 cache misses, then proceed
to optimizing L1i/L1d cache misses.                              valgrind --tool=massif command arg

cg annotate can provide the data in more detail. If the
name of a source file is given, it also annotates (hence      where command arg is the program which is to be ob-
the program’s name) each line of the source file with the     served and its parameter(s), The program will be sim-
number of cache hits and misses corresponding to that         ulated and all calls to memory allocation functions are
line. This information allows the programmer to drill         recognized. The call site is recorded along with a times-
down to the exact line where cache misses are a problem.      tamp value; the new allocation size is added to both the
The program interface is a bit raw: as of this writing, the   whole-program total and total for the specific call site.
cachegrind data file and the source file must be in the       The same applies to the functions which free memory
same directory.                                               where, obviously, the size of the freed block is subtracted
                                                              from the appropriated sums. This information can then
It should, at this point, be noted again: cachegrind is       be used to create a graph showing the memory use over
a simulator which does not use measurements from the          the lifetime of the program, splitting each time value ac-
processor. The actual cache implementation in the pro-        cording to the location which requested the allocation.

  src/readelf -a -w src/readelf                       106,047,201 bytes x ms

      bytes
                                                                                                   x4C13D6C:__libdw_allocat

                                                                                                   x4E29F83:file_read_elf
    55k
                                                                                                   x4C0E7D5:Dwarf_Abbrev_Ha
    50k
                                                                                                   stack(s)
    45k
                                                                                                   x4C0E8BE:insert_entry_2

    40k                                                                                            x4C0D64E:dwarf_begin_elf

    35k                                                                                            x1027D7D:_nl_intern_loca

                                                                                                   x102CF6C:read_alias_file
    30k
                                                                                                   x102CEE8:read_alias_file
    25k
                                                                                                   x9D75:_dl_new_object

    20k                                                                                            heap-admin

    15k                                                                                            x102D594:_nl_make_l10nfl

                                                                                                   x102D461:_nl_make_l10nfl
    10k
                                                                                                   x40C896:xmalloc
     5k
                                                                                                   x40B2C0:openbackend
     0k
       0.0    200.0   400.0   600.0   800.0 1000.0 1200.0 1400.0 1600.0 1800.0 2000.0 2200.0 ms

                                                     Figure 7.7: Massif Output

Before the process terminates massif creates two files:               cation calls made from within xmalloc.
massif.XXXXX.txt and massif.XXXXX.ps; XXXXX is
as before the PID of the process. The .txt file is a sum-             The second tool is called memusage; it is part of the GNU
mary of the memory use for all call sites and the .ps is              C library. It is a simplified version of massif (but existed
what can be seen in Figure 7.7.                                       a long time before massif). It only records the total mem-
                                                                      ory use for heap (including possible calls to mmap etc. if
Massif can also record the program’s stack usage, which               the -m option is given) and, optionally, the stack. The
can be useful to determine the total memory footprint of              results can be shown as a graph of the total memory use
an application. But this is not always possible. In some              over time or, alternatively, linearly over the calls made to
situations (some thread stacks or when signaltstack                   allocation functions. The graphs are created separately
is used) the valgrind runtime cannot know about the lim-              by the memusage script which, just as with valgrind, has
its of the stack . In these situations, it also does not make         to be used to start the application:
much sense to add these stacks’ sizes to the total. There
are several other situations where it makes no sense. If                memusage command arg
a program is affected by this, massif should be started
with the addition option --stacks=no. Note, this is an
                                                                      The -p IMGFILE option must be used to specify that the
option for valgrind and therefore must come before the
                                                                      graph should be generated in the file IMGFILE. This is a
name of the program which is being observed.
                                                                      PNG file. The code to collect the data is run in the actual
Some programs provide their own memory allocation im-                 program itself, it is not an simulation like valgrind. This
plementation or wrapper functions around the system’s                 means memusage is much faster than massif and usable
allocation functions. In the first case, allocations are nor-         in situations where massif would be not useful. Besides
mally missed; in the second case, the recorded call sites             total memory consumption, the code also records alloca-
hide information, since only the address of the call in               tion sizes and, on program termination, it shows a his-
the wrapper function is recorded. For this reason, it is              togram of the used allocation sizes. This information is
possible to add additional functions to the list of alloca-           written to standard error.
tion functions. The --alloc-fn=xmalloc parameter
                                                                      Sometimes it is not possible (or feasible) to call the pro-
would specify that the function xmalloc is also an al-
                                                                      gram which is supposed to be observed directly. An ex-
location function, which is often the case in GNU pro-
                                                                      ample is the compiler stage of gcc, which is started by
grams. Calls to xmalloc are recorded, but not the allo-
                                                                      the gcc driver program. In this case the name of the pro-

gram which should be observed must be provided to the          allocator, they have limited abilities to free objects. See
memusage script using the -n NAME parameter. This pa-          the GNU C library manual for details.
rameter is also useful if the program which is observed
starts other programs. If no program name is specified         So, how can a situation where the use of obstacks (or
all started programs will be profiled.                         similar techniques) is advisable be recognized from the
                                                               graphs? Without consulting the source, possible candi-
Both programs, massif and memusage, have additional            dates for the changes cannot be identified, but the graph
options. A programmer finding herself in the position          can provide an entry point for the search. If many al-
needing more functionality should first consult the man-       locations are made from the same location, this could
ual or help messages to make sure the additional func-         mean that allocation in bulk might help. In Figure 7.7,
tionality is not already implemented.                          we can see such a possible candidate in the allocations at
                                                               address 0x4c0e7d5. From about 800ms into the run un-
Now that we know how the data about memory allocation          til 1,800ms into the run this is the only area (except the
can be captured, it is necessary to discuss how this data      top, green one) which grows. Moreover, the slope is not
can be interpreted in the context of memory and cache          steep, which means we have a large number of relatively
use. The main aspects of efficient dynamic memory allo-        small allocations. This is, indeed, a candidate for the use
cation are linear allocation and compactness of the used       of obstacks or similar techniques.
portion. This goes back to making prefetching efficient
and reducing cache misses.                                     Another problem the graphs can show is when the total
                                                               number of allocations is high. This is especially easy to
A program which has to read in an arbitrary amount of          see if the graph is not drawn linearly over time but, in-
data for later processing could do this by creating a list     stead, linearly over the number of calls (the default with
where each of the list elements contains a new data item.      memusage). In that case, a gentle slope in the graph
The overhead for this allocation method might be min-          means a lot of small allocations. memusage will not say
imal (one pointer for a single-linked list) but the cache      where the allocations took place, but the comparison with
effects when using the data can reduce the performance         massif’s output can say that, or the programmer might
dramatically.                                                  recognize it right away. Many small allocations should
One problem is, for instance, that there is no guarantee       be consolidated to achieve linear memory use.
that sequentially allocated memory is laid out sequen-         But there is another, equally important, aspect to this lat-
tially in memory. There are many possible reasons for          ter class of cases: many allocations also means higher
this:                                                          overhead in administrative data. This by itself might not
                                                               be that problematic. The red area named “heap-admin”
     • memory blocks inside a large memory chunk ad-           represents this overhead in the massif graph and it is quite
       ministrated by the memory allocator are actually        small. But, depending on the malloc implementation,
       returned from the back to the front;                    this administrative data is allocated along with the data
                                                               blocks, in the same memory. For the current malloc im-
     • a memory chunk is exhausted and a new one is            plementation in the GNU C library, this is the case: every
       started in a different part of the address space;       allocated block has at least a 2-word header (8 bytes for
                                                               32-bit platforms, 16 bytes for 64-bit platforms). In addi-
     • the allocation requests are for different sizes which   tion, block sizes are often a bit larger than necessary due
       are served from different memory pools;                 to the way memory is administrated (rounding up block
     • the interleaving allocations in the various threads     sizes to specific multiples).
       of multi-threaded programs.                             This all means that memory used by the program is in-
                                                               terspersed with memory only used by the allocator for
If data must be allocated up front for later processing,       administrative purposes. We might see something like
the linked-list approach is clearly a bad idea. There is       this:
no guarantee (or even likelihood) that the consecutive el-
ements in the list are laid out consecutively in memory.
To ensure contiguous allocations, that memory must not                            Header   Data   Padding

be allocated in small chunks. Another layer of memory
handling must be used; it can easily be implemented by         Each block represents one memory word. In this small
the programmer. An alternative is to use the obstack im-       region of memory we have four allocated blocks. The
plementation available in the GNU C library. This allo-        overhead due to the block header and padding is 50%.
cator requests large blocks of memory from the system’s        Due to the placement of the header, this automatically
allocator and then hands arbitrarily large or small blocks     means that the effective prefetch rate of the processor is
of memory out. These allocations are always sequential         lowered by up to 50% as well. If the blocks were be
unless the large memory chunk is exhausted, which is,          processed sequentially (to take maximum advantage of
depending on the requested allocation sizes, pretty rare.      prefetching), the processor would read all the header and
Obstacks are not a complete replacement for a memory           padding words into the cache, even though they are never

supposed to be read from or written to by the application      information related to the branches in the code. It must
itself. Only the runtime uses the header words, and the        be preserved for later.
runtime only comes into play when the block is freed.
                                                               Once the program binary is available, it should be used
One could at this point argue that the implementation          to run a representative set of workloads. Whatever work-
should be changed to put the administrative data some-         load is used, the final binary will be optimized to do this
where else. This is indeed done in some implementa-            task well. Consecutive runs of the program are possible
tions, and it might prove to be a good idea. There are         and, in general necessary; all the runs will contribute to
many aspects to be kept in mind, though, security not be-      the same output file. Before the program terminates, the
ing the least of them. Regardless of whether we might          data collected during the program run is written out into
see a change in the future, the padding issue will never       files with the extension .gcda. These files are created
go away (amounting to 16% of the data in the example,          in the directory which contains the source file. The pro-
when ignoring the headers). Only if the programmer di-         gram can be executed from any directory, and the binary
rectly takes control of allocations can this be avoided.       can be copied, but the directory with the sources must be
When alignment requirements come into play there can           available and writable. Again, one output file is created
still be holes, but this is also something under control of    for each input source file. If the program is run multiple
the programmer.                                                times, it is important that the .gcda files of the previous
                                                               run are found in the source directories since otherwise
7.4   Improving Branch Prediction                              the data of the runs cannot be accumulated in one file.
                                                               When a representative set of tests has been run, it is time
In section 6.2.2, two methods to improve L1i use through       to recompile the application. The compiler has to be able
branch prediction and block reordering were mentioned:         to find the .gcda files in the same directory which holds
static prediction through __builtin_expect and pro-            the source files. The files cannot be moved since the com-
file guided optimization (PGO). Correct branch predic-         piler would not find them and the embedded checksum
tion has performance impacts, but here we are interested       for the files would not match anymore. For the recom-
in the memory usage improvements.                              pilation, replace the -fprofile-generate parameter
                                                               with -fprofile-use. It is essential that the sources
The use of __builtin_expect (or better the likely              do not change in any way that would change the gener-
and unlikely macros) is simple. The definitions are            ated code. That means: it is OK to change white spaces
placed in a central header and the compiler takes care         and edit comments, but adding more branches or basic
of the rest. There is a little problem, though: it is easy     blocks invalidates the collected data and the compilation
enough for a programmer to use likely when really              will fail.
unlikely was meant and vice versa. Even if somebody
uses a tool like oprofile to measure incorrect branch pre-     This is all the programmer has to do; it is a fairly sim-
dictions and L1i misses these problems are hard to detect.     ple process. The most important thing to get right is the
                                                               selection of representative tests to perform the measure-
There is one easy method, though. The code in sec-             ments. If the test workload does not match the way the
tion A.2 shows an alternative definition of the likely         program is actually used, the performed optimizations
and unlikely macros which measure actively, at run-            might actually do more harm than good. For this reason,
time, whether the static predictions are correct or not.       is it often hard to use PGO for libraries. Libraries can
The results can then be examined by the programmer or          be used in many–sometimes widely different–scenarios.
tester and adjustments can be made. The measurements           Unless the use cases are indeed similar, it is usually bet-
do not actually take the performance of the program into       ter to rely exclusively on static branch prediction using
account, they simply test the static assumptions made by       __builtin_expect.
the programmer. More details can be found, along with
the code, in the section referenced above.                     A few words on the .gcno and .gcda files. These are
                                                               binary files which are not immediately usable for inspec-
PGO is quite easy to use with gcc these days. It is a three-   tion. It is possible, though, to use the gcov tool, which is
step process, though, and certain requirements must be         also part of the gcc package, to examine them. This tool
fulfilled. First, all source files must be compiled with the   is mainly used for coverage analysis (hence the name) but
additional -fprofile-generate option. This option              the file format used is the same as for PGO. The gcov tool
must be passed to all compiler runs and to the command         generates output files with the extension .gcov for each
which links the program. Mixing object files compiled          source file with executed code (this might include sys-
with and without this option is possible, but PGO will         tem headers). The files are source listings which are an-
not do any good for those that do not have it enabled.         notated, according to the parameters given to gcov, with
The compiler generates a binary which behaves normally         branch counter, probabilities, etc.
except that it is significantly larger and slower because it
records (and stores) information about whether branches
are taken or not. The compiler also emits a file with the
extension .gcno for each input file. This file contains

      0 0x3000000000 C                        0 0x3000000B50: (within /lib64/ld-2.5.so)
      1 0x 7FF000000 D                     3320 0x3000000B53: (within /lib64/ld-2.5.so)
      2 0x3000001000 C                    58270 0x3000001080: _dl_start (in /lib64/ld-2.5.so)
      3 0x3000219000 D                   128020 0x30000010AE: _dl_start (in /lib64/ld-2.5.so)
      4 0x300021A000 D                   132170 0x30000010B5: _dl_start (in /lib64/ld-2.5.so)
      5 0x3000008000 C                 10489930 0x3000008B20: _dl_setup_hash (in /lib64/ld-2.5.so)
      6 0x3000012000 C                 13880830 0x3000012CC0: _dl_sysdep_start (in /lib64/ld-2.5.so)
      7 0x3000013000 C                 18091130 0x3000013440: brk (in /lib64/ld-2.5.so)
      8 0x3000014000 C                 19123850 0x3000014020: strlen (in /lib64/ld-2.5.so)
      9 0x3000002000 C                 23772480 0x3000002450: dl_main (in /lib64/ld-2.5.so)

                                                 Figure 7.8: Output of the pagein Tool

7.5    Page Fault Optimization                                          that page is _dl_start. The initial code accesses a vari-
                                                                        able on page 7FF00000016 . This happens just 3,320 cy-
                                                                        cles after the first page fault and is most likely the second
On operating systems like Linux with demand-paging                      instruction of the program (just three bytes after the first
support, an mmap call only modifies the page tables. It                 instruction). If one looks at the program, one will notice
makes sure that, for file-backed pages, the underlying                  that there is something peculiar about this memory ac-
data can be found and, for anonymous memory, that, on                   cess. The instruction in question is a call instruction,
access, pages initialized with zeros are provided. No ac-               which does not explicitly load or store data. It does store
tual memory is allocated at the time of the mmap call.46                the return address on the stack, though, and this is ex-
The allocation part happens when a memory page is first                 actly what happens here. This is not the official stack of
accessed, either by reading or writing data, or by exe-                 the process, though, it is valgrind’s internal stack of the
cuting code. In response to the ensuing page fault, the                 application. This means when interpreting the results of
kernel takes control and determines, using the page table               pagein it is important to keep in mind that valgrind intro-
tree, the data which has to be present on the page. This                duces some artifacts.
resolution of the page fault is not cheap, but it happens               The output of pagein can be used to determine which
for every single page which is used by a process.                       code sequences should ideally be adjacent in the pro-
To minimize the cost of page faults, the total number of                gram code. A quick look at the /lib64/ld-2.5.so
used pages has to be reduced. Optimizing the code for                   code shows that the first instructions immediately call
size will help with this. To reduce the cost of a spe-                  the function _dl_start, and that these two places are
cific code path (for instance, the start-up code), it is also           on different pages. Rearranging the code to move the
possible to rearrange code so that, in that code path, the              code sequences onto the same page can avoid–or at least
number of touched pages is minimized. It is not easy to                 delay–a page fault. It is, so far, a cumbersome process to
determine the right order, though.                                      determine what the optimal code layout should be. Since
                                                                        the second use of a page is, by design, not recorded, one
The author wrote a tool, based on the valgrind toolset,                 needs to use trial and error to see the effects of a change.
to measure page faults as they happen. Not the num-                     Using call graph analysis, it is possible to guess about
ber of page faults, but the reason why they happen. The                 possible call sequences; this might help speed up the pro-
pagein tool emits information about the order and tim-                  cess of sorting the functions and variables.
ing of page faults. The output, written to a file named
pagein.<PID>, looks as in Figure 7.8. The second col-                   At a very coarse level, the call sequences can be seen
umn specifies the address of the page which is paged-                   by looking a the object files making up the executable or
in. Whether it is a code or data page is indicated in                   DSO. Starting with one or more entry points (i.e., func-
the third column, which contains ‘C’ or ‘D’ respectively.               tion names), the chain of dependencies can be computed.
The fourth column specifies the number of cycles which                  Without much effort this works well at the object file
passed since the first page fault. The rest of the line is              level. In each round, determine which object files con-
valgrind’s attempt to find a name for the address which                 tain needed functions and variables. The seed set has to
caused the page fault. The address value itself is correct              be specified explicitly. Then determine all undefined ref-
but the name is not always accurate if no debug informa-                erences in those object files and add them to the set of
tion is available.                                                      needed symbols. Repeat until the set is stable.

In the example in Figure 7.8, execution starts at address               The second step in the process is to determine an order.
3000000B5016 , which forces the system to page in the                   The various object files have to be grouped together to fill
page at address 300000000016 . Shortly after that, the                  as few pages as possible. As an added bonus, no function
page after this is also brought in; the function called on              should cross over a page boundary. A complication in all
                                                                        this is that, to best arrange the object files, it has to be
   46 If you want to say “Wrong!” wait a second, it will be qualified
                                                                        known what the linker will do later. The important fact
later that there are exceptions.

here is that the linker will put the object files into the exe-   free to ignore the advice, but it also can pre-fault pages.
cutable or DSO in the same order in which they appear in          The advantage here is that the granularity is finer. Indi-
the input files (e.g., archives), and on the command line.        vidual pages or page ranges in any mapped address space
This gives the programmer sufficient control.                     area can be pre-faulted. For memory-mapped files which
                                                                  contain a lot of data which is not used at runtime, this can
For those who are willing to invest a bit more time, there        have huge advantages over using MAP_POPULATE.
have been successful attempts at reordering made using
automatic call tracing via the __cyg_profile_func_-               Beside these active approaches to minimizing the num-
enter and __cyg_profile_func_exit hooks gcc in-                   ber of page faults, it is also possible to take a more pas-
serts when called with the -finstrument-functions                 sive approach which is popular with the hardware design-
option [17]. See the gcc manual for more information              ers. A DSO occupies neighboring pages in the address
on these __cyg_* interfaces. By creating a trace of the           space, one range of pages each for the code and the data.
program execution, the programmer can more accurately             The smaller the page size, the more pages are needed to
determine the call chains. The results in [17] are a 5%           hold the DSO. This, in turn, means more page faults, too.
decrease in start-up costs, just through reordering of the        Important here is that the opposite is also true. For larger
functions. The main benefit is the reduced number of              page sizes, the number of necessary pages for the map-
page faults, but the TLB cache also plays a role–an in-           ping (or anonymous memory) is reduced; with it falls the
creasingly important role given that, in virtualized envi-        number of page faults.
ronments, TLB misses become significantly more expen-
sive.                                                             Most architectures support page sizes of 4k. On IA-64
                                                                  and PPC64, page sizes of 64k are also popular. That
By combining the analysis of the pagein tool with the             means the smallest unit in which memory is given out
call sequence information, it should be possible to opti-         is 64k. The value has to be specified when compiling the
mize certain phases of the program (such as start-up) to          kernel and cannot be changed dynamically (at least not
minimize the number of page faults.                               at the moment). The ABIs of the multiple-page-size ar-
                                                                  chitectures are designed to allow running an application
The Linux kernel provides two additional mechanisms to            with either page size. The runtime will make the nec-
avoid page faults. The first one is a flag for mmap which         essary adjustments, and a correctly-written program will
instructs the kernel to not only modify the page table but,       not notice a thing. Larger page sizes mean more waste
in fact, to pre-fault all the pages in the mapped area. This      through partially-used pages, but, in some situations, this
is achieved by simply adding the MAP_POPULATE flag to             is OK.
the fourth parameter of the mmap call. This will cause
the mmap call to be significantly more expensive, but, if         Most architectures also support very large page sizes of
all pages which are mapped by the call are being used             1MB or more. Such pages are useful in some situations,
right away, the benefits can be large. Instead of having a        too, but it makes no sense to have all memory given out
number of page faults, which each are pretty expensive            in units that large. The waste of physical RAM would
due to the overhead incurred by synchronization require-          simply be too large. But very large pages have their ad-
ments etc., the program would have one, more expensive,           vantages: if huge data sets are used, storing them in 2MB
mmap call. The use of this flag has disadvantages, though,        pages on x86-64 would require 511 fewer page faults (per
in cases where a large portion of the mapped pages are            large page) than using the same amount of memory with
not used soon (or ever) after the call. Mapped, unused            4k pages. This can make a big difference. The solution is
pages are obviously a waste of time and memory. Pages             to selectively request memory allocation which, just for
which are immediately pre-faulted and only much later             the requested address range, uses huge memory pages
used also can clog up the system. The memory is allo-             and, for all the other mappings in the same process, uses
cated before it is used and this might lead to shortages of       the normal page size.
memory in the meantime. On the other hand, in the worst
case, the page is simply reused for a new purpose (since          Huge page sizes come with a price, though. Since the
it has not been modified yet), which is not that expensive        physical memory used for large pages must be contin-
but still, together with the allocation, adds some cost.          uous, it might, after a while, not be possible to allo-
                                                                  cate such pages due to memory fragmentation. People
The granularity of MAP_POPULATE is simply too coarse.             are working on memory defragmentation and fragmen-
And there is a second possible problem: this is an op-            tation avoidance, but it is very complicated. For large
timization; it is not critical that all pages are, indeed,        pages of, say, 2MB the necessary 512 consecutive pages
mapped in. If the system is too busy to perform the op-           are always hard to come by, except at one time: when
eration the pre-faulting can be dropped. Once the page            the system boots up. This is why the current solution
is really used the program takes the page fault, but this         for large pages requires the use of a special filesystem,
is not worse than artificially creating resource scarcity.        hugetlbfs. This pseudo filesystem is allocated on re-
An alternative is to use the POSIX_MADV_WILLNEED ad-              quest by the system administrator by writing the number
vice with the posix_madvise function. This is a hint to           of huge pages which should be reserved to
the operating system that, in the near future, the program
                                                                         /proc/sys/vm/nr_hugepages
will need the page described in the call. The kernel is

This operation might fail if not enough continuous mem-       By using the same file name in the open call, multiple
ory can be located. The situation gets especially interest-   processes can share the same huge pages and collaborate.
ing if virtualization is used. A virtualized system using     It is also possible to make the pages executable, in which
the VMM model does not directly administrate physi-           case the PROT_EXEC flag must also be set in the mmap
cal memory and, therefore, cannot by itself allocate the      call. As in the System V shared memory example, the
hugetlbfs. It has to rely on the VMM, and this feature        value of LENGTH must be a multiple of the system’s huge
is not guaranteed to be supported. For the KVM model,         page size.
the Linux kernel running the KVM module can perform
the hugetlbfs allocation and possibly pass a subset of        A defensively-written program (as all programs should
the pages thus allocated on to one of the guest domains.      be) can determine the mount point at runtime using a
                                                              function like this:
Later, when a program needs a large page, there are mul-
tiple possibilities:
                                                              char *hugetlbfs_mntpoint(void) {
      • the program can use the System V shared memory          char *result = NULL;
                                                                FILE *fp = setmntent(_PATH_MOUNTED, "r");
        interfaces with the SHM_HUGETLB flag.
                                                                if (fp != NULL) {
      • a filesystem of type hugetlbfs can actually be            struct mntent *m;
                                                                  while ((m = getmntent(fp)) != NULL)
        mounted and the program can then create a file un-
                                                                     if (strcmp(m->mnt_fsname,
        der the mount point and use mmap to map one or
                                                                                "hugetlbfs") == 0) {
        more pages as anonymous memory.                                result = strdup(m->mnt_dir);
                                                                       break;
In the first case, the hugetlbfs need not be mounted.                }
                                                                  endmntent(fp);
Code requesting one or more large pages could look like
                                                                }
this:
                                                                return result;
                                                              }

     key_t k = ftok("/some/key/file", 42);
     int id = shmget(k, LENGTH,                               More information for both these cases can be found in
                     SHM_HUGETLB|IPC_CREAT
                                                              the hugetlbpage.txt file which comes as part of the kernel
                     |SHM_R|SHM_W);
     void *a = shmat(id, NULL, 0);
                                                              source tree. The file also describes the special handling
                                                              needed for IA-64.

                                                                                      450
The critical parts of this code sequence are the use of the                           400
SHM_HUGETLB flag and the choice of the right value for                                350
                                                                Cycles/List Element

LENGTH, which must be a multiple of the huge page size
                                                                                      300
for the system. Different architectures have different val-
ues. The use of the System V shared memory interface                                  250
has the nasty problem of depending on the key argument                                200
to differentiate (or share) mappings. The ftok interface                              150
can easily produce conflicts which is why, if possible, it                            100
is better to use other mechanisms.                                                     50
If the requirement to mount the hugetlbfs filesystem is                                 0
not a problem, it is better to use it instead of System V                               210    213 216 219 222 225            228
shared memory. The only real problems with using the                                           Working Set Size (Bytes)
special filesystem are that the kernel must support it, and                                       4kB Pages   2MB Pages
that there is no standardized mount point yet. Once the
filesystem is mounted, for instance at /dev/hugetlb, a
program can make easy use of it:                                                      Figure 7.9: Follow with Huge Pages, NPAD=0

                                                              To illustrate the advantages of huge pages, Figure 7.9
     int fd = open("/dev/hugetlb/file1",                      shows the results of running the random Follow test for
                   O_RDWR|O_CREAT, 0700);                     NPAD=0. This is the same data shown in Figure 3.15,
     void *a = mmap(NULL, LENGTH,                             but, this time, we measure the data also with memory al-
                    PROT_READ|PROT_WRITE,                     located in huge pages. As can be seen the performance
                    fd, 0);                                   advantage can be huge. For 220 bytes the test using huge
                                                              pages is 57% faster. This is due to the fact that this size

