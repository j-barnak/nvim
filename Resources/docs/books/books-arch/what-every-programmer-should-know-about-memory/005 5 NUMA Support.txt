5     NUMA Support                                                  eter of C which is the absolute minimum. AMD’s first-
                                                                    generation Opteron processors have three hypertransport
In section 2 we saw that, on some machines, the cost                links per processor. At least one of the processors has to
of access to specific regions of physical memory differs            have a Southbridge attached to one link, meaning, cur-
depending on where the access originated. This type of              rently, that a hypercube with C = 2 can be implemented
hardware requires special care from the OS and the ap-              directly and efficiently. The next generation will at some
plications. We will start with a few details of NUMA                point have four links, at which point C = 3 hypercubes
hardware, then we will cover some of the support the                will be possible.
Linux kernel provides for NUMA.
                                                                    This does not mean, though, that larger accumulations
                                                                    of processors cannot be supported. There are companies
5.1   NUMA Hardware
                                                                    which have developed crossbars allowing larger sets of
                                                                    processors to be used (e.g., Newisys’s Horus). But these
Non-uniform memory architectures are becoming more                  crossbars increase the NUMA factor and they stop being
and more common. In the simplest form of NUMA, a                    effective at a certain number of processors.
processor can have local memory (see Figure 2.3) which
                                                                    The next step up means connecting groups of CPUs and
is cheaper to access than memory local to other proces-
                                                                    implementing a shared memory for all of them. All such
sors. The difference in cost for this type of NUMA sys-
                                                                    systems need specialized hardware and are by no means
tem is not high, i.e., the NUMA factor is low.
                                                                    commodity systems. Such designs exist at several levels
NUMA is also–and especially–used in big machines. We                of complexity. A system which is still quite close to a
have described the problems of having many processors               commodity machine is IBM x445 and similar machines.
access the same memory. For commodity hardware all                  They can be bought as ordinary 4U, 8-way machines with
processors would share the same Northbridge (ignoring               x86 and x86-64 processors. Two (at some point up to
the AMD Opteron NUMA nodes for now, they have their                 four) of these machines can then be connected to work
own problems). This makes the Northbridge a severe                  as a single machine with shared memory. The intercon-
bottleneck since all memory traffic is routed through it.           nect used introduces a significant NUMA factor which
Big machines can, of course, use custom hardware in                 the OS, as well as applications, must take into account.
place of the Northbridge but, unless the memory chips
                                                                    At the other end of the spectrum, machines like SGI’s Al-
used have multiple ports–i.e. they can be used from mul-
                                                                    tix are designed specifically to be interconnected. SGI’s
tiple busses–there still is a bottleneck. Multiport RAM
                                                                    NUMAlink interconnect fabric is very fast and has low
is complicated and expensive to build and support and,
                                                                    latency at the same time; both properties are require-
therefore, it is hardly ever used.
                                                                    ments for high-performance computing (HPC), specifi-
The next step up in complexity is the model AMD uses                cally when Message Passing Interfaces (MPI) are used.
where an interconnect mechanism (Hyper Transport in                 The drawback is, of course, that such sophistication and
AMD’s case, technology they licensed from Digital) pro-             specialization is very expensive. They make a reason-
vides access for processors which are not directly con-             ably low NUMA factor possible but with the number of
nected to the RAM. The size of the structures which can             CPUs these machines can have (several thousands) and
be formed this way is limited unless one wants to in-               the limited capacity of the interconnects, the NUMA fac-
crease the diameter (i.e., the maximum distance between             tor is actually dynamic and can reach unacceptable levels
any two nodes) arbitrarily.                                         depending on the workload.
                                                                    More commonly used are solutions where many com-
                                                 6       8
                                                                    modity machines are connected using high-speed net-
      2           2      4                           7              working to form a cluster. These are no NUMA ma-
                                     5                              chines, though; they do not implement a shared address
                                             2               4      space and therefore do not fall into any category which is
                                                                    discussed here.
      1           1      3               1           3
                                                                    5.2    OS Support for NUMA
    C=1           C=2                        C=3

                                                                    To support NUMA machines, the OS has to take the dis-
Figure 5.1: Hypercubes                             tributed nature of the memory into account. For instance,
                                                                    if a process is run on a given processor, the physical
An efficient topology for connecting the nodes is the hy-           RAM assigned to the process’s address space should ide-
percube, which limits the number of nodes to 2C where               ally come from local memory. Otherwise each instruc-
C is the number of interconnect interfaces each node has.           tion has to access remote memory for code and data.
Hypercubes have the smallest diameter for all systems               There are special cases to be taken into account which
with 2n CPUs and n interconnects. Figure 5.1 shows                  are only present in NUMA machines. The text segment
the first three hypercubes. Each hypercube has a diam-              of DSOs is normally present exactly once in a machine’s

physical RAM. But if the DSO is used by processes and           This is a pessimization which helps the system avoid se-
threads on all CPUs (for instance, the basic runtime li-        vere problems and makes it more predictable under nor-
braries like libc) this means that all but a few proces-        mal operation. But it does decrease overall system per-
sors have to have remote accesses. The OS ideally would         formance, in some situations significantly. This is why
“mirror” such DSOs into each processor’s physical RAM           Linux allows the memory allocation rules to be selected
and use local copies. This is an optimization, not a re-        by each process. A process can select a different strategy
quirement, and generally hard to implement. It might            for itself and its children. We will introduce the inter-
not be supported or only in a limited fashion.                  faces which can be used for this in section 6.
To avoid making the situation worse, the OS should not          5.3     Published Information
migrate a process or thread from one node to another.
The OS should already try to avoid migrating processes
on normal multi-processor machines because migrating            The kernel publishes, through the sys pseudo file system
from one processor to another means the cache content is        (sysfs), information about the processor caches below
lost. If load distribution requires migrating a process or
                                                                      /sys/devices/system/cpu/cpu*/cache
thread off of a processor, the OS can usually pick an ar-
bitrary new processor which has sufficient capacity left.       In section 6.2.1 we will see interfaces which can be used
In NUMA environments the selection of the new proces-           to query the size of the various caches. What is important
sor is a bit more limited. The newly selected processor         here is the topology of the caches. The directories above
should not have higher access costs to the memory the           contain subdirectories (named index*) which list infor-
process is using than the old processor; this restricts the     mation about the various caches the CPU possesses. The
list of targets. If there is no free processor matching that    files type, level, and shared_cpu_map are the im-
criteria available, the OS has no choice but to migrate to      portant files in these directories as far as the topology is
a processor where memory access is more expensive.              concerned. For an Intel Core 2 QX6700 the information
                                                                looks as in Table 5.1.
In this situation there are two possible ways forward.
First, one can hope the situation is temporary and the                                 type       level    shared_cpu_map
process can be migrated back to a better-suited proces-                   index0    Data             1          00000001
sor. Alternatively, the OS can also migrate the process’s         cpu0    index1    Instruction      1          00000001
memory to physical pages which are closer to the newly-                   index2    Unified          2          00000003
used processor. This is quite an expensive operation.                     index0    Data             1          00000002
Possibly huge amounts of memory have to be copied, al-            cpu1    index1    Instruction      1          00000002
                                                                          index2    Unified          2          00000003
beit not necessarily in one step. While this is happening
                                                                          index0    Data             1          00000004
the process, at least briefly, has to be stopped so that mod-
                                                                  cpu2    index1    Instruction      1          00000004
ifications to the old pages are correctly migrated. There                 index2    Unified          2          0000000c
are a whole list of other requirements for page migration                 index0    Data             1          00000008
to be efficient and fast. In short, the OS should avoid it        cpu3    index1    Instruction      1          00000008
unless it is really necessary.                                            index2    Unified          2          0000000c
Generally, it cannot be assumed that all processes on a
NUMA machine use the same amount of memory such                  Table 5.1: sysfs Information for Core 2 CPU Caches
that, with the distribution of processes across the pro-
cessors, memory usage is also equally distributed. In
                                                                What this data means is as follows:
fact, unless the applications running on the machines are
very specific (common in the HPC world, but not out-
side) the memory use will be very unequal. Some appli-                • Each core25 has three caches: L1i, L1d, L2.
cations will use vast amounts of memory, others hardly                • The L1d and L1i caches are not shared with any
any. This will, sooner or later, lead to problems if mem-               other core–each core has its own set of caches.
ory is always allocated local to the processor where the                This is indicated by the bitmap in shared_cpu_-
request is originated. The system will eventually run out               map having only one set bit.
of memory local to nodes running large processes.
                                                                      • The L2 cache on cpu0 and cpu1 is shared, as is
In response to these severe problems, memory is, by de-                 the L2 on cpu2 and cpu3.
fault, not allocated exclusively on the local node. To uti-
lize all the system’s memory the default strategy is to
stripe the memory. This guarantees equal use of all the         If the CPU had more cache levels, there would be more
memory of the system. As a side effect, it becomes possi-       index* directories.
ble to freely migrate processes between processors since,       For a four-socket, dual-core Opteron machine the cache
on average, the access cost to all the memory used does         information looks like Table 5.2. As can be seen these
not change. For small NUMA factors, striping is accept-           25 The knowledge that cpu0 to cpu3 are cores comes from another
able but still not optimal (see data in section 5.4).           place that will be explained shortly.

                     type       level   shared_cpu_map                   (physical_package_id 0 to 3),
         index0   Data            1           00000001
 cpu0    index1   Instruction     1           00000001             c) each processor has two cores, and
         index2   Unified         2           00000001
         index0   Data            1           00000002             d) none of the cores share any cache.
 cpu1    index1   Instruction     1           00000002
         index2   Unified         2           00000002          This is exactly what corresponds to earlier Opterons.
         index0   Data            1           00000004
 cpu2    index1   Instruction     1           00000004          What is completely missing in the data provided so far is
         index2   Unified         2           00000004          information about the nature of NUMA on this machine.
         index0   Data            1           00000008          Any SMP Opteron machine is a NUMA machine. For
 cpu3    index1   Instruction     1           00000008          this data we have to look at yet another part of the sys
         index2   Unified         2           00000008          file system which exists on NUMA machines, namely in
         index0   Data            1           00000010          the hierarchy below
 cpu4    index1   Instruction     1           00000010
         index2   Unified         2           00000010                  /sys/devices/system/node
         index0   Data            1           00000020
 cpu5    index1   Instruction     1           00000020          This directory contains a subdirectory for every NUMA
         index2   Unified         2           00000020          node on the system. In the node-specific directories there
         index0   Data            1           00000040          are a number of files. The important files and their con-
 cpu6    index1   Instruction     1           00000040          tent for the Opteron machine described in the previous
         index2   Unified         2           00000040          two tables are shown in Table 5.4.
         index0   Data            1           00000080
 cpu7    index1   Instruction     1           00000080                                      cpumap     distance
         index2   Unified         2           00000080                          node0      00000003 10 20 20 20
                                                                                node1      0000000c 20 10 20 20
Table 5.2: sysfs Information for Opteron CPU Caches                             node2      00000030 20 20 10 20
                                                                                node3      000000c0 20 20 20 10
processors also have three caches: L1i, L1d, L2. None
of the cores shares any level of cache. The interesting                Table 5.4: sysfs Information for Opteron Nodes
part for this system is the processor topology. Without
this additional information one cannot make sense of the
cache data. The sys file system exposes this information        This information ties all the rest together; now we have
in the files below                                              a complete picture of the architecture of the machine.
                                                                We already know that the machine has four processors.
     /sys/devices/system/cpu/cpu*/topology                      Each processor constitutes its own node as can be seen
                                                                by the bits set in the value in cpumap file in the node*
Table 5.3 shows the interesting files in this hierarchy for
                                                                directories. The distance files in those directories con-
the SMP Opteron machine.
                                                                tains a set of values, one for each node, which represent
        physical_                     core_      thread_        a cost of memory accesses at the respective nodes. In
        package_id     core_id    siblings      siblings        this example all local memory accesses have the cost 10,
 cpu0                       0     00000003       00000001       all remote access to any other node has the cost 20.26
              0                                                 This means that, even though the processors are orga-
 cpu1                       1     00000003       00000002
                                                                nized as a two-dimensional hypercube (see Figure 5.1),
 cpu2                       0     0000000c       00000004
              1                                                 accesses between processors which are not directly con-
 cpu3                       1     0000000c       00000008
                                                                nected is not more expensive. The relative values of the
 cpu4                       0     00000030       00000010       costs should be usable as an estimate of the actual differ-
              2
 cpu5                       1     00000030       00000020       ence of the access times. The accuracy of all this infor-
 cpu6                       0     000000c0       00000040       mation is another question.
              3
 cpu7                       1     000000c0       00000080
                                                                5.4      Remote Access Costs

Table 5.3: sysfs Information for Opteron CPU Topol-
ogy                                                             The distance is relevant, though. In [1] AMD documents
                                                                the NUMA cost of a four socket machine. For write op-
Taking Table 5.2 and Table 5.3 together we can see that         erations the numbers are shown in Figure 5.3. Writes
                                                                  26 This is, by the way, incorrect.The ACPI information is appar-
   a) none of the CPU has hyper-threads (the thread_-           ently wrong since, although the processors used have three coherent
      siblings bitmaps have one bit set),                       HyperTransport links, at least one processor must be connected to a
                                                                Southbridge. At least one pair of nodes must therefore have a larger
   b) the system in fact has a total of four processors         distance.

00400000 default file=/bin/cat mapped=3 N3=3
00504000 default file=/bin/cat anon=1 dirty=1 mapped=2 N3=2
00506000 default heap anon=3 dirty=3 active=0 N3=3
38a9000000 default file=/lib64/ld-2.4.so mapped=22 mapmax=47 N1=22
38a9119000 default file=/lib64/ld-2.4.so anon=1 dirty=1 N3=1
38a911a000 default file=/lib64/ld-2.4.so anon=1 dirty=1 N3=1
38a9200000 default file=/lib64/libc-2.4.so mapped=53 mapmax=52 N1=51 N2=2
38a933f000 default file=/lib64/libc-2.4.so
38a943f000 default file=/lib64/libc-2.4.so anon=1 dirty=1 mapped=3 mapmax=32 N1=2 N3=1
38a9443000 default file=/lib64/libc-2.4.so anon=1 dirty=1 N3=1
38a9444000 default anon=4 dirty=4 active=0 N3=4
2b2bbcdce000 default anon=1 dirty=1 N3=1
2b2bbcde4000 default anon=2 dirty=2 N3=2
2b2bbcde6000 default file=/usr/lib/locale/locale-archive mapped=11 mapmax=8 N0=11
7fffedcc7000 default stack anon=2 dirty=2 N3=2

Figure 5.2: Content of /proc/PID/numa_maps

are slower than reads, this is no surprise. The interest-                  status of a process itself. It is possible to determine how
ing parts are the costs of the 1- and 2-hop cases. The                     the memory-mapped files, the Copy-On-Write (COW)27
two 1-hop cases actually have slightly different costs.                    pages and anonymous memory are distributed over the
See [1] for the details. The fact we need to remem-                        nodes in the system. For each process the kernel pro-
ber from this chart is that 2-hop reads and writes are                     vides a pseudo-file /proc/PID/numa_maps, where PID
30% and 49% (respectively) slower than 0-hop reads. 2-                     is the ID of the process, as shown in Figure 5.2. The
hop writes are 32% slower than 0-hop writes, and 17%                       important information in the file is the values for N0 to
slower than 1-hop writes. The relative position of pro-                    N3, which indicate the number of pages allocated for the
cessor and memory nodes can make a big difference.                         memory area on nodes 0 to 3. It is a good guess that the
The next generation of processors from AMD will fea-                       program was executed on a core on node 3. The pro-
ture four coherent HyperTransport links per processor. In                  gram itself and the dirtied pages are allocated on that
that case a four socket machine would have diameter of                     node. Read-only mappings, such as the first mapping for
one. With eight sockets the same problem returns, with a                   ld-2.4.so and libc-2.4.so as well as the shared file
vengeance, since the diameter of a hypercube with eight                    locale-archive are allocated on other nodes.
nodes is three.
                                                                           As we have seen in Figure 5.3, when performed across
                          160%                                             nodes the read performance falls by 9% and 30% respec-
                                                                           tively for 1- and 2-hop reads. For execution, such reads
                          140%                                             are needed and, if the L2 cache is missed, each cache line
 Slowdown Vs 0-Hop Read

                          120%                                             incurs these additional costs. All the costs measured for
                                                                           large workloads beyond the size of the cache would have
                          100%                                             to be increased by 9%/30% if the memory is remote to
                          80%                                              the processor.
                          60%                                              To see the effects in the real world we can measure the
                                                                           bandwidth as in section 3.5.1 but this time with the mem-
                          40%
                                                                           ory being on a remote node, one hop away. The result
                          20%                                              of this test when compared with the data for using local
                                                                           memory can be seen in Figure 5.4. The numbers have a
                           0%
                                                                           few big spikes in both directions which are the result of
                                 0 Hop  1 Hop    1 Hop       2 Hop         a problem of measuring multi-threaded code and can be
                                      Number of Hops                       ignored. The important information in this graph is that
                                       Reads   Writes                      read operations are always 20% slower. This is signifi-
                                                                           cantly slower than the 9% in Figure 5.3, which is, most
                                                                           likely, not a number for uninterrupted read/write opera-
Figure 5.3:                      Read/Write Performance with Multiple      tions and might refer to older processor revisions. Only
Nodes
                                                                              27 Copy-On-Write is a method often used in OS implementations

                                                                           when a memory page has one user at first and then has to be copied
All this information is available but it is cumbersome to
                                                                           to allow independent users. In many situations the copying is unneces-
use. In section 6.5 we will see an interface which helps                   sary, at all or at first, in which case it makes sense to only copy when
accessing and using this information easier.                               either user modifies the memory. The operating system intercepts the
                                                                           write operation, duplicates the memory page, and then allows the write
The last piece of information the system provides is in the                instruction to proceed.

