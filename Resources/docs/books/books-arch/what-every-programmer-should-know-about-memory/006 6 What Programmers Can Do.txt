                            15%                                                            6      What Programmers Can Do
                            10%
Slowdown Vs Local Memory
                             5%                                                            After the descriptions in the previous sections it is clear
                             0%                                                            that there are many, many opportunities for programmers
                            −5%                                                            to influence a program’s performance, positively or neg-
                           −10%                                                            atively. And this is for memory-related operations only.
                           −15%                                                            We will proceed in covering the opportunities from the
                           −20%                                                            ground up, starting with the lowest levels of physical
                           −25%                                                            RAM access and L1 caches, up to and including OS func-
                           −30%                                                            tionality which influences memory handling.
                           −35%
                           −40%                                                            6.1     Bypassing the Cache
                                  10    13       16       19          22   25   28
                              2        2     2     2      2     2               2
                                       Working Set Size (Bytes)                            When data is produced and not (immediately) consumed
                                          Read    Write        Copy
                                                                                           again, the fact that memory store operations read a full
                                                                                           cache line first and then modify the cached data is detri-
                                                                                           mental to performance. This operation pushes data out
Figure 5.4: Operating on Remote Memory
                                                                                           of the caches which might be needed again in favor of
                                                                                           data which will not be used soon. This is especially true
AMD knows.                                                                                 for large data structures, like matrices, which are filled
                                                                                           and then used later. Before the last element of the matrix
For working set sizes which fit into the caches, the perfor-                               is filled the sheer size evicts the first elements, making
mance of write and copy operations is also 20% slower.                                     caching of the writes ineffective.
For working sets exceeding the size of the caches, the
write performance is not measurably slower than the op-                                    For this and similar situations, processors provide sup-
eration on the local node. The speed of the interconnect                                   port for non-temporal write operations. Non-temporal in
is fast enough to keep up with the memory. The dominat-                                    this context means the data will not be reused soon, so
ing factor is the time spent waiting on the main memory.                                   there is no reason to cache it. These non-temporal write
                                                                                           operations do not read a cache line and then modify it;
                                                                                           instead, the new content is directly written to memory.
                                                                                           This might sound expensive but it does not have to be.
                                                                                           The processor will try to use write-combining (see sec-
                                                                                           tion 3.3.3) to fill entire cache lines. If this succeeds no
                                                                                           memory read operation is needed at all. For the x86 and
                                                                                           x86-64 architectures a number of intrinsics are provided
                                                                                           by gcc:

                                                                                           #include <emmintrin.h>
                                                                                           void _mm_stream_si32(int *p, int a);
                                                                                           void _mm_stream_si128(int *p, __m128i a);
                                                                                           void _mm_stream_pd(double *p, __m128d a);
                                                                                           #include <xmmintrin.h>
                                                                                           void _mm_stream_pi(__m64 *p, __m64 a);
                                                                                           void _mm_stream_ps(float *p, __m128 a);
                                                                                           #include <ammintrin.h>
                                                                                           void _mm_stream_sd(double *p, __m128d a);
                                                                                           void _mm_stream_ss(float *p, __m128 a);

                                                                                           These instructions are used most efficiently if they pro-
                                                                                           cess large amounts of data in one go. Data is loaded from
                                                                                           memory, processed in one or more steps, and then written
                                                                                           back to memory. The data “streams” through the proces-
                                                                                           sor, hence the names of the intrinsics.
                                                                                           The memory address must be aligned to 8 or 16 bytes re-
                                                                                           spectively. In code using the multimedia extensions it is
                                                                                           possible to replace the normal _mm_store_* intrinsics

with these non-temporal versions. In the matrix multi-                                     i                               j
plication code in section A.1 we do not do this since the
written values are reused in a short order of time. This
is an example where using the stream instructions is not
useful. More on this code in section 6.2.1.
The processor’s write-combining buffer can hold requests
for partial writing to a cache line for only so long. It
is generally necessary to issue all the instructions which
modify a single cache line one after another so that the
                                                               j                               i
write-combining can actually take place. An example for
how to do this is as follows:
Figure 6.1: Matrix Access Pattern

#include <emmintrin.h>
void setbytes(char *p, int c)                                  havior shown in Figure 6.1.
{
  __m128i i = _mm_set_epi8(c, c, c, c,
                                                               We measure the time it takes to initialize a 3000 × 3000
                           c, c, c, c,                         matrix. To see how memory behaves, we use store in-
                           c, c, c, c,                         structions which do not use the cache. On IA-32 proces-
                           c, c, c, c);                        sors the “non-temporal hint” is used for this. For com-
  _mm_stream_si128((__m128i *)&p[0], i);                       parison we also measure ordinary store operations. The
  _mm_stream_si128((__m128i *)&p[16], i);                      results can be seen in Table 6.1.
  _mm_stream_si128((__m128i *)&p[32], i);
  _mm_stream_si128((__m128i *)&p[48], i);                                                  Inner Loop Increment
}                                                                                            Row        Column
                                                                         Normal            0.048s        0.127s
                                                                         Non-Temporal      0.048s        0.160s
Assuming the pointer p is appropriately aligned, a call to
this function will set all bytes of the addressed cache line
Table 6.1: Timing Matrix Initialization
to c. The write-combining logic will see the four gener-
ated movntdq instructions and only issue the write com-
mand for the memory once the last instruction has been         For the normal writes which do use the cache we see
executed. To summarize, this code sequence not only            the expected result: if memory is used sequentially we
avoids reading the cache line before it is written, it also    get a much better result, 0.048s for the whole operation
avoids polluting the cache with data which might not be        translating to about 750MB/s, compared to the more-or-
needed soon. This can have huge benefits in certain situa-     less random access which takes 0.127s (about 280MB/s).
tions. An example of everyday code using this technique        The matrix is large enough that the caches are essentially
is the memset function in the C runtime, which should          ineffective.
use a code sequence like the above for large blocks.
                                                               The part we are mainly interested in here are the writes
Some architectures provide specialized solutions. The          bypassing the cache. It might be surprising that the se-
PowerPC architecture defines the dcbz instruction which        quential access is just as fast here as in the case where the
can be used to clear an entire cache line. The instruction     cache is used. The reason for this result is that the proces-
does not really bypass the cache since a cache line is al-     sor is performing write-combining as explained above.
located for the result, but no data is read from memory. It    In addition, the memory ordering rules for non-temporal
is more limited than the non-temporal store instructions       writes are relaxed: the program needs to explicitly in-
since a cache line can only be set to all-zeros and it pol-    sert memory barriers (sfence instructions for the x86
lutes the cache (in case the data is non-temporal), but no     and x86-64 processors). This means the processor has
write-combining logic is needed to achieve the results.        more freedom to write back the data and thereby using
                                                               the available bandwidth as well as possible.
To see the non-temporal instructions in action we will
look at a new test which is used to measure writing to a       In the case of column-wise access in the inner loop the
matrix, organized as a two-dimensional array. The com-         situation is different. The results for uncached accesses
piler lays out the matrix in memory so that the leftmost       are significantly slower than in the case of cached ac-
(first) index addresses the row which has all elements laid    cesses (0.16s, about 225MB/s). Here we can see that no
out sequentially in memory. The right (second) index ad-       write combining is possible and each memory cell must
dresses the elements in a row. The test program iterates       be addressed individually. This requires constantly se-
over the matrix in two ways: first by increasing the col-      lecting new rows in the RAM chips with all the associ-
umn number in the inner loop and then by increasing the        ated delays. The result is a 25% worse result than the
row index in the inner loop. This means we get the be-         cached run.

On the read side, processors, until recently, lacked sup-       mance. In this section we will show what kinds of code
port aside from weak hints using non-temporal access            changes can help to improve that performance. Contin-
(NTA) prefetch instructions. There is no equivalent to          uing from the previous section, we first concentrate on
write-combining for reads, which is especially bad for          optimizations to access memory sequentially. As seen in
uncacheable memory such as memory-mapped I/O. In-               the numbers of section 3.3, the processor automatically
tel, with the SSE4.1 extensions, introduced NTA loads.          prefetches data when memory is accessed sequentially.
They are implemented using a small number of streaming
load buffers; each buffer contains a cache line. The first      The example code used is a matrix multiplication. We
movntdqa instruction for a given cache line will load a
                                                                use two square matrices of 1000 × 1000 double ele-
cache line into a buffer, possibly replacing another cache      ments. For those who have forgotten the math, given
line. Subsequent 16-byte aligned accesses to the same           two matrices A and B with elements aij and bij with
cache line will be serviced from the load buffer at little      0 ≤ i, j < N the product is
cost. Unless there are other reasons to do so, the cache
line will not be loaded into a cache, thus enabling the                     N
                                                                            X −1
loading of large amounts of memory without polluting             (AB)ij =          aik bkj = ai1 b1j + ai2 b2j + · · · + ai(N −1) b(N −1)j
                                                                            k=0
the caches. The compiler provides an intrinsic for this
instruction:
                                                                A straight-forward C implementation of this can look like
                                                                this:
#include <smmintrin.h>
__m128i _mm_stream_load_si128 (__m128i *p);
                                                                  for (i = 0; i < N; ++i)
                                                                    for (j = 0; j < N; ++j)
                                                                      for (k = 0; k < N; ++k)
This intrinsic should be used multiple times, with ad-                  res[i][j] += mul1[i][k] * mul2[k][j];
dresses of 16-byte blocks passed as the parameter, un-
til each cache line is read. Only then should the next
cache line be started. Since there are a few streaming
read buffers it might be possible to read from two mem-         The two input matrices are mul1 and mul2. The result
ory locations at once.                                          matrix res is assumed to be initialized to all zeroes. It
                                                                is a nice and simple implementation. But it should be
What we should take away from this experiment is that           obvious that we have exactly the problem explained in
modern CPUs very nicely optimize uncached write and             Figure 6.1. While mul1 is accessed sequentially, the in-
more recently even read accesses as long as they are se-        ner loop advances the row number of mul2. That means
quential. This knowledge can come in very handy when            that mul1 is handled like the left matrix in Figure 6.1
handling large data structures which are used only once.        while mul2 is handled like the right matrix. This cannot
Second, caches can help to cover up some–but not all–of         be good.
the costs of random memory access. Random access in
this example is 70% slower due to the implementation of         There is one possible remedy one can easily try. Since
RAM access. Until the implementation changes, random            each element in the matrices is accessed multiple times it
accesses should be avoided whenever possible.                   might be worthwhile to rearrange (“transpose,” in math-
                                                                ematical terms) the second matrix mul2 before using it.
In the section about prefetching we will again take a look
at the non-temporal flag.
                                                                            N −1
                                                                            X           T         T         T                      T
                                                                 (AB)ij =          aik bjk = ai1 bj1 + ai2 bj2 + · · · + ai(N −1) bj(N −1)
6.2     Cache Access                                                        k=0

Programmers wishing to improve their programs’ perfor-          After the transposition (traditionally indicated by a su-
mance will find it best to focus on changes affected the        perscript ‘T’) we now iterate over both matrices sequen-
level 1 cache since those will likely yield the best results.   tially. As far as the C code is concerned, it now looks
We will discuss it first before extending the discussion to     like this:
the other levels. Obviously, all the optimizations for the
level 1 cache also affect the other caches. The theme for
all memory access is the same: improve locality (spatial
                                                                  double tmp[N][N];
and temporal) and align the code and data.                        for (i = 0; i < N; ++i)
                                                                    for (j = 0; j < N; ++j)
                                                                      tmp[i][j] = mul2[j][i];
6.2.1    Optimizing Level 1 Data Cache Access                     for (i = 0; i < N; ++i)
                                                                    for (j = 0; j < N; ++j)
                                                                      for (k = 0; k < N; ++k)
In section section 3.3 we have already seen how much                    res[i][j] += mul1[i][k] * tmp[j][k];

the effective use of the L1d cache can improve perfor-

We create a temporary variable to contain the transposed               this means that, to fully utilize the cache line, we should
matrix. This requires touching additional memory, but                  unroll the middle loop 8 times. Continuing this thought,
this cost is, hopefully, recovered since the 1000 non-                 to effectively use the res matrix as well, i.e., to write 8
sequential accesses per column are more expensive (at                  results at the same time, we should unroll the outer loop 8
least on modern hardware). Time for some performance                   times as well. We assume here cache lines of size 64 but
tests. The results on a Intel Core 2 with 2666MHz clock                the code works also well on systems with 32 byte cache
speed are (in clock cycles):                                           lines since both cache lines are also 100% utilized. In
                                                                       general it is best to hardcode cache line sizes at compile
                          Original          Transposed                 time by using the getconf utility as in:
         Cycles       16,765,297,870      3,922,373,010
         Relative         100%                23.4%                      gcc -DCLS=$(getconf LEVEL1_DCACHE_LINESIZE) ...

Through the simple transformation of the matrix we can                 If the binaries are supposed to be generic, the largest
achieve a 76.6% speed-up! The copy operation is more                   cache line size should be used. With very small L1ds
than made up. The 1000 non-sequential accesses really                  this might mean that not all the data fits into the cache
hurt.                                                                  but such processors are not suitable for high-performance
                                                                       programs anyway. The code we arrive at looks some-
The next question is whether this is the best we can do.               thing like this:
We certainly need an alternative method anyway which
does not require the additional copy. We will not always
have the luxury to be able to perform the copy: the matrix
                                                                         #define SM (CLS / sizeof (double))
can be too large or the available memory too small.
                                                                         for (i = 0; i < N; i += SM)
The search for an alternative implementation should start                  for (j = 0; j < N; j += SM)
                                                                             for (k = 0; k < N; k += SM)
with a close examination of the math involved and the op-                      for (i2 = 0, rres = &res[i][j],
erations performed by the original implementation. Triv-                            rmul1 = &mul1[i][k]; i2 < SM;
                                                                                    ++i2, rres += N, rmul1 += N)
ial math knowledge allows us to see that the order in                            for (k2 = 0, rmul2 = &mul2[k][j];
which the additions for each element of the result ma-                                k2 < SM; ++k2, rmul2 += N)
                                                                                   for (j2 = 0; j2 < SM; ++j2)
trix are performed is irrelevant as long as each addend                              rres[j2] += rmul1[k2] * rmul2[j2];
appears exactly once.28 This understanding allows us to
look for solutions which reorder the additions performed
in the inner loop of the original code.
                                                                       This looks quite scary. To some extent it is but only
Now let us examine the actual problem in the execution                 because it incorporates some tricks. The most visible
of the original code. The order in which the elements                  change is that we now have six nested loops. The outer
of mul2 are accessed is: (0,0), (1,0), . . . , (N -1,0), (0,1),        loops iterate with intervals of SM (the cache line size di-
(1,1), . . . . The elements (0,0) and (0,1) are in the same            vided by sizeof(double)). This divides the multipli-
cache line but, by the time the inner loop completes one               cation in several smaller problems which can be handled
round, this cache line has long been evicted. For this                 with more cache locality. The inner loops iterate over
example, each round of the inner loop requires, for each               the missing indexes of the outer loops. There are, once
of the three matrices, 1000 cache lines (with 64 bytes for             again, three loops. The only tricky part here is that the k2
the Core 2 processor). This adds up to much more than                  and j2 loops are in a different order. This is done since,
the 32k of L1d available.                                              in the actual computation, only one expression depends
                                                                       on k2 but two depend on j2.
But what if we handle two iterations of the middle loop
together while executing the inner loop? In this case                  The rest of the complication here results from the fact
we use two double values from the cache line which                     that gcc is not very smart when it comes to optimizing ar-
is guaranteed to be in L1d. We cut the L1d miss rate in                ray indexing. The introduction of the additional variables
half. That is certainly an improvement, but, depending                 rres, rmul1, and rmul2 optimizes the code by pulling
on the cache line size, it still might not be as good as we            common expressions out of the inner loops, as far down
can get it. The Core 2 processor has a L1d cache line size             as possible. The default aliasing rules of the C and C++
of 64 bytes. The actual value can be queried using                     languages do not help the compiler making these deci-
                                                                       sions (unless restrict is used, all pointer accesses are
     sysconf (_SC_LEVEL1_DCACHE_LINESIZE)                              potential sources of aliasing). This is why Fortran is still
                                                                       a preferred language for numeric programming: it makes
at runtime or using the getconf utility from the com-                  writing fast code easier.29
mand line so that the program can be compiled for a spe-                 29 In theory the restrict keyword introduced into the C lan-

cific cache line size. With sizeof(double) being 8                     guage in the 1999 revision should solve the problem. Compilers have
                                                                       not caught up yet, though. The reason is mainly that too much incorrect
  28 We ignore arithmetic effects here which might change the occur-   code exists which would mislead the compiler and cause it to generate
rence of overflows, underflows, or rounding.                           incorrect object code.

                                     Original         Transposed                               Sub-Matrix           Vectorized
                     Cycles      16,765,297,870      3,922,373,010                            2,895,041,480        1,588,711,750
                     Relative        100%                23.4%                                    17.3%                9.47%

Table 6.2: Matrix Multiplication Timing

How all this work pays off can be seen in Table 6.2. By                                       35%
avoiding the copying we gain another 6.1% of perfor-
mance. Plus, we do not need any additional memory.                                            30%

                                                                   Slowdown Vs 1 Cache Line
The input matrices can be arbitrarily large as long as the                                    25%
result matrix fits into memory as well. This is a require-
ment for a general solution which we have now achieved.                                       20%

There is one more column in Table 6.2 which has not                                           15%
been explained. Most modern processors nowadays in-                                           10%
clude special support for vectorization. Often branded
as multi-media extensions, these special instructions al-                                      5%
low processing of 2, 4, 8, or more values at the same                                          0%
time. These are often SIMD (Single Instruction, Mul-
tiple Data) operations, augmented by others to get the                                        −5%
data in the right form. The SSE2 instructions provided                                           210    213 216 219 222 225                       228
by Intel processors can handle two double values in one                                                 Working Set Size (Bytes)
operation. The instruction reference manual lists the in-                                       Sequential 4 CLs   Random 2 CLs    Random 4 CLs
trinsic functions which provide access to these SSE2 in-
structions. If these intrinsics are used the program runs
another 7.3% (relative to the original) faster. The result is                         Figure 6.2: Spreading Over Multiple Cache Lines
a program which runs in 10% of the time of the original
code. Translated into numbers which people recognize,           Unsurprisingly, in all cases there are no negative effects
we went from 318 MFLOPS to 3.35 GFLOPS. Since we                if the working set fits into L1d. Once L1d is no longer
are only interested in memory effects here, the program         sufficient, penalties are paid by using two cache lines in
code is pushed out into section A.1.                            the process instead of one. The red line shows the data
It should be noted that, in the last version of the code,       when the list is laid out sequentially in memory. We see
we still have some cache problems with mul2; prefetch-          the usual two step patterns: about 17% penalty when the
ing still will not work. But this cannot be solved with-        L2 cache is sufficient and about 27% penalty when the
out transposing the matrix. Maybe the cache prefetching         main memory has to be used.
units will get smarter to recognize the patterns, then no       In the case of random memory accesses the relative data
additional change would be needed. 3.19 GFLOPS on a             looks a bit different. The slowdown for working sets
2.66 GHz processor with single-threaded code is not bad,        which fit into L2 is between 25% and 35%. Beyond that
though.                                                         it goes down to about 10%. This is not because the penal-
What we optimized in the example of the matrix multi-           ties get smaller but, instead, because the actual memory
plication is the use of the loaded cache lines. All bytes of    accesses get disproportionally more costly. The data also
a cache line are always used. We just made sure they are        shows that, in some cases, the distance between the el-
used before the cache line is evacuated. This is certainly      ements does matter. The Random 4 CLs curve shows
a special case.                                                 higher penalties because the first and fourth cache lines
                                                                are used.
It is much more common to have data structures which
fill one or more cache lines where the program uses only        An easy way to see the layout of a data structure com-
a few members at any one time. In Figure 3.11 we have           pared to cache lines is to use the pahole program (see
already seen the effects of large structure sizes if only       [4]). This program examines the data structures defined
few members are used.                                           in a binary. Take a program containing this definition:

Figure 6.2 shows the results of yet another set of bench-
marks performed using the by now well-known program.            struct foo {
This time two values of the same list element are added.           int a;
In one case, both elements are in the same cache line; in          long fill[7];
another case, one element is in the first cache line of the        int b;
list element and the second is in the last cache line. The      };
graph shows the slowdown we are experiencing.

struct foo {
        int                                        a;                            /*        0       4 */

           /* XXX 4 bytes hole, try to pack */

        long int                   fill[7];                                      /*        8      56 */
        /* --- cacheline 1 boundary (64 bytes) --- */
        int                        b;                                            /*      64        4 */
}; /* size: 72, cachelines: 2 */
   /* sum members: 64, holes: 1, sum holes: 4 */
   /* padding: 4 */
   /* last cacheline: 8 bytes */

Figure 6.3: Output of pahole Run

When compiled on a 64-bit machine, the output of pa-             grammer should always follow the following two rules:
hole contains (among other things) the output shown in
Figure 6.3. This output tells us a lot. First, it shows that        1. Always move the structure element which is most
the data structure uses up more than one cache line. The               likely to be the critical word to the beginning of
tool assumes the currently used processor’s cache line                 the structure.
size, but this value can be overridden using a command
line parameter. Especially in cases where the size of the           2. When accessing the data structures, and the order
structure is barely over the limit of a cache line, and many           of access is not dictated by the situation, access the
objects of this type are allocated, it makes sense to seek             elements in the order in which they are defined in
a way to compress that structure. Maybe a few elements                 the structure.
can have a smaller type, or maybe some fields are actu-
ally flags which can be represented using individual bits.       For small structures, this means that the elements should
                                                                 be arranged in the order in which they are likely accessed.
In the case of the example the compression is easy and it        This must be handled in a flexible way to allow the other
is hinted at by the program. The output shows that there         optimizations, such as filling holes, to be applied as well.
is a hole of four bytes after the first element. This hole is    For bigger data structures each cache line-sized block
caused by the alignment requirement of the structure and         should be arranged to follow the rules.
the fill element. It is easy to see that the element b,
which has a size of four bytes (indicated by the 4 at the        If the object itself is not aligned as expected, reorder-
end of the line), fits perfectly into the gap. The result in     ing elements is not worth the time it takes, though. The
this case is that the gap no longer exists and that the data     alignment of an object is determined by the alignment
structure fits onto one cache line. The pahole tool can          requirement of the data type. Each fundamental type has
perform this optimization itself. If the --reorganize            its own alignment requirement. For structured types the
parameter is used and the structure name is added at the         largest alignment requirement of any of its elements de-
end of the command line the output of the tool is the op-        termines the alignment of the structure. This is almost
timized structure and the cache line use. Besides moving         always smaller than the cache line size. This means even
elements to fill gaps, the tool can also optimize bit fields     if the members of a structure are lined up to fit into the
and combine padding and holes. For more details see [4].         same cache line an allocated object might not have an
                                                                 alignment matching the cache line size. There are two
Having a hole which is just large enough for the trailing        ways to ensure that the object has the alignment which
element is, of course, the ideal situation. For this opti-       was used when designing the layout of the structure:
mization to be useful it is required that the object itself is
aligned to a cache line. We get to that in a bit.                    • the object can be allocated with an explicit align-
The pahole output also allows to see easily whether ele-               ment requirement. For dynamic allocation a call
ments have to be reordered so that those elements which                to malloc would only allocate the object with an
are used together are also stored together. Using the pa-              alignment matching that of the most demanding
hole tool, it is easily possible to determine which ele-               standard type (usually long double). It is pos-
ments are on the same cache line and when, instead, the                sible to use posix_memalign, though, to request
elements have to be reshuffled to achieve that. This is not            higher alignments.
an automatic process but the tool can help quite a bit.
                                                                       #include <stdlib.h>
The position of the individual structure elements and the              int posix_memalign(void **memptr,
way they are used is important, too. As we have seen                                       size_t align,
in section 3.5.2 the performance of code with the critical                                 size_t size);
word late in the cache line is worse. This means a pro-

      The function stores a pointer pointing to the newly-                                  400%
      allocated memory in the pointer variable pointed to

                                                               Slowdown Vs Aligned Access
      by memptr. The memory block is size bytes in                                          350%
      size and is aligned on a align-byte boundary.                                         300%
      For objects allocated by the compiler (in .data,                                      250%
      .bss, etc, and on the stack) a variable attribute can
      be used:                                                                              200%
                                                                                            150%
        struct strtype variable                                                             100%
           __attribute((aligned(64)));
                                                                                            50%
                                                                                             0%
      In this case the variable is aligned at a 64 byte                                        210   213 216 219 222 225                228
      boundary regardless of the alignment requirement                                               Working Set Size (Bytes)
                                                                                                      Sequential Inc   Random Inc
      of the strtype structure. This works for global
      variables as well as automatic variables.
      For arrays this method does not work as one might                                     Figure 6.4: Overhead of Unaligned Accesses
      expect. Only the first element of the array would
      be aligned unless the size of each array element is
      a multiple of the alignment value. It also means        Figure 6.4 shows the effects of unaligned memory ac-
      that every single variable must be annotated ap-        cesses. The now well-known tests which increment a
      propriately. The use of posix_memalign is also          data element while visiting memory (sequentially or ran-
      not entirely free since the alignment requirements      domly) are measured, once with aligned list elements and
      usually lead to fragmentation and/or higher mem-        once with deliberately misaligned elements. The graph
      ory consumption.                                        shows the slowdown the program incurs because of the
                                                              unaligned accesses. The effects are more dramatic for the
    • the alignment requirement of a user-defined type        sequential access case than for the random case because,
      can be changed by using a type attribute:               in the latter case, the costs of unaligned accesses are par-
                                                              tially hidden by the generally higher costs of the mem-
                                                              ory access. In the sequential case, for working set sizes
        struct strtype {                                      which do fit into the L2 cache, the slowdown is about
          ...members...
        } __attribute((aligned(64)));
                                                              300%. This can be explained by the reduced effective-
                                                              ness of the L1 cache. Some increment operations now
                                                              touch two cache lines, and beginning work on a list ele-
                                                              ment now often requires reading of two cache lines. The
      This will cause the compiler to allocate all objects    connection between L1 and L2 is simply too congested.
      with the appropriate alignment, including arrays.
                                                              For very large working set sizes, the effects of the un-
      The programmer has to take care of requesting the
                                                              aligned access are still 20% to 30%–which is a lot given
      appropriate alignment for dynamically allocated ob-
                                                              that the aligned access time for those sizes is long. This
      jects, though. Here once again posix_memalign
                                                              graph should show that alignment must be taken seri-
      must be used. It is easy enough to use the alignof
                                                              ously. Even if the architecture supports unaligned ac-
      operator gcc provides and pass the value as the sec-
                                                              cesses, this must not be taken as “they are as good as
      ond parameter to posix_memalign.
                                                              aligned accesses”.
                                                              There is some fallout from these alignment requirements,
The multimedia extensions previously mentioned in this
                                                              though. If an automatic variable has an alignment re-
section almost always require that the memory accesses
                                                              quirement, the compiler has to ensure that it is met in all
are aligned. I.e., for 16 byte memory accesses the address
                                                              situations. This is not trivial since the compiler has no
is supposed to be 16 byte aligned. The x86 and x86-64
                                                              control over the call sites and the way they handle the
processors have special variants of the memory opera-
                                                              stack. This problem can be handled in two ways:
tions which can handle unaligned accesses but these are
slower. This hard alignment requirement is nothing new
for most RISC architectures which require full alignment                               1. The generated code actively aligns the stack, in-
for all memory accesses. Even if an architecture sup-                                     serting gaps if necessary. This requires code to
ports unaligned accesses this is sometimes slower than                                    check for alignment, create alignment, and later
using appropriate alignment, especially if the misalign-                                  undo the alignment.
ment causes a load or store to use two cache lines instead
of one.                                                                                2. Require that all callers have the stack aligned.

All of the commonly used application binary interfaces         the results in Figure 3.11: in this case we had increas-
(ABIs) follow the second route. Programs will likely fail      ing amounts of unused data in the elements of the array.
if a caller violates the rule and alignment is needed in the   The result was that prefetching was increasingly less ef-
callee. Keeping alignment intact does not come for free,       fective and the program, for large data sets, became less
though.                                                        efficient.
The size of a stack frame used in a function is not neces-     For large working sets it is important to use the available
sarily a multiple of the alignment. This means padding is      cache as well as possible. To achieve this, it might be
needed if other functions are called from this stack frame.    necessary to rearrange data structures. While it is easier
The big difference is that the stack frame size is, in most    for the programmer to put all the data which conceptually
cases, known to the compiler and, therefore, it knows          belongs together in the same data structure, this might
how to adjust the stack pointer to ensure alignment for        not be the best approach for maximum performance. As-
any function which is called from that stack frame. In         sume we have a data structure as follows:
fact, most compilers will simply round the stack frame
size up and be done with it.
                                                                 struct order {
This simple way to handle alignment is not possible if
                                                                    double price;
variable length arrays (VLAs) or alloca are used. In
                                                                    bool paid;
that case, the total size of the stack frame is only known          const char *buyer[5];
at runtime. Active alignment control might be needed in             long buyer_id;
this case, making the generated code (slightly) slower.          };
On some architectures, only the multimedia extensions
require strict alignment; stacks on those architectures are
always minimally aligned for the normal data types, usu-       Further assume that these records are stored in a big array
ally 4 or 8 byte alignment for 32- and 64-bit architectures    and that a frequently-run job adds up the expected pay-
respectively. On these systems, enforcing the alignment        ments of all the outstanding bills. In this scenario, the
incurs unnecessary costs. That means that, in this case,       memory used for the buyer and buyer_id fields is un-
we might want to get rid of the strict alignment require-      necessarily loaded into the caches. Judging from the data
ment if we know that it is never depended upon. Tail           in Figure 3.11 the program will perform up to 5 times
functions (those which call no other functions) which do       worse than it could.
no multimedia operations do not need alignment. Nei-
                                                               It is much better to split the order data structure in two
ther do functions which only call functions which need
                                                               pieces, storing the first two fields in one structure and the
no alignment. If a large enough set of functions can be
                                                               other fields elsewhere. This change certainly increases
identified, a program might want to relax the alignment
                                                               the complexity of the program, but the performance gains
requirement. For x86 binaries gcc has support for relaxed
                                                               might justify this cost.
stack alignment requirements:
                                                               Finally, let us consider another cache use optimization
                                                               which, while also applying to the other caches, is pri-
     -mpreferred-stack-boundary=2
                                                               marily felt in the L1d access. As seen in Figure 3.8 an
                                                               increased associativity of the cache benefits normal op-
If this option is given a value of N , the stack alignment     eration. The larger the cache, the higher the associativity
requirement will be set to 2N bytes. So, if a value of 2       usually is. The L1d cache is too large to be fully associa-
is used, the stack alignment requirement is reduced from       tive but not large enough to have the same associativity as
the default (which is 16 bytes) to just 4 bytes. In most       L2 caches. This can be a problem if many of the objects
cases this means no additional alignment operation is          in the working set fall into the same cache set. If this
needed since normal stack push and pop operations work         leads to evictions due to overuse of a set, the program
on four-byte boundaries anyway. This machine-specific          can experience delays even though much of the cache is
option can help to reduce code size and also improve ex-       unused. These cache misses are sometimes called con-
ecution speed. But it cannot be applied for many other         flict misses. Since the L1d addressing uses virtual ad-
architectures. Even for x86-64 it is generally not appli-      dresses, this is actually something the programmer can
cable since the x86-64 ABI requires that floating-point        have control over. If variables which are used together
parameters are passed in an SSE register and the SSE in-       are also stored together the likelihood of them falling into
structions require full 16 byte alignment. Nevertheless,       the same set is minimized. Figure 6.5 shows how quickly
whenever the option is usable it can make a noticeable         the problem can hit.
difference.                                                    In the figure, the now familiar Follow30 with NPAD=15
Efficient placement of structure elements and alignment        test is measured with a special setup. The X–axis is the
are not the only aspects of data structures which influence    distance between two list elements, measured in empty
cache efficiency. If an array of structures is used, the en-    30 The test was performed on a 32-bit machine, hence NPAD=15

tire structure definition affects performance. Remember        means one 64-byte cache line per list element.

     z
                                                                                                                              y
10
 9                                                                                                                                16
 8                                                                                                                           14
 7                                                                                                                      12
 6                                                                                                                 10
 5
 4                                                                                                             8
 3                                                                                                         6
 2                                                                                                     4
 1                                                                                                 2
 0                                                                                                         x
            16         32          48         64          80          96          112      128

Figure 6.5: Cache Associativity Effects

list elements. In other words, a distance of 2 means that      effects and degraded performance. Unaligned accesses
the next element’s address is 128 bytes after the previ-       can increase the probability of conflict misses since each
ous one. All elements are laid out in the virtual ad-          access might require an additional cache line.
dress space with the same distance. The Y–axis shows
the total length of the list. Only one to 16 elements are                    14               7 6    4 3      0
used, meaning that the total working set size is 64 to                             Index         Bank    Byte
1024 bytes. The z–axis shows the average number of
cycles needed to traverse each list element.
Figure 6.6: Bank Address of L1d on AMD
The result shown in the figure should not be surprising.
If few elements are used, all the data fits into L1d and the   If this optimization is performed, another related opti-
access time is only 3 cycles per list element. The same        mization is possible, too. AMD’s processors, at least,
is true for almost all arrangements of the list elements:      implement the L1d as several individual banks. The L1d
the virtual addresses are nicely mapped to L1d slots with      can receive two data words per cycle but only if both
almost no conflicts. There are two (in this graph) spe-        words are stored in different banks or in a bank with the
cial distance values for which the situation is different.     same index. The bank address is encoded in the low bits
If the distance is a multiple of 4096 bytes (i.e., distance    of the virtual address as shown in Figure 6.6. If variables
of 64 elements) and the length of the list is greater than     which are used together are also stored together the like-
eight, the average number of cycles per list element in-       lihood that they are in different banks or the same bank
creases dramatically. In these situations all entries are in   with the same index is high.
the same set and, once the list length is greater than the
associativity, entries are flushed from L1d and have to be
re-read from L2 the next round. This results in the cost       6.2.2       Optimizing Level 1 Instruction Cache Access
of about 10 cycles per list element.
With this graph we can determine that the processor used       Preparing code for good L1i use needs similar techniques
has an L1d cache with associativity 8 and a total size of      as good L1d use. The problem is, though, that the pro-
32kB. That means that the test could, if necessary, be         grammer usually does not directly influence the way L1i
used to determine these values. The same effects can be        is used unless s/he writes code in assembler. If compil-
measured for the L2 cache but, here, it is more complex        ers are used, programmers can indirectly determine the
since the L2 cache is indexed using physical addresses         L1i use by guiding the compiler to create a better code
and it is much larger.                                         layout.

Programmers will hopefully see this data as an indica-         Code has the advantage that it is linear between jumps.
tion that set associativity is something worth paying at-      In these periods the processor can prefetch memory effi-
tention to. Laying out data at boundaries that are powers      ciently. Jumps disturb this nice picture because
of two happens often enough in the real world, but this is
exactly the situation which can easily lead to the above
                                                                      • the jump target might not be statically determined;

      • and even if it is static the memory fetch might take              which are known to increase the code size are disabled.
        a long time if it misses all caches.                              Using this option often produces surprising results. Es-
                                                                          pecially if the compiler cannot really take advantage of
These problems create stalls in execution with a possibly                 loop unrolling and inlining, this option is a big win.
severe impact on performance. This is why today’s pro-                    Inlining can be controlled individually as well. The com-
cessors invest heavily in branch prediction (BP). Highly                  piler has heuristics and limits which guide inlining; these
specialized BP units try to determine the target of a jump                limits can be controlled by the programmer. The -finline-
as far ahead of the jump as possible so that the processor                limit option specifies how large a function must be to be
can initiate loading the instructions at the new location                 considered too large for inlining. If a function is called
into the cache. They use static and dynamic rules and are                 in multiple places, inlining it in all of them would cause
increasingly good at determining patterns in execution.                   an explosion in the code size. But there is more. As-
Getting data into the cache as soon as possible is even                   sume a function inlcand is called in two functions f1
more important for the instruction cache. As mentioned                    and f2. The functions f1 and f2 are themselves called
in section 3.1, instructions have to be decoded before                    in sequence.
they can be executed and, to speed this up (important
                                                                                start f1                   start inlcand
on x86 and x86-64), instructions are actually cached in
                                                                                  code f1                    code inlcand
the decoded form, not in the byte/word form read from
                                                                                  inlined inlcand          end inlcand
memory.
                                                                                  more code f1
To achieve the best L1i use programmers should look out                         end f1                     start f1
for at least the following aspects of code generation:                                                       code f1
                                                                                start f2                   end f1
                                                                                  code f2
     1. reduce the code footprint as much as possible. This
                                                                                  inlined inlcand          start f2
        has to be balanced with optimizations like loop un-
                                                                                  more code f2               code f2
        rolling and inlining.
                                                                                end f2                     end f2
     2. code execution should be linear without bubbles.31
     3. aligning code when it makes sense.                                               Table 6.3: Inlining Vs Not

We will now look at some compiler techniques available                    Table 6.3 shows how the generated code could look like
to help with optimizing programs according to these as-                   in the cases of no inline and inlining in both functions.
pects.                                                                    If the function inlcand is inlined in both f1 and f2 the
                                                                          total size of the generated code is size f1 + size f2 +
Compilers have options to enable levels of optimization;                  2× size inlcand. If no inlining happens, the total size
specific optimizations can also be individually enabled.                  is smaller by size inlcand. This is how much more L1i
Many of the optimizations enabled at high optimization                    and L2 cache is needed if f1 and f2 are called shortly af-
levels (-O2 and -O3 for gcc) deal with loop optimizations                 ter one another. Plus: if inlcand is not inlined, the code
and function inlining. In general, these are good opti-                   might still be in L1i and it will not have to be decoded
mizations. If the code which is optimized in these ways                   again. Plus: the branch prediction unit might do a bet-
accounts for a significant part of the total execution time               ter job of predicting jumps since it has already seen the
of the program, overall performance can be improved.                      code. If the compiler default for the upper limit on the
Inlining of functions, in particular, allows the compiler to              size of inlined functions is not the best for the program,
optimize larger chunks of code at a time which, in turn,                  it should be lowered.
enables the generation of machine code which better ex-
ploits the processor’s pipeline architecture. The handling                There are cases, though, when inlining always makes
of both code and data (through dead code elimination or                   sense. If a function is only called once it might as well be
value range propagation, and others) works better when                    inlined. This gives the compiler the opportunity to per-
larger parts of the program can be considered as a single                 form more optimizations (like value range propagation,
unit.                                                                     which might significantly improve the code). That inlin-
                                                                          ing might be thwarted by the selection limits. gcc has, for
A larger code size means higher pressure on the L1i (and                  cases like this, an option to specify that a function is al-
also L2 and higher level) caches. This can lead to less                   ways inlined. Adding the always_inline function at-
performance. Smaller code can be faster. Fortunately gcc                  tribute instructs the compiler to do exactly what the name
has an optimization option to specify this. If -Os is used                suggests.
the compiler will optimize for code size. Optimizations
  31 Bubbles describe graphically the holes in the execution in the
                                                                          In the same context, if a function should never be inlined
pipeline of a processor which appear when the execution has to wait
                                                                          despite being small enough, the noinline function at-
for resources. For more details the reader is referred to literature on   tribute can be used. Using this attribute makes sense even
processor design.                                                         for small functions if they are called often from different

places. If the L1i content can be reused and the overall       often-executed code is linear in memory while the rarely-
footprint is reduced this often makes up for the additional    executed code is moved somewhere where it does not
cost of the extra function call. Branch prediction units       hurt prefetching and L1i efficiency.
are pretty good these days. If inlining can lead to more
aggressive optimizations things look different. This is        gcc provides two methods to achieve this. First, the com-
something which must be decided on a case-by-case ba-          piler can take profiling output into account while recom-
sis.                                                           piling code and lay out the code blocks according to the
                                                               profile. We will see how this works in section 7. The
The always_inline attribute works well if the inline           second method is through explicit branch prediction. gcc
code is always used. But what if this is not the case?         recognizes __builtin_expect:
What if the inlined function is called only occasionally:

                                                                 long __builtin_expect(long EXP, long C);

  void fct(void) {
    ... code block A ...
   if (condition)
     inlfct()
                                                               This construct tells the compiler that the expression EXP
   ... code block C ...                                        most likely will have the value C. The return value is EXP.
  }
                                                               __builtin_expect is meant to be used in an condi-
                                                               tional expression. In almost all cases will it be used in the
                                                               context of boolean expressions in which case it is much
The code generated for such a code sequence in general         more convenient to define two helper macros:
matches the structure of the sources. That means first
comes the code block A, then a conditional jump which,
if the condition evaluates to false, jumps forward. The          #define unlikely(expr) __builtin_expect(!!(expr), 0)
                                                                 #define likely(expr) __builtin_expect(!!(expr), 1)
code generated for the inlined inlfct comes next, and
finally the code block C. This looks all reasonable but it
has a problem.
                                                               These macros can then be used as in
If the condition is frequently false, the execution is
not linear. There is a big chunk of unused code in the
middle which not only pollutes the L1i due to prefetch-          if (likely(a > 1))
ing, it also can cause problems with branch prediction. If
the branch prediction is wrong the conditional expression
can be very inefficient.                                       If the programmer makes use of these macros and then
                                                               uses the -freorder-blocks optimization option gcc
This is a general problem and not specific to inlining         will reorder blocks as in the figure above. This option is
functions. Whenever conditional execution is used and          enabled with -O2 but disabled for -Os. There is another
it is lopsided (i.e., the expression far more often leads to   gcc option to reorder block (-freorder-blocks-and-
one result than the other) there is the potential for false    partition) but it has limited usefulness because it does
static branch prediction and thus bubbles in the pipeline.     not work with exception handling.
This can be prevented by telling the compiler to move
the less often executed code out of the main code path.        There is another big advantage of small loops, at least
In that case the conditional branch generated for an if        on certain processors. The Intel Core 2 front end has a
statement would jump to a place out of the order as can        special feature called Loop Stream Detector (LSD). If a
be seen in the following figure.                               loop has no more than 18 instructions (none of which
                                                               is a call to a subroutine), requires only up to 4 decoder
                                                               fetches of 16 bytes, has at most 4 branch instructions, and
                                                               is executed more than 64 times, than the loop is some-
            A        I     B           C
                                                               times locked in the instruction queue and therefore more
                                                               quickly available when the loop is used again. This ap-
            A        I      C                 B                plies, for instance, to small inner loops which are entered
                                                               many times through an outer loop. Even without such
                                                               specialized hardware compact loops have advantages.
The upper parts represents the simple code layout. If the      Inlining is not the only aspect of optimization with re-
area B, e.g. generated from the inlined function inlfct        spect to L1i. Another aspect is alignment, just as for
above, is often not executed because the conditional I         data. There are obvious differences: code is a mostly lin-
jumps over it, the prefetching of the processor will pull      ear blob which cannot be placed arbitrarily in the address
in cache lines containing block B which are rarely used.       space and it cannot be influenced directly by the pro-
Using block reordering this can be changed, with a re-         grammer as the compiler generates the code. There are
sult that can be seen in the lower part of the figure. The     some aspects which the programmer can control, though.

Aligning each single instruction does not make any sense.                 assembler the function and all instructions in it can be
The goal is to have the instruction stream be sequential.                 explicitly aligned. The assembler provides for all archi-
So alignment only makes sense in strategic places. To                     tectures the .align pseudo-op to do that. For high-level
decide where to add alignments it is necessary to under-                  languages the compiler must be told about alignment re-
stand what the advantages can be. Having an instruction                   quirements. Unlike for data types and variables this is not
at the beginning of a cache line32 means that the prefetch                possible in the source code. Instead a compiler option is
of the cache line is maximized. For instructions this also                used:
means the decoder is more effective. It is easy to see that,
if an instruction at the end of a cache line is executed, the
                                                                            -falign-functions=N
processor has to get ready to read a new cache line and
decode the instructions. There are things which can go
wrong (such as cache line misses), meaning that an in-                    This option instructs the compiler to align all functions
struction at the end of the cache line is, on average, not                to the next power-of-two boundary greater than N. That
as effectively executed as one at the beginning.                          means a gap of up to N bytes is created. For small func-
                                                                          tions using a large value for N is a waste. Equally for
Combine this with the follow-up deduction that the prob-                  code which is executed only rarely. The latter can hap-
lem is most severe if control was just transferred to the                 pen a lot in libraries which can contain both popular and
instruction in question (and hence prefetching is not ef-                 not-so-popular interfaces. A wise choice of the option
fective) and we arrive at our final conclusion where align-               value can speed things up or save memory by avoiding
ment of code is most useful:                                              alignment. All alignment is turned off by using one as
                                                                          the value of N or by using the -fno-align-functions
     • at the beginning of functions;                                     option.
     • at the beginning of basic blocks which are reached                 The alignment for the second case above–beginning of
       only through jumps;                                                basic blocks which are not reached sequentially–can be
                                                                          controlled with a different option:
     • to some extent, at the beginning of loops.

In the first two cases the alignment comes at little cost.                  -falign-jumps=N
Execution proceeds at a new location and, if we choose
it to be at the beginning of a cache line, we optimize                    All the other details are equivalent, the same warning
prefetching and decoding.33 The compiler accomplishes                     about waste of memory applies.
this alignment through the insertion of a series of no-op
instructions to fill the gap created by aligning the code.                The third case also has its own option:
This “dead code” takes a little space but does not nor-
mally hurt performance.                                                     -falign-loops=N
The third case is slightly different: aligning the begin-
ning of each loop might create performance problems.                      Yet again, the same details and warnings apply. Except
The problem is that beginning of a loop often follows                     that here, as explained before, alignment comes at a run-
other code sequentially. If the circumstances are not very                time cost since either no-ops or a jump instruction has to
lucky there will be a gap between the previous instruc-                   be executed if the aligned address is reached sequentially.
tion and the aligned beginning of the loop. Unlike in the
previous two cases, this gap cannot be completely dead.                   gcc knows about one more option for controlling align-
After execution of the previous instruction the first in-                 ment which is mentioned here only for completeness.
struction in the loop must be executed. This means that,                  -falign-labels aligns every single label in the code
following the previous instruction, there either must be a                (basically the beginning of each basic block). This, in
number of no-op instructions to fill the gap or there must                all but a few exceptional cases, slows down the code and
be an unconditional jump to the beginning of the loop.                    therefore should not be used.
Neither possibility is free. Especially if the loop itself
is not executed often, the no-ops or the jump might cost                  6.2.3   Optimizing Level 2 and Higher Cache Access
more than one saves by aligning the loop.
There are three ways the programmer can influence the                     Everything said about optimizations for level 1 caches
alignment of code. Obviously, if the code is written in                   also applies to level 2 and higher cache accesses. There
                                                                          are two additional aspects of last level caches:
   32 For some processors cache lines are not the atomic blocks for in-

structions. The Intel Core 2 front end issues 16 byte blocks to the de-
coder. They are appropriately aligned and so no issued block can span         • cache misses are always very expensive. While
a cache line boundary. Aligning at the beginning of a cache line still
has advantages since it optimizes the positive effects of prefetching.
                                                                                L1 misses (hopefully) frequently hit L2 and higher
   33 For instruction decoding processors often use a smaller unit than         cache, thus limiting the penalties, there is obvi-
cache lines, 16 bytes in case of x86 and x86-64.                                ously no fallback for the last level cache.

    • L2 caches and higher are often shared by multiple       Once we have a formula for the memory requirement we
      cores and/or hyper-threads. The effective cache         can compare it with the cache size. As mentioned before,
      size available to each execution unit is therefore      the cache might be shared with multiple other cores. Cur-
      usually less than the total cache size.                 rently34 the only way to get correct information without
                                                              hardcoding knowledge is through the /sys filesystem.
                                                              In Table 5.2 we have seen the what the kernel publishes
To avoid the high costs of cache misses, the working          about the hardware. A program has to find the directory:
set size should be matched to the cache size. If data is              /sys/devices/system/cpu/cpu*/cache
only needed once this obviously is not necessary since
the cache would be ineffective anyway. We are talking         for the last level cache. This can be recognized by the
about workloads where the data set is needed more than        highest numeric value in the level file in that directory.
once. In such a case the use of a working set which is        When the directory is identified the program should read
too large to fit into the cache will create large amounts     the content of the size file in that directory and divide
of cache misses which, even with prefetching being per-       the numeric value by the number of bits set in the bitmask
formed successfully, will slow down the program.              in the file shared_cpu_map.

A program has to perform its job even if the data set         The value which is computed this way is a safe lower
is too large. It is the programmer’s job to do the work       limit. Sometimes a program knows a bit more about the
in a way which minimizes cache misses. For last-level         behavior of other threads or processes. If those threads
caches this is possible–just as for L1 caches–by work-        are scheduled on a core or hyper-thread sharing the cache,
ing on the job in smaller pieces. This is very similar to     and the cache usage is known to not exhaust its fraction
the optimized matrix multiplication on page 50. One dif-      of the total cache size, then the computed limit might be
ference, though, is that, for last level caches, the data     too low to be optimal. Whether more than the fair share
blocks which are be worked on can be bigger. The code         should be used really depends on the situation. The pro-
becomes yet more complicated if L1 optimizations are          grammer has to make a choice or has to allow the user to
needed, too. Imagine a matrix multiplication where the        make a decision.
data sets–the two input matrices and the output matrix–
do not fit into the last level cache together. In this case   6.2.4    Optimizing TLB Usage
it might be appropriate to optimize the L1 and last level
cache accesses at the same time.                              There are two kinds of optimization of TLB usage. The
The L1 cache line size is usually constant over many pro-     first optimization is to reduce the number of pages a pro-
cessor generations; even if it is not, the differences will   gram has to use. This automatically results in fewer TLB
be small. It is no big problem to just assume the larger      misses. The second optimization is to make the TLB
size. On processors with smaller cache sizes two or more      lookup cheaper by reducing the number higher level di-
cache lines will then be used instead of one. In any case,    rectory tables which must be allocated. Fewer tables
it is reasonable to hardcode the cache line size and opti-    means less memory is used which can result in higher
mize the code for it.                                         cache hit rates for the directory lookup.

For higher level caches this is not the case if the program   The first optimization is closely related to the minimiza-
is supposed to be generic. The sizes of those caches can      tion of page faults. We will cover that topic in detail
vary widely. Factors of eight or more are not uncommon.       in section 7.5. While page faults usually are a one-time
It is not possible to assume the larger cache size as a de-   cost, TLB misses are a perpetual penalty given that the
fault since this would mean the code performs poorly on       TLB cache is usually small and it is flushed frequently.
all machines except those with the biggest cache. The         Page faults are orders of magnitude more expensive than
opposite choice is bad too: assuming the smallest cache       TLB misses but, if a program is running long enough
means throwing away 87% of the cache or more. This is         and certain parts of the program are executed frequently
bad; as we can see from Figure 3.14 using large caches        enough, TLB misses can outweigh even page fault costs.
can have a huge impact on the program’s speed.                It is therefore important to regard page optimization not
                                                              only from the perspective of page faults but also from the
What this means is that the code must dynamically ad-         TLB miss perspective. The difference is that, while page
just itself to the cache line size. This is an optimiza-      fault optimizations only require page-wide grouping of
tion specific to the program. All we can say here is          the code and data, TLB optimization requires that, at any
that the programmer should compute the program’s re-          point in time, as few TLB entries are in use as possible.
quirements correctly. Not only are the data sets them-
selves needed, the higher level caches are also used for      The second TLB optimization is even harder to control.
other purposes; for example, all the executed instructions    The number of page directories which have to be used
are loaded from cache. If library functions are used this     depends on the distribution of the address ranges used in
cache usage might add up to a significant amount. Those       the virtual address space of the process. Widely vary-
library functions might also need data of their own which     ing locations in the address space mean more directories.
further reduces the available memory.                           34 There definitely will sometime soon be a better way!

A complication is that Address Space Layout Random-            all expect there to be more than one stream of mem-
ization (ASLR) leads to exactly these situations. The          ory accesses. The processor tries to automatically assign
load addresses of stack, DSOs, heap, and possibly exe-         each cache miss to such a stream and, if the threshold
cutable are randomized at runtime to prevent attackers of      is reached, start hardware prefetching. CPUs today can
the machine from guessing the addresses of functions or        keep track of eight to sixteen separate streams for the
variables.                                                     higher level caches.
Only if maximum performance is critical ASLR should            The units responsible for the pattern recognition are asso-
be turned off. The costs of the extra directories is low       ciated with the respective cache. There can be a prefetch
enough to make this step unnecessary in all but a few ex-      unit for the L1d and L1i caches. There is most probably
treme cases. One possible optimization the kernel could        a prefetch unit for the L2 cache and higher. The L2 and
at any time perform is to ensure that a single mapping         higher prefetch unit is shared with all the other cores and
does not cross the address space boundary between two          hyper-threads using the same cache. The number of eight
directories. This would limit ASLR in a minimal fashion        to sixteen separate streams therefore is quickly reduced.
but not enough to substantially weaken it.
                                                               Prefetching has one big weakness: it cannot cross page
The only way a programmer is directly affected by this         boundaries. The reason should be obvious when one
is when an address space region is explicitly requested.       realizes that the CPUs support demand paging. If the
This happens when using mmap with MAP_FIXED. Allo-             prefetcher were allowed to cross page boundaries, the
cating new a address space region this way is very dan-        access might trigger an OS event to make the page avail-
gerous and hardly ever done. It is possible, though, and,      able. This by itself can be bad, especially for perfor-
if it is used and the addresses can be freely chosen, the      mance. What is worse is that the prefetcher does not
programmer should know about the boundaries of the last        know about the semantics of the program or the OS itself.
level page directory and select the requested address ap-      It might therefore prefetch pages which, in real life, never
propriately.                                                   would be requested. That means the prefetcher would
                                                               run past the end of the memory region the processor ac-
6.3     Prefetching                                            cessed in a recognizable pattern before. This is not only
                                                               possible, it is very likely. If the processor, as a side effect
                                                               of a prefetch, triggered a request for such a page the OS
The purpose of prefetching is to hide the latency of a         might even be completely thrown off its tracks if such a
memory access. The command pipeline and out-of-order           request could never otherwise happen.
(OOO) execution capabilities of today’s processors can
hide some latency but, at best, only for accesses which        It is therefore important to realize that, regardless of how
hit the caches. To cover the latency of main memory ac-        good the prefetcher is at predicting the pattern, the pro-
cesses, the command queue would have to be incredibly          gram will experience cache misses at page boundaries
long. Some processors without OOO try to compensate            unless it explicitly prefetches or reads from the new page.
by increasing the number of cores, but this is a bad trade     This is another reason to optimize the layout of data as
unless all the code in use is parallelized.                    described in section 6.2 to minimize cache pollution by
                                                               keeping unrelated data out.
Prefetching can further help to hide latency. The proces-
sor performs prefetching on its own, triggered by certain      Because of this page limitation the processors today do
events (hardware prefetching) or explicitly requested by       not have terribly sophisticated logic to recognize prefetch
the program (software prefetching).                            patterns. With the still predominant 4k page size there is
                                                               only so much which makes sense. The address range in
                                                               which strides are recognized has been increased over the
6.3.1    Hardware Prefetching
                                                               years, but it probably does not make much sense to go
                                                               beyond the 512 byte window which is often used today.
The trigger for the CPU to start hardware prefetching is       Currently prefetch units do not recognize non-linear ac-
usually a sequence of two or more cache misses in a            cess patterns. It is more likely than not that such patterns
certain pattern. These cache misses can be to succeed-         are truly random or, at least, sufficiently non-repeating
ing or preceding cache lines. In old implementations           that it makes no sense to try recognizing them.
only cache misses to adjacent cache lines are recognized.
With contemporary hardware, strides are recognized as          If hardware prefetching is accidentally triggered there is
well, meaning that skipping a fixed number of cache lines      only so much one can do. One possibility is to try to
is recognized as a pattern and handled appropriately.          detect this problem and change the data and/or code lay-
                                                               out a bit. This is likely to prove hard. There might be
It would be bad for performance if every single cache          special localized solutions like using the ud2 instruc-
miss triggered a hardware prefetch. Random memory              tion35 on x86 and x86-64 processors. This instruction,
access patterns, for instance to global variables, are quite   which cannot be executed itself, is used after an indirect
common and the resulting prefetches would mostly waste         jump instruction; it is used as a signal to the instruction
FSB bandwidth. This is why, to kickstart prefetching,
at least two cache misses are needed. Processors today           35 Or non-instruction. It is the recommended undefined opcode.

fetcher that the processor should not waste efforts decod-     ently. What can generally be said is that _MM_HINT_T0
ing the following memory since the execution will con-         fetches data to all levels of the cache for inclusive caches
tinue at a different location. This is a very special sit-     and to the lowest level cache for exclusive caches. If
uation, though. In most cases one has to live with this        the data item is in a higher level cache it is loaded into
problem.                                                       L1d. The _MM_HINT_T1 hint pulls the data into L2 and
                                                               not into L1d. If there is an L3 cache the _MM_HINT_T2
It is possible to completely or partially disable hardware     hints can do something similar for it. These are details,
prefetching for the entire processor. On Intel proces-         though, which are weakly specified and need to be veri-
sors an Model Specific Register (MSR) is used for this         fied for the actual processor in use. In general, if the data
(IA32 MISC ENABLE, bit 9 on many processors; bit 19            is to be used right away using _MM_HINT_T0 is the right
disables only the adjacent cache line prefetch). This, in      thing to do. Of course this requires that the L1d cache
most cases, has to happen in the kernel since it is a privi-   size is large enough to hold all the prefetched data. If the
leged operation. If profiling shows that an important ap-      size of the immediately used working set is too large, pre-
plication running on a system suffers from bandwidth ex-       fetching everything into L1d is a bad idea and the other
haustion and premature cache evictions due to hardware         two hints should be used.
prefetches, using this MSR is a possibility.
                                                               The fourth hint, _MM_HINT_NTA, allows telling the pro-
6.3.2   Software Prefetching                                   cessor to treat the prefetched cache line specially. NTA
                                                               stands for non-temporal aligned which we already ex-
                                                               plained in section 6.1. The program tells the processor
The advantage of hardware prefetching is that programs
                                                               that polluting caches with this data should be avoided as
do not have to be adjusted. The drawbacks, as just de-
                                                               much as possible since the data is only used for a short
scribed, are that the access patterns must be trivial and
                                                               time. The processor can therefore, upon loading, avoid
that prefetching cannot happen across page boundaries.
                                                               reading the data into the lower level caches for inclusive
For these reasons we now have more possibilities, soft-
                                                               cache implementations. When the data is evicted from
ware prefetching the most important of them. Software
                                                               L1d the data need not be pushed into L2 or higher but,
prefetching does require modification of the source code
                                                               instead, can be written directly to memory. There might
by inserting special instructions. Some compilers sup-
                                                               be other tricks the processor designers can deploy if this
port pragmas to more or less automatically insert pre-
                                                               hint is given. The programmer must be careful using this
fetch instructions. On x86 and x86-64 Intel’s convention
                                                               hint: if the immediate working set size is too large and
for compiler intrinsics to insert these special instructions
                                                               forces eviction of a cache line loaded with the NTA hint,
is generally used:
                                                               reloading from memory will occur.

                                                                                       1200
#include <xmmintrin.h>                                                                 1100
enum _mm_hint                                                                          1000
                                                                 Cycles/List Element

{                                                                                       900
   _MM_HINT_T0 = 3,                                                                     800
   _MM_HINT_T1 = 2,                                                                     700
   _MM_HINT_T2 = 1,                                                                     600
   _MM_HINT_NTA = 0                                                                     500
};                                                                                      400
void _mm_prefetch(void *p,                                                              300
                   enum _mm_hint h);                                                    200
                                                                                        100
                                                                                          0
                                                                                          210   213 216 219 222 225             228
Programs can use the _mm_prefetch intrinsic on any
                                                                                                Working Set Size (Bytes)
pointer in the program. Most processors (certainly all
                                                                                                  One Thread   With Prefetch
x86 and x86-64 processors) ignore errors resulting from
invalid pointers which makes the life of the programmer
significantly easier. If the passed pointer references valid
Figure 6.7: Average with Prefetch, NPAD=31
memory, the prefetch unit will be instructed to load the
data into cache and, if necessary, evict other data. Unnec-
essary prefetches should definitely be avoided since this      Figure 6.7 shows the results of a test using the now fa-
might reduce the effectiveness of the caches and it con-       miliar pointer chasing framework. The list is randomly
sumes memory bandwidth (possibly for two cache lines           laid out in memory. The difference to previous test is
in case the evicted cache line is dirty).                      that the program actually spends some time at each list
                                                               node (about 160 cycles). As we learned from the data in
The different hints to be used with the _mm_prefetch           Figure 3.15, the program’s performance suffers badly as
intrinsic are implementation defined. That means each          soon as the working set size is larger than the last-level
processor version can implement them (slightly) differ-        cache.

We can now try to improve the situation by issuing pre-        The compiler will figure out whether prefetching makes
fetch requests ahead of the computation. I.e., in each         sense and, if so, how far ahead it should look. For small
round of the loop we prefetch a new element. The dis-          arrays this can be a disadvantage and, if the size of the
tance between the prefetched node in the list and the node     array is not known at compile time, the results might be
which is currently worked on must be carefully chosen.         worse. The gcc manual warns that the benefits highly
Given that each node is processed in 160 cycles and that       depend on the form of the code and that in some situation
we have to prefetch two cache lines (NPAD=31), a dis-          the code might actually run slower. Programmers have to
tance of five list elements is enough.                         use this option carefully.
The results in Figure 6.7 show that the prefetch does in-
deed help. As long as the working set size does not ex-        6.3.3   Special Kind of Prefetch: Speculation
ceed the size of the last level cache (the machine has
512kB = 219 B of L2) the numbers are identical. The            The OOO execution capability of a modern processor al-
prefetch instructions do not add a measurable extra bur-       lows moving instructions around if they do not conflict
den. As soon as the L2 size is exceeded the prefetching        with each other. For instance (using this time IA-64 for
saves between 50 to 60 cycles, up to 8%. The use of            the example):
prefetch cannot hide all the penalties but it does help a
bit.
                                                                 st8             [r4] = 12
AMD implements, in their family 10h of the Opteron               add             r5 = r6, r7;;
line, another instruction: prefetchw. This instruction           st8             [r18] = r5
has so far no equivalent on the Intel side and is not avail-
able through intrinsics. The prefetchw instruction tells
the CPU to prefetch the cache line into L1 just like the
other prefetch instructions. The difference is that the        This code sequence stores 12 at the address specified by
cache line is immediately put into ’M’ state. This will be     register r4, adds the content of registers r6 and r7 and
a disadvantage if no write to the cache line follows later.    stores it in register r5. Finally it stores the sum at the
If there are one or more writes, they will be accelerated      address specified by register r18. The point here is that
since the writes do not have to change the cache state–        the add instruction can be executed before–or at the same
that happened when the cache line was prefetched. This         time as–the first st8 instruction since there is no data
is especially important for contended cache lines where a      dependency. But what happens if one of the addends has
simple read of a cache line in another processor’s cache       to be loaded?
would first change the state to ’S’ in both caches.
Prefetching can have bigger advantages than the mea-             st8             [r4] = 12
ger 8% we achieved here. But it is notoriously hard              ld8             r6 = [r8];;
to do right, especially if the same binary is supposed           add             r5 = r6, r7;;
to perform well on a variety of machines. The perfor-            st8             [r18] = r5
mance counters provided by the CPU can help the pro-
grammer to analyze prefetches. Events which can be
counted and sampled include hardware prefetches, soft-
                                                               The extra ld8 instruction loads the value from the ad-
ware prefetches, useful/used software prefetches, cache
                                                               dress specified by the register r8. There is an obvious
misses at the various levels, and more. In section 7.1 we
                                                               data dependency between this load instruction and the
will introduce a number of these events. All these coun-
                                                               following add instruction (this is the reason for the ;;
ters are machine specific.
                                                               after the instruction, thanks for asking). What is criti-
When analyzing programs one should first look at the           cal here is that the new ld8 instruction–unlike the add
cache misses. When a large source of cache misses is           instruction–cannot be moved in front of the first st8.
located one should try to add a prefetch instruction for       The processor cannot determine quickly enough during
the problematic memory accesses. This should be done           the instruction decoding whether the store and load con-
in one place at a time. The result of each modifica-           flict, i.e., whether r4 and r8 might have same value. If
tion should be checked by observing the performance            they do have the same value, the st8 instruction would
counters measuring useful prefetch instructions. If those      determine the value loaded into r6. What is worse, the
counters do not increase the prefetch might be wrong, it       ld8 might also bring with it a large latency in case the
is not given enough time to load from memory, or the pre-      load misses the caches. The IA-64 architecture supports
fetch evicts memory from the cache which is still needed.      speculative loads for this case:

gcc today is able to emit prefetch instructions automati-
cally in one situation. If a loop is iterating over an array
                                                                 ld8.a      r6 = [r8];;
the following option can be used:
                                                                 [... other instructions ...]
     -fprefetch-loop-arrays                                      st8        [r4] = 12

  ld8.c.clr        r6 = [r8];;                                   viously be scheduled so that the prefetch thread is pop-
  add              r5 = r6, r7;;                                 ulating a cache accessed by both threads. There are two
  st8              [r18] = r5                                    special solutions worth mentioning:

                                                                                         • Use hyper-threads (see page 29) on the same core.
The new ld8.a and ld8.c.clr instructions belong to-                                        In this case the prefetch can go into L2 (or even
gether and replace the ld8 instruction in the previous                                     L1d).
code sequence. The ld8.a instruction is the speculative
load. The value cannot be used directly but the processor                                • Use “dumber” threads than SMT threads which
can start the work. At the time when the ld8.c.clr in-                                     can do nothing but prefetch and other simple oper-
struction is reached the content might have been loaded                                    ations. This is an option processor manufacturers
already (given there is a sufficient number of instruc-                                    might explore.
tions in the gap). The arguments for this instruction must
match that for the ld8.a instruction. If the preceding
                                                                 The use of hyper-threads is particularly intriguing. As
st8 instruction does not overwrite the value (i.e., r4 and
                                                                 we have seen on page 29, the sharing of caches is a prob-
r8 are the same), nothing has to be done. The speculative
                                                                 lem if the hyper-threads execute independent code. If,
load does its job and the latency of the load is hidden. If
                                                                 instead, one thread is used as a prefetch helper thread
the store and load do conflict the ld8.c.clr reloads the
                                                                 this is not a problem. To the contrary, it is the desired
value from memory and we end up with the semantics of
                                                                 effect since the lowest level cache is preloaded. Further-
a normal ld8 instruction.
                                                                 more, since the prefetch thread is mostly idle or wait-
Speculative loads are not (yet?) widely used. But as the         ing for memory, the normal operation of the other hyper-
example shows it is a very simple yet effective way to           thread is not disturbed much if it does not have to access
hide latencies. Prefetching is basically equivalent and,         main memory itself. The latter is exactly what the pre-
for processors with few registers, speculative loads prob-       fetch helper thread prevents.
ably do not make much sense. Speculative loads have
                                                                 The only tricky part is to ensure that the helper thread is
the (sometimes big) advantage of loading the value di-
                                                                 not running too far ahead. It must not completely pollute
rectly into the register and not into the cache line where
                                                                 the cache so that the oldest prefetched values are evicted
it might be evicted again (for instance, when the thread
                                                                 again. On Linux, synchronization is easily done using
is descheduled). If speculation is available it should be
                                                                 the futex system call [7] or, at a little bit higher cost,
used.
                                                                 using the POSIX thread synchronization primitives.

6.3.4   Helper Threads                                                                   1200
                                                                                         1100
When one tries to use software prefetching one often runs                                1000
                                                                   Cycles/List Element

into problems with the complexity of the code. If the                                     900
code has to iterate over a data structure (a list in our case)                            800
                                                                                          700
one has to implement two independent iterations in the
                                                                                          600
same loop: the normal iteration doing the work and the                                    500
second iteration, which looks ahead, to use prefetching.                                  400
This easily gets complex enough that mistakes are likely.                                 300
                                                                                          200
Furthermore, it is necessary to determine how far to look                                 100
ahead. Too little and the memory will not be loaded in                                      0
time. Too far and the just loaded data might have been
                                                                                            210      213 216 219 222 225                        228
evicted again. Another problem is that prefetch instruc-
                                                                                                     Working Set Size (Bytes)
tions, although they do not block and wait for the mem-
                                                                                              One Thread   With Prefetch   With Helper Thread
ory to be loaded, take time. The instruction has to be
decoded, which might be noticeable if the decoder is too
busy, for instance, due to well written/generated code.                   Figure 6.8: Average with Helper Thread, NPAD=31
Finally, the code size of the loop is increased. This de-
creases the L1i efficiency. If one tries to avoid parts of
this cost by issuing multiple prefetch requests in a row         The benefits of the approach can be seen in Figure 6.8.
(in case the second load does not depend on the result           This is the same test as in Figure 6.7 only with the ad-
of the first) one runs into problems with the number of          ditional result added. The new test creates an additional
outstanding prefetch requests.                                   helper thread which runs about 100 list entries ahead and
                                                                 reads (not only prefetches) all the cache lines of each list
An alternative approach is to perform the normal oper-           element. In this case we have two cache lines per list el-
ation and the prefetch completely separately. This can           ement (NPAD=31 on a 32-bit machine with 64 byte cache
happen using two normal threads. The threads must ob-            line size).

                                 CPU                                             CPU

                 RAM         Northbridge                        RAM          Northbridge

                             Southbridge           Ethernet                  Southbridge        Ethernet

                              (a) DMA Initiated                           (b) DMA and DCA Executed

Figure 6.9: Direct Cache Access

The two threads are scheduled on two hyper-threads of           of the helper thread reduces the performance by between
the same core. The test machine has only one core but           10% and 60% (mostly at the lower end, ignore the small-
the results should be about the same if there is more than      est working set sizes again, the noise is too high). This
one core. The affinity functions, which we will introduce       should be expected since, if all the data is already in the
in section 6.4.3, are used to tie the threads down to the       L2 cache, the prefetch helper thread only uses system re-
appropriate hyper-thread.                                       sources without contributing to the execution.
To determine which two (or more) processors the OS              Once the L2 size is not sufficient is exhausted the pic-
knows are hyper-threads, the NUMA_cpu_level_mask                ture changes, though. The prefetch helper thread helps
interface from libNUMA can be used (see Appendix D).            to reduce the runtime by about 25%. We still see a ris-
                                                                ing curve simply because the prefetches cannot be pro-
                                                                cessed fast enough. The arithmetic operations performed
#include <libNUMA.h>                                            by the main thread and the memory load operations of
ssize_t NUMA_cpu_level_mask(size_t destsize,                    the helper thread do complement each other, though. The
                            cpu_set_t *dest,
                            size_t srcsize,
                                                                resource collisions are minimal which causes this syner-
                            const cpu_set_t*src,                gistic effect.
                            unsigned int level);
                                                                The results of this test should be transferable to many
                                                                other situations. Hyper-threads, often not useful due to
                                                                cache pollution, shine in these situations and should be
This interface can be used to determine the hierarchy of
                                                                taken advantage of. The NUMA library introduced in
CPUs as they are connected through caches and mem-
                                                                Appendix D makes finding thread siblings very easy (see
ory. Of interest here is level 1 which corresponds to
                                                                the example in that appendix). If the library is not avail-
hyper-threads. To schedule two threads on two hyper-
                                                                able the sys file system allows a program to find the
threads the libNUMA functions can be used (error han-
                                                                thread siblings (see the thread_siblings column in
dling dropped for brevity):
                                                                Table 5.3). Once this information is available the pro-
                                                                gram just has to define the affinity of the threads and
  cpu_set_t self;
                                                                then run the loop in two modes: normal operation and
  NUMA_cpu_self_current_mask(sizeof(self),                      prefetching. The amount of memory prefetched should
                              &self);                           depend on the size of the shared cache. In this example
  cpu_set_t hts;                                                the L2 size is relevant and the program can query the size
  NUMA_cpu_level_mask(sizeof(hts), &hts,
                      sizeof(self), &self, 1);
                                                                using
  CPU_XOR(&hts, &hts, &self);
                                                                        sysconf(_SC_LEVEL2_CACHE_SIZE)

                                                                Whether or not the progress of the helper thread must be
After this code is executed we have two CPU bit sets.           restricted depends on the program. In general it is best to
self can be used to set the affinity of the current thread      make sure there is some synchronization since schedul-
and the mask in hts can be used to set the affinity of          ing details could otherwise cause significant performance
the helper thread. This should ideally happen before the        degradations.
thread is created. In section 6.4.3 we will introduce the
interface to set the affinity. If there is no hyper-thread      6.3.5    Direct Cache Access
available the NUMA_cpu_level_mask function will re-
turn 1. This can be used as a sign to avoid this optimiza-
tion.                                                           One sources of cache misses in a modern OS is the han-
                                                                dling of incoming data traffic. Modern hardware, like
The result of this benchmark might be surprising (or per-       Network Interface Cards (NICs) and disk controllers, has
haps not). If the working set fits into L2, the overhead        the ability to write the received or read data directly into

memory without involving the CPU. This is crucial for             6.4     Multi-Thread Optimizations
the performance of the devices we have today, but it also
causes problems. Assume an incoming packet from a
network: the OS has to decide how to handle it by look-           When it comes to multi-threading, there are three differ-
ing at the header of the packet. The NIC places the packet        ent aspects of cache use which are important:
into memory and then notifies the processor about the ar-
rival. The processor has no chance to prefetch the data                  • Concurrency
since it does not know when the data will arrive, and
                                                                         • Atomicity
maybe not even where exactly it will be stored. The re-
sult is a cache miss when reading the header.                            • Bandwidth
Intel has added technology in their chipsets and CPUs
to alleviate this problem [14]. The idea is to populate           These aspects also apply to multi-process situations but,
the cache of the CPU which will be notified about the             because multiple processes are (mostly) independent, it
incoming packet with the packet’s data. The payload of            is not so easy to optimize for them. The possible multi-
the packet is not critical here, this data will, in general, be   process optimizations are a subset of those available for
handled by higher-level functions, either in the kernel or        the multi-thread scenario. So we will deal exclusively
at user level. The packet header is used to make decisions        with the latter here.
about the way the packet has to be handled and so this
                                                                  In this context concurrency refers to the memory effects a
data is needed immediately.
                                                                  process experiences when running more than one thread
The network I/O hardware already has DMA to write the             at a time. A property of threads is that they all share
packet. That means it communicates directly with the              the same address space and, therefore, can all access the
memory controller which potentially is integrated in the          same memory. In the ideal case, the memory regions
Northbridge. Another side of the memory controller is             used by the threads most of the time are distinct, in which
the interface to the processors through the FSB (assum-           case those threads are coupled only lightly (common in-
ing the memory controller is not integrated into the CPU          put and/or output, for instance). If more than one thread
itself).                                                          uses the same data, coordination is needed; this is when
                                                                  atomicity comes into play. Finally, depending on the
The idea behind Direct Cache Access (DCA) is to ex-               machine architecture, the available memory and inter-
tend the protocol between the NIC and the memory con-             processor bus bandwidth available to the processors is
troller. In Figure 6.9 the first figure shows the beginning       limited. We will handle these three aspects separately
of the DMA transfer in a regular machine with North-              in the following sections–although they are, of course,
and Southbridge. The NIC is connected to (or is part of)          closely linked.
the Southbridge. It initiates the DMA access but pro-
vides the new information about the packet header which
                                                                  6.4.1     Concurrency Optimizations
should be pushed into the processor’s cache.
The traditional behavior would be, in step two, to simply         Initially, in this section, we will discuss two separate is-
complete the DMA transfer with the connection to the              sues which actually require contradictory optimizations.
memory. For the DMA transfers with the DCA flag set               A multi-threaded application uses common data in some
the Northbridge additionally sends the data on the FSB            of its threads. Normal cache optimization calls for keep-
with a special, new DCA flag. The processor always                ing data together so that the footprint of the application
snoops the FSB and, if it recognizes the DCA flag, it tries       is small, thus maximizing the amount of memory which
to load the data directed to the processor into the lowest        fits into the caches at any one time.
cache. The DCA flag is, in fact, a hint; the processor can
freely ignore it. After the DMA transfer is finished the          There is a problem with this approach, though: if mul-
processor is signaled.                                            tiple threads write to a memory location, the cache line
                                                                  must be in ‘E’ (exclusive) state in the L1d of each respec-
The OS, when processing the packet, first has to deter-           tive core. This means that a lot of RFO messages are sent,
mine what kind of packet it is. If the DCA hint is not            in the worst case one for each write access. So a normal
ignored, the loads the OS has to perform to identify the          write will be suddenly very expensive. If the same mem-
packet most likely hit the cache. Multiply this saving of         ory location is used, synchronization is needed (maybe
hundreds of cycles per packet with tens of thousands of           through the use of atomic operations, which is handled
packets which can be processed per second, and the sav-           in the next section). The problem is also visible, though,
ings add up to very significant numbers, especially when          when all the threads are using different memory locations
it comes to latency.                                              and are supposedly independent.
Without the integration of I/O hardware (a NIC in this            Figure 6.10 shows the results of this “false sharing”. The
case), chipset, and CPUs such an optimization is not pos-         test program (shown in section A.3) creates a number of
sible. It is therefore necessary to make sure to select the       threads which do nothing but increment a memory loca-
platform wisely if this technology is needed.                     tion (500 million times). The measured time is from the

                  18
                                                                                             2
                  16

                                                                                Time (sec)
                  14
     Time (sec)
                  12                                                                         1
                  10
                   8                                                                         0
                                                                                                   1       2         3                4
                   6                                                                                    Number of Threads
                   4
                   2
                   0                                                                             Figure 6.11: Overhead, Quad Core
                       1       2         3                  4
                           Number of Threads
                                                                            more than one of these processors were used we would,
                                                                            of course, see results similar to those in Figure 6.10. De-
Figure 6.10: Concurrent Cache Line Access Overhead                         spite the increasing use of multi-core processors, many
                                                                            machines will continue to use multiple processors and,
                                                                            therefore, it is important to handle this scenario correctly,
program start until the program finishes after waiting for                  which might mean testing the code on real SMP ma-
the last thread. The threads are pinned to individual pro-                  chines.
cessors. The machine has four P4 processors. The blue
                                                                            There is a very simple “fix” for the problem: put every
values represent runs where the memory allocations as-
                                                                            variable on its own cache line. This is where the conflict
signed to each thread are on separate cache lines. The
                                                                            with the previously mentioned optimization comes into
red part is the penalty occurred when the locations for
                                                                            play, specifically, the footprint of the application would
the threads are moved to just one cache line.
                                                                            increase a lot. This is not acceptable; it is therefore nec-
The blue measurements (time needed when using indi-                         essary to come up with a more intelligent solution.
vidual cache lines) match what one would expect. The
                                                                            What is needed is to identify which variables are used by
program scales without penalty to many threads. Each
                                                                            only one thread at a time, those used by only one thread
processor keeps its cache line in its own L1d and there
                                                                            ever, and maybe those which are contested at times. Dif-
are no bandwidth issues since not much code or data has
                                                                            ferent solutions for each of these scenarios are possible
to be read (in fact, it is all cached). The measured slight
                                                                            and useful. The most basic criterion for the differentia-
increase is really system noise and probably some pre-
                                                                            tion of variables is: are they ever written to and how often
fetching effects (the threads use sequential cache lines).
                                                                            does this happen.
The measured overhead, computed by dividing the time
                                                                            Variables which are never written to and those which are
needed when using one single cache line versus a sep-
                                                                            only initialized once are basically constants. Since RFO
arate cache line for each thread, is 390%, 734%, and
                                                                            messages are only needed for write operations, constants
1,147% respectively. These large numbers might be sur-
                                                                            can be shared in the cache (‘S’ state). So, these vari-
prising at first sight but, when thinking about the cache
                                                                            ables do not have to be treated specially; grouping them
interaction needed, it should be obvious. The cache line
                                                                            together is fine. If the programmer marks the variables
is pulled from one processor’s cache just after it has fin-
                                                                            correctly with const, the tool chain will move the vari-
ished writing to the cache line. All processors, except the
                                                                            ables away from the normal variables into the .rodata
one which has the cache line at any given moment, are
                                                                            (read-only data) or .data.rel.ro (read-only after relo-
delayed and cannot do anything. Each additional proces-
                                                                            cation) section37 No other special action is required. If,
sor will just cause more delays.
                                                                            for some reason, variables cannot be marked correctly
It is clear from these measurements that this scenario                      with const, the programmer can influence their place-
must be avoided in programs. Given the huge penalty,                        ment by assigning them to a special section.
this problem is, in many situations, obvious (profiling
                                                                            When the linker constructs the final binary, it first ap-
will show the code location, at least) but there is a pitfall
                                                                            pends the sections with the same name from all input
with modern hardware. Figure 6.11 shows the equivalent
                                                                            files; those sections are then arranged in an order deter-
measurements when running the code on a single pro-
                                                                            mined by the linker script. This means that, by mov-
cessor, quad core machine (Intel Core 2 QX 6700). Even
                                                                            ing all variables which are basically constant but are not
with this processor’s two separate L2s the test case does
                                                                            marked as such into a special section, the programmer
not show any scalability issues. There is a slight over-
                                                                            can group all of those variables together. There will not
head when using the same cache line more than once
                                                                            be a variable which is often written to between them. By
but it does not increase with the number of cores.36 If
                                                                            aligning the first variable in that section appropriately,
    36 I cannot explain the lower number when all four cores are used but     37 Sections, identified by their names are the atomic units containing

it is reproducible.                                                         code and data in an ELF file.

it is possible to guarantee that no false sharing happens.            Another drawback of using thread-local storage (TLS)
Assume this little example:                                           is that, if the use of the variable shifts over to another
                                                                      thread, the current value of the variable in the old thread
                                                                      is not available to new thread. Each thread’s copy of the
  int foo = 1;
                                                                      variable is distinct. Often this is not a problem at all and,
  int bar __attribute__((section(".data.ro"))) = 2;                   if it is, the shift over to the new thread needs coordina-
  int baz = 3;                                                        tion, at which time the current value can be copied.
  int xyzzy __attribute__((section(".data.ro"))) = 4;

                                                                      A bigger problem is possible waste of resources. If only
                                                                      one thread ever uses the variable at any one time, all
If compiled, this input file defines four variables. The in-          threads have to pay a price in terms of memory. If a
teresting part is that the variables foo and baz, and bar             thread does not use any TLS variables, the lazy alloca-
and xyzzy are grouped together respectively. Without                  tion of the TLS memory area prevents this from being a
the attribute definitions the compiler would allocate all             problem (except for TLS in the application itself). If a
four variables in the sequence in which they are defined              thread uses just one TLS variable in a DSO, the memory
in the source code the a section named .data.38 With the              for all the other TLS variables in this object will be allo-
code as-is the variables bar and xyzzy are placed in a                cated, too. This could potentially add up if TLS variables
section named .data.ro. The section name .data.ro                     are used on a large scale.
is more or less arbitrary. A prefix of .data. guarantees              In general the best advice which can be given is
that the GNU linker will place the section together with
the other data sections.
                                                                         1. Separate at least read-only (after initialization) and
The same technique can be applied to separate out vari-                     read-write variables. Maybe extend this separation
ables which are mostly read but occasionally also written                   to read-mostly variables as a third category.
to. Simply choose a different section name. This sepa-
ration seems to make sense in some cases like the Linux                  2. Group read-write variables which are used together
kernel.                                                                     into a structure. Using a structure is the only way
If a variable is only ever used by one thread, there is an-                 to ensure the memory locations for all of those
other way to specify the variable. In this case it is possi-                variables are close together in a way which is trans-
ble and useful to use thread-local variables (see [8]). The                 lated consistently by all gcc versions..
C and C++ language in gcc allow variables to be defined
                                                                         3. Move read-write variables which are often written
as per-thread using the __thread keyword.
                                                                            to by different threads onto their own cache line.
                                                                            This might mean adding padding at the end to fill
                                                                            a remainder of the cache line. If combined with
  int foo = 1;                                                              step 2, this is often not really wasteful. Extending
  __thread int bar = 2;
  int baz = 3;                                                              the example above, we might end up with code as
  __thread int xyzzy = 4;                                                   follows (assuming bar and xyzzy are meant to be
                                                                            used together):

The variables bar and xyzzy are not allocated in the
                                                                               int foo = 1;
normal data segment; instead each thread has its own                           int baz = 3;
separate area where such variables are stored. The vari-                       struct {
                                                                                 struct al1 {
ables can have static initializers. All thread-local vari-                          int bar;
ables are addressable by all other threads but, unless a                            int xyzzy;
                                                                                 };
thread passes a pointer to a thread-local variable to those                      char pad[CLSIZE - sizeof(struct al1)];
other threads, there is no way the other threads can find                      } rwstruct __attribute__((aligned(CLSIZE))) =
                                                                                 { { .bar = 2, .xyzzy = 4 } };
that variable. Due to the variable being thread-local, false
sharing is not a problem–unless the program artificially
creates a problem. This solution is easy to set up (the
compiler and linker do all the work), but it has its cost.                   Some code changes are needed (references to bar
When a thread is created, it has to spend some time on                       have to be replaced with rwstruct.bar, likewise
setting up the thread-local variables, which requires time                   for xyzzy) but that is all. The compiler and linker
and memory. In addition, addressing thread-local vari-                       do all the rest.39
ables is usually more expensive than using global or auto-
matic variables (see [8] for explanations of how the costs               4. If a variable is used by multiple threads, but every
are minimized automatically, if possible).                                  use is independent, move the variable into TLS.
  38 This is not guaranteed by the ISO C standard but it is how gcc     39 So far this code has to be compiled with -fms-extensions on

works.                                                                the command line.

for (i = 0; i < N; ++i)         for (i = 0; i < N; ++i)         for (i = 0; i < N; ++i) {
  __sync_add_and_fetch(&var,1);   __sync_fetch_and_add(&var,1);   long v, n;
                                                                  do {
                                                                    v = var;
                                                                    n = v + 1;
                                                                  } while (!__sync_bool_compare_and_swap(&var,
                                                                                                         v,n));
                                                                }

        1. Add and Read Result                    2. Add and Return Old Value                 3. Atomic Replace with New Value

Figure 6.12: Atomic Increment in a Loop

6.4.2     Atomicity Optimizations                                          Compare-and-Swap (CAS) This is a ternary operation
                                                                               which writes a value provided as a parameter into
If multiple threads modify the same memory location                            an address (the second parameter) only if the cur-
concurrently, processors do not guarantee any specific                         rent value is the same as the third parameter value;
result. This is a deliberate decision made to avoid costs
which are unnecessary in 99.999% of all cases. For in-                     Atomic Arithmetic These operations are only available
stance, if a memory location is in the ‘S’ state and two                       on x86 and x86-64, which can perform arithmetic
threads concurrently have to increment its value, the exe-                     and logic operations on memory locations. These
cution pipeline does not have to wait for the cache line to                    processors have support for non-atomic versions of
be available in the ‘E’ state before reading the old value                     these operations but RISC architectures do not. So
from the cache to perform the addition. Instead it reads                       it is no wonder that their availability is limited.
the value currently in the cache and, once the cache line
is available in state ‘E’, the new value is written back.                  An architecture supports either the LL/SC or the CAS in-
The result is not as expected if the two cache reads in the                struction, not both. Both approaches are basically equiv-
two threads happen simultaneously; one addition will be                    alent; they allow the implementation of atomic arithmetic
lost.                                                                      operations equally well, but CAS seems to be the pre-
For situations where concurrent operations can happen,                     ferred method these days. All other operations can be
processors provide atomic operations. These atomic op-                     indirectly implemented using it. For instance, an atomic
erations would, for instance, not read the old value un-                   addition:
til it is clear that the addition could be performed in a
way that the addition to the memory location appears as
atomic. In addition to waiting for other cores and proces-                      int curval;
sors, some processors even signal atomic operations for                         int newval;
specific addresses to other devices on the motherboard.                         do {
All this makes atomic operations slower.                                          curval = var;
                                                                                  newval = curval + addend;
Processor vendors decided to provide different sets of                          } while (CAS(&var, curval, newval));
atomic operations. Early RISC processors, in line with
the ‘R’ for reduced, provided very few atomic operations,
sometimes only an atomic bit set and test.40 At the other
end of the spectrum, we have x86 and x86-64 which pro-                     The result of the CAS call indicates whether the operation
vide a large number of atomic operations. The generally                    succeeded or not. If it returns failure (non-zero value),
available atomic operations can be categorized in four                     the loop is run again, the addition is performed, and the
classes:                                                                   CAS call is tried again. This repeats until it is success-
                                                                           ful. Noteworthy about the code is that the address of the
                                                                           memory location has to be computed in two separate in-
Bit Test These operations set or clear a bit atomically                    structions.42 For LL/SC the code looks about the same:
      and return a status indicating whether the bit was
      set before or not.

Load Lock/Store Conditional (LL/SC)41 The LL/SC                                 int curval;
                                                                                int newval;
     operations work as a pair where the special load
                                                                                do {
     instruction is used to start an transaction and the                          curval = LL(var);
     final store will only succeed if the location has not                        newval = curval + addend;
     been modified in the meantime. The store oper-                             } while (SC(var, newval));
     ation indicates success or failure, so the program
     can repeat its efforts if necessary.
                                                                              42 The CAS opcode on x86 and x86-64 can avoid the load of the value
  40 HP Parisc still does not provide more. . .
                                                                           in the second and later iterations but, on this platform, we can write the
  41 Some people use “linked” instead of “lock”, it is all the same.       atomic addition in a simpler way, with a single addition opcode.

Here we have to use a special load instruction (LL) and        Each loop iteration in the example therefore results in, at
we do not have to pass the current value of the memory         most, one RFO cache request and nothing else.
location to SC since the processor knows if the memory
location has been modified in the meantime.                    What all this means is that it is crucial to define the ma-
                                                               chine abstraction at a level at which atomic arithmetic
The big differentiators are x86 and x86-64, where we           and logic operations can be utilized. CAS should not be
have the atomic operations and, here, it is important to       universally used as the unification mechanism.
select the proper atomic operation to achieve the best re-
sult. Figure 6.12 shows three different ways to imple-         For most processors, the atomic operations are, by them-
ment an atomic increment operation. All three produce          selves, always atomic. One can avoid them only by pro-
different code on x86 and x86-64 while the code might          viding completely separate code paths for the case when
be identical on other architectures. There are huge per-       atomicity is not needed. This means more code, a con-
formance differences. The following table shows the ex-        ditional, and further jumps to direct execution appropri-
ecution time for 1 million increments by four concurrent       ately.
threads. The code uses the built-in primitives of gcc (_-      For x86 and x86-64 the situation is different: the same
_sync_*).                                                      instructions can be used in both atomic and non-atomic
                                                               ways. To make them atomic, a special prefix for the in-
      1. Exchange Add       2. Add Fetch     3. CAS            struction is used: the lock prefix. This opens the door
                                                               for atomic operations to avoid the high costs if the atom-
                   0.23s            0.21s      0.73s
                                                               icity requirement in a given situation is not needed. Code
                                                               in libraries, for example, which always has to be thread-
The first two numbers are similar; we see that returning       safe if needed, can benefit from this. No information is
the old value is a little bit faster. The important piece of   needed when writing the code, the decision can be made
information is the highlighted field, the cost when using      at runtime. The trick is to jump over the lock prefix.
CAS. It is, unsurprisingly, a lot more expensive. There        This trick applies to all the instructions which the x86
are several reasons for this: 1. there are two memory op-      and x86-64 processor allow to prefix with lock.
erations, 2. the CAS operation by itself is more compli-
cated and requires even conditional operation, and 3. the
whole operation has to be done in a loop in case two con-
current accesses cause a CAS call to fail.                             cmpl $0, multiple_threads
                                                                       je   1f
Now a reader might ask a question: why would some-                     lock
body use the complicated and longer code which uti-               1:   add $1, some_var
lizes CAS? The answer to this is: the complexity is usu-
ally hidden. As mentioned before, CAS is currently the
unifying atomic operation across all interesting architec-
tures. So some people think it is sufficient to define all     If this assembler code appears cryptic, do not worry, it
atomic operations in terms of CAS. This makes programs         is simple. The first instruction checks whether a vari-
simpler. But as the numbers show, the results can be ev-       able is zero or not. Nonzero in this case indicates that
erything but optimal. The memory handling overhead of          more than one thread is running. If the value is zero,
the CAS solution is huge. The following illustrates the        the second instruction jumps to label 1. Otherwise, the
execution of just two threads, each on its own core.           next instruction is executed. This is the tricky part. If
                                                               the je instruction does not jump, the add instruction is
                                                               executed with the lock prefix. Otherwise it is executed
      Thread #1       Thread #2      var Cache State
                                                               without the lock prefix.
     v = var                         ‘E’ on Proc 1
     n = v + 1       v = var         ‘S’ on Proc 1+2           Adding a potentially expensive operation like a condi-
     CAS(var)        n = v + 1       ‘E’ on Proc 1             tional jump (expensive in case the branch prediction is
                     CAS(var)        ‘E’ on Proc 2             wrong) seems to be counter productive. Indeed it can be:
                                                               if multiple threads are running most of the time, the per-
We see that, within this short period of execution, the        formance is further decreased, especially if the branch
cache line status changes at least three times; two of the     prediction is not correct. But if there are many situa-
changes are RFOs. Additionally, the second CAS will            tions where only one thread is in use, the code is sig-
fail, so that thread has to repeat the whole operation. Dur-   nificantly faster. The alternative of using an if-then-else
ing that operation the same can happen again.                  construct introduces an additional unconditional jump in
                                                               both cases which can be slower. Given that an atomic
In contrast, when the atomic arithmetic operations are         operation costs on the order of 200 cycles, the cross-
used, the processor can keep the load and store opera-         over point for using the trick (or the if-then-else block)
tions needed to perform the addition (or whatever) to-         is pretty low. This is definitely a technique to be kept in
gether. It can ensure that concurrently-issued cache line      mind. Unfortunately this means gcc’s __sync_* primi-
requests are blocked until the atomic operation is done.       tives cannot be used.

6.4.3   Bandwidth Considerations                              possible. The scheduler does not really know anything
                                                              about the workload, though. It can gather information
                                                              from cache misses etc but this is not much help in many
When many threads are used, and they do not cause cache       situations.
contention by using the same cache lines on different
cores, there still are potential problems. Each proces-
sor has a maximum bandwidth to the memory which is                    Core 1    Core 2            Core 3   Core 4
shared by all cores and hyper-threads on that processor.                   Cache                       Cache
Depending on the machine architecture (e.g., the one in
Figure 2.1), multiple processors might share the same
bus to memory or the Northbridge.                                                        Memory

The processor cores themselves run at frequencies where,
at full speed, even in perfect conditions, the connection                Figure 6.13: Inefficient Scheduling
to the memory cannot fulfill all load and store requests
without waiting. Now, further divide the available band-
width by the number of cores, hyper-threads, and pro-         One situation which can cause big memory bus usage is
cessors sharing a connection to the Northbridge and sud-      when two threads are scheduled on different processors
denly parallelism becomes a big problem. Efficient pro-       (or cores in different cache domains) and they use the
grams may be limited in their performance by the avail-       same data set. Figure 6.13 shows such a situation. Core 1
able memory bandwidth.                                        and 3 access the same data (indicated by the same color
                                                              for the access indicator and the memory area). Similarly
Figure 3.32 shows that increasing the FSB speed of a pro-     core 2 and 4 access the same data. But the threads are
cessor can help a lot. This is why, with growing numbers      scheduled on different processors. This means each data
of cores on a processor, we will also see an increase in      set has to be read twice from memory. This situation can
the FSB speed. Still, this will never be enough if the        be handled better.
program uses large working sets and it is sufficiently op-
timized. Programmers have to be prepared to recognize
                                                                      Core 1    Core 2            Core 3   Core 4
problems due to limited bandwidth.
                                                                           Cache                       Cache
The performance measurement counters of modern pro-
cessors allow the observation of FSB contention. On
Core 2 processors the NUS_BNR_DRV event counts the                                       Memory
number of cycles a core has to wait because the bus is
not ready. This indicates that the bus is highly used and
loads from or stores to main memory take even longer                      Figure 6.14: Efficient Scheduling
than usual. The Core 2 processors support more events
which can count specific bus actions like RFOs or the         In Figure 6.14 we see how it should ideally look like.
general FSB utilization. The latter might come in handy       Now the total cache size in use is reduced since now
when investigating the possibility of scalability of an ap-   core 1 and 2 and core 3 and 4 work on the same data.
plication during development. If the bus utilization rate     The data sets have to be read from memory only once.
is already close to 1.0 then the scalability opportunities
are minimal.                                                  This is a simple example but, by extension, it applies to
                                                              many situations. As mentioned before, the scheduler in
If a bandwidth problem is recognized, there are several       the kernel has no insight into the use of data, so the pro-
things which can be done. They are sometimes con-             grammer has to ensure that scheduling is done efficiently.
tradictory so some experimentation might be necessary.        There are not many kernel interfaces available to commu-
One solution is to buy faster computers, if there are some    nicate this requirement. In fact, there is only one: defin-
available. Getting more FSB speed, faster RAM mod-            ing thread affinity.
ules, and possibly memory local to the processor, can–
and probably will–help. It can cost a lot, though. If the     Thread affinity means assigning a thread to one or more
program in question is only needed on one (or a few ma-       cores. The scheduler will then choose among those cores
chines) the one-time expense for the hardware might cost      (only) when deciding where to run the thread. Even if
less than reworking the program. In general, though, it is    other cores are idle they will not be considered. This
better to work on the program.                                might sound like a disadvantage, but it is the price one
                                                              has to pay. If too many threads exclusively run on a set
After optimizing the program code itself to avoid cache       of cores the remaining cores might mostly be idle and
misses, the only option left to achieve better bandwidth      there is nothing one can do except change the affinity.
utilization is to place the threads better on the available   By default threads can run on any core.
cores. By default, the scheduler in the kernel will assign
a thread to a processor according to its own policy. Mov-     There are a number of interfaces to query and change the
ing a thread from one core to another is avoided when         affinity of a thread:

#define _GNU_SOURCE                                           #define _GNU_SOURCE
#include <sched.h>                                            #include <sched.h>
int sched_setaffinity(pid_t pid, size_t size,                 #define CPU_ALLOC_SIZE(count)
                      const cpu_set_t *cpuset);               #define CPU_ALLOC(count)
int sched_getaffinity(pid_t pid, size_t size,                 #define CPU_FREE(cpuset)
                      cpu_set_t *cpuset);

                                                              The return value of the CPU_ALLOC_SIZE macro is the
These two interfaces are meant to be used for single-
                                                              number of bytes which have to be allocated for a cpu_-
threaded code. The pid argument specifies which pro-
                                                              set_t structure which can handle count CPUs. To al-
cess’s affinity should be changed or determined. The
                                                              locate such a block the CPU_ALLOC macro can be used.
caller obviously needs appropriate privileges to do this.
                                                              The memory allocated this way must be freed with a call
The second and third parameter specify the bitmask for
                                                              to CPU_FREE. These macros will likely use malloc and
the cores. The first function requires the bitmask to be
                                                              free behind the scenes but this does not necessarily have
filled in so that it can set the affinity. The second fills
                                                              to remain this way.
in the bitmask with the scheduling information of the se-
lected thread. The interfaces are declared in <sched.h>.      Finally, a number of operations on CPU set objects are
                                                              defined:
The cpu_set_t type is also defined in that header, along
with a number of macros to manipulate and use objects
of this type.
                                                              #define _GNU_SOURCE
                                                              #include <sched.h>
                                                              #define CPU_EQUAL(cpuset1, cpuset2)
#define _GNU_SOURCE                                           #define CPU_AND(destset, cpuset1, cpuset2)
#include <sched.h>                                            #define CPU_OR(destset, cpuset1, cpuset2)
#define CPU_SETSIZE                                           #define CPU_XOR(destset, cpuset1, cpuset2)
#define CPU_SET(cpu, cpusetp)                                 #define CPU_EQUAL_S(setsize, cpuset1, cpuset2)
#define CPU_CLR(cpu, cpusetp)                                 #define CPU_AND_S(setsize, destset, cpuset1,
#define CPU_ZERO(cpusetp)                                                        cpuset2)
#define CPU_ISSET(cpu, cpusetp)                               #define CPU_OR_S(setsize, destset, cpuset1,
#define CPU_COUNT(cpusetp)                                                     cpuset2)
                                                              #define CPU_XOR_S(setsize, destset, cpuset1,
                                                                                 cpuset2)

CPU_SETSIZE specifies how many CPUs can be rep-
resented in the data structure. The other three macros
manipulate cpu_set_t objects. To initialize an object         These two sets of four macros can check two sets for
CPU_ZERO should be used; the other two macros should          equality and perform logical AND, OR, and XOR op-
be used to select or deselect individual cores. CPU_-         erations on sets. These operations come in handy when
ISSET tests whether a specific processor is part of the       using some of the libNUMA functions (see Appendix D).
set. CPU_COUNT returns the number of cores selected in
the set. The cpu_set_t type provide a reasonable de-          A process can determine on which processor it is cur-
fault value for the upper limit on the number of CPUs.        rently running using the sched_getcpu interface:
Over time it certainly will prove too small; at that point
the type will be adjusted. This means programs always
have to keep the size in mind. The above convenience          #define _GNU_SOURCE
                                                              #include <sched.h>
macros implicitly handle the size according to the defi-
                                                              int sched_getcpu(void);
nition of cpu_set_t. If more dynamic size handling is
needed an extended set of macros should be used:

                                                              The result is the index of the CPU in the CPU set. Due
#define _GNU_SOURCE                                           to the nature of scheduling this number cannot always be
#include <sched.h>                                            100% correct. The thread might have been moved to a
#define CPU_SET_S(cpu, setsize, cpusetp)                      different CPU between the time the result was returned
#define CPU_CLR_S(cpu, setsize, cpusetp)
#define CPU_ZERO_S(setsize, cpusetp)                          and when the thread returns to userlevel. Programs al-
#define CPU_ISSET_S(cpu, setsize, cpusetp)                    ways have to take this possibility of inaccuracy into ac-
#define CPU_COUNT_S(setsize, cpusetp)                         count. More important is, in any case, the set of CPUs the
                                                              thread is allowed to run on. This set can be retrieved us-
                                                              ing sched_getaffinity. The set is inherited by child
These interfaces take an additional parameter with the        threads and processes. Threads cannot rely on the set to
size. To be able to allocate dynamically sized CPU sets       be stable over the lifetime. The affinity mask can be set
three macros are provided:                                    from the outside (see the pid parameter in the prototypes

above); Linux also supports CPU hot-plugging which                reality an aspect of NUMA programming which is cov-
means CPUs can vanish from the system–and, therefore,             ered in the next section. One only has to extend the no-
also from the affinity CPU set.                                   tion of “memory” to the caches. This will become ever
                                                                  more important once the number of levels of cache in-
In multi-threaded programs, the individual threads of-            creases. For this reason, a solution to multi-core schedul-
ficially have no process ID as defined by POSIX and,              ing is available in the NUMA support library. See the
therefore, the two functions above cannot be used. In-            code samples in Appendix D for ways to determine the
stead <pthread.h> declares four different interfaces:             affinity masks without hardcoding system details or div-
                                                                  ing into the depth of the /sys filesystem.

#define _GNU_SOURCE                                               6.5     NUMA Programming
#include <pthread.h>
int pthread_setaffinity_np(pthread_t th,
                       size_t size,                               For NUMA programming everything said so far about
                       const cpu_set_t *cpuset);
int pthread_getaffinity_np(pthread_t th,                          cache optimizations applies as well. The differences only
                       size_t size,                               start below that level. NUMA introduces different costs
                       cpu_set_t *cpuset);                        when accessing different parts of the address space. With
int pthread_attr_setaffinity_np(
                                                                  uniform memory access we can optimize to minimize
                       pthread_attr_t *at,
                       size_t size,                               page faults (see section 7.5) but that is about it. All pages
                       const cpu_set_t *cpuset);                  are created equal.
int pthread_attr_getaffinity_np(
                       pthread_attr_t *at,                        NUMA changes this. Access costs can depend on the
                       size_t size,                               page which is accessed. Differing access costs also in-
                       cpu_set_t *cpuset);
                                                                  crease the importance of optimizing for memory page
                                                                  locality. NUMA is inevitable for most SMP machines
                                                                  since both Intel with CSI (for x86,x86-64, and IA-64)
The first two interfaces are basically equivalent to the two      and AMD (for Opteron) use it. With an increasing num-
we have already seen, except that they take a thread han-         ber of cores per processor we are likely to see a sharp
dle in the first parameter instead of a process ID. This          reduction of SMP systems being used (at least outside
allows addressing individual threads in a process. It also        data centers and offices of people with terribly high CPU
means that these interfaces cannot be used from another           usage requirements). Most home machines will be fine
process, they are strictly for intra-process use. The third       with just one processor and hence no NUMA issues. But
and fourth interfaces use a thread attribute. These at-           this a) does not mean programmers can ignore NUMA
tributes are used when creating a new thread. By setting          and b) it does not mean there are not related issues.
the attribute, a thread can be scheduled from the start on a
                                                                  If one thinks about generalizations to NUMA one quickly
specific set of CPUs. Selecting the target processors this
                                                                  realizes the concept extends to processor caches as well.
early–instead of after the thread already started–can be
                                                                  Two threads on cores using the same cache will collabo-
of advantage on many different levels, including (and es-
                                                                  rate faster than threads on cores not sharing a cache. This
pecially) memory allocation (see NUMA in section 6.5).
                                                                  is not a fabricated case:
Speaking of NUMA, the affinity interfaces play a big role
in NUMA programming, too. We will come back to that                     • early dual-core processors had no L2 sharing.
case shortly.
                                                                        • Intel’s Core 2 QX 6700 and QX 6800 quad core
So far, we have talked about the case where the working
                                                                          chips, for instance, have two separate L2 caches.
set of two threads overlaps such that having both threads
on the same core makes sense. The opposite can be true,                 • as speculated early, with more cores on a chip and
too. If two threads work on separate data sets, having                    the desire to unify caches, we will have more levels
them scheduled on the same core can be a problem. Both                    of caches.
threads fight for the same cache, thereby reducing each
others effective use of the cache. Second, both data sets
have to be loaded into the same cache; in effect this in-         Caches form their own hierarchy; placement of threads
creases the amount of data that has to be loaded and,             on cores becomes important for sharing (or not) of the
therefore, the available bandwidth is cut in half.                various caches. This is not very different from the prob-
                                                                  lems NUMA is facing and, therefore, the two concepts
The solution in this case is to set the affinity of the threads   can be unified. Even people only interested in non-SMP
so that they cannot be scheduled on the same core. This is        machines should therefore read this section.
the opposite from the previous situation, so it is important
to understand the situation one tries to optimize before          In section 5.3 we have seen that the Linux kernel pro-
making any changes.                                               vides a lot of information which is useful–and needed–in
                                                                  NUMA programming. Collecting this information is not
Optimizing for cache sharing to optimize bandwidth is in          that easy, though. The currently available NUMA library

on Linux is wholly inadequate for this purpose. A much
more suitable version is currently under construction by                            System Default Policy
the author.
The existing NUMA library, libnuma, part of the nu-                                       Task Policy
mactl package, provides no access to system architecture
information. It is only a wrapper around the available                        VMA Policy            ShMem Policy
system calls together with some convenience interfaces
for commonly used operations. The system calls avail-
able on Linux today are:

Figure 6.15: Memory Policy Hierarchy
mbind Select binding of specified memory pages.

set_mempolicy Set the default memory binding pol-                 shared memory segments. If no policy for the specific
        icy.                                                      address is present, the task’s policy is used. If this is also
                                                                  not present the system’s default policy is used.
get_mempolicy Get the default memory binding pol-
        icy.                                                      The system default is to allocate memory local to the
                                                                  thread requesting the memory. No task and VMA poli-
migrate_pages Migrate all pages of a process on a
                                                                  cies are provided by default. For a process with multiple
        given set of nodes to a different set of nodes.
                                                                  threads the local node is the “home” node, the one which
move_pages Move selected pages to given node or re-               first ran the process. The system calls mentioned above
        quest node information about pages.                       can be used to select different policies.

These interfaces are declared in the <numaif.h> header            6.5.2   Specifying Policies
which comes along with the libnuma library. Before we
go into more details we have to understand the concept            The set_mempolicy call can be used to set the task pol-
of memory policies.                                               icy for the current thread (task in kernel-speak). Only the
                                                                  current thread is affected, not the entire process.
6.5.1    Memory Policy

The idea behind defining a memory policy is to allow              #include <numaif.h>
                                                                  long set_mempolicy(int mode,
existing code to work reasonably well in a NUMA en-
                                                                                      unsigned long *nodemask,
vironment without major modifications. The policy is                                  unsigned long maxnode);
inherited by child processes, which makes it possible to
use the numactl tool. This tool can be used to, among
other things, start a program with a given policy.
                                                                  The mode parameter must be one of the MPOL_* con-
The Linux kernel supports the following policies:                 stants introduced in the previous section. The nodemask
                                                                  parameter specifies the memory nodes to use for future
MPOL_BIND Memory is allocated only from the given                 allocations and maxnode is the number of nodes (i.e.,
        set of nodes. If this is not possible allocation fails.   bits) in nodemask. If mode is MPOL_DEFAULT no mem-
                                                                  ory nodes need to be specified and the nodemask param-
MPOL_PREFERRED Memory is preferably allocated from                eter is ignored. If a null pointer is passed as nodemask
        the given set of nodes. If this fails memory from         for MPOL_PREFERRED the local node is selected. Oth-
        other nodes is considered.                                erwise MPOL_PREFERRED uses the lowest node number
                                                                  with the corresponding bit set in nodemask.
MPOL_INTERLEAVE Memory is allocated equally from
        the specified nodes. The node is selected either by       Setting a policy does not have any effect on already-
        the offset in the virtual memory region for VMA-          allocated memory. Pages are not automatically migrated;
        based policies, or through a free-running counter         only future allocations are affected. Note the difference
        for task-based policies.                                  between memory allocation and address space reserva-
                                                                  tion: an address space region established using mmap
MPOL_DEFAULT Choose the allocation based on the de-
                                                                  is usually not automatically allocated. The first read or
        fault for the region.                                     write operation on the memory region will allocate the
                                                                  appropriate page. If the policy changes between accesses
This list seems to recursively define policies. This is half      to different pages of the same address space region, or
true. In fact, memory policies form a hierarchy (see Fig-         if the policy allows allocation of memory from different
ure 6.15). If an address is covered by a VMA policy then          nodes, a seemingly uniform address space region might
this policy is used. A special kind of policy is used for         be scattered across many memory nodes.

6.5.3   Swapping and Policies                                         case this flag is used together with MPOL_MF_MOVE
                                                                      and/or MPOL_MF_MOVEALL the call will fail if any
If physical memory runs out, the system has to drop clean             page cannot be moved.
pages and save dirty pages to swap. The Linux swap im-
                                                                MPOL_MF_MOVE The kernel will try to move any page
plementation discards node information when it writes
                                                                      in the address range allocated on a node not in the
pages to swap. That means when the page is reused and
                                                                      set specified by nodemask. By default, only pages
paged in the node which is used will be chosen from
                                                                      used exclusively by the current process’s page ta-
scratch. The policies for the thread will likely cause a
                                                                      bles are moved.
node which is close to the executing processors to be
chosen, but the node might be different from the one used       MPOL_MF_MOVEALL Like MPOL_MF_MOVE but the ker-
before.                                                               nel will try to move all pages, not just those used
                                                                      by the current process’s page tables alone. This
This changing association means that the node associa-
                                                                      operation has system-wide implications since it in-
tion cannot be stored by a program as a property of the
                                                                      fluences the memory access of other processes–
page. The association can change over time. For pages
                                                                      which are possibly not owned by the same user–
which are shared with other processes this can also hap-
                                                                      as well. Therefore MPOL_MF_MOVEALL is a privi-
pen because a process asks for it (see the discussion of
                                                                      leged operation (CAP_NICE capability is needed).
mbind below). The kernel by itself can migrate pages if
one node runs out of space while other nodes still have
free space.                                                     Note that support for MPOL_MF_MOVE and MPOL_MF_-
                                                                MOVEALL was added only in the 2.6.16 Linux kernel.
Any node association the user-level code learns about can
therefore be true for only a short time. It is more of a hint   Calling mbind without any flags is most useful when the
than absolute information. Whenever accurate knowl-             policy for a newly reserved address range has to be spec-
edge is required the get_mempolicy interface should             ified before any pages are actually allocated.
be used (see section 6.5.5).

6.5.4   VMA Policy                                              void *p = mmap(NULL, len,
                                                                               PROT_READ|PROT_WRITE,
To set the VMA policy for an address range a different                         MAP_ANON, -1, 0);
                                                                if (p != MAP_FAILED)
interface has to be used:
                                                                  mbind(p, len, mode, nodemask, maxnode,
                                                                        0);

#include <numaif.h>
long mbind(void *start, unsigned long len,
           int mode,                                            This code sequence reserve an address space range of
           unsigned long *nodemask,                             len bytes and specifies that the policy mode referencing
           unsigned long maxnode,                               the memory nodes in nodemask should be used. Unless
           unsigned flags);                                     the MAP_POPULATE flag is used with mmap, no memory
                                                                will have been allocated by the time of the mbind call
                                                                and, therefore, the new policy applies to all pages in that
This interface registers a new VMA policy for the ad-           address space region.
dress range [start, start + len). Since memory han-
dling operates on pages the start address must be page-         The MPOL_MF_STRICT flag alone can be used to deter-
aligned. The len value is rounded up to the next page           mine whether any page in the address range described by
size.                                                           the start and len parameters to mbind is allocated on
                                                                nodes other than those specified by nodemask. No allo-
The mode parameter specifies, again, the policy; the val-       cated pages are changed. If all pages are allocated on the
ues must be chosen from the list in section 6.5.1. As with      specified nodes, the VMA policy for the address space
set_mempolicy, the nodemask parameter is only used              region will be changed according to mode.
for some policies. Its handling is identical.
                                                                Sometimes rebalancing of memory is needed, in which
The semantics of the mbind interface depends on the             case it might be necessary to move pages allocated on
value of the flags parameter. By default, if flags is           one node to another node. Calling mbind with MPOL_-
zero, the system call sets the VMA policy for the address       MF_MOVE set makes a best effort to achieve that. Only
range. Existing mappings are not affected. If this is not       pages which are solely referenced by the process’s page
sufficient there are currently three flags to modify this       table tree are considered for moving. There can be multi-
behavior; they can be selected individually or together:        ple users in the form of threads or other processes which
                                                                share that part of the page table tree. It is not possible
MPOL_MF_STRICT The call to mbind will fail if not all           to affect other processes which happen to map the same
     pages are on the nodes specified by nodemask. In           data. These pages do not share the page table entries.

If both the MPOL_MF_STRICT and MPOL_MF_MOVE bits              extreme circumstances. A thread can be assigned to an-
are set in the flags parameter passed to mbind the ker-       other CPU as the result of rebalancing the CPU loads. In-
nel will try to move all pages which are not allocated on     formation about the current CPU and node might there-
the specified nodes. If this is not possible the call will    fore be short-lived. The scheduler will try to keep the
fail. Such a call might be useful to determine whether        thread on the same CPU, and possibly even on the same
there is a node (or set of nodes) which can house all the     core, to minimize performance losses due to cold caches.
pages. Several combinations can be tried in succession        This means it is useful to look at the current CPU and
until a suitable node is found.                               node information; one only must avoid assuming the as-
                                                              sociation will not change.
The use of MPOL_MF_MOVEALL is harder to justify unless
running the current process is the main purpose of the        libNUMA provides two interfaces to query the node in-
computer. The reason is that even pages that appear in        formation for a given virtual address space range:
multiple page tables are moved. That can easily affect
other processes in a negative way. This operation should
thus be used with caution.                                    #include <libNUMA.h>
                                                              int NUMA_mem_get_node_idx(void *addr);
                                                              int NUMA_mem_get_node_mask(void *addr,
6.5.5   Querying Node Information                                                        size_t size,
                                                                                         size_t __destsize,
                                                                                         memnode_set_t *dest);
The get_mempolicy interface can be used to query a
variety of facts about the state of NUMA for a given ad-
dress.
                                                              NUMA_mem_get_node_mask sets in dest the bits for all
                                                              memory nodes on which the pages in the range [addr,
                                                              addr+size) are (or would be) allocated, according to
#include <numaif.h>
long get_mempolicy(int *policy,                               the governing policy. NUMA_mem_get_node only looks
             const unsigned long *nmask,                      at the address addr and returns the index of the mem-
             unsigned long maxnode,                           ory node on which this address is (or would be) allo-
             void *addr, int flags);                          cated. These interfaces are simpler to use than get_-
                                                              mempolicy and probably should be preferred.

                                                              The CPU currently used by a thread can be queried using
When get_mempolicy is called with zero for the flags          sched_getcpu (see section 6.4.3). Using this informa-
parameter, the information about the policy for address       tion, a program can determine the memory node(s) which
addr is stored in the word pointed to by policy and           are local to the CPU using the NUMA_cpu_to_memnode
in the bitmask for the nodes pointed to by nmask. If          interface from libNUMA:
addr falls into an address space region for which a VMA
policy has been specified, information about that policy
is returned. Otherwise information about the task policy      #include <libNUMA.h>
or, if necessary, system default policy will be returned.     int NUMA_cpu_to_memnode(size_t cpusetsize,
                                                                                      const cpu_set_t *cpuset,
If the MPOL_F_NODE flag is set in flags, and the policy                               size_t memnodesize,
governing addr is MPOL_INTERLEAVE, the value stored                                   memnode_set_t *
                                                                                      memnodeset);
in the word pointed to by policy is the index of the node
on which the next allocation is going to happen. This in-
formation can potentially be used to set the affinity of
a thread which is going to work on the newly-allocated        A call to this function will set (in the memory node set
memory. This might be a less costly way to achieve prox-      pointed to by the fourth parameter) all the bits corre-
imity, especially if the thread has yet to be created.        sponding to memory nodes which are local to any of the
                                                              CPUs in the set pointed to by the second parameter. Just
The MPOL_F_ADDR flag can be used to retrieve yet an-
                                                              like CPU information itself, this information is only cor-
other completely different data item. If this flag is used,
                                                              rect until the configuration of the machine changes (for
the value stored in the word pointed to by policy is
                                                              instance, CPUs get removed and added).
the index of the memory node on which the memory for
the page containing addr has been allocated. This in-         The bits in the memnode_set_t objects can be used in
formation can be used to make decisions about possible        calls to the low-level functions like get_mempolicy.
page migration, to decide which thread could work on the      It is more convenient to use the other functions in lib-
memory location most efficiently, and many more things.       NUMA. The reverse mapping is available through:
The CPU–and therefore memory node–a thread is using
is much more volatile than its memory allocations. Mem-
ory pages are, without explicit requests, only moved in       #include <libNUMA.h>

int NUMA_memnode_to_cpu(size_t memnodesize,                   The program will not experience any errors unless the
                        const memnode_set_t *                 bitmasks are empty after the masking, so CPU sets are
                        memnodeset,                           an almost-invisible means to control program execution.
                        size_t cpusetsize,
                        cpu_set_t *cpuset);                   This method is especially efficient on machines with lots
                                                              of CPUs and/or memory nodes. Moving a process into a
                                                              new CPU set is as simple as writing the process ID into
                                                              the tasks file of the appropriate CPU set.
The bits set in the resulting cpuset are those of the
CPUs local to any of the memory nodes with correspond-        The directories for the CPU sets contain a number of
ing bits set in memnodeset. For both interfaces, the          other files which can be used to specify details like be-
programmer has to be aware that the information can           havior under memory pressure and exclusive access to
change over time (especially with CPU hot-plugging). In       CPUs and memory nodes. The interested reader is re-
many situations, a single bit is set in the input bit set,    ferred to the file Documentation/cpusets.txt in the
but it is also meaningful, for instance, to pass the entire   kernel source tree.
set of CPUs retrieved by a call to sched_getaffinity
to NUMA_cpu_to_memnode to determine which are the             6.5.7   Explicit NUMA Optimizations
memory nodes the thread ever can have direct access to.
                                                              All the local memory and affinity rules cannot help out
6.5.6   CPU and Node Sets                                     if all threads on all the nodes need access to the same
                                                              memory regions. It is, of course, possible to simply re-
Adjusting code for SMP and NUMA environments by               strict the number of threads to a number supportable by
changing the code to use the interfaces described so far      the processors which are directly connected to the mem-
might be prohibitively expensive (or impossible) if the       ory node. This does not take advantage of SMP NUMA
sources are not available. Additionally, the system ad-       machines, though, and is therefore not a real option.
ministrator might want to impose restrictions on the re-      If the data in question is read-only there is a simple solu-
sources a user and/or process can use. For these situa-       tion: replication. Each node can get its own copy of the
tions the Linux kernel supports so-called CPU sets. The       data so that no inter-node accesses are necessary. Code
name is a bit misleading since memory nodes are also          to do this can look like this:
covered. They also have nothing to do with the cpu_-
set_t data type.

The interface to CPU sets is, at the moment, a special        void *local_data(void) {
filesystem. It is usually not mounted (so far at least).        static void *data[NNODES];
This can be changed with                                        int node =
                                                                  NUMA_memnode_self_current_idx();
                                                                if (node == -1)
     mount -t cpuset none /dev/cpuset                             /* Cannot get node, pick one. */
                                                                  node = 0;
                                                                if (data[node] == NULL)
The mount point /dev/cpuset must of course exist at               data[node] = allocate_data();
that time. The content of this directory is a description       return data[node];
of the default (root) CPU set. It comprises initially all     }
CPUs and all memory nodes. The cpus file in that di-          void worker(void) {
rectory shows the CPUs in the CPU set, the mems file the        void *data = local_data();
memory nodes, the tasks file the processes.                     for (...)
                                                                  compute using data
To create a new CPU set one simply creates a new direc-       }
tory somewhere in the hierarchy. The new CPU set will
inherit all settings from the parent. Then the CPUs and
memory nodes for new CPU set can be changed by writ-
ing the new values into the cpus and mems pseudo files        In this code the function worker prepares by getting a
in the new directory.                                         pointer to the local copy of the data by a call to local_-
                                                              data. Then it proceeds with the loop, which uses this
If a process belongs to a CPU set, the settings for the       pointer. The local_data function keeps a list of the al-
CPUs and memory nodes are used as masks for the affin-        ready allocated copies of the data around. Each system
ity and memory policy bitmasks. That means the pro-           has a limited number of memory nodes, so the size of the
gram cannot select any CPU in the affinity mask which         array with the pointers to the per-node memory copies
is not in the cpus file for the CPU set the process is us-    is limited in size. The NUMA_memnode_system_count
ing (i.e., where it is listed in the tasks file). Similarly   function from libNUMA returns this number. If memory
for the node masks for the memory policy and the mems         for the given node has not yet been allocated for the cur-
file.                                                         rent node (recognized by a null pointer in data at the in-

dex returned by the NUMA_memnode_self_current_-                      width. This possibility must be examined in detail before
idx call), a new copy is allocated.                                  the approach is taken. In theory, using all the bandwidth
                                                                     available to a processor can have positive effects. A fam-
It is important to realize that nothing terrible happens if          ily 10h Opteron processor can be directly connected to
the threads get scheduled onto another CPU connected to              up to four other processors. Utilizing all that additional
a different memory node after the getcpu system call43 .             bandwidth, perhaps coupled with appropriate prefetches
It just means that the accesses using the data variable in           (especially prefetchw) could lead to improvements if
worker access memory on another memory node. This
                                                                     the rest of the system plays along.
slows the program down until data is computed anew,
but that is all. The kernel will always avoid gratuitous
rebalancing of the per-CPU run queues. If such a trans-
fer happens it is usually for a good reason and will not
happen again for the near future.
Things are more complicated when the memory area in
question is writable. Simple duplication will not work in
this case. Depending on the exact situation there might a
number of possible solutions.
For instance, if the writable memory region is used to
accumulate results, it might be possible to first create a
separate region for each memory node in which the re-
sults are accumulated. Then, when this work is done, all
the per-node memory regions are combined to get the to-
tal result. This technique can work even if the work never
really stops, but intermediate results are needed. The re-
quirement for this approach is that the accumulation of
a result is stateless, i.e., it does not depend on the previ-
ously collected results.
It will always be better, though, to have direct access to
the writable memory region. If the number of accesses
to the memory region is substantial, it might be a good
idea to force the kernel to migrate the memory pages in
question to the local node. If the number of accesses
is really high, and the writes on different nodes do not
happen concurrently, this could help. But be aware that
the kernel cannot perform miracles: the page migration
is a copy operation and as such it is not cheap. This cost
has to be amortized.

6.5.8   Utilizing All Bandwidth

The numbers in Figure 5.4 show that access to remote
memory when the caches are ineffective is not measur-
ably slower than access to local memory. This means
a program could possibly save bandwidth to the local
memory by writing data it does not have to read again
into memory attached to another processor. The band-
width of the connection to the DRAM modules and the
bandwidth of the interconnects are mostly independent,
so parallel use could improve overall performance.
Whether this is really possible depends on many fac-
tors. One really has to be sure that caches are ineffec-
tive since otherwise the slowdown related to remote ac-
cesses is measurable. Another big problem is whether
the remote node has any needs for its own memory band-
   43 The user-level sched_getcpu interface is implemented using

the getcpu system call which should not be used directly and has a
different interface.

