ford to pipe all the data they need or provide through the   3      CPU Caches
CPU. Instead, they read or write the data directly from/to
the main memory (Direct Memory Access, DMA). In              CPUs are today much more sophisticated than they were
Figure 2.1 we can see that the cards can talk through        only 25 years ago. In those days, the frequency of the
the South- and Northbridge directly with the memory.         CPU core was at a level equivalent to that of the mem-
Other buses, like USB, also require FSB bandwidth–even       ory bus. Memory access was only a bit slower than reg-
if they do not use DMA–since the Southbridge is con-         ister access. But this changed dramatically in the early
nected via the Northbridge to the processor through the      90s, when CPU designers increased the frequency of the
FSB, too.                                                    CPU core but the frequency of the memory bus and the
While DMA is certainly beneficial, it means that there is    performance of RAM chips did not increase proportion-
more competition for the FSB bandwidth. In times with        ally. This is not due to the fact that faster RAM could
high DMA traffic the CPU might stall more than usual         not be built, as explained in the previous section. It is
while waiting for data from the main memory. There           possible but it is not economical. RAM as fast as current
are ways around this given the right hardware. With an       CPU cores is orders of magnitude more expensive than
architecture as in Figure 2.3 one can make sure the com-     any dynamic RAM.
putation uses memory on nodes which are not affected         If the choice is between a machine with very little, very
by DMA. It is also possible to attach a Southbridge to       fast RAM and a machine with a lot of relatively fast
each node, equally distributing the load on the FSB of       RAM, the second will always win given a working set
all the nodes. There are a myriad of possibilities. In       size which exceeds the small RAM size and the cost of
section 6 we will introduce techniques and programming       accessing secondary storage media such as hard drives.
interfaces which help achieving the improvements which       The problem here is the speed of secondary storage, usu-
are possible in software.                                    ally hard disks, which must be used to hold the swapped
Finally it should be mentioned that some cheap systems       out part of the working set. Accessing those disks is or-
have graphics systems without separate, dedicated video      ders of magnitude slower than even DRAM access.
RAM. Those systems use parts of the main memory as           Fortunately it does not have to be an all-or-nothing deci-
video RAM. Since access to the video RAM is frequent         sion. A computer can have a small amount of high-speed
(for a 1024x768 display with 16 bpp at 60Hz we are talk-     SRAM in addition to the large amount of DRAM. One
ing 94MB/s) and system memory, unlike RAM on graph-          possible implementation would be to dedicate a certain
ics cards, does not have two ports this can substantially    area of the address space of the processor as containing
influence the systems performance and especially the la-     the SRAM and the rest the DRAM. The task of the op-
tency. It is best to ignore such systems when performance    erating system would then be to optimally distribute data
is a priority. They are more trouble than they are worth.    to make use of the SRAM. Basically, the SRAM serves
People buying those machines know they will not get the      in this situation as an extension of the register set of the
best performance.                                            processor.
                                                             While this is a possible implementation it is not viable.
                                                             Ignoring the problem of mapping the physical resources
                                                             of such SRAM-backed memory to the virtual address
                                                             spaces of the processes (which by itself is terribly hard)
                                                             this approach would require each process to administer
                                                             in software the allocation of this memory region. The
                                                             size of the memory region can vary from processor to
                                                             processor (i.e., processors have different amounts of the
                                                             expensive SRAM-backed memory). Each module which
                                                             makes up part of a program will claim its share of the
                                                             fast memory, which introduces additional costs through
                                                             synchronization requirements. In short, the gains of hav-
                                                             ing fast memory would be eaten up completely by the
                                                             overhead of administering the resources.
                                                             So, instead of putting the SRAM under the control of
                                                             the OS or user, it becomes a resource which is transpar-
                                                             ently used and administered by the processors. In this
                                                             mode, SRAM is used to make temporary copies of (to
                                                             cache, in other words) data in main memory which is
                                                             likely to be used soon by the processor. This is possible
                                                             because program code and data has temporal and spa-
                                                             tial locality. This means that, over short periods of time,
                                                             there is a good chance that the same code or data gets

reused. For code this means that there are most likely
                                                                                       Main Memory
loops in the code so that the same code gets executed
over and over again (the perfect case for spatial locality).                                                     Bus
Data accesses are also ideally limited to small regions.
Even if the memory used over short time periods is not
close together there is a high chance that the same data                        Cache                  CPU Core
will be reused before long (temporal locality). For code
this means, for instance, that in a loop a function call is
made and that function is located elsewhere in the ad-                 Figure 3.1: Minimum Cache Configuration
dress space. The function may be distant in memory, but
calls to that function will be close in time. For data it
means that the total amount of memory used at one time         Figure 3.1 shows the minimum cache configuration. It
(the working set size) is ideally limited but the memory       corresponds to the architecture which could be found in
used, as a result of the random access nature of RAM, is       early systems which deployed CPU caches. The CPU
not close together. Realizing that locality exists is key to   core is no longer directly connected to the main mem-
the concept of CPU caches as we use them today.                ory.16 All loads and stores have to go through the cache.
                                                               The connection between the CPU core and the cache is
A simple computation can show how effective caches             a special, fast connection. In a simplified representation,
can theoretically be. Assume access to main memory             the main memory and the cache are connected to the sys-
takes 200 cycles and access to the cache memory take           tem bus which can also be used for communication with
15 cycles. Then code using 100 data elements 100 times         other components of the system. We introduced the sys-
each will spend 2,000,000 cycles on memory operations          tem bus as “FSB” which is the name in use today; see
if there is no cache and only 168,500 cycles if all data       section 2.2. In this section we ignore the Northbridge; it
can be cached. That is an improvement of 91.5%.                is assumed to be present to facilitate the communication
The size of the SRAM used for caches is many times             of the CPU(s) with the main memory.
smaller than the main memory. In the author’s experi-          Even though most computers for the last several decades
ence with workstations with CPU caches the cache size          have used the von Neumann architecture, experience has
has always been around 1/1000th of the size of the main        shown that it is of advantage to separate the caches used
memory (today: 4MB cache and 4GB main memory).                 for code and for data. Intel has used separate code and
This alone does not constitute a problem. If the size of       data caches since 1993 and never looked back. The mem-
the working set (the set of data currently worked on) is       ory regions needed for code and data are pretty much
smaller than the cache size it does not matter. But com-       independent of each other, which is why independent
puters do not have large main memories for no reason.          caches work better. In recent years another advantage
The working set is bound to be larger than the cache.          emerged: the instruction decoding step for the most com-
This is especially true for systems running multiple pro-      mon processors is slow; caching decoded instructions
cesses where the size of the working set is the sum of the     can speed up the execution, especially when the pipeline
sizes of all the individual processes and the kernel.          is empty due to incorrectly predicted or impossible-to-
What is needed to deal with the limited size of the cache      predict branches.
is a set of good strategies to determine what should be        Soon after the introduction of the cache the system got
cached at any given time. Since not all data of the work-      more complicated. The speed difference between the
ing set is used at exactly the same time we can use tech-      cache and the main memory increased again, to a point
niques to temporarily replace some data in the cache with      that another level of cache was added, bigger and slower
others. And maybe this can be done before the data is          than the first-level cache. Only increasing the size of the
actually needed. This prefetching would remove some            first-level cache was not an option for economical rea-
of the costs of accessing main memory since it happens         sons. Today, there are even machines with three levels
asynchronously with respect to the execution of the pro-       of cache in regular use. A system with such a processor
gram. All these techniques and more can be used to make        looks like Figure 3.2. With the increase on the number of
the cache appear bigger than it actually is. We will dis-      cores in a single CPU the number of cache levels might
cuss them in section 3.3. Once all these techniques are        increase in the future even more.
exploited it is up to the programmer to help the processor.
How this can be done will be discussed in section 6.           Figure 3.2 shows three levels of cache and introduces the
                                                               nomenclature we will use in the remainder of the docu-
3.1   CPU Caches in the Big Picture                            ment. L1d is the level 1 data cache, L1i the level 1 in-
                                                               struction cache, etc. Note that this is a schematic; the
                                                               data flow in reality need not pass through any of the
Before diving into technical details of the implementa-        higher-level caches on the way from the core to the main
tion of CPU caches some readers might find it useful to           16 In even earlier systems the cache was attached to the system bus
first see in some more details how caches fit into the “big    just like the CPU and the main memory. This was more a hack than a
picture” of a modern computer system.                          real solution.

                        Main Memory                                       3.2    Cache Operation at High Level
                                                   Bus
                                                                          To understand the costs and savings of using a cache we
                                                                          have to combine the knowledge about the machine ar-
               L3 Cache
                                                                          chitecture and RAM technology from section 2 with the
                                                                          structure of caches described in the previous section.
               L2 Cache                 L1i Cache
                                                                          By default all data read or written by the CPU cores is
                                                                          stored in the cache. There are memory regions which
              L1d Cache                 CPU Core                          cannot be cached but this is something only the OS im-
                                                                          plementers have to be concerned about; it is not visible
                                                                          to the application programmer. There are also instruc-
                                                                          tions which allow the programmer to deliberately bypass
Figure 3.2: Processor with Level 3 Cache                         certain caches. This will be discussed in section 6.
                                                                          If the CPU needs a data word the caches are searched
                                                                          first. Obviously, the cache cannot contain the content
memory. CPU designers have a lot of freedom design-
                                                                          of the entire main memory (otherwise we would need
ing the interfaces of the caches. For programmers these
                                                                          no cache), but since all memory addresses are cacheable,
design choices are invisible.
                                                                          each cache entry is tagged using the address of the data
In addition we have processors which have multiple cores                  word in the main memory. This way a request to read or
and each core can have multiple “threads”. The differ-                    write to an address can search the caches for a matching
ence between a core and a thread is that separate cores                   tag. The address in this context can be either the virtual
have separate copies of (almost17 ) all the hardware re-                  or physical address, varying based on the cache imple-
sources. The cores can run completely independently                       mentation.
unless they are using the same resources–e.g., the con-
                                                                          Since for the tag, in addition to the actual memory, addi-
nections to the outside–at the same time. Threads, on the
                                                                          tional space is required, it is inefficient to chose a word as
other hand, share almost all of the processor’s resources.
                                                                          the granularity of the cache. For a 32-bit word on an x86
Intel’s implementation of threads has only separate reg-
                                                                          machine the tag itself might need 32 bits or more. Fur-
isters for the threads and even that is limited, some regis-
                                                                          thermore, since spatial locality is one of the principles on
ters are shared. The complete picture for a modern CPU
                                                                          which caches are based, it would be bad to not take this
therefore looks like Figure 3.3.
                                                                          into account. Since neighboring memory is likely to be
                                                                          used together it should also be loaded into the cache to-
                                                                          gether. Remember also what we learned in section 2.2.1:
                         Main Memory                                      RAM modules are much more effective if they can trans-
                                                                 Bus      port many data words in a row without a new CAS or
                                                                          even RAS signal. So the entries stored in the caches are
                                                                          not single words but, instead, “lines” of several contigu-
                                                                          ous words. In early caches these lines were 32 bytes
                                                                          long; nowadays the norm is 64 bytes. If the memory
                                                                          bus is 64 bits wide this means 8 transfers per cache line.
                                                                          DDR supports this transport mode efficiently.
                                                                          When memory content is needed by the processor the
Figure 3.3: Multi processor, multi-core, multi-thread
                                                                          entire cache line is loaded into the L1d. The memory
                                                                          address for each cache line is computed by masking the
                                                                          address value according to the cache line size. For a 64
In this figure we have two processors, each with two
                                                                          byte cache line this means the low 6 bits are zeroed. The
cores, each of which has two threads. The threads share
                                                                          discarded bits are used as the offset into the cache line.
the Level 1 caches. The cores (shaded in the darker gray)
                                                                          The remaining bits are in some cases used to locate the
have individual Level 1 caches. All cores of the CPU
                                                                          line in the cache and as the tag. In practice an address
share the higher-level caches. The two processors (the
                                                                          value is split into three parts. For a 32-bit address it might
two big boxes shaded in the lighter gray) of course do
                                                                          look as follows:
not share any caches. All this will be important, espe-
cially when we are discussing the cache effects on multi-
process and multi-thread applications.                                          31                                              0
                                                                                      Tag               Cache Set         Offset
                                                                                       T                   S               O
  17 Early multi-core processors even had separate 2nd level caches and

no 3rd level cache.                                                       With a cache line size of 2O the low O bits are used

as the offset into the cache line. The next S bits select               ory it would not see the content of dirty cache lines in
the “cache set”. We will go into more detail soon on                    other processors. Providing direct access to the caches
why sets, and not single slots, are used for cache lines.               of one processor from another processor would be terri-
For now it is sufficient to understand there are 2S sets of             bly expensive and a huge bottleneck. Instead, processors
cache lines. This leaves the top 32−S−O = T bits which                  detect when another processor wants to read or write to a
form the tag. These T bits are the value associated with                certain cache line.
each cache line to distinguish all the aliases18 which are
                                                                        If a write access is detected and the processor has a clean
cached in the same cache set. The S bits used to address
                                                                        copy of the cache line in its cache, this cache line is
the cache set do not have to be stored since they are the
                                                                        marked invalid. Future references will require the cache
same for all cache lines in the same set.
                                                                        line to be reloaded. Note that a read access on another
When an instruction modifies memory the processor still                 CPU does not necessitate an invalidation, multiple clean
has to load a cache line first because no instruction mod-              copies can very well be kept around.
ifies an entire cache line at once (exception to the rule:
                                                                        More sophisticated cache implementations allow another
write-combining as explained in section 6.1). The con-
                                                                        possibility to happen. Assume a cache line is dirty in
tent of the cache line before the write operation therefore
                                                                        one processor’s cache and a second processor wants to
has to be loaded. It is not possible for a cache to hold
                                                                        read or write that cache line. In this case the main mem-
partial cache lines. A cache line which has been written
                                                                        ory is out-of-date and the requesting processor must, in-
to and which has not been written back to main memory
                                                                        stead, get the cache line content from the first proces-
is said to be “dirty”. Once it is written the dirty flag is
                                                                        sor. Through snooping, the first processor notices this
cleared.
                                                                        situation and automatically sends the requesting proces-
To be able to load new data in a cache it is almost always              sor the data. This action bypasses main memory, though
first necessary to make room in the cache. An eviction                  in some implementations the memory controller is sup-
from L1d pushes the cache line down into L2 (which                      posed to notice this direct transfer and store the updated
uses the same cache line size). This of course means                    cache line content in main memory. If the access is for
room has to be made in L2. This in turn might push the                  writing the first processor then invalidates its copy of the
content into L3 and ultimately into main memory. Each                   local cache line.
eviction is progressively more expensive. What is de-
                                                                        Over time a number of cache coherency protocols have
scribed here is the model for an exclusive cache as is
                                                                        been developed. The most important is MESI, which we
preferred by modern AMD and VIA processors. Intel
                                                                        will introduce in section 3.3.4. The outcome of all this
implements inclusive caches19 where each cache line in
                                                                        can be summarized in a few simple rules:
L1d is also present in L2. Therefore evicting from L1d is
much faster. With enough L2 cache the disadvantage of
wasting memory for content held in two places is mini-                      • A dirty cache line is not present in any other pro-
mal and it pays off when evicting. A possible advantage                       cessor’s cache.
of an exclusive cache is that loading a new cache line                      • Clean copies of the same cache line can reside in
only has to touch the L1d and not the L2, which could be                      arbitrarily many caches.
faster.
The CPUs are allowed to manage the caches as they like                  If these rules can be maintained, processors can use their
as long as the memory model defined for the processor                   caches efficiently even in multi-processor systems. All
architecture is not changed. It is, for instance, perfectly             the processors need to do is to monitor each others’ write
fine for a processor to take advantage of little or no mem-             accesses and compare the addresses with those in their
ory bus activity and proactively write dirty cache lines                local caches. In the next section we will go into a few
back to main memory. The wide variety of cache archi-                   more details about the implementation and especially the
tectures among the processors for the x86 and x86-64,                   costs.
between manufacturers and even within the models of
the same manufacturer, are testament to the power of the                Finally, we should at least give an impression of the costs
memory model abstraction.                                               associated with cache hits and misses. These are the
                                                                        numbers Intel lists for a Pentium M:
In symmetric multi-processor (SMP) systems the caches
of the CPUs cannot work independently from each other.                                     To Where        Cycles
All processors are supposed to see the same memory con-                                    Register         ≤1
tent at all times. The maintenance of this uniform view                                      L1d            ∼3
of memory is called “cache coherency”. If a processor
                                                                                              L2           ∼ 14
were to look simply at its own caches and main mem-
                                                                                         Main Memory       ∼ 240
  18 All cache lines with the same S part of the address are known by

the same alias.
  19 This generalization is not completely correct. A few caches are    These are the actual access times measured in CPU cy-
exclusive and some inclusive caches have exclusive cache properties.    cles. It is interesting to note that for the on-die L2 cache

a large part (probably even the majority) of the access                   ing set size. The Y–axis shows the average number of
time is caused by wire delays. This is a physical lim-                    CPU cycles it takes to process one element; note that the
itation which can only get worse with increasing cache                    scale for the Y–axis is logarithmic. The same applies in
sizes. Only process shrinking (for instance, going from                   all the diagrams of this kind to the X–axis. The size of
60nm for Merom to 45nm for Penryn in Intel’s lineup)                      the working set is always shown in powers of two.
can improve those numbers.
                                                                          The graph shows three distinct plateaus. This is not sur-
The numbers in the table look high but, fortunately, the                  prising: the specific processor has L1d and L2 caches,
entire cost does not have to be paid for each occurrence                  but no L3. With some experience we can deduce that the
of the cache load and miss. Some parts of the cost can                    L1d is 213 bytes in size and that the L2 is 220 bytes in
be hidden. Today’s processors all use internal pipelines                  size. If the entire working set fits into the L1d the cycles
of different lengths where the instructions are decoded                   per operation on each element is below 10. Once the L1d
and prepared for execution. Part of the preparation is                    size is exceeded the processor has to load data from L2
loading values from memory (or cache) if they are trans-                  and the average time springs up to around 28. Once the
ferred to a register. If the memory load operation can                    L2 is not sufficient anymore the times jump to 480 cycles
be started early enough in the pipeline, it may happen in                 and more. This is when many or most operations have to
parallel with other operations and the entire cost of the                 load data from main memory. And worse: since data is
load might be hidden. This is often possible for L1d; for                 being modified dirty cache lines have to be written back,
some processors with long pipelines for L2 as well.                       too.
There are many obstacles to starting the memory read                      This graph should give sufficient motivation to look into
early. It might be as simple as not having sufficient re-                 coding improvements which help improve cache usage.
sources for the memory access or it might be that the final               We are not talking about a difference of a few measly per-
address of the load becomes available late as the result of               cent here; we are talking about orders-of-magnitude im-
another instruction. In these cases the load costs cannot                 provements which are sometimes possible. In section 6
be hidden (completely).                                                   we will discuss techniques which allow writing more ef-
                                                                          ficient code. The next section goes into more details of
For write operations the CPU does not necessarily have                    CPU cache designs. The knowledge is good to have but
to wait until the value is safely stored in memory. As                    not necessary for the rest of the paper. So this section
long as the execution of the following instructions ap-                   could be skipped.
pears to have the same effect as if the value were stored
in memory there is nothing which prevents the CPU from                    3.3     CPU Cache Implementation Details
taking shortcuts. It can start executing the next instruc-
tion early. With the help of shadow registers which can
hold values no longer available in a regular register it is               Cache implementers have the problem that each cell in
even possible to change the value which is to be stored in                the huge main memory potentially has to be cached. If
the incomplete write operation.                                           the working set of a program is large enough this means
                                                                          there are many main memory locations which fight for
                    1000                                                  each place in the cache. Previously it was noted that a
                     500                                                  ratio of 1-to-1000 for cache versus main memory size is
                                                                          not uncommon.
                     200
 Cycles/Operation

                     100
                      50                                                  3.3.1    Associativity
                      20
                      10                                                  It would be possible to implement a cache where each
                       5                                                  cache line can hold a copy of any memory location (see
                       2                                                  Figure 3.5). This is called a fully associative cache. To
                       1                                                  access a cache line the processor core would have to
                       210   213 216 219 222 225             228          compare the tags of each and every cache line with the
                             Working Set Size (Bytes)                     tag for the requested address. The tag would be com-
                                                                          prised of the entire part of the address which is not the
                                                                          offset into the cache line (that means, S in the figure on
Figure 3.4: Access Times for Random Writes            page 15 is zero).
                                                                          There are caches which are implemented like this but,
For an illustration of the effects of cache behavior see                  by looking at the numbers for an L2 in use today, will
Figure 3.4. We will talk about the program which gener-                   show that this is impractical. Given a 4MB cache with
ated the data later; it is a simple simulation of a program               64B cache lines the cache would have 65,536 entries.
which accesses a configurable amount of memory repeat-                    To achieve adequate performance the cache logic would
edly in a random fashion. Each data item has a fixed size.                have to be able to pick from all these entries the one
The number of elements depends on the selected work-                      matching a given tag in just a few cycles. The effort to

                                     Tag         Offset                             exactly one comparator, one multiplexer (two in this di-
                                                                                    agram where tag and data are separated, but this is not
                                 T T T T                                            a hard requirement on the design), and some logic to
             T       Comp                                 O                         select only valid cache line content. The comparator is
                 T           Comp                         O                         complex due to the speed requirements but there is only
                     T                                        O
                                                                                    one of them now; as a result more effort can be spent
                                  Comp
                                                                                    on making it fast. The real complexity in this approach
                         T             Comp                     O
                                                                                    lies in the multiplexers. The number of transistors in a
       Tag                                       O O O O              Data          simple multiplexer grows with O(log N), where N is the
                                                                                    number of cache lines. This is tolerable but might get
                                                                                    slow, in which case speed can be increased by spending
                                                                                    more real estate on transistors in the multiplexers to par-
Figure 3.5: Fully Associative Cache Schematics                                 allelize some of the work and to increase the speed. The
                                                                                    total number of transistors can grow slowly with a grow-
implement this would be enormous.                                                   ing cache size which makes this solution very attractive.
                                                                                    But it has a drawback: it only works well if the addresses
For each cache line a comparator is needed to compare                               used by the program are evenly distributed with respect
the large tag (note, S is zero). The letter next to each                            to the bits used for the direct mapping. If they are not,
connection indicates the width in bits. If none is given                            and this is usually the case, some cache entries are heav-
it is a single bit line. Each comparator has to compare                             ily used and therefore repeated evicted while others are
two T-bit-wide values. Then, based on the result, the ap-                           hardly used at all or remain empty.
propriate cache line content is selected and made avail-
able. This requires merging as many sets of O data lines                                                          Tag      Set    Offset
as there are cache buckets. The number of transistors                                        S                                                     S
needed to implement a single comparator is large espe-                                 MUX                                                         MUX
cially since it must work very fast. No iterative com-                                 ···                         T T T T                             ···
                                                                                       ···        T       Comp                        O                ···
parator is usable. The only way to save on the number
                                                                                       ···            T           Comp                     O           ···
of comparators is to reduce the number of them by iter-
atively comparing the tags. This is not suitable for the                               ···                T         Comp                   O           ···
same reason that iterative comparators are not: it takes                               ···                    T          Comp                  O     ···
too long.
                                                                                        Tag                                      O O O O           Data
Fully associative caches are practical for small caches
(for instance, the TLB caches on some Intel processors
are fully associative) but those caches are small, really
small. We are talking about a few dozen entries at most.                                 Figure 3.7: Set-Associative Cache Schematics

For L1i, L1d, and higher level caches a different ap-                               This problem can be solved by making the cache set as-
proach is needed. What can be done is to restrict the                               sociative. A set-associative cache combines the good
search. In the most extreme restriction each tag maps                               features of the full associative and direct-mapped caches
to exactly one cache entry. The computation is simple:                              to largely avoid the weaknesses of those designs. Fig-
given the 4MB/64B cache with 65,536 entries we can                                  ure 3.7 shows the design of a set-associative cache. The
directly address each entry by using bits 6 to 21 of the                            tag and data storage are divided into sets, one of which is
address (16 bits). The low 6 bits are the index into the                            selected by the address of a cache line. This is similar to
cache line.                                                                         the direct-mapped cache. But instead of only having one
                                                                                    element for each set value in the cache a small number
                                 Tag       Set   Offset                             of values is cached for the same set value. The tags for
                                                                                    all the set members are compared in parallel, which is
                             S                            S
             T                                                    O                 similar to the functioning of the fully associative cache.
             T                             T                      O                 The result is a cache which is not easily defeated by
                     MUX

                                                          MUX

                                 T     Comp           O
             T                                                    O                 unfortunate–or deliberate–selection of addresses with the
             T                                                    O                 same set numbers and at the same time the size of the
                                                  O                                 cache is not limited by the number of comparators which
       Tag                                                            Data          can be implemented economically. If the cache grows it
                                                                                    is (in this figure) only the number of columns which in-
                                                                                    creases, not the number of rows. The number of rows
Figure 3.6: Direct-Mapped Cache Schematics                                    (and therefore comparators) only increases if the asso-
                                                                                    ciativity of the cache is increased. Today processors are
Such a direct-mapped cache is fast and relatively easy                              using associativity levels of up to 24 for L2 caches or
to implement as can be seen in Figure 3.6. It requires                              higher. L1 caches usually get by with 8 sets.

     L2                                                         Associativity
   Cache               Direct                          2                                                     4                                 8
    Size       CL=32            CL=64       CL=32           CL=64                                    CL=32          CL=64          CL=32            CL=64
    512k     27,794,595     20,422,527    25,222,611       18,303,581             24,096,510                      17,356,121      23,666,929       17,029,334
     1M      19,007,315     13,903,854    16,566,738       12,127,174             15,537,500                      11,436,705      15,162,895       11,233,896
     2M      12,230,962      8,801,403     9,081,881        6,491,011              7,878,601                       5,675,181       7,391,389        5,382,064
     4M       7,749,986      5,427,836     4,736,187        3,159,507              3,788,122                       2,418,898       3,430,713        2,125,103
     8M       4,731,904      3,209,693     2,690,498        1,602,957              2,207,655                       1,228,190       2,111,075        1,155,847
    16M       2,620,587      1,528,592     1,958,293        1,089,580              1,704,878                         883,530       1,671,541          862,324

Table 3.1: Effects of Cache Size, Associativity, and Line Size

Given our 4MB/64B cache and 8-way set associativity                                                  28
the cache we are left with has 8,192 sets and only 13

                                                                        Cache Misses (in Millions)
bits of the tag are used in addressing the cache set. To                                             24
determine which (if any) of the entries in the cache set                                             20
contains the addressed cache line 8 tags have to be com-
pared. That is feasible to do in very short time. With an                                            16
experiment we can see that this makes sense.                                                         12
Table 3.1 shows the number of L2 cache misses for a                                                   8
program (gcc in this case, the most important benchmark
of them all, according to the Linux kernel people) for                                                4
changing cache size, cache line size, and associativity set
                                                                                                      0
size. In section 7.2 we will introduce the tool to simulate                                               512k      1M      2M   4M            8M     16M
the caches as required for this test.                                                                                     Cache Size
                                                                                                                 Direct   2-way   4-way   8-way
Just in case this is not yet obvious, the relationship of all
these values is that the cache size is
Figure 3.8: Cache Size vs Associativity (CL=32)
    cache line size × associativity × number of sets

                                                                    much smaller. We should not completely discount the ef-
The addresses are mapped into the cache by using
                                                                    fects, though. In the example program the peak memory
                                                                    use is 5.6M. So with a 8MB cache there are unlikely to
                                                                    be many (more than two) uses for the same cache set.
               O   =      log2 cache line size                      With a larger working set the savings can be higher as
               S   =      log2 number of sets                       we can see from the larger benefits of associativity for
                                                                    the smaller cache sizes.
in the way the figure on page 15 shows.
                                                                    In general, increasing the associativity of a cache above
Figure 3.8 makes the data of the table more comprehen-              8 seems to have little effects for a single-threaded work-
sible. It shows the data for a fixed cache line size of             load. With the introduction of hyper-threaded proces-
32 bytes. Looking at the numbers for a given cache size             sors where the first level cache is shared and multi-core
we can see that associativity can indeed help to reduce             processors which use a shared L2 cache the situation
the number of cache misses significantly. For an 8MB                changes. Now you basically have two programs hitting
cache going from direct mapping to 2-way set associative            on the same cache which causes the associativity in prac-
cache saves almost 44% of the cache misses. The proces-             tice to be halved (or quartered for quad-core processors).
sor can keep more of the working set in the cache with              So it can be expected that, with increasing numbers of
a set associative cache compared with a direct mapped               cores, the associativity of the shared caches should grow.
cache.                                                              Once this is not possible anymore (16-way set associa-
                                                                    tivity is already hard) processor designers have to start
In the literature one can occasionally read that introduc-          using shared L3 caches and beyond, while L2 caches are
ing associativity has the same effect as doubling cache             potentially shared by a subset of the cores.
size. This is true in some extreme cases as can be seen
in the jump from the 4MB to the 8MB cache. But it                   Another effect we can study in Figure 3.8 is how the in-
certainly is not true for further doubling of the associa-          crease in cache size helps with performance. This data
tivity. As we can see in the data, the successive gains are         cannot be interpreted without knowing about the working

set size. Obviously, a cache as large as the main mem-        All entries are chained in a circular list using the n el-
ory would lead to better results than a smaller cache, so     ement, either in sequential or random order. Advancing
there is in general no limit to the largest cache size with   from one entry to the next always uses the pointer, even if
measurable benefits.                                          the elements are laid out sequentially. The pad element
                                                              is the payload and it can grow arbitrarily large. In some
As already mentioned above, the size of the working set       tests the data is modified, in others the program only per-
at its peak is 5.6M. This does not give us any absolute       forms read operations.
number of the maximum beneficial cache size but it al-
lows us to estimate the number. The problem is that           In the performance measurements we are talking about
not all the memory used is contiguous and, therefore,         working set sizes. The working set is made up of an ar-
we have, even with a 16M cache and a 5.6M working             ray of struct l elements. A working set of 2N bytes
set, conflicts (see the benefit of the 2-way set associa-     contains
tive 16MB cache over the direct mapped version). But
it is a safe bet that with the same workload the benefits          2N /sizeof(struct l)
of a 32MB cache would be negligible. But who says the         elements. Obviously sizeof(struct l) depends on
working set has to stay the same? Workloads are grow-         the value of NPAD. For 32-bit systems, NPAD=7 means the
ing over time and so should the cache size. When buying       size of each array element is 32 bytes, for 64-bit systems
machines, and one has to choose the cache size one is         the size is 64 bytes.
willing to pay for, it is worthwhile to measure the work-
ing set size. Why this is important can be seen in the
figures on page 21.                                           Single Threaded Sequential Access            The simplest
                                                              case is a simple walk over all the entries in the list. The
                                                              list elements are laid out sequentially, densely packed.
                                                              Whether the order of processing is forward or backward
                                                              does not matter, the processor can deal with both direc-
                                                              tions equally well. What we measure here–and in all the
                        Random                                following tests–is how long it takes to handle a single list
                                                              element. The time unit is a processor cycle. Figure 3.10
                                                              shows the result. Unless otherwise specified, all mea-
                       Sequential                             surements are made on a Pentium 4 machine in 64-bit
                                                              mode which means the structure l with NPAD=0 is eight
                                                              bytes in size.
Figure 3.9: Test Memory Layouts
                                                              The first two measurements are polluted by noise. The
                                                              measured workload is simply too small to filter the ef-
Two types of tests are run. In the first test the elements    fects of the rest of the system out. We can safely assume
are processed sequentially. The test program follows the      that the values are all at the 4 cycles level. With this in
pointer n but the array elements are chained so that they     mind we can see three distinct levels:
are traversed in the order in which they are found in mem-
ory. This can be seen in the lower part of Figure 3.9.
There is one back reference from the last element. In the         • Up to a working set size of 214 bytes.
second test (upper part of the figure) the array elements         • From 215 bytes to 220 bytes.
are traversed in a random order. In both cases the array
elements form a circular single-linked list.                      • From 221 bytes and up.

3.3.2   Measurements of Cache Effects                         These steps can be easily explained: the processor has a
                                                              16kB L1d and 1MB L2. We do not see sharp edges in the
All the figures are created by measuring a program which      transition from one level to the other because the caches
can simulate working sets of arbitrary size, read and write   are used by other parts of the system as well and so the
access, and sequential or random access. We have al-          cache is not exclusively available for the program data.
ready seen some results in Figure 3.4. The program cre-       Specifically the L2 cache is a unified cache and also used
ates an array corresponding to the working set size of        for the instructions (NB: Intel uses inclusive caches).
elements of this type:                                        What is perhaps not quite expected are the actual times
                                                              for the different working set sizes. The times for the L1d
                                                              hits are expected: load times after an L1d hit are around
     struct l {                                               4 cycles on the P4. But what about the L2 accesses?
       struct l *n;                                           Once the L1d is not sufficient to hold the data one might
       long int pad[NPAD];                                    expect it would take 14 cycles or more per element since
     };                                                       this is the access time for the L2. But the results show
                                                              that only about 9 cycles are required. This discrepancy

                                  10                                                           can be explained by the advanced logic in the processors.
                                                                                               In anticipation of using consecutive memory regions, the
                                   9                                                           processor prefetches the next cache line. This means that
                                   8                                                           when the next line is actually used it is already halfway
           Cycles/List Element
                                   7                                                           loaded. The delay required to wait for the next cache line
                                   6                                                           to be loaded is therefore much less than the L2 access
                                                                                               time.
                                   5
                                   4                                                           The effect of prefetching is even more visible once the
                                   3                                                           working set size grows beyond the L2 size. Before we
                                                                                               said that a main memory access takes 200+ cycles. Only
                                   2
                                                                                               with effective prefetching is it possible for the processor
                                   1                                                           to keep the access times as low as 9 cycles. As we can
                                   0                                                           see from the difference between 200 and 9, this works
                                   210     213 216 219 222 225                    228          out nicely.
                                            Working Set Size (Bytes)
                                                                                               We can observe the processor while prefetching, at least
                                                                                               indirectly. In Figure 3.11 we see the times for the same
Figure 3.10: Sequential Read Access, NPAD=0                        working set sizes but this time we see the graphs for dif-
                                                                                               ferent sizes of the structure l. This means we have fewer
                                                                                               but larger elements in the list. The different sizes have
                                 350
                                                                                               the effect that the distance between the n elements in the
                                 300                                                           (still consecutive) list grows. In the four cases of the
                                                                                               graph the distance is 0, 56, 120, and 248 bytes respec-
      Cycles/List Element

                                 250                                                           tively.

                                 200                                                           At the bottom we can see the line from Figure 3.10, but
                                                                                               this time it appears more or less as a flat line. The times
                                 150                                                           for the other cases are simply so much worse. We can see
                                                                                               in this graph, too, the three different levels and we see the
                                 100
                                                                                               large errors in the tests with the small working set sizes
                                  50                                                           (ignore them again). The lines more or less all match
                                                                                               each other as long as only the L1d is involved. There is
                                   0                                                           no prefetching necessary so all element sizes just hit the
                                   210      213 216 219 222 225                   228          L1d for each access.
                                            Working Set Size (Bytes)                           For the L2 cache hits we see that the three new lines
                                         NPAD=0    NPAD=7    NPAD=15    NPAD=31
                                                                                               all pretty much match each other but that they are at a
                                                                                               higher level (about 28). This is the level of the access
                                                                                               time for the L2. This means prefetching from L2 into
Figure 3.11: Sequential Read for Several Sizes
                                                                                               L1d is basically disabled. Even with NPAD=7 we need a
                                                                                               new cache line for each iteration of the loop; for NPAD=0,
                                 400                                                           instead, the loop has to iterate eight times before the next
                                 350                                                           cache line is needed. The prefetch logic cannot load a
                                                                                               new cache line every cycle. Therefore we see a stall to
                                 300
 Cycles/List Element

                                                                                               load from L2 in every iteration.
                                 250
                                                                                               It gets even more interesting once the working set size
                                 200                                                           exceeds the L2 capacity. Now all four lines vary widely.
                                                                                               The different element sizes play obviously a big role in
                                 150
                                                                                               the difference in performance. The processor should rec-
                                 100                                                           ognize the size of the strides and not fetch unnecessary
                                                                                               cache lines for NPAD=15 and 31 since the element size
                                 50
                                                                                               is smaller than the prefetch window (see section 6.3.1).
                                   0                                                           Where the element size is hampering the prefetching ef-
                                   210     212 214 216 218 220 222                  224        forts is a result of a limitation of hardware prefetching:
                                             Working Set Size (Bytes)                          it cannot cross page boundaries. We are reducing the ef-
                                                  On Cache Line   On Page
                                                                                               fectiveness of the hardware scheduler by 50% for each
                                                                                               size increase. If the hardware prefetcher were allowed to
                                                                                               cross page boundaries and the next page is not resident
Figure 3.12: TLB Influence for Sequential Read                         or valid the OS would have to get involved in locating the

page. That means the program would experience a page                         down for larger NPAD values is the reduced efficiency of
fault it did not initiate itself. This is completely unaccept-               the TLB cache. Since the physical address has to be com-
able since the processor does not know whether a page is                     puted before a cache line can be read for either L2 or
not present or does not exist. In the latter case the OS                     main memory the address translation penalties are addi-
would have to abort the process. In any case, given that,                    tive to the memory access times. This in part explains
for NPAD=7 and higher, we need one cache line per list                       why the total cost per list element for NPAD=31 is higher
element the hardware prefetcher cannot do much. There                        than the theoretical access time for the RAM.
simply is no time to load the data from memory since
all the processor does is read one word and then load the                    We can glimpse a few more details of the prefetch im-
next element.                                                                plementation by looking at the data of test runs where
                                                                             the list elements are modified. Figure 3.13 shows three
Another big reason for the slowdown are the misses of                        lines. The element width is in all cases 16 bytes. The first
the TLB cache. This is a cache where the results of the                      line is the now familiar list walk which serves as a base-
translation of a virtual address to a physical address are                   line. The second line, labeled “Inc”, simply increments
stored, as is explained in more detail in section 4. The                     the pad[0] member of the current element before going
TLB cache is quite small since it has to be extremely                        on to the next. The third line, labeled “Addnext0”, takes
fast. If more pages are accessed repeatedly than the TLB                     the pad[0] list element of the next element and adds it
cache has entries for the translation from virtual to phys-                  to the pad[0] member of the current list element.
ical address has to be constantly repeated. This is a very
costly operation. With larger element sizes the cost of                      The naı̈ve assumption would be that the “Addnext0” test
a TLB lookup is amortized over fewer elements. That                          runs slower because it has more work to do. Before ad-
means the total number of TLB entries which have to be                       vancing to the next list element a value from that element
computed per list element is higher.                                         has to be loaded. This is why it is surprising to see that
                                                                             this test actually runs, for some working set sizes, faster
To observe the TLB effects we can run a different test.                      than the “Inc” test. The explanation for this is that the
For one measurement we lay out the elements sequen-                          load from the next list element is basically a forced pre-
tially as usual. We use NPAD=7 for elements which oc-                        fetch. Whenever the program advances to the next list
cupy one entire cache line. For the second measurement                       element we know for sure that element is already in the
we place each list element on a separate page. The rest                      L1d cache. As a result we see that the “Addnext0” per-
of each page is left untouched and we do not count it in                     forms as well as the simple “Follow” test as long as the
the total for the working set size.20 The consequence is                     working set size fits into the L2 cache.
that, for the first measurement, each list iteration requires
a new cache line and, for every 64 elements, a new page.                     The “Addnext0” test runs out of L2 faster than the “Inc”
For the second measurement each iteration requires load-                     test, though. It needs more data loaded from main mem-
ing a new cache line which is on a new page.                                 ory. This is why the “Addnext0” test reaches the 28 cy-
                                                                             cles level for a working set size of 221 bytes. The 28 cy-
The result can be seen in Figure 3.12. The measurements                      cles level is twice as high as the 14 cycles level the “Fol-
were performed on the same machine as Figure 3.11.                           low” test reaches. This is easy to explain, too. Since the
Due to limitations of the available RAM the working set                      other two tests modify memory an L2 cache eviction to
size had to be restricted to 224 bytes which requires 1GB                    make room for new cache lines cannot simply discard the
to place the objects on separate pages. The lower, red                       data. Instead it has to be written to memory. This means
curve corresponds exactly to the NPAD=7 curve in Fig-                        the available bandwidth on the FSB is cut in half, hence
ure 3.11. We see the distinct steps showing the sizes of                     doubling the time it takes to transfer the data from main
the L1d and L2 caches. The second curve looks radically                      memory to L2.
different. The important feature is the huge spike start-
ing when the working set size reaches 213 bytes. This                        One last aspect of the sequential, efficient cache han-
is when the TLB cache overflows. With an element size                        dling is the size of the cache. This should be obvious
of 64 bytes we can compute that the TLB cache has 64                         but it still should be pointed out. Figure 3.14 shows the
entries. There are no page faults affecting the cost since                   timing for the Increment benchmark with 128-byte ele-
the program locks the memory to prevent it from being                        ments (NPAD=15 on 64-bit machines). This time we see
swapped out.                                                                 the measurement from three different machines. The first
                                                                             two machines are P4s, the last one a Core2 processor.
As can be seen the number of cycles it takes to compute                      The first two differentiate themselves by having different
the physical address and store it in the TLB is very high.                   cache sizes. The first processor has a 32k L1d and an 1M
The graph in Figure 3.12 shows the extreme case, but it                      L2. The second one has 16k L1d, 512k L2, and 2M L3.
should now be clear that a significant factor in the slow-                   The Core2 processor has 32k L1d and 4M L2.

  20 Yes, this is a bit inconsistent because in the other tests we count
                                                                             The interesting part of the graph is not necessarily how
the unused part of the struct in the element size and we could define        well the Core2 processor performs relative to the other
NPAD so that each element fills a page. In that case the working set         two (although it is impressive). The main point of in-
sizes would be very different. This is not the point of this test, though,   terest here is the region where the working set size is
and since prefetching is ineffective anyway this makes little difference.

                                                                                              too large for the respective last level cache and the main
                            30                                                                memory gets heavily involved.
                            25                                                                As expected, the larger the last level cache is the longer
      Cycles/List Element                                                                     the curve stays at the low level corresponding to the L2
                            20                                                                access costs. The important part to notice is the perfor-
                                                                                              mance advantage this provides. The second processor
                            15                                                                (which is slightly older) can perform the work on the
                                                                                              working set of 220 bytes twice as fast as the first proces-
                            10                                                                sor. All thanks to the increased last level cache size. The
                                                                                              Core2 processor with its 4M L2 performs even better.
                             5
                                                                                              For a random workload this might not mean that much.
                             0                                                                But if the workload can be tailored to the size of the last
                             210      213 216 219 222 225                        228          level cache the program performance can be increased
                                       Working Set Size (Bytes)                               quite dramatically. This is why it sometimes is worth-
                                              Follow   Inc   Addnext0                         while to spend the extra money for a processor with a
                                                                                              larger cache.

Figure 3.13: Sequential Read and Write, NPAD=1

                            700                                                               Single Threaded Random Access We have seen that
                                                                                              the processor is able to hide most of the main memory
                            600
                                                                                              and even L2 access latency by prefetching cache lines
                                                                                              into L2 and L1d. This can work well only when the mem-
 Cycles/List Element

                            500
                                                                                              ory access is predictable, though.
                            400
                                                                                              If the access pattern is unpredictable or random the situa-
                            300                                                               tion is quite different. Figure 3.15 compares the per-list-
                                                                                              element times for the sequential access (same as in Fig-
                            200                                                               ure 3.10) with the times when the list elements are ran-
                                                                                              domly distributed in the working set. The order is deter-
                            100
                                                                                              mined by the linked list which is randomized. There is no
                              0                                                               way for the processor to reliably prefetch data. This can
                                                                                              only work by chance if elements which are used shortly
                              210     213 216 219 222 225                        228
                                                                                              after one another are also close to each other in memory.
                                      Working Set Size (Bytes)
                                  P4/32k/1M    P4/16k/512k/2M     Core2/32k/4M                There are two important points to note in Figure 3.15.
                                                                                              The first is the large number of cycles needed for grow-
                                                                                              ing working set sizes. The machine makes it possible
Figure 3.14: Advantage of Larger L2/L3 Caches                                to access the main memory in 200-300 cycles but here
                                                                                              we reach 450 cycles and more. We have seen this phe-
                            500                                                               nomenon before (compare Figure 3.11). The automatic
                            450                                                               prefetching is actually working to a disadvantage here.
                            400
                                                                                              The second interesting point is that the curve is not flat-
 Cycles/List Element

                            350                                                               tening at various plateaus as it has been for the sequen-
                            300                                                               tial access cases. The curve keeps on rising. To explain
                            250                                                               this we can measure the L2 access of the program for
                            200                                                               the various working set sizes. The result can be seen in
                                                                                              Figure 3.16 and Table 3.2.
                            150
                            100                                                               The figure shows that, when the working set size is larger
                             50                                                               than the L2 size, the cache miss ratio (L2 accesses / L2
                                                                                              misses) starts to grow. The curve has a similar form to
                              0
                                                                                              the one in Figure 3.15: it rises quickly, declines slightly,
                              210     213 216 219 222 225                        228          and starts to rise again. There is a strong correlation with
                                      Working Set Size (Bytes)                                the cycles per list element graph. The L2 miss rate will
                                               Sequential    Random                           grow until it eventually reaches close to 100%. Given a
                                                                                              large enough working set (and RAM) the probability that
                                                                                              any of the randomly picked cache lines is in L2 or is in
Figure 3.15: Sequential vs Random Read, NPAD=0                                            the process of being loaded can be reduced arbitrarily.

                                Sequential                                                                       Random
     Set                                 Ratio       Ł2 Accesses                                                           Ratio      L2 Accesses
     Size   L2 Hit   L2 Miss    #Iter   Miss/Hit     per Iteration                  L2 Hit         L2 Miss       #Iter    Miss/Hit    per Iteration
     220    88,636      843    16,384        0.94%             5.5                30,462             4721       1,024      13.42%                 34.4
      21
     2      88,105     1,584    8,192        1.77%            10.9                21,817            15,151        512      40.98%                 72.2
      22
     2      88,106     1,600    4,096        1.78%            21.9                22,258            22,285        256      50.03%                174.0
      23
     2      88,104     1,614    2,048        1.80%            43.8                27,521            26,274        128      48.84%                420.3
     224    88,114     1,655    1,024        1.84%            87.7                33,166            29,115          64     46.75%                973.1
      25
     2      88,112     1,730     512         1.93%          175.5                 39,858            32,360          32     44.81%           2,256.8
      26
     2      88,112     1,906     256         2.12%          351.6                 48,539            38,151          16     44.01%           5,418.1
      27
     2      88,114     2,244     128         2.48%          705.9                 62,423            52,049           8     45.47%          14,309.0
     228    88,120     2,939       64        3.23%        1,422.8                 81,906            87,167           4     51.56%          42,268.3
      29
     2      88,137     4,318       32        4.67%        2,889.2        119,079                   163,398           2     57.84%         141,238.5

Table 3.2: L2 Hits and Misses for Sequential and Random Walks, NPAD=0

The increasing cache miss rate alone explains some of
                                                                                           60%
the costs. But there is another factor. Looking at Ta-
ble 3.2 we can see in the L2/#Iter columns that the total                                  50%
number of L2 uses per iteration of the program is grow-
ing. Each working set is twice as large as the one be-               L2 Misses             40%
fore. So, without caching we would expect double the
main memory accesses. With caches and (almost) per-                                        30%
fect predictability we see the modest increase in the L2
use shown in the data for sequential access. The increase                                  20%
is due to the increase of the working set size and nothing
                                                                                           10%
else.
For random access the per-element access time more than                                    0%
doubles for each doubling of the working set size. This                                      210     213 216 219 222 225                          228
means the average access time per list element increases                                             Working Set Size (Bytes)
since the working set size only doubles. The reason be-                                                      Sequential   Random
hind this is a rising rate of TLB misses. In Figure 3.17 we
see the cost for random accesses for NPAD=7. Only this
time the randomization is modified. While in the normal                                             Figure 3.16: L2d Miss Ratio
case the entire list of randomized as one block (indicated
by the label ∞) the other 11 curves show randomizations                                    550
which are performed in smaller blocks. For the curve                                       500
labeled ‘60’ each set of 60 pages (245.760 bytes) is ran-                                  450
domized individually. That means all list elements in the
                                                                     Cycles/List Element

                                                                                           400
block are traversed before going over to an element in                                     350
the next block. This has the effect that number of TLB                                     300
entries which are used at any one time is limited.
                                                                                           250
The element size for NPAD=7 is 64 bytes, which corre-                                      200
sponds to the cache line size. Due to the randomized or-                                   150
der of the list elements it is unlikely that the hardware                                  100
prefetcher has any effect, most certainly not for more                                      50
than a handful of elements. This means the L2 cache                                          0
miss rate does not differ significantly from the random-                                     210     213 216 219 222 225                         228
ization of the entire list in one block. The performance                                             Working Set Size (Bytes)
of the test with increasing block size approaches asymp-
                                                                                              60 120 240        480 960 1920       3840   7680
totically the curve for the one-block randomization. This                                     15360 30720       61440 ∞
means the performance of this latter test case is signifi-
cantly influenced by the TLB misses. If the TLB misses
can be lowered the performance increases significantly                         Figure 3.17: Page-Wise Randomization, NPAD=7

(in one test we will see later up to 38%).                      line is needed. In the next section we will see how this is
                                                                currently implemented.
3.3.3   Write Behavior                                          Before we get to this there are two more cache policies
                                                                to mention:
Before we start looking at the cache behavior when mul-
tiple execution contexts (threads or processes) use the                • write-combining; and
same memory we have to explore a detail of cache im-
plementations. Caches are supposed to be coherent and                  • uncacheable.
this coherency is supposed to be completely transparent
for the userlevel code. Kernel code is a different story; it    Both these policies are used for special regions of the
occasionally requires cache flushes.                            address space which are not backed by real RAM. The
                                                                kernel sets up these policies for the address ranges (on
This specifically means that, if a cache line is modified,
                                                                x86 processors using the Memory Type Range Regis-
the result for the system after this point in time is the
                                                                ters, MTRRs) and the rest happens automatically. The
same as if there were no cache at all and the main mem-
                                                                MTRRs are also usable to select between write-through
ory location itself had been modified. This can be imple-
                                                                and write-back policies.
mented in two ways or policies:
                                                                Write-combining is a limited caching optimization more
                                                                often used for RAM on devices such as graphics cards.
    • write-through cache implementation;
                                                                Since the transfer costs to the devices are much higher
    • write-back cache implementation.                          than the local RAM access it is even more important
                                                                to avoid doing too many transfers. Transferring an en-
                                                                tire cache line just because a word in the line has been
The write-through cache is the simplest way to imple-           written is wasteful if the next operation modifies the next
ment cache coherency. If the cache line is written to,          word. One can easily imagine that this is a common oc-
the processor immediately also writes the cache line into       currence, the memory for horizontal neighboring pixels
main memory. This ensures that, at all times, the main          on a screen are in most cases neighbors, too. As the name
memory and cache are in sync. The cache content could           suggests, write-combining combines multiple write ac-
simply be discarded whenever a cache line is replaced.          cesses before the cache line is written out. In ideal cases
This cache policy is simple but not very fast. A pro-           the entire cache line is modified word by word and, only
gram which, for instance, modifies a local variable over        after the last word is written, the cache line is written to
and over again would create a lot of traffic on the FSB         the device. This can speed up access to RAM on devices
even though the data is likely not used anywhere else and       significantly.
might be short-lived.
                                                                Finally there is uncacheable memory. This usually means
The write-back policy is more sophisticated. Here the           the memory location is not backed by RAM at all. It
processor does not immediately write the modified cache         might be a special address which is hardcoded to have
line back to main memory. Instead, the cache line is only       some functionality implemented outside the CPU. For
marked as dirty. When the cache line is dropped from the        commodity hardware this most often is the case for mem-
cache at some point in the future the dirty bit will instruct   ory mapped address ranges which translate to accesses
the processor to write the data back at that time instead       to cards and devices attached to a bus (PCIe etc). On
of just discarding the content.                                 embedded boards one sometimes finds such a memory
Write-back caches have the chance to be significantly           address which can be used to turn an LED on and off.
better performing, which is why most memory in a sys-           Caching such an address would obviously be a bad idea.
tem with a decent processor is cached this way. The pro-        LEDs in this context are used for debugging or status re-
cessor can even take advantage of free capacity on the          ports and one wants to see this as soon as possible. The
FSB to store the content of a cache line before the line        memory on PCIe cards can change without the CPU’s
has to be evacuated. This allows the dirty bit to be cleared    interaction, so this memory should not be cached.
and the processor can just drop the cache line when the
room in the cache is needed.                                    3.3.4     Multi-Processor Support

But there is a significant problem with the write-back im-      In the previous section we have already pointed out the
plementation. When more than one processor (or core or          problem we have when multiple processors come into
hyper-thread) is available and accessing the same mem-          play. Even multi-core processors have the problem for
ory it must still be assured that both processors see the       those cache levels which are not shared (at least the L1d).
same memory content at all times. If a cache line is dirty
on one processor (i.e., it has not been written back yet)       It is completely impractical to provide direct access from
and a second processor tries to read the same memory lo-        one processor to the cache of another processor. The con-
cation, the read operation cannot just go out to the main       nection is simply not fast enough, for a start. The prac-
memory. Instead the content of the first processor’s cache      tical alternative is to transfer the cache content over to

the other processor in case it is needed. Note that this      the processor’s cache handling visible to the outside. The
also applies to caches which are not shared on the same       address of the cache line in question is visible on the ad-
processor.                                                    dress bus. In the following description of the states and
                                                              their transitions (shown in Figure 3.18) we will point out
The question now is when does this cache line transfer        when the bus is involved.
have to happen? This question is pretty easy to answer:
when one processor needs a cache line which is dirty in       Initially all cache lines are empty and hence also Invalid.
another processor’s cache for reading or writing. But         If data is loaded into the cache for writing the cache
how can a processor determine whether a cache line is         changes to Modified. If the data is loaded for reading
dirty in another processor’s cache? Assuming it just be-      the new state depends on whether another processor has
cause a cache line is loaded by another processor would       the cache line loaded as well. If this is the case then the
be suboptimal (at best). Usually the majority of mem-         new state is Shared, otherwise Exclusive.
ory accesses are read accesses and the resulting cache
lines are not dirty. Processor operations on cache lines      If a Modified cache line is read from or written to on
are frequent (of course, why else would we have this          the local processor, the instruction can use the current
paper?) which means broadcasting information about            cache content and the state does not change. If a sec-
changed cache lines after each write access would be im-      ond processor wants to read from the cache line the first
practical.                                                    processor has to send the content of its cache to the sec-
                                                              ond processor and then it can change the state to Shared.
What developed over the years is the MESI cache co-           The data sent to the second processor is also received
herency protocol (Modified, Exclusive, Shared, Invalid).      and processed by the memory controller which stores the
The protocol is named after the four states a cache line      content in memory. If this did not happen the cache line
can be in when using the MESI protocol:                       could not be marked as Shared. If the second processor
                                                              wants to write to the cache line the first processor sends
                                                              the cache line content and marks the cache line locally
Modified: The local processor has modified the cache
                                                              as Invalid. This is the infamous “Request For Owner-
     line. This also implies it is the only copy in any
                                                              ship” (RFO) operation. Performing this operation in the
     cache.
                                                              last level cache, just like the I→M transition is compara-
Exclusive: The cache line is not modified but known to        tively expensive. For write-through caches we also have
     not be loaded into any other processor’s cache.          to add the time it takes to write the new cache line con-
                                                              tent to the next higher-level cache or the main memory,
Shared: The cache line is not modified and might exist        further increasing the cost.
     in another processor’s cache.
                                                              If a cache line is in the Shared state and the local pro-
Invalid: The cache line is invalid, i.e., unused.             cessor reads from it no state change is necessary and the
                                                              read request can be fulfilled from the cache. If the cache
                                                              line is locally written to the cache line can be used as well
This protocol developed over the years from simpler ver-      but the state changes to Modified. It also requires that all
sions which were less complicated but also less efficient.    other possible copies of the cache line in other proces-
With these four states it is possible to efficiently imple-   sors are marked as Invalid. Therefore the write operation
ment write-back caches while also supporting concurrent       has to be announced to the other processors via an RFO
use of read-only data on different processors.                message. If the cache line is requested for reading by a
                                                              second processor nothing has to happen. The main mem-
                                                              ory contains the current data and the local state is already
                                                              Shared. In case a second processor wants to write to the
         M                    E                               cache line (RFO) the cache line is simply marked Invalid.
                                      local read              No bus operation is needed.
                                      local write
                                                              The Exclusive state is mostly identical to the Shared state
                                      remote read
                                                              with one crucial difference: a local write operation does
                                      remote write
         S                    I                               not have to be announced on the bus. The local cache
                                                              is known to be the only one holding this specific cache
                                                              line. This can be a huge advantage so the processor will
                                                              try to keep as many cache lines as possible in the Exclu-
                                                              sive state instead of the Shared state. The latter is the
Figure 3.18: MESI Protocol Transitions                fallback in case the information is not available at that
                                                              moment. The Exclusive state can also be left out com-
The state changes are accomplished without too much           pletely without causing functional problems. It is only
effort by the processors listening, or snooping, on the       the performance that will suffer since the E→M transi-
other processors’ work. Certain operations a processor        tion is much faster than the S→M transition.
performs are announced on external pins and thus make

From this description of the state transitions it should be                time–have to access the same memory regions to syn-
clear where the costs specific to multi-processor opera-                   chronize.
tions are. Yes, filling caches is still expensive but now
we also have to look out for RFO messages. Whenever                        Concurrency is severely limited by the finite bandwidth
such a message has to be sent things are going to be slow.                 available for the implementation of the necessary syn-
                                                                           chronization. Programs need to be carefully designed to
There are two situations when RFO messages are neces-                      minimize accesses from different processors and cores
sary:                                                                      to the same memory locations. The following measure-
                                                                           ments will show this and the other cache effects related
                                                                           to multi-threaded code.
     • A thread is migrated from one processor to another
       and all the cache lines have to be moved over to the
       new processor once.
                                                                           Multi Threaded Access To ensure that the gravity of
     • A cache line is truly needed in two different pro-                  the problems introduced by concurrently using the same
       cessors. 21                                                         cache lines on different processors is understood, we will
                                                                           look here at some more performance graphs for the same
In multi-thread or multi-process programs there is always                  program we used before. This time, though, more than
some need for synchronization; this synchronization is                     one thread is running at the same time. What is measured
implemented using memory. So there are some valid                          is the fastest runtime of any of the threads. This means
RFO messages. They still have to be kept as infrequent                     the time for a complete run when all threads are done is
as possible. There are other sources of RFO messages,                      even higher. The machine used has four processors; the
though. In section 6 we will explain these scenarios. The                  tests use up to four threads. All processors share one bus
Cache coherency protocol messages must be distributed                      to the memory controller and there is only one bus to the
among the processors of the system. A MESI transition                      memory modules.
cannot happen until it is clear that all the processors in
                                                                           Figure 3.19 shows the performance for sequential read-
the system have had a chance to reply to the message.
                                                                           only access for 128 bytes entries (NPAD=15 on 64-bit ma-
That means that the longest possible time a reply can
                                                                           chines). For the curve for one thread we can expect a
take determines the speed of the coherency protocol.22
                                                                           curve similar to Figure 3.11. The measurements are for a
Collisions on the bus are possible, latency can be high in
                                                                           different machine so the actual numbers vary.
NUMA systems, and of course sheer traffic volume can
slow things down. All good reasons to focus on avoiding                    The important part in this figure is of course the behavior
unnecessary traffic.                                                       when running multiple threads. Note that no memory is
                                                                           modified and no attempts are made to keep the threads
There is one more problem related to having more than
                                                                           in sync when walking the linked list. Even though no
one processor in play. The effects are highly machine
                                                                           RFO messages are necessary and all the cache lines can
specific but in principle the problem always exists: the
                                                                           be shared, we see up to an 18% performance decrease
FSB is a shared resource. In most machines all proces-
                                                                           for the fastest thread when two threads are used and up
sors are connected via one single bus to the memory con-
                                                                           to 34% when four threads are used. Since no cache lines
troller (see Figure 2.1). If a single processor can saturate
                                                                           have to be transported between the processors this slow-
the bus (as is usually the case) then two or four processors
                                                                           down is solely caused by the one or both of the two bot-
sharing the same bus will restrict the bandwidth available
                                                                           tlenecks: the shared bus from the processor to the mem-
to each processor even more.
                                                                           ory controller and bus from the memory controller to the
Even if each processor has its own bus to the memory                       memory modules. Once the working set size is larger
controller as in Figure 2.2 there is still the bus to the                  than the L3 cache in this machine all three threads will
memory modules. Usually this is one bus but, even in                       be prefetching new list elements. Even with two threads
the extended model in Figure 2.2, concurrent accesses to                   the available bandwidth is not sufficient to scale linearly
the same memory module will limit the bandwidth.                           (i.e., have no penalty from running multiple threads).

The same is true with the AMD model where each pro-                        When we modify memory things get even uglier. Fig-
cessor can have local memory. All processors can indeed                    ure 3.20 shows the results for the sequential Increment
concurrently access their local memory quickly, espe-                      test. This graph is using a logarithmic scale for the Y
cially with the integrated memory controller. But multi-                   axis. So, do not be fooled by the apparently small dif-
thread and multi-process programs–at least from time to                    ferences. We still have about a 18% penalty for run-
                                                                           ning two threads and now an amazing 93% penalty for
  21 At a smaller level the same is true for two cores on the same pro-
                                                                           running four threads. This means the prefetch traffic to-
cessor. The costs are just a bit smaller. The RFO message is likely to
be sent many times.
                                                                           gether with the write-back traffic is pretty much saturat-
  22 Which is why we see nowadays, for instance, AMD Opteron sys-          ing the bus when four threads are used.
tems with three sockets. Each processor is exactly one hop away given
that the processors only have three hyperlinks and one is needed for the   We use the logarithmic scale to show the results for the
Northbridge connection.                                                    L1d range. What can be seen is that, as soon as more

                            500                                                           than one thread is running, the L1d is basically ineffec-
                                                                                          tive. The single-thread access times exceed 20 cycles
                            450
                                                                                          only when the L1d is not sufficient to hold the work-
                            400                                                           ing set. When multiple threads are running, those access
      Cycles/List Element
                            350                                                           times are hit immediately, even with the smallest work-
                            300                                                           ing set sizes.
                            250                                                           One aspect of the problem is not shown here. It is hard to
                            200                                                           measure with this specific test program. Even though the
                            150                                                           test modifies memory and we therefore must expect RFO
                            100                                                           messages we do not see higher costs for the L2 range
                                                                                          when more than one thread is used. The program would
                             50
                                                                                          have to use a large amount of memory and all threads
                              0                                                           must access the same memory in parallel. This is hard
                              210      213 216 219 222 225                     228        to achieve without a lot of synchronization which would
                                       Working Set Size (Bytes)                           then dominate the execution time.
                                     #Threads=1   #Threads=2   #Threads=4
                                                                                          Finally in Figure 3.21 we have the numbers for the Add-
                                                                                          nextlast test with random access of memory. This figure
Figure 3.19: Sequential Read Access, Multiple Threads                                     is provided mainly to show the appallingly high numbers.
                                                                                          It now takes around 1,500 cycles to process a single list
                            1000                                                          element in the extreme case. The use of more threads
                             500                                                          is even more questionable. We can summarize the effi-
                                                                                          ciency of multiple thread use in a table.
                             200
 Cycles/List Element

                             100                                                               #Threads     Seq Read      Seq Inc    Rand Add
                              50                                                                        2         1.69       1.69          1.54
                                                                                                        4         2.98       2.07          1.65
                              20
                              10
                               5                                                                 Table 3.3: Efficiency for Multiple Threads

                               2
                                                                                          The table shows the efficiency for the multi-thread run
                               1                                                          with the largest working set size in the three figures on
                               210      213 216 219 222 225                     228       page 28. The number shows the best possible speed-up
                                        Working Set Size (Bytes)                          the test program incurs for the largest working set size by
                                     #Threads=1   #Threads=2   #Threads=4                 using two or four threads. For two threads the theoretical
                                                                                          limits for the speed-up are 2 and, for four threads, 4. The
                                                                                          numbers for two threads are not that bad. But for four
Figure 3.20: Sequential Increment, Multiple Threads                                  threads the numbers for the last test show that it is almost
                                                                                          not worth it to scale beyond two threads. The additional
                            1600                                                          benefit is minuscule. We can see this more easily if we
                            1400                                                          represent the data in Figure 3.21 a bit differently.

                            1200                                                          The curves in Figure 3.22 show the speed-up factors, i.e.,
 Cycles/List Element

                                                                                          relative performance compared to the code executed by
                            1000                                                          a single thread. We have to ignore the smallest sizes, the
                             800                                                          measurements are not accurate enough. For the range of
                                                                                          the L2 and L3 cache we can see that we indeed achieve
                             600                                                          almost linear acceleration. We almost reach factors of
                             400                                                          2 and 4 respectively. But as soon as the L3 cache is
                                                                                          not sufficient to hold the working set the numbers crash.
                             200                                                          They crash to the point that the speed-up of two and four
                               0                                                          threads is identical (see the fourth column in Table 3.3).
                               210      213 216 219 222 225                     228       This is one of the reasons why one can hardly find moth-
                                        Working Set Size (Bytes)                          erboard with sockets for more than four CPUs all using
                                                                                          the same memory controller. Machines with more pro-
                                     #Threads=1   #Threads=2   #Threads=4
                                                                                          cessors have to be built differently (see section 5).
                                                                                          These numbers are not universal. In some cases even
Figure 3.21: Random Addnextlast, Multiple Threads                                    working sets which fit into the last level cache will not

             4.5
               4                                                           The meaning of the variables is as follows:
             3.5
               3
                                                                                      N    = Number of instructions.
  Speed-Up
             2.5
                                                                                  Fmem     = Fraction of N that access memory.
               2
                                                                                    Ghit   = Fraction of loads that hit the cache.
             1.5
               1                                                                   Tproc   = Number of cycles per instruction.
             0.5                                                                  Tcache   = Number of cycles for cache hit.
               0                                                                  Tmiss    = Number of cycles for cache miss.
               210     213 216 219 222 225                  228                    Texe    = Execution time for program.
                        Working Set Size (Bytes)
                     #Threads=1   #Threads=2   #Threads=4

                                                                           For it to make any sense to use two threads the execution
                                                                           time of each of the two threads must be at most half of
Figure 3.22: Speed-Up Through Parallelism
                                                                           that of the single-threaded code. The only variable on
                                                                           either side is the number of cache hits. If we solve the
allow linear speed-ups. In fact, this is the norm since                    equation for the minimum cache hit rate required to not
threads are usually not as decoupled as is the case in this                slow down the thread execution by 50% or more we get
test program. On the other hand it is possible to work                     the graph in Figure 3.23.
with large working sets and still take advantage of more
than two threads. Doing this requires thought, though.                      100%
We will talk about some approaches in section 6.                             90%
                                                                             80%
Special Case: Hyper-Threads Hyper-Threads (some-                             70%
times called Symmetric Multi-Threading, SMT) are im-
plemented by the CPU and are a special case since the                        60%
individual threads cannot really run concurrently. They                      50%
all share almost all the processing resources except for
the register set. Individual cores and CPUs still work                       40%
in parallel but the threads implemented on each core are                     30%
limited by this restriction. In theory there can be many
threads per core but, so far, Intel’s CPUs at most have                      20%
two threads per core. The CPU is responsible for time-                       10%
multiplexing the threads. This alone would not make
much sense, though. The real advantage is that the CPU                        0%
can schedule another hyper-thread and take advantage of                                60%        70%      80%       90%       100%
available resources such as arithmetic logic units (ALUs)
when the currently running hyper-thread is delayed. In                       Figure 3.23: Minimum Cache Hit Rate For Speed-Up
most cases this is a delay caused by memory accesses.
If two threads are running on one hyper-threaded core the                  The input, measured on the X–axis, is the cache hit rate
program is only more efficient than the single-threaded                    Ghit of the single-thread code. The Y–axis shows the
code if the combined runtime of both threads is lower                      cache hit rate for the multi-threaded code. This value can
than the runtime of the single-threaded code. This is pos-                 never be higher than the single-threaded hit rate since,
sible by overlapping the wait times for different memory                   otherwise, the single-threaded code would use that im-
accesses which usually would happen sequentially. A                        proved code, too. For single-threaded hit rates–in this
simple calculation shows the minimum requirement on                        specific case–below 55% the program can in all cases
the cache hit rate to achieve a certain speed-up.                          benefit from using threads. The CPU is more or less idle
                                                                           enough due to cache misses to enable running a second
The execution time for a program can be approximated                       hyper-thread.
with a simple model with only one level of cache as fol-
lows (see [16]):                                                           The green area is the target. If the slowdown for the
                                                                           thread is less than 50% and the workload of each thread
                                                                           is halved the combined runtime might be less than the
     Texe       =     N (1 − Fmem )Tproc                                   single-thread runtime. For the modeled processor (num-
                                                                 
                         +Fmem (Ghit Tcache + (1 − Ghit )Tmiss )           bers for a P4 with hyper-threads were used) a program

with a hit rate of 60% for the single-threaded code re-                    The problem here are the virtual addresses used during
quires a hit rate of at least 10% for the dual-threaded pro-               execution which must to be translated with the help of
gram. That is usually doable. But if the single-threaded                   the Memory Management Unit (MMU) into physical ad-
code has a hit rate of 95% then the multi-threaded code                    dresses. This is a non-trivial operation. In the pipeline to
needs a hit rate of at least 80%. That is harder. Espe-                    execute an instruction the physical address might only be
cially, and this is the problem with hyper-threads, be-                    available at a later stage. This means that the cache logic
cause now the effective cache size (L1d here, in practice                  has to be very quick in determining whether the memory
also L2 and so on) available to each hyper-thread is cut                   location is cached. If virtual addresses could be used the
in half. Both hyper-threads use the same cache to load                     cache lookup can happen much earlier in the pipeline and
their data. If the working set of the two threads is non-                  in case of a cache hit the memory content can be made
overlapping the original 95% hit rate could also be cut in                 available. The result is that more of the memory access
half and is therefore much lower than the required 80%.                    costs could be hidden by the pipeline.
Hyper-threads are therefore only useful in a limited range                 Processor designers are currently using virtual address
of situations. The cache hit rate of the single-threaded                   tagging for the first level caches. These caches are rather
code must be low enough that given the equations above                     small and can be cleared without too much pain. At
and reduced cache size the new hit rate still meets the                    least partial clearing the cache is necessary if the page
goal. Then and only then can it make any sense at all to                   table tree of a process changes. It might be possible to
use hyper-threads. Whether the result is faster in prac-                   avoid a complete flush if the processor has an instruc-
tice depends on whether the processor is sufficiently able                 tion which allows to specify the virtual address range
to overlap the wait times in one thread with execution                     which has changed. Given the low latency of L1i and
times in the other threads. The overhead of parallelizing                  L1d caches (∼ 3 cycles) using virtual addresses is almost
the code must be added to the new total runtime and this                   mandatory.
additional cost often cannot be neglected.
                                                                           For larger caches including L2, L3, . . . caches physical
In section 6.3.4 we will see a technique where threads                     address tagging is needed. These caches have a higher
collaborate closely and the tight coupling through the                     latency and the virtual→physical address translation can
common cache is actually an advantage. This technique                      finish in time. Because these caches are larger (i.e., a lot
can be applicable to many situations if only the program-                  of information is lost when they are flushed) and refilling
mers are willing to put in the time and energy to extend                   them takes a long time due to the main memory access
their code.                                                                latency, flushing them often would be costly.
What should be clear is that if the two hyper-threads ex-                  It should, in general, not be necessary to know about the
ecute completely different code (i.e., the two threads are                 details of the address handling in those caches. They can-
treated like separate processors by the OS to execute sep-                 not be changed and all the factors which would influence
arate processes) the cache size is indeed cut in half which                the performance are normally something which should
means a significant increase in cache misses. Such OS                      be avoided or is associated with high cost. Overflowing
scheduling practices are questionable unless the caches                    the cache capacity is bad and all caches run into prob-
are sufficiently large. Unless the workload for the ma-                    lems early if the majority of the used cache lines fall into
chine consists of processes which, through their design,                   the same set. The latter can be avoided with virtually ad-
can indeed benefit from hyper-threads it might be best to                  dressed caches but is impossible for user-level processes
turn off hyper-threads in the computer’s BIOS.23                           to avoid for caches addressed using physical addresses.
                                                                           The only detail one might want to keep in mind is to not
                                                                           map the same physical memory location to two or more
3.3.5    Other Details
                                                                           virtual addresses in the same process, if at all possible.

So far we talked about the address as consisting of three
parts, tag, set index, and cache line offset. But what ad-                 Another detail of the caches which is rather uninterest-
dress is actually used? All relevant processors today pro-                 ing to programmers is the cache replacement strategy.
vide virtual address spaces to processes, which means                      Most caches evict the Least Recently Used (LRU) ele-
that there are two different kinds of addresses: virtual                   ment first. This is always a good default strategy. With
and physical.                                                              larger associativity (and associativity might indeed grow
                                                                           further in the coming years due to the addition of more
The problem with virtual addresses is that they are not                    cores) maintaining the LRU list becomes more and more
unique. A virtual address can, over time, refer to dif-                    expensive and we might see different strategies adopted.
ferent physical memory addresses. The same address in
different processes also likely refers to different physi-                 As for the cache replacement there is not much a pro-
cal addresses. So it is always better to use the physical                  grammer can do. If the cache is using physical address
memory address, right?                                                     tags there is no way to find out how the virtual addresses
                                                                           correlate with the cache sets. It might be that cache lines
   23 Another reason to keep hyper-threads enabled is debugging. SMT       in all logical pages are mapped to the same cache sets,
is astonishingly good at finding some sets of problems in parallel code.

leaving much of the cache unused. If anything, it is the        As a result CPU designers spend a lot of time and chip
job of the OS to arrange that this does not happen too          real estate on branch prediction so that pipeline stalls
often.                                                          happen as infrequently as possible.
With the advent of virtualization things get even more          On CISC processors the decoding stage can also take
complicated. Now not even the OS has control over the           some time. The x86 and x86-64 processors are espe-
assignment of physical memory. The Virtual Machine              cially affected. In recent years these processors therefore
Monitor (VMM, aka Hypervisor) is responsible for the            do not cache the raw byte sequence of the instructions in
physical memory assignment.                                     L1i but instead they cache the decoded instructions. L1i
                                                                in this case is called the “trace cache”. Trace caching
The best a programmer can do is to a) use logical mem-          allows the processor to skip over the first steps of the
ory pages completely and b) use page sizes as large as          pipeline in case of a cache hit which is especially good if
meaningful to diversify the physical addresses as much          the pipeline stalled.
as possible. Larger page sizes have other benefits, too,
but this is another topic (see section 4).                      As said before, the caches from L2 on are unified caches
                                                                which contain both code and data. Obviously here the
3.4     Instruction Cache                                       code is cached in the byte sequence form and not de-
                                                                coded.

Not just the data used by the processor is cached; the          To achieve the best performance there are only a few
instructions executed by the processor are also cached.         rules related to the instruction cache:
However, this cache is much less problematic than the
data cache. There are several reasons:                             1. Generate code which is as small as possible. There
                                                                      are exceptions when software pipelining for the
                                                                      sake of using pipelines requires creating more code
      • The quantity of code which is executed depends on
                                                                      or where the overhead of using small code is too
        the size of the code that is needed. The size of the
                                                                      high.
        code in general depends on the complexity of the
        problem. The complexity of the problem is fixed.           2. Help the processor making good prefetching de-
                                                                      cisions. This can be done through code layout or
      • While the program’s data handling is designed by              with explicit prefetching.
        the programmer the program’s instructions are usu-
        ally generated by a compiler. The compiler writers
                                                                These rules are usually enforced by the code generation
        know about the rules for good code generation.
                                                                of a compiler. There are a few things the programmer
      • Program flow is much more predictable than data         can do and we will talk about them in section 6.
        access patterns. Today’s CPUs are very good at
        detecting patterns. This helps with prefetching.        3.4.1   Self Modifying Code

      • Code always has quite good spatial and temporal         In early computer days memory was a premium. People
        locality.                                               went to great lengths to reduce the size of the program
                                                                to make more room for program data. One trick fre-
                                                                quently deployed was to change the program itself over
There are a few rules programmers should follow but             time. Such Self Modifying Code (SMC) is occasionally
these mainly consist of rules on how to use the tools. We       still found, these days mostly for performance reasons or
will discuss them in section 6. Here we talk only about         in security exploits.
the technical details of the instruction cache.
                                                                SMC should in general be avoided. Though it is gener-
Ever since the core clock of CPUs increased dramati-            ally correctly executed there are boundary cases which
cally and the difference in speed between cache (even           are not and it creates performance problems if not done
first level cache) and core grew, CPUs have been de-            correctly. Obviously, code which is changed cannot be
signed with pipelines. That means the execution of an           kept in the trace cache which contains the decoded in-
instruction happens in stages. First an instruction is de-      structions. But even if the trace cache is not used because
coded, then the parameters are prepared, and finally it         the code has not been executed at all (or for some time)
is executed. Such a pipeline can be quite long (> 20            the processor might have problems. If an upcoming in-
stages for Intel’s Netburst architecture). A long pipeline      struction is changed while it already entered the pipeline
means that if the pipeline stalls (i.e., the instruction flow   the processor has to throw away a lot of work and start
through it is interrupted) it takes a while to get up to        all over again. There are even situations where most of
speed again. Pipeline stalls happen, for instance, if the       the state of the processor has to be tossed away.
location of the next instruction cannot be correctly pre-
dicted or if it takes too long to load the next instruction     Finally, since the processor assumes–for simplicity rea-
(e.g., when it has to be read from memory).                     sons and because it is true in 99.9999999% of all cases–

that the code pages are immutable, the L1i implementa-
tion does not use the MESI protocol but instead a simpli-                     16
fied SI protocol. This means if modifications are detected                    14
a lot of pessimistic assumptions have to be made.
                                                                              12
It is highly advised to avoid SMC whenever possible.

                                                               Bytes/Cycles
                                                                              10
Memory is not such a scarce resource anymore. It is
better to write separate functions instead of modifying                        8
one function according to specific needs. Maybe one day                        6
SMC support can be made optional and we can detect                             4
exploit code trying to modify code this way. If SMC ab-
solutely has to be used, the write operations should by-                       2
pass the cache as to not create problems with data in L1d                      0
needed in L1i. See section 6.1 for more information on                         210    213 216 219 222 225             228
these instructions.                                                                    Working Set Size (Bytes)
                                                                                           Read   Write   Copy
On Linux it is normally quite easy to recognize programs
which contain SMC. All program code is write-protected
when built with the regular toolchain. The programmer
has to perform significant magic at link time to create                            Figure 3.24: Pentium 4 Bandwidth
an executable where the code pages are writable. When
this happens, modern Intel x86 and x86-64 processors
have dedicated performance counters which count uses                          16
of self-modifying code. With the help of these counters it                    14
is quite easily possible to recognize programs with SMC                       12
even if the program will succeed due to relaxed permis-        Bytes/Cycles
sions.                                                                        10
                                                                               8
3.5     Cache Miss Factors                                                     6
                                                                               4
We have already seen that when memory accesses miss
                                                                               2
the caches the costs skyrocket. Sometimes this is not
avoidable and it is important to understand the actual                         0
costs and what can be done to mitigate the problem.                            210    213 216 219 222 225             228
                                                                                       Working Set Size (Bytes)
3.5.1    Cache and Memory Bandwidth                                                        Read   Write   Copy

To get a better understanding of the capabilities of the
processors we measure the bandwidth available in opti-            Figure 3.25: P4 Bandwidth with 2 Hyper-Threads
mal circumstances. This measurement is especially in-
teresting since different processor versions vary widely.
This is why this section is filled with the data of sev-                      16
eral different machines. The program to measure perfor-                       14
mance uses the SSE instructions of the x86 and x86-64                         12
processors to load or store 16 bytes at once. The working
                                                               Bytes/Cycles

set is increased from 1kB to 512MB just as in our other                       10
tests and it is measured how many bytes per cycle can be                       8
loaded or stored.
                                                                               6
Figure 3.24 shows the performance on a 64-bit Intel Net-                       4
burst processor. For working set sizes which fit into L1d
                                                                               2
the processor is able to read the full 16 bytes per cy-
cle, i.e., one load instruction is performed per cycle (the                    0
movaps instruction moves 16 bytes at once). The test                           210    213 216 219 222 225             228
does not do anything with the read data, we test only the                              Working Set Size (Bytes)
read instructions themselves. As soon as the L1d is not                                    Read   Write   Copy
sufficient anymore the performance goes down dramati-
cally to less than 6 bytes per cycle. The step at 218 bytes
is due to the exhaustion of the DTLB cache which means                               Figure 3.26: Core 2 Bandwidth
additional work for each new page. Since the reading

is sequential prefetching can predict the accesses per-        optimal 16 bytes per cycle. Once L1d is not sufficient
fectly and the FSB can stream the memory content at            anymore the performance drops significantly. As with
about 5.3 bytes per cycle for all sizes of the working set.    the Netburst processor, the write performance is signifi-
The prefetched data is not propagated into L1d, though.        cantly lower. Due to the high read performance the dif-
These are of course numbers which will never be achiev-        ference is even higher here. In fact, when even the L2 is
able in a real program. Think of them as practical limits.     not sufficient anymore the speed difference increases to
                                                               a factor of 20! This does not mean the Core 2 proces-
What is more astonishing than the read performance is          sors perform poorly. To the contrary, their performance
the write and copy performance. The write performance,         is always better than the Netburst core’s.
even for small working set sizes, does not ever rise above
4 bytes per cycle. This indicates that, in these Netburst      In Figure 3.27 the test runs two threads, one on each of
processors, Intel elected to use a Write-Through mode          the two cores of the Core 2 processor. Both threads ac-
for L1d where the performance is obviously limited by          cess the same memory, not necessarily perfectly in sync,
the L2 speed. This also means that the performance of          though. The results for the read performance are not dif-
the copy test, which copies from one memory region into        ferent from the single-threaded case. A few more jitters
a second, non-overlapping memory region, is not signifi-       are visible which is to be expected in any multi-threaded
cantly worse. The necessary read operations are so much        test case.
faster and can partially overlap with the write operations.
The most noteworthy detail of the write and copy mea-          The interesting point is the write and copy performance
surements is the low performance once the L2 cache is          for working set sizes which would fit into L1d. As can be
not sufficient anymore. The performance drops to 0.5           seen in the figure, the performance is the same as if the
bytes per cycle! That means write operations are by a          data had to be read from the main memory. Both threads
factor of ten slower than the read operations. This means      compete for the same memory location and RFO mes-
optimizing those operations is even more important for         sages for the cache lines have to be sent. The problematic
the performance of the program.                                point is that these requests are not handled at the speed
                                                               of the L2 cache, even though both cores share the cache.
In Figure 3.25 we see the results on the same processor        Once the L1d cache is not sufficient anymore modified
but with two threads running, one pinned to each of the        entries are flushed from each core’s L1d into the shared
two hyper-threads of the processor. The graph is shown         L2. At that point the performance increases significantly
at the same scale as the previous one to illustrate the dif-   since now the L1d misses are satisfied by the L2 cache
ferences and the curves are a bit jittery simply because of    and RFO messages are only needed when the data has
the problem of measuring two concurrent threads. The           not yet been flushed. This is why we see a 50% reduction
results are as expected. Since the hyper-threads share all     in speed for these sizes of the working set. The asymp-
the resources except the registers each thread has only        totic behavior is as expected: since both cores share the
half the cache and bandwidth available. That means even        same FSB each core gets half the FSB bandwidth which
though each thread has to wait a lot and could award           means for large working sets each thread’s performance
the other thread with execution time this does not make        is about half that of the single threaded case.
any difference since the other thread also has to wait for
the memory. This truly shows the worst possible use of         Because there are significant differences even between
hyper-threads.                                                 the processor versions of one vendor it is certainly worth-
                                                               while looking at the performance of other vendors’ pro-
Compared to Figure 3.24 and 3.25 the results in Fig-           cessors, too. Figure 3.28 shows the performance of an
ure 3.26 and 3.27 look quite different for an Intel Core 2     AMD family 10h Opteron processor. This processor has
processor. This is a dual-core processor with shared L2        64kB L1d, 512kB L2, and 2MB of L3. The L3 cache is
which is four times as big as the L2 on the P4 machine.        shared between all cores of the processor. The results of
This only explains the delayed drop-off of the write and       the performance test can be seen in Figure 3.28.
copy performance, though.
                                                               The first detail one notices about the numbers is that the
There are other, bigger differences. The read perfor-          processor is capable of handling two instructions per cy-
mance throughout the working set range hovers around           cle if the L1d cache is sufficient. The read performance
the optimal 16 bytes per cycle. The drop-off in the read       exceeds 32 bytes per cycle and even the write perfor-
performance after 220 bytes is again due to the working        mance is, with 18.7 bytes per cycle, high. The read curve
set being too big for the DTLB. Achieving these high           flattens quickly, though, and is, with 2.3 bytes per cycle,
numbers means the processor is not only able to prefetch       pretty low. The processor for this test does not prefetch
the data and transport the data in time. It also means the     any data, at least not efficiently.
data is prefetched into L1d.
                                                               The write curve on the other hand performs according to
The write and copy performance is dramatically differ-         the sizes of the various caches. The peak performance
ent, too. The processor does not have a Write-Through          is achieved for the full size of the L1d, going down to 6
policy; written data is stored in L1d and only evicted         bytes per cycle for L2, to 2.8 bytes per cycle for L3, and
when necessary. This allows for write speeds close to the      finally .5 bytes per cycle if not even L3 can hold all the

                                                                     data. The performance for the L1d cache exceeds that of
                    16                                               the (older) Core 2 processor, the L2 access is equally fast
                    14                                               (with the Core 2 having a larger cache), and the L3 and
                                                                     main memory access is slower.
                    12
                                                                     The copy performance cannot be better than either the
     Bytes/Cycles
                    10
                                                                     read or write performance. This is why we see the curve
                     8                                               initially dominated by the read performance and later by
                     6                                               the write performance.
                     4                                               The multi-thread performance of the Opteron processor
                     2                                               is shown in Figure 3.29. The read performance is largely
                                                                     unaffected. Each thread’s L1d and L2 works as before
                     0
                                                                     and the L3 cache is in this case not prefetched very well
                     210   213 216 219 222 225            228        either. The two threads do not unduly stress the L3 for
                            Working Set Size (Bytes)                 their purpose. The big problem in this test is the write
                               Read   Write   Copy                   performance. All data the threads share has to go through
                                                                     the L3 cache. This sharing seems to be quite inefficient
                                                                     since even if the L3 cache size is sufficient to hold the
Figure 3.27: Core 2 Bandwidth with 2 Threads          entire working set the cost is significantly higher than an
                                                                     L3 access. Comparing this graph with Figure 3.27 we see
                                                                     that the two threads of the Core 2 processor operate at the
                    36                                               speed of the shared L2 cache for the appropriate range of
                    32                                               working set sizes. This level of performance is achieved
                    28                                               for the Opteron processor only for a very small range of
                    24                                               the working set sizes and even here it approaches only
     Bytes/Cycles

                                                                     the speed of the L3 which is slower than the Core 2’s L2.
                    20
                    16
                                                                     3.5.2   Critical Word Load
                    12
                     8
                                                                     Memory is transferred from the main memory into the
                     4                                               caches in blocks which are smaller than the cache line
                     0                                               size. Today 64 bits are transferred at once and the cache
                     210   213 216 219 222 225            228        line size is 64 or 128 bytes. This means 8 or 16 transfers
                            Working Set Size (Bytes)                 per cache line are needed.
                               Read   Write   Copy
                                                                     The DRAM chips can transfer those 64-byte blocks in
                                                                     burst mode. This can fill the cache line without any fur-
Figure 3.28: AMD Family 10h Opteron Bandwidth                   ther commands from the memory controller and the pos-
                                                                     sibly associated delays. If the processor prefetches cache
                                                                     lines this is probably the best way to operate.

                    28                                               If a program’s cache access of the data or instruction
                                                                     caches misses (that means, it is a compulsory cache miss,
                    24                                               because the data is used for the first time, or a capacity
                    20                                               cache miss, because the limited cache size requires evic-
     Bytes/Cycles

                                                                     tion of the cache line) the situation is different. The word
                    16                                               inside the cache line which is required for the program
                    12                                               to continue might not be the first word in the cache line.
                                                                     Even in burst mode and with double data rate transfer
                     8
                                                                     the individual 64-bit blocks arrive at noticeably different
                     4                                               times. Each block arrives 4 CPU cycles or more later
                                                                     than the previous one. If the word the program needs to
                     0
                                                                     continue is the eighth of the cache line the program has to
                     210   213 216 219 222 225            228        wait an additional 30 cycles or more after the first word
                            Working Set Size (Bytes)                 arrives.
                               Read   Write   Copy
                                                                     Things do not necessarily have to be like this. The mem-
                                                                     ory controller is free to request the words of the cache
Figure 3.29: AMD Fam 10h Bandwidth with 2 Threads                    line in a different order. The processor can communicate
                                                                     which word the program is waiting on, the critical word,

and the memory controller can request this word first.                                        Hyper-threads, by definition share everything but the reg-
Once the word arrives the program can continue while                                          ister set. This includes the L1 caches. There is not much
the rest of the cache line arrives and the cache is not yet in                                more to say here. The fun starts with the individual cores
a consistent state. This technique is called Critical Word                                    of a processor. Each core has at least its own L1 caches.
First & Early Restart.                                                                        Aside from this there are today not many details in com-
                                                                                              mon:
Processors nowadays implement this technique but there
are situations when that is not possible. If the processor
                                                                                                     • Early multi-core processors had no shared caches
prefetches data the critical word is not known. Should
                                                                                                       at all.
the processor request the cache line during the time the
prefetch operation is in flight it will have to wait until the                                       • Later Intel models have shared L2 caches for dual-
critical word arrives without being able to influence the                                              core processors. For quad-core processors we have
order.                                                                                                 to deal with separate L2 caches for each pair of two
                                                                                                       cores. There are no higher level caches.
Slowdown Vs Critical Word at Start

                                        0%                                                           • AMD’s family 10h processors have separate L2
                                                                                                       caches and a unified L3 cache.
                                     −0.2%

                                     −0.4%                                                    A lot has been written in the propaganda material of the
                                                                                              processor vendors about the advantage of their respec-
                                     −0.6%                                                    tive models. Having no shared cache has an advantage
                                                                                              if the working sets handled by the cores do not over-
                                     −0.8%                                                    lap. This works well for single-threaded programs. Since
                                      −1%                                                     this is still often the reality today this approach does not
                                                                                              perform too badly. But there is always some overlap.
                                     −1.2%                                                    The caches all contain the most actively used parts of
                                          210   213 216 219 222 225               228         the common runtime libraries which means some cache
                                                Working Set Size (Bytes)                      space is wasted.
                                                    Sequential   Random                       Completely sharing all caches beside L1 as Intel’s dual-
                                                                                              core processors do can have a big advantage. If the work-
                                                                                              ing set of the threads working on the two cores over-
Figure 3.30: Critical Word at End of Cache Line          laps significantly the total available cache memory is in-
                                                                                              creased and working sets can be bigger without perfor-
Even with these optimizations in place the position of the                                    mance degradation. If the working sets do not overlap In-
critical word on a cache line matters. Figure 3.30 shows                                      tel’s Advanced Smart Cache management is supposed to
the Follow test for sequential and random access. Shown                                       prevent any one core from monopolizing the entire cache.
is the slowdown of running the test with the pointer used
                                                                                              If both cores use about half the cache for their respective
in the chase in the first word versus the case when the
                                                                                              working sets there is some friction, though. The cache
pointer is in the last word. The element size is 64 bytes,
                                                                                              constantly has to weigh the two cores’ cache use and the
corresponding the cache line size. The numbers are quite
                                                                                              evictions performed as part of this rebalancing might be
noisy but it can be seen that, as soon as the L2 is not suffi-
                                                                                              chosen poorly. To see the problems we look at the results
cient to hold the working set size, the performance of the
                                                                                              of yet another test program.
case where the critical word is at the end is about 0.7%
slower. The sequential access appears to be affected a bit                                    The test program has one process constantly reading or
more. This would be consistent with the aforementioned                                        writing, using SSE instructions, a 2MB block of mem-
problem when prefetching the next cache line.                                                 ory. 2MB was chosen because this is half the size of the
                                                                                              L2 cache of this Core 2 processor. The process is pinned
                                                                                              to one core while a second process is pinned to the other
3.5.3                                   Cache Placement                                       core. This second process reads and writes a memory
                                                                                              region of variable size. The graph shows the number of
Where the caches are placed in relationship to the hyper-                                     bytes per cycle which are read or written. Four different
threads, cores, and processors is not under control of the                                    graphs are shown, one for each combination of the pro-
programmer. But programmers can determine where the                                           cesses reading and writing. The read/write graph is for
threads are executed and then it becomes important how                                        the background process, which always uses a 2MB work-
the caches relate to the used CPUs.                                                           ing set to write, and the measured process with variable
                                                                                              working set to read.
Here we will not go into details of when to select what
cores to run the threads. We will only describe architec-                                     The interesting part of the graph is the part between 220
ture details which the programmer has to take into ac-                                        and 223 bytes. If the L2 cache of the two cores were com-
count when setting the affinity of the threads.                                               pletely separate we could expect that the performance of

                   16                                                       know the workloads and the details of the machine archi-
                                                                            tecture to achieve the best performance. Fortunately we
                   14
                                                                            have support to determine the machine architecture. The
                   12                                                       interfaces will be introduced in later sections.
                   10
     Bytes/Cycle

                    8                                                       3.5.4                    FSB Influence

                    6                                                       The FSB plays a central role in the performance of the
                    4                                                       machine. Cache content can only be stored and loaded
                    2                                                       as quickly as the connection to the memory allows. We
                                                                            can show how much so by running a program on two
                    0                                                       machines which only differ in the speed of their memory
                    210   213 216 219 222 225                    228        modules. Figure 3.32 shows the results of the Addnext0
                           Working Set Size (Bytes)                         test (adding the content of the next elements pad[0] el-
                   Read/Read   Write/Read   Read/Write   Write/Write        ement to the own pad[0] element) for NPAD=7 on a 64-
                                                                            bit machine. Both machines have Intel Core 2 proces-
                                                                            sors, the first uses 667MHz DDR2 modules, the second
Figure 3.31: Bandwidth with two Processes                800MHz modules (a 20% increase).

                                                                                                    175
all four tests would drop between 221 and 222 bytes, that
means, once the L2 cache is exhausted. As we can see                                                150

                                                                              Cycles/List Element
in Figure 3.31 this is not the case. For the cases where                                            125
the background process is writing this is most visible.
The performance starts to deteriorate before the working                                            100
set size reaches 1MB. The two processes do not share                                                75
memory and therefore the processes do not cause RFO
messages to be generated. These are pure cache eviction                                             50
problems. The smart cache handling has its problems
                                                                                                    25
with the effect that the experienced cache size per core is
closer to 1MB than the 2MB per core which are available.                                              0
One can only hope that, if caches shared between cores                                                210      213 216 219 222 225              228
remain a feature of upcoming processors, the algorithm                                                         Working Set Size (Bytes)
used for the smart cache handling will be fixed.                                                                   Core2/667   Core2/800

Having a quad-core processor with two L2 caches was
just a stop-gap solution before higher-level caches could
Figure 3.32: Influence of FSB Speed
be introduced. This design provides no significant per-
formance advantage over separate sockets and dual-core
processors. The two cores communicate via the same bus                      The numbers show that, when the FSB is really stressed
which is, at the outside, visible as the FSB. There is no                   for large working set sizes, we indeed see a large bene-
special fast-track data exchange.                                           fit. The maximum performance increase measured in this
                                                                            test is 18.2%, close to the theoretical maximum. What
The future of cache design for multi-core processors will                   this shows is that a faster FSB indeed can pay off big
lie in more layers. AMD’s 10h processor family makes                        time. It is not critical when the working set fits into
the start. Whether we will continue to see lower level                      the caches (and these processors have a 4MB L2). It
caches be shared by a subset of the cores of a proces-                      must be kept in mind that we are measuring one program
sor remains to be seen (in the 2008 generation of proces-                   here. The working set of a system comprises the memory
sors L2 caches are not shared). The extra levels of cache                   needed by all concurrently running processes. This way
are necessary since the high-speed and frequently used                      it is easily possible to exceed 4MB memory or more with
caches cannot be shared among many cores. The per-                          much smaller programs.
formance would be impacted. It would also require very
large caches with high associativity. Both numbers, the                     Today some of Intel’s processors support FSB speeds
cache size and the associativity, must scale with the num-                  up to 1,333MHz which would mean another 60% in-
ber of cores sharing the cache. Using a large L3 cache                      crease. The future is going to see even higher speeds.
and reasonably-sized L2 caches is a reasonable trade-off.                   If speed is important and the working set sizes are larger,
The L3 cache is slower but it is ideally not as frequently                  fast RAM and high FSB speeds are certainly worth the
used as the L2 cache.                                                       money. One has to be careful, though, since even though
                                                                            the processor might support higher FSB speeds the moth-
For programmers all these different designs mean com-                       erboard/Northbridge might not. It is critical to check the
plexity when making scheduling decisions. One has to                        specifications.

