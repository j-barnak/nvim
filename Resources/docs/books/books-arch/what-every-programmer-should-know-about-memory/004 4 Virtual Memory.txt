4     Virtual Memory                                                       individually set by the OS. The page directory entry de-
                                                                           termines the address of a physical memory page; more
The virtual memory (VM) subsystem of a processor im-                       than one entry in the page directory can point to the same
plements the virtual address spaces provided to each pro-                  physical address. The complete physical address of the
cess. This makes each process think it is alone in the                     memory cell is determined by combining the page ad-
system. The list of advantages of virtual memory are de-                   dress from the page directory with the low bits from the
scribed in detail elsewhere so they will not be repeated                   virtual address. The page directory entry also contains
here. Instead this section concentrates on the actual im-                  some additional information about the page such as ac-
plementation details of the virtual memory subsystem                       cess permissions.
and the associated costs.
                                                                           The data structure for the page directory is stored in main
A virtual address space is implemented by the Memory                       memory. The OS has to allocate contiguous physical
Management Unit (MMU) of the CPU. The OS has to                            memory and store the base address of this memory re-
fill out the page table data structures, but most CPUs do                  gion in a special register. The appropriate bits of the
the rest of the work themselves. This is actually a pretty                 virtual address are then used as an index into the page
complicated mechanism; the best way to understand it                       directory, which is actually an array of directory entries.
is to introduce the data structures used to describe the
virtual address space.                                                     For a concrete example, this is the layout used for 4MB
                                                                           pages on x86 machines. The Offset part of the virtual
The input to the address translation performed by the                      address is 22 bits in size, enough to address every byte in
MMU is a virtual address. There are usually few–if any–                    a 4MB page. The remaining 10 bits of the virtual address
restrictions on its value. Virtual addresses are 32-bit val-               select one of the 1024 entries in the page directory. Each
ues on 32-bit systems, and 64-bit values on 64-bit sys-                    entry contains a 10 bit base address of a 4MB page which
tems. On some systems, for instance x86 and x86-64,                        is combined with the offset to form a complete 32 bit
the addresses used actually involve another level of indi-                 address.
rection: these architectures use segments which simply
cause an offset to be added to every logical address. We                   4.2   Multi-Level Page Tables
can ignore this part of address generation, it is trivial and
not something that programmers have to care about with                     4MB pages are not the norm, they would waste a lot of
respect to performance of memory handling.24                               memory since many operations an OS has to perform re-
                                                                           quire alignment to memory pages. With 4kB pages (the
4.1    Simplest Address Translation                                        norm on 32-bit machines and, still, often on 64-bit ma-
                                                                           chines), the Offset part of the virtual address is only 12
                                                                           bits in size. This leaves 20 bits as the selector of the
The interesting part is the translation of the virtual ad-
                                                                           page directory. A table with 220 entries is not practical.
dress to a physical address. The MMU can remap ad-
                                                                           Even if each entry would be only 4 bytes the table would
dresses on a page-by-page basis. Just as when addressing
                                                                           be 4MB in size. With each process potentially having its
cache lines, the virtual address is split into distinct parts.
                                                                           own distinct page directory much of the physical memory
These parts are used to index into various tables which
                                                                           of the system would be tied up for these page directories.
are used in the construction of the final physical address.
For the simplest model we have only one level of tables.                   The solution is to use multiple levels of page tables. The
                                                                           level then form a huge, sparse page directory; address
                       Virtual Address
                                                                           space regions which are not actually used do not require
           Directory                Offset                                 allocated memory. The representation is therefore much
                                             Physical Page                 more compact, making it possible to have the page tables
                                             Physical Address
                                                                           for many processes in memory without impacting perfor-
                Page Directory                                             mance too much.
                                                                           Today the most complicated page table structures com-
                                                                           prise four levels. Figure 4.2 shows the schematics of such
                Directory Entry
                                                                           an implementation. The virtual address is, in this exam-
                                                                           ple, split into at least five parts. Four of these parts are
                                                                           indexes into the various directories. The level 4 directory
Figure 4.1: 1-Level Address Translation                          is referenced using a special-purpose register in the CPU.
                                                                           The content of the level 4 to level 2 directories is a ref-
                                                                           erence to next lower level directory. If a directory entry
Figure 4.1 shows how the different parts of the virtual
                                                                           is marked empty it obviously need not point to any lower
address are used. A top part is used to select an entry
                                                                           directory. This way the page table tree can be sparse and
in a Page Directory; each entry in that directory can be
                                                                           compact. The entries of the level 1 directory are, just like
   24 Segment limits on x86 are performance-relevant but that is another   in Figure 4.1, partial physical addresses, plus auxiliary
story.                                                                     data like access permissions.

                                                             Virtual Address
                      Level 4 Index    Level 3 Index         Level 2 Index         Level 1 Index               Offset

                                                                                                                         Physical Page

                                                                                                                        Physical Address
                                                                                              Level 1 Directory
                                                                 Level 2 Directory
                                      Level 3 Directory
              Level 4 Directory
                                                                                                   Level 1 Entry
                                                                   Level 2 Entry
                                       Level 3 Entry
               Level 4 Entry

Figure 4.2: 4-Level Address Translation

To determine the physical address corresponding to a vir-                    shared libraries) are mapped at randomized addresses [9].
tual address the processor first determines the address of                   The randomization extends to the relative position of the
the highest level directory. This address is usually stored                  various parts; that implies that the various memory re-
in a register. Then the CPU takes the index part of the                      gions in use in a process are widespread throughout the
virtual address corresponding to this directory and uses                     virtual address space. By applying some limits to the
that index to pick the appropriate entry. This entry is the                  number of bits of the address which are randomized the
address of the next directory, which is indexed using the                    range can be restricted, but it certainly, in most cases, will
next part of the virtual address. This process continues                     not allow a process to run with just one or two directories
until it reaches the level 1 directory, at which point the                   for levels 2 and 3.
value of the directory entry is the high part of the physi-
cal address. The physical address is completed by adding                     If performance is really much more important than se-
the page offset bits from the virtual address. This process                  curity, randomization can be turned off. The OS will
is called page tree walking. Some processors (like x86                       then usually at least load all DSOs contiguously in vir-
and x86-64) perform this operation in hardware, others                       tual memory.
need assistance from the OS.
                                                                             4.3      Optimizing Page Table Access
Each process running on the system might need its own
page table tree. It is possible to partially share trees but
                                                                             All the data structures for the page tables are kept in the
this is rather the exception. It is therefore good for per-
                                                                             main memory; this is where the OS constructs and up-
formance and scalability if the memory needed by the
                                                                             dates the tables. Upon creation of a process or a change
page table trees is as small as possible. The ideal case
                                                                             of a page table the CPU is notified. The page tables are
for this is to place the used memory close together in the
                                                                             used to resolve every virtual address into a physical ad-
virtual address space; the actual physical addresses used
                                                                             dress using the page table walk described above. More to
do not matter. A small program might get by with using
                                                                             the point: at least one directory for each level is used in
just one directory at each of levels 2, 3, and 4 and a few
                                                                             the process of resolving a virtual address. This requires
level 1 directories. On x86-64 with 4kB pages and 512
                                                                             up to four memory accesses (for a single access by the
entries per directory this allows the addressing of 2MB
                                                                             running process) which is slow. It is possible to treat
with a total of 4 directories (one for each level). 1GB of
                                                                             these directory table entries as normal data and cache
contiguous memory can be addressed with one directory
                                                                             them in L1d, L2, etc., but this would still be far too slow.
for levels 2 to 4 and 512 directories for level 1.
                                                                             From the earliest days of virtual memory, CPU designers
Assuming all memory can be allocated contiguously is
                                                                             have used a different optimization. A simple computa-
too simplistic, though. For flexibility reasons the stack
                                                                             tion can show that only keeping the directory table en-
and the heap area of a process are, in most cases, allo-
                                                                             tries in the L1d and higher cache would lead to horrible
cated at pretty much opposite ends of the address space.
                                                                             performance. Each absolute address computation would
This allows either area to grow as much as possible if
                                                                             require a number of L1d accesses corresponding to the
needed. This means that there are most likely two level 2
                                                                             page table depth. These accesses cannot be parallelized
directories needed and correspondingly more lower level
                                                                             since they depend on the previous lookup’s result. This
directories.
                                                                             alone would, on a machine with four page table levels,
But even this does not always match current practice. For                    require at the very least 12 cycles. Add to that the proba-
security reasons the various parts of an executable (code,                   bility of an L1d miss and the result is nothing the instruc-
data, heap, stack, Dynamic Shared Objects (DSOs), aka                        tion pipeline can hide. The additional L1d accesses also
                                                                             steal precious bandwidth to the cache.

So, instead of just caching the directory table entries,       also possible that the address space layout of a process
the complete computation of the address of the physi-          changes. There are two ways to deal with this problem:
cal page is cached. For the same reason that code and
data caches work, such a cached address computation is
                                                                      • The TLB is flushed whenever the page table tree is
effective. Since the page offset part of the virtual address
                                                                        changed.
does not play any part in the computation of the physi-
cal page address, only the rest of the virtual address is             • The tags for the TLB entries are extended to ad-
used as the tag for the cache. Depending on the page size               ditionally and uniquely identify the page table tree
this means hundreds or thousands of instructions or data                they refer to.
objects share the same tag and therefore same physical
address prefix.
                                                               In the first case the TLB is flushed whenever a context
The cache into which the computed values are stored is         switch is performed. Since, in most OSes, a switch from
called the Translation Look-Aside Buffer (TLB). It is          one thread/process to another requires executing some
usually a small cache since it has to be extremely fast.       kernel code, TLB flushes are restricted to leaving (and
Modern CPUs provide multi-level TLB caches, just as            sometimes entering) the kernel address space. On vir-
for the other caches; the higher-level caches are larger       tualized systems it also happens when the kernel has to
and slower. The small size of the L1TLB is often made          call the VMM and on the way back. If the kernel and/or
up for by making the cache fully associative, with an          VMM does not have to use virtual addresses, or can reuse
LRU eviction policy. Recently, this cache has been grow-       the same virtual addresses as the process or kernel which
ing in size and, in the process, was changed to be set as-     made the system/VMM call (i.e., the address spaces are
sociative. As a result, it might not be the oldest entry       overlaid), the TLB only has to be flushed if, upon leaving
which gets evicted and replaced whenever a new entry           the kernel or VMM, the processor resumes execution of
has to be added.                                               a different process or kernel.
As noted above, the tag used to access the TLB is a part       Flushing the TLB is effective but expensive. When exe-
of the virtual address. If the tag has a match in the cache,   cuting a system call, for instance, the kernel code might
the final physical address is computed by adding the page      be restricted to a few thousand instructions which touch,
offset from the virtual address to the cached value. This      perhaps, a handful of new pages (or one huge page, as
is a very fast process; it has to be since the physical ad-    is the case for Linux on some architectures). This work
dress must be available for every instruction using abso-      would replace only as many TLB entries as pages are
lute addresses and, in some cases, for L2 look-ups which       touched. For Intel’s Core2 architecture with its 128 ITLB
use the physical address as the key. If the TLB lookup         and 256 DTLB entries, a full flush would mean that more
misses the processor has to perform a page table walk;         than 100 and 200 entries (respectively) would be flushed
this can be quite costly.                                      unnecessarily. When the system call returns to the same
Prefetching code or data through software or hardware          process, all those flushed TLB entries can be used again,
could implicitly prefetch entries for the TLB if the ad-       but they will be gone. The same is true for often-used
dress is on another page. This cannot be allowed for           code in the kernel or VMM. On each entry into the ker-
hardware prefetching because the hardware could initiate       nel the TLB has to be filled from scratch even though
page table walks that are invalid. Programmers therefore       the page tables for the kernel and VMM usually do not
cannot rely on hardware prefetching to prefetch TLB en-        change and, therefore, TLB entries could, in theory, be
tries. It has to be done explicitly using prefetch instruc-    preserved for a very long time. This also explains why
tions. TLBs, just like data and instruction caches, can        the TLB caches in today’s processors are not bigger: pro-
appear in multiple levels. Just as for the data cache, the     grams most likely will not run long enough to fill all these
TLB usually appears in two flavors: an instruction TLB         entries.
(ITLB) and a data TLB (DTLB). Higher-level TLBs such           This fact, of course, did not escape the CPU architects.
as the L2TLB are usually unified, as is the case with the      One possibility to optimize the cache flushes is to indi-
other caches.                                                  vidually invalidate TLB entries. For instance, if the ker-
                                                               nel code and data falls into a specific address range, only
4.3.1   Caveats Of Using A TLB                                 the pages falling into this address range have to evicted
                                                               from the TLB. This only requires comparing tags and,
                                                               therefore, is not very expensive. This method is also use-
The TLB is a processor-core global resource. All threads       ful in case a part of the address space is changed, for
and processes executed on the processor core use the           instance, through a call to munmap.
same TLB. Since the translation of virtual to physical ad-
dresses depends on which page table tree is installed, the     A much better solution is to extend the tag used for the
CPU cannot blindly reuse the cached entries if the page        TLB access. If, in addition to the part of the virtual ad-
table is changed. Each process has a different page ta-        dress, a unique identifier for each page table tree (i.e., a
ble tree (but not the threads in the same process) as does     process’s address space) is added, the TLB does not have
the kernel and the VMM (hypervisor) if present. It is          to be completely flushed at all. The kernel, VMM, and

the individual processes all can have unique identifiers.       The use of large page sizes brings some problems with
The only issue with this scheme is that the number of           it, though. The memory regions used for the large pages
bits available for the TLB tag is severely limited, while       must be contiguous in physical memory. If the unit size
the number of address spaces is not. This means some            for the administration of physical memory is raised to the
identifier reuse is necessary. When this happens the TLB        size of the virtual memory pages, the amount of wasted
has to be partially flushed (if this is possible). All en-      memory will grow. All kinds of memory operations (like
tries with the reused identifier must be flushed but this is,   loading executables) require alignment to page bound-
hopefully, a much smaller set.                                  aries. This means, on average, that each mapping wastes
                                                                half the page size in physical memory for each mapping.
This extended TLB tagging is of advantage outside the           This waste can easily add up; it thus puts an upper limit
realm of virtualization when multiple processes are run-        on the reasonable unit size for physical memory alloca-
ning on the system. If the memory use (and hence TLB            tion.
entry use) of each of the runnable processes is limited,
there is a good chance the most recently used TLB entries       It is certainly not practical to increase the unit size to
for a process are still in the TLB when it gets scheduled       2MB to accommodate large pages on x86-64. This is
again. But there are two additional advantages:                 just too large a size. But this in turn means that each
                                                                large page has to be comprised of many smaller pages.
     1. Special address spaces, such as those used by the       And these small pages have to be contiguous in physical
        kernel and VMM, are often only entered for a short      memory. Allocating 2MB of contiguous physical mem-
        time; afterward control is often returned to the ad-    ory with a unit page size of 4kB can be challenging. It
        dress space which initiated the entry. Without tags,    requires finding a free area with 512 contiguous pages.
        one or two TLB flushes are performed. With tags         This can be extremely difficult (or impossible) after the
        the calling address space’s cached translations are     system runs for a while and physical memory becomes
        preserved and, since the kernel and VMM address         fragmented.
        space do not often change TLB entries at all, the
                                                                On Linux it is therefore necessary to allocate these big
        translations from previous system calls, etc. can
                                                                pages at system start time using the special hugetlbfs
        still be used.
                                                                filesystem. A fixed number of physical pages are re-
     2. When switching between two threads of the same          served for exclusive use as big virtual pages. This ties
        process no TLB flush is necessary at all. With-         down resources which might not always be used. It also
        out extended TLB tags the entry into the kernel         is a limited pool; increasing it normally means restart-
        destroys the first thread’s TLB entries, though.        ing the system. Still, huge pages are the way to go in
                                                                situations where performance is a premium, resources
Some processors have, for some time, implemented these          are plenty, and cumbersome setup is not a big deterrent.
extended tags. AMD introduced a 1-bit tag extension             Database servers are an example.
with the Pacifica virtualization extensions. This 1-bit Ad-
                                                                Increasing the minimum virtual page size (as opposed to
dress Space ID (ASID) is, in the context of virtualization,
                                                                optional big pages) has its problems, too. Memory map-
used to distinguish the VMM’s address space from that of
                                                                ping operations (loading applications, for example) must
the guest domains. This allows the OS to avoid flushing
                                                                conform to these page sizes. No smaller mappings are
the guest’s TLB entries every time the VMM is entered
                                                                possible. The location of the various parts of an exe-
(for instance, to handle a page fault) or the VMM’s TLB
                                                                cutable have, for most architectures, a fixed relationship.
entries when control returns to the guest. The architec-
                                                                If the page size is increased beyond what has been taken
ture will allow the use of more bits in the future. Other
                                                                into account when the executable or DSO was built, the
mainstream processors will likely follow suit and support
                                                                load operation cannot be performed. It is important to
this feature.
                                                                keep this limitation in mind. Figure 4.3 shows how the
                                                                alignment requirements of an ELF binary can be deter-
4.3.2    Influencing TLB Performance                            mined. It is encoded in the ELF program header. In
                                                                this example, an x86-64 binary, the value is 20000016 =
There are a couple of factors which influence TLB per-          2, 097, 152 = 2MB which corresponds to the maximum
formance. The first is the size of the pages. Obviously,        page size supported by the processor.
the larger a page is, the more instructions or data objects
will fit into it. So a larger page size reduces the overall     There is a second effect of using larger page sizes: the
number of address translations which are needed, mean-          number of levels of the page table tree is reduced. Since
ing that fewer entries in the TLB cache are needed. Most        the part of the virtual address corresponding to the page
architectures nowadays allow the use of multiple differ-        offset increases, there are not that many bits left which
ent page sizes; some sizes can be used concurrently. For        need to be handled through page directories. This means
instance, the x86/x86-64 processors have a normal page          that, in case of a TLB miss, the amount of work which
size of 4kB but they can also use 4MB and 2MB pages             has to be done is reduced.
respectively. IA-64 and PowerPC allow sizes like 64kB           Beyond using large page sizes, it is possible to reduce the
as the base page size.

$ eu-readelf -l /bin/ls
Program Headers:
  Type   Offset   VirtAddr           PhysAddr           FileSiz MemSiz    Flg Align
...
  LOAD   0x000000 0x0000000000400000 0x0000000000400000 0x0132ac 0x0132ac R E 0x200000
  LOAD   0x0132b0 0x00000000006132b0 0x00000000006132b0 0x001a71 0x001a71 RW 0x200000
...

Figure 4.3: ELF Program Header Indicating Alignment Requirements

number of TLB entries needed by moving data which is           ory handling in the Dom0 and DomU kernels does not
used at the same time to fewer pages. This is similar to       have unrestricted access to physical memory. The VMM
some optimizations for cache use we talked about above.        does not hand out memory by giving out individual phys-
Only now the alignment required is large. Given that           ical pages and letting the guest OSes handle the address-
the number of TLB entries is quite small this can be an        ing; this would not provide any protection against faulty
important optimization.                                        or rogue guest domains. Instead, the VMM creates its
                                                               own page table tree for each guest domain and hands out
4.4     Impact Of Virtualization                               memory using these data structures. The good thing is
                                                               that access to the administrative information of the page
                                                               table tree can be controlled. If the code does not have
Virtualization of OS images will become more and more          appropriate privileges it cannot do anything.
prevalent; this means another layer of memory handling
is added to the picture. Virtualization of processes (basi-    This access control is exploited in the virtualization Xen
cally jails) or OS containers do not fall into this category   provides, regardless of whether para- or hardware (aka
since only one OS is involved. Technologies like Xen or        full) virtualization is used. The guest domains construct
KVM enable–with or without help from the processor–            their page table trees for each process in a way which is
the execution of independent OS images. In these situa-        intentionally quite similar for para- and hardware virtu-
tions there is one piece of software alone which directly      alization. Whenever the guest OS modifies its page ta-
controls access to the physical memory.                        bles the VMM is invoked. The VMM then uses the up-
                                                               dated information in the guest domain to update its own
                                                               shadow page tables. These are the page tables which are
                                                               actually used by the hardware. Obviously, this process
      Xen I/O Xen I/O
      Support Support                                          is quite expensive: each modification of the page table
                                                               tree requires an invocation of the VMM. While changes
                                                               to the memory mapping are not cheap without virtualiza-
                                                               tion they become even more expensive now.
      Dom0 Kernel       DomU Kernel     DomU Kernel
                                                               The additional costs can be really large, considering that
                                                               the changes from the guest OS to the VMM and back
                                                               themselves are already quite expensive. This is why the
                        Xen VMM
                                                               processors are starting to have additional functionality to
                                                               avoid the creation of shadow page tables. This is good
                                                               not only because of speed concerns but it also reduces
Figure 4.4: Xen Virtualization Model               memory consumption by the VMM. Intel has Extended
                                                               Page Tables (EPTs) and AMD calls it Nested Page Ta-
In the case of Xen (see Figure 4.4) the Xen VMM is             bles (NPTs). Basically both technologies have the page
that piece of software. The VMM does not implement             tables of the guest OSes produce “host virtual addresses”
many of the other hardware controls itself, though. Un-        from the “guest virtual address”. The host virtual ad-
like VMMs on other, earlier systems (and the first re-         dresses must then be further translated, using the per-
lease of the Xen VMM) the hardware outside of memory           domain EPT/NPT trees, into actual physical addresses.
and processors is controlled by the privileged Dom0 do-        This will allow memory handling at almost the speed of
main. Currently, this is basically the same kernel as the      the no-virtualization case since most VMM entries for
unprivileged DomU kernels and, as far as memory han-           memory handling are removed. It also reduces the mem-
dling is concerned, they do not differ. Important here is      ory use of the VMM since now only one page table tree
that the VMM hands out physical memory to the Dom0             for each domain (as opposed to process) has to be main-
and DomU kernels which, themselves, then implement             tained.
the usual memory handling as if they were running di-
                                                               The results of the additional address translation steps are
rectly on a processor.
                                                               also stored in the TLB. That means the TLB does not
To implement the separation of the domains which is re-        store the virtual physical address but, instead, the com-
quired for the virtualization to be complete, the mem-         plete result of the lookup. It was already explained that

AMD’s Pacifica extension introduced the ASID to avoid         leads to less work, fewer bugs, and, perhaps, less friction
TLB flushes on each entry. The number of bits for the         where the two memory handlers touch since the memory
ASID is one in the initial release of the processor exten-    handler in a Linux guest makes the same assumptions as
sions; this is just enough to differentiate VMM and guest     the memory handler in the outer Linux kernel which runs
OS. Intel has virtual processor IDs (VPIDs) which serve       on the bare hardware.
the same purpose, only there are more of them. But the
VPID is fixed for each guest domain and therefore it can-     Overall, programmers must be aware that, with virtual-
not be used to mark separate processes and avoid TLB          ization used, the cost of cache misses (instruction, data,
flushes at that level, too.                                   or TLB) is even higher than without virtualization. Any
                                                              optimization which reduces this work will pay off even
The amount of work needed for each address space mod-         more in virtualized environments. Processor designers
ification is one problem with virtualized OSes. There         will, over time, reduce the difference more and more
is another problem inherent in VMM-based virtualiza-          through technologies like EPT and NPT but it will never
tion, though: there is no way around having two layers        completely go away.
of memory handling. But memory handling is hard (es-
pecially when taking complications like NUMA into ac-
count, see section 5). The Xen approach of using a sep-
arate VMM makes optimal (or even good) handling hard
since all the complications of a memory management im-
plementation, including “trivial” things like discovery of
memory regions, must be duplicated in the VMM. The
OSes have fully-fledged and optimized implementations;
one really wants to avoid duplicating them.

                      Guest Kernel      Guest Kernel
      Userlevel
      Process         KVM VMM           KVM VMM

                     Linux Kernel

Figure 4.5: KVM Virtualization Model

This is why carrying the VMM/Dom0 model to its con-
clusion is such an attractive alternative. Figure 4.5 shows
how the KVM Linux kernel extensions try to solve the
problem. There is no separate VMM running directly
on the hardware and controlling all the guests; instead, a
normal Linux kernel takes over this functionality. This
means the complete and sophisticated memory handling
functionality in the Linux kernel is used to manage the
memory of the system. Guest domains run alongside
the normal user-level processes in what the creators call
“guest mode”. The virtualization functionality, para- or
full virtualization, is controlled by the KVM VMM. This
is just another userlevel process which happens to control
a guest domain using the special KVM device the kernel
implements.
The benefit of this model over the separate VMM of the
Xen model is that, even though there are still two mem-
ory handlers at work when guest OSes are used, there
only needs to be one implementation, that in the Linux
kernel. It is not necessary to duplicate the same function-
ality in another piece of code like the Xen VMM. This

