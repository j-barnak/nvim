                                                                    Glossary

absolute address A variable’s or routine’s        AND A logical bit-by-bit operation with
actual address in memory.                         two operands that calculates a 1 only if there
abstraction A model that renders                  is a 1 in both operands.
lower-level details of computer systems           antidependence Also called name
temporarily invisible to facilitate the design    dependence. An ordering forced by the
of sophisticated systems.                         reuse of a name, typically a register, rather
access bit Also called use bit or reference       than by a true dependence that carries a
bit. A field that is set whenever a page is       value between two instructions.
accessed and that is used to implement LRU        antifuse A structure in an integrated circuit
or other replacement schemes.                     that when programmed makes a permanent
acronym A word constructed by taking              connection between two wires.
the initial letters of a string of words. For     application binary interface (ABI) The
example: RAM is an acronym for Random             user portion of the instruction set plus
Access Memory, and CPU is an acronym for          the operating system interfaces used by
Central Processing Unit.                          application programmers. It defines a
active matrix display A liquid crystal            standard for binary portability across
display using a transistor to control the         computers.
transmission of light at each individual pixel.   architectural registers The instruction
address A value used to delineate the             set of visible registers of a processor; for
location of a specific data element within a      example, in LEGv8, these are the 32 integer
memory array.                                     and 32 floating-point registers.
address translation Also called address           arithmetic intensity The ratio of floating-
mapping. The process by which a virtual           point operations in a program to the
address is mapped to an address used to           number of data bytes accessed by a program
access memory.                                    from main memory.
addressing mode One of the several                arithmetic logic unit (ALU) Hardware that
addressing regimes delimited by their varied      performs addition, subtraction, and usually
use of operands and/or addresses.                 logical operations such as AND and OR.
aliasing A situation in which two addresses       assembler A program that translates a
access the same object; it can occur in           symbolic version of instruction into the
virtual memory when there are two virtual         binary version.
addresses for the same physical page.             assembler directive An operation that tells
alignment restriction A requirement               the assembler how to translate a program
that data be aligned in memory on natural         but does not produce machine instructions;
boundaries.                                       always begins with a period.
Amdahl’s Law A rule stating that the              assembly language A symbolic language
performance enhancement possible with             that can be translated into binary machine
a given improvement is limited by the             language.
amount that the improved feature is used.         asserted The signal is logically high or true.
It is a quantitative version of the law of        asserted signal A signal that is (logically)
diminishing returns.                              true, or 1.
G-2   Glossary

      backpatching A method for translating           branch prediction A method of resolving a
      from assembly language to machine               branch hazard that assumes a given outcome
      instructions in which the assembler builds a    for the conditional branch and proceeds
      (possibly incomplete) binary representation     from that assumption rather than waiting to
      of every instruction in one pass over           ascertain the actual outcome.
      a program and then returns to fill in           branch prediction buffer Also called
      previously undefined labels.                    branch history table. A small memory
      basic block A sequence of instructions          that is indexed by the lower portion of the
      without branches (except possibly at the        address of the branch instruction and that
      end) and without branch targets or branch       contains one or more bits indicating whether
      labels (except possibly at the beginning).      the branch was recently taken or not.
      behavioral specification Describes how a        branch taken A branch where the branch
      digital system operates functionally.           condition is satisfied and the program
      benchmark A program selected for use in         counter (PC) becomes the branch target. All
      comparing computer performance.                 unconditional branches are taken branches.
      biased notation A notation that represents      branch target address The address
      the most negative value by 00…000two and        specified in a branch, which becomes the
      the most positive value by 11…11two, with       new program counter (PC) if the branch
      0 typically having the value 10…00two,          is taken. In the ARMv8 architecture, the
      thereby biasing the number such that the        branch target is given by the sum of the
      number plus the bias has a nonnegative          offset field of the instruction and the address
      representation.                                 of the branch.
      binary digit Also called binary bit. One of     branch target buffer A structure that
      the two numbers in base 2, 0 or 1, that are     caches the destination PC or destination
      the components of information.                  instruction for a branch. It is usually
                                                      organized as a cache with tags, making it
      bisection bandwidth The bandwidth               more costly than a simple prediction
      between two equal parts of a multiprocessor.    buffer.
      This measure is for a worst-case split of the
      multiprocessor.                                 bus In logic design, a collection of data lines
                                                      that are treated together as a single logical
      block (or line) The minimum unit of             signal; also, a shared collection of lines with
      information that can be either present or not   multiple sources and uses.
      present in a cache.
                                                      cache memory A small, fast memory that
      blocking assignment In Verilog, an              acts as a buffer for a slower, larger memory.
      assignment that completes before the
      execution of the next statement.                cache miss A request for data from the
                                                      cache that cannot be filled because the data
      branch address table Also called branch         are not present in the cache.
      table. A table of addresses of alternative
      instruction sequences.                          callee A procedure that executes a series
                                                      of stored instructions based on parameters
      branch-and-link instruction An                  provided by the caller and then returns
      instruction that branches to an address and     control to the caller.
      simultaneously saves the address of the
      following instruction in a register (LR or      callee-saved register A register saved by the
      X30 in LEGv8).                                  routine making a procedure call.
      branch not taken or (untaken branch) A          caller The program that instigates a
      branch where the branch condition is false      procedure and provides the necessary
      and the program counter (PC) becomes the        parameter values.
      address of the instruction that sequentially    caller-saved register A register saved by the
      follows the branch.                             routine being called.
                                                                                       Glossary    G-3

capacity miss A cache miss that                  commit unit The unit in a dynamic or
occurs because the cache, even with full         out-of-order execution pipeline that decides
associativity, cannot contain all the blocks     when it is safe to release the result of an
needed to satisfy the request.                   operation to programmer-visible registers
central processing unit (CPU) Also called        and memory.
central processor unit or processor. The         compiler A program that translates high-
active part of the computer, which contains      level language statements into assembly
the datapath and control and which adds          language statements.
numbers, tests numbers, signals I/O devices      compulsory miss Also called cold-start
to activate, and so on.                          miss. A cache miss caused by the first
clock cycle Also called tick, clock tick,        access to a block that has never been in the
clock period, clock, or cycle. The time for      cache.
one clock period, usually of the processor
                                                 conditional branch An instruction that
clock.
                                                 tests a value, and that allows for a subsequent
clock cycles per instruction (CPI) Average       transfer of control to a new address in the
number of clock cycles per instruction for a     program based on the outcome of the test.
program or program fragment.
                                                 conflict miss Also called collision miss. A
clock period The length of each clock            cache miss that occurs in a set-associative
cycle.                                           or direct-mapped cache when multiple
clock skew The difference in absolute time       blocks compete for the same set and that are
between the times when two state elements        eliminated in a fully associative cache of the
see a clock edge.                                same size.
clocking methodology The approach used           context switch A changing of the internal
to determine when data are valid and stable      state of the processor to allow a different
relative to the clock.                           process to use the processor that includes
Cloud Computing refers to large                  saving the state needed to return to the
collections of servers that provide services     currently executing process.
over the Internet; some providers rent           control The component of the processor
dynamically varying numbers of servers as        that commands the datapath, memory, and
a utility.                                       I/O devices according to the instructions of
cluster A set of computers connected over        the program.
a local area network that function as a single   control hazard Also called branch hazard.
large multiprocessor.                            Arises when the proper instruction cannot
clusters Collections of computers                execute in the proper pipeline clock cycle
connected via I/O over standard network          because the instruction that was fetched is
switches to form a message-passing               not the one that is needed; that is, the flow
multiprocessor.                                  of instruction addresses is not what the
coarse-grained multithreading A version          pipeline expected.
of hardware multithreading that implies          control signal A signal used for multiplexor
switching between threads only after             selection or for directing the operation of a
significant events, such as a last-level cache   functional unit; contrasts with a data signal,
miss.                                            which contains information that is operated
combinational element An operational             on by a functional unit.
element, such as an AND gate or an ALU.          correlating predictor A branch predictor
combinational logic A logic system whose         that combines local behavior of a particular
blocks do not contain memory and hence           branch and global information about the
compute the same output given the same           behavior of some recent number of executed
input.                                           branches.
G-4   Glossary

      CPU execution time Also called CPU time.          branch is always executed, independent
      The actual time the CPU spends computing          of whether the branch condition is true or
      for a specific task.                              false.
      crossbar network A network that allows            die The individual rectangular sections that
      any node to communicate with any other            are cut from a wafer, more informally known
      node in one pass through the network.             as chips.
      D flip-flop A flip-flop with one data input       direct-mapped cache A cache structure in
      that stores the value of that input signal in     which each memory location is mapped to
      the internal memory when the clock edge           exactly one location in the cache.
      occurs.                                           dividend A number being divided.
      data hazard Also called a pipeline data           divisor A number that the dividend is
      hazard. When a planned instruction                divided by.
      cannot execute in the proper clock cycle
      because data that are needed to execute the       don’t-care term An element of a logical
      instruction are not yet available.                function in which the output does not
                                                        depend on the values of all the inputs. Don’t-
      data race Two memory accesses forming a           care terms may be specified in different
      data race if they are from different threads to   ways.
      the same location, at least one is a write, and
      they occur one after another.                     double precision A floating-point value
                                                        represented in a 64-bit doubleword.
      data segment The segment of a UNIX
      object or executable file that contains a         doubleword Another natural unit of access
      binary representation of the initialized data     in a computer, usually a group of 64 bits;
      used by the program.                              corresponds to the size of a register in the
                                                        LEGv8 architecture.
      data transfer instruction A command that
      moves data between memory and registers.          dynamic branch prediction Prediction
                                                        of branches at runtime using runtime
      data-level parallelism Parallelism achieved       information.
      by performing the same operation on
      independent data.                                 dynamic multiple issue An approach to
                                                        implementing a multiple-issue processor
      datapath The component of the processor           where many decisions are made during
      that performs arithmetic operations.              execution by the processor.
      datapath element A unit used to operate           dynamic pipeline scheduling Hardware
      on or hold data within a processor. In            support for reordering the order of
      the LEGv8 implementation, the datapath            instruction execution so as to avoid stalls.
      elements include the instruction and data
      memories, the register file, the ALU, and         dynamic random access memory
      adders.                                           (DRAM) Memory built as an integrated
                                                        circuit; it provides random access to any
      deasserted The signal being logically low         location. Access times are 50 nanoseconds
      or false.                                         and cost per gigabyte in 2012 was
      deasserted signal A signal that is (logically)    $5 to $10.
      false, or 0.                                      dynamically linked libraries
      decoder A logic block that has an n-bit           (DLLs) Library routines that are linked to a
      input and 2n outputs, where only one output       program during execution.
      is asserted for each input combination.           edge-triggered clocking A clocking scheme in
      defect A microscopic flaw in a wafer or in        which all state changes occur on a clock edge.
      patterning steps that can result in the failure   embedded computer A computer inside
      of the die containing that defect.                another device used for running one
      delayed branch A type of branch where             predetermined application or collection of
      the instruction immediately following the         software.
                                                                                       Glossary     G-5

EOR A logical bit-by-bit operation with two      switching between threads after every
operands that calculates the exclusive OR        instruction.
of the two operands. That is, it calculates a    finite-state machine A sequential logic
1 only if the values are different in the two    function consisting of a set of inputs and
operands.                                        outputs, a next-state function that maps
error detection code A code that enables         the current state and the inputs to a new
the detection of an error in data, but not the   state, and an output function that maps the
precise location and, hence, correction of       current state and possibly the inputs to a set
the error.                                       of asserted outputs.
exception Also called an interrupt. An           flash memory A nonvolatile semiconductor
unscheduled event that disrupts program          memory. It is cheaper and slower than
execution; used to detect overflow.              DRAM but more expensive per bit and faster
exception enable Also called interrupt           than magnetic disks. Access times are about
enable. A signal or action that controls         5 to 50 microseconds and cost per gigabyte
whether the process responds to an               in 2012 was $0.75 to $1.00.
exception or not; necessary for preventing       flip-flop A memory element for which the
the occurrence of exceptions during              output is equal to the value of the stored state
intervals before the processor has safely        inside the element and for which the internal
saved the state needed to restart.               state is changed only on a clock edge.
executable file A functional program in          floating point Computer arithmetic that
the format of an object file that contains no    represents numbers in which the binary
unresolved references. It can contain symbol     point is not fixed.
tables and debugging information.
                                                 flush To discard instructions in a pipeline,
A “stripped executable” does not contain
                                                 usually due to an unexpected event.
that information. Relocation information
may be included for the loader.                  formal parameter A variable that is
                                                 the argument to a procedure or macro;
exponent In the numerical representation
                                                 replaced by that argument once the macro is
system of floating-point arithmetic,
                                                 expanded.
the value that is placed in the exponent
field.                                           forward reference A label that is used
external label Also called global label.         before it is defined.
A label referring to an object that can be       forwarding Also called bypassing. A
referenced from files other than the one in      method of resolving a data hazard by
which it is defined.                             retrieving the missing data element from
false sharing When two unrelated shared          internal buffers rather than waiting for it to
variables are located in the same cache block    arrive from programmer-visible registers or
and the full block is exchanged between          memory.
processors even though the processors are        fraction The value, generally between 0 and
accessing different variables.                   1, placed in the fraction field.
field programmable devices (FPD) An              frame pointer A value denoting the
integrated circuit containing combinational      location of the saved registers and local
logic, and possibly memory devices, that are     variables for a given procedure.
configurable by the end user.                    fully associative cache A cache structure in
field programmable gate array (FPGA) A           which a block can be placed in any location
configurable integrated circuit containing       in the cache.
both combinational logic blocks and flip-        fully connected network A network that
flops.                                           connects processor-memory nodes by
fine-grained multithreading A version            supplying a dedicated communication link
of hardware multithreading that implies          between every node.
G-6   Glossary

      fused multiply add A floating-point              imprecise interrupt Also called imprecise
      instruction that performs both a multiply        exception. Interrupts or exceptions in
      and an add, but rounds only once after the       pipelined computers that are not associated
      add.                                             with the exact instruction that was the cause
      gate A device that implements basic logic        of the interrupt or exception.
      functions, such as AND or OR.                    in-order commit A commit in which the
      global miss rate The fraction of references      results of pipelined execution are written
      that miss in all levels of a multilevel cache.   to the programmer visible state in the same
                                                       order that instructions are fetched.
      global pointer The register that is reserved
      to point to the static area.                     input device A mechanism through which
                                                       the computer is fed information, such as a
      guard The first of two extra bits kept on
                                                       microphone.
      the right during intermediate calculations
      of floating-point numbers; used to improve       instruction A command that computer
      rounding accuracy.                               hardware understands and obeys.
      handler Name of a software routine               instruction count The number of
      invoked to “handle” an exception or              instructions executed by the program.
      interrupt.                                       instruction format A form of
      hardware description language A                  representation of an instruction composed
      programming language for describing              of fields of binary numbers.
      hardware, used for generating simulations        instruction latency The inherent execution
      of a hardware design and also as input to        time for an instruction.
      synthesis tools that can generate actual         instruction-level parallelism The
      hardware.                                        parallelism among instructions.
      hardware multithreading Increasing
                                                       instruction mix A measure of the dynamic
      utilization of a processor by switching to
                                                       frequency of instructions across one or
      another thread when one thread is stalled.
                                                       many programs.
      hardware synthesis tools Computer-aided
                                                       instruction set architecture Also called
      design software that can generate a gate-level
                                                       architecture. An abstract interface between
      design based on behavioral descriptions of a
                                                       the hardware and the lowest-level software
      digital system.
                                                       that encompasses all the information
      hexadecimal Numbers in base 16.                  necessary to write a machine language
      high-level programming language A                program that will run correctly, including
      portable language such as C, C++, Java, or       instructions, registers, memory access, I/O,
      Visual Basic that is composed of words and       and so on.
      algebraic notation that can be translated by a   integrated circuit Also called a chip. A
      compiler into assembly language.                 device combining dozens to millions of
      hit rate The fraction of memory accesses         transistors.
      found in a level of the memory hierarchy.        interrupt An exception that comes from
      hit time The time required to access a level     outside of the processor. (Some architectures
      of the memory hierarchy, including the time      use the term interrupt for all exceptions.)
      needed to determine whether the access is a      interrupt handler A piece of code that
      hit or a miss.                                   is run as a result of an exception or an
      hold time The minimum time during                interrupt.
      which the input must be valid after the clock    issue packet The set of instructions that
      edge.                                            issues together in one clock cycle; the packet
      implementation Hardware that obeys the           may be determined statically by the compiler
      architecture abstraction.                        or dynamically by the processor.
                                                                                    Glossary     G-7

issue slots The positions from which             local area network (LAN) A network
instructions could issue in a given clock        designed to carry data within a
cycle; by analogy, these correspond to           geographically confined area, typically
positions at the starting blocks for a sprint.   within a single building.
Java bytecode Instruction from an                local label A label referring to an object
instruction set designed to interpret Java       that can be used only within the file in
programs.                                        which it is defined.
Just In Time compiler (JIT) The name             local miss rate The fraction of references
commonly given to a compiler that operates       to one level of a cache that miss; used in
at runtime, translating the interpreted          multilevel hierarchies.
code segments into the native code of the        lock A synchronization device that allows
computer.                                        access to data to only one processor at a
latch A memory element in which the              time.
output is equal to the value of the stored       lookup tables (LUTs) In a field
state inside the element and the state is        programmable device, the name given to the
changed whenever the appropriate inputs          cells because they consist of a small amount
change and the clock is asserted.                of logic and RAM.
latency (pipeline) The number of stages in       loop unrolling A technique to get more
a pipeline or the number of stages between       performance from loops that access arrays,
two instructions during execution.               in which multiple copies of the loop body
least recently used (LRU) A replacement          are made and instructions from different
scheme in which the block replaced is the        iterations are scheduled together.
one that has been unused for the longest         machine language Binary representation
time.                                            used for communication within a computer
least significant bit The rightmost bit in a     system.
LEGv8 doubleword.                                macro A pattern-matching and
level-sensitive clocking A timing                replacement facility that provides a simple
methodology in which state changes occur         mechanism to name a frequently used
at either high or low clock levels but are not   sequence of instructions.
instantaneous, as such changes are in edge-      magnetic disk Also called hard disk. A
triggered designs.                               form of nonvolatile secondary memory
linker Also called link editor. A systems        composed of rotating platters coated with a
program that combines independently              magnetic recording material. Because they
assembled machine language programs              are rotating mechanical devices, access times
and resolves all undefined labels into an        are about 5 to 20 milliseconds and cost per
executable file.                                 gigabyte in 2012 was $0.05 to $0.10.
liquid crystal display A display technology      main memory Also called primary
using a thin layer of liquid polymers that can   memory. Memory used to hold programs
be used to transmit or block light according     while they are running; typically consists of
to whether a charge is applied.                  DRAM in today’s computers.
load-use data hazard A specific form of          memory The storage area in which
data hazard in which the data being loaded       programs are kept when they are running,
by a load instruction have not yet become        and that contains the data needed by the
available when they are needed by another        running programs.
instruction.                                     memory hierarchy A structure that uses
loader A systems program that places an          multiple levels of memories; as the distance
object program in main memory so that it is      from the processor increases, the size of the
ready to execute.                                memories and the access time both increase.
G-8   Glossary

      message passing Communicating between             multiprocessor A computer system with
      multiple processors by explicitly sending and     at least two processors. This computer is in
      receiving information.                            contrast to a uniprocessor, which has one,
      metastability A situation that occurs if a        and is increasingly hard to find today.
      signal is sampled when it is not stable for       multistage network A network that
      the required setup and hold times, possibly       supplies a small switch at each node.
      causing the sampled value to fall into the        NAND gate An inverted AND gate.
      indeterminate region between a high and
                                                        network bandwidth Informally, the peak
      low value.
                                                        transfer rate of a network; can refer to
      microarchitecture The organization of the         the speed of a single link or the collective
      processor, including the major functional         transfer rate of all links in the network.
      units, their interconnection, and control.
                                                        next-state function A combinational
      million instructions per second (MIPS)            function that, given the inputs and the
      A measurement of program execution                current state, determines the next state of a
      speed based on the number of millions             finite-state machine.
      of instructions. MIPS is computed as the          nonblocking assignment An assignment
      instruction count divided by the product of       that continues after evaluating the right-
      the execution time and 106.                       hand side, assigning the left-hand side the
      MIMD or Multiple Instruction streams,             value only after all right-hand sides are
      Multiple Data streams. A multiprocessor.          evaluated.
      minterms Also called product terms. A set         nonblocking cache A cache that allows the
      of logic inputs joined by conjunction (AND        processor to make references to the cache
      operations); the product terms form the first     while the cache is handling an earlier miss.
      logic stage of the programmable logic array       nonuniform memory access (NUMA) A
      (PLA).                                            type of single address space multiprocessor
      miss penalty The time required to fetch a         in which some memory accesses are much
      block into a level of the memory hierarchy        faster than others depending on which
      from the lower level, including the time          processor asks for which word.
      to access the block, transmit it from one         nonvolatile memory A form of memory that
      level to the other, insert it in the level that   retains data even in the absence of a power
      experienced the miss, and then pass the           source and that is used to store programs
      block to the requestor.                           between runs. A DVD disk is nonvolatile.
      miss rate The fraction of memory accesses         nop An instruction that does no operation
      not found in a level of the memory                to change state.
      hierarchy.                                        NOR A logical bit-by-bit operation with
      most significant bit The leftmost bit in a        two operands that calculates the NOT of the
      LEGv8 doubleword.                                 OR of the two operands. That is, it calculates
      multicore microprocessor A                        a 1 only if there is a 0 in both operands.
      microprocessor containing multiple                NOR gate An inverted OR gate.
      processors (“cores”) in a single integrated       normalized A number in floating-point
      circuit. Virtually all microprocessors today      notation that has no leading 0s.
      in desktops and servers are multicore.
                                                        NOT A logical bit-by-bit operation with
      multilevel cache A memory hierarchy with          one operand that inverts the bits; that is, it
      multiple levels of caches, rather than just a     replaces every 1 with a 0, and every 0 with a 1.
      cache and main memory.                            object oriented language A programming
      multiple issue A scheme whereby multiple          language that is oriented around objects
      instructions are launched in one clock cycle.     rather than actions, or data versus logic.
                                                                                     Glossary     G-9

one’s complement A notation that                  personal mobile devices (PMDs) Small
represents the most negative value by             wireless devices to connect to the Internet;
10…000two and the most positive value             they rely on batteries for power, and
by 01…11two, leaving an equal number of           software is installed by downloading apps.
negatives and positives but ending up with        Conventional examples are smart phones
two zeros, one positive (00…00two) and one        and tablets.
negative (11…11two). The term is also used to     physical address An address in main
mean the inversion of every bit in a pattern:     memory.
0 to 1 and 1 to 0.
                                                  physically addressed cache A cache that is
opcode The field that denotes the operation       addressed by a physical address.
and format of an instruction.
                                                  pipeline stall Also called bubble. A stall
OpenMP An API for shared memory                   initiated in order to resolve a hazard.
multiprocessing in C, C++, or Fortran that        pipelining An implementation technique in
runs on UNIX and Microsoft platforms. It          which multiple instructions are overlapped
includes compiler directives, a library, and      in execution, much like an assembly line.
runtime directives.
                                                  pixel The smallest individual picture
OR A logical bit-by-bit operation with two        element. Screens are composed of hundreds
operands that calculates a 1 if there is a 1 in   of thousands to millions of pixels, organized
either operand.                                   in a matrix.
out-of-order execution A situation in             pop Remove element from stack.
pipelined execution when an instruction
                                                  precise interrupt Also called precise
blocked from executing does not cause the
                                                  exception. An interrupt or exception
following instructions to wait.
                                                  that is always associated with the correct
output device A mechanism that conveys            instruction in pipelined computers.
the result of a computation, such as a display,
                                                  prefetching A technique in which data
to a user or to another computer.
                                                  blocks needed in the future are brought
page fault An event that occurs when              into the cache early by the use of special
an accessed page is not present in main           instructions that specify the address of the
memory.                                           block.
page table The table containing the virtual       procedure A stored subroutine that
to physical address translations in a virtual     performs a specific task based on the
memory system. The table, which is stored         parameters with which it is provided.
in memory, is typically indexed by the            procedure call frame A block of memory
virtual page number; each entry in the            that is used to hold values passed to a
table contains the physical page number for       procedure as arguments, to save registers
that virtual page if the page is currently in     that a procedure may modify but that the
memory.                                           procedure’s caller does not want changed,
parallel processing program A single              and to provide space for variables local to a
program that runs on multiple processors          procedure.
simultaneously.                                   procedure frame Also called activation
PC-relative addressing An addressing              record. The segment of the stack containing
regime in which the address is the sum of         a procedure’s saved registers and local
the program counter (PC) and a constant in        variables.
the instruction.                                  process Includes one or more threads, the
personal computer (PC) A computer                 address space, and the operating system
designed for use by an individual, usually        state. Hence, a process switch usually
incorporating a graphics display, a keyboard,     invokes the operating system, but not a
and a mouse.                                      thread switch.
G-10   Glossary

       program counter (PC) The register                the terms in the logic functions as address
       containing the address of the instruction in     inputs and the outputs as bits in each word
       the program being executed.                      of the memory.
       programmable array logic (PAL) Contains          receive message routine A routine used
       a programmable and-plane followed by a           by a processor in machines with private
       fixed or-plane.                                  memories to accept a message from another
       programmable logic array (PLA) A                 processor.
       structured-logic element composed of             recursive procedures Procedures that
       a set of inputs and corresponding input          call themselves either directly or indirectly
       complements and two stages of logic, the         through a chain of calls.
       first generating product terms of the inputs     reduction A function that processes a data
       and input complements, and the second            structure and returns a single value.
       generating sum terms of the product terms.
       Hence, PLAs implement logic functions as a       reference bit Also called use bit or access
       sum of products.                                 bit. A field that is set whenever a page is
                                                        accessed and that is used to implement LRU
       programmable logic device (PLD) An               or other replacement schemes.
       integrated circuit containing combinational
       logic whose function is configured by the        reg In Verilog, a register.
       end user.                                        register file A state element that consists
       programmable ROM (PROM) A form of                of a set of registers that can be read and
       read-only memory that can be programmed          written by supplying a register number to be
       when a designer knows its contents.              accessed.
       propagation time The time required for           register renaming The renaming of
       an input to a flip-flop to propagate to the      registers by the compiler or hardware to
       outputs of the flip-flop.                        remove antidependences.
       protection A set of mechanisms for               register use convention Also called
       ensuring that multiple processes sharing the     procedure call convention. A software
       processor, memory, or I/O devices cannot         protocol governing the use of registers by
       interfere, intentionally or unintentionally,     procedures.
       with one another by reading or writing each      relocation information The segment of a
       other’s data. These mechanisms also isolate      UNIX object file that identifies instructions
       the operating system from a user process.        and data words that depend on absolute
       pseudoinstruction A common variation of          addresses.
       assembly language instructions often treated     remainder The secondary result of a
       as if it were an instruction in its own right.   division; a number that when added to the
       Pthreads A UNIX API for creating and             product of the quotient and the divisor
       manipulating threads. It is structured as a      produces the dividend.
       library.                                         reorder buffer The buffer that holds results
       push Add element to stack.                       in a dynamically scheduled processor until
       quotient The primary result of a division; a     it is safe to store the results to memory or a
       number that when multiplied by the divisor       register.
       and added to the remainder produces the          reservation station A buffer within a
       dividend.                                        functional unit that holds the operands and
       read-only memory (ROM) A memory                  the operation.
       whose contents are designated at creation        response time Also called execution time.
       time, after which the contents can only be       The total time required for the computer
       read. ROM is used as structured logic to         to complete a task, including disk accesses,
       implement a set of logic functions by using      memory accesses, I/O activities, operating
                                                                                       Glossary   G-11

system overhead, CPU execution time, and         send message routine A routine used
so on.                                           by a processor in machines with private
restartable instruction An instruction that      memories to pass a message to another
can resume execution after an exception is       processor.
resolved without the exceptions affecting the    sensitivity list The list of signals that
result of the instruction.                       specifies when an always block should be
return address A link to the calling site that   re-evaluated.
allows a procedure to return to the proper       separate compilation Splitting a program
address; in LEGv8, it is stored in register LR   across many files, each of which can be
(X30).                                           compiled without knowledge of what is in
                                                 the other files.
rotational latency Also called rotational
delay. The time required for the desired         sequential logic A group of logic elements
sector of a disk to rotate under the read/       that contain memory and hence whose value
write head; usually assumed to be half the       depends on the inputs as well as the current
rotation time.                                   contents of the memory.
round Method to make the intermediate            server A computer used for running
floating-point result fit the floating-point     larger programs for multiple users, often
format; the goal is typically to find the        simultaneously, and typically accessed only
nearest number that can be represented in        via a network.
the format. It is also the name of the second    set-associative cache A cache that has a
of two extra bits kept on the right during       fixed number of locations (at least two)
intermediate floating-point calculations,        where each block can be placed.
which improves rounding accuracy.                setup time The minimum time that the
scientific notation A notation that renders      input to a memory device must be valid
numbers with a single digit to the left of the   before the clock edge.
decimal point.                                   shared memory multiprocessor (SMP) A
secondary memory Nonvolatile memory              parallel processor with a single physical
used to store programs and data between          address space.
runs; typically consists of flash memory in      sign-extend Increases the size of a data item
PMDs and magnetic disks in servers.              by replicating the high-order sign bit of the
sector One of the segments that make up          original data item in the high-order bits of
a track on a magnetic disk; a sector is the      the larger, destination data item.
smallest amount of information that is read      silicon A natural element that is a
or written on a disk.                            semiconductor.
seek The process of positioning a read/write     silicon crystal ingot A rod composed of
head over the proper track on a disk.            a silicon crystal that is between 8 and 12
segmentation A variable-size address             inches in diameter and about 12 to 24 inches
mapping scheme in which an address               long.
consists of two parts: a segment number,         SIMD or Single Instruction stream,
which is mapped to a physical address, and a     Multiple Data streams. The same instruction
segment offset.                                  is applied to many data streams, as in a
selector value Also called control value.        vector processor.
The control signal that is used to select one    simple programmable logic device
of the input values of a multiplexor as the      (SPLD) Programmable logic device, usually
output of the multiplexor.                       containing either a single PAL or PLA.
semiconductor A substance that does not          simultaneous multithreading (SMT) A
conduct electricity well.                        version of multithreading that lowers
G-12   Glossary

       the cost of multithreading by utilizing          state element A memory element, such as a
       the resources needed for multiple issue,         register or a memory.
       dynamically scheduled microarchitecture.         static data The portion of memory that
       single precision A floating-point value          contains data whose size is known to the
       represented in a 32-bit word.                    compiler and whose lifetime is the program’s
       single-cycle implementation Also called          entire execution.
       single clock cycle implementation. An            static multiple issue An approach to
       implementation in which an instruction is        implementing a multiple-issue processor
       executed in one clock cycle. While easy to       where many decisions are made by the
       understand, it is too slow to be practical.      compiler before execution.
       SISD or Single Instruction stream, Single        static random access memory (SRAM) A
       Data stream. A uniprocessor.                     memory where data are stored statically (as
       Software as a Service (SaaS) delivers            in flip-flops) rather than dynamically (as in
       software and data as a service over the          DRAM). SRAMs are faster than DRAMs,
       Internet, usually via a thin program such as     but less dense and more expensive per bit.
       a browser that runs on local client devices,     sticky bit A bit used in rounding in
       instead of binary code that must be installed,   addition to guard and round that is set
       and runs wholly on that device. Examples         whenever there are nonzero bits to the right
       include web search and social networking.        of the round bit.
       source language The high-level language in       stored-program concept The idea that
       which a program is originally written.           instructions and data of many types can
       spatial locality The locality principle          be stored in memory as numbers and thus
       stating that if a data location is referenced,   be easy to change, leading to the stored
       data locations with nearby addresses will        program computer.
       tend to be referenced soon.                      strong scaling Speed-up achieved on a
       speculation An approach whereby                  multiprocessor without increasing the size of
       the compiler or processor guesses the            the problem.
       outcome of an instruction to remove it as a      structural hazard When a planned
       dependence in executing other instructions.      instruction cannot execute in the proper
       split cache A scheme in which a level            clock cycle because the hardware does not
       of the memory hierarchy is composed of           support the combination of instructions that
       two independent caches that operate in           are set to execute.
       parallel with each other, with one handling      structural specification Describes how
       instructions and one handling data.              a digital system is organized in terms of a
       SPMD Single Program, Multiple Data               hierarchical connection of elements.
       streams. The conventional MIMD                   sum of products A form of logical
       programming model, where a single                representation that employs a logical sum
       program runs across all processors.              (OR) of products (terms joined using the
       stack A data structure for spilling registers    AND operator).
       organized as a last-in-first-out queue.          supercomputer A class of computers with
       stack pointer A value denoting the most          the highest performance and cost; they are
       recently allocated address in a stack that       configured as servers and typically cost tens
       shows where registers should be spilled or       to hundreds of millions of dollars.
       where old register values can be found. In       superscalar An advanced pipelining
       LEGv8, it is register SP.                        technique that enables the processor to
       stack segment The portion of memory used         execute more than one instruction per clock
       by a program to hold procedure call frames.      cycle by selecting them during execution.
                                                                                     Glossary     G-13

supervisor mode Also called kernel mode.         secondary storage systems developers started
A mode indicating that a running process is      using the term to mean 1,000,000,000,000
an operating system process.                     (1012) bytes. To reduce confusion, we now
swap space The space on the disk reserved        use the term tebibyte (TiB) for 240 bytes,
for the full virtual memory space of a           defining terabyte (TB) to mean 1012 bytes.
process.                                         (Figure 1.1 shows the full range of decimal
                                                 and binary values and names.)
symbol table A table that matches names of
labels to the addresses of the memory words      text segment The segment of a UNIX object
that instructions occupy.                        file that contains the machine language code
                                                 for routines in the source file.
synchronization The process of
coordinating the behavior of two or more         thread A thread includes the program
processes, which may be running on               counter, the register state, and the stack. It
different processors.                            is a lightweight process; whereas threads
                                                 commonly share a single address space,
synchronizer failure A situation in which a
                                                 processes don’t.
flip-flop enters a metastable state and where
some logic blocks reading the output of the      three Cs model A cache model in which all
flip-flop see a 0 while others see a 1.          cache misses are classified into one of three
                                                 categories: compulsory misses, capacity
synchronous system A memory system
                                                 misses, and conflict misses.
that employs clocks and where data signals
are read only when the clock indicates that      throughput Also called bandwidth.
the signal values are stable.                    Another measure of performance, it is the
                                                 number of tasks completed per unit time.
system call A special instruction that
transfers control from user mode to a            tournament branch predictor A branch
dedicated location in supervisor code space,     predictor with multiple predictions for each
invoking the exception mechanism in the          branch and a selection mechanism that
process.                                         chooses which predictor to enable for a
system CPU time The CPU time spent in            given branch.
the operating system performing tasks on         track One of thousands of concentric
behalf of the program.                           circles that makes up the surface of a
systems software Software that provides          magnetic disk.
services that are commonly useful, including     transistor An on/off switch controlled by
operating systems, compilers, loaders, and       an electric signal.
assemblers.                                      translation-lookaside buffer (TLB) A
tag A field in a table used for a memory         cache that keeps track of recently used
hierarchy that contains the address              address mappings to try to avoid an access
information required to identify whether         to the page table.
the associated block in the hierarchy            truth table From logic, a representation of
corresponds to a requested word.                 a logical operation by listing all the values
task-level parallelism or process-level          of the inputs and then in each case showing
parallelism Utilizing multiple processors        what the resulting outputs should be.
by running independent programs                  underflow (floating-point) A situation
simultaneously.                                  in which a negative exponent becomes too
temporal locality The principle stating that     large to fit in the exponent field.
if a data location is referenced, then it will   uniform memory access (UMA) A
tend to be referenced again soon.                multiprocessor in which latency to any
terabyte (TB) Originally 1,099,511,627,776       word in main memory is about the same no
(240) bytes, although communications and         matter which processor requests the access.
G-14   Glossary

       units in the last place (ulp) The number of          virtual machine A virtual computer that
       bits in error in the least significant bits of the   appears to have nondelayed branches and
       significand between the actual number and            loads and a richer instruction set than the
       the number that can be represented.                  actual hardware.
       unmapped A portion of the address space              virtual memory A technique that uses
       that cannot have page faults.                        main memory as a “cache” for secondary
       unresolved reference A reference that                storage.
       requires more information from an outside            virtually addressed cache A cache that is
       source to be complete.                               accessed with a virtual address rather than a
       use bit Also called reference bit or access          physical address.
       bit. A field that is set whenever a page is          volatile memory Storage, such as DRAM,
       accessed and that is used to implement LRU           that retains data only if it is receiving power.
       or other replacement schemes.                        wafer A slice from a silicon ingot no more
       use latency Number of clock cycles between           than 0.1 inches thick, used to create chips.
       a load instruction and an instruction that           weak scaling Speed-up achieved on a
       can use the result of the load without stalling      multiprocessor while expanding the size of
       the pipeline.                                        the problem proportionally to the increase
       user CPU time The CPU time spent in a                in the number of processors.
       program itself.                                      wide area network (WAN) A network
       valid bit A field in the tables of a memory          extended over hundreds of kilometers that
       hierarchy that indicates that the associated         can span a continent.
       block in the hierarchy contains valid data.          wire In Verilog, specifies a combinational
       vector lane One or more vector functional            signal.
       units and a portion of the vector register file.     word The natural unit of access in a
       Inspired by lanes on highways that increase          computer, usually a group of 32 bits.
       traffic speed, multiple lanes execute vector
       operations simultaneously.                           workload A set of programs run on a
                                                            computer that is either the actual collection
       vectored interrupt An interrupt for which            of applications run by a user or constructed
       the address to which control is transferred is       from real programs to approximate such a
       determined by the cause of the exception.            mix. A typical workload specifies both the
       Verilog One of the two most common                   programs and the relative frequencies.
       hardware description languages.                      write buffer A queue that holds data
       very-large-scale integrated (VLSI)                   while the data are waiting to be written to
       circuit A device containing hundreds of              memory.
       thousands to millions of transistors.                write-back A scheme that handles writes
       very long instruction word (VLIW) A                  by updating values only to the block in the
       style of instruction set architecture that           cache, then writing the modified block to
       launches many operations that are defined to         the lower level of the hierarchy when the
       be independent in a single wide instruction,         block is replaced.
       typically with many separate opcode fields.          write-through A scheme in which writes
       VHDL One of the two most common                      always update both the cache and the
       hardware description languages.                      next lower level of the memory hierarchy,
       virtual address An address that                      ensuring that data are always consistent
       corresponds to a location in virtual space           between the two.
       and is translated by address mapping to a            yield The percentage of good dies from the
       physical address when memory is accessed.            total number of dies on the wafer.
Further Reading
Chapter 1
Barroso, L. and U. Hölzle [2007]. “The case for energy-proportional computing”,
IEEE Computer, December.
A plea to change the nature of computer components so that they use much less power
when lightly utilized.

Bell, C. G. [1996]. Computer Pioneers and Pioneer Computers, ACM and the
Computer Museum, videotapes.
Two videotapes on the history of computing, produced by Gordon and Gwen Bell,
including the following machines and their inventors: Harvard Mark-I, ENIAC,
EDSAC, IAS machine, and many others.

Burks, A.W., H.H. Goldstine, and J. von Neumann [1946]. “Preliminary discussion
of the logical design of an electronic computing instrument,” Report to the U.S.
Army Ordnance Department, p. 1; also appears in Papers of John von Neumann, W.
Aspray and A. Burks (Eds.), MIT Press, Cambridge, MA, and Tomash Publishers,
Los Angeles, 1987, 97–146.
A classic paper explaining computer hardware and software before the first
stored-program computer was built. We quote extensively from it in Chapter 3. It
simultaneously explained computers to the world and was a source of controversy
because the first draft did not give credit to Eckert and Mauchly.

Campbell-Kelly, M. and W. Aspray [1996]. Computer: A History of the Information
Machine, Basic Books, New York.
Two historians chronicle the dramatic story. The New York Times calls it well written
and authoritative.

Ceruzzi, P. F. [1998]. A History of Modern Computing, MIT Press, Cambridge, MA.
Contains a good description of the later history of computing: the integrated circuit
and its impact, personal computers, UNIX, and the Internet.

Curnow, H. J. and B. A. Wichmann [1976]. “A synthetic benchmark”, The Computer
J. 19(1):80.
Describes the first major synthetic benchmark, Whetstone, and how it was created.

Flemming, P. J. and J. J. Wallace [1986]. “How not to lie with statistics: The correct
way to summarize benchmark results”, Commun. ACM 29(3 (March)), 218–221.
Describes some of the underlying principles in using different means to summarize
performance results.
FR-2   Further Reading

       Goldstine, H. H. [1972]. The Computer: From Pascal to von Neumann, Princeton
       University Press, Princeton, NJ.
       A personal view of computing by one of the pioneers who worked with von Neumann.

       Hayes, B. [2007]. “Computing in a parallel universe”, American Scientist, Vol.
       95(November–December), 476–480.
       An overview of the parallel computing challenge written for the layman.

       Hennessy, J. L. and D. A. Patterson [2012]. Chapter 1 of Computer Architecture: A
       Quantitative Approach, fifth edition, Morgan Kaufmann Publishers, Waltham, MA.
       Section 1.5 goes into more detail on power, Section 1.6 contains much more detail on
       the cost of integrated circuits and explains the reasons for the difference between price
       and cost, and Section 1.8 gives more details on evaluating performance.

       Lampson, B.W. [1986]. “Personal distributed computing; The Alto and Ethernet
       software.” In ACM Conference on the History of Personal Workstations (January).

       Thacker, C.R. [1986]. “Personal distributed computing: The Alto and Ethernet
       hardware,” In ACM Conference on the History of Personal Workstations (January).
       These two papers describe the software and hardware of the landmark Alto.

       Metropolis, N., J. Howlett, and G. -C. Rota (Eds.) [1980]. A History of Computing
       in the Twentieth Century, Academic Press, New York.
       A collection of essays that describe the people, software, computers, and laboratories
       involved in the first experimental and commercial computers. Most of the authors
       were personally involved in the projects. An excellent bibliography of early reports
       concludes this interesting book.

       Public Broadcasting System [1992]. The Machine That Changed the World,
       videotapes.
       These five 1-hour programs include rare footage and interviews with pioneers of the
       computer industry.

       Slater, R. [1987]. Portraits in Silicon, MIT Press, Cambridge, MA.
       Short biographies of 31 computer pioneers.
       Stern, N. [1980]. “Who invented the first electronic digital computer?” Annals of
       the History of Computing 2:4 (October), 375–376.
       A historian s perspective on Atanasoff versus Eckert and Mauchly.

       Wilkes, M. V. [1985]. Memoirs of a Computer Pioneer, MIT Press, Cambridge, MA.
       A personal view of computing by one of the pioneers.
                                                                        Further Reading    FR-3

Chapter 2
Bayko, J. [1996]. “Great microprocessors of the past and present,” search for it on
the http://www.cpushack.com/CPU/cpu.html.
A personal view of the history of both representative and unusual microprocessors,
from the Intel 4004 to the Patriot Scientific ShBoom!

Kane, G. and J. Heinrich [1992]. MIPS RISC Architecture, Prentice Hall, Englewood
Cliffs, NJ.
This book describes the MIPS architecture in greater detail than Appendix A.

Levy, H. and R. Eckhouse [1989]. Computer Programming and Architecture: The
VAX, Digital Press, Boston.
This book concentrates on the VAX, but also includes descriptions of the Intel 8086,
IBM 360, and CDC 6600.

Morse, S., B. Ravenal, S. Mazor, and W. Pohlman [1980]. “Intel microprocessors—8080
to 8086”, Computer 13:10 (October).
The architecture history of the Intel from the 4004 to the 8086, according to the people
who participated in the designs.

Wakerly, J. [1989]. Microcomputer Architecture and Programming, Wiley, New York.
The Motorola 6800 is the main focus of the book, but it covers the Intel 8086, Motorola
6809, TI 9900, and Zilog Z8000.

Chapter 3
If you are interested in learning more about floating point, two publications by
David Goldberg [1991, 2002] are good starting points; they abound with pointers
to further reading. Several of the stories told in this section come from Kahan
[1972, 1983]. The latest word on the state of the art in computer arithmetic is often
found in the Proceedings of the latest IEEE-sponsored Symposium on Computer
Arithmetic, held every 2 years; the 16th was held in 2003.

Burks, A.W., H.H. Goldstine, and J. von Neumann [1946]. “Preliminary discussion
of the logical design of an electronic computing instrument,” Report to the U.S.
Army Ordnance Dept., p. 1; also in Papers of John von Neumann, W. Aspray and A.
Burks (Eds.), MIT Press, Cambridge, MA, and Tomash Publishers, Los Angeles,
1987, 97–146.
This classic paper includes arguments against floating-point hardware.
FR-4   Further Reading

       Goldberg, D. [2002]. “Computer arithmetic”. Appendix J of Computer Architecture:
       A Quantitative Approach, fifth edition, J. L. Hennessy and D. A. Patterson, Morgan
       Kaufmann Publishers, Waltham, MA.
       A more advanced introduction to integer and floating-point arithmetic, with emphasis
       on hardware. It covers Sections 3.4–3.6 of this book in just 10 pages, leaving another
       45 pages for advanced topics.
       Goldberg, D. [1991]. “What every computer scientist should know about floating-
       point arithmetic”, ACM Computing Surveys 23(1) 5–48.
       Another good introduction to floating-point arithmetic by the same author, this time
       with emphasis on software.

       Kahan, W. [1972]. “A survey of error-analysis”. Info. Processing 71 (Proc. IFIP
       Congress 71 in Ljubljana), Vol. 2, North-Holland Publishing, Amsterdam, 1214–
       1239
       This survey is a source of stories on the importance of accurate arithmetic.

       Kahan, W. [1983]. “Mathematics written in sand”, Proc. Amer. Stat. Assoc. Joint
       Summer Meetings of 1983, Statistical Computing Section, 12–26.
       The title refers to silicon and is another source of stories illustrating the importance of
       accurate arithmetic.

       Kahan, W. [1990]. “On the advantage of the 8087’s stack,” unpublished course notes,
       Computer Science Division, University of California, Berkeley.
       What the 8087 floating-point architecture could have been.

       Kahan, W. [1997]. Available at http://www.cims.nyu.edu/~dbindel/class/
       cs279/87stack.pdf.
       A collection of memos related to floating point, including “Beastly numbers” (another
       less famous Pentium bug), “Notes on the IEEE floating point arithmetic” (including
       comments on how some features are atrophying), and “The baleful effects of computing
       benchmarks” (on the unhealthy preoccupation on speed versus correctness, accuracy,
       ease of use, flexibility, …).

       Koren, I. [2002]. Computer Arithmetic Algorithms, second edition, A. K. Peters,
       Natick, MA.
       A textbook aimed at seniors and first-year graduate students that explains fundamental
       principles of basic arithmetic, as well as complex operations such as logarithmic and
       trigonometric functions.

       Wilkes, M. V. [1985]. Memoirs of a Computer Pioneer, MIT Press, Cambridge, MA.
       This computer pioneer’s recollections include the derivation of the standard hardware
       for multiply and divide developed by von Neumann.
                                                                          Further Reading     FR-5

Chapter 4
Bhandarkar, D. and D. W. Clark [1991]. “Performance from architecture:
Comparing a RISC and a CISC with similar hardware organizations,” Proc. Fourth
Conf. on Architectural Support for Programming Languages and Operating Systems,
IEEE/ACM (April), Palo Alto, CA, 310–319.
A quantitative comparison of RISC and CISC written by scholars who argued for
CISCs as well as built them; they conclude that MIPS is between 2 and 4 times faster
than a VAX built with similar technology, with a mean of 2.7.

Fisher, J. A. and B. R. Rau [1993]. Journal of Supercomputing (January), Kluwer.
This entire issue is devoted to the topic of exploiting ILP. It contains papers on both the
architecture and software and is a wonderful source for further references.

Hennessy, J. L. and D. A. Patterson [2012]. Computer Architecture: A Quantitative
Approach, fifth edition, Morgan Kaufmann, Waltham, MA.
Chapter 3 and Appendix C go into considerably more detail about pipelined processors
(almost 200 pages), including superscalar processors and VLIW processors. Appendix
G describes Itanium.

Jouppi, N. P. and D. W. Wall [1989]. “Available instruction-level parallelism for
superscalar and superpipelined processors,’’ Proc. Third Conf. on Architectural
Support for Programming Languages and Operating Systems, IEEE/ACM (April),
Boston, 272–82.
A comparison of deeply pipelined (also called superpipelined) and superscalar systems.

Kogge, P. M. [1981]. The Architecture of Pipelined Computers, McGraw-Hill, New
York.
A formal text on pipelined control, with emphasis on underlying principles.

Russell, R. M. [1978]. “The CRAY-1 computer system”, Commun. ACM 21:1
(January), 63–72.
A short summary of a classic computer that uses vectors of operations to remove
pipeline stalls.

Smith, A. and J. Lee [1984]. “Branch prediction strategies and branch target buffer
design”, Computer 17:1 (January), 6–22.
An early survey on branch prediction.

Smith, J. E. and A. R. Plezkun [1988]. “Implementing precise interrupts in pipelined
processors”, IEEE Trans. on Computers 37:5 (May), 562–573.
Covers the difficulties in interrupting pipelined computers.
FR-6   Further Reading

       Thornton, J. E. [1970]. Design of a Computer: The Control Data 6600, Scott,
       Foresman, Glenview, IL.
       A classic book describing a classic computer, considered the first supercomputer.

       Chapter 5
       Cantin, J. F. and M. D. Hill [2001]. “Cache performance for selected SPEC CPU2000
       benchmarks”, SIGARCH Computer Architecture News 29:4 (September), 13–18.
       A reference paper of cache miss rates for many cache sizes for the SPEC2000
       benchmarks.

       Conti, C, D. H. Gibson, and S. H. Pitowsky [1968]. “Structural aspects of the
       System/360 Model 85, part I: General organization”, IBM Systems J. 7:1, 2–14.
       A classic paper that describes the first commercial computer to use a cache and its
       resulting performance.

       Hennessy, J. and D. Patterson [2012]. Chapter 2 and Appendix B in Computer
       Architecture: A Quantitative Approach, fifth edition, Morgan Kaufmann Publishers,
       Waltham, MA.
       For more in-depth coverage of a variety of topics including protection, cache
       performance of out-of-order processors, virtually addressed caches, multilevel
       caches, compiler optimizations, additional latency tolerance mechanisms, and cache
       coherency.

       Kilburn, T., D. B. G Edwards, M. J. Lanigan, and F. H. Sumner [1962]. “One-level
       storage system,” IRE Transactions on Electronic Computers EC-11 (April), 223–
       35. Also appears in D. P. Siewiorek, C G Bell, and A. Newell [1982], Computer
       Structures: Principles and Examples, McGraw-Hill, New York, 135–48.
       This classic paper is the first proposal for virtual memory.

       LaMarca, A. and R. E. Ladner [1996]. “The influence of caches on the performance
       of heaps,” ACM J. of Experimental Algorithmics Vol. 1.
       This paper shows the difference between complexity analysis of an algorithm,
       instruction count performance, and memory hierarchy for four sorting algorithms.

       McCalpin, J.D. [1995]. “STREAM: Sustainable Memory Bandwidth in High
       Performance Computers,” https://www.cs.virginia.edu/stream/.
       A widely used microbenchmark that measures the performance of the memory system
       behind the caches.

       Przybylski, S. A. [1990]. Cache and Memory Hierarchy Design: A Performance-
       Directed Approach, Morgan Kaufmann Publishers, San Francisco.
       A thorough exploration of multilevel memory hierarchies and their performance.
                                                                     Further Reading    FR-7

Ritchie, D. [1984]. “The evolution of the UNIX time-sharing system”, AT& T Bell
Laboratories Technical Journal 1984:1577–1593.
The history of UNIX from one of its inventors.

Ritchie, D. M. and K. Thompson [1978]. “The UNIX time-sharing system”, Bell
System Technical Journal (August), 1991–2019.
A paper describing the most elegant operating system ever invented.

Silberschatz, A., P. Galvin, and G. Grange [2003]. Operating System Concepts, sixth
edition, Addison-Wesley, Reading, MA.
An operating systems textbook with a thorough discussion of virtual memory processes
and process management, and protection issues.

Smith, A. J. [1982]. “Cache memories”, Computing Surveys 14:3 (September),
473–530.
The classic survey paper on caches. This paper defined the terminology for the field
and has served as a reference for many computer designers.

Smith, D. K. and R. C. Alexander [1988]. Fumbling the Future: How Xerox Invented,
Then Ignored, the First Personal Computer, Morrow, New York.
A popular book that explains the role of Xerox PARC in laying the foundation for
today’s computing, but which Xerox did not substantially benefit from.

Tanenbaum, A. [2001]. Modern Operating Systems, second edition, Upper Saddle
River: Prentice Hall, NJ.
An operating system textbook with a good discussion of virtual memory.

Wilkes, M. [1965]. “Slave memories and dynamic storage allocation”, IEEE Trans.
Electronic Computers EC 14(2 (April)), 270–271.
The first classic paper on caches.

Chapter 6
Almasi, G. S. and A. Gottlieb [1989]. Highly Parallel Computing, Benjamin/
Cummings, Redwood City, CA.
A textbook covering parallel computers.

Amdahl, G.M. [1967]. “Validity of the single processor approach to achieving large
scale computing capabilities,” Proc. AFIPS Spring Joint Computer Conf., Atlantic
City, NJ (April), 483–85.
Written in response to the claims of the Illiac IV, this three-page article describes
Amdahl’s law and gives the classic reply to arguments for abandoning the current
form of computing.
FR-8   Further Reading

       Andrews, G. R. [1991]. Concurrent Programming: Principles and Practice,
       Benjamin/Cummings, Redwood City, CA.
       A text that gives the principles of parallel programming.

       Archibald, J. and J.-L. Baer [1986]. “Cache coherence protocols: Evaluation
       using a multiprocessor simulation model”, ACM Trans. on Computer Systems
       4:4 (November), 273–98.
       Classic survey paper of shared-bus cache coherence protocols.

       Arpaci-Dusseau, A., R. Arpaci-Dusseau, D. Culler, J. Hellerstein, and D. Patterson
       [1997]. “High-performance sorting on networks of workstations,” Proc. ACM SIG
       MOD/PODS Conference on Management of Data, Tucson, AZ (May), 12–15.
       How a world record sort was performed on a cluster, including architecture critique
       of the workstation and network interface. By April 1, 1997, they pushed the record to
       8.6 GB in 1 minute and 2.2 seconds to sort 100 MB.

       Asanovic, K., R. Bodik, B. C. Catanzaro, J. J. Gebis, P. Husbands, K. Keutzer, D.
       A. Patterson, W. L. Plishker, J. Shalf, S. W. Williams, and K. A. Yelick [2006]. “The
       landscape of parallel computing research: A view from Berkeley”. Tech. Rep. UCB/
       EECS-2006-183, EECS Department, University of California, Berkeley. (December
       18).
       Nicknamed the “Berkeley View,” this report lays out the landscape of the multicore
       challenge.

       Bailey, D.H., E. Barszcz, J.T. Barton, D.S. Browning, R.L. Carter, L. Dagum,
       R.A. Fatoohi, P.O. Frederickson, T.A. Lasinski, R.S. Schreiber, H.D. Simon, V.
       Venkatakrishnan, and S.K. Weeratunga. [1991]. “The NAS parallel benchmarks—
       summary and preliminary results,” Proceedings of the 1991 ACM/IEEE Conference
       on Supercomputing (August).
       Describes the NAS parallel benchmarks.

       Bell, C. G. [1985]. “Multis: A new class of multiprocessor computers”, Science
       228(April 26), 462–467.
       Distinguishes shared address and nonshared address multiprocessors based on micro-
       processors.
       Bienia, C., S. Kumar, J.P. Singh, and K. Li [2008]. “The PARSEC benchmark suite:
       characterization and architectural implications,” Princeton University Technical
       Report TR-81 1-008 (January).
       Describes the PARSEC parallel benchmarks. Also see http://parsec.cs.princeton.edu/.

       Cooper, B.F., A. Silberstein, E. Tam, R. Ramakrishnan, R. Sears [2010].
       Benchmarking cloud serving systems with YCSB, In: Proceedings of the 1st ACM
       Symposium on Cloud Computing, Indianapolis, Indiana, USA. http://dx.doi.
       org/10.1145/1807128.1807152 (June).
                                                                         Further Reading     FR-9

Presents the “Yahoo! Cloud Serving Benchmark” (YCSB) framework, with the goal
of facilitating performance comparisons of the new generation of cloud data serving
systems.

Culler, D. E., J. P. Singh, and A. Gupta [1998]. Parallel Computer Architecture,
Morgan Kaufmann, San Francisco.
A textbook on parallel computers.

Dongarra, J. J., J. R. Bunch, G. B. Moler, and G. W. Stewart [1979]. LINPACK Users’
Guide, Society for Industrial Mathematics.
The original document describing Linpack, which became a widely used parallel
benchmark.

Falk, H. [1976]. “Reaching for the gigaflop”, IEEE Spectrum 13:10 (October),
65–70.
Chronicles the sad story of the Illiac IV: four times the cost and less than one-tenth the
performance of original goals.

Flynn, M. J. [1966]. “Very high-speed computing systems”, Proc. IEEE 54:12
(December), 1901–09.
Classic article showing SISD/SIMD/MISD/MIMD classifications.

Hennessy, J. and D. Patterson [2012]. “Chapters 5 and Appendices F and I”. In
Computer Architecture: A Quantitative Approach, fifth edition, Morgan Kaufmann
Publishers, Waltham, MA.
A more in-depth coverage of a variety of multiprocessor and cluster topics, including
programs and measurements.

Henning, J. L. [2007]. “SPEC CPU suite growth: an historical perspective”, Computer
Architecture News Vol. 35, no. 1 (March).
Gives the history of SPEC, including the use of SPECrate to measure performance on
independent jobs, which is being used as a parallel benchmark.

Hord, R. M. [1982]. The Illiac-IV, the First Supercomputer, Computer Science Press,
Rockville, MD.
A historical accounting of the Illiac IV project.
Hwang, K. [1993]. Advanced Computer Architecture with Parallel Programming,
McGraw-Hill, New York.
Another textbook covering parallel computers.

Kozyrakis, C. and D. Patterson [2003]. “Scalable vector processors for embedded
systems”, IEEE Micro 23:6 (November–December), 36–45.
Examination of a vector architecture for the MIPS instruction set in media and signal
processing.
FR-10   Further Reading

        Laprie, J.-C. [1985]. “Dependable computing and fault tolerance: Concepts and
        terminology,” 15th Annual Int’l Symposium on Fault-Tolerant Computing FTCS 15,
        Digest of Papers, Ann Arbor, MI (June 19–21) 2–11.
        The paper that introduced standard definitions of dependability, reliability, and
        availability.
        Menabrea, L. F. [1842]. “Sketch of the analytical engine invented by Charles
        Babbage”, Bibliothèque Universelle de Genève (October).
        Certainly the earliest reference on multiprocessors, this mathematician made this
        comment while translating papers on Babbage’s mechanical computer.

        Patterson, D., G. Gibson, and R. Katz [1988]. “A case for redundant arrays of
        inexpensive disks (RAID),” SIGMOD Conference, 109–16.

        Pfister, G. F. [1998]. In Search of Clusters: The Coming Battle in Lowly Parallel
        Computing, second edition, Prentice Hall, Upper Saddle River, NJ.
        An entertaining book that advocates clusters and is critical of NUMA multiprocessors.

        Regnier, G., S. Makineni, R. Illikkal, R. Iyer, D. Minturn, R. Huggahalli, D. Newell,
        L. Cline, and A. Foong [2004]. TCP onloading for data center servers. IEEE
        Computer, 37(11):48–58.
        Describes the work of researchers at Intel Labs, who have experimented with alternative
        solutions that improve the server’s ability to process TCP/IP packets efficiently and at
        very high rates.

        Seitz, C. [1985]. “The Cosmic Cube”, Comm. ACM 28:1 (January), 22–31.
        A tutorial article on a parallel processor connected via a hypertree. The Cosmic Cube
        is the ancestor of the Intel supercomputers.

        Slotnick, D. L. [1982]. “The conception and development of parallel processors—a
        personal memoir”, Annals of the History of Computing 4:1 (January), 20–30.
        Recollections of the beginnings of parallel processing by the architect of the Illiac IV.

        Williams, S., A. Waterman, and D. Patterson [2009]. “Roofline: An insightful visual
        performance model for multicore architectures”, Communications of the ACM,
        52:4 (April), 65–76.
        Williams, S., J. Carter, L. Oliker, J. Shalf, and K. Yelick [2008]. “Lattice Boltzmann
        simulation optimization on leading multicore platforms,” International Parallel &
        Distributed Processing Symposium (IPDPS).
        Paper containing the results of the four multicores for LBMHD.

        Williams, S., L. Oliker, R. Vuduc, J. Shalf, K. Yelick, and J. Demmel [2007].
        “Optimization of sparse matrix-vector multiplication on emerging multicore
        platforms”, Supercomputing (SC)
        Paper containing the results of the four multicores for SPmV.
                                                                    Further Reading    FR-11

Williams, S. [2008]. Autotuning Performance of Multicore Computers, Ph.D.
Dissertation, U.C. Berkeley.
Dissertation containing the roofline model.

Woo, S.C., M. Ohara, E. Torrie, J.P. Singh, and A. Gupta. “The SPLASH-2 programs:
characterization and methodological considerations,” Proceedings of the 22nd
Annual International Symposium on Computer Architecture (ISCA ’95), May, 24–36.
Paper describing the second version of the Stanford parallel benchmarks.

Appendix A
There are a number of good texts on logic design. Here are some you might like to
look into.
Ashenden, P. [2007]. Digital Design: An Embedded Systems Approach Using VHDL/
Verilog, Waltham, MA: Morgan Kaufmann.

Ciletti, M. D. [2002]. Advanced Digital Design with the Verilog HDL, Englewood
Cliff s, NJ: Prentice Hall.
A thorough book on logic design using Verilog.

Harris, D. and S. Harris [2012]. Digital Design and Computer Architecture, Waltham,
MA: Morgan Kaufmann.
A unique and modern approach to digital design using VHDL and SystemVerilog.

Katz, R. H. [2004]. Modern Logic Design, 2nd ed., Reading, MA: Addison-Wesley.
A general text on logic design.

Wakerly, J. F. [2000]. Digital Design: Principles and Practices, 3rd ed., Englewood
Cliff s, NJ: Prentice Hall.
A general text on logic design.

Appendix B
Akeley, K. and T. Jermoluk [1988]. “High-Performance Polygon Rendering,” Proc.
SIGGRAPH 1988 (August), 239–46.

Akeley, K. [1993]. “RealityEngine Graphics.” Proc. SIGGRAPH 1993 (August),
109–16.

Blelloch, G. B. [1990]. “Prefix Sums and Their Applications”. In John H. Reif (Ed.),
Synthesis of Parallel Algorithms, Morgan Kaufmann Publishers, San Francisco.

Blythe, D. [2006]. “The Direct3D 10 System”, ACM Trans. Graphics Vol. 25 no. 3,
(July), 724–734.
FR-12   Further Reading

        Buck, I., T. Foley, D. Horn, J. Sugerman, K. Fatahlian, M. Houston, and P. Hanrahan
        [2004]. “Brook for GPUs: Stream Computing on Graphics Hardware.” Proc.
        SIGGRAPH 2004, 777–86, August. http://doi.acm.org/10.1145/1186562.1015800.

        Elder, G. [2002] “Radeon 9700.” Eurographics/SIGGRAPH Workshop on Graphics
        Hardware, Hot3D Session, www.graphicshardware.org/previous/www_2002/
        presentations/Hot3D-RADEON9700.ppt.

        Fernando, R. and M. J. Kilgard [2003]. The Cg Tutorial: The Definitive Guide to
        Programmable Real-Time Graphics, Addison-Wesley, Reading, MA.

        Fernando, R. (Ed.), [2004]. GPU Gems: Programming Techniques, Tips, and Tricks
        for Real-Time Graphics, Addison-Wesley, Reading, MA. https://developer.nvidia.
        com/gpugems/GPUGems/gpugems_pref01.html.

        Foley, J., A. van Dam, S. Feiner, and J. Hughes [1995]. Computer Graphics: Principles
        and Practice, second edition in C, Addison- Wesley, Reading, MA.

        Hillis, W. D. and G. L. Steele [1986]. “Data parallel algorithms”, Commun. ACM
        29:12 (Dec.), 1170–83. http://doi.acm.org/10.1145/7902.7903.
        IEEE 754R Working Group [2006]. DRAFT Standard for Floating-Point Arithmetic
        P754. www.validlab.com/754R/drafts/archive/2006-10-04.pdf.

        Industrial Light and Magic [2003]. OpenEXR, www.openexr.com.

        Intel Corporation [2007]. Intel 64 and IA-32 Architectures Optimization Reference
        Manual. November. Order Number: 248966-016. Also: http://www.intel.com/
        content/dam/www/public/us/en/documents/manuals/64-ia-32-architectures-
        optimization-manual.pdf.

        Kessenich, J. [2006]. The OpenGL Shading Language, Language Version 1.20, Sept.
        2006. www.opengl.org/documentation/specs/.
        Kirk, D. and D. Voorhies [1990]. “The Rendering Architecture of the DN10000VS.”
        Proc. SIGGRAPH 1990 (August), 299–307.

        Lindholm E., M.J. Kilgard, and H. Moreton [2001]. “A User-Programmable Vertex
        Engine.” Proc. SIGGRAPH 2001 (August), 149–58.
        Lindholm, E., J. Nickolls, S. Oberman, and J. Montrym [2008]. “NVIDIA Tesla:
        A Unified Graphics and Computing Architecture”, IEEE Micro Vol. 28, no. 2
        (March–April), 39–55.

        Microsoft Corporation. Microsoft DirectX Specification, https://msdn.microsoft.
        com/en-us/library/windows/apps/hh452744.aspx.

        Microsoft Corporation. [2003]. Microsoft DirectX 9 Programmable Graphics
        Pipeline, Microsoft Press, Redmond, WA.
                                                                 Further Reading   FR-13

Montrym, J., D. Baum, D. Dignam, and C. Migdal [1997]. “InfiniteReality: A Real-
Time Graphics System.” Proc. SIGGRAPH 1997 (August), 293–301.
Montrym, J. and H. Moreton [2005]. “The GeForce 6800”, IEEE Micro Vol. 25,
no. 2 (March–April), 41–51.
Moore, G. E. [1965]. “Cramming more components onto integrated circuits”,
Electronics, Vol. 38, no. 8 (April 19).
Nguyen, H. ed. [2008]. GPU Gems 3, Addison-Wesley, Reading, MA.
Nickolls, J., I. Buck, M. Garland, and K. Skadron [2008]. “Scalable Parallel
Programming with CUDA”, ACM Queue, Vol. 6, no. 2 (March–April), 40–53.
NVIDIA [2007]. CUDA Zone. http://www.nvidia.com/object/cuda_home_new.
html.
NVIDIA [2007]. CUDA Programming Guide 1.1. https://developer.nvidia.com/
nvidia-gpu-programming-guide.
NVIDIA [2007]. PTX: Parallel Thread Execution ISA version 1.1. www.nvidia.com/
object/io_1195170102263.html.
Nyland, L., M. Harris, and J. Prins [2007]. “Fast N-Body Simulation with CUDA”.
In GPU Gems 3, H. Nguyen (Ed.), Addison-Wesley, Reading, MA.
Oberman, S.F. and M.Y. Siu [2005]. “A High-Performance Area-Efficient
Multifunction Interpolator,” Proc. Seventeenth IEEE Symp. Computer Arithmetic,
272–79.
Pharr, M. (Ed.), [2005]. GPU Gems 2: Programming Techniques for High-Performance
Graphics and General-Purpose Computation, Addison-Wesley, Reading, MA.
Satish, N., M. Harris, and M. Garland [2008]. “Designing Efficient Sorting
Algorithms for Manycore GPUs,” NVIDIA Technical Report NVR-2008-001.
Segal, M. and K. Akeley [2006]. The OpenGL Graphics System: A Specification,
Version 2.1, Dec. 1, 2006. www.opengl.org/documentation/specs/.
Sengupta, S., M. Harris, Y. Zhang, and J.D. Owens [2007]. “Scan Primitives for
GPU Computing.” In Proc. of Graphics Hardware 2007 (August), 97–106.
Volkov, V. and J. Demmel [2008]. “LU, QR and Cholesky Factorizations using
Vector Capabilities of GPUs,” Technical Report No. UCB/EECS-2008-49, 1–11.
http://www.eecs.berkeley.edu/Pubs/TechRpts/2008/EECS-2008-49.pdf.
Williams, S., L. Oliker, R. Vuduc, J. Shalf, K. Yelick, and J. Demmel [2007].
“Optimization of sparse matrix-vector multiplication on emerging multicore
platforms,” In Proc. Supercomputing 2007, November.
FR-14   Further Reading

        Appendix D
        Bhandarkar, D. P. [1995]. Alpha Architecture and Implementations, Newton, MA:
        Digital Press.

        Darcy, J.D., and D. Gay [1996]. “FLECKmarks: Measuring floating point
        performance using a compliant arithmetic benchmark,” CS 252 class project, U.C
        Berkeley (see www.sonic.net/~jddarcy/Research/fleckmrk.pdf)).

        Digital Semiconductor. [1996]. Alpha Architecture Handbook, Version 3, Digital
        Press, Maynard, MA. Order number EC-QD2KB-TE (October).

        Furber, S. B. [1996]. ARM System Architecture, Addison-Wesley, Harlow, England.
        (See http://www.pearsonhighered.com/pearsonhigheredus/educator/product/products_
        detail.page?isbn=9780201675191&forced_logout=forced_logged_out#sthash.
        QX4WfErc).

        Hewlett-Packard [1994]. PA-RISC 2.0 Architecture Reference Manual, 3rd ed.

        Hitachi [1997]. SuperH RISC Engine SH7700 Series Programming Manual. (See
        http://am.renesas.com/products/mpumcu/superh/sh7700/Documentation.jsp).

        IBM. [1994]. The PowerPC Architecture, San Francisco: Morgan Kaufmann.

        Kane, G. [1996]. PA-RISC 2.0 Architecture, Upper Saddle River, NJ: Prentice Hall
        PTR.

        Kane, G. and J. Heinrich [1992]. MIPS RISC Architecture, Englewood Cliffs, NJ:
        Prentice Hall.
        Kissell, K.D. [1997]. MIPS16: High-Density for the Embedded Market.

        Magenheimer, D. J., L. Peters, K. W. Pettis, and D. Zuras [1988]. “Integer
        multiplication and division on the HP precision architecture”, IEEE Trans. on
        Computers 37(8), 980–990.

        MIPS [1997]. MIPS16 Application Specific Extension Product Description.

        Mitsubishi [1996]. Mitsubishi 32-Bit Single Chip Microcomputer M32R Family
        Software Manual (September).

        Muchnick, S. S. [1988]. “Optimizing compilers for SPARC”, Sun Technology 1:3
        (Summer), 64–77.

        Seal, D. Arm Architecture Reference Manual, 2nd ed, Morgan Kaufmann, 2000.

        Silicon Graphics [1996]. MIPS V Instruction Set.
                                                                        Further Reading    FR-15

Sites, R. L., and R. Witek (Eds.) [1995]. Alpha Architecture Reference Manual, 2nd
ed., Newton, MA: Digital Press.

Sloss, A. N., D. Symes, and C. Wright, ARM System Developer’s Guide, San Francisco:
Elsevier Morgan Kaufmann, 2004.
Sun Microsystems [1989]. The SPARC Architectural Manual, Version 8, Part No.
800-1399-09, August 25.

Sweetman, D. See MIPS Run, 2nd ed, Morgan Kaufmann, 2006.

Taylor, G., P. Hilfinger, J. Larus, D. Patterson, and B. Zorn [1986]. “Evaluation of the
SPUR LISP architecture,” Proc. 13th Symposium on Computer Architecture (June),
Tokyo.

Ungar, D., R. Blau, P. Foley, D. Samples, and D. Patterson [1984]. “Architecture
of SOAR: Smalltalk on a RISC,” Proc. 11th Symposium on Computer Architecture
(June), Ann Arbor, MI, 188–97.

Weaver, D. L. and T. Germond [1994]. The SPARC Architectural Manual, Version 9,
Prentice Hall, Englewood Cliffs, NJ.

Weiss, S. and J. E. Smith [1994]. Power and PowerPC, San Francisco: Morgan
Kaufmann.
