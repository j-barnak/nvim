                                                                                Glossary

ambient context                                    billion-dollar mistake
     An area in code where you can declare                The catchy industry term for many type
     types but cannot declare implementations.            systems allowing values such as null to
     Generally used in reference to .d.ts decla‐          be used in places that require a different
     ration files.                                        type. Coined by Tony Hoare in reference
                                                          to the amount of damage it seems to have
      See also declaration file.
                                                          caused.
any                                                     See also strict null checking
      A type that is allowed to be used anywhere
      and can be given anything. any can act       bottom type
      as a top type, in that any type can be            A type that has no possible values—the
      provided to a location of type any. Most          empty set of types. No type is assignable
      of the time, you probably want to use             to the bottom type. TypeScript provides
      unknown for more accurate type safety.            the never keyword to indicate a bottom
                                                        type.
      See also unknown, top type
                                                        See also never.
argument
     Something being provided as an input,         call signature
     used to refer to a value being passed to             Type system description of how a function
     a function. For functions, an argument is            may be called. Includes a list of parame‐
     the value being passed to a call, while a            ters and a return type.
     parameter is the value inside the function.
                                                   camel case
      See also parameter                                A naming convention where the first let‐
                                                        ter of each compound word after the first
assertion, type assertion                               in a name is capitalized, like camelCase.
      An assertion to TypeScript that a value is        The convention for names of members
      of a different type than what TypeScript          in many TypeScript type system con‐
      would otherwise expect.                           structs, including members of classes and
assignable, assignability                               interfaces.
      Whether one type is allowed to be used in
      place of another.

class

class                                                derived interface
        JavaScript syntax sugar around functions           An interface that extends at least one
        that assign to a prototype. TypeScript             other interface, referred to as a base inter‐
        allows working with JavaScript classes.            face. Doing so copies all the members
                                                           of the base interface into the derived
compile                                                    interface.
     Turning source code into another format.
     TypeScript includes a compiler that, in         discriminant
     addition to type checking, turns Type‐                A member of a discriminated union that
     Script source code into JavaScript and/or             has the same name but different type in
     declaration files.                                    each constituent.
        See also transpile                           discriminated union, discriminated type union
                                                           A union of types where a “discriminant”
conditional type
                                                           member exists with the same name but
      A type that resolves to one of two possible
                                                           different value in each constituent type.
      types, based on an existing type.
                                                           Checking the value of the discriminant
const assertion                                            acts as a form of type narrowing.
      as const type assertion shorthand that
                                                     distributivity
      tells TypeScript to use the most literal,             A property of TypeScript’s conditional
      read-only possible form of a value’s type.            types when given union template types:
constituent, constituent type                               their resultant type will be a union of
      One of the types in an intersection or                applying that conditional type to each of
      union type.                                           the constituents (types in the union type).
                                                            ConditionalType<T | U> is the same as
declaration file                                            Conditional<T> | Conditional<U>.
      A file with the .d.ts extension. Declaration
      files create an ambient context, meaning       duck typed
      they can only declare types and cannot               A common phrase for how JavaScript’s
      declare implementations.                             type system behaves. It comes from the
                                                           phrase, “If it looks like a duck and quacks
        See also ambient context                           like a duck, it’s probably a duck.” It means
decorator                                                  that JavaScript allows any value to be
     An experimental JavaScript proposal to                passed anywhere; if an object is asked for
     allow annotating a class or class member              a member that doesn’t exist, the result will
     with a function marked by a @. Doing so               be undefined.
     would have the function be run on that               See also structurally typed
     class or class member upon creation.
                                                     dynamically typed, dynamic typing
DefinitelyTyped                                           A classification of programming lan‐
      The massive repository of community-                guage that does not natively include a
      authored type definitions for packages              type checker. Examples of dynamically
      (DT for short). It contains thousands               typed programming languages include
      of .d.ts definitions along with automation          JavaScript and Ruby.
      around reviewing change proposals and
      publishing updates. Those definitions are      emit, emitted Code
      published as packages under the @types/              The output from a compiler, such as .js
      organization on npm, such as @types/                 files often produced by running tsc. The
      react.                                               TypeScript compiler’s JavaScript and/or

274     |   Glossary
                                                                                                         JSDoc

       declaration file emits can be controlled by   IDE, Integrated Development Environment
       its compiler options.                                Program that provides developer tooling
                                                            on top of a text editor for source code.
enum                                                        IDEs generally come with debuggers, syn‐
       A set of literal values stored in an                 tax highlighting, and plugins that surface
       object with a friendly name for each                 complaints from programming languages
       value. Enums are a rare example of a                 such as type errors. This book uses VS
       TypeScript-specific syntax extension to              Code for its IDE examples, but others
       vanilla JavaScript.                                  include Atom, Emacs, Vim, Visual Studio,
evolving any                                                and WebStorm.
      A special case of implicit any for variables   implementation signature
      who don’t have a type annotation or initial         The final signature declared on an over‐
      value. Their type will be evolved to what‐          loaded function, used for its implementa‐
      ever they are used with.                            tion’s parameters.
       See also implicit any                                 See also function overload
extending an interface                               implicit any
     When an interface declares that it extends            When TypeScript cannot immediately
     another interface. Doing so copies all                deduce the type of a class property, func‐
     members of the original interface into the            tion parameter, or variable, it implicitly
     new one.                                              assumes the type to be any. Implicit any
       See also interface                                  types for class properties and function
                                                           parameters may be configured to be type
function overload, overloaded function                     errors using the noImplicitAny compiler
      A way to describe a function able to be              option.
      called with drastically different sets of
      parameters.                                    interface
                                                           A named set of properties. TypeScript will
generic                                                    know a value that’s declared to be of a
     Allowing a different type to be substi‐               particular interface’s type will have that
     tuted for a construct each time a new                 interface’s declared properties.
     usage of the construct is created. Classes,
     interfaces, and type aliases may be made        interface merging
     generic.                                              A property of interfaces that when mul‐
                                                           tiple interfaces with the same name are
generic type argument, type argument                       declared in the same scope, they combine
     A type provided as the type parameter to a            into one interface instead of causing a
     generic construct.                                    type error about conflicting names. This
                                                           is most commonly used by definition
generic type parameter, type parameter
                                                           authors to augment global interfaces such
     A substituted type for a generic. Generic
                                                           as Window.
     type parameters may be provided with
     different type arguments for each instance      intersection type
     of the construct but will remain consistent           A type that uses the & operator to indi‐
     within that instance.                                 cate it has all the properties of both its
                                                           constituents.
global variable
      A variable that exists in the global scope,    JSDoc
      such as setTimeout in environments such                A standard for /** ... */ block com‐
      as browsers, Deno, and Node.                           ments that describe pieces of code such

                                                                                          Glossary   |    275
literal

          as classes, functions, and variables. Often   null
          used in JavaScript projects to roughly               One of the two primitive types in Java‐
          describe types.                                      Script that represents a lack of value. null
                                                               represents an intentional lack of value,
literal
                                                               while undefined represents a more gen‐
          A value that is known to be a distinct
                                                               eral lack of value.
          instance of a primitive.
                                                               See also undefined.
mapped types
    A type that takes in another type and per‐          optional
    forms some operation on each member of                    A function parameter, class property, or
    that type. In other words, it maps from                   member of an interface or object type
    members of one type into a new set of                     that doesn’t need to be provided. Indi‐
    members.                                                  cated by placing a ? after its name, or
                                                              for function parameters and class proper‐
module                                                        ties, alternately indicated by providing a
    A file with a top-level export or import.
                                                              default value with a =.
    These are generally either files in your
    source code or files in node_modules/               overload signature
    packages.                                                 One of the signatures declared on an
                                                              overloaded function to describe a way it
          See also script.
                                                              may be called.
module resolution                                              See also function overload
    The set of steps used to determine what
    file a module import resolves to. The               override
    TypeScript compiler can have this speci‐                  Redeclaring a property on a subclass-
    fied by its moduleResolution compiler                     derived interface object that already exists
    option.                                                   on the base.

namespace                                               parameter
    An old construct in TypeScript that                      A received input, commonly referring to
    creates a globally available object with                 what a function declares. For functions,
    “exported” contents available to call as                 an argument is the value being passed to a
    members of that object. Namespaces are a                 call, while a parameter is the value inside
    rare example of a TypeScript-specific syn‐               the function.
    tax extension to vanilla JavaScript. These
                                                               See also argument
    days, they’re mostly used in .d.ts declara‐
    tion files.                                         parameter property
                                                             A TypeScript syntax extension for declar‐
never
                                                             ing a property assigned to a member
          The TypeScript type representing the bot‐
                                                             property of the same type at the beginning
          tom type: a type that can have no possible
                                                             of a class constructor.
          values.
          See also bottom type.

non-null assertion
     A shorthand ! that asserts a type is not
     null or undefined.

276       | Glossary
                                                                                                             target

Pascal case                                                    video of Rick Astley’s seminal classic
      A naming convention where the first let‐                 “Never Gonna Give You Up.” I have hid‐
      ter of each compound word in a name                      den several in this book.
      is capitalized, like PascalCase. The con‐
                                                               See also https://oreil.ly/rickroll
      vention for names of many TypeScript
      type system constructs, including gener‐        script
      ics, interfaces, and type aliases.                       Any source code file that is not a module.
project references                                             See also module.
      A feature of TypeScript configuration files
      where they can reference other configura‐       strict mode
      tion files’ projects as dependencies. This             A collection of compiler options that
      allows you to use TypeScript as a build                increase the amount of strictness and
      coordinator to enforce a project depend‐               number of checks the TypeScript type
      ency tree.                                             checker performs. This can be enabled for
                                                             tsc with the --strict flag and in TSCon‐
primitive                                                    figuration files with the "strict": true
      An immutable data type built into Java‐                compilerOption.
      Script that is not an object. They are:
      null, undefined, boolean, string, num           strict null checking
      ber, bigint, and symbol.                               A strict mode for TypeScript where null
                                                             and undefined are no longer allowed to
privacy, private field                                       be provided to types that don’t explicitly
      A feature of JavaScript where class mem‐               include them.
      bers whose names begin with # can only
      be accessed inside that same class.                      See also billion-dollar mistake

readonly                                              structurally typed
     A TypeScript type system feature where                 A type system where any value that hap‐
     adding the readonly keyword in front of a              pens to satisfy a type is allowed to be used
     class or object member indicates it can’t be           as an instance of that type.
     reassigned.                                               See also duck typed
refactor                                              subclass
      A change to code that keeps most or all               A class that extends another class, referred
      of its behaviors the same. The TypeScript             to as a base class. Doing so copies mem‐
      language service is able to perform some              bers of the base class prototype to the
      refactors on source code when asked, such             child class prototype.
      as moving complex lines of code into a
      const variable.                                 target
                                                            The TypeScript compiler option to specify
return type                                                 how far back in syntax support JavaScript
      The type that must be returned by a func‐             code needs to be transpiled, such as "es5
      tion. If multiple return statements exist in          or "es2017". Although target defaults to
      the function with different types, it will be         "es3" for backward compatibility reasons,
      a union of all those possible types. If the           it’s advisable use as new JavaScript syntax
      function cannot possibly return, it will be           as possible per your target platform(s), as
      never.                                                supporting newer JavaScript features in
Rick Roll                                                   older environments necessitates creating
      An internet meme where users are tricked              more JavaScript code.
      into listening to and/or watching a music

                                                                                              Glossary   |     277
Thenable

Thenable                                             type annotation
     A JavaScript object with a .then method               An annotation after a name used to indi‐
     that takes in up to two callback func‐                cate its type. Consists of : and the name of
     tions and returns another Thenable. Most              a type.
     commonly implemented by the built-in
     Promise class, but user-defined classes         type guard
     and objects can work like a Thenable as               A piece of runtime logic that can be
     well.                                                 understood in the type system to only
                                                           allow some logic if a value is a particular
top type                                                   type.
      A type that can represent any possible
      type in a system.                              type narrowing
                                                           When TypeScript can deduce a more spe‐
        See also any, unknown                              cific type for a value inside a block of code
                                                           that is gated on a type guard.
transpile
      A term for compilation that turns source       type predicate
      code from one human-readable program‐                A function with a return type annotated
      ming language into another. TypeScript               to act as a type guard. Type predicate
      includes a compiler that turns .ts/.tsx              functions return a boolean value that
      TypeScript source code into .js files, which         indicates whether a value is a type.
      is sometimes referred to as transpilation.
                                                     type system
        See also compile                                   The set of rules for how a programming
                                                           language understands what types the con‐
TSConfig
                                                           structs in a program may have.
     A JSON configuration file for TypeScript.
     Most commonly named tsconfig.json or            undefined
     in the pattern tsconfig.*.json. Editors such            One of the two primitive types in Java‐
     as VS Code will read from a tsconfig.json               Script that represents a lack of value. null
     file in a directory to determine TypeScript             represents an intentional lack of value,
     language service configuration options.                 while undefined represents a more gen‐
tuple                                                        eral lack of value.
        An array of a fixed size where each ele‐             See also null.
        ment is given an explicit type.
                                                     union
        For example, [number, string | unde                  A type describing a value that can be two
        fined] is a tuple of size two where the              or more possible types. Represented by
        first element is type number and the sec‐            the | pipe between each possible type.
        ond element is type string | undefined.
                                                     unknown
type                                                         The TypeScript concept representing the
        An understanding of what members and                 top type. unknown does not allow arbitrary
        capabilities a value has. These can be               member access without type narrowing.
        primitives such as string, literals such
        as 123, or more complex shapes like func‐            See also any, top type
        tions and objects.

278     |   Glossary
                                                                                                    void

visibility                                          void
       Specifying whether a class member is                A type indicating the lack of returned
       visible to code outside the class. Indica‐          value from a function, represented by the
       ted before the member’s declaration with            void keyword in TypeScript. Functions
       the public, protected, and private key‐             are thought of as returning void if they
       words. Visibility and its keywords predate          have no return statements that return a
       JavaScript’s true # member privacy and              value.
       exist only in the TypeScript type system.
     See also privacy.

                                                                                     Glossary   |   279
